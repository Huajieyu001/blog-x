---
phase: 17
plan: "01"
name: operational-retention-acceptance-closure
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/ops/phase17-acceptance.mjs
  - scripts/ops/phase17-acceptance.test.mjs
autonomous: true
requirements: [OPS-07]
must_haves:
  truths:
    - "One generated local Phase 17 gate proves bounded cleanup of aggregated views and expired/revoked sessions without contacting a host or deleting non-disposable data."
    - "The same gate proves the secondary retention timer is merely a hardened, fixed local command until an operator separately installs and enables it."
    - "Health, backup, scheduler, TLS, and resource anomaly status/notification tests remain part of the explicit secret-free Phase 17 gate."
  artifacts:
    - path: scripts/ops/phase17-acceptance.mjs
      provides: "Deterministic local command manifest for all OPS-07 authority and observable-status suites."
    - path: scripts/ops/phase17-acceptance.test.mjs
      provides: "Regression proof that the acceptance manifest includes retention API and secondary timer contracts."
---

# Phase 17 Plan 01: Operational Retention Acceptance Closure

<objective>
Close OPS-07’s remaining delivery-evidence gap by making the existing bounded retention authority and dormant secondary timer part of the same deterministic, generated local acceptance command as the existing secret-free status and notification checks.
</objective>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Make the generated Phase 17 gate execute retention authority proofs</name>
  <files>scripts/ops/phase17-acceptance.mjs, scripts/ops/phase17-acceptance.test.mjs</files>
  <read_first>scripts/ops/phase17-acceptance.mjs; scripts/ops/phase17-acceptance.test.mjs; apps/api/test/operational-retention.test.ts; apps/api/src/content/operational-retention.ts; scripts/ops/run-secondary-job.test.mjs; deploy/secondary/secondary.test.mjs</read_first>
  <action>First extend the acceptance-manifest test to require an explicit local TypeScript test command for `apps/api/test/operational-retention.test.ts` and the existing secondary deployment/timer static-contract suite, while retaining every current status, notifier, monitor, TLS, and systemd-monitor suite exactly once. Then implement the smallest deterministic command manifest/runner change so `phase17-acceptance.mjs` invokes only those checked-in local suites with fixed arguments and propagates a nonzero child result. Keep database-dependent cases disposable/environment-gated; do not start Compose, systemd, cron, Docker, a server, or a notification provider.</action>
  <acceptance_criteria>
    - The acceptance gate includes the bounded dual-cleanup CLI/service test and the secondary timer installation/activation ordering contract.
    - Existing status-policy, job receipt, notification cooldown, monitor outcome, TLS evidence, and hardened monitor-template checks remain present once each.
    - The runner has no user-provided command, path, network, secret, or shell interpolation surface.
  </acceptance_criteria>
  <verify>node --test scripts/ops/phase17-acceptance.test.mjs; node scripts/ops/phase17-acceptance.mjs; corepack pnpm --filter @blog-x/api typecheck; node scripts/check-boundaries.mjs</verify>
  <done>A single local generated command supplies OPS-07 evidence for retention authority and safe observable operations without performing deployment work.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Audit terminal evidence and preserve production dormancy</name>
  <files>scripts/ops/phase17-acceptance.mjs, scripts/ops/phase17-acceptance.test.mjs</files>
  <read_first>scripts/ops-status.test.mjs; scripts/ops/notify.test.mjs; scripts/ops-monitor.test.mjs; scripts/ops/tls-evidence.test.mjs; ops/systemd/monitoring-systemd.test.mjs; deploy/secondary/secondary.test.mjs; Task 1 diff</read_first>
  <action>Run the narrowed gate against generated local fixtures and inspect its command list/output contract. Add only any missing manifest-level regression needed to prove failures stay visible, aggregate-only, and secret-free, and that no timer or monitor template is installed/enabled by this repository test path. Do not add product UI, backend schema, migrations, server addresses, notification endpoint configuration, or a preview refresh: OPS-07 has no user-visible page change.</action>
  <acceptance_criteria>
    - A failure in retention, backup, scheduler, TLS, health, or resource evidence produces a bounded terminal failure rather than a false pass.
    - Fixture output and receipts expose aggregate state/check identifiers only, never credentials, raw database URLs, article/session identifiers, filesystem authority, or notification authorization.
    - The test path never runs systemd/cron/Docker or contacts a server or webhook.
  </acceptance_criteria>
  <verify>node scripts/ops/phase17-acceptance.mjs; git diff --check; node scripts/check-boundaries.mjs</verify>
  <done>OPS-07 has deterministic local acceptance evidence while all production activation and secret configuration remain operator-only.</done>
</task>
</tasks>

<threat_model asvs="L1" block_on="high">
| ID | Threat | Severity | Blocking mitigation |
|---|---|---|---|
| T17-01-1 | Acceptance runner executes injected commands or contacts an external endpoint | high | Fixed checked-in command manifest, no shell interpolation, and manifest regression tests. |
| T17-01-2 | Cleanup/status evidence leaks credentials, session identifiers, or raw infrastructure facts | high | Preserve aggregate-only fixtures and redaction/strict projection suites in the one gate. |
| T17-01-3 | A timer/monitor is enabled or destructive cleanup runs during local validation | high | Test static contracts only; retain dormant systemd templates and environment-gated disposable database cases. |
</threat_model>

## Verification and success criteria

- Only the generated Phase 17 suite, API typecheck, and boundary check run locally.
- No schema change is planned. Any discovered schema requirement blocks for a repository migration, apply, and schema-verify task; never use `drizzle push`.
- No fixed-preview refresh: this plan changes only local acceptance orchestration, not a user-visible/product surface.

## Artifacts produced

- One deterministic OPS-07 local acceptance entry point that covers retention authority, dormant timer contracts, and observable monitoring safeguards.
