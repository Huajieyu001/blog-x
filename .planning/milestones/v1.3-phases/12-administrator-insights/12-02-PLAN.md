---
phase: 12-administrator-insights
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - apps/web/app/lib/api.ts
  - apps/web/app/lib/admin-analytics.test.ts
  - apps/web/app/admin/page.tsx
  - apps/web/app/admin/analytics/page.tsx
  - apps/web/app/admin/analytics/loading.tsx
  - apps/web/app/admin/_components/AdminAnalytics.tsx
  - apps/web/app/admin/admin.module.css
  - apps/web/e2e/admin-analytics.spec.ts
autonomous: true
requirements:
  - STAT-05
  - ADMN-02
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The authenticated `/admin` page presents 工作台 with content overview, primary creation flow, independent 30-day anonymous-PV summary, complete retained article management, and low-weight maintenance links in that order."
    - "The authenticated `/admin/analytics` page defaults to 30 days and exposes ordinary links for 7/30/90/400 days, total PV, zero-filled daily trend and data table, five sources, and up to eight currently-public top articles."
    - "Visible Chinese copy consistently says the metric is anonymous, best-effort PV—not independent visitors, precise anti-abuse, or billing evidence—and explains coarse sources without storing raw addresses."
    - "Valid zero, invalid range, upstream/contract failure, content-only failure, analytics-only failure, loading, and expired-session outcomes are honest and do not substitute zeros for failure."
    - "At 390, 768, and 1280 CSS pixels the document has no horizontal overflow, required targets are at least 44px, keyboard semantics remain native, and light/dark/system-resolved colors remain legible."
    - "Phase 12 changes only `<main>` content and page-local analytics presentation; Phase 13 still owns the shared admin navigation shell, mobile drawer, active-page semantics, and cross-page component unification."
  artifacts:
    - path: apps/web/app/admin/page.tsx
      provides: "Hierarchical administrator dashboard preserving current article actions"
    - path: apps/web/app/admin/analytics/page.tsx
      provides: "Authenticated SSR analytics page and strict range-state handling"
    - path: apps/web/app/admin/_components/AdminAnalytics.tsx
      provides: "Reusable server-rendered trend, sources, top list, disclosure, and state panels"
    - path: apps/web/app/lib/api.ts
      provides: "No-store, cookie-forwarding, discriminated admin fetch results"
      exports:
        - getAdminPostsResult
        - getAdminAnalytics
    - path: apps/web/e2e/admin-analytics.spec.ts
      provides: "Dashboard, analytics, privacy, responsive, theme, keyboard, and same-origin browser acceptance"
  key_links:
    - from: apps/web/app/admin/page.tsx
      to: apps/web/app/lib/api.ts
      via: "one parallel content read and one 30-day/limit-one analytics read with independent result branches"
      pattern: "Promise\.all"
    - from: apps/web/app/admin/analytics/page.tsx
      to: apps/web/app/lib/api.ts
      via: "validated range is serialized to the authenticated no-store API helper"
      pattern: "getAdminAnalytics"
    - from: apps/web/app/lib/api.ts
      to: packages/contracts/src/analytics.ts
      via: "server fetch parses the whole response through adminAnalyticsResponseSchema"
      pattern: "adminAnalyticsResponseSchema"
    - from: apps/web/app/admin/page.tsx
      to: apps/web/app/admin/_components/ArticleActions.tsx
      via: "all retained posts still render through ArticleActions variant=list"
      pattern: "variant=.?list"
---

<objective>
Turn the existing flat administrator landing page into the specified author workspace and add the complete lightweight anonymous-PV analytics view.

Purpose: Let the administrator understand content state, continue writing, and inspect current-public reading trends without client analytics state or a chart/UI dependency.
Output: Two connected SSR pages, discriminated no-store data helpers, semantic CSS/HTML analytics components, responsive/theme styling, and browser/unit coverage.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-administrator-insights/12-RESEARCH.md
@.planning/phases/12-administrator-insights/12-UI-SPEC.md
@.planning/phases/12-administrator-insights/12-01-SUMMARY.md
@apps/web/app/lib/api.ts
@apps/web/app/admin/page.tsx
@apps/web/app/admin/layout.tsx
@apps/web/app/admin/admin.module.css
@apps/web/app/admin/_components/ArticleActions.tsx
@apps/web/app/public.module.css
@apps/web/app/layout.tsx
@apps/web/next.config.ts

<interfaces>
From Plan 12-01: `AdminAnalytics` is the strict whole-response DTO; `AdminAnalyticsRange` is `7 | 30 | 90 | 400`; `adminAnalyticsResponseSchema` rejects partial, inconsistent, or privacy-expanding payloads.

Existing content DTO: `AdminPost` contains `id`, `title`, `slug`, `status`, `scheduledAt`, and `version`; the current `/admin/posts` response is already ordered by database `updatedAt DESC`, with `version` carrying that timestamp.

Existing auth shell: `AdminLayout` calls `getSessionStatus(cookieHeader)` and redirects to `/login` before rendering `/admin/*`; this file is deliberately not modified in Phase 12.

Existing article behavior: `<ArticleActions post={post} variant="list" />` is the retained list/lifecycle surface and must remain the only row action implementation.

Existing browser data boundary: `apps/web/next.config.ts` rewrites relative `/api/*` to `INTERNAL_API_ORIGIN`; SSR helpers alone forward the cookie and always use `cache: "no-store"`.
</interfaces>

<decisions>
- Per D-01, every analytics number and top row describes only articles currently satisfying shared public visibility. Top rows therefore show current status `已发布`; UI-SPEC clauses proposing hidden/deleted analytics rows are overridden, while the underlying aggregates remain stored for a later republish.
- Dashboard content counts and continue-writing state come from the existing admin post list, not the analytics response, so ADMN-02 does not broaden STAT-05's aggregate contract.
- Keep React Server Components, ordinary links, native `<details>`, semantic tables/lists, CSS custom-property bars, and the current theme authority. Add no chart, UI, icon, font, or client-state dependency.
</decisions>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ship the working dashboard hierarchy with independent content and analytics states</name>
  <files>apps/web/app/lib/api.ts, apps/web/app/lib/admin-analytics.test.ts, apps/web/app/admin/page.tsx, apps/web/app/admin/_components/AdminAnalytics.tsx, apps/web/app/admin/admin.module.css</files>
  <behavior>
    - Test 1: successful admin-post and analytics responses return separate `{kind:"ok", data}` values after strict parsing and no-store cookie-forwarding fetches.
    - Test 2: non-2xx, network failure, or malformed JSON/contract returns `{kind:"upstream_error"}` and never an empty array or zero analytics payload.
    - Test 3: dashboard content success plus analytics failure preserves creation, article management, and maintenance actions while showing only the analytics-card failure.
    - Test 4: content failure plus analytics success preserves the 30-day analytics card and static creation/maintenance actions while never showing fabricated content counts.
  </behavior>
  <action>Start RED in `apps/web/app/lib/admin-analytics.test.ts` by stubbing global fetch and proving exact URL, forwarded Cookie, `cache: "no-store"`, strict parsing, and discriminated failures for both admin posts and analytics. Preserve the existing `getAdminPosts()` API for unrelated pages; add `getAdminPostsResult()` for the dashboard and `getAdminAnalytics(cookieHeader, range, limit)` using only typed inputs and the Plan 12-01 response schema. Rebuild `/admin` as a Server Component that performs at most one content read and one fixed `range=30&limit=1` analytics read in parallel and renders, in document order: BLOG X / 管理 plus `工作台`, primary `新建草稿`, `内容概况`, `继续创作`, `最近 30 天访问`, `文章管理`, then `站点维护`. Derive published/draft/unpublished/scheduled counts from successful retained posts; use the already newest-first draft as continue-edit target and format `version`/`scheduledAt` in `Asia/Shanghai`. Render all retained posts with the unchanged `ArticleActions variant="list"`. Preserve the native POST Markdown export and exact disclosure that analytics is excluded. Implement the full UI-SPEC 8.2 cross-product: content and analytics may independently be populated, zero, or failed; static creation, maintenance, export, and logout access must survive either failure. Add the compact 30-day `DailyTrend` presentation and `查看完整统计 →` link without client fetching.</action>
  <verify>
    <automated>node --import tsx --test --test-reporter=tap apps/web/app/lib/admin-analytics.test.ts &amp;&amp; corepack pnpm --filter @blog-x/web typecheck</automated>
  </verify>
  <done>The administrator can use a real hierarchical dashboard to create/continue writing, inspect content counts and all retained article actions, see a truthful 30-day current-public PV summary, and recover from either upstream independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the complete semantic analytics page and exact Chinese states</name>
  <files>apps/web/app/admin/analytics/page.tsx, apps/web/app/admin/analytics/loading.tsx, apps/web/app/admin/_components/AdminAnalytics.tsx, apps/web/app/admin/admin.module.css, apps/web/e2e/admin-analytics.spec.ts</files>
  <behavior>
    - Test 1: no range defaults to 30; scalar 7/30/90/400 values select the correct URL and `aria-current=page`; invalid, duplicate, signed, decimal, blank, or extra-shaped range never reaches the API and renders the exact invalid-range recovery.
    - Test 2: populated response renders total PV, exact inclusive dates, one semantic daily trend, five fixed sources, at most eight top currently-public articles, and an exact daily table including zero days.
    - Test 3: valid zero renders the zero copy while upstream/network/contract failure renders `role=alert`, current-range retry, and return-to-dashboard actions; these states share no fabricated number or article.
    - Test 4: an expired/absent session is redirected by the existing layout before analytics content is visible.
  </behavior>
  <action>Start the browser acceptance inventory in `apps/web/e2e/admin-analytics.spec.ts` before implementing the page. Add `/admin/analytics` as a Server Component whose async `searchParams` accepts exactly zero or one scalar `range`; default only absence to 30, reject every other shape locally with `时间范围无效`, `请选择 7、30、90 或 400 天。`, `查看 30 天`, and `返回工作台`, and never forward invalid input. For valid input request `limit=8` once through `getAdminAnalytics`. Render the exact UI-SPEC order and Chinese microcopy: header and return link; always-visible neutral privacy `aside`; named range nav; `每日趋势` with total, highest day, figure caption and internal focusable scroller; native `查看每日数据` details/table with daily rows descending; fixed-order source counts, one-decimal percentages and coarse-referrer disclosure; then deterministic top articles. Because D-01 filters all outputs, top article titles always link to their retained admin edit page and the visible current status is `已发布`; do not render the UI-SPEC's obsolete hidden/deleted top-row branch. Make bar elements decorative, keep exact text/table values authoritative, clamp only the server-computed CSS ratio into 0..1, render no fake bars at total zero, and load no image, font, script, chart, or third-party resource. Add `loading.tsx` with `正在读取匿名 PV 趋势…`, `role=status`, `aria-busy=true`, structural neutral blocks, no fake values, and no perpetual shimmer. Use the exact zero/failure copy and actions from UI-SPEC section 8.3. Keep `/admin/layout.tsx`, LogoutButton, global navigation, editor/lifecycle semantics, and API rewrite unchanged for the Phase 13 boundary.</action>
  <verify>
    <automated>corepack pnpm --filter @blog-x/web typecheck &amp;&amp; node --import tsx --test --test-reporter=tap apps/web/app/lib/admin-analytics.test.ts</automated>
  </verify>
  <done>The authenticated analytics page exposes all four ranges and every required metric/state with native accessible SSR semantics, exact privacy language, same-origin data access, and no chart/client dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock responsive, theme, touch, keyboard, and Phase 13 handoff behavior</name>
  <files>apps/web/app/admin/admin.module.css, apps/web/e2e/admin-analytics.spec.ts</files>
  <behavior>
    - Test 1: at 390, 768, and 1280 widths, document scrollWidth is no greater than clientWidth; only the 400-day trend/table containers may scroll internally.
    - Test 2: dashboard is 1-column/2-column/3-column at the specified widths, analytics ranges are 2x2 on phone and one row at 768+, and content order remains DOM order.
    - Test 3: every new primary link/button/summary and the trend scroller computes to at least 44x44 where required and has visible `:focus-visible` styling.
    - Test 4: light, dark, and system-resolved light/dark show distinguishable surface, ink, line, accent, error, current-range, and trend styles without hard-coded light-only surfaces.
    - Test 5: keyboard can activate ranges and details, focus the 400-day horizontal scroller, and use every recovery/creation/maintenance action without positive tabindex or focus theft.
  </behavior>
  <action>Write the viewport, overflow, computed-target, keyboard, theme, reduced-motion, and request-origin assertions before finishing CSS. Extend the existing module with page-local tokens that consume the established root theme values, adding only a local `--danger` pair when needed. Cap content at 1120px; use 28/24/18px horizontal padding at 1280/768/390; preserve 48/36/32px section rhythm and 24/20/16px card padding. Add explicit phone `<=720`, tablet `721..1023`, and desktop `>=1024` layouts: three overview cards and dashboard two-column focus panels only on desktop; 768 stacks the main panels; 390 stacks everything and makes the range nav 2x2. Use `min-width:0`, `overflow-wrap:anywhere`, tabular numerals, and internal-only overflow for the 400-day trend/daily table. Style hover and focus equivalently, use a 2px accent outline with 2-4px offset, remove animation under reduced motion, and do not hide data labels, privacy copy, units, statuses, titles, or range choices. In Playwright, create content only through visible admin controls and same-origin relative requests, generate an actual anonymous public view through the public page when a populated metric is required, and assert the browser never contacts a third party, `INTERNAL_API_ORIGIN`, or either server address. Preserve the existing admin layout/header byte-for-byte so Phase 13 can replace the outer shell while keeping Phase 12 `<main>` content.</action>
  <verify>
    <automated>corepack pnpm --filter @blog-x/web typecheck &amp;&amp; corepack pnpm check:boundaries</automated>
  </verify>
  <done>The two pages meet automated 390/768/1280, 44px, overflow, keyboard, reduced-motion, and light/dark/system contracts without taking ownership of Phase 13 navigation or altering existing workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next SSR -> internal Fastify origin | Server-rendered pages forward only the administrator Cookie to a private no-store read. |
| Strict API payload -> rendered administrator HTML | Aggregate data and current public titles become visible UI. |
| Browser -> same-origin `/api/*` | Visible interactions must not reveal or directly call the internal backend or third parties. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Information Disclosure | SSR analytics helper/page | high | mitigate | Forward Cookie server-side only, use no-store, strict whole-response parsing, existing layout redirect, and same-origin browser assertions. |
| T-12-07 | Tampering | `range` search parameter | medium | mitigate | Validate scalar values before fetch; render exact invalid state and serialize only typed range/limit values. |
| T-12-08 | Information Disclosure | UI privacy language/top rows | medium | mitigate | Permanent exact Chinese disclosure and D-01 currently-public rows; React text escaping and strict DTO prevent hidden metadata/raw request values. |
| T-12-09 | Denial of Service | 400-day trend | low | mitigate | At most 400 server-provided rows, CSS/HTML rendering, internal overflow, no client measurement/chart runtime, and no N+1 requests. |
| T-12-10 | Spoofing | zero/error/loading presentation | medium | mitigate | Discriminated results and separate tested state copy prevent a failed read from masquerading as valid zero data. |
| T-12-SC | Tampering | frontend dependency surface | high | mitigate | Add no dependency or remote asset; boundary and manifest review keep the existing package authority unchanged. |
</threat_model>

<verification>
- Focused helper tests prove exact fetch/no-store/parse/error behavior; Web typecheck passes.
- Plan 12-03 runs the new browser suite in a generated authenticated local fixture with no skips.
- Playwright covers populated, valid-zero, invalid-range, content-only failure, analytics-only failure, expired session, and required Chinese privacy/error copy without conflating results.
- Playwright covers 390/768/1280, document overflow, internal 400-day overflow, >=44px targets, keyboard focus/details/range navigation, light/dark/system-resolved colors, and reduced motion.
- `corepack pnpm check:boundaries` proves no browser-visible internal origin, public server address, or third-party request was added.
- Subjective visual taste remains the milestone-end UAT item; it does not block these deterministic UI/accessibility gates.
</verification>

<success_criteria>
- ADMN-02's five-section dashboard hierarchy and primary writing entry are delivered without losing existing article actions or native export semantics.
- STAT-05's detailed page exposes all four ranges, exact aggregate views, disclosure, no-store access, and honest states.
- D-01 is reflected consistently in all analytics copy, links, and top rows; no current-hidden article is shown or counted.
- Phase 13 retains full ownership of shared admin navigation, active route, mobile menu, focus restoration, cross-page components, and article-action redesign.
- No package, external resource, production operation, server connection, `main` mutation, or release authorization is introduced; local release remains `BLOCKED`.
</success_criteria>

<output>
Create `.planning/phases/12-administrator-insights/12-02-SUMMARY.md` when done.
</output>
