---
phase: 02-complete-reading-experience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/db/schema.ts
  - apps/api/drizzle/0002_phase2_taxonomy.sql
  - apps/api/drizzle/meta/0002_snapshot.json
  - apps/api/drizzle/meta/_journal.json
  - packages/contracts/src/taxonomy.ts
  - packages/contracts/src/admin-posts.ts
  - packages/contracts/src/public-posts.ts
  - packages/contracts/src/index.ts
  - apps/api/src/content/taxonomy-repository.ts
  - apps/api/src/content/taxonomy-service.ts
  - apps/api/src/routes/taxonomy.ts
  - apps/api/src/routes/public-taxonomy.ts
  - apps/api/src/app.ts
  - apps/api/test/taxonomy.test.ts
  - apps/web/app/admin/taxonomy/page.tsx
  - apps/web/app/admin/_components/TaxonomyManager.tsx
  - apps/web/app/_components/PostCard.tsx
  - apps/web/app/_components/Pagination.tsx
  - apps/web/app/categories/page.tsx
  - apps/web/app/categories/[slug]/page.tsx
  - apps/web/app/tags/page.tsx
  - apps/web/app/tags/[slug]/page.tsx
  - apps/web/e2e/taxonomy.spec.ts
autonomous: true
requirements: [READ-04, TAXO-01]
must_haves:
  truths:
    - "D-01: an article has zero or one category and many tags; normalized category/tag slugs are globally unique and database constraints prevent duplicate tag associations."
    - "D-02: category/tag indices expose only terms with a currently published article; their ten-item filtered lists reuse the Phase 1 visibility predicate and deterministic ordering."
    - "D-04: an associated term cannot be deleted; the API reports its association count and never silently detaches or cascades articles."
    - "Category and tag cards expose wrapping navigable metadata without letting chips dominate title or summary; null category and zero tags omit their respective groups."
    - "Taxonomy create/edit/cancel, conflict status, labelled edit controls, focus return, 44px mobile targets and exact Chinese feedback follow the approved UI contract."
  backstops:
    - { statement: "Long CJK/Latin term names remain readable or use an accessible full-name truncation only in compact controls at 375/768/1280px.", verification: backstop }
  artifacts:
    - { path: "apps/api/src/content/taxonomy-service.ts", provides: "transactional category/tag command authority" }
    - { path: "apps/api/src/routes/public-taxonomy.ts", provides: "published-only category/tag discovery APIs" }
    - { path: "apps/web/app/admin/_components/TaxonomyManager.tsx", provides: "accessible compact taxonomy administration UI" }
    - { path: "apps/web/e2e/taxonomy.spec.ts", provides: "real-browser taxonomy discovery and management proof" }
  prohibitions:
    - { requirement_id: TAXO-01, category: safety, status: unverified, flagged: true, statement: "No multiple-category model, bulk deletion, client-side taxonomy authority, or public disclosure of draft/unpublished/deleted associations." }
    - { requirement_id: READ-04, category: safety, status: unverified, flagged: true, statement: "No infinite scroll or browser request to a server IP; all browser calls remain same-origin relative /api." }
---

<objective>
Deliver the first Phase 2 vertical tracer: authenticated taxonomy management and published-only category/tag discovery. It honors D-01, D-02 and D-04 while preserving Phase 1 publishing and same-origin boundaries.
</objective>

## Artifacts this phase produces

- `categories`, `tags`, `article_tags`, `articles.categoryId` and their final FK/unique/index constraints in `apps/api/src/db/schema.ts`.
- `taxonomy.ts` strict schemas; `createTaxonomyRepository`, `createTaxonomyService`, `taxonomyRoutes`, and `publicTaxonomyRoutes`.
- `TaxonomyManager`, category/tag index/detail pages, and `taxonomy.spec.ts`.

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Author RED taxonomy tests and generated relational authority</name>
<read_first>
  - .planning/phases/02-complete-reading-experience/02-CONTEXT.md — D-01, D-02, D-04
  - .planning/phases/02-complete-reading-experience/02-RESEARCH.md — taxonomy model, visibility and collision pitfalls
  - .planning/phases/02-complete-reading-experience/02-PATTERNS.md — repository, contract and migration patterns
  - apps/api/src/db/schema.ts, apps/api/src/content/public-repository.ts, apps/api/src/routes/admin-posts.ts
</read_first>
<action>Write the API test file first as RED source assertions/expected-failure cases; do not execute a database-backed suite before activation. Then add categories/tags/article_tags and nullable article category FK with database-owned unique slugs, composite association uniqueness, restrictive FKs and public-filter indexes. Generate (never hand-edit historical) Drizzle SQL/snapshot/journal. Create strict allowlisted taxonomy and extended article DTO contracts, repository/service and guarded Fastify routes. Use one transaction for association count/delete; all unsafe routes require the existing Fastify session guard and exact Origin. Public joins must select only DTO fields, deduplicate tags deliberately, and reuse the fixed published predicate and ten-item stable pagination.</action>
<acceptance_criteria>
  - Unauthenticated/wrong-Origin mutations fail; duplicate slugs/tag joins conflict; an associated delete is 409 and retains every association.
  - Published-only term indexes/lists exclude hidden-state relationships; unknown slug is 404 while an existing zero-public term returns a valid empty page.
  - Public/admin schemas reject internal columns and unknown fields.
</acceptance_criteria>
<verify>rg -n "taxonomy|associated delete|published-only" apps/api/test/taxonomy.test.ts</verify>
</task>
<task type="auto" gate="blocking" tdd="false">
<name>Task 2: [BLOCKING] activate and inspect the taxonomy schema before UI/API dependent verification</name>
<read_first>
  - apps/api/drizzle.config.ts
  - apps/api/src/app.ts — advisory-lock migration and schema verification flow
  - .planning/phases/01-local-publishing-slice/01-VERIFICATION.md — isolated local verification policy
</read_first>
<action>Against only a runner-owned/local database, execute the canonical generated migration flow, then schema verification. Inspect that existing article rows remain readable and new FK/unique/index/table expectations exist. Do not use runtime push, reset an existing database, contact either cloud server, or continue to browser work if activation fails.</action>
<acceptance_criteria>
  - `corepack pnpm db:migrate` converges under the existing advisory lock and preserves existing articles.
  - `corepack pnpm db:schema:verify` validates all taxonomy structures and no untracked production push exists.
</acceptance_criteria>
<verify>DATABASE_URL=postgres://blog_x@127.0.0.1:5432/blog_x_verify_0201 corepack pnpm db:migrate && DATABASE_URL=postgres://blog_x@127.0.0.1:5432/blog_x_verify_0201 corepack pnpm db:schema:verify</verify>
</task>
<task type="auto" tdd="true">
<name>Task 3: Run GREEN migrated-database taxonomy API tests</name>
<read_first><item>apps/api/test/taxonomy.test.ts</item><item>apps/api/src/routes/taxonomy.ts</item><item>apps/api/src/routes/public-taxonomy.ts</item></read_first>
<action>With the Task 2 isolated migrated database active, execute the RED taxonomy suite as a real Fastify/PostgreSQL test. Fix only implementation defects exposed by the test; retain the final database constraints and do not substitute mocks for constraint/public-visibility cases.</action>
<acceptance_criteria><item>Authentication/Origin, duplicate slug/join, associated-delete conflict, hidden-state leakage, malformed input and deterministic filtered-pagination cases pass against the migrated schema.</item><item>The command names the exact current test file and uses the migrated local database.</item></acceptance_criteria>
<verify>DATABASE_URL=postgres://blog_x@127.0.0.1:5432/blog_x_verify_0201 AUTH_TEST_DATABASE_URL=postgres://blog_x@127.0.0.1:5432/blog_x_verify_0201 corepack pnpm exec tsx --test apps/api/test/taxonomy.test.ts</verify>
</task>
<task type="auto" tdd="true">
<name>Task 4: Deliver taxonomy administration and public paginated discovery</name>
<read_first>
  - .planning/phases/02-complete-reading-experience/02-UI-SPEC.md — taxonomy, chips, copy and accessibility contracts
  - apps/web/app/admin/_components/ArticleEditor.tsx, apps/web/app/_components/PostCard.tsx, apps/web/app/_components/Pagination.tsx
  - packages/contracts/src/taxonomy.ts
</read_first>
<action>Add compact admin categories/tags management and article category select/tag checkbox assignment using strict contracts and relative `/api`. Add public category/tag indices and filtered pages, preserving query-aware pagination. Extend cards with optional category and wrapping tags. Implement exact edit/cancel/status/focus semantics and documented empty/404 distinction; do not add search, dashboard chrome, or client data ownership.</action>
<acceptance_criteria>
  - Browser creates/edits a term, sees prefilling, focus restoration and exact status/error copy; associated delete remains blocked.
  - Visitor navigates category/tag links, sees only published cards and filter-preserving pagination; unknown term is true 404 and valid empty term is normal empty state.
</acceptance_criteria>
<verify>corepack pnpm exec playwright test apps/web/e2e/taxonomy.spec.ts --workers=1</verify>
</task>
</tasks>

<threat_model>
ASVS Level 1; block_on: high.

| Threat | STRIDE | ASVS control area | Severity | Mitigation owner/task | Automated verification |
|---|---|---|---:|---|---|
| Taxonomy authorization/CSRF | Spoofing/Tampering | V4 access control, V3 session handling | high | Task 1 routes | Task 3 migrated API auth/Origin cases. |
| Slug/association collision or race | Tampering | V5 validation, V8 data integrity | high | Task 1 schema/service | Task 3 unique/FK/transaction cases. |
| Public hidden-state leakage | Information disclosure | V5 validation, V8 data integrity | high | Task 1 public repository/routes | Task 3 all-lifecycle-state DTO cases. |
| Long identifiers/layout overflow | Denial of service | V1 architecture | medium | Task 4 web UI | Task 4 Playwright responsive check. |
</threat_model>

<verification>API taxonomy suite, migration/schema activation, and one-worker Playwright taxonomy journey pass locally.</verification>
<success_criteria>READ-04 and TAXO-01 are a useful end-to-end slice with no taxonomy data leak or silent association loss.</success_criteria>
