---
phase: 02-complete-reading-experience
plan: "04"
type: execute
wave: 4
depends_on: [02-01, 02-03]
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - apps/api/src/db/schema.ts
  - apps/api/drizzle/0004_phase2_media.sql
  - apps/api/drizzle/meta/0004_snapshot.json
  - apps/api/drizzle/meta/_journal.json
  - packages/contracts/src/media.ts
  - packages/contracts/src/admin-posts.ts
  - packages/contracts/src/index.ts
  - apps/api/src/media/storage.ts
  - apps/api/src/media/processor.ts
  - apps/api/src/content/media-service.ts
  - apps/api/src/routes/media.ts
  - apps/api/src/app.ts
  - apps/api/src/content/markdown.ts
  - apps/api/test/media.test.ts
  - apps/web/app/admin/_components/MediaPanel.tsx
  - apps/web/app/admin/_components/ArticleEditor.tsx
  - apps/web/app/_components/ArticleBody.tsx
  - apps/web/app/public.module.css
  - apps/web/e2e/media.spec.ts
autonomous: true
requirements: [MEDIA-01]
must_haves:
  truths:
    - "D-09: only JPEG/PNG/WebP up to 5 MiB pass declared-type, signature, decode, static/animation, pixel/dimension and bounded-resource validation; SVG, GIF/polyglots and decompression bombs fail."
    - "D-10: a protected source plus metadata-stripped, orientation-corrected static derivative is recorded; derivatives downsize longest edge to 2400px without upscaling."
    - "D-11: immutable random IDs and DB lookup expose only same-origin `/media/<id>` derivatives; no user filename, source key or filesystem path reaches URLs/DTOs."
    - "D-12: upload/select/insert is reliable but no permanent media deletion or garbage collection UI exists."
    - "Purposeful alt is required before insertion/cover selection unless explicit decorative opt-in clears it; source stays in the editor after failed processing and no failed URL is inserted."
  backstops:
    - { statement: "A nonstandard orientation/dimension derivative renders within the reading width without layout shift at 375/768/1280px.", verification: backstop }
  artifacts:
    - { path: "apps/api/src/media/storage.ts", provides: "movable API-owned source/derivative storage interface and local adapter" }
    - { path: "apps/api/src/routes/media.ts", provides: "guarded upload and derivative-only same-origin stream endpoint" }
    - { path: "apps/web/app/admin/_components/MediaPanel.tsx", provides: "accessible upload/alt/insert/cover controls" }
    - { path: "apps/web/e2e/media.spec.ts", provides: "browser media lifecycle evidence" }
  prohibitions:
    - { requirement_id: MEDIA-01, category: safety, status: unverified, flagged: true, statement: "No SVG, GIF/animation, data/file URL, raw filename URL, public source asset, CDN, Next image optimizer, client decoder, or server-IP media path." }
    - { requirement_id: MEDIA-01, category: safety, status: unverified, flagged: true, statement: "No permanent media delete or unreferenced-media cleanup in Phase 2." }
---

<objective>Deliver validated image upload through same-origin reading and Markdown insertion, honoring D-09 through D-12 while retaining the server renderer as URL authority.</objective>

## Artifacts this phase produces

- `media` schema/migration and strict public/admin media symbols; `Storage`, `LocalMediaStorage`, `processMedia`, `MediaService`, and `mediaRoutes`.
- `MediaPanel`, Article Editor media/cover integration, and `media.spec.ts`.
- Post-approval dependency artifacts: `apps/api/package.json` and `pnpm-lock.yaml` for the approved exact multipart/processor packages.

<tasks>
<task type="checkpoint:human-verify">
<name>Task 1: Verify the exact Fastify multipart and image-processing dependencies before installation</name>
<read_first><item>02-RESEARCH.md — media package legitimacy policy</item><item>package.json and pnpm-lock.yaml</item></read_first>
<action>Do not install a package automatically. Present the verified Fastify-5-compatible multipart parser and image processor candidates, their official package metadata/version compatibility and native deployment footprint for explicit approval. Resume only with approved exact package names.</action>
<acceptance_criteria><item>User approves the exact packages after legitimacy review.</item><item>No dependency or lockfile changes occur before approval.</item></acceptance_criteria>
<verify>npm view @fastify/multipart version peerDependencies && npm view sharp version engines</verify>
</task>
<task type="auto" tdd="true">
<name>Task 2: Author RED hostile-media tests and create media authority</name>
<read_first><item>02-CONTEXT.md — D-09..D-12</item><item>02-RESEARCH.md — processing/storage flow and threat register</item><item>02-PATTERNS.md — stream reply and admin guard patterns</item><item>apps/api/src/app.ts, schema.ts, content/markdown.ts</item></read_first>
<action>Only after Task 1 approval, add the exact approved dependencies and lockfile entries. Write the hostile-upload test file first as RED source assertions/expected-failure cases; do not execute database-backed media tests before activation. Add media records with random public IDs, protected source/derivative keys, detected MIME/dimensions and article cover reference; generate the Drizzle migration. Implement a configured non-served local-root adapter with exact-key atomic writes/cleanup, bounded decode/conversion concurrency and derivative-only DB-backed stream reads. Enforce multipart/file-count/raw-size limits, magic/decode/static/pixel/dimension checks, orientation correction, metadata removal and no-upscale 2400px derivative. Guard upload with session/Origin; stream `/media/:id` only by valid UUID DB lookup with known MIME, `nosniff` and immutable cache headers. Extend the Markdown sanitizer narrowly for `/media/<UUID>` only.</action>
<acceptance_criteria><item>All malformed, mismatched, oversize, animated, pixel-bomb, traversal and DB/processing cleanup cases reject without leaked paths/source or orphaned exact assets.</item><item>Valid JPEG/PNG/WebP gives a metadata-free static derivative and strict URL/size DTO; unknown media is 404.</item><item>Existing articles survive the additive migration and external safe image policy remains narrow.</item></acceptance_criteria>
<verify>rg -n "polyglot|pixel|traversal|derivative" apps/api/test/media.test.ts</verify>
</task>
<task type="auto" gate="blocking" tdd="false">
<name>Task 3: [BLOCKING] activate and verify media schema before using media behavior</name>
<read_first><item>apps/api/drizzle.config.ts</item><item>apps/api/src/app.ts</item><item>02-PATTERNS.md — migration and media-stream guidance</item></read_first>
<action>Apply only the generated migration to a runner-owned/local DB and run schema verification; inspect preservation of existing article rows and media FK/index constraints. Do not proceed to the browser journey on failure.</action>
<acceptance_criteria><item>Migration is retry-safe under the existing lock.</item><item>Schema verification detects the media table/constraints and retained articles.</item></acceptance_criteria>
<verify>DATABASE_URL=postgres://blog_x@127.0.0.1:5432/blog_x_verify_0204 corepack pnpm db:migrate && DATABASE_URL=postgres://blog_x@127.0.0.1:5432/blog_x_verify_0204 corepack pnpm db:schema:verify</verify>
</task>
<task type="auto" tdd="true">
<name>Task 4: Run GREEN migrated-database media API tests</name>
<read_first><item>apps/api/test/media.test.ts</item><item>apps/api/src/routes/media.ts</item><item>apps/api/src/media/storage.ts</item></read_first>
<action>After Task 3 completes, execute the exact hostile-media API suite against its isolated migrated database and generated media root. Fix only defects the suite exposes. Verify cleanup names are exact/generated and do not replace database/storage evidence with mocks.</action>
<acceptance_criteria><item>Valid and invalid uploads, source secrecy, derivative stream, sanitizer path, processing/DB cleanup and traversal cases pass against activated schema/storage.</item><item>The live API command follows—not precedes—the migration/schema gate.</item></acceptance_criteria>
<verify>DATABASE_URL=postgres://blog_x@127.0.0.1:5432/blog_x_verify_0204 AUTH_TEST_DATABASE_URL=postgres://blog_x@127.0.0.1:5432/blog_x_verify_0204 corepack pnpm exec tsx --test apps/api/test/media.test.ts</verify>
</task>
<task type="auto" tdd="true">
<name>Task 5: Add accessible upload, alt semantics, insertion and cover selection</name>
<read_first><item>02-UI-SPEC.md — media controls, exact copy and accessibility</item><item>apps/web/app/admin/_components/ArticleEditor.tsx</item><item>packages/contracts/src/media.ts</item></read_first>
<action>Add the specified labelled input/status/alt/decorative/insert/cover panel using relative `/api`, no storage tokens and no browser image validation authority. Preserve unsaved Markdown and cursor position. Disable duplicate upload/insert while pending; prevent non-decorative use without trimmed alt, keep alternate choice editable on failure, and reserve image aspect ratio with recorded dimensions in public article rendering.</action>
<acceptance_criteria><item>Browser uploads valid images, sees same-origin derivative, inserts exactly the documented Markdown and can persist cover alt/decorative metadata.</item><item>Missing alt and invalid/oversize files show exact copy with no URL insertion; decorative is the only empty-alt path.</item></acceptance_criteria>
<verify>corepack pnpm exec playwright test apps/web/e2e/media.spec.ts --workers=1</verify>
</task>
</tasks>
<threat_model>
ASVS Level 1; block_on: high.

| Threat | STRIDE | ASVS control area | Severity | Mitigation owner/task | Automated verification |
|---|---|---|---:|---|---|
| Polyglot/decompression/path traversal | Tampering/Denial of service | V5 validation, V12 files/resources | high | Task 2 processor/storage | Task 4 magic/decode/pixel/traversal cases. |
| Source/EXIF disclosure | Information disclosure | V9 communications, V12 file handling | high | Task 2 media route/DTO | Task 4 derivative-only/metadata/log cases. |
| Stored unsafe media URL | Tampering | V5 validation | high | Task 2 markdown policy | Task 4 sanitizer `/media/<UUID>` cases. |
| Image CPU/memory exhaustion | Denial of service | V12 files/resources | high | Task 2 bounded processor | Task 4 byte/pixel/concurrency cases. |
</threat_model>
<verification>Approved dependencies, API hostile-media suite, migration gate and one-worker media browser proof pass locally.</verification>
<success_criteria>MEDIA-01 is a safe, same-origin vertical slice without a destructive media lifecycle.</success_criteria>
