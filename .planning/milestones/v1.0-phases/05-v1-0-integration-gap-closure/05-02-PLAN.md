---
phase: 05-v1-0-integration-gap-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/backup/content-verifier.mjs
  - scripts/backup/manifest.mjs
  - scripts/backup/backup.test.mjs
  - scripts/backup/production/policy.mjs
  - scripts/backup/production/source-authority.mjs
  - scripts/backup/production/collector.mjs
  - scripts/backup/production/encryption.mjs
  - scripts/backup/production/transport.mjs
  - scripts/backup/production/mounted-directory.mjs
  - scripts/backup/production/retention.mjs
  - scripts/backup/production/results.mjs
  - scripts/backup/production/adapter.mjs
  - scripts/backup/production-pipeline.mjs
  - scripts/backup/production.test.mjs
  - ops/backup-policy.names.json
  - ops/systemd/blog-x-backup.service
  - ops/systemd/blog-x-backup.timer
  - scripts/fixtures/prohibitions/production-backup-incomplete-or-unsafe.json
  - scripts/fixtures/prohibitions/production-backup-safe.json
autonomous: true
requirements:
  - OPS-03
  - OPS-05
must_haves:
  truths:
    - "OPS-03: the scheduled production pipeline first creates a complete database/portable-Markdown/media/config-image-migration backup set under exact production-source authority, verifies it, then performs authenticated encryption, atomic mounted-directory transfer, receipt binding, catalog-safe retention, redacted result, and alert-outcome recording."
    - "The complete-set byte/shape verifier is split from root authority: existing `verifyBackupSet()` and all rehearsal path/project/create/restore validators retain exactly their Phase 4 public behavior, while production calls the shared read-only verifier only after its separate source-root validator passes."
    - "Rehearsal roots are rejected by production authority, production-source roots are rejected by rehearsal authority, and an exact generated production-source fixture is accepted only by the production validator plus shared content verifier."
    - "The concrete production collector owns restrictive staging and atomic finalization; it runs only fixed allowlisted local PostgreSQL dump and unchanged portable-export operations, copies only API-owned source/derivative media, builds sanitized config/image/migration inventory, hashes every member, writes manifest then COMPLETE, verifies, and renames without accepting policy commands or arbitrary data roots."
    - "The concrete mounted-directory provider accepts no URL, generic command, remote shell, SDK, or dynamic provider; it verifies an exact non-symlink mount root and identity sentinel, atomically writes authenticated ciphertext/receipt/catalog/result files, fsyncs before rename, and fails closed when the mount is absent, wrong, broad, or unverified."
    - "Retention begins only after receipt digest equality, touches only catalog-verified encrypted objects below the exact mount prefix, and preserves the configured minimum known-good complete objects; listing, identity, receipt, or catalog ambiguity deletes nothing."
    - "Generated fakes exist only for deterministic stage fault injection and are explicitly excluded from backup/release evidence; the concrete mounted provider itself is exercised against a generated mount fixture without network."
    - "The dormant daily persistent systemd unit invokes one production pipeline CLI that collects then adapts when its strict untracked service/source/database/media/config/image/key/mount/alert authority exists, and fails closed otherwise; installation, enablement, real mount identity, live schedule, off-host fact, and alert delivery remain future OPS-05 evidence."
  artifacts:
    - path: scripts/backup/content-verifier.mjs
      provides: authority-parameterized read-only complete-set byte and semantic validation
      exports:
        - verifyCompleteBackupSetContents
    - path: scripts/backup/production/source-authority.mjs
      provides: narrow production complete-set source-root validation separate from rehearsal
      exports:
        - validateProductionBackupSource
        - verifyProductionBackupSource
    - path: scripts/backup/production/collector.mjs
      provides: fixed-authority restrictive production complete-set collection and atomic finalization
      exports:
        - collectProductionBackupSet
        - createProductionInventory
    - path: scripts/backup/production/encryption.mjs
      provides: streaming authenticated encryption with canonical bound metadata
      exports:
        - canonicalBackupAad
        - encryptBackupPayload
    - path: scripts/backup/production/mounted-directory.mjs
      provides: concrete no-network mounted-directory transfer, receipt, catalog, and atomic filesystem provider
      exports:
        - validateMountedDestination
        - createMountedDirectoryTransport
    - path: scripts/backup/production/adapter.mjs
      provides: fail-closed production source-to-encryption/receipt/retention/result/alert orchestration
      exports:
        - runProductionBackup
    - path: scripts/backup/production/results.mjs
      provides: strict redacted append-only production result and alert-outcome schemas
      exports:
        - productionBackupResultSchema
        - redactProductionBackupResult
    - path: scripts/backup/production.test.mjs
      provides: concrete collector-to-generated-mount journey, cross-mode rejection, and fake-only fault injection
    - path: scripts/backup/production-pipeline.mjs
      provides: scheduled collect-verify-encrypt-transfer-retain-result-alert CLI
    - path: ops/systemd/blog-x-backup.service
      provides: hardened dormant production-runnable mounted-backup oneshot template
  key_links:
    - from: scripts/backup/manifest.mjs
      to: scripts/backup/content-verifier.mjs
      via: existing verifyBackupSet keeps its signature and rehearsal validator, then delegates only read-only content inspection
      pattern: "verifyBackupSet|verifyCompleteBackupSetContents"
    - from: scripts/backup/production/source-authority.mjs
      to: scripts/backup/content-verifier.mjs
      via: production validates exact service-owned/generated-test source authority before shared read-only inspection
      pattern: "validateProductionBackupSource|verifyCompleteBackupSetContents"
    - from: scripts/backup/production/collector.mjs
      to: scripts/backup/content-verifier.mjs
      via: fixed local authorities fill restrictive staging, manifest/hash/COMPLETE is written, atomic final root is read-back verified before handoff
      pattern: "collectProductionBackupSet|verifyProductionBackupSource|COMPLETE|rename"
    - from: scripts/backup/production/adapter.mjs
      to: scripts/backup/production/mounted-directory.mjs
      via: concrete provider receives authenticated ciphertext only after production source and encryption validation, then returns digest-bound receipt/catalog state
      pattern: "createMountedDirectoryTransport|receipt"
    - from: scripts/backup/production/adapter.mjs
      to: scripts/backup/production/retention.mjs
      via: verified mounted receipt/catalog are mandatory before exact-prefix minimum-known-good retention
      pattern: "applySafeRetention|minimumKnownGood"
    - from: scripts/backup/production-pipeline.mjs
      to: scripts/backup/production/adapter.mjs
      via: pipeline passes only the newly collected and verified final production set to authenticated mounted adapter
      pattern: "collectProductionBackupSet|runProductionBackup"
    - from: ops/systemd/blog-x-backup.service
      to: scripts/backup/production-pipeline.mjs
      via: least-privileged daily oneshot invokes fixed collect-then-adapt CLI after untracked source/data/key/mount/alert conditions
      pattern: "production-pipeline.mjs|ConditionPathIsMountPoint|UMask"
  prohibitions:
    - statement: "Production backup must not reuse rehearsal root authority, accept incomplete/plaintext input, treat generated fakes as evidence, or call an unverified mount/receipt/catalog/alert outcome successful."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/backup/production.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/production-backup-incomplete-or-unsafe.json
      check_clean_fixture: scripts/fixtures/prohibitions/production-backup-safe.json
    - statement: "Mounted transport and retention must not accept URLs, arbitrary commands, remote shells, symlink/broad/wrong-identity roots, pre-receipt deletion, out-of-prefix objects, or deletion of the minimum known-good set."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/backup/production.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/production-backup-incomplete-or-unsafe.json
      check_clean_fixture: scripts/fixtures/prohibitions/production-backup-safe.json
    - statement: "Production collection must not reuse `blogxverify_*`, accept an existing/local/production-like test target, execute a policy-supplied command, read arbitrary data roots, omit any database/export/media/config-image-migration authority, or finalize an incomplete/tampered set."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/backup/production.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/production-backup-incomplete-or-unsafe.json
      check_clean_fixture: scripts/fixtures/prohibitions/production-backup-safe.json
---

<objective>
As an operator, I want the scheduled production job to create a complete set and then encrypt, mount-transfer, receipt, retain, record, and alert it, so that no manual pre-existing set, rehearsal authority, or fake can masquerade as a daily backup.

Purpose: Close the entire executable production path in G2/INT-02/FLOW-08—collect through mounted receipt—while preserving Phase 4 rehearsal and keeping live activation as future release evidence.
Output: Shared verifier, production source collector/authority, mounted adapter, pipeline CLI/service, authenticated receipt/retention/result contracts, tests, and required fixtures.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/05-v1-0-integration-gap-closure/05-RESEARCH.md
@.planning/phases/05-v1-0-integration-gap-closure/05-PATTERNS.md
@.planning/phases/05-v1-0-integration-gap-closure/05-01-SUMMARY.md
@.planning/phases/04-secure-operations-and-release-gate/04-02-PLAN.md
@.planning/phases/04-secure-operations-and-release-gate/04-02-SUMMARY.md
@.planning/phases/04-secure-operations-and-release-gate/04-VERIFICATION.md

<interfaces>
- `verifyBackupSet(root)` keeps its exact public signature, Phase 4 generated staging/final root validation, result shape, and tests. Extract only the read-only content walk into `verifyCompleteBackupSetContents(root, rootValidator)`; callers cannot omit the validator.
- Production source authority is an immediate canonical set-ID child of an exact configured service-owned base (production) or exact generated `blog-x-production-source-*` base (test). It rejects the workspace, `/`, temp root, links, rehearsal prefixes, active restore authority, and unresolved paths.
- The collector has a closed operation inventory, not a generic runner: fixed PostgreSQL custom dump; fixed API portable-export entrypoint preserving strict v1; exact API media source/derivative enumeration; and an allowlisted name-only config/image/migration inventory. Policy supplies authorities/paths/IDs, never executable argv, SQL, URL, shell, or arbitrary file lists.
- Tests inject the process/data runner only at the narrow fixed-operation seam and use an exact generated `blogxprodverify_*` Compose/database/media namespace; real CLI uses the fixed shipped operations. Neither may target `blogxverify_*`, `blogxlocal`, developer databases/volumes, or existing production-like namespaces.
- The concrete provider is local filesystem code for an already provisioned mounted directory. Production validation requires `ConditionPathIsMountPoint`/mount inspection plus an exact strict identity sentinel matching the closed profile ID. Tests inject mount inspection over an exact generated fixture but execute the real copy/fsync/rename/receipt/catalog/retention code.
- The test fake implements the same narrow interface solely to fault each stage. Its result includes `scope: generated-fake` and cannot parse as production/release evidence. Concrete generated-mount results use `scope: generated-mounted-fixture`, also not live off-host proof.
- Tracked files contain names/classes only. Untracked policy supplies exact source base, mount root/profile, service-owned encryption-key authority, retention choice, result location, and alert-outcome sink. No URL/command/provider SDK/credential value is tracked.
</interfaces>
</context>

## Artifacts this plan produces

- `verifyCompleteBackupSetContents(root, rootValidator)` with mandatory authority injection and two wrappers that cannot accept each other's roots.
- Concrete mounted-directory ciphertext/receipt/catalog/result operations, tested without network on a generated mount fixture.
- Fake transport only for failure injection, explicitly non-evidentiary.
- A production pipeline CLI/service that creates a new verified set then mounts/transfers it when strict untracked collector/key/mount/alert inputs exist, but performs no provisioning, mounting, networking, systemd activation, or live alert delivery.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Split content verification from mutually exclusive source authorities</name>
  <files>scripts/backup/content-verifier.mjs, scripts/backup/manifest.mjs, scripts/backup/backup.test.mjs, scripts/backup/production/policy.mjs, scripts/backup/production/source-authority.mjs, scripts/backup/production.test.mjs</files>
  <read_first>
    <item>`scripts/backup/manifest.mjs#verifyBackupSet` current authority selection, link walk, manifest/COMPLETE hashing, portable parse, inventory and media equality.</item>
    <item>`scripts/backup/paths.mjs` exact generated rehearsal roots and cleanup functions, which must not gain a production exception.</item>
    <item>`backup.test.mjs` existing accepted/tampered/extra/link cases and 05-01 migration-count 7 inventory.</item>
  </read_first>
  <action>
    Start RED with three roots containing equivalent complete bytes: exact rehearsal, exact generated production-source, and unsafe broad/symlink. Extract all read-only file enumeration, COMPLETE/manifest binding, member hash/size, portable v1, inventory, and media consistency into `verifyCompleteBackupSetContents(root, rootValidator)`. The validator argument is mandatory and executes before any read; no default or boolean production switch exists. Keep `verifyBackupSet(root)` as the same exported wrapper using only its existing `validateFinalBackupRoot`/`validateBackupStaging` behavior.

    Add strict `validateProductionBackupSource` and `verifyProductionBackupSource`: production accepts only an immediate valid set-ID child of the exact untracked service source base with no links and restrictive ownership/mode; tests accept only an exact `mkdtemp` `blog-x-production-source-*` fixture through an injected test authority. Explicitly prove production rejects rehearsal roots, rehearsal rejects generated/real-shaped production roots, both reject broad/link/traversal/workspace roots, and the generated production source passes the shared content checks including migration inventory count 7.
  </action>
  <acceptance_criteria>
    <criterion>`verifyBackupSet()` signature/result and all prior accepted/rejected rehearsal cases remain unchanged.</criterion>
    <criterion>Production and rehearsal wrappers reject each other's roots before member reads; there is no mode flag or relaxed regex.</criterion>
    <criterion>An exact generated production source with complete valid bytes passes only production authority plus the shared content verifier; tamper/extra/missing/link cases fail identically.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/backup/backup.test.mjs scripts/backup/production.test.mjs scripts/backup/restore.test.mjs</automated>
  </verify>
  <done>Complete-set semantics are reusable without letting either rehearsal or production inherit the other's path/destructive authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collect and atomically finalize every production content authority</name>
  <files>scripts/backup/production/policy.mjs, scripts/backup/production/source-authority.mjs, scripts/backup/production/collector.mjs, scripts/backup/content-verifier.mjs, scripts/backup/production.test.mjs</files>
  <read_first>
    <item>`scripts/backup/create.mjs` Phase 4 staging/manifest/COMPLETE/rename ordering, but not its `blogxverify_*` policy or Compose authority.</item>
    <item>`apps/api/src/ops/portable-export.ts`, media storage keys, Compose service ownership, and config/image/migration inventory format at count 7.</item>
    <item>Production source final/staging validators from Task 1 and the strict shared content verifier.</item>
  </read_first>
  <action>
    Implement `collectProductionBackupSet(policy, deps)` with exact production source base, fresh canonical set ID, exclusive `.incomplete-<token>` staging at 0700 and payloads at 0600. Its dependency surface exposes named operations only: `dumpPostgresCustom`, `writePortableExportV1`, `copyApiMedia`, `readAllowlistedInventory`, clock, and filesystem. The shipped implementation maps those to fixed local executables/entrypoints and exact data roots; no policy field can specify a command, argv, SQL, URL, shell, module, or arbitrary file list.

    Collect a PostgreSQL custom dump, unchanged strict portable export v1, every database-inventoried source and derivative media byte from the exact API-owned media root, and sanitized name-only config/image/migration inventory including migration count/fingerprint and immutable image digests. Cross-check media IDs/paths/hashes against database/export inventory. Generate manifest hashes, write manifest, write manifest-bound COMPLETE last, call production source verification, fsync, and atomically rename to the exact final set. On collector/process/read/hash/verify/rename failure, never publish final or replace a known-good set; retain only a validated restrictive incomplete diagnostic root.

    Tests use injected named runners against an exact generated `blogxprodverify_*` project/database/media namespace or pure generated authority fixture, prove each authority byte is present, run twice/parallel/interrupted, and fault database/export/media/config/image/migration/manifest/COMPLETE/finalization stages. Explicitly reject rehearsal `blogxverify_*`, `blogxlocal`, existing developer/production-like targets, missing/extra media, arbitrary command fields, and broad/link data roots.
  </action>
  <acceptance_criteria>
    <criterion>A fresh generated production-shaped authority produces one complete set containing database dump, unchanged portable export, all source/derivative media, and sanitized config/image/migration inventory with exact hashes.</criterion>
    <criterion>COMPLETE is last and manifest-bound; final rename happens only after production verification; every injected failure preserves prior known-good sets and publishes no final.</criterion>
    <criterion>Only fixed named local operations execute, with redacted output and no policy command/URL/shell/credential; all rehearsal/existing/production-like test authority is rejected.</criterion>
    <criterion>Parallel/interrupted collection isolates exact staging/final roots and cleanup never crosses source authority.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/backup/production.test.mjs scripts/backup/backup.test.mjs</automated>
  </verify>
  <done>The scheduled path can create and verify its own complete production-format set from all four durable authorities without borrowing rehearsal collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute authenticated transfer and retention through the concrete mounted provider</name>
  <files>scripts/backup/production/encryption.mjs, scripts/backup/production/transport.mjs, scripts/backup/production/mounted-directory.mjs, scripts/backup/production/retention.mjs, scripts/backup/production/results.mjs, scripts/backup/production/adapter.mjs, scripts/backup/production.test.mjs</files>
  <read_first>
    <item>The new production source result and Phase 4 manifest actual-byte hashing; ciphertext must bind those exact facts.</item>
    <item>`restore.test.mjs` no-mutation-before-preflight, exact paths, links, interruption, and parallel isolation patterns.</item>
    <item>05-RESEARCH authenticated metadata, receipt-before-retention, minimum known-good, redaction, and alert outcome.</item>
  </read_first>
  <action>
    Implement the actual provider journey: production-source preflight; canonical member envelope; fresh in-memory data key/nonce from strict service-owned key authority; streaming AES-256-GCM; AAD binding format/version, set ID, source manifest SHA-256, creation time, retention policy ID, and destination profile ID; concrete mounted transfer to an exclusive restrictive `.incomplete` sibling; file fsync, ciphertext digest, receipt JSON binding local/remote digest and AAD digest, directory fsync, then atomic rename. Plain source members must never appear beneath the mount.

    `validateMountedDestination` must reject absent/not-a-mount, wrong identity sentinel/profile, root/workspace/temp/rehearsal/source overlap, links, unexpected ownership/mode, and any policy URL/command. Catalog only exact encrypted-object/receipt/result names below the fixed profile prefix. Run retention only after receipt verification, preserve at least `minimumKnownGood`, and delete nothing on list/parse/hash/identity ambiguity. Record a strict redacted result and an append-only alert outcome (`recorded` or `unconfirmed`); an unconfirmed outcome is nonzero. Exercise the real provider over a generated mount fixture with injected mount inspection. Keep `createGeneratedFakeTransport` only for encryption/transfer/receipt/catalog/delete/result/alert fault injection and mark every fake result non-evidentiary.
  </action>
  <acceptance_criteria>
    <criterion>The concrete generated-mount run produces only authenticated ciphertext, receipt, verified catalog/retention, redacted result, and alert-outcome files through atomic restrictive writes; no plaintext member appears.</criterion>
    <criterion>Receipt mismatch, wrong mount identity, catalog/list ambiguity, retention fault, result fault, or alert failure exits nonzero and preserves every prior known-good object.</criterion>
    <criterion>Parallel/interrupted concrete and fake runs use distinct exact prefixes; no incomplete object becomes catalog-good and cleanup cannot cross authority.</criterion>
    <criterion>Fake outcomes are rejected by the production result/release evidence parser and are never described as mounted/off-host success.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/backup/production.test.mjs &amp;&amp; node --test scripts/backup/backup.test.mjs scripts/backup/restore.test.mjs</automated>
  </verify>
  <done>The shipped no-network provider performs the complete encrypted filesystem transfer/receipt/retention/result path, while tests retain a separately marked fake for deterministic failures only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire the scheduled collect-then-adapt pipeline without claiming live activation</name>
  <files>scripts/backup/production-pipeline.mjs, scripts/backup/production/collector.mjs, scripts/backup/production/adapter.mjs, scripts/backup/production.test.mjs, ops/backup-policy.names.json, ops/systemd/blog-x-backup.service, ops/systemd/blog-x-backup.timer, scripts/fixtures/prohibitions/production-backup-incomplete-or-unsafe.json, scripts/fixtures/prohibitions/production-backup-safe.json</files>
  <read_first>
    <item>Current service/timer and name-only policy; replace its verifier-only creator without creating a server installation action.</item>
    <item>Concrete collector and mounted provider policy/source/data/mount/key/result/alert prerequisites and exact redacted result contract.</item>
    <item>Existing `GSD_PROHIB_SUBJECT` fail-first/clean control and Phase 4 boundary terminology.</item>
  </read_first>
  <action>
    Make `scripts/backup/production-pipeline.mjs` strict-parse the untracked policy, validate collector service/database/media/config/image/migration authority and empty exact source staging authority, create a fresh complete set through `collectProductionBackupSet`, read-back verify it, validate the mounted destination/key/alert authority, then invoke `runProductionBackup` with that exact final root. It exits zero only after collection, encryption, receipt, retention, result, and alert outcome all succeed; absent/wrong authority fails redacted and closed. It accepts no pre-existing-set shortcut, transport selector, URL, generic command, remote shell, or fake mode.

    Point the dormant oneshot service only at this pipeline CLI. Require untracked policy/key/source/database/media/config/image/alert paths and `ConditionPathIsMountPoint` for the configured mount, retain non-root identity, `UMask=0077`, no-new-privileges, private temp, strict system/home protection, filesystem write allowlists, and bounded logs; timer remains daily/Persistent. Tests inspect text and invoke the whole pipeline only with generated isolated production-shaped source plus generated mount fixtures—never install/enable/start systemd. Upgrade the tracked names contract without values. Make the unsafe descriptor RED and clean collect-then-mounted descriptor GREEN, and pin rehearsal cross-mode tests.
  </action>
  <acceptance_criteria>
    <criterion>The real pipeline creates a new complete set and completes against exact generated source/data/mount/key/alert fixtures; it fails closed when any collection or adapter prerequisite disappears or identity changes.</criterion>
    <criterion>The unit invokes only production-pipeline.mjs, requires collector/mount/path conditions, and contains no verifier collector, pre-existing-set shortcut, fake selector, URL, remote command, credential, or automatic installation/enablement.</criterion>
    <criterion>The known-bad `production-backup-incomplete-or-unsafe` is fail-first; the clean mounted descriptor passes; prior rehearsal backup/restore suites remain green.</criterion>
    <criterion>Docs/evidence wording deferred to 05-03 distinguishes executable adapter proof from unresolved real mount, schedule, destination, off-host, and alert-delivery facts.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/backup/production.test.mjs scripts/backup/backup.test.mjs scripts/backup/restore.test.mjs</automated>
  </verify>
  <done>A hardened dormant daily service can collect then run the mounted adapter when separately configured, yet no local result activates it or proves a real off-host mount/schedule/alert.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high. Any unmitigated high-severity row blocks this plan.

| Threat | Severity | Required mitigation/evidence |
|---|---|---|
| Shared content verifier bypasses root authority | High | Mandatory validator argument before reads; mutually exclusive wrappers; no default/mode switch; cross-mode tests. |
| Rehearsal gains persistent/destructive production authority | High | Existing public wrapper/validators unchanged; separate production source/mount modules and cleanup rules. |
| Scheduled job encrypts a stale/manual/incomplete set instead of collecting | High | Pipeline creates a fresh set through fixed collector, verifies COMPLETE/content, and hands that exact final root directly to adapter; no shortcut. |
| Policy injects a command or arbitrary data source | High | Closed named collector operations, fixed shipped argv/entrypoints, exact data roots, strict unknown-field rejection, injected tests only. |
| Plaintext, incomplete, or unauthenticated backup accepted | High | COMPLETE/source verification first; AES-GCM AAD; fsync/atomic rename; receipt digest equality before catalog/success. |
| Ordinary directory mistaken for off-host mount | High | Mountpoint inspection plus exact identity sentinel/profile; generated fixture explicitly scoped non-live; release remains BLOCKED. |
| Retention deletes last good or unrelated object | High | Exact mount prefix/catalog/receipt checks, minimum-known-good invariant, no deletion on ambiguity/fault. |
| Fake becomes evidence | High | Explicit generated-fake scope rejected by result/release schemas and full-gate/audit boundary checks. |
| Secret/destination/topology leaks | High | Untracked restrictive policy/key, redacted IDs/digests only, no URL/command, 05-03 boundary scan. |
| Symlink/traversal/interruption corrupts mounted data | High | lstat/realpath, exclusive incomplete files, fsync/rename, exact prefix, parallel/interruption tests. |

Production constraint: do not contact either server or any remote service; do not provision/mount a destination, install/enable/start systemd, or deliver a live alert. Concrete generated-mount execution is local filesystem proof only. Real mount/off-host identity and active daily result remain future evidence; release stays `BLOCKED`.
</threat_model>

<verification>

- [ ] Shared content verification passes only after the correct mutually exclusive root validator.
- [ ] Concrete collector creates database/export/media/config-image-migration set with restrictive atomic COMPLETE finalization and fixed operations only.
- [ ] Concrete generated-mount success and all fault stages execute with non-vacuous TAP; fake scope is never evidence.
- [ ] Existing rehearsal backup/restore behavior and cleanup authority remain unchanged.
- [ ] Hardened CLI/service are production-runnable with exact untracked prerequisites and fail closed without them.
- [ ] No network, provider SDK, URL, command hook, mount action, systemd activation, live alert, or production claim occurs.

</verification>

<success_criteria>

- The verifier-only root blocker is removed without weakening rehearsal: production has its own collector, exact source authority, and shared read-only content semantics.
- The scheduled pipeline actually creates a complete all-authority set, then a concrete no-network mounted provider performs encrypted transfer, receipt, catalog, safe retention, result, and alert outcome.
- Generated fake support is fault-only and cannot satisfy production/release evidence.
- OPS-03 has executable adapter code; OPS-05 still blocks until real mount/off-host/schedule/alert and other live evidence exist.
- G2, INT-02, FLOW-08, and Roadmap criterion 2 are honestly closed at the implementation-contract level, never as a live-production claim.

</success_criteria>
