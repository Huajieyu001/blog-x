---
phase: 05-v1-0-integration-gap-closure
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - scripts/release-gate/schema.mjs
  - scripts/release-gate/bundle.mjs
  - scripts/release-gate/validate.mjs
  - scripts/release-gate.test.mjs
  - scripts/release-gate.mjs
  - ops/release-evidence.blocked.json
  - scripts/fixtures/prohibitions/release-sequence-circular.json
  - scripts/fixtures/prohibitions/release-sequence-linear.json
  - scripts/phase5-receipt.mjs
  - scripts/phase5-receipt.test.mjs
  - scripts/fixtures/prohibitions/premature-phase5-audit.json
  - scripts/fixtures/prohibitions/phase5-receipt-clean.json
  - ops/phase5-full-gate-receipt.json
  - scripts/check-boundaries.mjs
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - docs/RELEASE-GATE.md
  - docs/ROLLBACK.md
  - docs/OPERATIONS.md
  - README.md
  - apps/api/test/backup-restore.test.ts
  - apps/web/e2e/phase4-restore.spec.ts
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - OPS-01
  - OPS-03
  - OPS-05
must_haves:
  truths:
    - "OPS-05: strict version-2 evidence has separate pure evaluators: complete current pre-release evidence returns `PRE_RELEASE_READY` without post-release evidence, while post verification requires and byte-binds the exact valid predecessor decision plus deployed artifact digest, HTTPS-domain smoke, and explicit continue/rollback decision."
    - "Pre-release backup evidence accepts only a live concrete collect-then-mounted production result with complete database/export/media/config set and restore linkage, authenticated encryption, receipt, safe retention, redacted result, and confirmed alert evidence; generated pipeline/mount/fake scope can never satisfy readiness."
    - "The canonical tracked release evidence is strict v2, locator-free, and `BLOCKED`; tracked `READY`, `PRE_RELEASE_READY`, or `POST_RELEASE_VERIFIED` and any deploy/transition/unfreeze/contact capability fail deterministic tests."
    - "`--phase5-full` extends Phase 1-4 and directly runs an actual isolated generated production-shaped Compose/database/media collector through complete-set verification, authenticated generated-mounted receipt/retention/result/alert, plus browser/lifecycle/export/restore, rehearsal, fake faults, release, boundaries, interruption/parallel isolation, cleanup, and terminal BLOCKED."
    - "A strict redacted Phase 5 receipt is atomically written only after every required suite passes and terminal canonical BLOCKED succeeds; failures preserve the prior valid receipt or create none."
    - "The receipt binds a clean committed implementation revision, exact command/mode, exact suite manifest and source/result digests, local/generated-production-pipeline/generated-mounted-fixture/fake-fault scope, timestamps, and canonical BLOCKED evidence/decision digest without secrets, addresses, or live claims."
    - "Tasks 1-3 implementation must be committed before the final gate runs; receipt, milestone audit, and subsequent verification evidence belong to a later evidence/documentation commit and must never be folded back into an implementation commit."
    - "The milestone audit can become passed only after it verifies and cites the receipt SHA-256; premature/missing/mismatched/stale receipt, dirty/uncommitted implementation, or audit time before receipt completion fails boundary/verification checks."
  artifacts:
    - path: scripts/release-gate/schema.mjs
      provides: strict version-2 separate pre-release and post-release evidence schemas
      exports:
        - releaseEvidenceSchema
        - preReleaseEvidenceSchema
        - postReleaseEvidenceSchema
        - evidenceReferenceSchema
    - path: scripts/release-gate/validate.mjs
      provides: pure predecessor-bound pre and post evaluators
      exports:
        - evaluatePreReleaseReadiness
        - evaluatePostReleaseVerification
        - validateReleaseEvidence
        - formatReleaseDecision
    - path: scripts/phase5-receipt.mjs
      provides: strict redacted atomic full-gate receipt creation and verification
      exports:
        - phase5ReceiptSchema
        - writePhase5ReceiptAtomic
        - verifyPhase5Receipt
        - hashPhase5Receipt
    - path: ops/phase5-full-gate-receipt.json
      provides: retained post-pass receipt bound to the committed implementation and terminal BLOCKED result
    - path: scripts/check-boundaries.mjs
      provides: integrated artifact safety plus receipt-before-audit ordering enforcement
    - path: scripts/local-verify.mjs
      provides: canonical local-only `--phase5-full` orchestration and terminal receipt emission
    - path: .planning/v1.0-MILESTONE-AUDIT.md
      provides: receipt-citing post-pass milestone re-audit
  key_links:
    - from: scripts/release-gate/validate.mjs
      to: scripts/backup/production/results.mjs
      via: pre-release strict-validates concrete production-mounted result and rejects generated fake/mount fixture scope
      pattern: "production-backup-result|mounted-directory|scope|receipt|retention|alert"
    - from: scripts/release-gate/validate.mjs
      to: scripts/release-gate/bundle.mjs
      via: post evaluator verifies actual-byte predecessor bundle/decision digest before smoke and continue/rollback semantics
      pattern: "predecessor|hashEvidenceArtifact|readEvidenceArtifact"
    - from: scripts/local-verify.mjs
      to: scripts/phase5-receipt.mjs
      via: after the last required suite and canonical expect-blocked, pass committed revision, suite/source/result digests, command, scope, timestamps, and decision to atomic receipt writer
      pattern: "phase5-full|writePhase5ReceiptAtomic|expect-blocked"
    - from: scripts/local-verify.mjs
      to: scripts/backup/production-pipeline.mjs
      via: canonical gate creates exact generated production-shaped Compose/database/media authority, runs collector through mounted receipt, proves bytes/results, then cleans only that authority
      pattern: "blogxprodverify_|production-pipeline|collectProductionBackupSet|generated-production-pipeline"
    - from: scripts/phase5-receipt.mjs
      to: ops/release-evidence.blocked.json
      via: receipt binds actual canonical evidence bytes and exact terminal BLOCKED decision digest
      pattern: "canonicalEvidenceSha256|canonicalDecisionSha256|BLOCKED"
    - from: scripts/check-boundaries.mjs
      to: .planning/v1.0-MILESTONE-AUDIT.md
      via: a passed audit requires a strict receipt, matching cited SHA-256, ancestor implementation revision, and audit timestamp after receipt completion
      pattern: "phase5-full-gate-receipt|full_gate_receipt_sha256|audit"
    - from: .planning/v1.0-MILESTONE-AUDIT.md
      to: ops/phase5-full-gate-receipt.json
      via: post-pass audit cites and verifies exact receipt path/digest and honest local/generated scope
      pattern: "full_gate_receipt_sha256|implementation_revision|BLOCKED"
  prohibitions:
    - statement: "Pre-release readiness must not require post-release evidence, and post-release evidence must not pass without an exact valid predecessor binding."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/release-gate.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/release-sequence-circular.json
      check_clean_fixture: scripts/fixtures/prohibitions/release-sequence-linear.json
    - statement: "A Phase 5 receipt or passing audit must not exist for a dirty/uncommitted implementation, incomplete suite manifest, failed/non-BLOCKED terminal result, fake live claim, missing/mismatched digest, or audit written before receipt completion."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/phase5-receipt.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/premature-phase5-audit.json
      check_clean_fixture: scripts/fixtures/prohibitions/phase5-receipt-clean.json
    - statement: "No artifact, CLI, receipt, runbook, audit, or verifier may unfreeze, transition, connect, probe, upload, deploy, roll back, or modify either cloud host."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/release-gate.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/automatic-deploy.json
      check_clean_fixture: ops/release-evidence.blocked.json
    - statement: "Local/generated evidence must not be described in prose as live mount/off-host/schedule/alert/TLS/RPO/RTO/deployment/post-release truth."
      status: resolved
      verification: judgment
---

<objective>
As a release owner, I want non-circular decisions and a commit-bound full-gate receipt, so that milestone closure is reproducible and auditable without deploying, transitioning, contacting a server, or converting generated filesystem evidence into live production truth.

Purpose: Close G3/INT-03/FLOW-09, integrate the corrected media and executable mounted-backup paths, and prevent a premature audit from outrunning committed implementation evidence.
Output: Release evidence v2, separate evaluators, canonical BLOCKED state, centralized boundaries/runbooks, Phase 1-5 gate, atomic receipt module/artifact, and receipt-citing post-pass audit.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/05-v1-0-integration-gap-closure/05-RESEARCH.md
@.planning/phases/05-v1-0-integration-gap-closure/05-PATTERNS.md
@.planning/phases/05-v1-0-integration-gap-closure/05-01-SUMMARY.md
@.planning/phases/05-v1-0-integration-gap-closure/05-02-SUMMARY.md
@.planning/phases/04-secure-operations-and-release-gate/04-03-SUMMARY.md
@.planning/phases/04-secure-operations-and-release-gate/04-VERIFICATION.md
@.planning/phases/05-v1-0-integration-gap-closure/COVERAGE.md

<interfaces>
- Preserve v1 release safety: strict objects, regular-file/no-link exact bundle roots, actual-byte SHA-256, injected clock, secret/address scan, no-extra-file rule, redacted INVALID/BLOCKED, and canonical `--expect-blocked`.
- Pre-release sections omit post facts. Post evidence separately binds the validated pre decision/bundle, deployed digests, domain smoke, and continue/rollback. Deployment is an external human action and no CLI transition exists.
- 05-01 already owns minimal `phase5MediaSelection` and migration count 7. This plan extends it plus `phase4Selection('full')`; it does not duplicate or weaken either.
- 05-02 concrete generated pipeline execution proves collector plus mounted filesystem behavior, while generated fake proves faults. Neither is live off-host evidence. Only a future live collect-then-mounted result plus confirmed alert evidence can satisfy pre-release.
- Receipt target is fixed at `ops/phase5-full-gate-receipt.json`; writes use a restrictive exclusive sibling, fsync, strict parse/readback, atomic rename, and parent fsync. Failure before terminal BLOCKED must not mutate a prior valid receipt.
- Final receipt generation requires a clean worktree and committed implementation HEAD. Receipt/audit changes occur afterward and are committed separately as evidence/docs; a later phase verification report must cite the same receipt digest.
</interfaces>
</context>

## Artifacts this plan produces

- Strict release evidence v2 and required `release-sequence-circular` fail-first control.
- `blog-x-phase5-full-gate-receipt` v1 with exact committed revision, command/mode, suite manifest/source/result digests, scope, timestamps, and BLOCKED binding.
- `--phase5-full` as the sole milestone gate; it writes no success receipt before the terminal canonical decision.
- Audit pass content only after receipt verification; no pre-written pass, live claim, or production state change.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Split pre-release readiness from predecessor-bound post verification</name>
  <files>scripts/release-gate/schema.mjs, scripts/release-gate/bundle.mjs, scripts/release-gate/validate.mjs, scripts/release-gate.test.mjs, scripts/release-gate.mjs, ops/release-evidence.blocked.json, scripts/fixtures/prohibitions/release-sequence-circular.json, scripts/fixtures/prohibitions/release-sequence-linear.json</files>
  <read_first>
    <item>Current v1 release schema/validator/CLI, especially mandatory postRelease, byte/path/time checks, and exact exit codes.</item>
    <item>05-02 concrete mounted result schema, generated scopes, source/receipt/retention/alert facts, and 04-03 rollback/operations semantics.</item>
    <item>Audit G3/FLOW-09 and 05-RESEARCH predecessor state machine.</item>
  </read_first>
  <action>
    Start RED: complete current pre evidence with no post artifact must return `PRE_RELEASE_READY`/0. Define strict v2 canonical, pre, and post shapes—not an optional v1 field. Pre requires explicit scoped unfreeze, two host baselines, same-origin media/private-link/no-public-data-plane, a fresh concrete production collector result proving database/export/source+derivative media/config-image-migration completeness, independent restore, authenticated mounted ciphertext/receipt, safe retention, confirmed alert evidence, operations/TLS, and immutable rollback. `generated-production-pipeline`, `generated-mounted-fixture`, or `generated-fake` scope always blocks and is never coerced into live evidence.

    Post requires exact valid pre decision ID and actual-byte bundle/decision SHA-256, deployed Web/API digests, HTTPS-domain smoke, and explicit continue/rollback outcome. Legitimate missing/failed facts are BLOCKED or POST_RELEASE_FAILED; malformed/tampered/stale/mismatched/unsafe values are INVALID. Add mutually exclusive expectation-only flags for blocked, pre-ready, and post-verified; no deploy/transition/write/network/subprocess capability. Canonical v2 remains locator-free BLOCKED.
  </action>
  <acceptance_criteria>
    <criterion>Complete pre/no-post reaches PRE_RELEASE_READY; any pre schema requiring post goes RED under the named fixture.</criterion>
    <criterion>Post without predecessor blocks; wrong/tampered/stale predecessor invalidates; failed smoke is explicit non-success; exact predecessor/smoke/decision verifies.</criterion>
    <criterion>Generated pipeline, generated-mounted-fixture, and generated-fake results independently block pre readiness; only future live collect-then-mounted evidence with all facts can satisfy the schema.</criterion>
    <criterion>Canonical normal mode is BLOCKED/1 and expect-blocked BLOCKED/0; validation is pure and non-deploying.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/release-gate.test.mjs &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked</automated>
  </verify>
  <done>The release sequence is reachable and one-way without letting generated backup evidence or post facts create pre-release authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define atomic receipt and premature-audit boundaries</name>
  <files>scripts/phase5-receipt.mjs, scripts/phase5-receipt.test.mjs, scripts/check-boundaries.mjs, scripts/local-verify.test.mjs, scripts/release-gate.test.mjs, scripts/fixtures/prohibitions/premature-phase5-audit.json, scripts/fixtures/prohibitions/phase5-receipt-clean.json, docs/RELEASE-GATE.md, docs/ROLLBACK.md, docs/OPERATIONS.md, README.md</files>
  <read_first>
    <item>`scripts/backup/create.mjs` restrictive staging/verify/atomic rename and release bundle hash/redaction conventions.</item>
    <item>`scripts/check-boundaries.mjs` release artifact scan and current milestone audit frontmatter/status.</item>
    <item>05-01 focused verifier outputs and 05-02 mounted/fake scope distinctions.</item>
  </read_first>
  <action>
    Define a strict receipt with exact keys: format/version, implementationRevision, command argv, mode `phase5-full`, scope `local-generated-production-pipeline-and-fake-fault-only`, startedAt/completedAt, suiteManifest plus its SHA-256, per-suite source/result digests and nonzero counts/outcomes, canonicalEvidenceSha256, canonicalDecisionSha256/state BLOCKED, and no unknown fields. Validate revision format and require it to be the clean committed HEAD supplied before the run. Scan all scalar/output material for secrets, server/destination addresses, URLs presented as live endpoints, and production-success language.

    Implement restrictive atomic write/readback/hash helpers. Receipt creation is callable only with a complete exact suite manifest, all pass/nonzero/no-skip results, and terminal BLOCKED. Any earlier failure leaves the previous receipt byte-identical or absent. Extend boundaries so a passed audit is invalid unless receipt exists, strict-validates, its exact file SHA-256 equals audit frontmatter `full_gate_receipt_sha256`, audit `implementation_revision` equals receipt revision, revision is an ancestor, and audit timestamp is not earlier than receipt completion. The premature audit fixture must go RED and clean receipt fixture GREEN.

    Expose a read-only `node scripts/phase5-receipt.mjs verify --receipt=<fixed-path>` CLI used by Task 4 and later verification. It strict-parses and hashes only; it never creates a receipt, edits an audit, contacts a service, or changes release state.

    Update runbooks honestly: scheduled collector plus mounted adapter is shipped, but real collector service authority, mount/off-host identity, schedule/alert activation remain release evidence; generated production-shaped pipeline/fake and receipt scope are local only. Explain implementation commits precede final gate; receipt/audit/subsequent verification are later evidence documentation.
  </action>
  <acceptance_criteria>
    <criterion>Receipt schema rejects missing/extra suite, zero/skip/fail, dirty/uncommitted revision, non-BLOCKED decision, fake live claim, secret/address, timestamp inversion, or digest mismatch.</criterion>
    <criterion>Fault before rename preserves old receipt; success fsyncs/renames/readback-validates exact bytes and exposes a stable SHA-256.</criterion>
    <criterion>A passed/premature audit without exact valid receipt citation fails boundary checks; the clean control passes.</criterion>
    <criterion>Docs distinguish executable provider proof from live activation and state the later evidence-commit ordering.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/phase5-receipt.test.mjs scripts/release-gate.test.mjs scripts/local-verify.test.mjs &amp;&amp; corepack pnpm check:boundaries</automated>
  </verify>
  <done>There is one atomic, redacted, commit-bound evidence contract and no path to write a passing audit before it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finish and commit the Phase 1-5 implementation gate without issuing a receipt</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs, scripts/check-boundaries.mjs, scripts/phase5-receipt.mjs, scripts/phase5-receipt.test.mjs, apps/api/test/backup-restore.test.ts, apps/web/e2e/phase4-restore.spec.ts, docs/OPERATIONS.md, README.md</files>
  <read_first>
    <item>`phase4Selection('full')`, 05-01 `phase5MediaSelection`, offline prerequisites, semantic TAP/Playwright gates, exact cleanup, interruption, and parallel children.</item>
    <item>Phase 1-4 verification reports and milestone audit missing direct lifecycle browser evidence.</item>
    <item>05-02 fixed production collector, concrete mounted journey, pipeline CLI, and separately marked fake fault suite.</item>
  </read_first>
  <action>
    Add deterministic `phase5Selection('full')` as a strict superset of Phase 4 full plus 05-01 media/migration evidence. Include all API/database/browser/export/restore and rehearsal suites, release/receipt/boundaries, and 05-02 pipeline. Create a fresh exact `blogxprodverify_<token>` Compose project with matching generated database and media volume, no host-published API/PostgreSQL, exact generated production source root, generated mount identity fixture, generated key/alert authority, and no existing/local/production target. Seed deterministic database rows plus source/derivative media and allowlisted config/image/migration inputs.

    Run the real collector through fixed injected local runner operations against that isolated namespace: PostgreSQL dump, unchanged portable export, media copy, sanitized inventory, manifest/hash/COMPLETE, production verification, atomic finalization; then real encryption and mounted receipt/catalog/retention/result/alert. Prove database/export/media/config authorities and byte/hash equality, ciphertext excludes plaintext, receipt binds the newly created set, and result scope is exactly `generated-production-pipeline`. Separately run fake faults. Preserve offline/no-pull, redaction, nonzero/skip/TODO rejection, interruption/parallel unique authorities, and exact cleanup of production-shaped Compose/database/media/source/mount/key/alert roots. The generated scope is permanently rejected by release readiness.

    Accumulate a deterministic suite manifest before execution with each suite ID/kind/path and source SHA-256, then digest only redacted semantic results and counts. Final ordering is all suites -> cleanup/parallel proof -> canonical v2 `--expect-blocked` -> receipt writer. Receipt writer is unreachable on any earlier failure. `local-verify.test.mjs` proves strict superset, exact-once registration, concrete-mounted versus fake-fault scope, digest/ordering, and no receipt mutation on failure.

    During this task run focused unit/structural checks only; do not run the canonical final command and do not create/update `ops/phase5-full-gate-receipt.json` or the audit. Finish Tasks 1-3, let execute-plan create their normal implementation commits, and require a clean committed worktree before Task 4. Never amend those commits with receipt/audit/verification evidence.
  </action>
  <acceptance_criteria>
    <criterion>Selection is an exact Phase 1-5 superset and names every required suite/fixture once with source-digest coverage.</criterion>
    <criterion>Runner structure proves terminal BLOCKED precedes the only receipt call; any suite/cleanup/parallel/release failure cannot write a receipt.</criterion>
    <criterion>The gate creates—not reuses—an isolated production-shaped database/media/source set, proves all collected authorities/bytes through mounted result, and exactly cleans it under interruption/parallel runs.</criterion>
    <criterion>Generated-production-pipeline is implementation evidence and fake is fault-only; both are permanently rejected as live release evidence.</criterion>
    <criterion>All implementation files are committed and the worktree is clean before final-gate eligibility; receipt and audit remain untouched.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/local-verify.test.mjs scripts/phase5-receipt.test.mjs scripts/release-gate.test.mjs scripts/backup/production.test.mjs &amp;&amp; corepack pnpm check:boundaries &amp;&amp; corepack pnpm -r typecheck</automated>
  </verify>
  <done>The complete gate is implemented, structurally pinned, and committed, but no milestone receipt or audit pass has been issued prematurely.</done>
</task>

<task type="auto">
  <name>Task 4: Run committed full gate, atomically retain receipt, then re-audit</name>
  <files>ops/phase5-full-gate-receipt.json, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
    <item>Committed Task 1-3 implementation HEAD and clean-worktree check; stop if either is absent.</item>
    <item>`phase5ReceiptSchema`, receipt verification CLI/tests, and premature-audit boundary rules.</item>
    <item>Current audit G1-G3/INT-01-03/FLOW-07-09 and `COVERAGE.md` mapping.</item>
  </read_first>
  <action>
    Require a clean worktree and capture the committed Task 1-3 HEAD as `implementationRevision`; if implementation changes are uncommitted, stop. Run exactly `corepack pnpm local:verify -- --phase5-full --interruption-check --parallel-check`. The runner may atomically write the fixed receipt only after every suite and terminal canonical BLOCKED pass. If exit is nonzero, semantic output is incomplete, receipt is absent/changed incorrectly, source revision differs, or receipt verification fails, stop and do not edit the audit.

    Independently verify receipt schema, actual file SHA-256, exact command/mode, implementation revision, complete suite/source/result digests including generated collector-to-mounted pipeline, local/generated scope, timestamps, and BLOCKED binding. Only then update audit and cite direct evidence. State precisely: scheduled collect-then-mounted executable gap is closed; no real collector authority, mount/off-host destination, schedule activation, alert delivery, host/TLS/deployment/post-release action occurred; these remain OPS-05 evidence and canonical state is BLOCKED.

    Commit receipt and audit as a later evidence/documentation commit, separate from all implementation commits. The subsequent Phase 5 verification report must verify/cite the same receipt digest and belong to this later evidence stage; never amend implementation history.
  </action>
  <acceptance_criteria>
    <criterion>The retained receipt exists only after exact full-gate exit 0 and terminal BLOCKED, and binds the committed pre-receipt implementation revision.</criterion>
    <criterion>Audit exact receipt digest/path/revision match independently verified bytes and its timestamp follows receipt completion.</criterion>
    <criterion>Premature audit, receipt mutation/mismatch, dirty implementation, missing suite/result, or non-BLOCKED decision makes verification/boundary checks fail.</criterion>
    <criterion>Receipt/audit are committed later and separately; audit closes executable gaps without claiming live mount/off-host/schedule/alert/server evidence.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase5-receipt.mjs verify --receipt=ops/phase5-full-gate-receipt.json &amp;&amp; node --test scripts/phase5-receipt.test.mjs scripts/release-gate.test.mjs scripts/local-verify.test.mjs &amp;&amp; corepack pnpm check:boundaries &amp;&amp; rg -n "status: passed|full_gate_receipt_sha256|implementation_revision|OPS-01|OPS-03|OPS-05|INT-01|INT-02|INT-03|FLOW-07|FLOW-08|FLOW-09|BLOCKED" .planning/v1.0-MILESTONE-AUDIT.md</automated>
  </verify>
  <done>The committed implementation has one verified atomic receipt and a later receipt-bound audit, or the audit remains unchanged; production remains BLOCKED.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high. Any unmitigated high-severity row blocks the plan and audit update.

| Threat | Severity | Required mitigation/evidence |
|---|---|---|
| Circular pre-release authorization | High | Separate schemas/evaluators; pre/no-post fail-first fixture; no post field in pre schema. |
| Post forges predecessor | High | Actual-byte predecessor bundle/decision ID/hash/time binding and mismatch INVALID tests. |
| Generated collector/mount/fake becomes live release evidence | High | Scope-discriminated schemas; every generated scope blocks pre readiness and receipt carries explicit local pipeline scope. |
| Receipt emitted after partial/failed run | High | Single terminal call after all suites/cleanup/parallel/canonical BLOCKED; atomic preservation tests. |
| Receipt binds dirty or mutable implementation | High | Clean worktree, committed HEAD before run, source/suite digests, implementation commits completed first. |
| Audit is edited before or independently of proof | High | Required receipt path/hash/revision/timestamp binding in boundary/test; premature fixture fail-first. |
| Receipt leaks secrets or asserts live facts | High | Strict keys, scalar scans/redaction, digest-only results, fixed local/generated scope, false-claim boundary tests. |
| Validator/docs gain deployment capability | High | Expectation-only read operations, no network/subprocess deployment/transition, capability scans. |
| Regression gate omits critical flow | High | Explicit Phase 1-5 superset, exact suite manifest, source/result digests, skip/TODO/zero rejection. |
| Full gate reuses an existing/local/production-like backup target | High | Fresh `blogxprodverify_*` database/media and exact generated roots only; cross-mode/identity checks and exact cleanup. |

Production constraint: no server/cloud contact, mount provisioning, off-host transfer over network, systemd install/enable/start, live alert, certificate check, deployment, rollback, or state transition. Local mounted-directory tests use generated filesystem fixtures only. Production remains `BLOCKED` after the receipt and audit.
</threat_model>

<verification>

- [ ] Release v2 proves independent pre readiness, predecessor-bound post verification, all generated pipeline/mount/fake scope rejection, and canonical BLOCKED.
- [ ] Receipt tests prove strict schema, atomicity, clean committed revision, terminal-only emission, and premature-audit rejection.
- [ ] Tasks 1-3 implementation is committed before the exact full gate; receipt/audit/verification evidence is later and separate.
- [ ] Exact Phase 1-5 command passes all suites and produces a verified receipt bound to terminal BLOCKED.
- [ ] Audit cites exact receipt digest/revision and retains honest unresolved live evidence/no-server-contact statements.

</verification>

<success_criteria>

- PRE_RELEASE_READY and POST_RELEASE_VERIFIED are separate, reachable, predecessor-bound decisions with no deployment capability.
- Canonical release evidence remains locator-free BLOCKED and generated collector/mount/fake scopes cannot satisfy pre readiness.
- Full Phase 1-5 acceptance has a strict atomic receipt bound to committed code, exact suites/results, command/scope/time, and BLOCKED.
- Audit closure cannot precede or diverge from receipt bytes and uses a later evidence/documentation commit.
- OPS-01, OPS-03, OPS-05; G1-G3; INT-01-03; FLOW-07-09; and all Roadmap criteria have direct mapped evidence without live production claims.

</success_criteria>
