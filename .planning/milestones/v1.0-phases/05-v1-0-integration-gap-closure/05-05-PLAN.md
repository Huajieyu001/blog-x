---
phase: 05-v1-0-integration-gap-closure
plan: "05"
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json
  - scripts/fixtures/prohibitions/phase5-receipt-actual-results.json
  - scripts/phase5-receipt-prohibitions.test.mjs
  - scripts/phase5-receipt-concurrency.test.mjs
  - scripts/helpers/phase5-receipt-parent-worker.mjs
  - scripts/phase5-receipt.mjs
  - scripts/phase5-receipt.test.mjs
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - scripts/check-boundaries.mjs
  - ops/phase5-full-gate-receipt.json
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
gap_closure: true
requirements:
  - OPS-05
user_setup: []
must_haves:
  truths:
    - "The two named receipt prohibition fixtures exist as committed data-only artifacts and are actually read by focused tests: the historical suite-ID/revision-derived fixed-count shape is rejected, while a canonical record rebuilt from captured output is accepted."
    - "Deterministic committed subprocess tests prove that two barrier-released parent writers cannot both own or replace one generated receipt, and no test uses sleep timing as its coordination authority."
    - "A narrow test-only lifecycle observer can pause generated temporary-target locking only at enumerated events including `recovery-guard-acquired`, `lock-created-before-readback`, and `lock-release-before-ownership-check`; it only awaits IPC release and is rejected for the canonical receipt/full-gate path."
    - "A matching live PID and birth identity is never displaced; a dead or SIGKILLed owner is recoverable only after liveness/birth recheck; a live reused PID with a different birth identity is treated as a stale old owner rather than as the current writer."
    - "Recovery-guard contention permits at most one recovery owner, and writer/recovery release compares held dev, ino, and nonce before unlink so an ownership-changed path is preserved and the operation fails closed."
    - "The fixed O_EXCL writer lock, fixed O_EXCL recovery guard, pre-suite predecessor tuple, immediate pre-rename compare-and-swap, atomic fsync/rename/readback, and v2-only writer remain mandatory and are not weakened for tests."
    - "The exact Phase 5 full selection executes the new prohibition and concurrency test sources and includes every selected source exactly once in the receipt manifest; helper source remains bound by the clean committed implementation revision."
    - "A passed milestone audit cannot introduce or retain contradictory frontmatter/body implementation revisions: the one exact current audit/receipt byte pair is admitted only as read-only migration input, while every replacement audit must declare the body-revision contract and match one exact receipt-derived revision in both locations."
    - "All test, runner, receipt-lock, and boundary implementation changes are committed with a clean worktree before the exact full gate runs; that run creates a receipt-only commit, and only a later audit-only commit corrects the body and cites the new receipt."
    - "The executor never edits 05-VERIFICATION.md; a new independent Phase 5 verifier reconstructs the evidence and decides whether the remaining gaps are closed."
    - "Canonical production release remains locator-free BLOCKED, and every task stays within local offline generated/fake scope without server, SSH, deployment, real mount, systemd, live alert, TLS, rollback, unfreeze, or release-state transition activity."
  artifacts:
    - path: scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json
      provides: committed historical synthetic-formula and fixed-count violation input consumed by receipt prohibition tests
    - path: scripts/fixtures/prohibitions/phase5-receipt-actual-results.json
      provides: committed captured-output and canonical actual-result clean input consumed by receipt prohibition tests
    - path: scripts/phase5-receipt-prohibitions.test.mjs
      provides: fixture-driven rejection of the old synthetic result shape and acceptance/recomputation of actual canonical evidence
    - path: scripts/phase5-receipt-concurrency.test.mjs
      provides: deterministic lifecycle-event-controlled two-parent, live-owner, SIGKILL/dead-owner, PID-reuse, recovery-guard, inode, nonce, and partial-create regression coverage
    - path: scripts/helpers/phase5-receipt-parent-worker.mjs
      provides: generated-target subprocess worker with ready/lifecycle-event/release IPC barriers for lock acquisition, recovery critical sections, create readback, release, write, hold, and termination tests
    - path: scripts/phase5-receipt.mjs
      provides: fixed-lock and recovery implementation retaining safe ownership checks, bounded contention handling, predecessor CAS, v2-only atomic write, and a generated-target-only passive lifecycle observer
      exports:
        - acquirePhase5ReceiptWriterLock
        - releasePhase5ReceiptWriterLock
        - writePhase5ReceiptAtomic
        - phase5ExecutionResultSchema
        - phase5ReceiptSchema
    - path: scripts/local-verify.mjs
      provides: exact Phase 5 selection and execution wiring for the new committed tests plus clean-HEAD receipt orchestration
      exports:
        - phase5Selection
        - createPhase5SuiteManifest
    - path: scripts/local-verify.test.mjs
      provides: exact selection/manifest coverage and milestone audit body-revision boundary regressions
    - path: scripts/check-boundaries.mjs
      provides: strict passed-audit frontmatter/body revision consistency enforcement with one exact byte-bound migration predecessor
      exports:
        - auditMilestoneReceipt
        - evaluateRepositoryBoundaries
    - path: ops/phase5-full-gate-receipt.json
      provides: strict replacement v2 receipt from the exact clean committed Phase 5 full gate
    - path: .planning/v1.0-MILESTONE-AUDIT.md
      provides: later audit whose frontmatter and Receipt-Bound Full Gate body cite the same new receipt-derived implementation revision
  key_links:
    - from: scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json
      to: scripts/phase5-receipt-prohibitions.test.mjs
      via: the test reads the committed violation fixture, verifies its historical phase5-semantic-pass suite/revision formula and fixed-count shape, and requires strict rejection
      pattern: "phase5-receipt-synthetic-results|phase5-semantic-pass|assert\\.throws|reject"
    - from: scripts/fixtures/prohibitions/phase5-receipt-actual-results.json
      to: scripts/phase5-receipt-prohibitions.test.mjs
      via: the test parses the committed captured output, recomputes counts/output bytes/output digest/result digest, and accepts only the matching canonical record
      pattern: "phase5-receipt-actual-results|parseSemanticTapResult|hashPhase5ResultRecord|phase5ExecutionResultSchema"
    - from: scripts/phase5-receipt-concurrency.test.mjs
      to: scripts/helpers/phase5-receipt-parent-worker.mjs
      via: parent tests wait for explicit IPC ready and lifecycle-event messages, perform generated-path mutation or start the contender, then send the matching release message before asserting outcomes
      pattern: "phase5-receipt-parent-worker|ready|recovery-guard-acquired|lock-created-before-readback|lock-release-before-ownership-check|release|SIGKILL"
    - from: scripts/helpers/phase5-receipt-parent-worker.mjs
      to: scripts/phase5-receipt.mjs
      via: each worker uses the same public acquire/write/release path as the full-gate parent against one exact generated receipt target and translates passive lifecycle observations into IPC event/wait-for-release barriers
      pattern: "acquirePhase5ReceiptWriterLock|writePhase5ReceiptAtomic|releasePhase5ReceiptWriterLock|testLifecycleObserver|process\.send"
    - from: scripts/local-verify.mjs
      to: scripts/phase5-receipt-prohibitions.test.mjs
      via: both new focused test files are selected, executed, TAP-recorded, and source-hashed exactly once by the Phase 5 full gate
      pattern: "phase5-receipt-prohibitions\\.test\\.mjs|phase5-receipt-concurrency\\.test\\.mjs|recordPhase5Command|createPhase5SuiteManifest"
    - from: scripts/check-boundaries.mjs
      to: .planning/v1.0-MILESTONE-AUDIT.md
      via: non-migration passed audits require a body-revision contract and exactly one Receipt-Bound Full Gate implementation revision equal to frontmatter and verified receipt
      pattern: "audit_body_revision_contract|implementation_revision|Receipt-Bound Full Gate|phase5_audit_body_revision"
    - from: .planning/v1.0-MILESTONE-AUDIT.md
      to: ops/phase5-full-gate-receipt.json
      via: only the audit-only commit after the receipt-only commit copies the verified receipt SHA-256, implementation revision, completion facts, and retained BLOCKED state
      pattern: "full_gate_receipt_version: 2|full_gate_receipt_sha256|implementation_revision|BLOCKED"
  prohibitions:
    - statement: "A suite result derived from suite ID, implementation revision, the phase5-semantic-pass formula, or fixed 1/1 fields must not be accepted as actual execution evidence."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/phase5-receipt-prohibitions.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json
      check_clean_fixture: scripts/fixtures/prohibitions/phase5-receipt-actual-results.json
    - statement: "A competing parent, live owner, PID-reused owner, recovery-guard contender, or ownership-changed lock path must not unlink or replace another writer's authority or receipt."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/phase5-receipt-concurrency.test.mjs
    - statement: "The test lifecycle observer must not run for the fixed canonical receipt/full gate or supply liveness, birth, ownership, CAS, cleanup, stale/recoverable, or outcome verdicts."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/phase5-receipt-concurrency.test.mjs
    - statement: "A passed audit must not contradict its verified receipt revision between frontmatter and body, and a legacy migration exception must not accept any bytes other than the exact current predecessor pair."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/local-verify.test.mjs
    - statement: "No local evidence, test helper, receipt writer, audit, or runner may contact a server or authorize deployment, rollback, unfreeze, or production readiness."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/release-gate.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/automatic-deploy.json
      check_clean_fixture: ops/release-evidence.blocked.json
---

<objective>
Close every remaining independent Phase 5 verification gap with committed, executable negative controls and deterministic receipt-lock regressions, then regenerate receipt and audit evidence in strict order.

Purpose: Make the 05-04 claims durable rather than narrative: the named fixtures must be consumed, lock/recovery safety must survive real subprocess races and interruption, and the milestone audit must be machine-protected against a stale body revision.
Output: Fixture-driven prohibition tests, barrier-controlled parent-writer tests/helper, strengthened boundary/audit consistency enforcement, a new exact-gate v2 receipt-only commit, a later audit-only commit, and an independent verification handoff.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md
@.planning/phases/05-v1-0-integration-gap-closure/05-04-PLAN.md
@.planning/phases/05-v1-0-integration-gap-closure/05-04-SUMMARY.md
@.planning/v1.0-MILESTONE-AUDIT.md
@ops/phase5-full-gate-receipt.json
@scripts/phase5-receipt.mjs
@scripts/phase5-receipt.test.mjs
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/check-boundaries.mjs

<interfaces>
- Preserve receipt v2, canonical actual-result records, output byte/digest binding, the exact command, local-generated-production-pipeline-and-fake-fault-only scope, and canonical BLOCKED decision from 05-04.
- Preserve one fixed `${receiptPath}.lock`, one fixed `${receiptPath}.lock.recovery`, O_EXCL/0600 creation, PID plus birth-identity liveness, dev/ino/nonce ownership, pre-suite predecessor existence/version/SHA-256, immediate pre-rename CAS, restrictive incomplete sibling, file/parent fsync, atomic rename, strict readback, and v2-only writing. Tests may use the existing injected process-inspection seam only for PID-reuse cases; they must not inject lock ownership decisions.
- Add one narrow `testLifecycleObserver`-style seam only to acquisition/recovery/release internals. It is accepted only when the validated receipt is a generated `/tmp/blog-x-phase5-receipt-*` target, is absent from and rejected for the fixed canonical receipt/full-gate path, and recognizes a closed event set including `recovery-guard-acquired`, `lock-created-before-readback`, and `lock-release-before-ownership-check`. The observer receives frozen event metadata only and may return `undefined` synchronously or a Promise that waits for an IPC release signal and resolves `undefined`; any other return/resolution, rejection, or throw aborts fail-closed. No observer result is interpreted as liveness, PID birth, ownership, stale/recoverable, CAS, cleanup, or pass/fail authority.
- All race tests use generated `/tmp/blog-x-phase5-receipt-*` targets accepted by the production validator. The subprocess worker communicates only through inherited Node IPC and explicit `ready` -> lifecycle `event` -> matching `release` barriers. It relays events and waits; it cannot mutate lock facts or supply any verdict. Bounded timeouts are deadlock guards, never the signal that an owner has acquired, died, recovered, replaced, or released; do not use arbitrary sleeps, filesystem watches, or polling as timing authority.
- The test coordinator pauses at `recovery-guard-acquired` before stale-writer removal to start/release a competing recovery parent; pauses at `lock-release-before-ownership-check` before inode replacement or same-inode nonce mutation; and pauses at `lock-created-before-readback` after O_EXCL open/write but before path readback to replace the path and prove partial-create cleanup preserves the replacement.
- The synthetic fixture is data-only and reproduces the former suite-ID plus implementation-revision formula and flat fixed-count result shape. The test first proves the fixture really represents that formula, then proves the strict v2 actual-result/receipt path rejects it.
- The actual fixture is data-only and contains safe captured TAP output plus its expected non-fixed canonical record facts. The test must rebuild normalized output bytes, parser counts, invocation record, output SHA-256, and result SHA-256 from fixture content; acceptance may not be a bare schema parse of hand-authored hashes.
- Keep the current audit SHA-256 `9b6191dd45837f5e7f5045ef865460a16f714287b5825e5a1f8cc98af4d9b2d8` and current receipt SHA-256 `9c0aa9943017604ce4b25a25546355890afbbc0a0a8ba5289a7055918df79ee4` as one exact read-only migration pair so the new strict checker can be committed before the gate without editing the passed audit first. Any byte change, receipt mismatch, or attempted second legacy pair fails closed.
- Every replacement passed audit declares `audit_body_revision_contract: 1`; within `## Receipt-Bound Full Gate`, exactly one implementation-revision claim must equal both frontmatter `implementation_revision` and the verified receipt's `implementationRevision`. The exact migration pair is removed naturally when Task 3 replaces the receipt and Task 4 replaces the audit.
- Add both new test files to the Phase 5 selection and execute them in `runPhase5FullChecks`; update the exact manifest cardinality from 28 to the derived selected-source total (expected 30 after this plan), while retaining uniqueness and one pipeline/one boundary entry.
- Tasks 1-2 may create normal test/implementation commits, but must leave receipt, audit, and 05-VERIFICATION bytes unchanged. Task 3 starts only from the clean committed Task 1-2 HEAD and commits only the generated receipt. Task 4 then commits only the audit. Summary/planning handoff follows separately.
- No task connects to cloud servers, public endpoints, or network services. Only local offline dependencies, generated temporary roots, generated/fake authorities, loopback, and the already-established local verifier runtime are in scope.
</interfaces>
</context>

## Why this plan has four tasks

The RED fixtures/harness must be independently reviewable before production locking and boundary changes. All GREEN implementation and exact-gate selection changes then need one clean committed authority. The replacement receipt must be generated and committed alone from that authority, and the corrected passed audit must follow in a distinct audit-only commit so it can never cite evidence that did not yet exist.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit fixture-driven and barrier-controlled RED regressions</name>
  <files>scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json, scripts/fixtures/prohibitions/phase5-receipt-actual-results.json, scripts/phase5-receipt-prohibitions.test.mjs, scripts/phase5-receipt-concurrency.test.mjs, scripts/helpers/phase5-receipt-parent-worker.mjs, scripts/phase5-receipt.test.mjs, scripts/local-verify.test.mjs</files>
  <read_first>
    <item>05-VERIFICATION.md — exact missing fixture, two-parent/stale-owner, and audit-body findings.</item>
    <item>05-04-PLAN.md and 05-04-SUMMARY.md — promised fixture paths, writer-lock acceptance, commit ordering, and claims that require durable tests.</item>
    <item>scripts/phase5-receipt.mjs and scripts/phase5-receipt.test.mjs — public schema/lock/write APIs, generated target validator, process-inspection seam, current live-owner/CAS tests, and missing recovery races.</item>
    <item>scripts/local-verify.mjs and scripts/local-verify.test.mjs — actual record builder, current 28-source selection, and receipt/audit test surfaces.</item>
    <item>scripts/check-boundaries.mjs and .planning/v1.0-MILESTONE-AUDIT.md — frontmatter-only revision comparison and stale `68b9178...` body claim.</item>
  </read_first>
  <action>
    Create both named JSON fixtures first. The synthetic fixture must encode the former `phase5-semantic-pass:${suiteId}:${implementationRevision}` derivation, its matching digest, and the legacy flat fixed `tests: 1`/`passed: 1` shape. The actual fixture must contain safe complete TAP v13 output with more than one passing test, timing/source facts, and expected canonical invocation/result facts derived from that output. Keep both data-only: no executable commands, paths outside generated scope, credentials, addresses, or production-ready language.

    Add `phase5-receipt-prohibitions.test.mjs` that reads both exact fixture paths. Prove the violation fixture's digest matches the historical formula before asserting rejection; do not merely label arbitrary invalid JSON "synthetic." For the clean fixture, run the strict TAP parser/recorder path, recompute normalized output byte length/SHA-256 and canonical result SHA-256, compare every field to the fixture, and assert the strict execution-result schema accepts it. Mutating output, count, formula inputs, record bytes, or digest must fail.

    Add a narrow IPC worker and `phase5-receipt-concurrency.test.mjs`. The worker accepts only an exact generated receipt path and an enumerated action over Node IPC, reports `ready`, waits on a coordinator barrier, then uses the public acquire/write/release functions. Its test lifecycle observer reports only enumerated `{ type: "event", name, token }` messages and awaits the exact matching `{ type: "release", token }`; add assertions that an unknown/duplicate release or event cannot proceed. It must never accept a shell command, server address, arbitrary cleanup root, canonical repository receipt path, or injected liveness, PID-birth, ownership, CAS, stale/recoverable, cleanup, or outcome verdict.

    Cover these deterministic scenarios without sleeps, filesystem watches, or polling authority: two ready parent writers released on one barrier with exactly one acquisition/write; A holding a live lock while B fails without inode/nonce/receipt mutation; A killed with SIGKILL after `acquired`, awaited through process close, then B recovers and writes; an explicitly dead PID owner recovers; an alive same-PID/different-birth record recovers while same-PID/same-birth refuses. For two recovery contenders, pause A on `recovery-guard-acquired`, start/release B while A still owns the guard, assert B cannot remove the guard/writer, then release A and prove one recovery/write. For release safety, pause on `lock-release-before-ownership-check`, have the coordinator perform inode replacement and, in a separate case, same-inode nonce mutation, release the observer, then require release failure and byte-for-byte preservation of the replacement. For partial creation cleanup, pause on `lock-created-before-readback` after O_EXCL open/write, replace the path from the coordinator, release the observer, and prove readback/cleanup fails closed without unlinking the replacement. Existing predecessor-CAS and failed-write preservation tests remain mandatory.

    Add RED assertions that the two new tests are selected/executed by Phase 5 and that a nonlegacy passed audit with a body revision differing from frontmatter/receipt produces a distinct machine finding. At least the selection and audit assertions must fail against current production code; commit only fixtures, tests, and helper as the RED commit. Do not modify implementation, receipt, audit, or 05-VERIFICATION in this task.
  </action>
  <acceptance_criteria>
    <criterion>Both named fixtures are committed and test code reads each exact path; synthetic authenticity and actual-record recomputation are assertions, not comments.</criterion>
    <criterion>The concurrency suite coordinates every race through ready/lifecycle-event/matching-release IPC and process-close events; it uses no sleep, fs.watch, or polling as acquisition, death, recovery, mutation, or release authority.</criterion>
    <criterion>RED cases explicitly wait for `recovery-guard-acquired`, `lock-created-before-readback`, and `lock-release-before-ownership-check`; they prove the current code lacks the required passive observer rather than attempting to guess critical-section timing.</criterion>
    <criterion>RED output identifies missing selection/audit enforcement and any exposed lock/recovery defect without touching canonical receipt/audit evidence.</criterion>
    <criterion>The test-only commit contains neither `scripts/phase5-receipt.mjs`, `scripts/local-verify.mjs`, `scripts/check-boundaries.mjs`, `ops/phase5-full-gate-receipt.json`, `.planning/v1.0-MILESTONE-AUDIT.md`, nor 05-VERIFICATION.md.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/phase5-receipt-prohibitions.test.mjs scripts/phase5-receipt-concurrency.test.mjs scripts/phase5-receipt.test.mjs scripts/local-verify.test.mjs</automated>
  </verify>
  <done>Committed RED evidence names and exercises every verifier-requested fixture/race/body contradiction while canonical evidence remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make locking, selection, and audit consistency GREEN, then freeze a clean implementation HEAD</name>
  <files>scripts/phase5-receipt.mjs, scripts/phase5-receipt.test.mjs, scripts/local-verify.mjs, scripts/local-verify.test.mjs, scripts/check-boundaries.mjs, scripts/phase5-receipt-prohibitions.test.mjs, scripts/phase5-receipt-concurrency.test.mjs, scripts/helpers/phase5-receipt-parent-worker.mjs</files>
  <read_first>
    <item>The committed Task 1 RED output and exact fixture/harness contracts.</item>
    <item>scripts/phase5-receipt.mjs — `createLock`, `removeDeadLock`, `releaseRawLock`, acquisition recovery-guard branch, authority WeakSet, predecessor reader, and atomic writer.</item>
    <item>scripts/local-verify.mjs — `phase5Selection`, `createPhase5SuiteManifest`, `runPhase5FullChecks`, result recorder, committed HEAD check, and outer lock lifecycle.</item>
    <item>scripts/check-boundaries.mjs — `auditFrontmatter`, `auditMilestoneReceipt`, tracked audit inclusion, and boundary machine result.</item>
  </read_first>
  <action>
    Make the RED tests pass with the smallest production changes. Preserve one fixed O_EXCL writer and recovery path and the existing opaque authority/CAS contract. Add the narrow lifecycle observer defined in `<interfaces>` and thread it only through generated-target create/recovery/release calls. Emit `lock-created-before-readback` after the O_EXCL handle/record exists but before path readback, `recovery-guard-acquired` after the guard is authenticated but before stale-writer inspection/removal, and `lock-release-before-ownership-check` before the final path dev/ino/nonce comparison. Freeze event data, await the observer only as a barrier, require synchronous or resolved `undefined`, reject unknown events and any verdict-like/non-undefined return, and prove the fixed canonical target and ordinary full-gate call path cannot enable it. The local verifier must never pass this option.

    If contention exposes an acquisition race, use bounded state retries around exact EEXIST/dead-guard transitions; a matching live owner or live recovery guard still fails immediately and no age-based removal is allowed. All cleanup/release branches, including failure after O_EXCL open and before readback, must close held handles and compare dev/ino/nonce before unlink. On mismatch, preserve the path and surface a failure. Observer errors abort and execute the same ownership-safe cleanup; they cannot return any liveness, PID birth, ownership, stale/recoverable, CAS, cleanup, or outcome verdict. Do not add a public option that tells production code an owner is dead or bypasses ownership checks.

    Update Phase 5 selection and execution so both new test sources run exactly once as Phase 5-only Node suites and are immediately TAP-parsed/recorded. Compute/validate the manifest against the selected source set (30 unique entries after adding the two tests, including one pipeline and one boundary); never silently omit a selected test or infer its pass from another suite. The helper is not a standalone suite, but remains bound by the clean committed implementation revision.

    Strengthen `auditMilestoneReceipt` with an exact body contract. Parse only the `## Receipt-Bound Full Gate` section, require one unambiguous implementation-revision claim, and require equality with frontmatter and verified receipt. Introduce `audit_body_revision_contract: 1` for replacement passed audits. To avoid the forbidden audit-before-receipt edit, admit only the exact current audit SHA-256 `9b6191...d9b2d8` paired with exact predecessor receipt SHA-256 `9c0aa9...79ee4` as read-only migration input. This exception is byte-bound, cannot be configured, cannot accept a modified audit/receipt, and must not suppress any digest, timestamp, ancestry, or BLOCKED-state check. Every other missing contract, duplicate/malformed body claim, or mismatch emits `phase5_audit_body_revision`.

    Run focused fixture, concurrency, receipt, local-verifier, release, boundary, and typecheck checks until GREEN. Confirm the exact predecessor audit/receipt pair still passes only its migration path, while any one-byte audit or receipt mutation fails. Commit all implementation/test changes, then require an empty `git status --porcelain` and capture that commit as the only Task 3 implementation revision. Assert the current receipt, audit, and 05-VERIFICATION hashes still equal their Task 1 baselines. Do not run the exact full gate yet.
  </action>
  <acceptance_criteria>
    <criterion>All requested races pass repeatedly under ready/event/release barrier control: exactly one writer/recovery winner, no live-owner displacement, safe SIGKILL/dead/PID-reuse recovery, and preserved replacement lock bytes on inode/nonce mismatch.</criterion>
    <criterion>Recovery contention pauses at authenticated `recovery-guard-acquired`; inode replacement and same-inode nonce mutation pause before release ownership comparison; partial-create replacement pauses after O_EXCL creation and before readback. Each case resumes only through matching IPC release and proves fail-closed preservation.</criterion>
    <criterion>The lifecycle observer is accepted only for validated generated temporary receipt targets, receives passive frozen events, cannot alter any security verdict, and is absent/rejected in canonical receipt and normal full-gate execution.</criterion>
    <criterion>Writer lock, recovery guard, predecessor CAS, atomic rename/fsync/readback, and v2-only writer assertions remain covered and unchanged in strength.</criterion>
    <criterion>The two fixture tests are GREEN and Phase 5 selection/execution/manifest contains both new test sources exactly once.</criterion>
    <criterion>Boundary tests reject any new passed audit with frontmatter/body/receipt revision disagreement; only the exact byte-bound predecessor pair is accepted for one-way migration.</criterion>
    <criterion>Focused tests, boundary audit, and workspace typecheck pass from a clean committed implementation HEAD while receipt, audit, and 05-VERIFICATION bytes remain unchanged.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/phase5-receipt-prohibitions.test.mjs scripts/phase5-receipt-concurrency.test.mjs scripts/phase5-receipt.test.mjs scripts/local-verify.test.mjs scripts/release-gate.test.mjs &amp;&amp; corepack pnpm check:boundaries &amp;&amp; corepack pnpm -r typecheck &amp;&amp; git status --porcelain</automated>
  </verify>
  <done>The clean committed implementation contains durable fixture/race/body checks and is frozen as the authority for a new exact full-gate receipt.</done>
</task>

<task type="auto">
  <name>Task 3: Run the exact clean full gate and commit only the replacement receipt</name>
  <files>ops/phase5-full-gate-receipt.json</files>
  <read_first>
    <item>The clean committed Task 2 HEAD and empty worktree; stop if either differs.</item>
    <item>The unchanged predecessor receipt/audit/verification hashes and exact migration tuple.</item>
    <item>scripts/local-verify.mjs — exact selection, new test execution, finalization, clean-HEAD check, and receipt writer order.</item>
    <item>scripts/phase5-receipt.mjs — fixed lock/recovery, expected predecessor CAS, atomic v2 writer, and read-only verifier.</item>
  </read_first>
  <action>
    Require an empty worktree and capture the clean committed Task 1-2 HEAD. Record predecessor receipt, audit, and 05-VERIFICATION SHA-256 values. Run exactly:

    `corepack pnpm local:verify -- --phase5-full --interruption-check --parallel-check`

    Use only established offline local verifier images/dependencies, loopback, generated temporary roots, and fake/generated authorities. Do not substitute a focused run, omit flags, reuse a prior log, edit the receipt manually, or contact any server/network authority. The canonical parent acquisition must pass no lifecycle observer and the receipt implementation must reject any attempt to attach one to the fixed target. The gate must execute and record the two new committed test suites, then retain the fixed lock through cleanup, parallel isolation, clean-HEAD recheck, predecessor CAS, atomic replacement, and strict readback.

    On any failure, interruption, live-lock contention, stale-recovery ambiguity, worktree/HEAD drift, source/manifest mismatch, fixture/race failure, or predecessor change, stop. The predecessor receipt, audit, and 05-VERIFICATION must remain byte-for-byte unchanged; do not repair evidence manually and do not proceed to Task 4.

    On exit 0, independently run the read-only receipt verifier and focused receipt/fixture/concurrency tests. Confirm receipt version 2, exact command/scope/BLOCKED decision, implementation revision equal to the captured Task 2 HEAD, all selected sources including both new test files exactly once, canonical actual result/output bindings, and no orphan lock/guard. Commit only `ops/phase5-full-gate-receipt.json`. The audit is now intentionally an older citation and must not be edited or boundary-certified until Task 4; 05-VERIFICATION remains untouched.
  </action>
  <acceptance_criteria>
    <criterion>The exact command exits 0 from the clean Task 2 implementation revision and records both new focused suites as actual passing results.</criterion>
    <criterion>The normal full-gate parent uses no lifecycle observer; the test-only pause surface is unavailable for the fixed canonical receipt target.</criterion>
    <criterion>The new receipt's manifest/source digests reflect every changed runner, boundary, receipt, and selected test source; every record digest/output length/count recomputes.</criterion>
    <criterion>Receipt write follows all suites, cleanup, interruption/parallel checks, predecessor CAS, and actual BLOCKED output under one safely released fixed lock.</criterion>
    <criterion>The successful evidence commit changes exactly `ops/phase5-full-gate-receipt.json`; audit and 05-VERIFICATION retain prior bytes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase5-receipt.mjs verify --receipt=ops/phase5-full-gate-receipt.json &amp;&amp; node --test scripts/phase5-receipt-prohibitions.test.mjs scripts/phase5-receipt-concurrency.test.mjs scripts/phase5-receipt.test.mjs &amp;&amp; git diff --exit-code -- .planning/v1.0-MILESTONE-AUDIT.md .planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md</automated>
  </verify>
  <done>A receipt-only commit binds the full gate and all new controls to one clean implementation revision; no passed audit has been edited early.</done>
</task>

<task type="auto">
  <name>Task 4: Correct and machine-bind the later audit, then hand off independent verification</name>
  <files>.planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
    <item>The committed Task 3 receipt, its SHA-256, implementation revision, completion time, suite/result counts, and commit ancestry.</item>
    <item>.planning/v1.0-MILESTONE-AUDIT.md — stale body `68b9178...` claim, current frontmatter, BLOCKED state, and live blockers.</item>
    <item>scripts/check-boundaries.mjs and focused tests — required body-revision contract and exact replacement-audit checks.</item>
    <item>05-VERIFICATION.md — gaps-found report reserved for a separate verifier.</item>
  </read_first>
  <action>
    Require the Task 3 receipt-only commit to be an ancestor of a clean HEAD. Independently verify the receipt and derive its file SHA-256, implementation revision, completion time, selected source/result counts, and aggregate actual outcomes.

    Only now edit the milestone audit. Set `audit_body_revision_contract: 1`; update frontmatter receipt digest/revision/time facts to the Task 3 receipt; replace stale `68b9178...` in `## Receipt-Bound Full Gate` with exactly the same full implementation revision; and keep exactly one machine-parseable body revision claim. Retain actual-result wording, generated/local scope, every unresolved live prerequisite, and canonical BLOCKED state. Do not claim Phase 5 passed or production readiness.

    Run strict boundary and receipt checks. Assert the old exact migration tuple no longer applies, the audit body/frontmatter/receipt revision is identical, audit timestamp follows receipt completion, and implementation/receipt/audit commit ancestry is implementation -> receipt-only -> audit-only. Commit only `.planning/v1.0-MILESTONE-AUDIT.md`.

    After the audit-only commit, let execute-plan create 05-05-SUMMARY.md in its normal later documentation commit and hand off to a new independent Phase 5 verifier. The executor must not edit 05-VERIFICATION.md, ROADMAP.md, STATE.md, or REQUIREMENTS.md and must not mark OPS-05/Phase 5 complete. The verifier must independently consume both fixtures, rerun deterministic lock tests, reconstruct receipt hashes/counts/source bindings, validate commit ordering and audit body/frontmatter equality, and retain production BLOCKED/live blockers.
  </action>
  <acceptance_criteria>
    <criterion>The later audit declares the body-revision contract and contains one full implementation revision equal to frontmatter and the verified Task 3 receipt; stale `68b9178...` is absent.</criterion>
    <criterion>Receipt and boundary checks pass without the migration exception, and a changed/duplicate/mismatched body revision fails the focused machine check.</criterion>
    <criterion>The audit commit changes exactly the milestone audit and follows the receipt-only commit; summary/handoff follows separately.</criterion>
    <criterion>05-VERIFICATION remains gaps_found until a new independent verifier decides otherwise, and canonical production release remains BLOCKED with every live precondition retained.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase5-receipt.mjs verify --receipt=ops/phase5-full-gate-receipt.json &amp;&amp; corepack pnpm check:boundaries &amp;&amp; node --test scripts/phase5-receipt-prohibitions.test.mjs scripts/phase5-receipt-concurrency.test.mjs scripts/phase5-receipt.test.mjs scripts/local-verify.test.mjs &amp;&amp; rg -n "audit_body_revision_contract: 1|full_gate_receipt_version: 2|full_gate_receipt_sha256|implementation_revision|BLOCKED" .planning/v1.0-MILESTONE-AUDIT.md &amp;&amp; rg -n "status: gaps_found|requirements_blocked: \[OPS-05\]" .planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md</automated>
  </verify>
  <done>The audit-only commit cites the new receipt consistently in frontmatter and body, and a separate verifier—not the executor—is queued to decide Phase 5 closure.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high. Any unmitigated high-severity row blocks receipt replacement or audit editing.

| Threat / failure | Severity | Required mitigation and evidence |
|---|---|---|
| Missing fixtures make prohibition evidence narrative-only | High | Both exact paths are must-have artifacts, committed, read, authenticity-checked, mutated, and included in focused tests. |
| Synthetic formula/fixed 1/1 result is accepted | High | Historical formula recomputation plus strict rejection; actual fixture rebuilt from captured output and non-fixed counts. |
| Race outcome depends on sleeps, fs.watch, or polling | High | IPC ready/lifecycle-event/matching-release barriers and awaited process close; timeouts only detect deadlock. |
| Test observer alters a security decision or reaches canonical execution | High | Generated-target validation, closed event set, passive frozen metadata, wait-only Promise, ignored return values, fail-closed errors, fixed-target rejection, and no local-verifier wiring. |
| Live or PID-reused writer is stolen | High | PID plus birth equality for live refusal; birth mismatch for stale old owner; real subprocess and injected birth tests. |
| Dead/SIGKILL writer permanently blocks evidence | High | Await actual process death, acquire fixed recovery guard, recheck death/birth, recover exactly once. |
| Two recovery contenders unlink each other | High | Pause the authenticated guard owner at `recovery-guard-acquired`, run the contender, then release; fixed O_EXCL guard and bounded EEXIST handling yield one winner and preserved authority. |
| Replaced lock path is unlinked on release/error cleanup | High | Pause at `lock-release-before-ownership-check`; held-handle dev/ino plus nonce comparison preserves both inode-replacement and same-inode nonce-mutation bytes and closes handles. |
| Partial create failure unlinks another path owner | High | Pause at `lock-created-before-readback` after O_EXCL open, replace path externally, then prove readback cleanup refuses unlink and preserves replacement bytes. |
| Concurrency test helper becomes arbitrary execution surface | High | Generated target validator, enumerated IPC actions, no shell/server/canonical target, no ownership-verdict injection. |
| New tests exist but exact gate omits them | High | Selection/execution assertions, manifest uniqueness/cardinality, TAP recording, retained receipt source/result entries. |
| Strict audit checker makes receipt-before-audit ordering impossible | High | One immutable audit+receipt SHA pair as read-only migration input; every replacement requires the new contract. |
| Legacy migration exception becomes a general bypass | High | Both exact SHA-256 values hard-bound, no configuration, no second pair, all other receipt/audit checks remain strict. |
| Audit is edited before a successful receipt exists | High | Clean implementation first, exact gate, receipt-only commit, later audit-only commit, then summary/verifier. |
| New source changes leave an old receipt | High | Exact full gate reruns from clean committed Task 2 HEAD and replaces receipt after all new tests/boundaries pass. |
| Local evidence grants live authority | High | Local/generated/fake scope and BLOCKED remain exact; no server/network/deployment/mount/systemd/alert/TLS/rollback/unfreeze capability. |

Production constraint: no cloud/server contact, SSH, public endpoint, network transfer, real mount, systemd install/enable/start, live alert, TLS check, deployment, rollback, unfreeze, or release-state transition. Only local offline generated/fake authorities, loopback, and generated temporary filesystem targets are permitted.
</threat_model>

<verification>

- [ ] Both named fixtures exist in Git, appear in `must_haves.artifacts`, are read by tests, and prove authentic synthetic rejection plus actual canonical acceptance.
- [ ] Ready/event/release-controlled subprocess tests cover two parent writers, live owner, dead/SIGKILL owner, PID birth reuse, recovery-guard contention, inode/nonce-safe release, and partial-create cleanup without sleep/watch/poll authority.
- [ ] The lifecycle observer is generated-target-only, wait-only, verdict-free, fixed-target rejected, and absent from normal full-gate execution.
- [ ] Existing fixed lock/recovery, predecessor CAS, v2-only atomic write, preservation, and readback controls do not regress.
- [ ] New test sources are selected, executed, source-hashed, and result-recorded by the exact Phase 5 full gate.
- [ ] Clean Tasks 1-2 implementation precedes one successful exact gate; receipt-only commit precedes audit-only commit.
- [ ] Passed audit body/frontmatter/receipt revision equality is machine-enforced after the one exact migration predecessor is replaced.
- [ ] 05-VERIFICATION remains untouched by the executor and is rerun only by a new independent verifier.
- [ ] No live/server/network operation occurs and canonical production release remains BLOCKED.

</verification>

<success_criteria>

- The independent verifier can execute—not infer—both negative-control fixtures and every promised concurrency/recovery case.
- The retained receipt binds the changed test/runner/boundary sources and their actual results to one clean implementation revision.
- The milestone audit cannot disagree about implementation revision between frontmatter, body, and receipt, and its commit provably follows the receipt-only commit.
- OPS-05 is ready for independent Phase 5 closure without changing production state or asserting any unresolved live fact.

</success_criteria>

<output>
After execution, create `.planning/phases/05-v1-0-integration-gap-closure/05-05-SUMMARY.md`, then route to a new independent Phase 05 verifier. The executor must not edit `.planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md`.
</output>
