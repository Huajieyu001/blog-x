---
phase: 03-distribution-and-portability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - compose.yaml
  - apps/web/Dockerfile
  - apps/api/test/public-distribution.test.ts
  - packages/contracts/src/distribution.ts
  - packages/contracts/src/index.ts
  - apps/api/src/content/public-repository.ts
  - apps/api/src/routes/public-posts.ts
  - apps/web/app/lib/api.ts
  - apps/web/app/lib/site-metadata.ts
  - apps/web/app/lib/site-metadata.test.ts
  - apps/web/app/rss.xml/route.ts
autonomous: true
requirements: [SEO-02, FEED-01]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "FEED-01: `/rss.xml` returns valid no-store RSS 2.0 for at most the newest 20 predicate-visible articles, with escaped summaries and permanent same-origin link/guid values."
    - "SEO-02 foundation: one strict public distribution DTO uses `publicPredicate`; no non-public row, raw Markdown, storage key, session, or admin field crosses it."
    - "A focused Phase 3 command creates/migrates a generated disposable database, provides its URL, executes semantic tests, and fails on skip or zero tests."
    - "Externally visible URLs derive from validated `PUBLIC_ORIGIN`; `INTERNAL_API_ORIGIN` remains server-fetch-only."
  artifacts:
    - path: scripts/local-verify.mjs
      provides: generated Phase 3 API and managed-browser verification seams
    - path: packages/contracts/src/distribution.ts
      provides: strict public distribution allowlist
      exports: [publicDistributionSchema]
    - path: apps/api/src/content/public-repository.ts
      provides: publicPredicate-owned discovery snapshot
    - path: apps/web/app/rss.xml/route.ts
      provides: safe same-origin RSS Route Handler
  key_links:
    - from: scripts/local-verify.mjs
      to: apps/api/test/public-distribution.test.ts
      via: generated migrated `PHASE3_TEST_DATABASE_URL` and skip/zero-test rejection
      pattern: "phase3-api|PHASE3_TEST_DATABASE_URL"
    - from: apps/api/src/content/public-repository.ts
      to: apps/api/src/routes/public-posts.ts
      via: publicPredicate distribution method parsed through the shared schema
      pattern: "publicPredicate|publicDistributionSchema"
    - from: apps/web/app/rss.xml/route.ts
      to: apps/web/app/lib/api.ts
      via: server-only no-store `/public/distribution` fetch
      pattern: "getPublicDistribution|/public/distribution"
  prohibitions:
    - statement: "Public distribution and RSS must disclose none of any non-public article's content or persistence-only data."
      status: resolved
      verification: test
    - statement: "Public URLs must not derive from an internal API origin, cloud address, or hardcoded production hostname."
      status: resolved
      verification: test
    - statement: "A missing database URL, skipped semantic suite, or zero executed tests must not produce a passing verification."
      status: resolved
      verification: test
    - statement: "No dependency, package install, external API, SDK, service, CDN, webhook, remote credential, or cloud request is allowed."
      status: resolved
      verification: judgment
---

<objective>
Establish the public distribution/RSS contract and the non-skippable local Phase 3 verification substrate.

Purpose: Deliver a thin publication-only distribution path and remove ambient-database ambiguity before metadata and export expand it.
Output: Generated disposable runner, strict public projection, validated-origin RSS, and focused tests.
</objective>

<scope_rationale>
Accepted scope warning: this plan names 13 total files, above the 10-file advisory threshold, because they form one cohesive, independently verifiable public-distribution slice: the generated database/test topology proves the strict API projection that the same plan renders as RSS. Splitting again would fragment ownership of the generated verification topology and allow its fail-closed database, origin, skip/zero-test, and cleanup contract to diverge from the first feature that proves it. The scope remains bounded to 3 focused tasks, every task touches at most 5 files, and the calibrated estimate is a low-context 38% (38,000 tokens, low confidence).
</scope_rationale>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-distribution-and-portability/03-RESEARCH.md
@.planning/phases/03-distribution-and-portability/03-PATTERNS.md
@.planning/phases/03-distribution-and-portability/COVERAGE.md
@apps/web/AGENTS.md

<interfaces>
- `publicPredicate` remains `published AND deletedAt IS NULL AND publishedAt IS NOT NULL` and is the sole article-publicity authority.
- Public order remains `publishedAt DESC, UUID DESC`; RSS takes the first 20 after repository ordering.
- `PublicResult<T>` remains `ok | not_found | upstream_error`; malformed/failed distribution responses are upstream failures.
- `publicPostRoutes` is already app-registered; `/public/distribution` is added there to keep the tracer slice bounded.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a generated Phase 3 database through one non-skippable semantic test</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs, compose.yaml, apps/web/Dockerfile, apps/api/test/public-distribution.test.ts</files>
  <read_first>
    <item>`scripts/local-verify.mjs` generated namespace, migrations, database suites, generated credentials/origin, log audit, and bounded cleanup.</item>
    <item>`scripts/local-verify.test.mjs`, `compose.yaml`, and `apps/web/Dockerfile` for rejection fixtures and build/runtime environment ownership.</item>
    <item>`apps/api/test/phase2-public-visibility.test.ts` as DB setup analog, while replacing its missing-URL skip with a failure.</item>
  </read_first>
  <behavior>
    - "`--phase3-api` creates and migrates a generated disposable database, injects `PHASE3_TEST_DATABASE_URL`, and executes a real create/read sentinel."
    - "Missing URL, TAP skip, or zero semantic tests fails the command."
    - "Named `--phase3-metadata`, `--phase3-export-api`, `--phase3-export-browser`, and `--phase3-full` selections reuse this topology and are covered by option/suite-selection fixtures before later feature files fill them."
    - "Generated PUBLIC_ORIGIN remains distinct from INTERNAL_API_ORIGIN; cleanup stays exact to generated targets."
  </behavior>
  <action>
    Extend the canonical launcher first. Add `--phase3-api` so it owns a generated namespace/database, migrates it, provides the test URL, captures TAP, and fails on skips or zero semantic tests. Start the new suite with a real migrated-schema create/read sentinel and throw when configuration is absent. Establish named `--phase3-metadata`, `--phase3-export-api`, `--phase3-export-browser`, and `--phase3-full` selections on the same topology, using generated origin, credentials, and run ID; operations tests must prove deterministic option/suite selection even before later feature files fill those paths. Pass `PUBLIC_ORIGIN` separately through Compose/Web build/runtime, retain internal API routing, secret redaction, and exact cleanup, and reject cloud/non-loopback origins before requests.
  </action>
  <verify>
    <automated>corepack pnpm test:ops &amp;&amp; corepack pnpm local:verify -- --phase3-api</automated>
  </verify>
  <done>One command proves generated DB creation/migration, semantic execution, no-skip enforcement, and bounded cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the sentinel to the strict public distribution contract</name>
  <files>packages/contracts/src/distribution.ts, packages/contracts/src/index.ts, apps/api/src/content/public-repository.ts, apps/api/src/routes/public-posts.ts, apps/api/test/public-distribution.test.ts</files>
  <read_first>
    <item>`03-PATTERNS.md` public contract, repository selection, Fastify response, and non-skippable API sections.</item>
    <item>`packages/contracts/src/public-posts.ts`, `apps/api/src/content/public-repository.ts`, and `apps/api/src/routes/public-posts.ts` for strict schemas, transactions, ordering, and routing.</item>
    <item>The Task 1 runner/test seam; retain its failure semantics while replacing the sentinel with feature assertions.</item>
  </read_first>
  <behavior>
    - "Published rows appear in stable order; draft, unpublished, soft-deleted, and null-publication marker strings are absent."
    - "Only strict public article/taxonomy/About facts cross the response; Markdown/private keys are rejected."
    - "Malformed repository output fails strict parsing rather than being stripped or returned."
  </behavior>
  <action>
    Replace/expand the sentinel with failing all-lifecycle cases. Create/export strict distribution schemas. Add a repeatable-read `distribution()` method whose article query literally uses `publicPredicate`, explicit public columns, stable ordering, deterministic public term relations/counts, and only published About. Add GET `/public/distribution` to the registered public-post plugin and parse the exact outgoing DTO. Retain Task 1's missing-URL failure and runner skip/zero-test rejection.
  </action>
  <verify>
    <automated>corepack pnpm --filter @blog-x/contracts test &amp;&amp; corepack pnpm local:verify -- --phase3-api</automated>
  </verify>
  <done>The disposable database proves every and only predicate-visible discovery fact crosses the strict route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the distribution contract as safe same-origin RSS</name>
  <files>apps/web/app/lib/api.ts, apps/web/app/lib/site-metadata.ts, apps/web/app/lib/site-metadata.test.ts, apps/web/app/rss.xml/route.ts</files>
  <read_first>
    <item>Read `apps/web/AGENTS.md`, then installed Next 16.3 metadata and Route Handler docs named in `03-PATTERNS.md` immediately before Web edits.</item>
    <item>`apps/web/app/lib/api.ts#getPublic` for internal-origin/no-store/strict-result semantics.</item>
    <item>`03-PATTERNS.md` validated origin, RSS serializer, hostile XML, and Route Handler sections.</item>
  </read_first>
  <behavior>
    - "The public-origin helper accepts only absolute HTTP(S) origins without credentials, non-root path, query, or fragment; production missing/invalid config fails closed."
    - "Hostile XML text and disallowed controls yield well-formed escaped RSS without Markdown/rendered HTML."
    - "RSS emits at most 20 ordered items, RFC-822 dates, identical permanent link/guid values, RSS content type, and no-store."
  </behavior>
  <action>
    Read installed Next 16.3 docs first. Add `getPublicDistribution()` through the server-only helper. Create validated public-origin/URL and pure RSS helpers; remove disallowed controls, escape every interpolated value including URLs, use summaries, and cap the ordered DTO at 20. Implement dynamic GET `/rss.xml` with the installed Route Handler signature. Do not import API/DB modules, expose the internal origin, add a dependency, or emit Markdown HTML.
  </action>
  <verify>
    <automated>corepack pnpm --filter @blog-x/web exec tsx --test app/lib/site-metadata.test.ts &amp;&amp; corepack pnpm --filter @blog-x/web typecheck</automated>
  </verify>
  <done>One predicate-visible article is representable as valid same-origin RSS while hostile text and internal topology remain inert/absent.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high.

## Trust Boundaries

| Boundary | Sensitive flow |
|---|---|
| Local verifier → database/services | Generated URLs, credentials, namespaces, and cleanup targets cross process/container boundaries. |
| PostgreSQL → Fastify public projection | Retained source becomes public only through `publicPredicate` and strict selection. |
| Fastify → Next → RSS consumer | Strict JSON and untrusted text become public XML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01-01 | Tampering | verification result | high | mitigate | Task 1 owns a migrated disposable DB and rejects missing URL, skip, or zero tests. |
| T-03-01-02 | Information disclosure | public distribution/RSS | high | mitigate | Tasks 2-3 use publicPredicate, explicit schemas, hidden-marker tests, and origin separation. |
| T-03-01-03 | Tampering | RSS XML | high | mitigate | Task 3 removes invalid controls, escapes all values, and excludes HTML authority. |
| T-03-01-04 | Denial of service | distribution/feed | medium | mitigate | Narrow columns, stable order, no rendering/media bytes, and 20-item feed cap. |
| T-03-01-05 | Information disclosure | verifier origin/logs | high | mitigate | Task 1 uses loopback generated origins, secret redaction, and bounded cleanup. |
</threat_model>

<verification>
Run `corepack pnpm local:verify -- --phase3-api`, then the RSS pure tests. Passing requires a migrated disposable database, non-skipped distribution semantics, valid XML behavior, local origins, and bounded cleanup.
</verification>

<success_criteria>
- SEO-02 has a publication-only distribution source for later Sitemap work.
- FEED-01 has a valid, escaped, stable same-origin RSS representation.
- Phase 3 API semantics cannot pass because the database was absent or the test skipped.
- No external integration/dependency or cloud host participates.
</success_criteria>

<output>
Create `.planning/phases/03-distribution-and-portability/03-01-SUMMARY.md` when complete.
</output>
