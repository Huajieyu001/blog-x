---
phase: 03-distribution-and-portability
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/web/app/lib/site-metadata.ts
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/posts/[slug]/page.tsx
  - apps/web/app/categories/page.tsx
  - apps/web/app/categories/[slug]/page.tsx
  - apps/web/app/tags/page.tsx
  - apps/web/app/tags/[slug]/page.tsx
  - apps/web/app/archives/page.tsx
  - apps/web/app/about/page.tsx
  - apps/web/app/robots.ts
  - apps/web/app/sitemap.ts
  - apps/web/e2e/phase3-distribution.spec.ts
autonomous: true
requirements: [SEO-01, SEO-02, FEED-01]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "SEO-01: every public route family emits a route-specific non-empty title/description, exactly one canonical, and complete Open Graph title/description/type/url/siteName."
    - "Canonical policy is exact: absent or exact scalar `page=1` uses the query-free base; only exact scalar base-10 `2..totalPages` self-canonicalizes; repeated/array, extra, leading-zero, invalid, and out-of-range shapes are `noindex,follow`."
    - "SEO-02: robots advertises the canonical Sitemap; Sitemap contains only indexable static routes, published About, predicate-visible articles/terms, and exact real pages."
    - "Dynamic metadata preserves strict `not_found` versus `upstream_error`; no outage or malformed DTO becomes an indexable 404/canonical page."
    - "Managed Playwright receives runner-generated Web/API/DB topology, credentials, origin, and run ID; missing inputs or a skipped journey fail."
  artifacts:
    - path: apps/web/app/lib/site-metadata.ts
      provides: canonical classification and metadata composition
    - path: apps/web/app/robots.ts
      provides: Next 16 crawler policy
    - path: apps/web/app/sitemap.ts
      provides: dynamic publication-only Sitemap
    - path: apps/web/e2e/phase3-distribution.spec.ts
      provides: managed semantic metadata/discovery journey
  key_links:
    - from: apps/web/app/lib/site-metadata.ts
      to: apps/web/app/layout.tsx
      via: validated origin supplies metadataBase and shared defaults
      pattern: "PUBLIC_ORIGIN|metadataBase"
    - from: apps/web/app/sitemap.ts
      to: apps/web/app/lib/api.ts
      via: 03-01 strict public distribution fetch
      pattern: "getPublicDistribution"
    - from: apps/web/e2e/phase3-distribution.spec.ts
      to: scripts/local-verify.mjs
      via: required generated origin/credentials/run-id and managed service lifecycle
      pattern: "E2E_WEB_ORIGIN|E2E_ADMIN_USERNAME|E2E_RUN_ID"
  prohibitions:
    - statement: "Metadata, robots, Sitemap, RSS, and HTML must disclose no hidden lifecycle marker or internal/cloud origin."
      status: resolved
      verification: test
    - statement: "Only exact canonical pagination shapes may be indexable or appear in Sitemap; all listed noncanonical shapes must be noindex,follow."
      status: resolved
      verification: test
    - statement: "Playwright must not default credentials/origin, skip on missing inputs, use an ambient service, or call PostgreSQL/Fastify/cloud hosts directly from the browser."
      status: resolved
      verification: test
    - statement: "Installed Next 16 documentation must be read before Web edits; no remembered signature, handwritten head serialization, dependency, static snapshot, or external service may replace it."
      status: resolved
      verification: judgment
---

<objective>
Expand the 03-01 distribution tracer into complete public metadata, robots, and Sitemap behavior under a managed browser topology.

Purpose: Satisfy SEO-01/SEO-02 with one explicit canonical policy, publication-only discovery, and true absence/outage semantics.
Output: Metadata for every route family, Next-owned robots/Sitemap, and non-skippable Playwright evidence.
</objective>

<scope_rationale>
Accepted scope warning: this plan names 13 total files, above the 10-file advisory threshold, because they form one cohesive, independently verifiable cross-route metadata/discovery slice: one canonical classifier and managed browser journey prove metadata, robots, and Sitemap behavior across every existing public route family. Splitting again would fragment the cross-route contract and duplicate or sequence changes to the shared helper and generated Playwright evidence, making canonical/indexability rules easier to drift between route families. The scope remains bounded to 3 focused tasks, every task touches at most 5 files, and the calibrated estimate is a low-context 44% (44,000 tokens, low confidence).
</scope_rationale>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-distribution-and-portability/03-RESEARCH.md
@.planning/phases/03-distribution-and-portability/03-PATTERNS.md
@.planning/phases/03-distribution-and-portability/COVERAGE.md
@.planning/phases/03-distribution-and-portability/03-01-SUMMARY.md
@apps/web/AGENTS.md

<interfaces>
- Use 03-01 `getPublicDistribution`, validated public URL helpers, and generated `--phase3-metadata` topology.
- `publicPostPageSize` remains 10 and public query parsing remains strict positive base-10.
- Only a parsed `{error:'not_found'}` is absence; connection/5xx/malformed results are upstream failures.
- Next 16 metadata values are shallow-merged, so each route emits a complete nested Open Graph object.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace home and article data into rendered metadata under managed Playwright</name>
  <files>apps/web/app/lib/site-metadata.ts, apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/app/posts/[slug]/page.tsx, apps/web/e2e/phase3-distribution.spec.ts</files>
  <read_first>
    <item>Read `apps/web/AGENTS.md`, then installed Next 16.3 metadata/generateMetadata docs listed in `03-PATTERNS.md` immediately before edits.</item>
    <item>`03-PATTERNS.md` static/dynamic metadata, exact URL policy, Playwright requirements, and error classification.</item>
    <item>Existing home/article pages and `apps/web/e2e/phase2-reading.spec.ts` as rendering and exact-origin analogs.</item>
  </read_first>
  <behavior>
    - "Root/home/article emit route-specific title, description, canonical, and complete Open Graph values; post description prefers SEO description, then summary, then fallback."
    - "Absent and exact scalar page=1 canonicalize to `/`; exact real page 2 self-canonicalizes; malformed/repeated/extra/leading-zero/out-of-range variants are noindex,follow."
    - "Verified missing article is 404; upstream/malformed responses fail without a misleading canonical."
    - "The E2E throws on missing runner inputs and asserts every HTTP(S) browser request uses the generated Web origin."
  </behavior>
  <action>
    Read installed docs first. Extend pure metadata/canonical helpers and root `metadataBase`, title template/default, description, Open Graph defaults, and RSS alternate. Wire home/article `generateMetadata` to the same strict page-data semantics. Implement the resolved policy exactly: query-free base for absent/exact scalar page=1; self-canonical only for a single exact base-10 page in `2..totalPages`; repeated/array values, extra keys, leading zeroes, empty/non-base-10/invalid values, and out-of-range values get `noindex,follow`. Create the Playwright journey with required generated origin/credentials/run ID and no skip/default path; publish fixtures through visible same-origin UI and assert semantic head output.
  </action>
  <verify>
    <automated>corepack pnpm local:verify -- --phase3-metadata</automated>
  </verify>
  <done>Home and one real article have correct managed-browser metadata/canonicals while every noncanonical and error case is classified safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand exact metadata and pagination policy across taxonomy routes</name>
  <files>apps/web/app/categories/page.tsx, apps/web/app/categories/[slug]/page.tsx, apps/web/app/tags/page.tsx, apps/web/app/tags/[slug]/page.tsx, apps/web/e2e/phase3-distribution.spec.ts</files>
  <read_first>
    <item>Installed Next 16 metadata docs again immediately before these Web edits, plus `03-PATTERNS.md` metadata coverage table.</item>
    <item>Existing category/tag pages for `PublicResult`, pagination, absence, and upstream branches.</item>
    <item>Task 1 canonical helper and managed E2E patterns; extend rather than duplicate them.</item>
  </read_first>
  <behavior>
    - "Category/tag list and detail pages have unique titles/descriptions/canonicals/Open Graph values."
    - "Detail pages apply the identical resolved scalar/extra/array/leading-zero/invalid/out-of-range canonical/noindex policy using real totalPages."
    - "Unknown term is 404; term upstream/malformed failures are errors and emit no misleading canonical."
  </behavior>
  <action>
    Re-read installed docs and apply the shared complete metadata helper to category/tag list/detail routes. Derive term descriptions and total page count only from strict public results. Use the Task 1 canonical classifier unchanged so taxonomy routes cannot drift. Extend the same required-input Playwright journey with public and hidden term fixtures, exact metadata, page 1/base equivalence, page 2 self-canonical, every invalid query class, verified missing terms, and upstream distinction.
  </action>
  <verify>
    <automated>corepack pnpm local:verify -- --phase3-metadata</automated>
  </verify>
  <done>All taxonomy routes share the exact metadata/indexability contract and managed browser evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete archives/About metadata and publication-only robots/Sitemap</name>
  <files>apps/web/app/archives/page.tsx, apps/web/app/about/page.tsx, apps/web/app/robots.ts, apps/web/app/sitemap.ts, apps/web/e2e/phase3-distribution.spec.ts</files>
  <read_first>
    <item>Read installed Next 16.3 robots and sitemap metadata-file docs named in `03-PATTERNS.md` immediately before edits.</item>
    <item>`03-PATTERNS.md` metadata coverage/Sitemap decisions and 03-01 public distribution interface.</item>
    <item>Existing archives/About absence/upstream behavior and the current managed Phase 3 E2E.</item>
  </read_first>
  <behavior>
    - "Archives and published About have complete unique metadata; unpublished About is absent and excluded."
    - "Robots allows public paths, disallows admin/login/api, and names the canonical Sitemap."
    - "Sitemap contains root, lists, archives, published About, public posts/terms, and only query-free page 1 plus exact real pages 2..N."
    - "Hidden markers and all noncanonical URL shapes are absent from robots/Sitemap/RSS/HTML."
  </behavior>
  <action>
    Read installed metadata-file docs first. Add complete archives/About metadata preserving strict absence/outage semantics. Implement dynamic Next `robots.ts` and `sitemap.ts` from the validated public origin and 03-01 distribution DTO. Enumerate the query-free base and exact pages `2..totalPages`; never emit page=1, repeated/extra/invalid/out-of-range variants, media/feed/private endpoints, hidden content, or invented timestamps. Extend managed E2E to parse responses, assert exact allow/disallow/Sitemap sets, RSS alternate/feed behavior, lifecycle secrecy, and runtime reflection without rebuild.
  </action>
  <verify>
    <automated>corepack pnpm local:verify -- --phase3-metadata</automated>
  </verify>
  <done>SEO-01/SEO-02 and feed discoverability are semantically proven for every current public route family under the resolved canonical policy.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high.

## Trust Boundaries

| Boundary | Sensitive flow |
|---|---|
| Fastify public DTO → Next metadata | Parsed public facts become titles, descriptions, canonical/OG URLs, and indexability directives. |
| Next → crawler/browser | Configured origin and lifecycle-derived URLs become robots and Sitemap output. |
| Runner → Playwright | Generated credentials/origin/run ID and managed service topology enter the browser journey. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-02-01 | Information disclosure | metadata/Sitemap | high | mitigate | Tasks 1-3 consume strict public DTOs and assert hidden markers absent everywhere. |
| T-03-02-02 | Tampering | canonical/indexability | high | mitigate | One pure classifier covers exact scalar/query/range rules and is exercised across route families. |
| T-03-02-03 | Information disclosure | public URLs | high | mitigate | All outputs derive from validated PUBLIC_ORIGIN; exact-origin listener rejects internal/cloud mixing. |
| T-03-02-04 | Tampering | absence/outage metadata | high | mitigate | Tasks preserve parsed not-found versus upstream failure in page and metadata paths. |
| T-03-02-05 | Tampering | Playwright evidence | high | mitigate | Runner-owned topology and required env fail on missing inputs/skip/zero tests. |
</threat_model>

<verification>
Run `corepack pnpm local:verify -- --phase3-metadata`. It must manage database/API/Web lifecycle and generated credentials/origin, then execute a non-skipped one-worker browser journey covering head metadata, canonical/noindex variants, robots, Sitemap, RSS, lifecycle secrecy, and error classification.
</verification>

<success_criteria>
- SEO-01 covers every current public route family with unique complete metadata.
- SEO-02 exposes Next-owned robots/Sitemap with exactly the resolved public canonical set.
- The canonical policy handles absent/exact page 1, exact pages 2..N, and every prohibited query shape exactly.
- Browser evidence cannot pass through missing topology inputs, skips, ambient services, or cloud/external requests.
</success_criteria>

<output>
Create `.planning/phases/03-distribution-and-portability/03-02-SUMMARY.md` when complete.
</output>
