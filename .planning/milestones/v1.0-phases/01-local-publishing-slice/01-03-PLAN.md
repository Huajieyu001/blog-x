---
phase: 01-local-publishing-slice
plan: "03"
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - packages/contracts/src/auth.ts
  - packages/contracts/src/index.ts
  - apps/api/src/app.ts
  - apps/api/src/auth/sessions.ts
  - apps/api/src/db/seed-admin.ts
  - apps/api/src/routes/auth.ts
  - apps/api/test/auth-session.test.ts
  - apps/web/app/lib/api.ts
  - apps/web/app/login/page.tsx
  - apps/web/app/admin/layout.tsx
  - apps/web/e2e/auth-session.spec.ts
autonomous: true
requirements: [AUTH-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Exactly one seed-created administrator can log in; no registration, role, OAuth, or public recovery surface exists."
    - "Opaque sessions survive navigation, rotate on login, expire/revoke server-side, and are removed on logout."
    - "Fastify authorizes every protected request; Next redirects are presentation only."
  artifacts:
    - path: apps/api/src/auth/sessions.ts
      provides: "Opaque session issue/lookup/revoke authority"
    - path: apps/api/src/routes/auth.ts
      provides: "Login/session/logout routes"
    - path: apps/web/e2e/auth-session.spec.ts
      provides: "Browser session lifecycle proof"
  key_links:
    - from: apps/web/app/login/page.tsx
      to: apps/api/src/routes/auth.ts
      via: "relative /api/auth/login"
      pattern: "/api/auth/login"
    - from: apps/api/src/routes/auth.ts
      to: apps/api/src/auth/sessions.ts
      via: "session issue/status/revoke"
      pattern: "session"
  assumptions:
    - "AUTH-01 unavailable states use generic 401 behavior and reveal neither administrator identity nor session reason."
  prohibitions:
    - requirement_id: AUTH-01
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT expand the single-owner identity boundary into registration, multiple administrators/roles, OAuth, or public password recovery."
---

<objective>
Extract and complete the single-administrator authentication/session slice proven by the tracer.

Purpose: Make Fastify authorization comprehensive before more management endpoints are added.
Output: Auth contracts, seed, session service/routes, protected layouts, and API/browser lifecycle tests.
</objective>

## Phase Goal

**As a** sole administrator, **I want to** maintain a secure server-side login session, **so that** only I can reach management functions.

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-publishing-slice/01-CONTEXT.md
@.planning/phases/01-local-publishing-slice/01-RESEARCH.md
@.planning/phases/01-local-publishing-slice/SKELETON.md
@.planning/phases/01-local-publishing-slice/01-02-SUMMARY.md
@AGENTS.md
@docs/INFRASTRUCTURE.md
</context>

## Artifacts Produced

- Eleven-file auth/session extraction covering seed, contracts, API authority, web presentation, and browser proof.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete opaque session issue, validation, rotation, expiry, and revocation</name>
  <files>packages/contracts/src/auth.ts, packages/contracts/src/index.ts, apps/api/src/app.ts, apps/api/src/auth/sessions.ts, apps/api/src/db/seed-admin.ts, apps/api/src/routes/auth.ts, apps/api/test/auth-session.test.ts</files>
  <read_first>
    - REQUIREMENTS.md — CMS-001 and §6.3 authentication/security expectations
    - .planning/phases/01-local-publishing-slice/01-CONTEXT.md — D-04..D-06
    - .planning/phases/01-local-publishing-slice/01-RESEARCH.md — opaque-session pattern and cookie pitfalls
    - .planning/phases/01-local-publishing-slice/01-02-SUMMARY.md — actual tracer contract exports
    - apps/api/src/app.ts — consolidated tracer behavior to extract
    - apps/api/src/db/schema.ts — administrator/session columns
  </read_first>
  <behavior>
    - "Seed reads username/password from validated runtime input, stores Argon2id, is idempotent for the same sole account, and logs no credential."
    - "Wrong credentials and absent/expired/revoked tokens return generic 401."
    - "Successful login rotates the previous session and stores only the token digest; session status survives refresh."
    - "Logout revokes the row, clears the cookie, and old-token reuse fails."
    - "Cookie is HttpOnly, SameSite=Lax, Path=/, no Domain, Secure under HTTPS; auth responses are no-store."
  </behavior>
  <action>Write the injection tests first, then extract tracer auth into focused contracts/service/routes. Keep exactly one administrator row and make the seed refuse an attempt to create a second identity. Use Argon2id, CSPRNG tokens, SHA-256 digest-at-rest, expiry and explicit revocation. Rotate an existing browser session at login and delete/revoke at logout. Register the auth plugin in `app.ts`; redact credentials and Cookie/Set-Cookie values. Reject untrusted Origin on unsafe methods. Do not add identity features excluded by D-04.</action>
  <verify>
    <automated>pnpm --filter @blog-x/api test -- auth-session</automated>
  </verify>
  <acceptance_criteria>
    - All login/session/logout cases pass against migrated PostgreSQL.
    - Only a digest is persisted and auth logs contain no secret/token/cookie value.
    - Seed cannot introduce a second administrator.
  </acceptance_criteria>
  <done>Fastify has one complete, tested server-side session authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver login, protected navigation, and logout through the browser</name>
  <files>apps/web/app/lib/api.ts, apps/web/app/login/page.tsx, apps/web/app/admin/layout.tsx, apps/web/e2e/auth-session.spec.ts</files>
  <read_first>
    - REQUIREMENTS.md — protected management scenario
    - .planning/phases/01-local-publishing-slice/01-CONTEXT.md — D-05 session UX
    - packages/contracts/src/auth.ts — Task 1 wire contract
    - apps/api/src/routes/auth.ts — Task 1 endpoints
    - apps/web/app/TracerAdmin.tsx — proven relative-fetch behavior
  </read_first>
  <behavior>
    - "Browser login uses relative /api, keeps the cookie outside JavaScript, and navigates to /admin."
    - "Protected admin navigation redirects to /login when session status is unauthorized, but direct API writes still depend on Fastify."
    - "Refresh preserves a valid session; logout returns to login and the previous cookie cannot reopen admin."
  </behavior>
  <action>Create the dedicated login page and protected admin layout, preserving relative browser requests and a server-only internal API origin for SSR checks. Forward only the inbound Cookie header when checking session server-side. Surface generic login errors and pending state without token/profile storage. Add visible logout behavior. Keep Fastify as the only authorization authority and prove direct API denial after the UI redirect/logout.</action>
  <verify>
    <automated>pnpm exec playwright test apps/web/e2e/auth-session.spec.ts --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - Wrong login, valid login, refresh, logout, expired session, and revoked-token reuse pass in a real browser.
    - Browser storage contains no credential/session token.
    - All browser calls remain relative `/api`.
  </acceptance_criteria>
  <done>The sole administrator has a durable, protected browser session with clear logout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| credentials/cookie → Fastify | Spoofing and credential disclosure |
| Next protected layout → Fastify | Mistaking presentation redirect for authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Spoofing | sessions | high | mitigate | CSPRNG/digest/expiry/revocation/rotation tests. |
| T-03-02 | Information disclosure | auth logs/DTO | high | mitigate | Redaction and allowlisted contract. |
| T-03-03 | Elevation of privilege | protected routes | high | mitigate | Fastify guard remains authoritative. |
| T-03-04 | Tampering | unsafe cookie request | medium | mitigate | SameSite plus trusted-Origin validation. |
</threat_model>

## Source Audit

Full set-wide audit: `SKELETON.md#canonical-source-audit`.

| Source | IDs owned here | Status |
|---|---|---|
| REQ | AUTH-01 | COVERED |
| RESEARCH | opaque sessions and Argon2id | COVERED |
| CONTEXT | D-04, D-05, D-06 | COVERED |

<verification>API session suite and browser auth journey pass.</verification>
<success_criteria>Every management boundary now depends on the sole Fastify session authority.</success_criteria>
<output>Create `.planning/phases/01-local-publishing-slice/01-03-SUMMARY.md`.</output>
