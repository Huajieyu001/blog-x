---
phase: 01-local-publishing-slice
plan: "08"
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - package.json
  - .env.example
  - compose.yaml
  - playwright.config.ts
  - scripts/check-boundaries.mjs
  - scripts/local-verify.mjs
  - apps/api/Dockerfile
  - apps/web/Dockerfile
  - apps/web/e2e/phase1-publishing.spec.ts
  - README.md
autonomous: true
requirements: [AUTH-01, CONT-01, CONT-02, CONT-03, READ-01, READ-02, OPS-04]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One documented command starts isolated local web/API/PostgreSQL, activates generated migrations, seeds a generated-secret administrator, and verifies all seven Phase 1 requirements."
    - "Concurrent migrations serialize, interrupted verification can be rerun without destructive reset, and parallel verification namespaces cannot corrupt each other."
    - "Boundary checks prove web has no database/session/lifecycle/Markdown ownership and browser code has no server public IP."
    - "The complete browser journey proves every lifecycle visibility transition, metadata, pagination, and safe permalink rendering."
  artifacts:
    - path: scripts/local-verify.mjs
      provides: "Canonical isolated, rerunnable Phase 1 verification command"
    - path: scripts/check-boundaries.mjs
      provides: "Deployment-separation and freeze guard"
    - path: apps/web/e2e/phase1-publishing.spec.ts
      provides: "Whole-phase real-browser acceptance"
    - path: README.md
      provides: "Local prerequisites/start/health/migrate/seed/test/stop instructions"
  key_links:
    - from: scripts/local-verify.mjs
      to: apps/web/e2e/phase1-publishing.spec.ts
      via: "migration-activated one-worker Playwright run"
      pattern: "phase1-publishing"
    - from: scripts/check-boundaries.mjs
      to: apps/web
      via: "source/import/address policy scan"
      pattern: "apps/web"
  assumptions:
    - "Plan 01-08 is the sole owner of OPS-04 migration concurrency, interruption/retry, and parallel-run acceptance; Plan 01-01 proves only clean activation before its seeded tracer."
    - "Docker Compose or a compatible local container runtime is an explicit prerequisite; no remote server is a fallback."
  prohibitions: []
---

<objective>
Make Phase 1 reproducible from local startup through whole-browser acceptance, including interruption and parallel safety.

Purpose: Turn the completed slices into one deterministic OPS-04 developer workflow and final seven-requirement proof.
Output: Container definitions, boundary/local verification runners, full browser journey, and concise operating documentation.
</objective>

## Phase Goal

**As a** developer, **I want to** verify the complete publishing system locally with one rerunnable command, **so that** Phase 1 is proven without any production-server dependency.

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-publishing-slice/01-CONTEXT.md
@.planning/phases/01-local-publishing-slice/01-RESEARCH.md
@.planning/phases/01-local-publishing-slice/SKELETON.md
@.planning/phases/01-local-publishing-slice/01-07-SUMMARY.md
@AGENTS.md
@docs/INFRASTRUCTURE.md
</context>

## Artifacts Produced

- Ten-file local operations/acceptance slice with deterministic migration ordering, isolation, full browser coverage, and freeze-safe documentation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the isolated, interruption-safe local verification runner and boundary gate</name>
  <files>package.json, .env.example, compose.yaml, playwright.config.ts, scripts/check-boundaries.mjs, scripts/local-verify.mjs, apps/api/Dockerfile, apps/web/Dockerfile</files>
  <read_first>
    - REQUIREMENTS.md — deployment boundary, local isolation, secrets, and security constraints
    - .planning/REQUIREMENTS.md — OPS-04 exact requirement
    - .planning/phases/01-local-publishing-slice/01-RESEARCH.md — environment availability, migration ordering, same-origin and startup pitfalls
    - .planning/phases/01-local-publishing-slice/01-07-SUMMARY.md — actual final build/test commands
    - apps/api/src/app.ts — migrate/seed/server entry modes
    - apps/api/drizzle/0000_phase1_walking_skeleton.sql — generated schema artifact
    - compose.yaml — tracer services to harden
    - docs/INFRASTRUCTURE.md — production freeze and target browser path
  </read_first>
  <behavior>
    - "Clean local verification validates prerequisites/env, starts only web/API/PostgreSQL, waits for health, migrates, verifies schema, seeds, and tests."
    - "Two migration processes serialize and leave one ledger row; a killed run can be retried without resetting the volume."
    - "Parallel runs use distinct validated Compose/database namespaces and neither can clean up the other's resources."
    - "Boundary gate rejects database/auth/lifecycle/renderer imports in web, browser-visible server IPs, tracked secrets, and commands targeting 47.99.80.8."
    - "Captured logs contain no admin password, token, Cookie/Set-Cookie value, or database credential."
  </behavior>
  <action>Start with failure fixtures for boundary/redaction/isolation checks. Finalize resource-conscious web/API Dockerfiles and Compose health checks; PostgreSQL is not published beyond the local Compose network unless an explicitly local diagnostic profile is selected. `.env.example` lists names/non-secret development guidance, while the runner generates test credentials in process environment without printing them. `scripts/local-verify.mjs` chooses a collision-free validated project/database namespace, starts services, waits for health, runs two advisory-locked migrations concurrently, inspects the migration ledger/tables, seeds, then executes typecheck, boundary/API/browser suites. Add an automated termination after start/migration begins and prove rerun convergence without volume deletion. Add two parallel namespace runs and bounded cleanup that can target only the script-created names. `check-boundaries.mjs` structurally enforces the SKELETON ownership and production freeze; avoid fragile prose-only greps and assert dedicated known-bad fixtures fail.</action>
  <verify>
    <automated>pnpm local:verify -- --infrastructure-only --interruption-check --parallel-check</automated>
  </verify>
  <acceptance_criteria>
    - Clean, concurrent-migration, interruption/retry, and parallel-isolation cases pass with real schema inspection.
    - Cleanup cannot target an unvalidated/broad name and never resets a persistent volume to obtain a pass.
    - Boundary known-bad fixtures fail and the real tree passes.
    - Log-redaction assertions pass without emitting the tested secrets.
  </acceptance_criteria>
  <done>The complete local topology starts and recovers deterministically while enforcing deployment/security boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove all seven requirements in one browser journey and document exact commands</name>
  <files>apps/web/e2e/phase1-publishing.spec.ts, scripts/local-verify.mjs, package.json, README.md</files>
  <read_first>
    - REQUIREMENTS.md — §§5.1–5.2, §6 and end-to-end acceptance scenarios
    - .planning/REQUIREMENTS.md — exact Phase 1 requirement statements
    - .planning/ROADMAP.md — four Phase 1 success criteria
    - .planning/phases/01-local-publishing-slice/SKELETON.md — canonical source audit/invariants
    - .planning/phases/01-local-publishing-slice/01-03-SUMMARY.md — auth UI selectors/commands
    - .planning/phases/01-local-publishing-slice/01-05-SUMMARY.md — lifecycle UI selectors/commands
    - .planning/phases/01-local-publishing-slice/01-06-SUMMARY.md — list/pagination behavior
    - .planning/phases/01-local-publishing-slice/01-07-SUMMARY.md — permalink/404 behavior
  </read_first>
  <behavior>
    - "Browser logs in, saves all metadata plus code/table/quote/link/image Markdown, previews, publishes, finds the home card/page data, and opens the safe permalink."
    - "Browser edits while preserving publishedAt, confirms a published slug change, unpublishes, republishes, and soft-deletes."
    - "Draft/unpublished/deleted states are absent from home/count and return the same detail 404; published states appear on the next request."
    - "The journey uses visible controls and relative /api, not direct DB setup for the behavior under test."
  </behavior>
  <action>Write one independent whole-phase Playwright journey using visible UI controls; fixtures may create only the initial generated-secret administrator through the documented seed. Cover AUTH-01, every CONT-01 action, CONT-03 fields, CONT-02 state matrix, READ-01 metadata/pagination, READ-02 syntax/security, and OPS-04 relative/local path. Assert first publication time survives normal edit/republish, published slug needs confirmation, soft-deleted source/slug remains retained through an API/DB diagnostic owned by the runner, and public visibility changes immediately.

Wire this journey into the canonical `pnpm local:verify -- --full-phase` command after migration/schema checks. Document prerequisites, environment-file creation, start, health, generated migration, seed, full verify, and bounded stop commands in README, including expected local ports and Docker/Chromium troubleshooting. State that Phase 1 uses only local infrastructure; provide no SSH/SCP/deployment command and no fallback to 47.99.80.8 or 124.222.91.230.</action>
  <verify>
    <automated>pnpm local:verify -- --full-phase --interruption-check --parallel-check</automated>
  </verify>
  <acceptance_criteria>
    - The canonical command passes all seven Phase 1 requirement IDs from a clean isolated namespace.
    - Full journey exercises real browser controls and every public visibility transition.
    - README lets a developer start, health-check, migrate, seed, verify, and stop without undocumented steps.
    - No browser request or documented command targets either server public IP.
  </acceptance_criteria>
  <done>Phase 1 is reproducibly proven locally from startup through publishing and public reading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| verification runner → Compose/DB | Destructive cleanup, migration race, cross-run corruption |
| generated secrets → process/logs | Credential disclosure |
| repository commands → infrastructure | Frozen production access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Tampering | concurrent/interrupted migration | high | mitigate | Advisory lock, ledger/schema inspection, kill/retry proof. |
| T-08-02 | Tampering | parallel cleanup | high | mitigate | Validated unique namespaces and bounded cleanup tests. |
| T-08-03 | Information disclosure | test env/logs | high | mitigate | Generated env-only secrets and log-redaction assertions. |
| T-08-04 | Elevation of privilege | architecture boundary | high | mitigate | Structural web ownership/IP/freeze gate with known-bad fixtures. |
| T-08-05 | Denial of service | local test resources | low | accept | Three small services and one Chromium worker fit the declared local/2-core budget. |
</threat_model>

## Source Audit

Full set-wide audit: `SKELETON.md#canonical-source-audit`.

| Source | IDs owned here | Status |
|---|---|---|
| GOAL/REQ | AUTH-01, CONT-01, CONT-02, CONT-03, READ-01, READ-02, OPS-04 final acceptance | COVERED |
| RESEARCH | R-ARCH, R-SECURITY, and sole ownership of R-MIGRATE concurrency/interruption/startup acceptance | COVERED |
| CONTEXT | D-01..D-13 whole-phase journey | COVERED |

<verification>Run `pnpm local:verify -- --full-phase --interruption-check --parallel-check`.</verification>
<success_criteria>All seven requirements pass locally with retry/parallel safety and zero production-server dependency.</success_criteria>
<output>Create `.planning/phases/01-local-publishing-slice/01-08-SUMMARY.md`.</output>
