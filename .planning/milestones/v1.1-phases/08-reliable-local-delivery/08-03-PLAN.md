---
phase: 08-reliable-local-delivery
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - scripts/refresh-local.mjs
  - scripts/refresh-local-runtime-core.mjs
  - scripts/refresh-local-facts.mjs
  - scripts/refresh-local.test.mjs
  - ops/v1.1-local-delivery-evidence.json
autonomous: true
requirements:
  - DEVX-01
  - DEVX-02
  - DEVX-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The existing refresh plan requires the complete sanitized Phase 6/7 acceptance result after offline target inspection and before migration/cutover; acceptance failure leaves the serving runtime untouched (D-11, D-13, D-15, D-16, D-17)."
    - "Canonical post-cutover facts prove the same full Git SHA in API/Web labels, exactly three healthy services, exact project/port/volumes, same-origin health/search routes and an honest read-only representative reading outcome (D-03, D-16, D-19)."
    - "When public content exists, the verifier discovers the first strict public-list slug read-only and proves `/posts/<slug>` at the fixed origin; an empty public set is recorded explicitly without seeding or inventing content (D-15, D-16)."
    - "The version-1 receipt contains exact acceptance counts/digests, fixed-runtime projections, implementation revision and `releaseState: BLOCKED`, is atomically non-overwriting and verifies without changing its bytes (D-14, D-18)."
    - "Normal execution emits concise start/complete progress for each exact stage; every terminal failure prints the exact failed stage plus one mapped sanitized recovery action, including the shared D-08 seed pre-warm path, before final success prints revision, fixed URL, route/reading summary, actual counts, receipt path and `RELEASE BLOCKED` (D-05)."
    - "Resolved edge truth for DEVX-01: D-13/D-14 tests interruption at every terminal stage, proves pre-cutover non-mutation or exact post-cutover rollback, and leaves a bound non-overwriting claim/failure record."
    - "Resolved edge truth for DEVX-02: D-06 through D-09 test that offline target inspection completes before the acceptance/migration barrier and generated origins never appear in target labels or canonical facts."
    - "Resolved edge truth for DEVX-03: D-14/D-15/D-17 require one acceptance result bound to the same claimed revision; concurrent/repeated delivery and missing/partial acceptance cannot publish success."
  artifacts:
    - path: scripts/refresh-local.mjs
      provides: final phase order, mandatory pre-cutover complete acceptance and stage progress callbacks
      exports: ["createRefreshPlan", "runLocalRefresh", "runRefreshCli"]
    - path: scripts/refresh-local-runtime-core.mjs
      provides: acceptance execution/binding, exhaustive safe stage/recovery mapping, strict v1.1 receipt verifier and terminal delivery output
      exports: ["REFRESH_TERMINAL_STAGES", "SAFE_RECOVERY_BY_STAGE", "formatRefreshStageProgress", "safeRecoveryForRefreshFailure", "formatRefreshFailure", "assertAllowedRefreshCommand", "verifyRawRefreshEvidence", "runRefreshCliBoundary"]
    - path: scripts/refresh-local-facts.mjs
      provides: `/search` plus read-only representative reading fact and sanitized projection
      exports: ["assertReadingFact", "assertRouteFacts", "projectSanitizedFacts"]
    - path: scripts/refresh-local.test.mjs
      provides: complete phase order, route/reading, receipt, output, interruption, rollback and verifier regressions
    - path: ops/v1.1-local-delivery-evidence.json
      provides: sole current-revision fixed-3100 delivery receipt
      contains: '"releaseState": "BLOCKED"'
  key_links:
    - from: scripts/refresh-local.mjs
      to: scripts/local-delivery-acceptance.mjs
      via: required `accept-v1.1` plan phase before `migrate`
      pattern: "accept-v1.1.*migrate"
    - from: scripts/refresh-local-runtime-core.mjs
      to: scripts/local-delivery-acceptance.mjs
      via: exact allowlisted no-argument Node command and strict machine-result parser
      pattern: "scripts/local-delivery-acceptance.mjs|parseLocalDeliveryAcceptanceRecord"
    - from: scripts/refresh-local.mjs
      to: scripts/refresh-local-runtime-core.mjs
      via: `runRefreshCliBoundary` supplies one stage reporter to `runLocalRefresh`, then maps every terminal error through `safeRecoveryForRefreshFailure`; seed failures delegate to 08-01 `formatSeedPrewarmInstruction`
      pattern: "formatRefreshStageProgress|safeRecoveryForRefreshFailure|formatSeedPrewarmInstruction|runLocalRefresh"
    - from: scripts/refresh-local-runtime-core.mjs
      to: scripts/refresh-local-facts.mjs
      via: post-cutover reconstruction validates routes, reading fact, revision labels and persistence
      pattern: "assertRouteFacts|assertReadingFact|projectSanitizedFacts"
    - from: scripts/refresh-local-runtime-core.mjs
      to: ops/v1.1-local-delivery-evidence.json
      via: exclusive temp/fsync/link/directory-fsync publication followed by read-only reconstruction
      pattern: "LOCAL_DELIVERY_EVIDENCE_PATH|publishEvidence|verifyRawRefreshEvidence"
    - from: ops/v1.1-local-delivery-evidence.json
      to: compose.yaml
      via: sanitized post-cutover facts prove exact canonical project/services/volumes/ports and three health states
      pattern: "postCutover|portOwnerExact|containersHealthy"
  prohibitions:
    - requirement_id: DEVX-03
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT introduce or execute SSH, deployment, registry publication, cloud-server mutation or a production decision other than `BLOCKED`."
---

## Phase Goal

**As the** Blog X developer, **I want to** finish each major change with one evidence-producing fixed-runtime refresh, **so that** I can trust that the committed discovery experience is the code currently visible at `http://127.0.0.1:3100` while production remains frozen.

<objective>
Bind the complete generated acceptance result into the existing refresh before canonical mutation, extend read-only fixed-route evidence, seal the final receipt/output contract, then perform exactly one real delivery attempt for the clean implementation revision.

Purpose: complete DEVX-01..03 with one provenance chain from committed source through isolated acceptance and data-preserving cutover to independently verified fixed-3100 evidence.
Output: acceptance-aware refresh runtime, representative route facts, final receipt verifier/output and the generated v1.1 receipt.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-reliable-local-delivery/08-CONTEXT.md
@.planning/phases/08-reliable-local-delivery/08-RESEARCH.md
@.planning/phases/08-reliable-local-delivery/08-PATTERNS.md
@.planning/phases/08-reliable-local-delivery/08-01-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-02-SUMMARY.md
@package.json
@compose.yaml
@scripts/refresh-local.mjs
@scripts/refresh-local-runtime-core.mjs
@scripts/refresh-local-facts.mjs
@scripts/refresh-local.test.mjs
@scripts/local-delivery-acceptance.mjs
@scripts/local-delivery-acceptance.test.mjs
@packages/contracts/src/public-posts.ts

<interfaces>
- Plan 08-01 supplies the exact `local:deliver` entry, v1.1 path/format/kind, port owner and immutable offline target facts.
- Plan 08-02 supplies `parseLocalDeliveryAcceptanceRecord` for `blog-x-v1.1-local-delivery-acceptance` version 1 with exact counts/digests and `BLOCKED`.
- `createRefreshPlan().phases` is the single workflow authority; new acceptance is one phase in it, not a parallel delivery script.
- `/api/public/articles?page=1` returns strict `{page,pageSize,totalItems,totalPages,items[]}`; each item has a public `slug`, while public detail is `/posts/<slug>`.
- `verifyRawRefreshEvidence` reconstructs Git/lock/images/Compose/volumes/routes/release facts and compares the sanitized current runtime to `stages.postCutover` without mutating receipt/runtime.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make full isolated acceptance a pre-cutover refresh barrier</name>
  <files>scripts/refresh-local.mjs, scripts/refresh-local-runtime-core.mjs, scripts/refresh-local.test.mjs</files>
  <read_first>
    <item>scripts/refresh-local.mjs — final 08-01 phase sequence and rollback boundary.</item>
    <item>scripts/refresh-local-runtime-core.mjs — exact Node argv policy, runtime state, phase executor and evidence construction.</item>
    <item>scripts/refresh-local.test.mjs — phase-order, stage-fault, pre/post-cutover and sanitized evidence fixtures.</item>
    <item>scripts/local-delivery-acceptance.mjs — 08-02 no-argument command, result prefix and strict `parseLocalDeliveryAcceptanceRecord`.</item>
    <item>scripts/local-delivery-acceptance.test.mjs — complete/partial/stale result contract examples.</item>
    <item>scripts/refresh-local-live.mjs — sealed production `runArgv` boundary; no new injection knob is needed.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-CONTEXT.md — D-11 and D-13 through D-18.</item>
  </read_first>
  <behavior>
    - "`createRefreshPlan().phases` orders `inspect-target-images`, `accept-v1.1`, `migrate`, `schema-verify`, `cutover-api-web`; acceptance never runs after canonical mutation."
    - "The only new allowed child argv is `node scripts/local-delivery-acceptance.mjs` with no options/environment additions; a near-match is rejected."
    - "The runtime requires one exact `BLOG X V1.1 ACCEPTANCE RESULT` record, parses it through the 08-02 schema, stores it in state and refuses migration if execution/result/BLOCKED/count/digest validation fails."
    - "Acceptance-stage failures produce a claim-bound sanitized failure report, leave preflight persistence/routes/images unchanged and never call migration/cutover/rollback."
  </behavior>
  <action>
    Add failing phase-order and fault-path tests. Insert exact phase `accept-v1.1` after `inspect-target-images` and before `migrate` in `createRefreshPlan`. Extend `assertAllowedRefreshCommand` with only `node scripts/local-delivery-acceptance.mjs`. In `createRawRefreshRuntime.execute`, run that command, extract exactly one machine-result line, parse it with `parseLocalDeliveryAcceptanceRecord`, require `releaseState: BLOCKED` and nonzero pass-only aggregate counts, and store the canonical record plus its SHA-256 on runtime state. Do not expose an acceptance command override through `runRefreshCli`, production live factories or environment. Add `accept-v1.1` to terminal-stage/failure-report tests and assert no migration, cutover or rollback command appears after its failure.
  </action>
  <acceptance_criteria>
    <criterion>Complete isolated acceptance is a mandatory single stage before any canonical migration/cutover.</criterion>
    <criterion>Only the exact no-argument coordinator command is allowlisted and a malformed/partial/failed result stops the workflow.</criterion>
    <criterion>Acceptance failure leaves canonical database/media/routes and serving API/Web images at the preflight baseline.</criterion>
    <criterion>The accepted record is revision-run evidence input, not a second receipt writer or canonical Compose authority.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/local-delivery-acceptance.test.mjs scripts/refresh-local.test.mjs</automated>
  </verify>
  <done>The canonical refresh cannot migrate or cut over unless the complete generated Phase 6/7 acceptance record has passed strict parsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal representative reading facts, final receipt and stage-safe operator output</name>
  <files>scripts/refresh-local.mjs, scripts/refresh-local-runtime-core.mjs, scripts/refresh-local-facts.mjs, scripts/refresh-local.test.mjs</files>
  <read_first>
    <item>scripts/refresh-local.mjs — `runLocalRefresh` phase loop, exact `seed-prerequisites`/acceptance order, rollback path and direct-entry error boundary.</item>
    <item>scripts/refresh-local-runtime-core.mjs — `PHASE_REPORT_STAGE`, `createRawRefreshRuntime.currentPhase`, `runRefreshCliBoundary`, D-08 `classifySeedPrerequisiteFailure`/`formatSeedPrewarmInstruction`, route/evidence schemas and verifier Git normalization.</item>
    <item>scripts/refresh-local-facts.mjs — route key contracts, persistence comparisons, sanitized projection and fact collection.</item>
    <item>scripts/refresh-local.test.mjs — stale/final route fixtures, evidence schema, read-only verifier, every exact terminal-stage fault loop and current output capture fixtures.</item>
    <item>packages/contracts/src/public-posts.ts — strict public list fields and `/posts/<slug>` input authority.</item>
    <item>apps/web/app/search/page.tsx — stable content-independent public search page to add to fixed route facts.</item>
    <item>compose.yaml — exact three service healthchecks and fixed origin/ports/volumes.</item>
    <item>ops/release-evidence.blocked.json — canonical production decision input; remains read-only.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-CONTEXT.md — D-03, D-05 and D-15 through D-19.</item>
  </read_first>
  <behavior>
    - "Static final routes include `/search` at 200 in addition to existing home/taxonomy/archive/health/search/related contracts, all with redirect:error, exact fixed origin and 1 MiB body bound."
    - "A separate `reading` fact fetches `/api/public/articles?page=1` read-only; when items exist it hashes the first strict slug and proves same-origin `/posts/<encoded-slug>` 200, otherwise records exact `empty_public_set` without mutation."
    - "Evidence contains exact top-level `acceptance`, validates its counts/digests/BLOCKED schema, includes sanitized `reading`, and never stores a raw slug, raw response, generated origin, secret, mount or command."
    - "Normal CLI execution prints exactly one concise `START` and `COMPLETE` record for each completed high-level phase, including `seed-prerequisites` and `accept-v1.1`; it never marks a failed stage complete or reports progress for a stage not entered."
    - "`REFRESH_TERMINAL_STAGES` covers CLI/source/claim setup, adapter/materialization, local Docker/preflight/seed/build/inspect/accept/migrate/schema/cutover/routes/release/evidence publication, independent evidence verification, final output, rollback verification, recollection and failure-report publication; every member has one `SAFE_RECOVERY_BY_STAGE` action and the emitted failure names the exact uncollapsed stage."
    - "A typed D-08 seed failure delegates to the 08-01 `formatSeedPrewarmInstruction` producer; all other recovery text is fixed by stage, preserves claims/evidence/data authority and never grants alternate ports/projects/volumes, registry retrieval, destructive migration/reset, server access or deployment."
    - "Normal CLI success independently verifies the just-written receipt, tolerating only that exact generated receipt as post-run Git output, then prints revision, fixed URL, exact `VISIBLE` route/reading change summary, Phase 6/7 counts, receipt path and `RELEASE BLOCKED`."
    - "Terminal failure output contains only the exact stage, stable recovery action and optional seed class; raw error messages/child stdout or stderr, secrets, URLs other than the fixed public URL, image/store/host paths and generated authority are excluded."
    - "Later receipt verification permits only an ancestor relationship plus exact Phase 8 receipt/summary documentation paths and requires committed raw lockfile equality; receipt bytes remain unchanged."
  </behavior>
  <action>
    Add tests first for populated and empty public-list outcomes, malformed/redirect/oversize/duplicate data, raw-slug exclusion, acceptance schema, post-run Git normalization and exact output. Add `/search` to the static route contract. Add `reading` as a first-class collected/projected fact with `assertReadingFact`: fetch and strictly shape-check `/api/public/articles?page=1`; if `items.length > 0`, accept only a nonempty string slug, fetch `${ORIGIN}/posts/${encodeURIComponent(slug)}` with redirect error and record `state: "verified"`, list/detail status and body hashes plus `slugSha256`; if empty, require `totalItems: 0`, `totalPages: 0` and record `state: "empty_public_set"` with null detail/slug fields. Never seed canonical data.

    Add `acceptance` to the exact evidence schema using the 08-02 parser and require its stored canonical SHA-256, layer result/output digests, exact run counts and aggregate pass-only counts. Include sanitized reading/acceptance in the final projection and verifier comparison while preserving all persistence transitions. Update `verifyRawRefreshEvidence` Git handling so the immediate post-run state may contain only the exact new receipt path, and later HEAD ancestry may differ only by that receipt plus `08-01/02/03-SUMMARY.md`; retain the strict clean gate before the attempt.

    Per D-05, export frozen `REFRESH_TERMINAL_STAGES`, `SAFE_RECOVERY_BY_STAGE`, pure `formatRefreshStageProgress(stage, status)`, `safeRecoveryForRefreshFailure({ stage, error })`, and `formatRefreshFailure({ stage, recovery })` from `scripts/refresh-local-runtime-core.mjs`. Cover exactly `cli_validation`, `source_authority`, `attempt_claim_preflight`, `attempt_claim_publication`, `adapter_construction`, `claim_attachment`, `lockfile_plan_materialization`, `local_docker_authority`, `preflight_collection`, `seed-prerequisites`, `build-api`, `build-web`, `inspect-target-images`, `accept-v1.1`, `migrate`, `schema-verify`, `cutover-api-web`, `routes`, `release-blocked`, `write-evidence`, `evidence_verification`, `final_output`, `rollback-api-web`, `verify-rollback`, `failure_recollection`, and `failure_report_publication`; delete the `inspect-target-images -> build-web` collapse so failure stage truth is exact. Each stage must map to a fixed non-authority-changing recovery action: CLI/source stages correct invocation and create a new clean branch commit; claim stages preserve existing claim/failure/evidence and prohibit same-SHA retry; local-authority/preflight stages restore only the approved local Docker/canonical facts; seed/build/inspect stages delegate typed seed cases to the 08-01 `formatSeedPrewarmInstruction` and otherwise repair committed offline inputs; acceptance repairs only generated acceptance; migration/schema permits forward repair only; cutover/routes/release/evidence/final-output stages verify rollback/current facts read-only and preserve artifacts; rollback/recollection/publication stops automatic retry and requires read-only inspection of retained authority. No mapping may suggest another port/project/volume, retrieval fallback, volume/data cleanup, down migration, server action or deployment.

    Extend `runLocalRefresh({ adapter, plan, onStage })` so the sealed boundary-supplied reporter emits one fixed start record immediately before each entered phase and one complete record only after it returns; include rollback start/complete records on recovery. In `runRefreshCliBoundary`, report the setup stages around source resolution, claim absence/publication, adapter construction, claim attachment and lockfile plan materialization; pass the same reporter to `executeRefresh(adapter, plan, { onStage })`; explicitly bracket independent receipt verification and final success output as `evidence_verification` and `final_output`. On every catch, derive the exact current stage, publish the existing sanitized failure report when claim authority exists, select the safe recovery, emit one fixed failure block, and rethrow only a sanitized reported error. Refactor the D-08 branch created in 08-01 into this common failure path so `formatSeedPrewarmInstruction` is called and emitted exactly once, as the separate instruction following the seed-stage failure line. Update the direct-entry catch in `scripts/refresh-local.mjs` so it never prints the original error/child output after a reported failure and uses a fixed `cli_validation` fallback before boundary reporting is available.

    Add table-driven tests over every `REFRESH_TERMINAL_STAGES` member plus all five seed classes. Assert exact start/complete order on success, exact failed stage with no false completion, one mapped recovery action, seed delegation to the 08-01 producer, claim/failure-report preservation, pre/post-cutover mutation/rollback invariants, and absence of sentinel child stdout/stderr, credentials, database URLs, tokens, image IDs/references, generated origins and host/store paths. Source-policy tests must also prove the recovery table contains no alternate-port/project/volume, registry, cleanup, remote/server or deployment authority. After normal `executeRefresh`, call `verifyEvidence`, assert same revision/receipt bytes, and print a fixed concise terminal block with the full SHA, `http://127.0.0.1:3100`, an exact `VISIBLE` line derived only from the verified `/search` and representative-reading facts, actual Phase 6/7 totals, `ops/v1.1-local-delivery-evidence.json`, and `RELEASE BLOCKED`.
  </action>
  <acceptance_criteria>
    <criterion>Final fixed facts prove `/search` and either a real current public reading route or an explicit strict empty-public-set outcome without canonical writes.</criterion>
    <criterion>The receipt binds acceptance counts/digests, same revision image labels, exact runtime/persistence/route/reading facts and terminal `BLOCKED`.</criterion>
    <criterion>Receipt verification reconstructs current facts, allows only the exact receipt/documentation Git delta and leaves receipt bytes unchanged.</criterion>
    <criterion>Every successful entered phase emits exactly one start/complete pair through `runLocalRefresh`/`runRefreshCliBoundary`; every terminal-stage fixture emits the exact uncollapsed failed stage, no false completion and exactly one mapped safe recovery action.</criterion>
    <criterion>All five typed seed failures use the actual 08-01 `formatSeedPrewarmInstruction` producer; every other stage uses its own fixed recovery mapping, and no guidance changes port/project/volume authority, retrieves dependencies, mutates retained data, accesses a server or deploys.</criterion>
    <criterion>Terminal success includes the exact evidence-derived `VISIBLE` summary and final block; terminal failure/success output is concise and excludes raw child output, secrets, generated authority, private paths, internal origins and production authorization.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/refresh-local.mjs &amp;&amp; node --check scripts/refresh-local-runtime-core.mjs &amp;&amp; node --test scripts/local-delivery-acceptance.test.mjs scripts/refresh-local.test.mjs &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The final schema and verifier connect complete acceptance to exact current-revision fixed-3100 route evidence, exhaustive stage-safe recovery output and a human-readable `RELEASE BLOCKED` result.</done>
</task>

<task type="auto">
  <name>Task 3: Perform the sole clean-revision canonical delivery and verify its receipt</name>
  <precondition>The Task 1/2 implementation commits are HEAD, `git status --porcelain` is empty, no claim/failure/final receipt exists for that full SHA, prepared seed images satisfy the lock/store contract, and the existing canonical `blogxlocal` runtime owns port 3100 plus both retained volumes.</precondition>
  <reversibility rating="costly">The command migrates the retained database forward and switches serving API/Web images; rollback restores images but D-12 deliberately does not down-migrate schema.</reversibility>
  <files>ops/v1.1-local-delivery-evidence.json</files>
  <read_first>
    <item>package.json — exact `local:deliver` command created by 08-01.</item>
    <item>scripts/refresh-local.mjs — final phase order and sealed normal CLI.</item>
    <item>scripts/refresh-local-runtime-core.mjs — preconditions, exhaustive `SAFE_RECOVERY_BY_STAGE`, 08-01 `formatSeedPrewarmInstruction`, atomic receipt and independent verifier.</item>
    <item>scripts/local-delivery-acceptance.mjs — complete generated acceptance invoked by the refresh.</item>
    <item>compose.yaml — canonical service/volume/health authority.</item>
    <item>ops/phase6-local-refresh-evidence.json — historical evidence that must remain untouched.</item>
    <item>ops/release-evidence.blocked.json — production state that must remain BLOCKED.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-01-SUMMARY.md — implemented command/provenance contracts.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-02-SUMMARY.md — implemented complete acceptance record contracts.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-CONTEXT.md — all D-01 through D-19 completion constraints.</item>
  </read_first>
  <action>
    Assert the precondition with read-only exact checks. Run `corepack pnpm local:deliver` exactly once for the clean full HEAD; do not retry the same SHA after any claim. If `seed-prerequisites` stops on a typed D-08 class, follow only the separate instruction produced by the implemented 08-01 `formatSeedPrewarmInstruction` path, preserve the claim/failure artifacts, remediate and commit a new SHA before another delivery attempt; do not retrieve dependencies, weaken provenance or delete claims/evidence. For any other failure, follow only the exact `SAFE_RECOVERY_BY_STAGE` action and preserve its authority boundaries. On success, retain the atomically generated `ops/v1.1-local-delivery-evidence.json`, run the fixed read-only verifier diagnostic once, and inspect its sanitized fields for exact implementation revision, acceptance run/count/digests, canonical three-health/project/port/volume facts, route/reading proof and `BLOCKED`. Do not connect to either server, deploy, publish an image, alter release evidence or run any broad cleanup.
  </action>
  <acceptance_criteria>
    <criterion>The normal package command succeeds once for the clean implementation SHA and the fixed runtime ends on target API/Web immutable IDs with PostgreSQL/media identity preserved.</criterion>
    <criterion>The complete Phase 6/7 generated acceptance and fixed-route verifier both pass with actual nonzero counts and zero skip/TODO/failure.</criterion>
    <criterion>The new receipt is the only generated authority, verifies read-only, names the exact SHA and fixed URL, and remains `releaseState: BLOCKED`.</criterion>
    <criterion>Any stopped attempt exposes only the exact mapped stage/recovery output; a seed stop demonstrably uses the implemented 08-01 formatter and no failure authorizes same-SHA retry or authority changes.</criterion>
    <criterion>Historical evidence, unrelated containers/data, both servers and all production/deployment authority remain untouched.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/refresh-local.mjs --verify-evidence=ops/v1.1-local-delivery-evidence.json &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The current committed implementation is the canonical healthy `blogxlocal` runtime at fixed 3100, with a verified machine receipt and production still explicitly blocked.</done>
</task>

</tasks>

## Atomic commit sequence

1. Commit the mandatory pre-cutover acceptance barrier as `feat(08-03): gate refresh on full acceptance`.
2. Commit representative reading facts, final receipt schema/verifier and output as `feat(08-03): seal local delivery evidence`.
3. From that clean commit, run `corepack pnpm local:deliver` exactly once and commit only `ops/v1.1-local-delivery-evidence.json` as `chore(08-03): record fixed local delivery` after read-only verification.
4. Commit `08-03-SUMMARY.md` separately; no production or server action follows.

## Artifacts this phase produces

- `createRefreshPlan().phases` gains exact status/stage value `accept-v1.1` between `inspect-target-images` and `migrate`.
- `assertAllowedRefreshCommand` gains exact argv `node scripts/local-delivery-acceptance.mjs`; no flag-bearing form is allowed.
- Receipt schema `blog-x-v1.1-local-delivery-evidence` version 1 gains top-level field `acceptance` with `format`, `version`, `phase6Data`, `phase7Browser`, `counts`, `releaseState`, `sha256`.
- Fact/projection schema gains top-level `reading`; populated fields are `state`, `listStatus`, `listBodySha256`, `detailStatus`, `detailBodySha256`, `slugSha256`; the empty state uses the same exact keys with null detail/slug values.
- Static fixed route set gains `/search`; read-only discovery source gains `/api/public/articles?page=1`; populated representative detail uses `/posts/<encoded-public-slug>` without storing the slug.
- `runRefreshCliBoundary` normal success output gains exact lines for `REVISION`, `URL`, `ROUTES`, `READING`, `VISIBLE`, `ACCEPTANCE`, `EVIDENCE`, and `RELEASE BLOCKED`; `VISIBLE` is derived only from the verified `/search` and representative-reading facts.
- `runLocalRefresh` and `runRefreshCliBoundary` share `formatRefreshStageProgress`; all listed terminal stages map through `SAFE_RECOVERY_BY_STAGE`/`safeRecoveryForRefreshFailure`, and typed seed cases reuse 08-01 `formatSeedPrewarmInstruction`.
- Direct-entry failure rendering emits only the sanitized `formatRefreshFailure` result and never appends the original exception or captured child stream.
- New generated artifact `ops/v1.1-local-delivery-evidence.json` is atomically published once and committed only after fixed read-only verification.
- No new database schema/migration, CLI override, Compose authority, dependency, external API, SSH, deployment, registry publication, cloud-server mutation or production-READY field is produced.

## Source coverage audit

| Source | ID / locked requirement | Task action owner | Acceptance owner | Status |
|---|---|---|---|---|
| GOAL | Current committed code safely/reproducibly visible at fixed 3100 with machine evidence | 08-01 T1/T2 establishes sealed delivery and authority; 08-02 T1/T2 supplies full acceptance; 08-03 T1/T2/T3 binds and performs delivery | 08-01 sealed/offline/data criteria; 08-02 exact-count criteria; 08-03 receipt/runtime criteria | COVERED |
| REQ | DEVX-01 fixed command, retained data, idempotent migration and three health states | 08-01 T1 adds exact command; 08-01 T2 preserves project/volumes/order/rollback; 08-03 T3 runs it once | 08-01 T1 direct-command plus T2 authority/persistence criteria; 08-03 T3 canonical-success criterion | COVERED |
| REQ | DEVX-02 offline installed-dependency reuse without alternate project/origin | 08-01 T2 implements strict offline seed/target checks and D-08 pre-warm; 08-03 T3 follows only fixed recovery | 08-01 T2 provenance/classification/no-authority-change criteria; 08-03 T3 fixed-runtime criterion | COVERED |
| REQ | DEVX-03 current-revision page/API/routes and visible-change report | 08-02 T1/T2 produces full exact acceptance; 08-03 T1 binds it and T2 emits route/reading/output proof | 08-02 T1/T2 complete/pass-only criteria; 08-03 T1 barrier and T2 receipt/output criteria | COVERED |
| CONTEXT | D-01 one fixed command and immutable authority | 08-01 T1 adds exact `local:deliver` direct Node entry and forbids overrides | 08-01 T1 exact-package-command and unknown-argument criteria | COVERED |
| CONTEXT | D-02 explicit invocation; no watcher/hook | 08-01 T1 leaves one explicit package action; 08-03 T3 invokes it exactly once | 08-01 T1 no wrapper/pipeline criterion; 08-03 T3 single-attempt criterion | COVERED |
| CONTEXT | D-03 clean attached full SHA binding | 08-01 T1 implements `git.ref`/symbolic-ref/full-SHA collection and cross-stage linkage; 08-03 T2 retains receipt linkage | 08-01 T1 Git/ref rejection criteria; 08-03 T2 receipt/reconstruction criterion | COVERED |
| CONTEXT | D-04 exact canonical owner of 3100 | 08-01 T2 implements read-only Docker port-owner inspection and `assertCanonicalPortOwner` | 08-01 T2 exact owner/authority and no-kill/no-rebind criteria | COVERED |
| CONTEXT | D-05 concise progress, exact failed stage and safe recovery | 08-03 T2 implements `REFRESH_TERMINAL_STAGES`, stage reporter, exhaustive safe recovery map, sanitized direct-entry handling and final block | 08-03 T2 start/complete, every-stage failure/recovery and no-raw-output criteria | COVERED |
| CONTEXT | D-06 strict offline build with no fallback | 08-01 T2 retains exact network-none/pull-false/frozen-lock build and probe policy | 08-01 T2 offline argv/provenance and forbidden-command criteria | COVERED |
| CONTEXT | D-07 immutable seed ID + lock + populated neutral store | 08-01 T2 adds `seed-prerequisites` and preserves target probe/label/filesystem validation | 08-01 T2 immutable-ID/lock/store and explicit-classification criteria | COVERED |
| CONTEXT | D-08 missing/stale/incompatible seed stop plus separate pre-warm | 08-01 T2 implements five trusted classes, `classifySeedPrerequisiteFailure`, `formatSeedPrewarmInstruction` and boundary emission; 08-03 T2 delegates seed recovery to that producer | 08-01 T2 five-class/pre-mutation/separate-sanitized-output criteria; 08-03 T2 producer-reuse/no-authority-change criterion | COVERED |
| CONTEXT | D-09 exact canonical embedded public origin | 08-01 T2 updates and validates both Dockerfiles/targets against fixed origin | 08-01 T2 exact target-origin/provenance criterion | COVERED |
| CONTEXT | D-10 only two retained volumes; no reset/substitution | 08-01 T2 retains exact fixed authority/persistence tests and the single locked reversibility annotation without a duplicate decision checkpoint | 08-01 T2 canonical-volume/no-deletion/persistence criteria | COVERED |
| CONTEXT | D-11 builds/inspection then migrate/schema then cutover | 08-01 T2 fixes `seed-prerequisites`/build/inspect order; 08-03 T1 inserts acceptance before migrate | 08-01 T2 pre-mutation/order criterion; 08-03 T1 exact barrier criterion | COVERED |
| CONTEXT | D-12 forward-only migration preserving content/ledger | 08-01 T2 retains one-off migration/schema and transition assertions; 08-03 T2 maps migration recovery to forward repair only | 08-01 T2 rows/media/sequences/ledger criterion; 08-03 T2 no-destructive-recovery criterion | COVERED |
| CONTEXT | D-13 pre-cutover no mutation; post-cutover exact image rollback | 08-01 T2 retains rollback phases/tests; 08-03 T1 acceptance fault is pre-cutover; 08-03 T2 reports rollback stages exactly | 08-01 T2 immutable rollback/persistence criteria; 08-03 T1 baseline criterion; 08-03 T2 stage/rollback criteria | COVERED |
| CONTEXT | D-14 one durable non-overwriting attempt per clean SHA | 08-01 T1 implements claim/evidence/ref rejection; 08-02 T1 proves parallel generated runs; 08-03 T3 forbids same-SHA retry | 08-01 T1 duplicate/concurrency/atomic criteria; 08-02 T1 cardinality criterion; 08-03 T3 stopped-attempt criterion | COVERED |
| CONTEXT | D-15 generated exhaustive acceptance; canonical read-only | 08-02 T1/T2 seals generated Phase 6/7 coordinator; 08-03 T2 collects only read-only canonical routes/reading | 08-02 T1/T2 isolation/cleanup/no-authority criteria; 08-03 T2 no-canonical-write criterion | COVERED |
| CONTEXT | D-16 Phase 6/7 plus fixed runtime/routes | 08-02 T1/T2 emits complete machine records; 08-03 T1 binds them and T2 adds fixed `/search`/reading/runtime facts | 08-02 complete-result criteria; 08-03 T1 acceptance-barrier and T2 fixed-fact criteria | COVERED |
| CONTEXT | D-17 exact nonzero pass-only counts | 08-02 T1 parses exact suite/browser counts; T2 aggregates strict records; 08-03 T1 rejects partial/non-pass acceptance | 08-02 T1/T2 nonzero/no-fail-skip-TODO criteria; 08-03 T1 malformed/partial rejection criterion | COVERED |
| CONTEXT | D-18 one sanitized atomic receipt ending BLOCKED | 08-01 T1 creates sole v1.1 path/format; 08-03 T2 adds acceptance/reading and verifies bytes; T3 retains receipt | 08-01 T1 sole-path/non-overwrite criteria; 08-03 T2 schema/byte/output criteria; T3 BLOCKED criterion | COVERED |
| CONTEXT | D-19 fixed 3100 canonical Compose runtime at completion | 08-01 T2 validates exact project/services/port/volumes; 08-03 T3 performs and verifies canonical delivery | 08-01 T2 authority criterion; 08-03 T3 exact runtime/current-SHA criterion | COVERED |
| RESEARCH | Extend sealed CLI/runtime/facts; no parallel pipeline/dependency | 08-01 evolves existing modules; 08-02 coordinator is acceptance-only; 08-03 adds one stage to existing plan | All three plans retain sealed/no-new-dependency/no-alternate-authority criteria | COVERED |
| RESEARCH | Unambiguous v1.1 path/format/kind and preserved historical evidence | 08-01 T1/T2 changes all current producers/consumers together | 08-01 T1 sole-path/historical rejection and T2 provenance criteria | COVERED |
| RESEARCH | Read-only exact port owner and offline seed/target provenance | 08-01 T2 implements owner plus typed seed prerequisites | 08-01 T2 exact owner, five-class, offline and no-authority-change criteria | COVERED |
| RESEARCH | Complete isolated Phase 6/7 records/coordinator | 08-02 T1/T2 implements producers, parser and sealed coordinator | 08-02 T1/T2 exact count/cardinality/cleanup/redaction criteria | COVERED |
| RESEARCH | Acceptance before mutation; migration/cutover/rollback order | 08-03 T1 adds exact barrier; 08-01 T2 retains established order | 08-03 T1 pre-cutover baseline criteria plus 08-01 T2 persistence/rollback criteria | COVERED |
| RESEARCH | Content-independent `/search` plus optional public-slug reading proof | 08-03 T2 implements static route and strict populated/empty read-only reading fact | 08-03 T2 route/reading/no-write criterion | COVERED |
| RESEARCH | Exact receipt counts/digests/facts/output and BLOCKED | 08-03 T2 implements schema/verifier/progress/failure/final output; T3 verifies live receipt | 08-03 T2 receipt/output criteria and T3 BLOCKED/read-only-verifier criteria | COVERED |
| EDGE | DEVX-01 concurrency/interruption | D-13/D-14 actions plus 08-01/08-03 fault/concurrency loops | 08-01 claim/rollback criteria and 08-03 every-stage criteria | COVERED |
| EDGE | DEVX-02 seed/offline/origin ambiguity | D-06–D-09 actions plus typed five-class tests and exact target-origin validation | 08-01 T2 classification/provenance/no-authority-change criteria | COVERED |
| EDGE | DEVX-03 concurrent/parallel acceptance | D-14/D-15/D-17 actions plus 08-02 exact three-run coordinator | 08-02 parent/two-child/full-browser criteria | COVERED |
| PROHIB | P-01 no canonical volume/data/unrelated deletion | 08-01 descriptor-less flagged prohibition plus command/persistence tests | 08-01 T2 no-delete/no-substitute criteria | COVERED, FLAGGED-UNVERIFIED |
| PROHIB | P-02 no registry/network/alternate project/temp canonical origin | 08-01 descriptor-less flagged prohibition plus offline/provenance/recovery tests | 08-01 T2 offline/no-authority-change criteria | COVERED, FLAGGED-UNVERIFIED |
| PROHIB | P-03 no canonical data as acceptance fixture | 08-02 descriptor-less flagged prohibition plus sealed coordinator isolation | 08-02 T2 generated-authority/no-canonical-command criterion | COVERED, FLAGGED-UNVERIFIED |
| PROHIB | P-04 no SSH/deploy/cloud mutation/non-BLOCKED production decision | 08-03 descriptor-less flagged prohibition plus recovery/source-policy/receipt tests | 08-03 T2 no-server/deploy recovery criterion and T3 untouched-server criterion | COVERED, FLAGGED-UNVERIFIED |

Audit result: the ROADMAP goal, DEVX-01..03, D-01..D-19, every in-scope research recommendation, all three specless edge probes and all four kept prohibition-recall items have executable owners. Deferred production deployment, CI/registry publishing and Phase 7 fetch-timeout hardening remain excluded exactly as recorded. UI gate follows the documented `--skip-ui` path because Phase 8 changes no visual or interaction design.

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Risk | Control |
|---|---|---|
| acceptance coordinator -> refresh | forged/partial/stale generated evidence | exact schema/cardinality/count/digest/BLOCKED parser before migration |
| public list -> representative route | malformed slug, redirect or private-data leakage | bounded strict public DTO shape, encoded slug, same-origin redirect-error fetch and digest-only projection |
| refresh -> canonical database/media | acceptance or route fixture mutation | generated acceptance only; canonical sources are read-only except forward migration |
| runtime facts -> final receipt | stale SHA/image/facts or overwrite | reconstruction, exact labels, atomic link publication and byte equality |
| local success -> production decision | accidental deployment/READY authority | release-gate `--expect-blocked`, boundary scan and no remote adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-03-01 | Spoofing | acceptance/revision/image linkage | high | mitigate | Canonical parser plus full SHA/lock/target labels and receipt digest linkage. |
| T-08-03-02 | Tampering | canonical data and receipt | high | mitigate | Read-only acceptance/routes, persistence transitions and atomic non-overwrite evidence. |
| T-08-03-03 | Repudiation | final delivery attempt | high | mitigate | One durable claim, stage-bound failure report, actual counts and verified receipt bytes. |
| T-08-03-04 | Information Disclosure | public DTO/output/receipt | high | mitigate | Strict public list shape, slug hashing, bounded bodies, redaction and digest-only facts. |
| T-08-03-05 | Denial of Service | full local gate | medium | accept | Bounded child/output/health/cleanup may fail delivery; pre-cutover failure preserves runtime. |
| T-08-03-06 | Elevation of Privilege | production/server capability | high | mitigate | Local exact argv only, boundary audit, frozen server fence and mandatory `BLOCKED`. |
</threat_model>

<verification>
- Run Task 1/2 automated commands before the operational attempt; require nonzero tests and no skip/TODO.
- Require the table-driven output suite to cover every exported `REFRESH_TERMINAL_STAGES` member, all five D-08 classes, exact start/complete ordering, no false completion, shared pre-warm producer reuse and raw-output/secret/path exclusion.
- Start Task 3 only from a clean committed HEAD with exact canonical port/project/volume/seed prerequisites.
- Run `corepack pnpm local:deliver` once, then the fixed read-only receipt verifier; never rerun the normal path for that SHA.
- Confirm all four descriptor-less prohibitions remain flagged/unverified for downstream judgment and no mechanical-check descriptor field was invented.
</verification>

<success_criteria>
- One explicit command rebuilds offline, preserves data, migrates, cuts over API/Web and reaches three healthy canonical services.
- Full Phase 6/7 acceptance is exact-counted and required before canonical mutation.
- The fixed runtime proves current SHA, canonical origin, same-origin public routes and honest representative reading state.
- Concise stage progress and every terminal failure provide exact, sanitized, non-authority-changing recovery guidance; D-08 uses the implemented shared pre-warm formatter.
- The sole new receipt verifies read-only and terminates in `RELEASE BLOCKED` with no server/deployment capability.
</success_criteria>

<output>
Create `.planning/phases/08-reliable-local-delivery/08-03-SUMMARY.md` when done.
</output>
