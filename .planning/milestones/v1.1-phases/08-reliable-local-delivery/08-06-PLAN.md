---
phase: 08-reliable-local-delivery
plan: "06"
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - apps/web/e2e/article-lifecycle.spec.ts
  - apps/web/e2e/auth-session.spec.ts
  - apps/web/e2e/draft-preview.spec.ts
  - apps/web/e2e/public-list.spec.ts
  - apps/web/e2e/public-reading.spec.ts
  - apps/web/e2e/walking-skeleton.spec.ts
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
autonomous: true
gap_closure: true
requirements: [DEVX-02, DEVX-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All six legacy Web E2E files consume one runner-owned generated main-browser fixture; none starts API/Next, owns a database, binds 3100 or performs independent teardown."
    - "The generated fixture supplies unique ports, database, media/backup paths, admin identity and run-scoped content, then acknowledges exact cleanup on success and failure."
    - "Lifecycle, auth, draft preview, list, reading and walking-skeleton behaviors remain asserted without test-only production routes."
    - "Canonical `blogxlocal` services, volumes and port 3100 remain read-only and untouched."
  artifacts:
    - path: scripts/local-verify.mjs
      provides: one generated main-browser fixture owner for the six migrated specs
    - path: scripts/local-verify.test.mjs
      provides: process/database/port ownership and cleanup regressions
  key_links:
    - from: scripts/local-verify.mjs
      to: apps/web/e2e/article-lifecycle.spec.ts
      via: runner-supplied generated origin and fixture facts shared by all six specs
      pattern: "E2E_WEB_ORIGIN|E2E_RUN_ID"
    - from: scripts/test-inventory.mjs
      to: scripts/local-verify.mjs
      via: `main-browser` fixture classification
      pattern: "main-browser"
---

<objective>
Move the six fixed-port/self-spawning browser scenarios under one generated fixture owner.

Purpose: make every legacy E2E scenario safe for exhaustive integration without duplicate processes, direct database authority or canonical fixture mutation.
Output: runner-compatible specs, generated seed/fact support and focused ownership/cleanup regressions.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-reliable-local-delivery/08-CONTEXT.md
@.planning/phases/08-reliable-local-delivery/08-05-SUMMARY.md
@scripts/test-inventory.mjs
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@apps/web/e2e/article-lifecycle.spec.ts
@apps/web/e2e/auth-session.spec.ts
@apps/web/e2e/draft-preview.spec.ts
@apps/web/e2e/public-list.spec.ts
@apps/web/e2e/public-reading.spec.ts
@apps/web/e2e/walking-skeleton.spec.ts
@apps/web/e2e/about-archive.spec.ts
@apps/web/e2e/public-shell.spec.ts

<interfaces>
- Existing generated runners own disposable Compose/database/ports and pass `E2E_WEB_ORIGIN`, admin credentials and a run ID.
- Five existing runner-compatible specs demonstrate the desired origin/fact contract; the six named files still contain child/fixed-port/direct-database ownership.
- Plan 08-05 assigns these six exact files to `main-browser`; this plan implements that owner but does not yet change the signed integration result schema.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert legacy specs to runner-supplied facts</name>
  <files>apps/web/e2e/article-lifecycle.spec.ts, apps/web/e2e/auth-session.spec.ts, apps/web/e2e/draft-preview.spec.ts, apps/web/e2e/public-list.spec.ts, apps/web/e2e/public-reading.spec.ts, apps/web/e2e/walking-skeleton.spec.ts</files>
  <read_first>
    <item>The six target specs — current child spawning, fixed origin and direct setup/cleanup.</item>
    <item>about-archive, public-shell, article-toc, media and taxonomy specs — established runner-owned contract.</item>
  </read_first>
  <behavior>
    - "Each spec fails clearly without runner-owned origin/facts and uses only page/API behavior against that origin."
    - "Source contains no process spawn, listen/bind, migration, database creation, direct host database URL, fixed 3100 or kill/teardown authority."
    - "Existing user-visible assertions remain, and data identities are unique by supplied run ID."
  </behavior>
  <action>
    Remove embedded service lifecycle and database setup. Consume exact sanitized runner fields, retain browser/API assertions and use run-scoped slugs/users. Move prerequisite data construction to the owner in Task 2 rather than adding production-only test endpoints. A spec may clean content through supported UI/API behavior but may not own infrastructure cleanup.
  </action>
  <verify><automated>corepack pnpm exec tsc --noEmit --project apps/web/tsconfig.json &amp;&amp; git diff --check</automated></verify>
  <done>The six specs are pure assertions over one externally owned generated fixture and preserve their behavioral intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supply one generated main-browser fixture and prove ownership</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs</files>
  <read_first>
    <item>scripts/local-verify.mjs — generated project/port/database/media/seed/process/Playwright helpers.</item>
    <item>scripts/local-verify.test.mjs — exact selector, environment, process tree and cleanup tests.</item>
    <item>scripts/test-inventory.mjs — six `main-browser` assignments.</item>
  </read_first>
  <behavior>
    - "Owner allocates one unique generated authority, seeds all six disjoint scenario facts, runs each assigned spec once and cleans the authority exactly once."
    - "No generated port/origin is embedded in canonical images; no canonical container, volume or database is addressed."
    - "Source regression rejects reintroduced spawn/fixed-3100/direct-database patterns in any migrated file."
    - "Failure at any spec still terminates owned children and acknowledges absence of exact generated containers, volumes, paths and database."
  </behavior>
  <action>
    Extend the existing generated main-browser fixture primitives, not a parallel runner. Seed all required states before browser execution and pass an exact environment object. Add tests for unique authority, required facts, per-file once-only scheduling input, prohibited source patterns, failure cleanup and canonical non-ownership. Do not execute overlapping Phase full selectors and do not change receipt/acceptance schemas yet.
  </action>
  <verify><automated>node --test scripts/local-verify.test.mjs &amp;&amp; corepack pnpm test &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated></verify>
  <done>One generated fixture can safely host all six migrated scenarios with exact lifecycle ownership and no canonical mutation.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| Threat | Severity | Control |
|---|---|---|
| Spec claims canonical port/database | high | Source prohibition tests and runner-required generated authority. |
| Cross-run data collision | high | Unique run ID, ports, database and content identities. |
| Child/path residue after failure | high | Central lifecycle owner and exact cleanup acknowledgement. |
| Test-only production backdoor | high | Fixture seeding occurs in runner; no new production endpoint. |
</threat_model>

<verification>
- Focused unit/type/boundary checks pass.
- The six sources have no independent infrastructure authority.
- No Docker canonical delivery or server access occurs.
</verification>

<success_criteria>
- All six scenarios run from one generated main-browser fixture contract.
- Their behavioral assertions remain meaningful and run-scoped.
- Canonical 3100 and retained data stay untouched.
</success_criteria>

<output>Create `.planning/phases/08-reliable-local-delivery/08-06-SUMMARY.md` when done.</output>
