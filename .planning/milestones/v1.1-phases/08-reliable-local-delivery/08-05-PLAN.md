---
phase: 08-reliable-local-delivery
plan: "05"
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - package.json
  - apps/api/package.json
  - scripts/test-inventory.mjs
  - scripts/test-inventory.test.mjs
  - scripts/default-test.mjs
  - scripts/default-test.test.mjs
autonomous: true
gap_closure: true
requirements: [DEVX-01, DEVX-02, DEVX-03]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every on-disk Contracts/API/Web unit or Web E2E file has one exact repository-relative manifest entry and one scope owner; omissions, additions and duplicate ownership fail."
    - "`corepack pnpm test` runs all self-contained coverage: both Contracts files, API Markdown/security files and all three Web unit files, with nonzero pass-only semantic accounting and no infrastructure."
    - "The remaining 13 API files and 17 Web E2E files are classified as runner-owned integration coverage without executing or weakening them in this plan."
    - "Root `test:integration` points exactly to the sealed zero-argument acceptance coordinator consumed by formal delivery."
  artifacts:
    - path: scripts/test-inventory.mjs
      provides: frozen exact default/integration package-test ownership
      exports: ["PACKAGE_TEST_INVENTORY", "DEFAULT_TEST_FILES", "INTEGRATION_TEST_FILES", "assertCompleteTestInventory"]
    - path: scripts/default-test.mjs
      provides: sealed default coordinator and nonzero pass-only parser
    - path: package.json
      provides: exact default and integration entry points
  key_links:
    - from: package.json
      to: scripts/default-test.mjs
      via: exact root `test` command
      pattern: "test.*default-test\\.mjs"
    - from: package.json
      to: scripts/local-delivery-acceptance.mjs
      via: exact root `test:integration` command
      pattern: "test:integration.*local-delivery-acceptance\\.mjs"
---

<objective>
Establish an honest self-contained default test gate and one executable ownership inventory for every package test file.

Purpose: separate fast infrastructure-free coverage from runner-owned integration coverage without hiding, deleting or double-owning any test.
Output: exact inventory, semantic default coordinator, package scripts and focused regressions.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-reliable-local-delivery/08-CONTEXT.md
@.planning/phases/08-reliable-local-delivery/08-VERIFICATION.md
@.planning/phases/08-reliable-local-delivery/08-04-SUMMARY.md
@package.json
@apps/api/package.json
@apps/web/package.json
@packages/contracts/package.json
@apps/api/test
@apps/web/app
@apps/web/lib
@apps/web/e2e

<interfaces>
- Current package inventory is 2 Contracts unit files, 15 API test files, 3 Web unit files and 17 Web E2E files.
- Default scope is Contracts 2 + API `markdown-renderer.test.ts` and `security-hardening.test.ts` + Web `search-discovery.test.ts`, `site-metadata.test.ts`, `search-encoding.test.ts`.
- Integration scope is the other 13 API files and all 17 Web E2E files. Fixture ownership is declarative here and becomes executable in Plans 08-06/07.
- Root already owns `tsx`; no dependency or lockfile change is required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exact complete package-test ownership</name>
  <files>scripts/test-inventory.mjs, scripts/test-inventory.test.mjs</files>
  <read_first>
    <item>All Contracts/API/Web unit and E2E test paths — authoritative on-disk inventory.</item>
    <item>scripts/local-verify.mjs and scripts/phase7-browser-verify.mjs — existing fixture families and Phase 7 ownership.</item>
  </read_first>
  <behavior>
    - "Each entry has exact path, kind, scope and integration fixture owner where applicable."
    - "Filesystem scan equals manifest paths; every path has cardinality one and default/integration sets are disjoint."
    - "Synthetic missing, added, duplicate or invalid-owner cases fail with exact paths."
  </behavior>
  <action>
    Write tests first, then add a frozen explicit manifest. Do not infer semantic ownership from environment text or broad globs. Assign integration files to `database`, `backup-restore`, `media`, `main-browser`, `error-browser`, `restore-browser` or `phase7-browser`; assign only `public-discovery.spec.ts` to Phase 7. Export derived frozen lists and a filesystem equality/cardinality assertion. The manifest may not accept caller-selected paths.
  </action>
  <verify><automated>node --test scripts/test-inventory.test.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The manifest accounts for every package test exactly once and exposes a stable contract for later generated runners.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal the semantic default coordinator and package scripts</name>
  <files>scripts/default-test.mjs, scripts/default-test.test.mjs, package.json, apps/api/package.json</files>
  <read_first>
    <item>scripts/test-inventory.mjs — Task 1 exact default paths.</item>
    <item>package manifests — current recursive/all-files commands and installed executors.</item>
  </read_first>
  <behavior>
    - "Default coordinator accepts zero args, runs exact literal Contracts/API/Web unit children and rejects path/env overrides."
    - "Every layer and aggregate has tests greater than zero and zero failed, cancelled, skipped or TODO."
    - "Malformed/absent summaries, child failure and empty selection fail; bounded output does not hide the cause."
    - "Root scripts are exactly `test: node scripts/default-test.mjs` and `test:integration: node scripts/local-delivery-acceptance.mjs`; API test delegates to exact two-file unit scope."
  </behavior>
  <action>
    Build the sealed coordinator from manifest-owned default paths, with exact literal child argv and strict TAP/result parsing. Test pass, zero, fail, cancel, skip, TODO, malformed and extra-argument cases. Replace recursive root test and the API wildcard as described; add the exact integration alias. Do not add placeholder infrastructure, fallback success, reporter suppression, dependencies or lockfile changes.
  </action>
  <verify><automated>node --test scripts/test-inventory.test.mjs scripts/default-test.test.mjs &amp;&amp; corepack pnpm test &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The default command is complete, self-contained, nonzero and pass-only, while integration remains one explicit sealed authority.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| Threat | Severity | Control |
|---|---|---|
| Test omitted or double-owned | high | Exact on-disk union and cardinality-one manifest regression. |
| Empty/skip-heavy default appears green | high | Strict nonzero semantic parser across every child and aggregate. |
| Caller selects a partial scope | high | Zero-argument coordinator and exact literal argv. |
| Infrastructure is faked for default | medium | Pure files only; no placeholder database/media/backup authority. |
</threat_model>

<verification>
- Plan structure/frontmatter, focused tests, `corepack pnpm test`, boundary scan and diff check pass.
- Default + integration manifest union equals the full on-disk package inventory.
- No Docker, receipt, server or production action occurs.
</verification>

<success_criteria>
- All self-contained files execute under root `test` with strict nonzero pass-only results.
- Every remaining package test has one explicit runner-owned classification.
- Root integration alias is exact and formal-delivery-compatible.
</success_criteria>

<output>Create `.planning/phases/08-reliable-local-delivery/08-05-SUMMARY.md` when done.</output>
