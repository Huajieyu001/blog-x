---
phase: 08-reliable-local-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/refresh-local.mjs
  - scripts/refresh-local-runtime-core.mjs
  - scripts/refresh-local-facts.mjs
  - scripts/refresh-local-live.mjs
  - scripts/refresh-local-test-core.mjs
  - scripts/refresh-local.test.mjs
  - apps/api/Dockerfile.refresh
  - apps/web/Dockerfile.refresh
autonomous: true
requirements:
  - DEVX-01
  - DEVX-02
  - DEVX-03
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`corepack pnpm local:deliver` delegates to one sealed no-argument Node CLI whose project, Compose file, origin and two retained volumes are immutable (D-01, D-02)."
    - "A dirty, detached or non-full Git revision, noncanonical port owner, wrong Compose project, wrong volume set, mutable seed or unverifiable target image fails before migration or cutover (D-03, D-04, D-07, D-08, D-10)."
    - "Every missing, stale, incompatible, lock-drifted or incomplete-store seed prerequisite is classified from trusted validation state and emits a separate fixed pre-warm instruction before migration/cutover without exposing child output, image/store paths or secrets (D-08)."
    - "Both target builds run with `--network=none`, `--pull=false`, the frozen lockfile, the neutral versioned pnpm store and exact canonical `PUBLIC_ORIGIN` (D-06, D-07, D-09)."
    - "Migration and schema verification precede immutable API/Web cutover; a pre-cutover failure leaves serving containers unchanged and a post-cutover failure restores exact prior image IDs (D-11, D-12, D-13)."
    - "The external receipt/path/kind are unambiguously v1.1 local-delivery authority while the historical Phase 6 receipt remains untouched (D-05, D-18)."
    - "Resolved edge truth for DEVX-01/DEVX-03 concurrency: D-14 permits exactly one durable claim per clean SHA; simultaneous or repeated attempts produce one winner and never overwrite claim, failure or receipt authority."
    - "Resolved edge truth for DEVX-02: D-06 through D-09 make registry/DNS availability irrelevant after seed preparation and make any temporary-origin or dependency fallback a pre-mutation failure."
  artifacts:
    - path: package.json
      provides: exact `local:deliver` operator command
      contains: '"local:deliver": "node scripts/refresh-local.mjs"'
    - path: scripts/refresh-local.mjs
      provides: fixed plan, offline target metadata and sealed CLI entry
      exports: ["FIXED_REFRESH", "createRefreshPlan", "runLocalRefresh", "runRefreshCli"]
    - path: scripts/refresh-local-runtime-core.mjs
      provides: exact argv, port-owner, typed seed-prerequisite classification/remediation, claim, migration, cutover, rollback and v1.1 receipt authority
      exports: ["LOCAL_DELIVERY_EVIDENCE_PATH", "LOCAL_DELIVERY_FORMAT", "LOCAL_DELIVERY_VERSION", "LOCAL_DELIVERY_REFRESH_KIND", "SEED_PREREQUISITE_KINDS", "assertSeedPrerequisiteFacts", "classifySeedPrerequisiteFailure", "formatSeedPrewarmInstruction", "assertAllowedRefreshCommand", "runRefreshCliBoundary"]
    - path: scripts/refresh-local-facts.mjs
      provides: exact canonical container, port-owner, volume, persistence and branch-qualified Git facts
      exports: ["REFRESH_AUTHORITY", "assertCanonicalPortOwner", "assertFixedRuntimeAuthority", "projectSanitizedFacts"]
    - path: scripts/refresh-local-live.mjs
      provides: sealed production verifier bound to the sole exported v1.1 evidence path
      exports: ["LOCAL_DELIVERY_EVIDENCE_PATH", "verifyProductionLiveRefreshEvidence"]
    - path: scripts/refresh-local-test-core.mjs
      provides: raw in-memory Git symbolic-ref boundary and virtual v1.1 evidence-path linkage
      exports: ["createRefreshTestRuntime"]
    - path: scripts/refresh-local.test.mjs
      provides: exact single-path, branch-ref, detached-HEAD, seed-class/pre-warm, live/test-core linkage and historical-path rejection regressions
    - path: apps/api/Dockerfile.refresh
      provides: exact offline API provenance label
      contains: "io.blog-x.refresh-kind=v1.1-offline-local-delivery"
    - path: apps/web/Dockerfile.refresh
      provides: exact offline Web provenance and canonical embedded origin
      contains: "ENV PUBLIC_ORIGIN=${PUBLIC_ORIGIN}"
  key_links:
    - from: package.json
      to: scripts/refresh-local.mjs
      via: direct no-argument Node invocation
      pattern: 'local:deliver.*node scripts/refresh-local\.mjs'
    - from: scripts/refresh-local.mjs
      to: scripts/refresh-local-runtime-core.mjs
      via: `runRefreshCliBoundary` receives only sealed production factories
      pattern: "runRefreshCliBoundary"
    - from: scripts/refresh-local-runtime-core.mjs
      to: compose.yaml
      via: exact `docker-compose -p blogxlocal -f compose.yaml` token allowlist
      pattern: "PROJECT.*blogxlocal|COMPOSE_FILE.*compose.yaml"
    - from: scripts/refresh-local-runtime-core.mjs
      to: scripts/refresh-local-facts.mjs
      via: preflight, verification and postcondition collection validate exact branch ref plus port/project/volume authority
      pattern: "symbolic-ref|assertCanonicalPortOwner|assertFixedRuntimeAuthority"
    - from: scripts/refresh-local-runtime-core.mjs
      to: scripts/refresh-local.mjs
      via: `seed-prerequisites` classifies only trusted validation failures and `runRefreshCliBoundary` emits the separate sanitized D-08 pre-warm instruction before any build/migration/cutover continuation
      pattern: "seed-prerequisites|classifySeedPrerequisiteFailure|formatSeedPrewarmInstruction"
    - from: scripts/refresh-local-live.mjs
      to: scripts/refresh-local-runtime-core.mjs
      via: imports `LOCAL_DELIVERY_EVIDENCE_PATH` for the sealed production verifier; no generic or Phase-6-named current-receipt alias remains
      pattern: "LOCAL_DELIVERY_EVIDENCE_PATH"
    - from: scripts/refresh-local-test-core.mjs
      to: scripts/refresh-local-runtime-core.mjs
      via: uses the same exported path for `/virtual-workspace/ops/v1.1-local-delivery-evidence.json` and exercises the allowlisted Git symbolic-ref process boundary
      pattern: "LOCAL_DELIVERY_EVIDENCE_PATH|symbolic-ref"
    - from: apps/web/Dockerfile.refresh
      to: scripts/refresh-local.mjs
      via: matching provenance kind and `http://127.0.0.1:3100` build argument
      pattern: "v1.1-offline-local-delivery|PUBLIC_ORIGIN"
  prohibitions:
    - requirement_id: DEVX-01
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT reset, remove, replace or redirect the canonical PostgreSQL/media volumes, or remove unrelated containers/data."
    - requirement_id: DEVX-02
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT use registry/network fallback, host node_modules, an alternate Compose project or a generated acceptance origin for canonical delivery."
---

## Phase Goal

**As the** Blog X developer, **I want to** update the fixed local blog with one sealed command, **so that** the current committed code is reproducibly visible at `http://127.0.0.1:3100` without risking retained content.

<objective>
Establish the developer-visible tracer: one fixed package command traverses the existing refresh planner/runtime/facts stack and publishes unambiguous v1.1 local-delivery authority, then harden its offline provenance and pre-mutation port/data checks.

Purpose: make the hardest constraint—safe canonical data-preserving cutover—provable before layering complete feature acceptance onto the same path.
Output: sealed `local:deliver` entry, v1.1 constants/path/kind, exact port-owner facts, offline provenance and regression coverage.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-reliable-local-delivery/08-CONTEXT.md
@.planning/phases/08-reliable-local-delivery/08-RESEARCH.md
@.planning/phases/08-reliable-local-delivery/08-PATTERNS.md
@package.json
@compose.yaml
@scripts/refresh-local.mjs
@scripts/refresh-local-runtime-core.mjs
@scripts/refresh-local-facts.mjs
@scripts/refresh-local-live.mjs
@scripts/refresh-local-test-core.mjs
@scripts/refresh-local.test.mjs
@apps/api/Dockerfile.refresh
@apps/web/Dockerfile.refresh

<interfaces>
- `FIXED_REFRESH` is the only operator authority: project `blogxlocal`, origin `http://127.0.0.1:3100`, services `api,web`, volumes `blogxlocal_postgres-data,blogxlocal_media-data`.
- `runRefreshCliBoundary` claims a clean full revision before production adapter construction and accepts only exact diagnostic options; the normal delivery path has no arguments.
- `createRawRefreshRuntime.execute(phase, plan)` owns preflight, offline build, target inspection, one-off migration/schema verification, immutable cutover, routes, BLOCKED release and atomic evidence.
- `assertFixedRuntimeAuthority` requires exactly three healthy `blogxlocal-*` containers, API/PostgreSQL unpublished ports, one loopback Web mapping and exactly two local labeled volumes.
- `LOCAL_DELIVERY_EVIDENCE_PATH` is the single current receipt constant: runtime writer/CLI parser, sealed live verifier and test core all consume `ops/v1.1-local-delivery-evidence.json`; the ambiguous `EVIDENCE_PATH` export is retired rather than aliased.
- Existing `ops/phase6-local-refresh-evidence.json` is immutable history only: it remains byte-untouched and is never read, written or accepted by the v1.1 delivery path.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one sealed `local:deliver` invocation through a v1.1 receipt</name>
  <files>package.json, scripts/refresh-local.mjs, scripts/refresh-local-runtime-core.mjs, scripts/refresh-local-facts.mjs, scripts/refresh-local-live.mjs, scripts/refresh-local-test-core.mjs, scripts/refresh-local.test.mjs</files>
  <read_first>
    <item>package.json — existing direct Node package-script convention.</item>
    <item>scripts/refresh-local.mjs — `FIXED_REFRESH`, `createRefreshPlan`, `runLocalRefresh` and sealed `runRefreshCli` entry.</item>
    <item>scripts/refresh-local-runtime-core.mjs — current Phase-6-named constants, exact CLI parser, atomic evidence schema and terminal output.</item>
    <item>scripts/refresh-local-facts.mjs — exact Git key schema, `assertPersistenceFacts`, cross-stage equality and `projectSanitizedFacts` projection.</item>
    <item>scripts/refresh-local-live.mjs — zero-argument production adapter/store/verifier seam and current `EVIDENCE_PATH` import/re-export to replace with the sole v1.1 constant.</item>
    <item>scripts/refresh-local-test-core.mjs — in-memory `resolveRevision` process boundary and historical virtual receipt path that must move to the sole v1.1 path.</item>
    <item>scripts/refresh-local.test.mjs — in-memory claim/artifact runtime, Git-source order, detached-HEAD, path-linkage, phase-order and evidence tests.</item>
    <item>ops/phase6-local-refresh-evidence.json — historical evidence that must remain byte-untouched and cannot satisfy new delivery verification.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-CONTEXT.md — D-01, D-02, D-03, D-05, D-14 and D-18.</item>
  </read_first>
  <behavior>
    - "The root package exposes exactly `local:deliver: node scripts/refresh-local.mjs`; no flags, shell composition or environment authority are present."
    - "A successful in-memory no-argument run reaches the existing full phase sequence and returns format `blog-x-v1.1-local-delivery-evidence`, version 1, the full SHA and `releaseState: BLOCKED`."
    - "Runtime writer/CLI parser, sealed live verifier and in-memory test core all consume the one exported `LOCAL_DELIVERY_EVIDENCE_PATH` value `ops/v1.1-local-delivery-evidence.json`; the historical Phase 6 path is not accepted as current authority and no second v1.1 receipt path exists."
    - "Collected and projected Git facts have exact keys `clean`, `implementationRevision`, `lockfileSha256`, `ref`; `ref` must be a nonempty `refs/heads/<name>` returned by `git symbolic-ref --quiet HEAD` and must remain equal across evidence stages and reconstruction."
    - "Dirty source, detached HEAD or invalid symbolic ref, unknown CLI options, second claims, existing final receipts and stale temp receipts fail before adapter mutation."
  </behavior>
  <action>
    Write failing tests first for D-01/D-02/D-03/D-05/D-14/D-18. Add `local:deliver` as the exact direct Node package script. In `scripts/refresh-local-runtime-core.mjs`, define and export `LOCAL_DELIVERY_EVIDENCE_PATH = "ops/v1.1-local-delivery-evidence.json"`, `LOCAL_DELIVERY_FORMAT = "blog-x-v1.1-local-delivery-evidence"`, `LOCAL_DELIVERY_VERSION = 1`, and wire them through `runLocalRefresh`, `runRefreshCliBoundary`, evidence absence/publication/schema verification and terminal output. Replace the ambiguous `EVIDENCE_PATH` API instead of retaining a compatibility alias: `scripts/refresh-local-live.mjs` must import/re-export `LOCAL_DELIVERY_EVIDENCE_PATH` and resolve that constant inside `verifyProductionLiveRefreshEvidence`, while `scripts/refresh-local-test-core.mjs` imports the same constant and resolves its only virtual receipt as `/virtual-workspace/ops/v1.1-local-delivery-evidence.json`. The existing Phase 6 JSON remains byte-untouched historical input only and must not be a writer, verifier or CLI option.

    Extend `assertAllowedRefreshCommand` with exact argv `git symbolic-ref --quiet HEAD`. In both production `resolveCleanRevision` and test-core `resolveRevision`, run clean status, symbolic-ref and full HEAD resolution before claim/adapter construction; accept only a nonempty `refs/heads/<name>` without whitespace/control characters, and treat a nonzero symbolic-ref result, empty output or invalid ref as detached/unprovable source failure. In `createRawRefreshFactSources().git` and `verifyRawRefreshEvidence`, resolve the same symbolic ref and carry it as `git.ref`. Update `scripts/refresh-local-facts.mjs` exact Git schema, validation and sanitized projection plus runtime-core projection/evidence validation so `ref` is required and identical across preflight, post-migration, post-cutover and reconstructed verification facts. Update every in-memory process fixture and path assertion in `scripts/refresh-local.test.mjs` to model symbolic-ref explicitly and to use only the v1.1 virtual receipt path; add detached/nonzero, empty, malformed and cross-stage/ref-reconstruction drift failures before runtime mutation. Preserve the existing phase order, sealed zero-argument factories, clean-full-SHA gate, atomic non-overwrite publication and all rollback/redaction tests. The normal command must not gain operational flags or environment-derived project/origin/volume authority.
  </action>
  <acceptance_criteria>
    <criterion>`package.json` contains one exact direct `local:deliver` command and no delivery wrapper/pipeline alternative.</criterion>
    <criterion>The in-memory end-to-end refresh and sealed production verifier both resolve the same exported `LOCAL_DELIVERY_EVIDENCE_PATH`, return/verify the new format/version for the same full SHA and branch ref with `BLOCKED`, and create no alternative v1.1 path.</criterion>
    <criterion>Facts and projections reject a missing, extra, malformed or drifting `git.ref`; production and test-core revision resolution both execute `git symbolic-ref --quiet HEAD` before claim/adapter construction.</criterion>
    <criterion>Historical-path verification, dirty or detached/unprovable Git, unknown arguments, duplicate claim/final receipt and stale temp receipt all fail before runtime mutation; the historical Phase 6 artifact stays unchanged.</criterion>
    <criterion>Existing rollback, redaction, atomic publication and fixed-authority tests remain green.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/refresh-local.mjs &amp;&amp; node --check scripts/refresh-local-runtime-core.mjs &amp;&amp; node --test scripts/refresh-local.test.mjs &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>A developer has one production-quality sealed entry whose complete existing refresh path terminates in current-revision v1.1 local-delivery evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed on port, project, volume and offline provenance drift</name>
  <reversibility rating="one-way">D-10 is already user-locked because weakening exact volume identity could irrecoverably lose the retained blog database or media; no new decision checkpoint is warranted.</reversibility>
  <files>scripts/refresh-local.mjs, scripts/refresh-local-runtime-core.mjs, scripts/refresh-local-facts.mjs, scripts/refresh-local.test.mjs, apps/api/Dockerfile.refresh, apps/web/Dockerfile.refresh</files>
  <read_first>
    <item>scripts/refresh-local.mjs — `createRefreshPlan`, `runLocalRefresh`, `probeOfflineBuilds` and the current preflight/build phase sequence.</item>
    <item>scripts/refresh-local-runtime-core.mjs — `assertAllowedRefreshCommand`, `createRawRefreshFactSources`, preflight/build/target/migration/cutover/rollback ordering.</item>
    <item>scripts/refresh-local-facts.mjs — `REFRESH_AUTHORITY`, `assertFixedRuntimeAuthority`, persistence transitions and sanitized projection.</item>
    <item>scripts/refresh-local.test.mjs — exact argv, Compose v5 JSON/NDJSON, authority, target and rollback fixtures.</item>
    <item>apps/api/Dockerfile.refresh — API seed-store/offline build and current refresh-kind label.</item>
    <item>apps/web/Dockerfile.refresh — Web offline build, embedded `PUBLIC_ORIGIN` and current refresh-kind label.</item>
    <item>compose.yaml — canonical service names, healthchecks, loopback 3100 mapping and two named volumes; read-only source of truth.</item>
    <item>scripts/refresh-seed-store.mjs — versioned neutral-store and verify-before-delete contract; do not duplicate it.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-CONTEXT.md — D-04 and D-06 through D-13 plus D-19.</item>
  </read_first>
  <behavior>
    - "Preflight runs the exact read-only argv `docker ps --filter publish=3100 --format {{json .}}` and accepts exactly the inspected healthy `blogxlocal-web-1` record; zero, duplicate, malformed or foreign owners fail without kill/rebind/down behavior."
    - "Both Dockerfiles and every plan/target/evidence validator use refresh kind `v1.1-offline-local-delivery` and exact canonical origin."
    - "`seed-prerequisites` runs after read-only preflight and before either target build; trusted seed ID/config/lock/store checks classify exactly `missing`, `stale`, `incompatible`, `lock-drifted` or `incomplete-store`, and every class stops before build continuation, migration or cutover."
    - "Every classified D-08 failure emits one separate deterministic pre-warm instruction that names only the class and safe repository-managed remediation sequence; raw child output, image references/IDs, lock digests, host/store paths, credentials and environment values are excluded."
    - "Wrong Compose services/project/ports, unexpected/missing volumes, mutable/drifted seeds, incomplete neutral store or target label/filesystem drift fail before migration."
    - "Only target API one-off migration/schema commands and API/Web immutable cutover are allowlisted; PostgreSQL identity/data/media/sequence/ledger facts remain stable and post-cutover failure rolls back API/Web only."
  </behavior>
  <action>
    Add `LOCAL_DELIVERY_REFRESH_KIND = "v1.1-offline-local-delivery"` and update both refresh Dockerfiles, plan labels, probe checks, target validation and evidence validation together per D-06/D-07/D-09. Add exact `seed-prerequisites` to `createRefreshPlan().phases` immediately after `preflight` and before `build-api`; in `createRawRefreshRuntime.execute`, carry only the canonical running image reference/expected immutable ID out of preflight, then resolve and validate both seed images only in that new stage before any target build, migration or cutover. Add one literal internal `SEED_PREREQUISITE_PROGRAM` and its exact token-level `docker run --rm --network=none --entrypoint node <immutable-seed-id> -e ... <application>` allowlist shape to prove one versioned neutral pnpm store, nonempty store contents and the expected prepared application/runtime layout; do not accept a mutable tag for this filesystem probe. Export frozen `SEED_PREREQUISITE_KINDS` with exactly `missing`, `stale`, `incompatible`, `lock-drifted`, `incomplete-store`; implement pure `assertSeedPrerequisiteFacts(...)`, `classifySeedPrerequisiteFailure(error)` and `formatSeedPrewarmInstruction(classification)`. Create classified errors from trusted operation/validation branches rather than parsing stderr: image-inspect absence is `missing`; immutable-ID change is `stale`; invalid application/config/runtime shape is `incompatible`; a nonmatching committed-lock label is `lock-drifted`; a missing, unversioned or empty neutral store probe is `incomplete-store`. Retain raw command failure only as the internal cause.

    Per D-08, make `runRefreshCliBoundary` recognize only those typed classifications after publishing the bound failure report and write a separate fixed pre-warm instruction before rethrowing the sanitized classified error. The instruction may state the class and direct the operator to repair the repository-managed API/Web seeds for the committed lock, verify the fixed offline probe, commit the remediation and retry once from the new clean revision; it must not contain raw child output, image references/IDs, lock digests, host/store paths, credentials, alternate project/port/volume values, registry retrieval or authority-changing steps. Table-test all five classes through the real runtime/test-core boundary, including exact phase stop order and captured output, and prove unknown build/source failures are not mislabeled as seed prerequisites.

    Add the exact read-only Docker published-port command to `assertAllowedRefreshCommand`; parse only Docker JSON records and implement `assertCanonicalPortOwner` so preflight proves the sole published 3100 owner is the same ID/name/project/service as the inspected canonical Web container. Carry only a sanitized `topology.portOwnerExact: true` into projections. Extend table-driven tests for zero/duplicate/malformed/foreign/canonical owners and prove no kill, port switch, Compose `down`, volume removal or alternate project argv becomes allowed. Preserve the exact immutable seed ID, committed lock digest, populated neutral versioned-store and target filesystem/label checks from D-07; retain D-10/D-11/D-12/D-13 ordering and exact persistence/rollback tests; do not change `compose.yaml` or `refresh-seed-store.mjs`.
  </action>
  <acceptance_criteria>
    <criterion>Canonical owner facts, three health states, exact services/ports/project and two named volumes are required before any migration or cutover.</criterion>
    <criterion>Offline build argv and both target images prove immutable seed ID, lock digest, populated versioned store, new refresh kind and exact canonical public origin.</criterion>
    <criterion>All five D-08 classes arise only from explicit seed validation branches in `seed-prerequisites`, stop before target build continuation/migration/cutover, and unknown failures remain unclassified.</criterion>
    <criterion>Each classified failure produces exactly one separate `formatSeedPrewarmInstruction` result through `runRefreshCliBoundary`; output is deterministic and excludes raw child output, references/IDs, digests, paths, secrets and any authority-changing remedy.</criterion>
    <criterion>Every wrong/ambiguous authority test fails without terminating a listener, deleting a container/volume or creating alternate data authority.</criterion>
    <criterion>Migration and rollback tests prove existing rows/media/sequences/ledger history and PostgreSQL identity survive.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/refresh-local.mjs &amp;&amp; node --check scripts/refresh-local-runtime-core.mjs &amp;&amp; node --test scripts/refresh-local.test.mjs &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The tracer is hardened so canonical delivery cannot start from ambiguous port/data/image authority and remains strictly offline and reversible at the API/Web cutover boundary.</done>
</task>

</tasks>

## Atomic commit sequence

1. Commit the sealed v1.1 tracer entry/path/schema as `feat(08-01): establish local delivery tracer`.
2. Commit exact port/data/offline provenance hardening as `feat(08-01): harden canonical refresh authority`.
3. Commit `08-01-SUMMARY.md` separately; do not run the real no-argument delivery attempt in this plan.

## Artifacts this phase produces

- `package.json` script `local:deliver` — exact `node scripts/refresh-local.mjs`, with no flags.
- `scripts/refresh-local.mjs` resolves clean status, non-detached `refs/heads/<name>` and full HEAD before claim, and returns the v1.1 format/version.
- `scripts/refresh-local.mjs` inserts exact `seed-prerequisites` after `preflight` and before both offline target builds.
- `scripts/refresh-local-runtime-core.mjs` exports `LOCAL_DELIVERY_EVIDENCE_PATH`, `LOCAL_DELIVERY_FORMAT`, `LOCAL_DELIVERY_VERSION`, `LOCAL_DELIVERY_REFRESH_KIND`, `SEED_PREREQUISITE_KINDS`, `assertSeedPrerequisiteFacts`, `classifySeedPrerequisiteFailure`, and `formatSeedPrewarmInstruction`.
- `scripts/refresh-local-facts.mjs` exports `assertCanonicalPortOwner`, adds sanitized schema field `topology.portOwnerExact`, and requires exact collected/projected Git key `git.ref`.
- `scripts/refresh-local-live.mjs` imports/re-exports `LOCAL_DELIVERY_EVIDENCE_PATH` and resolves the sealed production verifier only to that current receipt; the ambiguous generic `EVIDENCE_PATH` export is removed.
- `scripts/refresh-local-test-core.mjs` runs the raw `git symbolic-ref --quiet HEAD` process boundary and resolves its verifier only to `/virtual-workspace/ops/v1.1-local-delivery-evidence.json` through the shared constant.
- `scripts/refresh-local.test.mjs` proves exact symbolic-ref ordering, detached/empty/malformed/ref-drift failure, all five seed-prerequisite classes plus separate sanitized pre-warm output, live/test-core constant linkage, the sole v1.1 virtual path and historical-path rejection.
- Collected/projected Git facts gain exact schema field `git.ref`, populated only by `git symbolic-ref --quiet HEAD`, restricted to `refs/heads/<name>` and equality-bound across all evidence stages plus reconstruction.
- `apps/api/Dockerfile.refresh` carries exact provenance label `io.blog-x.refresh-kind=v1.1-offline-local-delivery`.
- `apps/web/Dockerfile.refresh` carries the same exact v1.1 provenance label and embedded `PUBLIC_ORIGIN=http://127.0.0.1:3100`.
- `ops/v1.1-local-delivery-evidence.json` is reserved as the only new final receipt path; this plan tests its writer but does not create the live artifact.
- Evidence identifiers are `blog-x-v1.1-local-delivery-evidence` version `1` and refresh kind `v1.1-offline-local-delivery`.
- No new CLI operational flag, environment override, Compose project, volume, schema, dependency, server, SSH, deployment or release capability is produced.

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Risk | Control |
|---|---|---|
| Git/worktree -> delivery plan | uncommitted or unprovable source spoofing | clean porcelain plus lowercase full HEAD before claim/adapter |
| host/Docker -> canonical runtime | foreign port/project/container authority | exact local Unix Docker authority, published-port JSON and inspect labels |
| seed/lock -> target images | registry fallback or image provenance substitution | immutable IDs, lock digest, neutral store, network-none/pull-false and exact labels |
| refresh -> retained PostgreSQL/media | destructive replacement or partial migration | exact two volumes, before/after digests, one-off forward migration and no removal argv |
| evidence -> operator | stale/overwritten or secret-bearing proof | exclusive atomic publication, exact schema, redacted digests and current SHA |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01-01 | Spoofing | Git/image/Compose identity | high | mitigate | Full SHA, immutable image IDs, exact labels and canonical Compose names are cross-checked before mutation. |
| T-08-01-02 | Tampering | retained volumes and receipt | high | mitigate | Exact volume identity plus pre/post persistence digests and atomic non-overwrite publication. |
| T-08-01-03 | Repudiation | concurrent refresh attempt | high | mitigate | D-14 durable per-SHA claim has exactly one winner and every post-claim failure publishes a bound report. |
| T-08-01-04 | Information Disclosure | diagnostics/evidence | medium | mitigate | Store only sanitized facts/digests and stage-safe remediation; retain source-policy redaction tests. |
| T-08-01-05 | Denial of Service | port owner/build/cutover | medium | accept | Bounded local commands may fail closed and require operator remediation; no process is killed or port silently changed. |
| T-08-01-06 | Elevation of Privilege | child command boundary | high | mitigate | Token-level allowlist, minimal environment and local Unix Docker socket only. |
</threat_model>

<verification>
- Run both task commands; every Node test must report a real nonzero pass set with no skip/TODO.
- Confirm the runtime writer/CLI, live verifier and test core consume `LOCAL_DELIVERY_EVIDENCE_PATH`, and focused tests accept only `ops/v1.1-local-delivery-evidence.json` as the v1.1 receipt path.
- Confirm production/test revision resolution and fact reconstruction execute exact `git symbolic-ref --quiet HEAD`, retain one valid `git.ref`, and fail detached/empty/malformed/ref-drift fixtures before mutation.
- Confirm `seed-prerequisites` precedes both builds and that every D-08 class produces the shared sanitized pre-warm instruction through `runRefreshCliBoundary` before any migration/cutover command; unclassified failures must not borrow that guidance.
- Inspect the source diff to confirm `compose.yaml`, historical Phase 6 receipt and all canonical volumes are unchanged.
- Confirm the historical Phase 6 path appears only as preserved historical-artifact coverage, never as a current writer/verifier/CLI constant, and no second v1.1 path is introduced.
- Confirm no operational delivery flags, alternate project/origin/volume knobs, registry commands, broad cleanup, server addresses or remote commands were introduced.
</verification>

<success_criteria>
- One exact package command reaches the established refresh stack and current v1.1 receipt authority.
- Runtime, sealed live verification and in-memory CLI tests share the single v1.1 receipt constant/path with no stale-path ambiguity.
- Branch-qualified Git facts and the symbolic-ref process boundary reject detached or drifting source authority before mutation.
- Offline image provenance, exact 3100 owner, project/services/ports/volumes and clean SHA fail closed before mutation.
- Missing, stale, incompatible, lock-drifted and incomplete-store seeds have deterministic typed failures and a separate safe pre-warm instruction with no raw output or authority expansion.
- Migration/cutover/rollback retain user data and never remove unrelated resources.
- Terminal local evidence remains sanitized and production state remains `BLOCKED`.
</success_criteria>

<output>
Create `.planning/phases/08-reliable-local-delivery/08-01-SUMMARY.md` when done.
</output>
