---
phase: 07-responsive-discovery-experience
plan: "04"
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - apps/web/e2e/public-discovery-fixture.ts
  - apps/web/e2e/public-discovery.spec.ts
  - scripts/phase7-browser-verify.mjs
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - READ-08
  - READ-09
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "UI[empty][covered]: independent Chromium checks blank/zero/out-of-range search and hidden true-zero related outcomes."
    - "UI[loading][covered]: network observation proves SSR navigation with no typing fetch, spinner, client result authority or stale substitution."
    - "UI[error][covered]: invalid/upstream search and failed related outcomes show distinct opaque recovery while the article remains readable."
    - "UI[populated][covered]: ordered search and related cards, counts, page state, actions and navigation pass real-browser assertions."
    - "UI[partial][covered]: malformed JSON/DTO becomes strict failure and never leaks a partial card or diagnostic."
    - "UI[overflow][covered]: exact 375/768/1280 geometry, columns, wrapping and scrollWidth checks pass."
    - "UI[zero-one-many][covered]: search zero/one/ten/eleven and related zero/one/four are independently exercised."
    - "UI[long-text][covered]: hostile/long Unicode and taxonomy remain escaped, bounded, wrapped and keyboard accessible."
  artifacts:
    - path: apps/web/e2e/public-discovery-fixture.ts
      provides: finite strict matrix for all search/related/lifecycle/failure acceptance paths
    - path: apps/web/e2e/public-discovery.spec.ts
      provides: independent full Phase 7 real-browser acceptance suite
    - path: scripts/phase7-browser-verify.mjs
      provides: one-command no-Docker Phase 7 gate with guaranteed exact-child cleanup
  key_links:
    - from: scripts/phase7-browser-verify.mjs
      to: apps/web/e2e/public-discovery.spec.ts
      via: full invocation runs every Phase 7 acceptance test without grep or skip
      pattern: "playwright|public-discovery.spec"
    - from: apps/web/e2e/public-discovery.spec.ts
      to: apps/web/e2e/public-discovery-fixture.ts
      via: finite symbolic modes cover strict success/failure/lifecycle behavior
      pattern: "search|related|lifecycle"
    - from: apps/web/e2e/public-discovery.spec.ts
      to: apps/web/app/search/page.tsx
      via: browser observes only generated Web routes, metadata and same-origin API smoke
      pattern: "/search|canonical|robots"
  prohibitions:
    - {status: unverified, flagged: true}
    - {status: unverified, flagged: true}
    - {status: unverified, flagged: true}
---

## Phase Goal

**As the** Blog X owner, **I want to** run one independent local acceptance gate, **so that** every Phase 7 search, related, responsive, SEO, privacy and topology promise is verified before later deployment work.

<objective>
Expand the retained generated-port fixture/spec into the complete acceptance matrix, then run a full independent gate with all unit, type and boundary regressions.

Purpose: prevent focused implementation tests from becoming the only evidence for the phase.
Output: finite fixture scenarios, exhaustive Chromium suite and one-command clean local gate.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@apps/web/AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-responsive-discovery-experience/07-CONTEXT.md
@.planning/phases/07-responsive-discovery-experience/07-UI-SPEC.md
@.planning/phases/07-responsive-discovery-experience/07-RESEARCH.md
@.planning/phases/07-responsive-discovery-experience/07-01-SUMMARY.md
@.planning/phases/07-responsive-discovery-experience/07-02-SUMMARY.md
@.planning/phases/07-responsive-discovery-experience/07-03-SUMMARY.md
@apps/web/e2e/public-discovery-fixture.ts
@apps/web/e2e/public-discovery.spec.ts
@scripts/phase7-browser-verify.mjs

<interfaces>
- Fixture controls are finite named modes only; valid payloads parse through shared strict schemas before send and malformed modes cannot become arbitrary response/proxy capability.
- Full `node scripts/phase7-browser-verify.mjs` runs without grep, Docker, fixed 3100, downloads, cloud/server access or broad process cleanup.
- All contexts record HTTP(S) requests and accept only the generated Web origin, including JavaScript-disabled navigation.
- The suite explicitly names all 20 edge IDs, eight UI categories and locked D-01 through D-16 decisions.
- No production source, package, API route, database/schema, Sitemap/RSS source, server or deployment file is changed by this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the strict browser matrix across every state, edge and privacy boundary</name>
  <files>apps/web/e2e/public-discovery-fixture.ts, apps/web/e2e/public-discovery.spec.ts, scripts/phase7-browser-verify.mjs</files>
  <read_first>
    <item>apps/web/e2e/public-discovery-fixture.ts — existing finite tracer/related modes and strict-response parsing.</item>
    <item>apps/web/e2e/public-discovery.spec.ts — retained 07-01 tracer and 07-03 focused implementation proofs.</item>
    <item>scripts/phase7-browser-verify.mjs — generated-port lifecycle and grep/full-run interface.</item>
    <item>apps/web/e2e/public-errors.spec.ts — HTTP/refusal/malformed and diagnostic non-disclosure analog.</item>
    <item>apps/web/e2e/phase3-distribution.spec.ts — canonical/noindex/Sitemap/RSS regression analog.</item>
    <item>.planning/phases/07-responsive-discovery-experience/07-01-PLAN.md — tracer and same-origin acceptance baseline.</item>
    <item>.planning/phases/07-responsive-discovery-experience/07-02-PLAN.md — 12 search edge truths and SEO matrix.</item>
    <item>.planning/phases/07-responsive-discovery-experience/07-03-PLAN.md — eight related edges and responsive implementation baseline.</item>
    <item>.planning/phases/07-responsive-discovery-experience/07-UI-SPEC.md — complete Browser Verification Contract and exact copy.</item>
  </read_first>
  <behavior>
    - "Playwright contains an exact named test or describe block `phase 7 edge and privacy matrix`, and focused grep discovers at least one test."
    - "Every strict search/related success, empty, invalid, out-of-range and failure state has semantic assertions rather than screenshots."
    - "Every 20-edge ID and hidden/diagnostic sentinel has an explicit positive or negative assertion."
    - "SEO, lifecycle, concurrency and same-origin behavior remains honest under repeat requests and hostile inputs."
  </behavior>
  <action>
    Extend only finite fixture modes: search empty/one/ten/eleven/out-of-range; HTTP 400/500/503, refusal, malformed JSON/DTO; related one/four/zero/failure/malformed; and one allowlisted symbolic lifecycle transition that removes a formerly public candidate on the next request. Strict-parse every successful fixture body and reject unknown control modes.

    Expand `public-discovery.spec.ts` with an exact named Playwright `test` or `describe` block `phase 7 edge and privacy matrix`; do not use a near-match or comment-only label. Under that block, add named semantic assertions for the 12 search and eight related edge truths from 07-02/07-03: duplicate/unknown/malformed/boundary/precision/ordering/concurrency; hidden-only/search dedup; related source exclusion/order/zero/failure/idempotency/concurrency. Assert hostile text escaping, absence of hidden lifecycle, raw Markdown, rank/score, IDs, stack/schema details, origins and both cloud IPs. Exercise D-07 body/head matrix: all search noindex, canonical only for exact normalized real shapes, page-one omission/page-two retention, `/search` absent from Sitemap and existing public RSS/Sitemap entries unchanged. Use only Web-origin page/API observations. The focused `--grep "phase 7 edge and privacy matrix"` path must reuse the runner's nonzero-test enforcement and fail if Playwright discovers or executes zero matching tests.
  </action>
  <acceptance_criteria>
    <criterion>The suite contains explicit named assertion blocks for all 20 edge IDs and eight UI considerations with no screenshot-only substitute.</criterion>
    <criterion>An exact named Playwright `test` or `describe` block `phase 7 edge and privacy matrix` exists, and the focused runner observably reports at least one executed test rather than succeeding on zero matches.</criterion>
    <criterion>Blank/zero/out-of-range/invalid/upstream and related zero/failure remain distinct with exact copy/actions and primary article retention.</criterion>
    <criterion>Repeated, concurrent and symbolic lifecycle requests are read-only/deterministic and immediately reflect valid public-state change.</criterion>
    <criterion>Metadata, Sitemap/RSS, privacy sentinels and same-origin assertions cover valid and hostile paths.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase7-browser-verify.mjs --grep "phase 7 edge and privacy matrix"</automated>
  </verify>
  <done>The retained real-browser suite has exhaustive finite evidence for every required state, edge, SEO and privacy boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run and harden the independent full Phase 7 acceptance gate</name>
  <files>apps/web/e2e/public-discovery-fixture.ts, apps/web/e2e/public-discovery.spec.ts, scripts/phase7-browser-verify.mjs</files>
  <read_first>
    <item>scripts/phase7-browser-verify.mjs — Task 1 full matrix runner and exact-child cleanup.</item>
    <item>apps/web/e2e/public-discovery.spec.ts — all focused and full acceptance blocks; none may be skipped or filtered.</item>
    <item>apps/web/e2e/public-discovery-fixture.ts — finite strict mode vocabulary and signal shutdown.</item>
    <item>scripts/local-verify.test.mjs — child cleanup/failure-path test analog.</item>
    <item>scripts/check-boundaries.mjs — source boundary regression gate.</item>
    <item>.planning/phases/07-responsive-discovery-experience/07-UI-SPEC.md — 375/768/1280, keyboard, theme and no-JS completion contract.</item>
  </read_first>
  <behavior>
    - "An unfiltered runner executes every tracer, related, responsive, state, SEO, privacy and topology assertion."
    - "375/768/1280, no-JavaScript, light/dark/system, focus/Escape/tab and 44px rules pass in the independent suite."
    - "Success and forced failure leave no exact fixture/Web child alive and never touch unrelated processes."
  </behavior>
  <action>
    Harden the runner only where full execution reveals a real lifecycle gap: require nonzero discovered tests, reject skipped/TODO acceptance, use workers=1, bounded health/test/termination waits, exact spawned handles and `SIGTERM` then bounded `SIGKILL` fallback in `finally`. Add a controlled forced-failure self-check if the analog supports it without broad process inspection. Run the suite with no grep so D-01 through D-16 are independently covered, including exact 375/768/1280 overflow/columns, computed 44px targets, compact menu tab/Escape/focus restoration, light/dark/system, reduced-motion-compatible behavior, no-JavaScript submit and all-request same-origin observation. Then run contract tests, focused resolver/metadata tests, recursive typecheck, boundary audit and diff check; fix only test harness defects inside the three listed files.
  </action>
  <acceptance_criteria>
    <criterion>`node scripts/phase7-browser-verify.mjs` runs unfiltered with nonzero tests, zero skip/TODO and all D-01–D-16 assertions.</criterion>
    <criterion>Every 375/768/1280, 44px, menu tab/Escape, theme/no-JS and no-overflow assertion passes independently of the 07-03 focused grep.</criterion>
    <criterion>Both normal and forced-failure cleanup leave no exact managed child alive and do not inspect/kill unrelated processes.</criterion>
    <criterion>Contracts, resolver/metadata tests, recursive typecheck, boundary audit and diff check all exit zero.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase7-browser-verify.mjs &amp;&amp; corepack pnpm --filter @blog-x/contracts test &amp;&amp; node --import tsx --test apps/web/lib/search-encoding.test.ts apps/web/app/lib/search-discovery.test.ts apps/web/app/lib/site-metadata.test.ts &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>One clean local command plus regressions independently proves the complete Phase 7 visitor contract and always cleans up its own children.</done>
</task>

</tasks>

## Atomic commit sequence

1. Commit the finite full-state fixture and exhaustive browser matrix as `test(07-04): cover discovery acceptance matrix`.
2. Commit the hardened independent gate as `test(07-04): close phase 7 browser gate`.
3. Commit `07-04-SUMMARY.md` separately; do not create Phase 8 release evidence.

## Artifacts this phase produces

- `apps/web/e2e/public-discovery-fixture.ts`: full finite strict acceptance fixture with clean shutdown.
- `apps/web/e2e/public-discovery.spec.ts`: unfiltered semantic Phase 7 browser suite.
- `scripts/phase7-browser-verify.mjs`: one-command generated-port no-Docker gate with exact-child cleanup.
- No production, dependency, API, database/schema, Docker, server, deployment or release artifact is produced.

## Source coverage audit

| Source | IDs / outcome | Executable owner | Status |
|---|---|---|---|
| ROADMAP / REQ | Phase goal; SRCH-01, SRCH-02, READ-08, READ-09 | 07-01 tracer; 07-02 search completion; 07-03 related/responsive; 07-04 full gate | COVERED |
| CONTEXT | D-01 desktop form; D-02 compact placement; D-03 native keyboard/touch/no-JS; D-04 stable GET/no live search | 07-01 Task 1; 07-04 Task 2 | COVERED |
| CONTEXT | D-05 count/actions/pagination; D-06 same-origin server adapter; D-07 noindex/canonical; D-08 compact card | 07-02 Tasks 1/2; 07-04 Tasks 1/2 | COVERED |
| CONTEXT | D-09 same order; D-10 honest states; D-11 related placement/columns; D-12 strict related cards | 07-02 Task 2; 07-03 Task 1; 07-04 | COVERED |
| CONTEXT | D-13 zero hidden; D-14 local failure; D-15 700/1023/theme/44px/focus; D-16 real 375/768/1280 proof | 07-03 Tasks 1/2; 07-04 Task 2 | COVERED |
| RESEARCH | R-01 whole-object/raw seam; R-02 shared outcome/canonical; R-03 server-only strict adapters; R-04 shared cards/pagination | 07-01/07-02 | COVERED |
| RESEARCH | R-05 related failure isolation; R-06 closed controls/long text/themes; R-07 generated-port strict harness; R-08 scope exclusions | 07-03/07-04 and all plan boundaries | COVERED |
| UI | empty, loading, error, populated, partial, overflow, zero-one-many, long-text | every plan `must_haves.truths`; 07-04 browser matrix | COVERED |
| EDGE | SRCH-01 adjacency, empty, encoding, ordering, concurrency | 07-02 truths/tests; 07-04 Task 1 | COVERED |
| EDGE | SRCH-02 boundary, adjacency, empty, encoding, ordering, precision, concurrency | 07-02 truths/tests; 07-04 Task 1 | COVERED |
| EDGE | READ-08 adjacency, empty, ordering | 07-03 truths/Task 1; 07-04 Task 1 | COVERED |
| EDGE | READ-09 adjacency, empty, ordering, idempotency, concurrency | 07-03 truths/tasks; 07-04 Task 1 | COVERED |
| PROHIB | P-01 no hidden/internal disclosure; P-02 no failure-as-zero/article suppression; P-03 no browser backend bypass | every plan descriptor-less flagged prohibition; 07-04 hostile matrix | COVERED, FLAGGED-UNVERIFIED |

Audit result: all four requirements, D-01 through D-16, eight UI considerations, 20 unique fallback edges and three prohibition categories map to executable work. No deferred server, Docker, deployment, schema or Phase 8 scope is introduced.

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Risk | Control |
|---|---|---|
| Browser -> generated Web | fixture/direct-origin bypass | record every context request and reject non-Web origins |
| Web -> strict fixture | hostile HTTP/JSON/DTO | finite modes and shared schema on valid payloads |
| Fixture controls -> state | arbitrary response/process capability | allowlisted symbolic modes only |
| Runner -> child processes | leak or unrelated kill | generated ports and exact handles with bounded cleanup |
| URL/outcome -> metadata | canonical poisoning or body/head split | real navigation matrix over PUBLIC_ORIGIN |

## STRIDE Threat Register

| ID | Category | Severity | Mitigation |
|---|---|---|---|
| T-07-04-01 | Spoofing | high | Require generated Web origin for every observed browser request. |
| T-07-04-02 | Tampering | high | Strict success schemas and finite named malformed modes. |
| T-07-04-03 | Information Disclosure | high | Scan body/head/hrefs/errors for hidden fields, diagnostics, origins and IPs. |
| T-07-04-04 | Repudiation | high | Require nonzero unfiltered semantic tests and zero skip/TODO. |
| T-07-04-05 | Denial of Service | medium | Bound health/test/cleanup and manage exact children only. |
| T-07-04-06 | Elevation of Privilege | high | Loopback-only fixture with no arbitrary proxy/filesystem/process command. |
</threat_model>

<verification>
- Run the complete unfiltered command and all regression commands from Task 2.
- Confirm focused 07-03 greps still pass but are not substituted for the full gate.
- Confirm no Docker, SSH, cloud/server, deployment, fixed-3100 refresh or external download appears in source or logs.
</verification>

<success_criteria>
- One independent full browser run covers all requirements and D-01 through D-16.
- All eight UI categories and all 20 unique edge predicates have named semantic evidence.
- Search/related privacy, failure honesty, article availability, SEO and same-origin topology pass hostile scenarios.
- Cleanup and all unit/type/boundary/diff regressions pass without production changes.
</success_criteria>

<output>
Create `.planning/phases/07-responsive-discovery-experience/07-04-SUMMARY.md` when done.
</output>
