---
phase: 07-responsive-discovery-experience
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - apps/web/app/lib/api.ts
  - apps/web/app/posts/[slug]/page.tsx
  - apps/web/app/_components/PostCard.tsx
  - apps/web/app/_components/SearchForm.tsx
  - apps/web/app/_components/PublicHeader.tsx
  - apps/web/app/_components/Pagination.tsx
  - apps/web/app/search/page.tsx
  - apps/web/app/public.module.css
  - apps/web/e2e/public-discovery-fixture.ts
  - apps/web/e2e/public-discovery.spec.ts
  - scripts/phase7-browser-verify.mjs
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - READ-08
  - READ-09
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "UI[empty][covered]: true zero related items hides the whole section; empty search remains an exact card-free terminal state."
    - "UI[loading][covered]: primary and related content are server-rendered with no secondary spinner, layout shift, stale substitution or client fetch."
    - "UI[error][covered]: related failure preserves the complete article and shows no false zero/404; search failure keeps its distinct recovery state."
    - "UI[populated][covered]: one-to-four related compact cards follow the article and search results keep the same public field vocabulary."
    - "UI[partial][covered]: malformed related/search objects fail strict parsing; optional public fields alone use established fallbacks."
    - "UI[overflow][covered]: 375/768/1280 have no horizontal overflow; related is one/auto/two columns and information order is unchanged."
    - "UI[zero-one-many][covered]: related zero/one/four and search zero/one/many are semantically and geometrically distinct."
    - "UI[long-text][covered]: long query/title/summary/taxonomy wraps safely in all widths/themes without unsafe HTML or focus loss."
    - "EDGE[READ-08/adjacency][covered]: the source article is excluded and adjacent related cards remain independent and deduplicated."
    - "EDGE[READ-08/empty][covered]: no shared taxonomy yields no fabricated card, heading or empty placeholder."
    - "EDGE[READ-08/ordering][covered]: Web preserves exact Phase 6 related order and never exposes/reranks by score."
    - "EDGE[READ-09/adjacency][covered]: identical fields/DOM order survive two-, auto- and one-column layouts."
    - "EDGE[READ-09/empty][covered]: true zero is hidden while HTTP/refusal/malformed failure is distinct, local and preserves the article."
    - "EDGE[READ-09/ordering][covered]: related API/DOM order stays identical across widths and light/dark/system themes."
    - "EDGE[READ-09/idempotency][covered]: the next SSR request reflects a lifecycle change with no stale/fabricated related state."
    - "EDGE[READ-09/concurrency][covered]: concurrent primary/related reads are isolated and cannot suppress or mutate the article."
  artifacts:
    - path: apps/web/app/lib/api.ts
      provides: strict server-only related-posts adapter over the existing internal transport
      exports: [getPublicRelatedPosts]
    - path: apps/web/app/posts/[slug]/page.tsx
      provides: isolated strict related-content composition after primary article body
    - path: apps/web/e2e/public-discovery.spec.ts
      provides: focused related TDD and responsive implementation acceptance tests
    - path: scripts/phase7-browser-verify.mjs
      provides: focused related and responsive grep execution on the retained generated-port harness
  key_links:
    - from: apps/web/app/posts/[slug]/page.tsx
      to: apps/web/app/lib/api.ts
      via: related fetch is isolated from the already-successful primary article outcome
      pattern: "getPublicRelatedPosts|Promise"
    - from: apps/web/app/posts/[slug]/page.tsx
      to: apps/web/app/_components/PostCard.tsx
      via: ordered strict related items use the same compact projection after the article body
      pattern: "PostCard|related"
    - from: apps/web/e2e/public-discovery.spec.ts
      to: scripts/phase7-browser-verify.mjs
      via: named related and responsive focused grep contracts
      pattern: "related populated zero and failure|responsive discovery implementation"
  prohibitions:
    - {status: unverified, flagged: true}
    - {status: unverified, flagged: true}
    - {status: unverified, flagged: true}
---

## Phase Goal

**As a** visitor on phone, tablet or desktop, **I want to** continue from an article into honest related reading, **so that** discovery remains usable regardless of content count, failure, theme or input method.

<objective>
Add strict related content as a tested vertical article slice, then complete production responsive/keyboard/no-JavaScript/theme behavior with focused real-browser proof inside the implementation tasks.

Purpose: finish the visitor-facing discovery experience before the independent Phase 7 gate.
Output: resilient related rendering and verified 375/768/1280 interaction/layout behavior.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@apps/web/AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-responsive-discovery-experience/07-CONTEXT.md
@.planning/phases/07-responsive-discovery-experience/07-UI-SPEC.md
@.planning/phases/07-responsive-discovery-experience/07-RESEARCH.md
@.planning/phases/07-responsive-discovery-experience/07-02-SUMMARY.md
@packages/contracts/src/public-discovery.ts
@apps/web/app/posts/[slug]/page.tsx
@apps/web/e2e/public-discovery.spec.ts
@scripts/phase7-browser-verify.mjs

<interfaces>
- `getPublicRelatedPosts(slug)` uses the existing server-only strict transport and `publicRelatedPostsResponseSchema`; browser-visible URLs remain relative/same-origin.
- Primary article success is authoritative. Related failure is caught locally and cannot throw into article not-found/error handling.
- Related true zero renders no section; populated results render after the body, preserve API order, exclude source, cap at four and expose no ranking field.
- Existing 700px and 1023px authorities govern compact menu and related two/auto/one-column layout; DOM/information order stays constant.
- All interactive controls retain at least 44px target, visible focus, theme support and reduced-motion behavior.
- No dependency, API route, database/schema, Docker, server, deployment or fixed-port change is allowed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add related cards with RED/GREEN proof for populated, zero and failure</name>
  <files>apps/web/app/lib/api.ts, apps/web/app/posts/[slug]/page.tsx, apps/web/app/_components/PostCard.tsx, apps/web/app/public.module.css, apps/web/e2e/public-discovery-fixture.ts, apps/web/e2e/public-discovery.spec.ts, scripts/phase7-browser-verify.mjs</files>
  <read_first>
    <item>packages/contracts/src/public-discovery.ts — exact related response schema, cap and strict fields.</item>
    <item>apps/web/app/lib/api.ts — existing strict server-only adapter seam.</item>
    <item>apps/web/app/posts/[slug]/page.tsx — primary article success/not-found/error authority and body order.</item>
    <item>apps/web/app/_components/PostCard.tsx — compact strict public projection created in 07-02.</item>
    <item>apps/web/e2e/public-discovery-fixture.ts — 07-01 finite loopback fixture to expand.</item>
    <item>apps/web/e2e/public-discovery.spec.ts — 07-01 tracer spec to expand with related RED/GREEN cases.</item>
    <item>scripts/phase7-browser-verify.mjs — generated-port exact-child runner and focused grep interface.</item>
    <item>apps/web/e2e/public-errors.spec.ts — closest article-retention versus error-boundary assertion analog.</item>
    <item>.planning/phases/07-responsive-discovery-experience/07-UI-SPEC.md — exact related heading, absence, order and recovery rules.</item>
  </read_first>
  <behavior>
    - "Two-to-four strict related items appear after the complete article body in exact API order and exclude the source."
    - "Zero related items removes the entire heading/list without placeholder."
    - "HTTP, refusal or malformed related failure preserves the article body and never becomes 404/no-results."
  </behavior>
  <action>
    Expand the existing Phase artifacts `apps/web/e2e/public-discovery-fixture.ts`, `apps/web/e2e/public-discovery.spec.ts` and `scripts/phase7-browser-verify.mjs`; the closest analogs remain the strict public-error fixture/spec and the generated-port local runner. In `public-discovery.spec.ts`, first write RED tests under the exact focused title `related populated zero and failure` for populated order/source exclusion/cap, true-zero section absence, and failed/malformed related response with the primary article still visible. Ensure `node scripts/phase7-browser-verify.mjs --grep "related populated zero and failure"` selects and executes these cases.

    Then implement GREEN: add strict `getPublicRelatedPosts` using `publicRelatedPostsResponseSchema`, compose it only after primary article success with a local caught outcome, and render compact PostCard items after the body. Preserve API order, maximum four, source exclusion and D-11 two/auto/one-column section placement; enforce D-12 strict fields/no score, D-13 true-zero hiding and D-14 failure isolation. The fixture may add only finite named related modes and must strict-parse valid payloads before sending.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/e2e/public-discovery.spec.ts` contains named RED/GREEN assertions for populated, zero-hidden and failure-preserves-article behavior.</criterion>
    <criterion>`scripts/phase7-browser-verify.mjs` accepts the focused related grep and runs a real Next article route against the strict local fixture.</criterion>
    <criterion>`apps/web/app/lib/api.ts` exports exact `getPublicRelatedPosts`, and both production and fixture success paths parse `publicRelatedPostsResponseSchema`.</criterion>
    <criterion>Related source is absent, one-to-four cards preserve API order, no score/Markdown/internal fields appear, and the section follows the body.</criterion>
    <criterion>HTTP/refusal/malformed related outcomes leave the article heading/body/permalink intact and render neither false zero nor 404.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase7-browser-verify.mjs --grep "related populated zero and failure" &amp;&amp; corepack pnpm --filter @blog-x/web typecheck</automated>
  </verify>
  <done>Related discovery is production-integrated and its populated, zero and failed paths are proven through the named real-browser seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete responsive discovery with focused 375/768/1280 interaction proof</name>
  <files>apps/web/app/_components/SearchForm.tsx, apps/web/app/_components/PublicHeader.tsx, apps/web/app/_components/PostCard.tsx, apps/web/app/_components/Pagination.tsx, apps/web/app/search/page.tsx, apps/web/app/posts/[slug]/page.tsx, apps/web/app/public.module.css, apps/web/e2e/public-discovery.spec.ts, scripts/phase7-browser-verify.mjs</files>
  <read_first>
    <item>apps/web/app/public.module.css — existing theme variables and 700px/1023px breakpoint authority.</item>
    <item>apps/web/app/_components/PublicHeader.tsx — compact menu state, closed tab exclusion, Escape and focus restoration.</item>
    <item>apps/web/app/_components/SearchForm.tsx — shared native form and 44px control contract.</item>
    <item>apps/web/app/_components/PostCard.tsx — shared default/compact information order.</item>
    <item>apps/web/app/_components/Pagination.tsx — semantic named navigation and focus surface.</item>
    <item>apps/web/e2e/public-shell.spec.ts — current 375/768/1280, theme, keyboard and no-JavaScript analog.</item>
    <item>apps/web/e2e/public-discovery.spec.ts — Task 1 retained strict fixture journey and focused grep seam.</item>
    <item>.planning/phases/07-responsive-discovery-experience/07-UI-SPEC.md — exact responsive Browser Verification Contract.</item>
  </read_first>
  <behavior>
    - "375/768/1280 retain identical content/DOM order with zero horizontal overflow and required one/auto/two-column related layout."
    - "Compact menu opens, tabs through visible controls only, submits search, closes with Escape and restores focus."
    - "Light/dark/system themes, visible focus, 44px controls and no-JavaScript form navigation remain usable."
  </behavior>
  <action>
    First extend `apps/web/e2e/public-discovery.spec.ts` with a named `responsive discovery implementation` block. Implement only the CSS/component changes required by D-15 and D-16: reuse 700px/1023px breakpoints and existing variables; ensure related two columns above 1023, auto-fit from 701–1023 and one column at/below 700; retain one DOM/information order; wrap long query/taxonomy/text; never use viewport-clipping fixed widths. Preserve closed-menu tab exclusion, Enter/click submission, Escape close/focus restoration and no-JavaScript native form. Keep targets at least 44px, visible `:focus-visible`, light/dark/system variables and reduced-motion rules.

    Before finishing this implementation task, run the focused Playwright grep and require executable assertions at exactly 375, 768 and 1280 for `scrollWidth <= clientWidth`, card/related order and columns; computed 44px form/menu/pagination targets; compact menu tab sequence plus Escape focus restoration; light/dark/system rendering; no-JavaScript submit; and absence of direct/internal origins. Do not defer these assertions to 07-04.
  </action>
  <acceptance_criteria>
    <criterion>The implementation-focused spec explicitly visits 375, 768 and 1280 and proves no horizontal overflow plus the expected related column geometry.</criterion>
    <criterion>At compact widths, only visible menu controls are tabbable, Enter submits search, Escape closes and restores focus, and no-JavaScript navigation still works.</criterion>
    <criterion>Search/menu/pagination controls compute to at least 44px with visible focus in light, dark and system themes.</criterion>
    <criterion>Long content wraps without ellipsis that hides meaning, source/article order does not change, and all browser requests remain same-origin.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase7-browser-verify.mjs --grep "responsive discovery implementation" &amp;&amp; corepack pnpm --filter @blog-x/web typecheck &amp;&amp; node scripts/check-boundaries.mjs</automated>
  </verify>
  <done>The actual implementation—not only the later full gate—has focused browser evidence for all locked widths, target sizes, menu semantics, themes and overflow rules.</done>
</task>

</tasks>

## Atomic commit sequence

1. Commit related vertical behavior and focused RED/GREEN browser proof as `feat(07-03): add resilient related reading`.
2. Commit responsive implementation and focused viewport/interaction proof as `feat(07-03): complete responsive discovery`.
3. Commit `07-03-SUMMARY.md` separately.

## Artifacts this phase produces

- `apps/web/app/lib/api.ts`: new Phase 7 exact export `getPublicRelatedPosts`, parsing `publicRelatedPostsResponseSchema` through the existing server-only transport.
- `apps/web/app/lib/api.ts` and `apps/web/app/posts/[slug]/page.tsx`: strict locally isolated related integration.
- `apps/web/app/_components/PostCard.tsx` and `apps/web/app/public.module.css`: one compact projection across related/search responsive layouts.
- `apps/web/e2e/public-discovery.spec.ts`: explicit related RED/GREEN seam plus focused responsive implementation tests.
- `scripts/phase7-browser-verify.mjs`: runnable `related populated zero and failure` and `responsive discovery implementation` greps.
- `apps/web/e2e/public-discovery-fixture.ts`: finite strict related populated/zero/failure scenarios.

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Risk | Control |
|---|---|---|
| Article outcome -> related outcome | secondary failure suppresses primary | local caught related state after primary success |
| Related API -> cards | hidden/source/scoring disclosure | strict schema, source exclusion, cap and preserved order |
| CSS/menu -> keyboard visitor | hidden focus or inaccessible target | actual viewport/tab/Escape/44px assertions |
| Browser -> origins | direct backend bypass | record all requests including no-JavaScript contexts |

## STRIDE Threat Register

| ID | Category | Severity | Mitigation |
|---|---|---|---|
| T-07-03-01 | Spoofing | high | Assert related/search navigation stays on generated Web origin. |
| T-07-03-02 | Tampering | high | Strict-parse finite related fixture and production responses. |
| T-07-03-03 | Information Disclosure | high | Exclude source/hidden/rank/score/Markdown/diagnostic fields. |
| T-07-03-04 | Repudiation | medium | Named RED/GREEN and responsive focused greps prove real route behavior. |
| T-07-03-05 | Denial of Service | medium | Cap related at four and reuse bounded exact-child harness. |
| T-07-03-06 | Elevation of Privilege | high | No arbitrary fixture control, client API origin or private surface. |
</threat_model>

<verification>
- Run both focused Playwright greps independently, then Web typecheck and boundary audit.
- Confirm related failure retains article HTML and zero state removes heading/list entirely.
- Confirm the responsive implementation task itself covers 375/768/1280, 44px, tab/Escape, themes, no-JS and overflow.
</verification>

<success_criteria>
- READ-08 and READ-09 plus all eight listed READ edge truths pass.
- D-11 through D-16 are implemented and evidenced in the same tasks that change production behavior.
- All eight UI considerations remain honest across search, article and related states.
</success_criteria>

<output>
Create `.planning/phases/07-responsive-discovery-experience/07-03-SUMMARY.md` when done.
</output>
