# Blog X

## What This Is

Blog X 是一套面向个人长期使用的自托管博客系统。访客可以在手机和桌面端快速阅读、发现与订阅公开文章；博主可以通过单管理员后台使用 Markdown 创建、预览、发布、下线、归档和迁移内容。

开发阶段以前端与入口的本地替代环境配合一台 2C4G Ubuntu 副服务器完成。备案审查结束且用户明确解除冻结后，前台与 HTTPS 入口才会部署到绑定 `huajieyu001.top` 的 2C2G 主服务器，后端与数据库继续由副服务器承载。

## Core Value

博主能够可靠地发布和保存 Markdown 内容，访客能够持续、快速地通过博客域名阅读已发布文章。

## Current Milestone: v1.2 Publishing Quality

**Goal:** 让博主可以安全地预约文章发布，并让已公开文章向搜索引擎提供严格、可验证的结构化数据。

**Target features:**
- 公开文章结构化数据
- 可取消、可改期且失败关闭的定时发布
- 幂等、有界且支持并发的本地发布任务
- 继续使用固定 `3100` 本地交付与验证链路

## Requirements

### Validated

- ✓ 单管理员可安全登录，并完成 Markdown 草稿、预览、发布、下线、改链和软删除闭环。 — v1.0 / Phase 1
- ✓ 访客可通过响应式首页、固定链接、分类、标签、归档、关于页和目录发现并阅读公开内容。 — v1.0 / Phases 1–2
- ✓ 图片上传、公开媒体、主题、错误恢复与移动端/桌面端体验均有同源浏览器验收。 — v1.0 / Phase 2
- ✓ Sitemap、robots.txt、RSS、规范链接、分享元数据和 Markdown 逻辑导出只消费公开内容。 — v1.0 / Phase 3
- ✓ 认证、CSRF、限流、输入、上传、凭据和公网拓扑边界已自动化加固。 — v1.0 / Phase 4
- ✓ 数据库、Markdown、媒体和配置具备完整集备份、恢复演练、保留、结果和告警契约。 — v1.0 / Phases 4–5
- ✓ 发布前后状态机、回滚门禁和提交绑定验收收据均保持本地成功与生产授权分离。 — v1.0 / Phases 4–5
- ✓ Phase 1–5 的 26 个计划通过独立验证，最终收据绑定 30 个实际结果与 503/503 通过计数。 — v1.0 / Phase 5
- ✓ 访客可按标题、摘要和正文搜索已发布文章；搜索入口、状态、分页和 SEO 策略在手机与桌面端均通过同源浏览器验收。 — v1.1 / Phases 6–7
- ✓ 文章详情可展示按分类与标签计算、结果稳定且仅含公开内容的相关文章，无匹配或异常时保持诚实状态。 — v1.1 / Phases 6–7
- ✓ 每个大步骤可用一条固定命令离线优先地更新本地 `3100` 环境、保留数据卷并执行健康检查。 — v1.1 / Phase 8

### Active

- [x] 编辑器支持异常刷新可恢复的自动保存。
- [ ] 管理员可设置未来发布时间并由受控任务自动发布。
- [x] 管理员可查看登录、发布、删除和配置变更的审计记录。
- [ ] 公开文章具备结构化数据。
- [ ] 可在明确隐私边界下提供聚合访问统计。

### Out of Scope

- 多作者与复杂角色权限 — 首期只有一个管理员。
- 用户社区、论坛、私信和即时聊天 — 偏离个人内容发布的核心价值。
- 会员、付费阅读和订单 — 首期无商业化目标。
- 个性化推荐算法 — 数据规模和收益不足以支撑复杂度。
- Kubernetes、微服务集群和多地域高可用 — 超出两台低配置服务器的合理运维范围。
- 视频与大文件托管 — 带宽和存储成本不可控，后续单独评估。
- 自建评论、邮件订阅和私密文章 — 需求尚未确认，且会显著扩大安全与合规范围。

## Context

- 已有 Hexo 博客源码和已发布站点快照存放在本地 `backups/`，可作为后续内容迁移来源。
- 原始需求基线位于仓库根目录 `REQUIREMENTS.md`，基础设施约定见 `docs/INFRASTRUCTURE.md`。
- 主服务器为 CentOS 7、2C2G，绑定博客域名，目前显示包含备案号的整改维护页。
- 副服务器为 Ubuntu、2C4G、无域名，计划承载 API、数据库、后台任务及备份；具体系统状态尚待只读核验。
- 两台服务器是否具备可用私网尚未确认；若无私网，正式上线前必须建立受限且加密的节点间连接。
- v1.0 已完成 5 个阶段、26 个计划和 25 项要求的独立验证；本地 Web/API/PostgreSQL 运行于 `blogxlocal` Compose 项目，入口为 `http://127.0.0.1:3100`。
- v1.1 已完成并归档：搜索、相关文章、响应式发现界面和可靠本地交付均通过自动化与独立验证。
- 本地展示环境必须在每个大步骤后重建、迁移并健康检查，避免验证环境通过但 `3100` 长期停留在旧镜像。
- 生产发布仍为 `BLOCKED`；未决产品问题（评论、统计深度、私密内容、图片规模、精确 RPO/RTO）继续作为后续决策项。

## Constraints

- **生产冻结**: 在用户明确解除前，不得连接、部署或修改主服务器 `47.99.80.8` — 备案信息正在等待监管审查。
- **开发拓扑**: 本地工作区暂代主服务器进行前台、入口和端到端验证 — 避免生产变更并支持快速迭代。
- **后端拓扑**: 副服务器 `124.222.91.230` 可用于后端、数据库和备份环境 — 无域名，因此浏览器不得直接依赖其公网地址。
- **资源**: 正式环境只有 2C2G + 2C4G — 避免重型搜索、微服务和常驻高内存组件。
- **安全**: 密码、私钥、令牌和数据库凭据不得写入仓库；数据库不得暴露公网 — 降低凭据泄漏与数据攻击风险。
- **内容持久性**: 正文、元数据、媒体和配置必须可导出、备份并完成恢复验证 — 博客内容是长期资产。
- **兼容性**: 前台必须适配现代移动端与桌面浏览器，并通过同一博客域名提供页面、API 和媒体 — 保持访问链路简单一致。
- **上线门禁**: 主服务器解除冻结、生产备份与回滚方案、节点间安全网络均验证通过后才能部署正式站点 — 避免影响备案审查和生产可用性。

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| 使用纵向 MVP 方式开发 | 尽早形成“登录—写作—发布—阅读”的可验证闭环 | ✓ Phase 1 已形成完整闭环 |
| 本地暂代主服务器 | 满足监管审查冻结，同时继续前台与入口开发 | ✓ Phase 1 全程未依赖主服务器 |
| 副服务器承担持久化与后台服务 | 利用 2C4G 资源并避免主服务器承载数据库 | — Pending |
| 浏览器只访问同域入口 | 不要求副服务器绑定域名，减少 CORS 与公网暴露 | ✓ Phase 1 通过相对 `/api` 与 Next 服务端代理验证 |
| 首期单管理员、无评论 | 控制认证、审核、反垃圾和隐私范围 | ✓ Phase 1 采用单一管理员会话边界 |
| P0 形成首个发布版本，P1 功能后续增量交付 | 保持 MVP 可在低资源环境中尽早上线 | ✓ Phase 1 完成发布闭环，Phase 2 继续阅读体验 |
| Web、API 与共享契约保持独立工作区边界 | 支持后续分离部署，并防止浏览器包引入数据库或认证内部实现 | ✓ Phase 1 边界检查与构建通过 |
| 原始 Markdown 是内容权威，预览与公开页共用服务端渲染器 | 防止预览与公开结果漂移，并集中执行不可信内容清理 | ✓ Phase 1 安全渲染与浏览器验收通过 |
| 所有保留文章永久占用 slug，生命周期变更使用显式动作 | 保护已暴露链接语义，避免客户端直接操纵状态 | ✓ Phase 1 发布、下线、改链与软删除验收通过 |
| 本地验收使用独立命名空间、迁移锁与指纹账本 | 并发或中断验证不得污染其他环境，重复迁移必须可审计收敛 | ✓ Phase 1 完整验收通过 |
| 导航、主题和目录采用渐进增强的响应式公共外壳 | 保证手机、平板、桌面和无脚本场景都保留完整阅读能力 | ✓ Phase 2 浏览器验收通过 |
| 公开分发统一消费严格的 published projection | RSS、Sitemap、SEO 与公开页面不得泄露草稿或内部拓扑 | ✓ Phase 3 分发与导出验收通过 |
| 本地 readiness 与生产 release authority 永久分离 | 通过测试、Git 状态或时间流逝都不能自动授权部署 | ✓ Phases 4–5 独立验证通过，生产仍 BLOCKED |
| 完整门禁收据绑定真实脱敏输出、源码摘要和终端决定 | 防止固定计数或合成摘要伪装成已执行验收 | ✓ Phase 5 最终 30-result v2 receipt 通过重构验证 |
| 每个大步骤后更新固定 `blogxlocal` 展示环境 | 用户应持续看到最新可运行界面，而不是只看到临时验收环境 | ✓ v1.1 / Phase 8 已验证 |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `$gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `$gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-09-04 for v1.2 Publishing Quality initialization*
