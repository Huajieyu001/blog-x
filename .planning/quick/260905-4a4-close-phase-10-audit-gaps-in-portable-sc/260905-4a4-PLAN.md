---
phase: quick
plan: "260905-4a4"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/tracer.test.ts
  - scripts/backup/production.test.mjs
  - scripts/local-verify.test.mjs
  - packages/contracts/src/distribution.ts
  - apps/api/src/db/schema.ts
  - apps/api/drizzle/meta/0008_snapshot.json
  - apps/api/drizzle/meta/_journal.json
  - scripts/backup/production/collector.mjs
  - scripts/backup/production/source-authority.mjs
  - scripts/local-verify.mjs
autonomous: true
requirements: [CONT-05, CONT-08]
user_setup: []
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "A non-null portable schedule is accepted only for a retained, non-deleted draft and still requires the timestamp/administrator pair; older version-1 articles with both schedule fields absent remain readable."
    - "The Drizzle schema, committed `0008_scheduled-publishing.sql`, generated snapshot/journal, runtime schema verifier, and schedule audit vocabulary describe the same nine-migration database authority."
    - "Production backup collection and source verification accept exactly migration count 9; generic historical fixtures remain version-agnostic and are not mechanically rewritten."
    - "An isolated restore runs the current API/contracts source and a Web `.next` artifact built for that restore's own loopback origin, never a parent-origin Web artifact."
    - "Restore teardown succeeds only after exact container and volume absence is observed; a failed `down` is acceptable only when absence is independently proven, and cleanup failures cannot suppress the original verification failure."
    - "All work is local on `dev`; no formal delivery, fixed preview refresh, server access, deployment, `main` mutation, dependency install, or destructive command occurs."
  artifacts:
    - path: packages/contracts/src/distribution.ts
      provides: "Portable schedule pair plus draft/non-deleted semantic invariant and legacy-v1 compatibility"
    - path: apps/api/drizzle/meta/0008_snapshot.json
      provides: "Generated Drizzle snapshot for the already committed scheduled-publishing migration"
    - path: apps/api/drizzle/meta/_journal.json
      provides: "Ordered ninth migration entry matching `0008_scheduled-publishing.sql`"
    - path: scripts/backup/production/collector.mjs
      provides: "Current nine-migration production inventory admission"
    - path: scripts/backup/production/source-authority.mjs
      provides: "Current nine-migration protected-source verification"
    - path: scripts/local-verify.mjs
      provides: "Restore-origin runtime authority and absence-confirmed cleanup convergence"
  key_links:
    - from: packages/contracts/src/distribution.ts
      to: apps/api/src/db/schema.ts
      via: "portable non-null schedule validation mirrors the database's paired active-draft constraint"
      pattern: "scheduledAt.*scheduledByAdministratorId|status.*draft|deletedAt"
    - from: apps/api/src/db/schema.ts
      to: apps/api/drizzle/meta/0008_snapshot.json
      via: "Drizzle generation records the schedule columns/checks/index and expanded audit-event check without schema drift"
      pattern: "article\.scheduled|articles_schedule"
    - from: scripts/backup/production/collector.mjs
      to: scripts/backup/production/source-authority.mjs
      via: "both independent production boundaries require the same exact current migration count"
      pattern: "migration\.count.*9|count must be 9"
    - from: scripts/local-verify.mjs
      to: apps/web/.next
      via: "the restore port is allocated before a restore-origin Web build is copied into a restore-owned canonical runtime override"
      pattern: "restoreContext|PUBLIC_ORIGIN|createCanonicalRuntimeAuthority"
    - from: scripts/local-verify.mjs
      to: Docker Compose restore namespace
      via: "bounded teardown retries are followed by exact container and postgres/media volume absence checks"
      pattern: "converge.*Restore.*Cleanup|confirm.*Restore.*Absent"
---

<objective>
Close the remaining Phase 10 audit gaps across portable schedule semantics, generated database authority, production backup migration inventory, and isolated recovery verification.

Purpose: Ensure a scheduled publication cannot be reconstructed into an impossible lifecycle state, database tooling has one coherent migration history, backups recognize the current schema, and canonical restore proof tests the current application at the exact restore origin without silently leaking resources.
Output: Tests-first contract/authority regressions, synchronized Drizzle and production inventory artifacts, restore-owned runtime/cleanup logic, and a passing local canonical integration run; the quick summary is created after execution.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-controlled-scheduled-publishing/10-REVIEW.md
@.planning/phases/10-controlled-scheduled-publishing/10-01-SUMMARY.md
@packages/contracts/src/distribution.ts
@packages/contracts/src/tracer.test.ts
@apps/api/src/db/schema.ts
@apps/api/drizzle/0008_scheduled-publishing.sql
@apps/api/drizzle/meta/_journal.json
@scripts/backup/production/collector.mjs
@scripts/backup/production/source-authority.mjs
@scripts/backup/production.test.mjs
@scripts/backup/restore.mjs
@scripts/backup/restore.test.mjs
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs

<interfaces>
- `portableExportManifestSchema` deliberately keeps both schedule fields optional together so pre-Phase-10 version-1 archives remain readable. New exports emit both fields explicitly. Preserve that compatibility while rejecting a non-null pair unless `status === "draft"` and `deletedAt === null`.
- PostgreSQL already enforces the pair and active-draft rules in `0008_scheduled-publishing.sql`. The SQL also installs all four schedule audit events. Do not rewrite, renumber, split, or duplicate that committed migration; there must remain exactly nine numbered SQL files (`0000` through `0008`).
- `apps/api/src/app.ts` already derives migration application count from the SQL directory and requires count 9 in `schemaVerify()`. Its exact schedule/audit checks are current. The missing database authority is the stale Drizzle audit-event declaration plus absent `meta/0008_snapshot.json` and journal entry.
- Drizzle metadata must be derived from the current schema, not invented from the SQL text. Because the tracked migration already exists, generate in a disposable isolated copy or equivalent safe generation seam, verify the generated SQL is semantically equivalent to the tracked migration, retain the tracked SQL bytes/name, and bring back only the generated snapshot plus its journal entry retagged to `0008_scheduled-publishing`. A subsequent repository `db:generate:check` must report no schema change and must not leave a tenth SQL file.
- Exact current migration-count authorities are `createProductionInventory()` in `scripts/backup/production/collector.mjs`, `verifyProductionBackupSource()` in `scripts/backup/production/source-authority.mjs`, and the generated production inventory fixture inside `scripts/local-verify.mjs`. Update these from 8 to 9 with their direct production tests. Counts 6/7/8 in generic backup/restore or refresh-transition fixtures model historical/relative versions and are not current-schema constants; leave them unchanged unless a failing direct assertion proves otherwise.
- `restoreBackupSet()` already accepts one absolute `composeOverride`, and its tests already prove exact argv propagation. Keep that boundary. The defect is in `runPhase4RestoreChecks()`: reusing the parent's override also reuses a `.next` built with the parent's different `PUBLIC_ORIGIN`.
- Allocate `restoreContext.publicOrigin` first, build the current Web package with that exact `PUBLIC_ORIGIN` and the existing internal API origin, then create a separate canonical runtime authority owned by `restoreContext`. The override must mount current API/contracts sources and copy the restore-built `.next`; it must not alias or overwrite the parent context's runtime root/override.
- Restore cleanup needs a restore-namespace validator, not `validateNamespace()` (which intentionally accepts only `blogxverify_*`). Reuse the exported restore validators from `scripts/backup/restore.mjs` and confirm absence by exact Compose-project label plus exact `${namespace}_postgres-data` and `${namespace}_media-data` volume names.
- In `finally`, attempt Compose convergence, generated restore-root cleanup, generated backup-root cleanup, and restore canonical-runtime-root cleanup even if another cleanup fails. Aggregate cleanup failures with the primary verification failure rather than replacing it or returning success. Keep bounded retries and `allowDuringShutdown`; do not introduce broad Docker pruning, wildcard deletion, or fixed-project mutation.
- `corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check` is a local disposable verifier, not formal `local:deliver`. It is the terminal integration gate for this quick task and must continue to finish in explicit release `BLOCKED` state.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" wave="1" depends_on="[]">
  <name>Task 1: Commit RED coverage for portable state, migration authority, restore origin, and teardown</name>
  <precondition>The branch is `dev`; the untracked Phase 10 review and timezone quick-task files belong to other work and must not be staged, modified, or removed.</precondition>
  <files>packages/contracts/src/tracer.test.ts, scripts/backup/production.test.mjs, scripts/local-verify.test.mjs</files>
  <behavior>
    - "A paired non-null schedule succeeds for a retained draft, but the same pair fails for `published`, `unpublished`, or soft-deleted articles; an absent legacy pair and an explicit null/null pair remain valid where otherwise well formed."
    - "Production collector inventory and protected source authority accept migration count 9 and reject stale count 8 as well as malformed/older counts, without changing generic historical backup fixtures."
    - "The migration authority test requires journal index 8/tag `0008_scheduled-publishing`, `meta/0008_snapshot.json`, exactly nine numbered SQL files, schedule columns/checks/index, and all four schedule audit events in both Drizzle schema and generated snapshot."
    - "The restore runner creates a restore-owned runtime authority only after building Web with the exact restore origin; it does not assign `context.composeOverride` to the restore context or reuse the parent's `.next`."
    - "Restore finalization uses bounded cleanup convergence and exact container/volume absence checks, attempts every generated path/runtime cleanup, and has no single ignored `down(... allowFailure: true)` as its success criterion."
    - "The generated production inventory inside the canonical verifier declares migration count 9."
  </behavior>
  <action>
    Extend the strict contract tracer with table-driven valid/invalid portable schedule lifecycle cases. Update only production-backup test fixtures and expectations that represent the current schema to 9, add an explicit stale-8 rejection, and retain unrelated count-6 failure/historical fixtures. Strengthen `scripts/local-verify.test.mjs` with narrow structural assertions over the migration directory/schema metadata and the `runPhase4RestoreChecks` slice: require a restore-specific Web build environment, a distinct `createCanonicalRuntimeAuthority(restoreContext)`, exact production migration count 9, restore-namespace absence checks, bounded convergence, and all-settled/aggregate finalization; expressly reject `composeOverride: context.composeOverride` and the former one-shot ignored `down`. Make assertions semantic enough to allow helper extraction but exact enough that parent-origin reuse or unchecked cleanup cannot satisfy them. Run the focused contract/production/local-verifier tests and require RED for the four audited defects, not fixture syntax, unrelated timezone work, Docker availability, or server access. Commit only these three test files as `test(quick-260905-4a4): expose phase 10 authority gaps`.
  </action>
  <verify>
    <automated>corepack pnpm --filter @blog-x/contracts test &amp;&amp; node --test scripts/backup/production.test.mjs scripts/local-verify.test.mjs</automated>
  </verify>
  <done>The tests-only commit captures every audit finding, retains legacy portable readability and historical fixture meaning, and fails solely because production implementation/metadata still expose those findings.</done>
</task>

<task type="auto" tdd="true" wave="2" depends_on="[1]">
  <name>Task 2: Synchronize portable, Drizzle, and production backup schema authority</name>
  <precondition>Task 1 is committed RED, and no generated metadata or production count implementation has yet been changed.</precondition>
  <files>packages/contracts/src/distribution.ts, apps/api/src/db/schema.ts, apps/api/drizzle/meta/0008_snapshot.json, apps/api/drizzle/meta/_journal.json, scripts/backup/production/collector.mjs, scripts/backup/production/source-authority.mjs</files>
  <behavior>
    - "Portable parsing preserves legacy omission and explicit null/null pairs but refuses any live schedule on a non-draft or deleted article."
    - "Drizzle declares the four schedule events already enforced by migration 0008, and its generated snapshot/journal are a zero-drift representation of the current schema."
    - "There is exactly one 0008 SQL migration, its bytes/name stay unchanged, the ledger remains exactly nine migrations, and another generation produces no new migration."
    - "Both production collection and protected-source verification require exactly count 9 and reject stale count 8 before a backup is accepted as current authority."
  </behavior>
  <action>
    Add the active-draft semantic check to `portableArticleSchema.superRefine` only when the pair is present and non-null; do not require the optional pair from older version-1 archives and do not expose schedule authority through public distribution contracts. Extend the Drizzle `audit_events_event_check` declaration with the exact four existing schedule event strings in migration order. Generate the next snapshot/journal from the corrected current schema through a disposable isolated generation workspace (or an equally safe non-destructive generation seam), verify its generated SQL is semantically equivalent to the tracked `0008_scheduled-publishing.sql`, then install the generated `0008_snapshot.json` and the ordered journal entry tagged exactly `0008_scheduled-publishing`. Do not alter historical snapshots or the tracked migration, and fail if generation would leave a duplicate/tenth SQL file. Change the two production backup current-schema checks from 8 to 9; leave historical/generic fixture numbers alone. Restore Task 1's contract and production tests to GREEN, run API/contracts typechecks, then run Drizzle generation again and require a clean migration directory. Commit only this task's six owned files as `fix(quick-260905-4a4): align scheduled schema authority`.
  </action>
  <verify>
    <automated>corepack pnpm --filter @blog-x/contracts test &amp;&amp; node --test scripts/backup/production.test.mjs &amp;&amp; node --test --test-name-pattern="Phase 10 migration authority" scripts/local-verify.test.mjs &amp;&amp; corepack pnpm --filter @blog-x/contracts typecheck &amp;&amp; corepack pnpm --filter @blog-x/api typecheck &amp;&amp; corepack pnpm db:generate:check &amp;&amp; git diff --check -- packages/contracts/src/distribution.ts apps/api/src/db/schema.ts apps/api/drizzle/meta/0008_snapshot.json apps/api/drizzle/meta/_journal.json scripts/backup/production/collector.mjs scripts/backup/production/source-authority.mjs</automated>
  </verify>
  <done>Portable lifecycle semantics match PostgreSQL, Drizzle generation reports no drift with one unchanged 0008 migration, and both production backup authorities recognize exactly the current nine-migration schema.</done>
</task>

<task type="auto" tdd="true" wave="2" depends_on="[1]">
  <name>Task 3: Give canonical restore its own origin-bound runtime and absence-confirmed cleanup</name>
  <precondition>Task 1 is committed RED. This task is implementation-file-disjoint from Task 2 and may be prepared in the same wave, but the terminal canonical command runs only after both implementations are present.</precondition>
  <files>scripts/local-verify.mjs</files>
  <behavior>
    - "After the restore port is allocated, Web is built with `PUBLIC_ORIGIN === restoreContext.publicOrigin` and the existing internal API origin before the restore override copies `.next`."
    - "The restore override is a distinct authority owned by `restoreContext`, mounts current API/contracts, and is passed to every restore and follow-up Compose command without mutating the parent authority."
    - "Restore teardown retries a bounded number of times, verifies no exact restore-project containers and neither exact restore volume remains, and fails if absence cannot be proven."
    - "Backup root, restore root, restore runtime root, and Compose cleanup are all attempted; multiple failures are aggregated and a primary test failure is never hidden by cleanup."
    - "The canonical generated production backup inventory records migration count 9, and the complete canonical/interruption/parallel suite ends PASS with release BLOCKED."
  </behavior>
  <action>
    Stop copying `context.composeOverride` into `restoreContext`. Inside the existing restore try/finally boundary, build the Web package for the newly allocated restore loopback origin with the same current internal API origin used by canonical integration, then call `createCanonicalRuntimeAuthority(restoreContext)` so current API/contracts and the restore-built `.next` share one restore-owned override. Keep the parent copied runtime intact for its already running services. Add a restore-specific exact absence helper and bounded convergence helper using the validators exported by `scripts/backup/restore.mjs`; inspect containers by the exact Compose project label and inspect only the exact postgres/media volumes. A nonzero `down` may be recorded, but success comes only from confirmed absence. Refactor restore finalization so Compose convergence, generated restore-root cleanup, generated backup-root cleanup, and restore runtime cleanup all execute even after another rejection; if verification and/or cleanup fails, throw one aggregate retaining all reasons. Update the generated production inventory fixture to count 9. Do not broaden the general `blogxverify_*` validator, reuse a parent `.next`, build an external origin, prune Docker globally, clean fixed `blogxlocal`, swallow cleanup failures, or invoke delivery. Restore Task 1's local-verifier tests to GREEN, run the default suite/typecheck/boundary checks, then—after Task 2 is present—run the full local canonical integration with interruption and parallel cleanup proof. Commit only `scripts/local-verify.mjs` as `fix(quick-260905-4a4): isolate restore runtime authority`.
  </action>
  <verify>
    <automated>node --test scripts/local-verify.test.mjs scripts/backup/restore.test.mjs &amp;&amp; corepack pnpm test &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check &amp;&amp; git diff --check</automated>
  </verify>
  <done>The restored Web is built for its own origin while API/contracts run current source, every generated restore authority is proven absent or the run fails, all cleanup errors remain observable, and the complete local canonical gate passes with release explicitly BLOCKED.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Portable archive to retained database | Version-1 JSON can recreate private schedule authority and therefore must not encode a publicly impossible lifecycle state. |
| Drizzle declaration to applied PostgreSQL | Source schema, generated metadata, ordered migration SQL, ledger count, and audit constraints must remain one authority. |
| Production database to backup evidence | Migration inventory decides whether a collected/source backup represents the current restorable schema. |
| Parent canonical runtime to restore runtime | Current source is shared, but build-time public origin and copied Web artifact must be isolated per loopback authority. |
| Restore verifier to Docker/filesystem cleanup | Generated namespaces, exact volumes, and temporary roots must be absent before success can be claimed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-Q-4A4-01 | Tampering | Portable scheduled article | high | mitigate | Mirror the database active-draft invariant in strict portable parsing while retaining only the deliberate legacy omission path. |
| T-Q-4A4-02 | Tampering | Schema/migration authority | high | mitigate | Generate the missing snapshot/journal, keep one immutable 0008 SQL, align Drizzle audit events, and require zero generation drift. |
| T-Q-4A4-03 | Repudiation | Production backup inventory | high | mitigate | Require exact current count 9 independently at collector creation and protected source verification; test stale 8 rejection. |
| T-Q-4A4-04 | Spoofing | Restore Web origin | high | mitigate | Build/copy `.next` only after allocating the exact restore origin and give restore a distinct override/runtime root. |
| T-Q-4A4-05 | Denial of service | Restore cleanup | high | mitigate | Retry bounded exact teardown, independently prove container/volume absence, attempt all path cleanups, and aggregate failures. |
| T-Q-4A4-06 | Elevation of privilege | Docker target selection | high | mitigate | Validate restore namespace and inspect only exact Compose-label/volume authorities; forbid globs, pruning, fixed projects, and server access. |
| T-Q-4A4-SC | Tampering | Package supply chain | high | mitigate | Add no dependency or lockfile change; use only installed Drizzle, Node, pnpm, and canonical local verification tooling. |
</threat_model>

<verification>
- The tests-only commit is demonstrably RED for the audited defects; Tasks 2 and 3 independently turn their owned subsets GREEN.
- `corepack pnpm db:generate:check` leaves exactly the nine tracked SQL migrations and no new diff, proving the generated `0008` metadata is current.
- Contract tests prove both backward readability and rejection of live schedules on published, unpublished, or deleted articles.
- Production tests prove exact nine-migration acceptance and stale-eight rejection at both collector and source-authority boundaries; generic count-6 and refresh transition fixtures retain their original semantic purpose.
- Local-verifier tests prove distinct restore runtime ownership/origin and absence-confirmed cleanup structurally; the canonical integration proves the real current API/contracts, restore-origin Web, backup/restore equality, browser journey, interruption, and parallel cleanup paths together.
- Run no `local:deliver`, `refresh-local.mjs`, fixed port-3100 mutation, SSH/SCP/rsync, external network check, server operation, deployment, package installation, global Docker prune, destructive command, branch switch, or `main` mutation.
</verification>

<success_criteria>
- All four Phase 10 audit gaps are closed with automated evidence rather than documentation-only claims.
- Portable schedule state, PostgreSQL/Drizzle schema, migration count, audit vocabulary, and production backup inventory agree exactly while legacy version-1 exports remain readable.
- The restore verifier exercises a Web artifact built for the restore origin and current API/contracts source, not stale images or a parent-origin build.
- A restore run cannot report success until its exact containers, volumes, runtime root, backup root, and restore root have converged absent.
- Default tests, contract/production/restore regressions, workspace typecheck, boundary checks, zero-drift Drizzle generation, and the complete local canonical integration all pass on `dev` with release BLOCKED.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID / item | Plan coverage | Status |
|---|---|---|---|
| REVIEW | Restore `.next` was built for the parent origin | Tasks 1 and 3 require a restore-port build and distinct runtime override. | COVERED |
| REVIEW | Restore `down` ignored failure without proving absence | Tasks 1 and 3 add bounded convergence, exact absence inspection, all-cleanup execution, and aggregation. | COVERED |
| REVIEW | Portable schedule pair lacked draft/non-deleted semantics | Tasks 1 and 2 add strict lifecycle cases and mirror the database invariant. | COVERED |
| REVIEW | Drizzle audit event check and migration metadata were stale/missing | Tasks 1 and 2 align the declaration and generated snapshot/journal while preserving one 0008 SQL. | COVERED |
| AUDIT | Ninth migration affects production backup authority | Tasks 1-3 update only collector/source/canonical current-schema constants and direct tests; historical generic fixtures stay unchanged. | COVERED |
| REQ | CONT-05 durable pending schedule state | Portable restoration can retain only a valid pending draft schedule. | COVERED |
| REQ | CONT-08 authenticated/audited fail-closed state | Drizzle/PostgreSQL audit vocabulary aligns, malformed restored state fails, and cleanup/backup authority fail closed. | COVERED |
| PROJECT | Production freeze and local-only verification | Every command is repository-local/disposable; both servers, fixed production entry, and `main` remain untouched. | COVERED |

No scheduling UI/timezone work, publisher algorithm change, public discovery redesign, server/deployment/TLS work, fixed local preview refresh, formal delivery, new dependency, or unrelated documentation change appears in this plan.

<output>
After implementation, create `.planning/quick/260905-4a4-close-phase-10-audit-gaps-in-portable-sc/260905-4a4-SUMMARY.md`. The executor owns the tests-first and implementation commits; the parent owns independent Phase 10 re-review, push of `dev`, and any later one-shot formal local delivery. Preserve the unrelated untracked `10-REVIEW.md` and `260905-45r` task, never access either server, never modify `main`, and never use a destructive command.
</output>
