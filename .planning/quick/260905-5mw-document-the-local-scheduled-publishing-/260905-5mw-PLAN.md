---
phase: quick
plan: "260905-5mw"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/OPERATIONS.md
  - .planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md
  - .planning/quick/260905-45r-fix-scheduled-datetime-timezone-round-tr/260905-45r-SUMMARY.md
autonomous: true
requirements: [CONT-05, CONT-07, CONT-08]
user_setup: []
estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A local operator can invoke one bounded due-publication batch through the existing `blogxlocal` Compose project with exactly one required integer limit from 1 through 100."
    - "The operator documentation states that PostgreSQL transaction time is authoritative, the command mutates eligible local drafts once and exits, success/failure output is redacted, and failures return nonzero."
    - "The documentation cannot be read as production scheduler activation or deployment authorization: production remains disabled and `BLOCKED`."
    - "The x27 and 45r quick-task coverage metadata reflects only the browser evidence later proven by the final Phase 10 verification and immutable delivery receipt."
    - "Only documentation and verification metadata change; runtime source, tests, dependencies, Compose behavior, servers, and `main` remain untouched."
  artifacts:
    - path: README.md
      provides: "Discoverable local due-publication command and link to the detailed operator contract"
    - path: docs/OPERATIONS.md
      provides: "Exact prerequisites, invocation, mutation, clock, output, retry, and production-blocked semantics"
    - path: .planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md
      provides: "Final machine-verifiable no-script lifecycle coverage status"
    - path: .planning/quick/260905-45r-fix-scheduled-datetime-timezone-round-tr/260905-45r-SUMMARY.md
      provides: "Final machine-verifiable timezone round-trip coverage status"
  key_links:
    - from: README.md
      to: docs/OPERATIONS.md
      via: "The quick-start command points operators to the detailed local-only contract"
      pattern: "publish:due|OPERATIONS"
    - from: docs/OPERATIONS.md
      to: apps/api/src/app.ts
      via: "Documented argument, output, failure, and one-shot behavior matches the existing CLI dispatch"
      pattern: "--limit=25|CURRENT_TIMESTAMP|BLOCKED"
    - from: .planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md
      to: .planning/phases/10-controlled-scheduled-publishing/10-VERIFICATION.md
      via: "Final coverage status is grounded in the canonical article lifecycle result"
      pattern: "status: pass"
    - from: .planning/quick/260905-45r-fix-scheduled-datetime-timezone-round-tr/260905-45r-SUMMARY.md
      to: ops/local-deliveries/b0556cb37978ec5668dc51e6ecafd7c955237a8e.json
      via: "Final coverage status is grounded in the reviewed 74/74 local delivery"
      pattern: "status: pass"
---

<objective>
Document the exact local scheduled-publishing operator command and reconcile the two stale quick-task coverage records with final Phase 10 evidence.

Purpose: Make the already-implemented publisher safely operable in the local Compose environment while keeping its database-time, redaction, one-shot, and production-freeze boundaries unambiguous.
Output: Focused README/operations guidance and evidence-grounded x27/45r coverage metadata; no runtime behavior change.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@README.md
@docs/OPERATIONS.md
@apps/api/package.json
@apps/api/src/app.ts
@apps/api/src/content/scheduled-publisher.ts
@apps/api/src/content/admin-repository.ts
@.planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md
@.planning/quick/260905-45r-fix-scheduled-datetime-timezone-round-tr/260905-45r-SUMMARY.md
@.planning/phases/10-controlled-scheduled-publishing/10-VERIFICATION.md
@ops/local-deliveries/b0556cb37978ec5668dc51e6ecafd7c955237a8e.json

<interfaces>
- The exact local invocation is `docker-compose -p blogxlocal -f compose.yaml run --rm api corepack pnpm --filter @blog-x/api publish:due -- --limit=25`; `25` is an example chosen by the operator, not a default.
- `parsePublishDueArguments()` requires exactly one `--limit=N`; only safe integers 1 through `publishDueMaximumLimit` (100) are valid, and malformed arguments fail before a pool is created.
- `publishDue()` uses PostgreSQL transaction time from the retained-row transaction, claims at most N rows in deterministic order, commits a complete batch or rolls it back, and then the CLI closes its pool and exits. It owns no timer, queue, HTTP route, daemon, or process-local scheduler.
- Success JSON contains only format/version/command, UTC `at`, limit, claimed/published counts, and published article IDs. Failure JSON contains only the same envelope, optional limit, and a typed code; content, slug, title, database URL, credentials, and cookies remain absent, and failure sets a nonzero exit code.
- The final Phase 10 verification binds canonical 57/57 plus responsive 17/17 evidence (74/74 total) to reviewed revision `b0556cb37978ec5668dc51e6ecafd7c955237a8e`; production remains `BLOCKED`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Document the exact local one-shot publishing operation</name>
  <files>README.md, docs/OPERATIONS.md</files>
  <action>Add a compact scheduled-publishing subsection to the manual local workflow in `README.md` and a detailed local operator subsection in `docs/OPERATIONS.md`. Show the exact `blogxlocal` Compose command from the interface above and state that PostgreSQL must already be healthy, migrated, and schema-verified. Explain that the operator must supply one integer from 1 through 100, the command immediately publishes up to that many eligible local drafts with no dry-run, database transaction time is the sole due/publication clock, and each invocation processes one batch and exits. Enumerate the safe success fields and typed failure envelope, state that invalid/configuration/candidate/transaction failures are redacted and nonzero, and advise intentional re-invocation rather than implying a resident scheduler. Keep both documents explicit that this is local-only, creates no timer/queue/HTTP endpoint, does not enable production scheduling, and leaves production `BLOCKED`. Do not add server addresses, remote commands, secrets, environment dumps, runtime code, dependencies, or deployment instructions.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const files=['README.md','docs/OPERATIONS.md'].map((p)=&gt;fs.readFileSync(p,'utf8')); const command='docker-compose -p blogxlocal -f compose.yaml run --rm api corepack pnpm --filter @blog-x/api publish:due -- --limit=25'; if(files.some((text)=&gt;!text.includes(command))) throw new Error('exact local command missing'); const detail=files[1]; for(const token of ['1', '100', 'CURRENT_TIMESTAMP', 'one-shot', 'publishedIds', 'nonzero', 'BLOCKED']) if(!detail.includes(token)) throw new Error('operator fact missing: '+token);"</automated>
  </verify>
  <done>Both operator documents expose the same runnable local command and accurately describe its prerequisites, bounded mutation, database clock, redacted output, nonzero failure, one-batch exit, and disabled-production boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile quick-task coverage metadata with final evidence</name>
  <files>.planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md, .planning/quick/260905-45r-fix-scheduled-datetime-timezone-round-tr/260905-45r-SUMMARY.md</files>
  <action>In x27 coverage, mark D1's lifecycle browser verification as passed and mark D1/D2 as machine-verifiable; ground their rationales in the final Phase 10 canonical `article-lifecycle.spec.ts` result and the reviewed delivery receipt. In 45r coverage, mark D2's enhanced DST/native unchanged-resubmission browser verification as passed and add the same exact later evidence reference. Change no other coverage outcome, requirement claim, task commit, duration, completion status, or executor-time verification narrative: the earlier blocked/deferred prose remains a truthful historical record, while frontmatter records the later terminal proof.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); for(const path of process.argv.slice(1)){const front=fs.readFileSync(path,'utf8').split(/^---$/m)[1]??''; if(/status: unknown|human_judgment: true/.test(front)) throw new Error('stale coverage metadata: '+path); if(!front.includes('ops/local-deliveries/b0556cb37978ec5668dc51e6ecafd7c955237a8e.json')) throw new Error('final receipt missing: '+path)}" .planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md .planning/quick/260905-45r-fix-scheduled-datetime-timezone-round-tr/260905-45r-SUMMARY.md</automated>
  </verify>
  <done>Only the previously unproven x27/45r coverage facts now read as passed and machine-verifiable, each bound to the exact final receipt; all historical implementation and executor-time evidence remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator documentation to local Compose CLI | Copying a documented command can mutate eligible local drafts, so scope, prerequisites, limit, and lack of dry-run must be explicit. |
| CLI result to terminal/log consumer | Machine-readable output must remain content- and credential-free on both success and failure. |
| Historical quick summaries to milestone evidence | Later status corrections must be attributable to the immutable reviewed receipt rather than unsupported retrospective claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-5MW-01 | Tampering | Operator command scope | high | mitigate | Require the exact `blogxlocal` Compose project and local file, name the mutating/no-dry-run behavior, and state that production scheduling remains disabled and `BLOCKED`. |
| T-5MW-02 | Information Disclosure | Documented command output | medium | mitigate | Enumerate only the source-defined redacted fields and forbid secrets, content, URLs, and raw environment output from examples. |
| T-5MW-03 | Repudiation | Reconciled quick coverage | medium | mitigate | Bind every later pass correction to Phase 10 verification and receipt for reviewed revision `b0556cb37978ec5668dc51e6ecafd7c955237a8e`; preserve the executor-time narrative. |
</threat_model>

<verification>
Run Task 1 and Task 2 focused consistency gates, then run `corepack pnpm test`, `node scripts/check-boundaries.mjs`, and `git diff --check -- README.md docs/OPERATIONS.md .planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md .planning/quick/260905-45r-fix-scheduled-datetime-timezone-round-tr/260905-45r-SUMMARY.md`. Review the final diff and stage only those four documentation/metadata files plus this quick task's SUMMARY/STATE closeout artifacts; do not execute the publishing command during verification.
</verification>

<success_criteria>
- The exact local Compose invocation is discoverable in both README and the operations guide, with source-consistent prerequisites, input range, DB-time, mutation, output, exit, retry, and production-freeze semantics.
- x27 D1 and 45r D2 no longer carry stale unknown coverage; x27 D1/D2 no longer require human judgment, and both summaries cite the exact final receipt.
- Default zero-infrastructure tests, boundary audit, and diff checks pass.
- The diff contains no runtime, source, test, dependency, Compose, server, deployment, `main`, or destructive-operation change.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID / item | Plan coverage | Status |
|---|---|---|---|
| GOAL | Local operator command plus final metadata reconciliation | Tasks 1–2 document the command and bind stale records to terminal evidence. | COVERED |
| REQ | CONT-05 schedule lifecycle and timezone operability | Task 2 reconciles only the already-proven native/enhanced browser coverage. | COVERED |
| REQ | CONT-07 bounded due publisher | Task 1 documents the exact required limit and one-shot database-authority behavior. | COVERED |
| REQ | CONT-08 fail-closed and auditable operation | Task 1 documents redacted nonzero failures; Task 2 preserves attributable evidence. | COVERED |
| RESEARCH | DB-only one-shot, no daemon/queue/HTTP, PostgreSQL clock | Task 1 carries all established Phase 10 operational constraints into operator docs. | COVERED |
| CONTEXT | Documentation/metadata only; local `dev`; production/server/main/destructive work forbidden | Both tasks and final gates are repository-local and exclude runtime or external-state changes. | COVERED |

<output>
After implementation, create `.planning/quick/260905-5mw-document-the-local-scheduled-publishing-/260905-5mw-SUMMARY.md`, update only the normal quick-task STATE metadata, commit and push the complete quick task on `dev`, and stop. Never connect to either server, deploy, modify `main`, run the documented publisher as part of verification, or execute a destructive command.
</output>
