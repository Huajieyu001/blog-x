---
phase: quick
plan: "260905-3mf"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup/restore.test.mjs
  - scripts/backup/restore.mjs
  - scripts/local-verify.test.mjs
  - scripts/local-verify.mjs
autonomous: true
requirements: [CONT-05, CONT-07, CONT-08]
user_setup: []
estimate:
  tokens: 5200
  raw_tokens: 5200
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Canonical restore startup, migration, schema verification, API comparison, browser comparison, and teardown all resolve one current runtime authority instead of mixing current source with stale cached contracts."
    - "The restore helper appends an explicitly supplied Compose override after the repository compose file for every restore-owned Compose command, while ordinary restore calls without an override retain their existing behavior."
    - "The restored portable manifest containing `scheduledAt` and `scheduledByAdministratorId` validates against the current contracts package."
    - "The full canonical integration, interruption, and parallel-cleanup gate passes locally without a delivery attempt or server access."
  artifacts:
    - path: scripts/backup/restore.mjs
      provides: "Optional dependency-only Compose runtime authority for the complete restore lifecycle"
    - path: scripts/local-verify.mjs
      provides: "Propagation of the canonical API/contracts/Web override into the generated restore namespace"
    - path: scripts/backup/restore.test.mjs
      provides: "Dynamic argv regression coverage for override inheritance and legacy no-override behavior"
    - path: scripts/local-verify.test.mjs
      provides: "Runner wiring regression coverage for one shared canonical restore authority"
  key_links:
    - from: scripts/local-verify.mjs
      to: scripts/backup/restore.mjs
      via: "`runPhase4RestoreChecks` copies `context.composeOverride` to `restoreContext` and supplies that exact path to `restoreBackupSet`"
      pattern: "composeOverride.*context\\.composeOverride"
    - from: scripts/backup/restore.mjs
      to: compose.yaml
      via: "restore inspection and mutation commands use base Compose configuration followed by the optional trusted override"
      pattern: "composeOverride"
---

<objective>
Make canonical backup/restore verification use the current API, contracts, and Web runtime authority in the restored namespace.

Purpose: Remove the stale-image false failure that rejects Phase 10 schedule fields from an otherwise valid portable manifest.
Output: Tests-first restore-argv coverage, narrow override propagation, and one passing full canonical verification run.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/10-controlled-scheduled-publishing/10-01-SUMMARY.md
@scripts/backup/restore.mjs
@scripts/backup/restore.test.mjs
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@packages/contracts/src/distribution.ts
@apps/api/test/backup-restore.test.ts

<interfaces>
- `createCanonicalRuntimeAuthority(context)` owns one generated override containing read-only current `apps/api`, current `packages/contracts`, and the copied current Web `.next`; its outer verification cleanup already removes that authority after all nested checks finish.
- `scripts/local-verify.mjs` already honors `context.composeOverride` in its own `composeArgs`, but the generated `restoreContext` omits it and `restoreBackupSet` currently composes from `compose.yaml` alone.
- `restoreBackupSet(input, dependencies)` is the correct dependency boundary: add no CLI flag, environment fallback, user-controlled path, second source copy, image rebuild, or new runtime root.
- Non-canonical Phase 4/5 restore checks have no override and must keep using the cached verification images exactly as before.
- The schedule fields already exist in the source contract. Do not alter application schema, export data, contract definitions, migrations, or restored fixtures to hide the stale-runtime failure.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Capture restore runtime-authority inheritance as a failing regression</name>
  <files>scripts/backup/restore.test.mjs, scripts/local-verify.test.mjs</files>
  <behavior>
    - "With an explicit dependency override, every restore-owned `docker-compose` invocation uses `-f compose.yaml -f &lt;exact override&gt;` in that order and exactly once."
    - "Without an override, restore command composition remains the existing single-file form."
    - "The canonical restore runner gives the generated restore context the parent canonical override, passes it into `restoreBackupSet`, and retains it for later comparison and teardown commands."
  </behavior>
  <action>Add an infrastructure-free `restoreBackupSet` test using the existing complete backup fixture, an empty-target inspector, and a recording fake command runner. Drive the production mutation path through PostgreSQL start, dump restore, migration, API start, schema verification, and Web start; return a valid generated API container ID where required. Assert the exact Compose prefix for supplied and omitted overrides, without accepting reordered, duplicated, environment-derived, or shell-expanded values. Add a focused source-wiring assertion around `runPhase4RestoreChecks` proving that the same parent override reaches both `restoreContext` and the restore-helper dependency. Run the focused tests and require RED because current restore composition ignores the supplied override and the runner does not propagate it. Commit only the two test files as `test(quick-260905-3mf): expose stale restore runtime authority`.</action>
  <verify>
    <automated>node --test --test-name-pattern="restore.*runtime override|Phase 4 restore runner" scripts/backup/restore.test.mjs scripts/local-verify.test.mjs</automated>
  </verify>
  <done>The tests-only commit fails for the missing override inheritance, while exercising no Docker, network, server, or production authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate one canonical runtime authority through restore</name>
  <files>scripts/backup/restore.mjs, scripts/local-verify.mjs</files>
  <behavior>
    - "Restore preflight inspection and every default mutation Compose command append the optional dependency override after `compose.yaml`."
    - "Canonical `runPhase4RestoreChecks` reuses the already-generated parent override for restore startup, comparison, browser checks, and `down`."
    - "Undefined override behavior remains unchanged for CLI restore and non-canonical restore verification."
  </behavior>
  <action>Extend the internal restore Compose-argument boundary to accept an optional dependency-only override and use it consistently from `defaultInspectTarget` and `defaultMutate`; preserve argv spawning and the existing namespace/database/root validation. In `runPhase4RestoreChecks`, assign the parent `context.composeOverride` to the generated `restoreContext` and pass that exact value in the `restoreBackupSet` dependency object. Reuse the same context for authority comparison and `finally` teardown; do not generate or clean a second runtime root. Make the focused tests GREEN, then run default tests, workspace typecheck/build, boundary checks, and the full canonical integration command. Commit the two implementation files as `fix(quick-260905-3mf): inherit canonical restore runtime authority`.</action>
  <verify>
    <automated>node --test scripts/backup/restore.test.mjs scripts/local-verify.test.mjs &amp;&amp; corepack pnpm test &amp;&amp; corepack pnpm -r typecheck &amp;&amp; corepack pnpm -r build &amp;&amp; corepack pnpm check:boundaries &amp;&amp; node scripts/local-verify.mjs --canonical-integration --interruption-check --parallel-check</automated>
  </verify>
  <done>All focused/default/static gates pass, the canonical restored API accepts the Phase 10 portable schedule fields through current contracts, restored browser verification passes, and generated interruption/parallel authorities cleanly converge.</done>
</task>

</tasks>

<verification>
- Inspect recorded restore argv to prove exact base-then-override ordering and no override leakage into ordinary restore calls.
- Run `node --test scripts/backup/restore.test.mjs scripts/local-verify.test.mjs`, `corepack pnpm test`, workspace typecheck/build, and boundary checks.
- Run `node scripts/local-verify.mjs --canonical-integration --interruption-check --parallel-check` once from the clean implementation revision and require semantic TAP/Playwright pass-only results plus generated cleanup acknowledgement.
- Do not run `local:deliver`, refresh a fixed runtime, access either server, deploy, switch to or modify `main`, or run destructive commands.
</verification>

<success_criteria>
- Restore-owned Compose commands can inherit the exact current runtime override without changing the standalone restore interface.
- The canonical restore namespace uses current `packages/contracts` and validates both Phase 10 schedule manifest fields.
- Full canonical integration, restore browser, interruption, parallel, and cleanup checks pass.
- No application contract/data workaround, package change, server operation, formal delivery attempt, or `main` mutation occurs.
</success_criteria>

<output>
After execution, create `.planning/quick/260905-3mf-make-canonical-restore-verification-inhe/260905-3mf-SUMMARY.md`. The executor owns task commits; the parent workflow owns metadata commit, review, push of `dev`, and any later formal local delivery. Preserve the production freeze and never access a server or modify `main`.
</output>
