---
phase: quick
plan: "260904-x27"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/article-lifecycle.spec.ts
  - apps/web/app/admin/_components/ArticleEditor.tsx
autonomous: true
requirements: [CONT-05, CONT-08]
user_setup: []
estimate:
  tokens: 8500
  raw_tokens: 8500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The lifecycle journey reads and preserves actual first-publication history through the exact `首次发布时间更正` control, never the pending-schedule control."
    - "A saved draft exposes native same-origin schedule and cancel forms when JavaScript is disabled, and each submit returns to a page showing the new durable schedule state."
    - "The JavaScript-enabled recovery gate still prevents lifecycle mutation until local recovery state has been evaluated."
    - "Focused browser coverage remains strict: exact labels, UTC value, same-origin action, keyboard submission, durable cancellation, and publication-time preservation are asserted."
  artifacts:
    - path: apps/web/e2e/article-lifecycle.spec.ts
      provides: "Regression proof for distinct publication/schedule semantics and the complete no-script schedule/cancel journey"
    - path: apps/web/app/admin/_components/ArticleEditor.tsx
      provides: "Server-rendered no-script lifecycle fallback without bypassing the enhanced editor recovery gate"
  key_links:
    - from: apps/web/app/admin/_components/ArticleEditor.tsx
      to: apps/web/app/admin/_components/ArticleActions.tsx
      via: "the pre-hydration branch exposes the existing lifecycle forms only to no-script browsers for an already-saved post"
      pattern: "noscript.*ArticleActions"
    - from: apps/web/e2e/article-lifecycle.spec.ts
      to: apps/web/app/admin/_components/ArticleEditor.tsx
      via: "a JavaScript-disabled context follows the native POST redirect and observes schedule then cancellation state"
      pattern: "javaScriptEnabled: false"
---

<objective>
Close the two Phase 10 browser-fixture regressions without reverting the new distinction between pending schedule metadata and actual first-publication history.

Purpose: Restore trustworthy lifecycle browser verification and make the promised native schedule/cancel path operable with JavaScript disabled.
Output: One strict tests-first browser correction and one narrow no-script rendering fix; no API, contract, server, or delivery change.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-controlled-scheduled-publishing/10-02-SUMMARY.md
@.planning/phases/10-controlled-scheduled-publishing/10-03-SUMMARY.md
@apps/web/e2e/article-lifecycle.spec.ts
@apps/web/app/admin/_components/ArticleEditor.tsx
@apps/web/app/admin/_components/ArticleActions.tsx

<interfaces>
- `ArticleEditor` deliberately renders only `编辑器加载中…` until `editorReady` is set by `useEffect`; with JavaScript disabled that transition never occurs, so the existing detail-page `ArticleActions` and its native forms are unreachable.
- `ArticleActions` already owns correct relative POST actions, server-safe initial form values, redirect-compatible schedule/cancel forms, and distinct `scheduledAt` display. Reuse it; do not create a second schedule form or another mutation path.
- After manual publication, `ArticleEditor` now exposes the actual retained timestamp under the exact label `首次发布时间更正`. The old exact `发布时间` locator is obsolete and must not be broadened to match both concepts.
- The generated canonical main-browser fixture supplies `E2E_WEB_ORIGIN`, administrator credentials, run ID, API proxy, and isolated data. Do not point the test at a server or the fixed production-like runtime.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Define the corrected publication and native no-script lifecycle journey</name>
  <files>apps/web/e2e/article-lifecycle.spec.ts</files>
  <behavior>
    - "Manual publish produces a non-empty actual first-publication value under `首次发布时间更正`; unconfirmed edits, unpublish, and republish preserve that exact value."
    - "Pending scheduling continues to use `预约发布时间` and disappears after manual publication, proving the two meanings cannot be selected interchangeably."
    - "With JavaScript disabled, a saved draft's same-origin native form is keyboard-submitted, the redirected detail page shows the normalized UTC schedule, and native cancellation returns with that schedule absent."
  </behavior>
  <action>Replace every obsolete exact `发布时间` locator with the exact `首次发布时间更正` label and retain the original non-empty/preservation assertions. Add an explicit post-publication assertion that the draft-only pending-schedule control is absent. Extend the JavaScript-disabled test through both native scheduling and native cancellation: assert each form action resolves under `webOrigin`, submit from a keyboard-operable button, require the existing exact detail URL after each redirect, verify the normalized `2032-02-01T01:30:00.000Z` value after scheduling, then verify the current-schedule text is absent and `设定预约` returns after cancellation. Close the context in `finally` so a RED failure does not leak it. Run the named Playwright file in the generated local main-browser fixture; the corrected publication path must advance, while the no-script case remains RED specifically because the editor renders only its loading branch. Do not relax locator exactness, skip either test, add arbitrary waits, use `force`, or replace visible UI proof with direct mutation requests. Commit only the test file as `test(quick-260904-x27): expose no-script schedule lifecycle`.</action>
  <verify>
    <automated>corepack pnpm exec playwright test apps/web/e2e/article-lifecycle.spec.ts --workers=1</automated>
  </verify>
  <done>The tests-only commit captures the new label contract and complete native schedule/cancel behavior; its only remaining focused failure is the absent no-script lifecycle form on the detail page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the existing lifecycle controls as a no-script fallback</name>
  <files>apps/web/app/admin/_components/ArticleEditor.tsx</files>
  <behavior>
    - "A server-rendered saved-post page exposes the existing `ArticleActions` lifecycle forms to no-script browsers while a new unsaved editor exposes none."
    - "JavaScript-enabled browsers retain the current recovery-first loading gate and do not gain a clickable pre-recovery mutation window."
    - "Native schedule and cancel submissions redirect back to the detail page, whose fresh server post renders the updated state without hydration."
  </behavior>
  <action>In the existing `!editorReady` branch, retain the loading/status markup and add a semantic `noscript` fallback containing the existing `ArticleActions` only when the server supplied a saved `post`. Do not duplicate forms, add a client-ready shortcut, remove the recovery gate, make pre-recovery controls interactive for scripting-enabled browsers, change `ArticleActions`, or alter API routes. Restore the focused browser file to GREEN in the generated canonical fixture, then run the default tests, workspace typecheck, Web build, and formatting check. Commit only `ArticleEditor.tsx` as `fix(quick-260904-x27): expose native no-script lifecycle`.</action>
  <verify>
    <automated>corepack pnpm exec playwright test apps/web/e2e/article-lifecycle.spec.ts --workers=1 &amp;&amp; corepack pnpm test &amp;&amp; corepack pnpm -r typecheck &amp;&amp; corepack pnpm --filter @blog-x/web build &amp;&amp; git diff --check -- apps/web/e2e/article-lifecycle.spec.ts apps/web/app/admin/_components/ArticleEditor.tsx</automated>
  </verify>
  <done>Both lifecycle browser cases pass against the generated local fixture, default tests and typecheck/build pass, no-script users can schedule and cancel through native forms, and recovery safety remains unchanged for the enhanced editor.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server-rendered admin state to native form | Authenticated retained draft state becomes a same-origin mutation control without client execution. |
| Browser form to admin API proxy | Untrusted local datetime/offset and session cookies cross the existing mutation guard. |
| Recovery storage to lifecycle controls | Client recovery status must be known before enhanced-browser lifecycle mutations become available. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-Q-X27-01 | Spoofing | Native form action | high | mitigate | Reuse relative same-origin `ArticleActions` forms and assert actions resolve under the generated origin. |
| T-Q-X27-02 | Tampering | Pre-recovery lifecycle controls | high | mitigate | Put the saved-post fallback inside `noscript`; retain the current `editorReady` recovery gate for scripting-enabled browsers. |
| T-Q-X27-03 | Repudiation | Native schedule/cancel result | medium | mitigate | Require both redirects and fresh server-rendered state after schedule and cancellation in Playwright. |
| T-Q-X27-04 | Information disclosure | Publication/schedule labels | low | mitigate | Assert distinct exact labels and keep schedule controls absent outside draft state. |
| T-Q-X27-SC | Tampering | Package supply chain | high | mitigate | Add no package and change no dependency or lockfile; use only pinned repository tooling. |
</threat_model>

<verification>
- Use the generated local canonical main-browser fixture for the focused Playwright file; if standalone fixture facts are not available to the executor, run `corepack pnpm local:verify --canonical-integration --interruption-check --parallel-check` once and require both `article-lifecycle.spec.ts` cases to pass.
- Run `corepack pnpm test`, `corepack pnpm -r typecheck`, `corepack pnpm --filter @blog-x/web build`, and `git diff --check`.
- Inspect the final diff for exact publication/schedule labels, both no-script mutations, context cleanup, and a no-script-only reuse of `ArticleActions`.
- Do not run `local:deliver`, refresh the fixed runtime, use Docker outside the canonical fixture, access either server, deploy, switch to or modify `main`, or run destructive commands.
</verification>

<success_criteria>
- The obsolete `发布时间` locator is gone; strict first-publication preservation remains covered through `首次发布时间更正`.
- No-script schedule and cancellation both succeed via visible, keyboard-operable, same-origin native controls and fresh redirected state.
- The enhanced editor's recovery-first mutation safety is preserved.
- Focused browser, default, typecheck, and Web build verification pass locally without a formal delivery attempt.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID / item | Plan coverage | Status |
|---|---|---|---|
| GOAL | Repair the Phase 10 lifecycle browser gate without reverting schedule/publication semantics | Tasks 1-2 correct the fixture and expose the existing forms only to no-script browsers. | COVERED |
| REQ | CONT-05 schedule, inspect, reschedule, and cancel | Task 1 proves native schedule/cancel and Task 2 makes the forms reachable. | COVERED |
| REQ | CONT-08 fail-closed invalid/recovery state | Task 2 retains the recovery-first enhanced-browser gate and reuses existing guarded routes. | COVERED |
| RESEARCH | Separate `scheduledAt` from first-publication history; use same-origin native forms | Task 1 uses exact distinct labels; Task 2 reuses `ArticleActions` rather than adding a data plane. | COVERED |
| CONTEXT | Local-only work, production freeze, no server/main/destructive operations | Verification is local and explicitly excludes delivery, server, deployment, main, and destructive work. | COVERED |

No deferred scheduler activation, server deployment, TLS, API redesign, editor no-script authoring, package installation, or unrelated UI work appears in this plan.

<output>
After execution, create `.planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md`. The executor owns the two task commits; the parent owns review, push of `dev`, and any later formal local delivery. Never access a server, modify `main`, or run a destructive command.
</output>
