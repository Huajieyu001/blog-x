---
phase: quick
plan: "260904-x27"
subsystem: testing
tags: [playwright, nextjs, noscript, scheduled-publishing]
requires:
  - phase: 10-controlled-scheduled-publishing
    provides: "Existing ArticleActions native lifecycle forms and guarded editor recovery state"
provides:
  - "Strict lifecycle assertions for first-publication history and native no-script scheduling"
  - "A saved-post-only noscript fallback that reuses ArticleActions"
affects: [phase-10-browser-verification, local-integration]
actuals:
  tokens: 2500
  tasks: 2
  commits: 7
tech-stack:
  added: []
  patterns:
    - "Keep JavaScript-disabled lifecycle access inside a noscript branch while enhanced controls remain behind editorReady."
key-files:
  created: []
  modified:
    - apps/web/e2e/article-lifecycle.spec.ts
    - apps/web/app/admin/_components/ArticleEditor.tsx
key-decisions:
  - "Use the existing ArticleActions forms for the no-script path instead of creating a second mutation surface."
  - "Keep first-publication and pending-schedule assertions under separate exact labels."
  - "Render the no-script ArticleActions boundary from the server detail page, outside the client editor recovery shell."
  - "Use a server-rendered fallback hidden by `@media (scripting: enabled)`, rather than relying on noscript accessibility-tree parsing."
requirements-completed: [CONT-05, CONT-08]
coverage:
  - id: D1
    description: "Lifecycle browser coverage distinguishes retained first-publication history from pending scheduling and proves native schedule/cancel navigation."
    requirement: CONT-05
    verification:
      - kind: e2e
        ref: "apps/web/e2e/article-lifecycle.spec.ts#schedule form remains a no-script, keyboard-operable same-origin control"
        status: unknown
    human_judgment: true
    rationale: "The generated local browser fixture was unavailable for a captured final run because Docker exited with status 1 before browser startup."
  - id: D2
    description: "Saved editor pages expose lifecycle forms to no-script users without changing the enhanced recovery gate."
    requirement: CONT-08
    verification:
      - kind: other
        ref: "corepack pnpm -r typecheck; PUBLIC_ORIGIN=http://127.0.0.1:3100 corepack pnpm --filter @blog-x/web build"
        status: pass
    human_judgment: true
    rationale: "The no-script navigation needs the generated local browser fixture for final behavioral proof."
duration: 60min
completed: 2026-09-05
status: complete
---

# Quick 260904-x27: No-script Schedule Lifecycle Summary

**Strict first-publication regression coverage and a saved-post-only no-script reuse of the existing schedule/cancel controls.**

## Accomplishments

- Replaced obsolete exact `发布时间` locators with `首次发布时间更正`, while preserving the first-publication timestamp across edits, unpublish, and republish.
- Extended the JavaScript-disabled journey to assert same-origin schedule and cancel actions, keyboard submission, redirects, normalized UTC scheduling, and durable cancellation state.
- Rendered `ArticleActions` only inside the pre-hydration saved-post `<noscript>` fallback; JavaScript-enabled users still remain on the recovery-first loading branch.

## Task Commits

1. **Task 1: Define the corrected publication and native no-script lifecycle journey** - `4934220` (`test`)
2. **Task 2: Render the existing lifecycle controls as a no-script fallback** - `89e8f13` (`fix`)
3. **Rule 1 follow-up: Force fresh protected admin lifecycle reads** - `371d223` (`fix`)
4. **Rule 1 follow-up: Await real native redirect completion** - `4cc5b4d` (`fix`)
5. **Rule 1 follow-up: Render native controls from the server page** - `59d1241` (`fix`)
6. **Rule 1 follow-up: Make the server fallback scripting-aware** - `3a63e31` (`fix`)
7. **Compatibility follow-up: Align existing publication-time browser coverage** - `f86c7e2` (`test`)

## Verification

- PASS — `corepack pnpm test` (46/46)
- PASS — `corepack pnpm -r typecheck`
- PASS — `PUBLIC_ORIGIN=http://127.0.0.1:3100 corepack pnpm --filter @blog-x/web build`
- PASS — `git diff --check -- apps/web/e2e/article-lifecycle.spec.ts apps/web/app/admin/_components/ArticleEditor.tsx`
- BLOCKED — captured `corepack pnpm local:verify --canonical-integration --interruption-check --parallel-check` stopped before browser startup with `docker exited with 1`; output is preserved in `/private/tmp/blog-x-x27-IO9CCw/canonical.log`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Same-URL native redirect had a false browser synchronization point**
- **Found during:** Parent canonical-root browser run after Task 2.
- **Issue:** `toHaveURL(detailUrl)` immediately passed because the form redirects to the same URL, allowing the assertion to inspect the old document before its native POST/navigation completed.
- **Fix:** The existing keyboard-driven browser journey now waits concurrently for the exact POST response and load-complete navigation, asserts its 302 status and `Location`, and confirms the authenticated API read has the expected persisted schedule/cancellation before checking fresh rendered state. The provisional `force-dynamic` exports were removed because existing protected reads already use `cache: "no-store"` and the added route setting was not independently justified.
- **Files modified:** `apps/web/e2e/article-lifecycle.spec.ts`, `apps/web/app/admin/page.tsx`, `apps/web/app/admin/posts/[id]/page.tsx`
- **Verification:** Default tests 46/46, workspace typecheck, and production Web build with a local `PUBLIC_ORIGIN` all pass. The parent will re-run the canonical browser fixture.
- **Committed in:** `4cc5b4d`

**2. [Rule 1 - Bug] Client editor shell did not retain the no-script fallback after native redirect**
- **Found during:** Parent canonical-root browser run after Task 4.
- **Issue:** The exact POST, 302 redirect, navigation, and authenticated read all passed, but the redirected DOM contained only the client editor's loading shell. The fallback nested inside `ArticleEditor` was therefore not a reliable server-rendered no-script boundary.
- **Fix:** Moved the saved-post `ArticleActions` fallback to the server `admin/posts/[id]` page, adjacent to `ArticleEditor`, and removed the inner copy. JavaScript-enabled users still get only the editor recovery-first path, while no-script users receive the existing native forms from the server response.
- **Files modified:** `apps/web/app/admin/posts/[id]/page.tsx`, `apps/web/app/admin/_components/ArticleEditor.tsx`
- **Verification:** Default tests 46/46, workspace typecheck, and production Web build with a local `PUBLIC_ORIGIN` all pass. The parent will re-run the canonical browser fixture.
- **Committed in:** `59d1241`

**3. [Rule 1 - Bug] Chromium omitted the redirected noscript subtree from its accessibility DOM**
- **Found during:** Parent trace inspection after Task 5.
- **Issue:** The final 200 HTML contained the correct scheduled state and cancellation form, yet Chromium's no-script accessibility tree omitted the nested `noscript` subtree, making the native controls unreachable to the browser test.
- **Fix:** Replaced `noscript` with a normal server-rendered fallback container. It is visible by default (fail-safe for no-script browsers) and hidden with `display: none` only under `@media (scripting: enabled)`. The enhanced test asserts that container is hidden, scopes its lifecycle input locators to the visible form, and the no-script test asserts the fallback is visible.
- **Files modified:** `apps/web/app/admin/posts/[id]/page.tsx`, `apps/web/app/admin/admin.module.css`, `apps/web/e2e/article-lifecycle.spec.ts`
- **Verification:** Default tests 46/46, workspace typecheck, and production Web build with a local `PUBLIC_ORIGIN` all pass. The parent will re-run the canonical browser fixture.
- **Committed in:** `3a63e31`

**4. [Rule 1 - Compatibility] Existing Phase 1 browser suites assumed a draft-owned publication timestamp**
- **Found during:** Parent canonical run after `article-lifecycle.spec.ts` passed.
- **Issue:** Draft preview and Phase 1 publishing coverage still filled or asserted the retired exact `发布时间` draft field, contradicting the current separation between pending scheduling and durable first-publication history.
- **Fix:** Draft coverage now asserts the first-publication notice and absence of `首次发布时间更正`; publication, unpublish, and republish assertions use the strict current correction label and retain the original non-empty/preservation checks.
- **Files modified:** `apps/web/e2e/draft-preview.spec.ts`, `apps/web/e2e/phase1-publishing.spec.ts`
- **Verification:** Default tests 46/46, workspace typecheck, and production Web build with a local `PUBLIC_ORIGIN` all pass.
- **Committed in:** `f86c7e2`

No Docker, server, or production state was changed to work around fixture availability.

## Next Phase Readiness

The final code history is ready for review and push. Re-run the canonical main-browser fixture to confirm the synchronization-aware native journey and convert both browser coverage items from `unknown` to `pass`.

## Self-Check: PASSED

- Both assigned source files exist and match their task commits.
- No generated `next-env.d.ts` change remains in the worktree.
