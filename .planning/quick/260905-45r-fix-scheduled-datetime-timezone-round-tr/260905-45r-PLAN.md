---
phase: quick
plan: "260905-45r"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/admin/_components/article-actions-schedule.ts
  - apps/web/app/admin/_components/article-actions-schedule.test.ts
  - apps/web/app/admin/_components/ArticleActions.tsx
  - apps/web/e2e/article-lifecycle.spec.ts
  - scripts/test-inventory.mjs
  - scripts/test-inventory.test.mjs
  - scripts/default-test.mjs
  - scripts/default-test.test.mjs
autonomous: true
requirements: [CONT-05, CONT-08]
user_setup: []
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "An existing scheduled instant is always rendered and submitted as one coherent `datetime-local` plus UTC-offset pair."
    - "SSR and JavaScript-disabled forms use the explicit China fallback `+08:00`, including the matching China-local wall time."
    - "After hydration, the wall time and offset move together to the browser timezone at the scheduled or newly selected target date, including DST changes."
    - "A changed `post.scheduledAt` prop or successful schedule response resets the controlled pair from the new durable instant without retaining stale form state."
    - "Reloading and resubmitting an unchanged schedule through either enhanced JavaScript or a native no-script form preserves the exact UTC instant."
  artifacts:
    - path: apps/web/app/admin/_components/article-actions-schedule.ts
      provides: "Pure offset formatting and instant-to-local schedule form-state conversion"
    - path: apps/web/app/admin/_components/article-actions-schedule.test.ts
      provides: "Deterministic unit proof for positive, negative, date-boundary, null, and offset-bound cases"
    - path: apps/web/app/admin/_components/ArticleActions.tsx
      provides: "Hydration-safe controlled schedule fields synchronized as one state"
    - path: apps/web/e2e/article-lifecycle.spec.ts
      provides: "JavaScript timezone/DST and no-script unchanged-resubmission round-trip proof"
  key_links:
    - from: apps/web/app/admin/_components/ArticleActions.tsx
      to: apps/web/app/admin/_components/article-actions-schedule.ts
      via: "SSR initialization, browser hydration, selected-date offset updates, and post-prop synchronization all use the same pure conversion"
      pattern: "schedule.*(Form|Fields|Offset)"
    - from: apps/web/e2e/article-lifecycle.spec.ts
      to: apps/web/app/admin/_components/ArticleActions.tsx
      via: "visible form values and exact schedule POST responses prove the submitted pair retains the durable instant"
      pattern: "timezoneId|scheduledAt"
    - from: scripts/default-test.mjs
      to: apps/web/app/admin/_components/article-actions-schedule.test.ts
      via: "the new pure helper suite is an exact owned member of the default Web test child"
      pattern: "article-actions-schedule\\.test\\.ts"
---

<objective>
Close Phase 10 review finding CR-01 by making the displayed schedule wall time and its UTC offset a single coherent, round-trippable value across server rendering, hydration, browser timezone/DST changes, durable post updates, and native form submission.

Purpose: Prevent an unchanged retained schedule from silently moving by the server/browser timezone difference or by a daylight-saving transition.
Output: Pure tested schedule conversion helpers, synchronized controlled form state, and strict JavaScript plus no-script browser round-trip regressions; no API, database, contract, delivery, or server change.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-controlled-scheduled-publishing/10-REVIEW.md
@.planning/phases/10-controlled-scheduled-publishing/10-02-SUMMARY.md
@.planning/quick/260904-x27-fix-phase-10-schedule-lifecycle-browser-/260904-x27-SUMMARY.md
@apps/web/app/admin/_components/ArticleActions.tsx
@apps/web/app/admin/_components/ArticleEditor.tsx
@apps/web/app/admin/posts/[id]/page.tsx
@apps/web/e2e/article-lifecycle.spec.ts
@scripts/test-inventory.mjs
@scripts/default-test.mjs

<interfaces>
- The API already accepts a strict local `scheduledAt` plus numeric `timezoneOffset` form pair and normalizes it to an offset-bearing instant. Do not change that route or its validation.
- `ArticleActions` currently computes the `datetime-local` value from the executing process timezone, initializes the separate offset to `+08:00`, and leaves the datetime input uncontrolled. That is the CR-01 defect.
- Server/no-script authority is intentionally China local: the first render must pair `+08:00` with the stored instant shifted by exactly +480 minutes.
- Enhanced browsers must derive the offset at the scheduled/selected date. In a DST timezone, `new Date()` today is not authority for a winter or summer appointment.
- `ArticleEditor.lifecycleChanged` returns a changed `AdminPost` to `ArticleActions`; the visible form must respond to `post.scheduledAt` changes as well as initial hydration.
- Default Web unit tests use `node:test` through the exact package-test inventory. Any new `.test.ts` must be registered in the inventory, Web child argv, and their exact coordinator tests.
- The canonical main-browser fixture supplies origin and credentials. Explicit Playwright browser contexts may select an IANA timezone but must still use only that generated local origin.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Specify coherent schedule conversion and unchanged round trips</name>
  <files>apps/web/app/admin/_components/article-actions-schedule.test.ts, apps/web/e2e/article-lifecycle.spec.ts, scripts/test-inventory.mjs, scripts/test-inventory.test.mjs, scripts/default-test.mjs, scripts/default-test.test.mjs</files>
  <behavior>
    - "A stored `2026-12-01T02:15:00.000Z` maps to `2026-12-01T10:15` with `+08:00`, and to `2026-11-30T21:15` with `-05:00`; null maps to an empty local value while retaining the selected offset."
    - "Offset formatting preserves the JavaScript/browser sign inversion, minute precision, and the accepted -14:00 through +14:00 range without consulting the machine timezone."
    - "In an `America/New_York` browser, selecting a winter local datetime pairs it with `-05:00`, while a summer target pairs with `-04:00`; the target date, not today's offset, is authoritative."
    - "After a JavaScript schedule succeeds, its controlled values survive the returned post update and reload; an immediate unchanged resubmit returns the exact same `scheduledAt` instant."
    - "After the no-script +08:00 schedule redirect, the server-rendered input shows the matching China-local time; an unchanged native resubmit returns a redirect and preserves the exact UTC instant before cancellation."
  </behavior>
  <action>Create a focused `node:test` suite for a pure schedule-form conversion module before that module exists. Assert exact positive/negative offsets, previous-day rollover, null state, minute precision, sign conversion, and strict supported bounds. Register the suite as a default `web-unit` in the exact test inventory and default Web child; update the inventory/coordinator tests and counts rather than bypassing their closed ownership model. Extend `article-lifecycle.spec.ts` with a JavaScript-enabled context using `timezoneId: "America/New_York"`: create a retained draft through the generated local API proxy, select a winter target and require `-05:00`, submit and assert the normalized UTC response, reload, assert the same local/offset pair, resubmit without edits, and prove the response plus authenticated GET retain the exact instant. Then select a summer target and require `-04:00`, proving DST follows the selected date and the changed post remains coherent after the mutation. Extend the existing no-script journey after its first schedule redirect to assert `2032-02-01T09:30` paired with `+08:00`, submit `改期预约` without changing either field, require the exact 302 Location, and prove both the authenticated GET and refreshed page still represent `2032-02-01T01:30:00.000Z` before continuing to cancellation. Close every explicit browser context in `finally`; use exact response URLs/statuses and no arbitrary waits, `force`, skipped assertions, direct production address, or relaxed instant comparison. Run the pure helper/default runner tests and focused Playwright file to record RED specifically at the absent helper and current incoherent rendering/resubmission behavior. Commit the tests and exact inventory wiring as `test(quick-260905-45r): expose schedule timezone drift`.</action>
  <verify>
    <automated>node --import tsx --test apps/web/app/admin/_components/article-actions-schedule.test.ts &amp;&amp; node --test scripts/test-inventory.test.mjs scripts/default-test.test.mjs &amp;&amp; corepack pnpm exec playwright test apps/web/e2e/article-lifecycle.spec.ts --workers=1</automated>
  </verify>
  <done>The committed regression suite deterministically specifies pure conversion, target-date DST behavior, post/reload coherence, and exact unchanged resubmission for both scripting modes; failures point only to CR-01 implementation gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize schedule wall time and target-date offset</name>
  <files>apps/web/app/admin/_components/article-actions-schedule.ts, apps/web/app/admin/_components/ArticleActions.tsx</files>
  <behavior>
    - "The SSR initializer atomically creates both fields from the durable instant and +480 minutes, so server markup and no-script submission agree."
    - "Hydration atomically replaces both fields from the durable instant and the browser offset at that instant; it never mixes browser wall time with the SSR offset."
    - "Changing the local target date in an enhanced browser updates the paired browser offset for that selected date, including DST, while an explicit offset edit remains the exact value submitted."
    - "A new `initialPost` or successful schedule/reschedule response re-derives the controlled pair from the new `scheduledAt`; cancellation and lifecycle transitions do not leave stale schedule controls."
    - "The form remains progressive: controlled React inputs and the emitted server HTML submit the same field names and values through the existing same-origin native action."
  </behavior>
  <action>Extract a dependency-free pure helper module that validates/formats numeric offsets and converts an absolute instant to the exact `datetime-local` wall time at a supplied offset without reading the host timezone. Keep +480 minutes as the explicit server fallback. In `ArticleActions`, replace the independent offset state and uncontrolled `defaultValue` with one controlled schedule-form state containing the local datetime and offset text. Initialize that state from `initialPost.scheduledAt` at +08:00 so SSR/hydration markup is stable. In an effect, derive the browser offset at the stored scheduled instant (or the relevant local target for an empty form) and replace both values together; on `initialPost` changes, update `post` and re-derive the pair from the new durable schedule rather than retaining stale input. When an enhanced user changes the local datetime, derive its browser offset from that selected local date so winter/summer transitions are correct; preserve deliberate valid manual offset edits as the paired submission value. When schedule JSON succeeds, apply the parsed post and synchronize through the same helper/onChanged path. Do not use `new Date()` today as the offset authority for a retained/selected future date, derive server values from `Date#getTimezoneOffset`, introduce an IANA timezone picker, change API parsing, duplicate forms, suppress hydration warnings, or make no-script behavior depend on effects. Restore the pure, default, and focused browser suites to GREEN, then run workspace typecheck, Web build, boundary checks, and diff hygiene. Commit only the helper and component implementation as `fix(quick-260905-45r): preserve schedule timezone round trips`.</action>
  <verify>
    <automated>corepack pnpm test &amp;&amp; node --test scripts/test-inventory.test.mjs scripts/default-test.test.mjs &amp;&amp; corepack pnpm exec playwright test apps/web/e2e/article-lifecycle.spec.ts --workers=1 &amp;&amp; corepack pnpm -r typecheck &amp;&amp; corepack pnpm --filter @blog-x/web build &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The SSR/no-script pair is coherently +08:00, enhanced browsers use the scheduled or selected target date's actual offset, post changes reset controlled values, unchanged resubmission preserves the instant in both scripting modes, and all local quality gates pass.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable UTC instant to rendered local controls | Trusted admin state is transformed into two user-visible fields whose combined meaning must remain exact. |
| SSR markup to hydrated state | Server +08:00 fallback is replaced by browser-derived target-date state without permitting a mixed pair. |
| Browser/native form to admin API proxy | Editable local datetime and offset cross the existing authenticated same-origin mutation boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-Q-45R-01 | Tampering | Schedule form pair | high | mitigate | Keep local datetime and offset in one controlled state and assert exact normalized responses after unchanged resubmission. |
| T-Q-45R-02 | Tampering | Hydration transition | high | mitigate | Initialize a coherent +08 pair, then replace both fields atomically from the target instant/browser timezone. |
| T-Q-45R-03 | Repudiation | Native no-script reschedule | medium | mitigate | Require exact POST, 302 Location, authenticated GET, and refreshed visible state for the retained instant. |
| T-Q-45R-04 | Denial of service | Invalid offset/date | low | mitigate | Keep existing required/pattern/API validation and bound pure numeric formatting; invalid input remains user-correctable and fail-closed. |
| T-Q-45R-SC | Tampering | Package supply chain | high | mitigate | Add no dependency or lockfile change; use only existing React, Node test, and Playwright tooling. |
</threat_model>

<verification>
- Run the new pure Web unit suite through the exact default test coordinator after updating its closed inventory and literal child argv.
- Run the full `article-lifecycle.spec.ts` in the generated canonical main-browser fixture; the new context must explicitly use `America/New_York` and never an external origin.
- Inspect final markup/state logic for a coherent +08 server initializer, target-date browser offset, controlled `scheduledAt`, synchronized post changes, exact existing field names, and no hydration suppression.
- Run default tests, inventory/coordinator tests, workspace typecheck, Web build, boundary checks, and `git diff --check`.
- Do not run `local:deliver`, refresh a fixed runtime, access either server, deploy, switch to or modify `main`, install packages, or run destructive commands.
</verification>

<success_criteria>
- Phase 10 review finding CR-01 is closed with exact automated proof rather than timezone-dependent assumptions.
- `datetime-local` and `timezoneOffset` describe the same instant on SSR, after hydration, after target-date/DST changes, after a changed post prop, and after reload.
- An unchanged JavaScript resubmit and an unchanged no-script +08 resubmit both preserve the original UTC `scheduledAt` byte-for-byte.
- All affected unit, browser, type, build, boundary, and inventory gates pass without API/database/contract/delivery changes.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID / item | Plan coverage | Status |
|---|---|---|---|
| REVIEW | CR-01 existing schedules reinterpreted in the wrong timezone | Tasks 1-2 cover coherent SSR/hydration state and exact unchanged resubmission. | COVERED |
| REQ | CONT-05 schedule, inspect, reschedule, and cancel | Browser tests preserve the selected instant through changed post state, reload, reschedule, and cancellation. | COVERED |
| REQ | CONT-08 fail closed without unsafe fallback | Existing guarded routes and strict date/offset validation remain authoritative in both scripting modes. | COVERED |
| PROJECT | Responsive same-origin local development under production freeze | Tests use only the generated local origin; no topology, server, or deployment action changes. | COVERED |
| AUDIT | Target-date DST, not today's browser offset | The New York winter/summer journey and pure offset tests require the selected date's actual offset. | COVERED |

No scheduler execution, due-publisher change, public visibility change, API/database/contract redesign, timezone database dependency, production deployment, TLS work, or unrelated UI redesign appears in this plan.

<output>
After execution, create `.planning/quick/260905-45r-fix-scheduled-datetime-timezone-round-tr/260905-45r-SUMMARY.md`. The executor owns the two task commits; the parent owns review, push of `dev`, and any later formal local delivery. Never access a server, modify `main`, install dependencies, or run a destructive command.
</output>
