---
phase: 05-v1-0-integration-gap-closure
plan: "04"
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - scripts/phase5-receipt.mjs
  - scripts/phase5-receipt.test.mjs
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - scripts/check-boundaries.mjs
  - scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json
  - scripts/fixtures/prohibitions/phase5-receipt-actual-results.json
  - docs/RELEASE-GATE.md
  - ops/phase5-full-gate-receipt.json
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
gap_closure: true
requirements:
  - OPS-05
user_setup: []
must_haves:
  truths:
    - "OPS-05: every retained Phase 5 suite result is created from output or a strict structured result captured during that exact execution; no suite digest or count is derived from suite ID, implementation revision, a success literal, or fixed `1/1` values."
    - "Receipt v2 embeds one canonical redacted execution-result record per manifest suite and binds the exact canonical record bytes plus the SHA-256 and byte length of the redacted captured output; aggregate counts equal the recorded invocation counts."
    - "TAP/database, Playwright, generated production-pipeline, and repository-boundary results use distinct strict parsers and all fail closed on missing/truncated/altered/zero/failed/skipped/TODO/cancelled/inconsistent output or an unexpected result scope."
    - "The Phase 5 manifest contains every selected test source exactly once plus an explicit generated-pipeline entry and boundary entry; finalization rejects a missing, duplicate, unknown, or unrecorded manifest ID."
    - "Result capture is owned by the parent full-gate run at command completion, redacts before hashing, preserves interleaved stdout/stderr order, and cannot be shared with interruption or parallel child namespaces."
    - "Before any Phase 5 suite executes, the parent full-gate process exclusively creates the one fixed receipt-writer lock, captures the exact predecessor receipt existence/version/SHA-256 under that lock, and holds the lock authority through compare-and-swap, atomic rename, and final receipt readback."
    - "Two competing parent `--phase5-full` processes cannot both run as receipt writers: exactly one fixed-lock owner proceeds, while a live-owner loser fails closed; an interrupted owner's stale lock is recoverable only after exact owner-liveness/birth-identity proof and cannot be stolen from a live or PID-reused writer."
    - "Receipt creation remains after all suites, generated pipeline runs, cleanup, interruption recovery, parallel isolation, and the actual canonical `BLOCKED` decision; any earlier failure preserves the previous receipt and milestone audit byte-for-byte."
    - "The existing v1 receipt is legacy read-only migration input while implementation is being committed; the writer accepts only v2, and a migrated audit explicitly requires v2 so it cannot be downgraded to a synthetic v1 receipt."
    - "Tasks 1-2 are committed with a clean worktree before the exact full gate runs; Task 3 commits only the verified replacement receipt, Task 4 updates the audit later, and an independent Phase 5 verifier—not this executor—decides whether to replace the gaps-found verification report."
    - "Canonical production release stays locator-free `BLOCKED`; the plan performs no server, network, mount provisioning, systemd activation, alert delivery, TLS, deployment, rollback, or release-state transition operation."
  artifacts:
    - path: scripts/phase5-receipt.mjs
      provides: strict v2 receipt, canonical execution-record bytes, fixed O_EXCL writer-lock and predecessor compare-and-swap authority, v1 read-only migration, and v2-only atomic writer
      exports:
        - phase5ExecutionResultSchema
        - phase5ReceiptSchema
        - canonicalPhase5ResultBytes
        - hashPhase5ResultRecord
        - acquirePhase5ReceiptWriterLock
        - releasePhase5ReceiptWriterLock
        - writePhase5ReceiptAtomic
        - verifyPhase5Receipt
    - path: scripts/local-verify.mjs
      provides: parent-owned pre-suite receipt lock/predecessor capture, execution-time mixed-result parsers, isolated result recorder, exact suite-result finalization, and terminal receipt orchestration
      exports:
        - parseSemanticTapResult
        - parsePlaywrightResult
        - parseBoundaryResult
        - createPhase5ResultRecorder
        - phase5Selection
    - path: scripts/check-boundaries.mjs
      provides: machine-readable actual boundary outcome/count and v2 receipt-before-audit enforcement
      exports:
        - evaluateRepositoryBoundaries
        - auditMilestoneReceipt
    - path: scripts/local-verify.test.mjs
      provides: RED/GREEN parser, capture, omission, inconsistency, interruption, and parallel-isolation coverage
    - path: scripts/phase5-receipt.test.mjs
      provides: v2 canonical-byte/digest, fixed-lock/CAS concurrency and interrupted-owner recovery, migration, atomic-preservation, downgrade, and premature-audit coverage
    - path: ops/phase5-full-gate-receipt.json
      provides: replacement v2 receipt containing actual canonical result records from the clean committed exact gate
    - path: .planning/v1.0-MILESTONE-AUDIT.md
      provides: later v2-receipt-citing milestone audit that retains all live production blockers
  key_links:
    - from: scripts/local-verify.mjs
      to: scripts/phase5-receipt.mjs
      via: the parent acquires the fixed receipt lock and captures its predecessor before `runSingle`; `createPhase5ResultRecorder` converts actual results, and terminal finalization uses the same held lock/predecessor authority through v2 write/readback/release
      pattern: "acquirePhase5ReceiptWriterLock|expectedPredecessor|createPhase5ResultRecorder|writePhase5ReceiptAtomic|releasePhase5ReceiptWriterLock"
    - from: scripts/local-verify.mjs
      to: scripts/backup/production/results.mjs
      via: both parallel generated pipeline returns are strict-parsed as actual `generated-production-pipeline` results before their canonical bytes and counts are recorded
      pattern: "productionBackupResultSchema|generated-production-pipeline|runPhase5GeneratedPipeline"
    - from: scripts/local-verify.mjs
      to: scripts/check-boundaries.mjs
      via: the boundary suite consumes one machine-readable result with the actual tracked-file and finding counts rather than inferring pass from exit code alone
      pattern: "parseBoundaryResult|BOUNDARY RESULT|filesChecked|findings"
    - from: scripts/phase5-receipt.mjs
      to: ops/phase5-full-gate-receipt.json
      via: one fixed O_EXCL lock serializes writers; target version/digest must still equal the pre-suite predecessor immediately before rename, then each record and final bytes are verified before the lock is safely released
      pattern: "phase5-full-gate-receipt\.json\.lock|expectedPredecessor|version.*2|resultRecord|resultSha256|redactedOutputSha256"
    - from: scripts/check-boundaries.mjs
      to: .planning/v1.0-MILESTONE-AUDIT.md
      via: a post-migration passed audit must declare receipt version 2 and match the verified receipt path, bytes, implementation revision, and timestamp ordering
      pattern: "full_gate_receipt_version|full_gate_receipt_sha256|implementation_revision"
    - from: .planning/v1.0-MILESTONE-AUDIT.md
      to: ops/phase5-full-gate-receipt.json
      via: only the later audit commit cites the independently verified replacement receipt; the gaps-found verification report remains for a subsequent independent verification run
      pattern: "full_gate_receipt_version: 2|full_gate_receipt_sha256|BLOCKED"
  prohibitions:
    - statement: "Suite result hashes or counts must not be synthesized from suite IDs, revision strings, pass literals, or fixed one-test values."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/local-verify.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json
      check_clean_fixture: scripts/fixtures/prohibitions/phase5-receipt-actual-results.json
    - statement: "Missing, altered, zero, skipped, TODO, cancelled, failed, truncated, duplicate, or internally inconsistent mixed-suite output must not produce a receipt candidate."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/phase5-receipt.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json
      check_clean_fixture: scripts/fixtures/prohibitions/phase5-receipt-actual-results.json
    - statement: "A failed or interrupted gate must not replace the previous receipt or edit the passed milestone audit."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/phase5-receipt.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/premature-phase5-audit.json
      check_clean_fixture: scripts/fixtures/prohibitions/phase5-receipt-clean.json
    - statement: "Competing parent full-gate writers, a stale predecessor, or an unsafe/stolen receipt lock must not overwrite another run's receipt; random incomplete siblings alone are not mutual exclusion."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/phase5-receipt.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/premature-phase5-audit.json
      check_clean_fixture: scripts/fixtures/prohibitions/phase5-receipt-clean.json
    - statement: "No local result, receipt, audit, parser, CLI, or runbook may contact a server or grant production readiness, deployment, rollback, or unfreeze authority."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/release-gate.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/automatic-deploy.json
      check_clean_fixture: ops/release-evidence.blocked.json
---

<objective>
Replace the synthetic Phase 5 suite outcomes with exact canonical records derived from actual execution output, then regenerate receipt and audit evidence in strict commit order while production remains BLOCKED.

Purpose: Close the single deterministic Phase 05 verification gap so OPS-05 and Roadmap criterion 4 are supported by actual TAP, Playwright, database, pipeline, and boundary outcomes rather than source-only hashes and fabricated counts.
Output: Receipt v2/result schemas, fail-closed mixed-output parsers and recorder wiring, a clean-implementation full-gate replacement receipt, a later receipt-bound audit, and an explicit handoff to independent Phase 5 verification.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md
@.planning/phases/05-v1-0-integration-gap-closure/05-RESEARCH.md
@.planning/phases/05-v1-0-integration-gap-closure/05-PATTERNS.md
@.planning/phases/05-v1-0-integration-gap-closure/COVERAGE.md
@.planning/phases/05-v1-0-integration-gap-closure/05-03-SUMMARY.md
@.planning/v1.0-MILESTONE-AUDIT.md
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/phase5-receipt.mjs
@scripts/phase5-receipt.test.mjs
@scripts/check-boundaries.mjs
@scripts/backup/production-pipeline.mjs
@scripts/backup/production/results.mjs
@ops/phase5-full-gate-receipt.json

<interfaces>
- Preserve 05-03's exact command, clean committed implementation authority, atomic exclusive sibling/fsync/rename/readback, source-byte manifest hashes, cleanup-before-write, audit ordering, local-only scope, and canonical `BLOCKED` decision.
- Receipt v2 has exactly `{ format, version, implementationRevision, command, mode, scope, startedAt, completedAt, suiteManifest, suiteManifestSha256, suites, canonicalEvidenceSha256, canonicalDecisionSha256, canonicalDecisionState }`. Manifest v2 has exactly `{ format, version, suites }`, with entries `{ id, kind, path, sourceSha256 }`. Each receipt suite has exactly `{ id, sourceSha256, resultRecord, resultSha256 }`; no legacy top-level count fields remain.
- Receipt v2 execution record shape is exact: `{ format, version, suiteId, kind, sourceSha256, invocations, counts, outcome }`. Each invocation is exact: `{ ordinal, parser, startedAt, completedAt, exitCode, signal, redactedOutputBytes, redactedOutputSha256, counts }`. Counts are exact: `{ tests, passed, failed, cancelled, skipped, todo }`.
- Invocation `parser` is exactly one of `node-tap-v13`, `playwright-line-v1`, `production-backup-result-v1`, or `repository-boundary-result-v1`; parser and suite kind must match.
- `canonicalPhase5ResultBytes(record)` recursively sorts object keys, preserves array order, serializes UTF-8 JSON with one defined trailing-newline rule, and rejects unsupported/non-finite values. `resultSha256` is SHA-256 of those exact bytes; the receipt embeds `resultRecord` so read-only verification can recompute it.
- Captured command output is redacted before hashing, strips ANSI control sequences, normalizes CRLF/CR to LF, retains all remaining bytes and their chronological stdout/stderr chunk order, and stores only its safe SHA-256/byte length in the receipt—not raw logs, credentials, cookies, database URLs, generated authorities, or endpoints.
- TAP/database uses the one complete terminal Node TAP footer and requires `tests = pass + fail + cancelled + skipped + todo`, tests/pass > 0, and every non-pass count zero. Playwright requires one `Running N tests` total to equal terminal passed+failed+skipped/did-not-run totals, with N/pass > 0 and every non-pass/flaky/interrupted count zero. Missing, duplicate/conflicting, truncated, or unparseable summaries fail.
- Pipeline records come only from strict `productionBackupResultSchema` values returned by the two actual parallel `runPhase5GeneratedPipeline()` calls; both must be distinct, complete, `generated-production-pipeline`, alert-recorded, receipt/manifest/ciphertext-bound, and retention-safe. Boundary records come only from one strict machine line produced by `evaluateRepositoryBoundaries`, with positive `filesChecked`, zero findings, and exit 0.
- Receipt mutual exclusion uses exact fixed paths derived from the already validated receipt target: `${receiptPath}.lock` is the writer lock and `${receiptPath}.lock.recovery` is the stale-recovery guard. Both are regular non-symlink files created with `open(..., "wx", 0o600)` (O_CREAT|O_EXCL), under the already validated non-symlink parent owned by the current UID and not group/world-writable. Random `.incomplete-*` siblings remain atomic staging only and never count as a lock.
- The writer-lock record is strict and redacted: `{ format: "blog-x-phase5-receipt-writer-lock", version: 1, ownerPid, ownerBirthIdentity, ownerNonce, acquiredAt }`. `ownerBirthIdentity` is the normalized nonempty output of fixed local `execFile("ps", ["-p", String(pid), "-o", "lstart="])`, paired with `process.kill(pid, 0)` liveness and rechecked before recovery so PID reuse is not mistaken for the original owner. A matching live PID+birth identity is never stale and makes the contender fail closed without waiting or mutation. Tests may inject only this fixed process-inspection seam, never lock ownership decisions.
- Stale recovery first acquires the fixed O_EXCL recovery guard, whose strict owner record, open handle, PID/birth identity, nonce, UID/mode/type checks, and stale-owner recovery rules are identical to the writer lock. All normal acquisition paths check the guard before creation and recheck it after writer-lock creation; a concurrent guard causes safe release/failure. While holding the guard, recovery re-reads the writer lock and process identity and may remove it only when the recorded PID is absent or its birth identity differs. It then creates and readback-verifies its own fixed writer lock before releasing the guard. Neither lock is removed by age alone. Release/recovery compares `dev`, `ino`, and owner nonce from the held open `FileHandle` against the path immediately before unlink, then verifies the path is absent; mismatch never unlinks. Bounded retry handles a guard owner that dies mid-recovery, and SIGKILL is covered by the same proven stale PID+birth-identity path.
- The winning parent acquires the writer lock before `runSingle`, then reads the target once under that lock as `expectedPredecessor: { exists, version, sha256 }` (`exists: false` requires no target). The same opaque lock authority and predecessor are mandatory inputs to `writePhase5ReceiptAtomic`. Immediately before rename it re-lstats/re-reads the target and requires exact existence/version/SHA-256 equality; mismatch fails without rename. The lock remains held through rename, fsync, strict final readback and byte comparison, and is released safely in the parent's outermost `finally` on both success and failure.
- The current v1 receipt and its cited audit remain immutable while Tasks 1-2 are implemented. V1 support is read-only and restricted to the existing historical artifact; `writePhase5ReceiptAtomic` accepts v2 only. Once the audit declares `full_gate_receipt_version: 2`, any v1/downgrade receipt is rejected.
- No task connects to either cloud server or any network service. Docker/Compose, loopback HTTP, generated temporary roots, and the generated mounted-directory fixture are the only runtime authorities.
</interfaces>
</context>

## Why this plan has four tasks

Four tasks are required to preserve the proof boundary: test-first schema/parser work and runner wiring must be committed before the canonical gate; the successful gate then creates a receipt-only evidence commit; only a later task may update the audit. Combining the last two would remove the independently checkable receipt-before-audit commit ordering that this gap closure is meant to protect.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical actual-result records and fail-closed mixed parsers</name>
  <files>scripts/phase5-receipt.mjs, scripts/phase5-receipt.test.mjs, scripts/local-verify.mjs, scripts/local-verify.test.mjs, scripts/check-boundaries.mjs, scripts/fixtures/prohibitions/phase5-receipt-synthetic-results.json, scripts/fixtures/prohibitions/phase5-receipt-actual-results.json</files>
  <read_first>
    <item>scripts/phase5-receipt.mjs — current v1 strict object, suite manifest/result checks, scalar scan, atomic writer, and read-only CLI.</item>
    <item>scripts/phase5-receipt.test.mjs — current schema, preservation, digest, and audit-ordering tests.</item>
    <item>scripts/local-verify.mjs — `command`, `redactText`, `assertSemanticTap`, `assertPlaywrightJourney`, `createPhase5SuiteManifest`, `phase5ReceiptCandidate`, and `committedImplementationHead`.</item>
    <item>scripts/local-verify.test.mjs — current semantic TAP/Playwright and terminal receipt structural assertions.</item>
    <item>scripts/check-boundaries.mjs — `trackedFiles`, `auditRepository`, CLI pass output, and `auditMilestoneReceipt`.</item>
    <item>ops/phase5-full-gate-receipt.json and .planning/v1.0-MILESTONE-AUDIT.md — exact legacy bytes that must remain readable and unchanged until Task 3 succeeds.</item>
  </read_first>
  <action>
    Start RED with table-driven tests and the two data-only prohibition fixtures. Prove the current synthetic formula/fixed-count candidate is rejected; prove missing and altered output, a zero test, TAP fail/skip/TODO/cancel, footer arithmetic mismatch, conflicting footer, Playwright fail/skip/zero/did-not-run/flaky/launch-count mismatch, invalid pipeline scope/result/digest/alert/retention, boundary zero-files/nonzero-findings/missing-marker, duplicate/unknown suite ID, altered result record, and aggregate/invocation mismatch all fail before a receipt candidate exists. Also RED-test that two writer calls using only different random incomplete siblings can both enter today, that a target changed after the pre-suite snapshot is overwritten today, and that a stale lock cannot be recovered safely without live-owner/birth-identity discrimination.

    GREEN by adding the exact v2 execution record and receipt schemas described in `<interfaces>`. Export `phase5ExecutionResultSchema`, `canonicalPhase5ResultBytes`, and `hashPhase5ResultRecord`. Every receipt suite entry must contain `id`, `sourceSha256`, the full safe `resultRecord`, and `resultSha256`; strict parsing recomputes the record digest and verifies manifest/source/ID/kind/count/invocation equality. Remove acceptance of bare arbitrary result digests and top-level fixed counts. Bump both receipt and suite manifest to version 2.

    Add pure `parseSemanticTapResult`, `parsePlaywrightResult`, and `parseBoundaryResult` helpers in `scripts/local-verify.mjs`; keep the existing `assertSemanticTap`/`assertPlaywrightJourney` exports only as compatibility wrappers around the strict parsers. Add `evaluateRepositoryBoundaries(root)` in `scripts/check-boundaries.mjs` so the CLI emits exactly one parseable `BLOG X BOUNDARY RESULT <json>` line containing actual `filesChecked`, `findings`, and outcome after auditing the tracked file list.

    Implement `acquirePhase5ReceiptWriterLock` and `releasePhase5ReceiptWriterLock` exactly as specified in `<interfaces>`, including fixed O_EXCL writer/recovery paths, strict no-link/UID/0600 checks, owner nonce+PID+birth identity, safe stale recovery, and owner-checked release. Acquisition returns an opaque authority plus the exact predecessor existence/version/SHA-256 captured under the lock. Make `writePhase5ReceiptAtomic` require that authority and compare the target to the expected predecessor immediately before rename, while retaining the lock through fsync/readback. Random staging siblings are never accepted as lock authority.

    Implement migration without invalidating historical proof mid-plan: split strict legacy-v1 read-only parsing from current-v2 parsing, make the atomic writer v2-only and lock/CAS-required, and allow the current exact v1 receipt/audit tuple to remain verifiable until replacement. A v2 audit must declare `full_gate_receipt_version: 2`; v2-to-v1 downgrade or a different synthetic v1 receipt must fail. Do not modify the retained receipt or audit in this task.
  </action>
  <acceptance_criteria>
    <criterion>`scripts/local-verify.mjs` no longer contains `phase5-semantic-pass` and no receipt path maps suites to literal `tests: 1, passed: 1`.</criterion>
    <criterion>`phase5ReceiptSchema` requires version 2 embedded `resultRecord` values and recomputes every `resultSha256` from `canonicalPhase5ResultBytes`; `writePhase5ReceiptAtomic` rejects version 1.</criterion>
    <criterion>TAP, Playwright, pipeline, and boundary negative cases named above each fail in a focused unit test; valid colored/CRLF output canonicalizes and parses to actual nonzero counts.</criterion>
    <criterion>Canonical bytes are invariant to object insertion order, sensitive to any semantic value/array/output-digest change, and contain no raw captured logs.</criterion>
    <criterion>The writer accepts only the opaque owner of the exact fixed O_EXCL lock, rejects unsafe/link/wrong-owner/wrong-mode/foreign-nonce locks, and rejects target existence/version/digest changes before rename while preserving predecessor receipt/audit bytes.</criterion>
    <criterion>Deterministic child-process tests prove a live matching owner cannot be displaced, an interrupted/dead owner can be recovered behind the fixed recovery guard without PID-reuse ambiguity, and release removes only the caller's unchanged lock.</criterion>
    <criterion>The existing v1 receipt and audit SHA-256 values remain unchanged, remain narrowly read-only verifiable during migration, and cannot satisfy a v2-declared audit.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/phase5-receipt.test.mjs scripts/local-verify.test.mjs &amp;&amp; corepack pnpm check:boundaries</automated>
  </verify>
  <done>Receipt v2 can represent only canonical actual-result records, all mixed output types have strict parsers, and historical proof remains readable but unwritable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture every selected execution at its owner and commit the implementation</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs, scripts/phase5-receipt.mjs, scripts/phase5-receipt.test.mjs, scripts/check-boundaries.mjs, docs/RELEASE-GATE.md</files>
  <read_first>
    <item>Task 1's v2 schemas, canonical byte function, strict parsers, migration tests, and unchanged historical receipt/audit.</item>
    <item>scripts/local-verify.mjs — every `runDatabaseSuite`, Node TAP, Playwright, restore authority, release, generated pipeline, boundary, `runSingle`, `parallelCheck`, and main receipt call site.</item>
    <item>scripts/backup/production-pipeline.mjs and scripts/backup/production/results.mjs — actual generated result fields, strict schema, scopes, receipt/retention/alert facts.</item>
    <item>scripts/check-boundaries.mjs — machine boundary result and passed-audit checks.</item>
    <item>docs/RELEASE-GATE.md — implementation-before-receipt and receipt-before-audit contract.</item>
  </read_first>
  <action>
    Start RED around a new per-run `createPhase5ResultRecorder(manifest, secrets)` owned only by the parent `--phase5-full` context. Tests must show two recorders with different namespaces cannot see each other's entries; internal interruption/parallel children never receive a receipt writer; repeated invocations aggregate under the one manifest suite without losing individual output digests; and finalize rejects any of the exact 28 manifest entries missing, duplicated, unknown, or kind/source-mismatched. The 28 entries are the existing 27 source paths plus explicit `kind: pipeline`, path `scripts/backup/production-pipeline.mjs`; the existing boundary entry remains explicit.

    Add deterministic two-parent subprocess tests against one generated validated receipt target and its one fixed lock path. Use explicit IPC/barrier hooks, not timing guesses: parent A acquires and proves the predecessor before any simulated suite, parent B attempts the same target while A is live and must fail without starting suites or changing receipt/audit, then A alone completes write/readback/release. In the interruption case, kill A only after lock/readback and before receipt mutation, prove predecessor receipt and audit bytes unchanged, then let B prove A's exact birth identity is dead, recover behind the fixed recovery guard, acquire, and complete. Add a CAS case that alters target bytes after acquisition: final write must fail and preserve the altered target plus audit. Assert exactly one successful writer in every race and no orphan live-owner lock is removed.

    Change `command()` to return actual start/completion times, exit code/signal, separate stdout/stderr, and a chronological interleaved captured stream assembled from data-event chunks. `runStep` remains the redacted error boundary. Immediately after each successful command, its owning database/TAP/Playwright call site invokes the strict parser and records the result; no later function may reconstruct a pass from a path, exit code, or revision. Redact secrets first, then normalize ANSI/newlines, hash exact safe bytes, and record actual byte length and parsed counts. If output is missing, unparseable, inconsistent, or the command signalled/nonzero, recording/finalization fails.

    Wire all selected paths, including the restore database authority and every selected browser journey. Record the two actual parallel `runPhase5GeneratedPipeline()` returned objects through `productionBackupResultSchema`, require distinct set IDs/digests and complete generated pipeline facts, then aggregate their actual two outcomes in the pipeline record. Record the final `corepack pnpm check:boundaries` machine result with its actual positive tracked-file count and zero findings. Bind `canonicalDecisionSha256` to the redacted actual `--expect-blocked` output bytes instead of `hashText("RELEASE BLOCKED")`.

    Remove `phase5ReceiptCandidate`. In the outer parent `main`, acquire the fixed receipt-writer lock and capture the expected predecessor before calling `runSingle`; children/internal modes cannot acquire or inherit this authority. Hold it across `runSingle`, cleanup, `parallelCheck`, clean-HEAD recheck, exact manifest/record finalization, compare-and-swap, atomic write/fsync/readback, and release it only from the outermost `finally`. A contender fails before suite execution. A suite, parser, pipeline, cleanup, interruption, parallel, revision, boundary, predecessor mismatch, lock-ownership, or canonical-release failure must prevent rename and preserve receipt/audit. Update `docs/RELEASE-GATE.md` to describe actual redacted output binding, one fixed serialized writer, expected-predecessor CAS, stale recovery, and v2 migration without changing local-only/BLOCKED scope.

    Run only the focused commands below. Let execute-plan commit Tasks 1 and 2 normally, then require `git status --porcelain` to be empty and capture that committed HEAD as the only Task 3 implementation revision. Do not run the exact `--phase5-full --interruption-check --parallel-check` command and do not modify the retained receipt, audit, or verification report in this task.
  </action>
  <acceptance_criteria>
    <criterion>`createPhase5SuiteManifest()` returns version 2 with exactly 28 unique IDs, exact source hashes, one explicit pipeline entry, and one boundary entry; result finalization returns exactly those 28 records.</criterion>
    <criterion>Every selected TAP/database/Playwright call site records the exact returned command result immediately after strict parsing; every aggregate count equals the sum of its invocation records.</criterion>
    <criterion>The pipeline record contains both actual distinct generated pipeline results and count 2/2; the boundary record derives counts from the actual machine result; the canonical decision digest derives from actual captured BLOCKED output.</criterion>
    <criterion>Tests prove missing suite output, a second recorder/parallel collision, an interrupted child, or any failure before finalization cannot call the writer or change prior receipt/audit bytes.</criterion>
    <criterion>Deterministic two-parent tests prove the lock is acquired before either suite recorder starts, exactly one live parent proceeds, the loser exits without mutation, dead-owner recovery is safe, CAS detects target changes, and lock authority lasts through final readback.</criterion>
    <criterion>Focused tests, production result tests, boundaries, and typecheck pass; implementation is committed and clean while `ops/phase5-full-gate-receipt.json`, `.planning/v1.0-MILESTONE-AUDIT.md`, and `05-VERIFICATION.md` retain their pre-task bytes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/local-verify.test.mjs scripts/phase5-receipt.test.mjs scripts/backup/production.test.mjs scripts/release-gate.test.mjs &amp;&amp; corepack pnpm check:boundaries &amp;&amp; corepack pnpm -r typecheck</automated>
  </verify>
  <done>The committed clean implementation owns and binds every actual mixed-suite result, but no replacement receipt or updated audit exists yet.</done>
</task>

<task type="auto">
  <name>Task 3: Run the clean committed exact gate and commit only the replacement receipt</name>
  <files>ops/phase5-full-gate-receipt.json</files>
  <read_first>
    <item>The clean committed Task 1-2 HEAD, `git status --porcelain`, and the unchanged legacy receipt/audit bytes; stop if implementation is dirty or uncommitted.</item>
    <item>scripts/phase5-receipt.mjs — v2-only writer, fixed O_EXCL writer/recovery locks, predecessor CAS, read-only v1 migration path, strict record/digest verifier, and exact fixed target.</item>
    <item>scripts/local-verify.mjs — final order: acquire/capture predecessor, actual suites/pipeline/boundary/BLOCKED, cleanup, interruption/parallel proof, committed-HEAD recheck, finalize, CAS/atomic writer/readback, safe release.</item>
    <item>scripts/phase5-receipt.test.mjs and scripts/local-verify.test.mjs — deterministic two-parent/live-owner/interruption/CAS preservation plus missing/altered/mixed-output controls.</item>
  </read_first>
  <action>
    Require `git status --porcelain` to be empty and record the committed Task 1-2 HEAD as `implementationRevision`. Record the current receipt version/SHA-256 and audit SHA-256 as the expected predecessor/failure-preservation baseline. Before any suite starts, the command must acquire the exact fixed O_EXCL lock and capture that same predecessor tuple under the lock. Run exactly:

    `corepack pnpm local:verify -- --phase5-full --interruption-check --parallel-check`

    Do not substitute a focused command, omit either flag, reuse an earlier run, edit the receipt manually, or run against any live/server/network authority. The runner may atomically replace the fixed receipt only after all actual result records, cleanup, parallel isolation, the actual canonical BLOCKED result, and the final clean-HEAD check succeed.

    If the command loses lock authority, sees a live competing owner, finds a predecessor version/digest mismatch, exits nonzero, is interrupted, reports missing/invalid/zero/skip/TODO/fail/inconsistent output, changes implementation HEAD/worktree, or leaves an invalid receipt, stop immediately. Prove the expected predecessor receipt and audit are byte-identical unless the explicit CAS test's external mutation is the detected cause; never overwrite that changed target. Do not attempt repair by editing evidence and do not proceed to Task 4.

    On exit 0, independently run the read-only verifier and focused receipt tests. Confirm version 2, exact command/mode/scope, pre-run implementation revision, manifest/source hashes, exactly 28 embedded records, per-record canonical digest equality, captured-output digest/length fields, actual aggregate/invocation count equality, two distinct pipeline results, positive boundary file count, actual BLOCKED decision digest, timestamps, and no raw secret/address/live claim. Let execute-plan commit only `ops/phase5-full-gate-receipt.json` as the first evidence commit. The milestone audit and Phase 5 verification report remain untouched.
  </action>
  <acceptance_criteria>
    <criterion>The exact command exits 0 from the clean committed Task 1-2 revision and produces a strict v2 receipt whose `implementationRevision` equals that pre-run HEAD.</criterion>
    <criterion>All 28 manifest entries have embedded canonical actual-result records and recomputable digests; real TAP/Playwright counts are retained rather than all suites reporting 1/1, and pipeline/boundary counts match their actual outputs.</criterion>
    <criterion>Receipt creation follows cleanup, parallel proof, and actual terminal BLOCKED output; no raw captured logs, credentials, addresses, or live production claims are retained.</criterion>
    <criterion>The exact fixed lock is held from pre-suite predecessor capture through final strict receipt readback; immediate pre-rename CAS matches the recorded predecessor, and safe release leaves no owner lock/recovery guard.</criterion>
    <criterion>Any failed attempt preserves both old receipt and audit bytes; successful execution commits the replacement receipt alone, with the old audit still citing the prior receipt until Task 4.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase5-receipt.mjs verify --receipt=ops/phase5-full-gate-receipt.json &amp;&amp; node --test scripts/phase5-receipt.test.mjs scripts/local-verify.test.mjs &amp;&amp; git diff --exit-code -- .planning/v1.0-MILESTONE-AUDIT.md .planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md</automated>
  </verify>
  <done>A separately committed replacement v2 receipt binds actual execution results from the exact clean full gate, while audit and verification evidence are still unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Update the audit after the receipt commit and hand off independent verification</name>
  <files>.planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
    <item>The committed Task 3 v2 receipt, its independently recomputed file SHA-256, implementation revision, completion time, manifest count, and actual result totals.</item>
    <item>.planning/v1.0-MILESTONE-AUDIT.md — current prior receipt citation and the claims about 27 passing semantic results.</item>
    <item>scripts/check-boundaries.mjs — v2 audit version/digest/revision/timestamp/downgrade enforcement.</item>
    <item>.planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md — the deterministic gap report that must remain unchanged until a separate verifier reruns goal-backward verification.</item>
  </read_first>
  <action>
    Require the Task 3 receipt-only commit to be an ancestor of a clean current HEAD. Re-run the read-only receipt verifier and derive—not estimate—the exact receipt SHA-256, implementation revision, completion time, manifest suite count, aggregate test/pass/fail/skip/TODO/cancelled counts, pipeline attempts, and boundary files checked.

    Only now update the milestone audit in a later evidence/documentation commit. Add `full_gate_receipt_version: 2`, replace the prior digest/revision/time citation, and replace the unsupported "27 passing nonzero semantic results" wording with the exact v2 actual-record facts. State that each result digest recomputes from the embedded canonical record and each output digest/length came from redacted bytes captured at execution time. Retain the exact local/generated scope, no-server-contact statement, all missing live host/private-link/mount/off-host/schedule/alert/TLS/deployment/post-release facts, and canonical `BLOCKED` state.

    Run the boundary and receipt checks after the edit. If receipt bytes, revision ancestry, v2 declaration, audit timestamp ordering, or actual count claims disagree, revert no evidence automatically: leave the prior audit unchanged, report failure, and stop. Do not edit `.planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md`, do not mark Phase 5 complete, and do not mark OPS-05 complete in state/requirements during execution.

    After this task's later audit commit and the normal 05-04 summary, hand the phase to an independent `$gsd-verify-phase 05` follow-up. That verifier must independently reconstruct v2 canonical record hashes, inspect actual counts and output-digest bindings, verify receipt/audit/revision ordering, and retain production BLOCKED/live blockers. Only that separate report may replace `status: gaps_found` with `passed` and complete Phase 5.
  </action>
  <acceptance_criteria>
    <criterion>The audit declares receipt version 2 and its path/SHA-256/implementation revision/timestamp exactly match independently verified receipt bytes and commit ancestry.</criterion>
    <criterion>Audit suite/count statements are derived from receipt v2 actual records and no longer claim fixed or synthetic per-suite outcomes.</criterion>
    <criterion>The audit commit is later than and separate from the receipt-only commit; downgrade to v1, missing result records, digest/count mismatch, or premature audit fails boundaries.</criterion>
    <criterion>Canonical release remains BLOCKED with every live precondition listed; `05-VERIFICATION.md` remains gaps_found pending the explicitly required independent verification follow-up.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase5-receipt.mjs verify --receipt=ops/phase5-full-gate-receipt.json &amp;&amp; corepack pnpm check:boundaries &amp;&amp; rg -n "full_gate_receipt_version: 2|full_gate_receipt_sha256|implementation_revision|actual|canonical|OPS-05|BLOCKED" .planning/v1.0-MILESTONE-AUDIT.md &amp;&amp; rg -n "status: gaps_found|requirements_blocked: \[OPS-05\]" .planning/phases/05-v1-0-integration-gap-closure/05-VERIFICATION.md</automated>
  </verify>
  <done>The later audit cites the exact committed v2 receipt without live claims, and Phase 5 is explicitly queued for independent re-verification rather than self-certified complete.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high. Any unmitigated high-severity row blocks receipt replacement and audit editing.

| Threat / failure | Severity | Required mitigation and evidence |
|---|---|---|
| Synthetic pass digest or fixed count survives | High | Delete formula/candidate; embedded strict actual record; canonical digest recomputation; source scan and violation fixture. |
| Output is missing, truncated, altered, or internally inconsistent | High | Complete type-specific terminal parser; arithmetic/launch/result invariants; captured-output digest/length; RED cases for every failure shape. |
| Skip/TODO/cancel/failure is reported as pass | High | TAP and Playwright non-pass counters/directives are fatal; schema aggregate must equal invocation sums and outcome pass only with all non-pass counts zero. |
| Database output is inferred from command success | High | Database suites use the same exact terminal TAP parser and execution-time recorder as Node suites after Compose capture. |
| Fake or partial generated pipeline becomes evidence | High | Strict production result schema; exactly two distinct actual parallel returns; generated scope, alert, receipt, manifest/ciphertext, retention checks; fake remains fault-only. |
| Boundary exit 0 hides zero or omitted audit work | High | Machine-readable actual tracked-file/finding result; positive files, zero findings, exact marker, exit 0 all required. |
| Output capture leaks secrets or loses stream order | High | Chronological chunk capture; secret/cookie/DB URL redaction before ANSI/newline normalization and hashing; store digest/length only; scalar scan. |
| Repeated/parallel runs mix result ownership | High | Per-context recorder keyed by exact manifest/run; no module global; child/internal mode cannot finalize/write; two-recorder and interruption tests. |
| Two parent full gates race and last rename wins | High | One fixed O_EXCL writer lock acquired before suites, held through final readback; deterministic two-parent barrier test proves exactly one writer and an unmodified loser. |
| Interrupted lock is stolen from a live/PID-reused writer | High | Strict PID plus birth identity, fixed O_EXCL recovery guard, no age-only cleanup, inode+nonce owner checks, dead-owner interruption recovery test. |
| Receipt target changes during a long gate | High | Pre-suite predecessor existence/version/SHA-256 captured under lock and re-compared immediately before rename; mismatch preserves current target/audit and fails closed. |
| Failure overwrites known historical proof | High | Existing v1 read-only; one v2-only atomic writer after final checks; before-rename faults and full-run failure preserve prior receipt/audit bytes. |
| Receipt-version downgrade restores synthetic evidence | High | V1 allowed only for the exact historical migration tuple; v2 audit declares version 2 and boundary rejects any v1/missing-version replacement. |
| Audit outruns receipt or implementation | High | Implementation commits first, receipt-only commit second, audit commit third; exact hash/revision/timestamp/ancestor checks. |
| Local evidence authorizes production action | High | Fixed local/generated scope, canonical BLOCKED, no remote/network/deploy capability, independent live blockers retained. |

Production constraint: no server/cloud contact, public endpoint, network transfer, real mount, systemd install/enable/start, live alert, TLS check, deployment, rollback, unfreeze, or state transition. Only generated local Docker/Compose, loopback, temporary filesystem, and mounted-directory fixtures are permitted.
</threat_model>

<verification>

- [ ] Focused RED/GREEN tests prove canonical actual-record bytes, mixed parser strictness, omission/alteration/count rejection, recorder isolation, fixed-lock/CAS ownership, stale recovery, migration, and atomic preservation.
- [ ] Deterministic two-parent and interrupted-owner tests prove exactly one writer wins, a live owner is never displaced, stale recovery requires death/birth-identity mismatch, and loser/failure/interruption preserves receipt/audit bytes.
- [ ] Tasks 1-2 implementation is committed and clean while prior receipt/audit/verification bytes remain unchanged.
- [ ] The exact full gate runs only from that committed revision and produces a strict v2 replacement receipt after cleanup, parallel isolation, and actual canonical BLOCKED output.
- [ ] Receipt is committed alone; only a later audit commit declares and cites v2 actual-result evidence.
- [ ] Independent Phase 5 verification reruns after 05-04 and is the only authority allowed to change the gaps-found report or mark the phase complete.
- [ ] No live/server/network operation occurs and canonical production release remains BLOCKED.

</verification>

<success_criteria>

- All suite outcome/count claims in the retained receipt are derived from actual execution-time TAP, Playwright, database, pipeline, or boundary records.
- Exact canonical record bytes and redacted captured-output digests/lengths are bound and independently recomputable; missing/altered/zero/skipped/TODO/cancelled/failed/inconsistent evidence fails closed.
- Fixed pre-suite O_EXCL locking plus expected-predecessor compare-and-swap prevents competing parents, interrupted owners, or external target changes from silently replacing receipt evidence.
- Clean implementation, receipt-only evidence, later audit, and independent verification remain separately ordered and auditable.
- The single OPS-05 verification gap is ready for independent closure without changing canonical BLOCKED or claiming any live production fact.

</success_criteria>

<output>
After completion, create `.planning/phases/05-v1-0-integration-gap-closure/05-04-SUMMARY.md`. Then route to independent Phase 05 verification; do not self-author a passing `05-VERIFICATION.md` inside this plan.
</output>
