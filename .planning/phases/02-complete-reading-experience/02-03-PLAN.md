---
phase: 02-complete-reading-experience
plan: "03"
type: execute
wave: 3
depends_on: [02-01]
files_modified:
  - packages/contracts/src/public-posts.ts
  - packages/contracts/src/index.ts
  - apps/api/src/content/markdown.ts
  - apps/api/src/routes/public-posts.ts
  - apps/api/test/markdown-renderer.test.ts
  - apps/web/app/_components/ArticleToc.tsx
  - apps/web/app/_components/ArticleBody.tsx
  - apps/web/app/posts/[slug]/page.tsx
  - apps/web/app/public.module.css
  - apps/web/e2e/article-toc.spec.ts
autonomous: true
requirements: [READ-03]
must_haves:
  truths:
    - "D-06: only h2/h3 headings produce deterministic NFKC Unicode-friendly IDs; duplicates receive stable -2/-3 suffixes and punctuation-only headings use a stable fallback."
    - "D-07: desktop has an offset sticky ToC beside the reading column, mobile/tablet has keyboard-operable closed native details before body, and no qualifying headings produces no ToC or gap."
    - "D-08: ordinary server-rendered hash links and labelled heading permalinks work without JavaScript; optional current-section enhancement never changes IDs or blocks reading."
    - "Server-rendered reading HTML remains the only untrusted-HTML boundary and heading anchors retain safe scroll/focus visibility."
  backstops: []
  artifacts:
    - { path: "apps/api/src/content/markdown.ts", provides: "renderer-owned heading ID and ToC metadata generation" }
    - { path: "apps/web/app/_components/ArticleToc.tsx", provides: "responsive accessible ToC presentation" }
    - { path: "apps/web/e2e/article-toc.spec.ts", provides: "multilingual anchor and responsive browser proof" }
  prohibitions:
    - { requirement_id: READ-03, category: safety, status: unverified, flagged: true, statement: "No client Markdown/HTML parser, client-generated heading ID, or unstable random anchor algorithm." }
    - { requirement_id: READ-03, category: safety, status: unverified, flagged: true, statement: "No sanitizer widening that permits raw HTML, executable attributes, data/file URLs, or unsafe heading content." }
---

<objective>Make article headings linkable and discoverable through a server-owned ToC, preserving externally visible anchors per D-06 through D-08.</objective>

## Artifacts this phase produces

- `TocEntry` contract fields in public article detail; renderer ToC result and deterministic heading-ID helper.
- `ArticleToc` plus responsive ToC/heading-anchor CSS and `article-toc.spec.ts`.

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add safe deterministic server-side heading IDs and ToC DTO</name>
<read_first><item>02-CONTEXT.md — D-06..D-08</item><item>02-RESEARCH.md — heading algorithm and security fixtures</item><item>apps/api/src/content/markdown.ts and test/markdown-renderer.test.ts</item></read_first>
<action>Write multilingual/duplicate/punctuation/nested/hostile heading tests first. Insert a HAST pass after parse and before final sanitize that extracts only h2/h3 text, assigns deterministic Unicode-safe IDs and returns ordered `{id, depth, text}` alongside sanitized HTML. Extend strict public article detail contracts/routes. Preserve h1/h4 rendering but omit them from ToC; permit only generated safe `id` attributes and add labelled normal heading hash links.</action>
<acceptance_criteria><item>Exact IDs/order/depth are stable across repeated rendering and unique within a document.</item><item>Rendered output still rejects hostile markup/protocols and does not leak Markdown.</item><item>Article with no h2/h3 has an empty ToC DTO.</item></acceptance_criteria>
<verify>corepack pnpm exec tsx --test apps/api/test/markdown-renderer.test.ts</verify>
</task>
<task type="auto" tdd="true">
<name>Task 2: Render responsive progressive ToC without changing reading authority</name>
<read_first><item>02-UI-SPEC.md — Article/ToC, responsive and focus contracts</item><item>apps/web/app/posts/[slug]/page.tsx</item><item>apps/web/app/_components/ArticleBody.tsx</item></read_first>
<action>Create an SSR ToC component: desktop semantic nav, narrow native details, no component when entries are absent. Compose it around—not inside—the only `dangerouslySetInnerHTML` boundary. Add sticky/single-column CSS, 24px anchor scroll margin and visible focus. Use plain hash links; any IntersectionObserver enhancement is optional and isolated.</action>
<acceptance_criteria><item>At 1280 ToC is visible/sticky; at 375/768 it precedes body and is keyboard-operable.</item><item>Hash links target matching IDs without JavaScript and no empty layout remains for heading-free articles.</item></acceptance_criteria>
<verify>corepack pnpm exec playwright test apps/web/e2e/article-toc.spec.ts --workers=1</verify>
</task>
</tasks>
<threat_model>
ASVS Level 1; block_on: high.

| Threat | STRIDE | ASVS control area | Severity | Mitigation owner/task | Automated verification |
|---|---|---|---:|---|---|
| Anchor collision/link instability | Tampering | V5 validation, V8 data integrity | high | Task 1 renderer | Task 1 multilingual duplicate regression fixtures. |
| Heading-based XSS | Tampering | V5 validation | high | Task 1 HAST/sanitizer | Task 1 hostile-heading sanitizer tests. |
| Accessibility denial | Denial of service | V1 architecture | medium | Task 2 ToC UI | Task 2 keyboard/hash/focus browser check. |
</threat_model>
<verification>Renderer and one-worker article ToC browser tests pass.</verification>
<success_criteria>READ-03 gives durable, accessible headings and navigation without SSR/client drift.</success_criteria>
