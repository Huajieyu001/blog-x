---
phase: 03-distribution-and-portability
plan: "03"
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - scripts/check-boundaries.mjs
  - apps/api/test/public-distribution.test.ts
  - apps/web/e2e/phase3-distribution.spec.ts
autonomous: true
requirements: [SEO-01, SEO-02, FEED-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`--phase3-full` is the canonical distribution acceptance path and includes recursive typecheck/build, prior compatibility suites, non-skipped Phase 3 DB/browser semantics, boundaries, secret audit, and exact cleanup."
    - "The generated Web origin is the sole browser, canonical, Open Graph, robots, Sitemap, RSS, and alternate-feed origin; internal/cloud/external targets fail before acceptance."
    - "Failure/interruption cannot broaden cleanup beyond the validated generated Compose/database/media targets."
    - "Known-bad fixtures prove skip, zero-test, origin mixing, outbound request, cloud address, and cleanup-target prohibitions."
  artifacts:
    - path: scripts/local-verify.mjs
      provides: canonical Phase 3 distribution/origin acceptance orchestration
    - path: scripts/local-verify.test.mjs
      provides: deterministic skip/origin/outbound/cleanup rejection fixtures
    - path: scripts/check-boundaries.mjs
      provides: static Web ownership and cloud/external boundary checks
  key_links:
    - from: scripts/local-verify.mjs
      to: apps/web/e2e/phase3-distribution.spec.ts
      via: generated origin/credentials/run-id and one-worker managed browser execution
      pattern: "phase3-full|phase3-distribution"
    - from: scripts/local-verify.mjs
      to: apps/api/test/public-distribution.test.ts
      via: generated migrated URL plus executed-test/skip enforcement
      pattern: "PHASE3_TEST_DATABASE_URL"
    - from: scripts/check-boundaries.mjs
      to: apps/web/app
      via: Web runtime scan rejects database/API internals and cloud/server addresses
      pattern: "webRuntimeSurface"
  prohibitions:
    - statement: "Canonical acceptance must not skip semantic tests, accept zero tests, use ambient services, or pass when a generated credential/origin is missing."
      status: resolved
      verification: test
    - statement: "No browser or public URL may target INTERNAL_API_ORIGIN, either cloud server, an external origin, a public test endpoint, or a hardcoded production hostname."
      status: resolved
      verification: test
    - statement: "Cleanup must not target an unresolved variable, broad directory, unvalidated namespace, unrelated container/volume/database, or media root outside the generated temp target."
      status: resolved
      verification: test
    - statement: "No dependency, remote API/service/CDN, or server/cloud action may be added for verification."
      status: resolved
      verification: judgment
---

<objective>
Harden the scoped Phase 3 runner into the canonical distribution/origin acceptance gate.

Purpose: Make SEO/feed evidence reproducible and fail-closed against skipped semantics, topology disclosure, outbound access, and unsafe cleanup.
Output: Final distribution runner branch, deterministic safety fixtures, and boundary enforcement.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-distribution-and-portability/03-RESEARCH.md
@.planning/phases/03-distribution-and-portability/03-PATTERNS.md
@.planning/phases/03-distribution-and-portability/03-01-SUMMARY.md
@.planning/phases/03-distribution-and-portability/03-02-SUMMARY.md
@.planning/phases/02-complete-reading-experience/02-VERIFICATION.md

<interfaces>
- Extend the 03-01/03-02 runner; do not create a parallel launcher or duplicate service lifecycle.
- Phase 3 full implies all Phase 2 suites and the Phase 3 API/browser journey.
- Database direct access remains runner-owned diagnostic/setup only; browser actions remain visible UI and same-origin `/api`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace canonical acceptance through generated DB/API/Web and real Chromium</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs, apps/api/test/public-distribution.test.ts, apps/web/e2e/phase3-distribution.spec.ts</files>
  <read_first>
    <item>03-01/03-02 summaries and `scripts/local-verify.mjs` Phase 2 full branch, suite selection, journey env, log audit, and cleanup.</item>
    <item>`scripts/local-verify.test.mjs` existing target validators and Phase 3 tests' fail-closed input requirements.</item>
    <item>`02-VERIFICATION.md` semantic evidence standard and same-origin browser/database diagnostic boundary.</item>
  </read_first>
  <behavior>
    - "`--phase3-full` includes all earlier suites plus Phase 3 API and browser semantics under one generated namespace."
    - "Captured TAP/Playwright results fail on skip, zero tests, missing generated inputs, or nonzero child result."
    - "Browser/public URLs equal generated Web origin; credentials and database URL do not appear in captured logs."
    - "Cleanup and interruption preserve exact namespace/database/media target validation."
  </behavior>
  <action>
    Promote the existing Phase 3 seams to one `--phase3-full` branch that implies Phase 2 full compatibility, runs the migrated public-distribution suite, clears/seeds through runner-owned controls, and runs the one-worker metadata/discovery journey with generated origin/credentials/run ID. Parse child output so skipped or zero semantic tests fail. Assert exact origin on browser/public links and keep secrets out of logs. Preserve concurrent migration/retry, schema inspection, retained sentinel, exact volume/media validation, and finally cleanup; do not add an externally reachable diagnostic route.
  </action>
  <verify>
    <automated>corepack pnpm test:ops &amp;&amp; corepack pnpm local:verify -- --phase3-full</automated>
  </verify>
  <done>One canonical command provides non-skipped DB/browser evidence with generated topology, secret hygiene, compatibility coverage, and bounded cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add negative origin, outbound, ownership, and cleanup gates</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs, scripts/check-boundaries.mjs, apps/web/e2e/phase3-distribution.spec.ts</files>
  <read_first>
    <item>`scripts/check-boundaries.mjs#webRuntimeSurface` and existing forbidden-import/address/secret checks.</item>
    <item>`scripts/local-verify.test.mjs` namespace/media-root fixtures and Task 1 generated origin logic.</item>
    <item>AGENTS.md server freeze, same-origin, credentials, database exposure, and no-heavy-component constraints.</item>
  </read_first>
  <behavior>
    - "Known-bad internal-origin canonical, cloud-server URL, external browser request, hardcoded production hostname, and public diagnostic endpoint fixture fails."
    - "Known-bad namespace/volume/database/media cleanup target fails before destructive cleanup."
    - "Web runtime importing pg/Drizzle/API internals or containing server addresses remains rejected."
    - "A clean local-only Phase 3 full run still passes all checks."
  </behavior>
  <action>
    Extend deterministic fixtures only for concrete Phase 3 origin/outbound/skip gaps. Reject internal origin reuse in public URLs, both cloud hosts, any non-loopback browser/public request, hardcoded public hostname, and public test-only routes. Preserve and exercise exact namespace/volume/database/media-root validators before cleanup. Keep existing Web ownership rules at least as strict; do not weaken a regex to accommodate metadata/RSS helpers. Finish by running boundaries and the complete local gate.
  </action>
  <verify>
    <automated>corepack pnpm test:ops &amp;&amp; corepack pnpm check:boundaries &amp;&amp; corepack pnpm local:verify -- --phase3-full</automated>
  </verify>
  <done>Negative fixtures make origin mixing, outbound/cloud access, skipped evidence, forbidden Web ownership, and unsafe cleanup blocking failures.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high.

## Trust Boundaries

| Boundary | Sensitive flow |
|---|---|
| Runner → Docker/PostgreSQL/API/Web | Generated credentials, origins, namespaces, databases, volumes, and lifecycle commands. |
| Runner → tests/browser | Environment and child output determine whether semantic evidence is genuine. |
| Browser/public output → network | URLs may disclose internal topology or escape local verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-03-01 | Tampering | test result interpretation | high | mitigate | Task 1 rejects skip/zero-test/missing-input outcomes. |
| T-03-03-02 | Information disclosure | origins/logs | high | mitigate | Both tasks enforce generated loopback origin and secret scans. |
| T-03-03-03 | Elevation of privilege | Web ownership | high | mitigate | Task 2 retains boundary rejection of DB/API internals in Web runtime. |
| T-03-03-04 | Tampering | cleanup target | high | mitigate | Exact validators and known-bad tests execute before bounded cleanup. |
| T-03-03-05 | Denial of service | verifier lifecycle | medium | mitigate | Managed children receive bounded shutdown and generated isolated namespaces. |
</threat_model>

<verification>
Run `corepack pnpm test:ops`, `corepack pnpm check:boundaries`, and `corepack pnpm local:verify -- --phase3-full`. All negative fixtures must fail in isolation and the clean managed topology must pass with zero skipped semantic tests and exact cleanup.
</verification>

<success_criteria>
- SEO/feed acceptance is reproducible through one local generated command.
- Skip/zero-test and missing topology inputs are blocking failures.
- Browser/public URLs cannot disclose internal/cloud topology or access an external network.
- Cleanup remains exact and recoverably scoped to generated targets.
</success_criteria>

<output>
Create `.planning/phases/03-distribution-and-portability/03-03-SUMMARY.md` when complete.
</output>
