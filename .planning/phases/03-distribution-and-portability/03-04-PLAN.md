---
phase: 03-distribution-and-portability
plan: "04"
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - packages/contracts/src/distribution.ts
  - apps/api/src/content/export-repository.ts
  - apps/api/src/routes/admin-export.ts
  - apps/api/src/app.ts
  - apps/api/test/distribution-export.test.ts
  - apps/web/app/admin/page.tsx
  - apps/web/e2e/phase3-distribution.spec.ts
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
autonomous: true
requirements: [PORT-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "PORT-01: an authenticated administrator downloads fixed-name `blog-x-export-v1.json` through same-origin POST `/api/admin/export`; unauthenticated is 401 and authenticated wrong/missing Origin is 403 before repository access."
    - "The strict version-1 JSON manifest contains every retained article lifecycle state with byte-for-byte Markdown and necessary taxonomy/About/nonbinary media-reference metadata."
    - "Binary media is explicitly excluded until Phase 4; no bytes/base64/blob/archive member, storage key/path, rendered HTML authority, auth/config secret, or internal/cloud origin enters the manifest."
    - "A disposable migrated DB roundtrip strict-parses and reconstructs normalized source maps with referential integrity equal to an independent source snapshot."
    - "The final managed browser journey downloads through the generated Web origin and cannot skip for missing credentials/origin/run ID."
  artifacts:
    - path: packages/contracts/src/distribution.ts
      provides: strict version-1 portable export schemas
      exports: [portableExportManifestSchema]
    - path: apps/api/src/content/export-repository.ts
      provides: read-only snapshot of retained source authority
      exports: [createExportRepository]
    - path: apps/api/src/routes/admin-export.ts
      provides: authenticated exact-origin JSON attachment
      exports: [adminExportRoutes]
    - path: apps/api/test/distribution-export.test.ts
      provides: authorization, disclosure, reconstruction, and no-skip DB proof
    - path: apps/web/app/admin/page.tsx
      provides: visible relative same-origin POST export control
  key_links:
    - from: apps/web/app/admin/page.tsx
      to: apps/api/src/routes/admin-export.ts
      via: relative POST `/api/admin/export` with opaque cookie and browser Origin
      pattern: "/api/admin/export|post"
    - from: apps/api/src/routes/admin-export.ts
      to: apps/api/src/content/export-repository.ts
      via: session and exact-Origin authorization complete before archive selection
      pattern: "administratorIdForToken|origin|archive"
    - from: apps/api/src/content/export-repository.ts
      to: packages/contracts/src/distribution.ts
      via: explicit source mapping parsed through manifest format/version 1
      pattern: "portableExportManifestSchema|blog-x-portable-export"
    - from: scripts/local-verify.mjs
      to: apps/api/test/distribution-export.test.ts
      via: generated migrated database URL and skip/zero-test rejection
      pattern: "phase3-export-api|distribution-export"
  prohibitions:
    - statement: "Export must never occur before opaque-session authentication and exact-Origin authorization; no public, GET, query-token, or cross-origin export variant may exist."
      status: resolved
      verification: test
    - statement: "The manifest must contain no binary media, base64/blob/archive member, source/derivative key, storage/filesystem path, rendered HTML authority, password/session/admin/config secret, or internal/cloud origin."
      status: resolved
      verification: test
    - statement: "Phase 3 must create no production import/reconstruction/upload endpoint, archive extractor, ZIP/tar format, media-copy job, dependency, SDK, service, CDN, or cloud workflow."
      status: resolved
      verification: test
    - statement: "The attachment filename is constant ASCII and contains no request/content/admin/timestamp value."
      status: resolved
      verification: test
    - statement: "Export API/browser verification must not pass through missing DB/topology input, skipped/zero tests, direct browser DB/Fastify access, or an external/cloud host."
      status: resolved
      verification: test
---

<objective>
Deliver a lossless authenticated logical content export while explicitly leaving binary media backup/restore to Phase 4.

Purpose: Satisfy PORT-01 with an exact versioned format, ASVS L1 authorization, full retained-source reconstruction, and non-skippable local evidence.
Output: Strict manifest, export repository/route, visible download control, reconstruction tests, and final Phase 3 acceptance.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-distribution-and-portability/03-RESEARCH.md
@.planning/phases/03-distribution-and-portability/03-PATTERNS.md
@.planning/phases/03-distribution-and-portability/COVERAGE.md
@.planning/phases/03-distribution-and-portability/03-03-SUMMARY.md
@apps/web/AGENTS.md

<interfaces>
- Extend 03-01 `distribution.ts`; format is exactly `blog-x-portable-export`, version `1`.
- `adminPostRepository.listRetained()` excludes soft-deleted rows and is not the export authority; use a dedicated read-only repository.
- Reuse admin route order: no-store, opaque session authentication, exact Origin, then repository access.
- Use the 03-03 runner; no standalone ambient database/Web/API invocation is valid plan verification.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one retained Markdown draft through protected export on a disposable database</name>
  <files>packages/contracts/src/distribution.ts, apps/api/src/content/export-repository.ts, apps/api/src/routes/admin-export.ts, apps/api/src/app.ts, apps/api/test/distribution-export.test.ts</files>
  <read_first>
    <item>`03-PATTERNS.md` versioned export, repository snapshot, auth/origin attachment, and no-skip API sections.</item>
    <item>`apps/api/src/content/admin-repository.ts`, `page-repository.ts#archive`, `routes/admin-posts.ts`, and `db/schema.ts` as selection/transaction/guard/source analogs.</item>
    <item>`apps/api/test/pages-archive.test.ts` for session/Origin/header patterns and the 03-01/03-03 runner contract.</item>
  </read_first>
  <behavior>
    - "No cookie returns 401/no-store; valid cookie with missing/unequal Origin returns 403/no-store; repository access occurs only after both pass."
    - "Successful POST returns exact JSON content type, no-store, and `attachment; filename=\"blog-x-export-v1.json\"`."
    - "A retained Unicode/hostile Markdown draft appears byte-for-byte in a strict format/version-1 manifest."
    - "Missing DB URL, skipped suite, or zero tests fails the runner."
  </behavior>
  <action>
    Begin with failing authorization/header/manifest cases. Extend strict nested schemas for articles/taxonomy/About/nonbinary media references; all objects are strict, relations use stable UUIDs, dates are offset ISO/null, and ordering is deterministic. Create a dedicated export repository using one read-only repeatable-read transaction, explicit source columns, raw Markdown without rendering, and all lifecycle rows including soft-deleted/null-publication. Add protected POST `/admin/export`, setting no-store, authenticating first, requiring exact configured Origin second, then snapshotting/parsing. Use only the constant filename and let DB errors fail rather than return a partial archive. Register in `app.ts`. Run exclusively through the generated `--phase3-export-api` database seam and make absent configuration throw, never skip.
  </action>
  <verify>
    <automated>corepack pnpm local:verify -- --phase3-export-api</automated>
  </verify>
  <done>One retained Markdown draft travels generated PostgreSQL → protected Fastify route → strict fixed-name JSON attachment with fail-closed auth/origin/test setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the protected archive as a visible same-origin browser download</name>
  <files>apps/web/app/admin/page.tsx, apps/web/e2e/phase3-distribution.spec.ts</files>
  <read_first>
    <item>Read `apps/web/AGENTS.md`, then installed Next 16.3 page/forms documentation listed in `03-PATTERNS.md` immediately before Web edits.</item>
    <item>`apps/web/app/admin/page.tsx` and existing article action components for protected page and relative same-origin behavior.</item>
    <item>03-03 managed E2E required-input/origin contract; extend the existing journey without DB/Fastify direct browser calls.</item>
  </read_first>
  <behavior>
    - "A visible admin control POSTs only relative `/api/admin/export` and triggers a browser download with the fixed filename."
    - "The download parses through the strict manifest schema and its request origin equals generated Web origin."
    - "No token, archive bytes, internal origin, database detail, or storage detail enters URL params, browser storage, or SSR props."
  </behavior>
  <action>
    Read installed docs first. Add a visible native relative POST download control to the authenticated admin page, preserving HttpOnly cookie authority and exact browser Origin. Extend the required-input managed E2E to initiate the visible control, capture the download, assert filename/content/schema, and retain the exact-origin request listener. Do not fetch the archive through SSR, store it in Web Storage, add a token/query parameter, or call the API host directly.
  </action>
  <verify>
    <automated>corepack pnpm local:verify -- --phase3-export-browser</automated>
  </verify>
  <done>A real authenticated browser downloads the strict archive only through the generated same-origin Web entrypoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove full retained-source reconstruction and seal final Phase 3 acceptance</name>
  <files>apps/api/test/distribution-export.test.ts, apps/web/e2e/phase3-distribution.spec.ts, scripts/local-verify.mjs, scripts/local-verify.test.mjs</files>
  <read_first>
    <item>`apps/api/src/db/schema.ts` as the independent exhaustive source-field inventory and `03-PATTERNS.md` reconstruction/prohibition cases.</item>
    <item>`apps/api/test/phase2-public-visibility.test.ts` lifecycle fixtures and `pages-archive.test.ts` relation/time normalization analog.</item>
    <item>03-03 canonical `--phase3-full` suite registration, output validation, secret/outbound, and cleanup gates.</item>
  </read_first>
  <behavior>
    - "Draft/published/unpublished/soft-deleted/null-publication rows, taxonomy, About, cover intent, and safe media references reconstruct exactly with no dangling ID."
    - "Independent DB normalization deep-equals test-only reconstructed manifest maps after JSON stringify/reparse; Markdown remains byte-for-byte."
    - "Forbidden key/text/header scans reject binary/storage/rendered/auth/config/internal/cloud data and user-controlled filename content."
    - "Route enumeration proves protected POST export exists and no GET/public/import counterpart exists."
    - "Final `--phase3-full` includes export DB/browser semantics and fails on skips/zero tests."
  </behavior>
  <action>
    Expand fixtures to every retained lifecycle/nullability/relation/About/cover/media-reference case. Query seeded source independently, normalize dates/arrays by stable IDs, reconstruct downloaded manifest into maps, reject dangling references, and deep-compare; stringify/reparse and assert Markdown bytes. Exclude `exportedAt` from source equality. Scan keys/text/headers for all prohibited binary/storage/auth/config/internal/cloud values and enumerate routes to prove there is no import/public/GET variant. Add the export suite and download assertions to canonical `--phase3-full`, retaining Phase 1/2/SEO/feed suites, generated inputs, skip/zero-test rejection, secret/outbound gates, and exact cleanup.
  </action>
  <verify>
    <automated>corepack pnpm test:ops &amp;&amp; corepack pnpm check:boundaries &amp;&amp; corepack pnpm local:verify -- --phase3-full</automated>
  </verify>
  <done>PORT-01 has a usable download and deterministic reconstruction equality without binary media or a production import surface; all four Phase 3 requirements pass the final local gate.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high.

## Trust Boundaries

| Boundary | Sensitive flow |
|---|---|
| Browser → protected export | Cookie-authenticated POST and exact Origin precede sensitive selection. |
| PostgreSQL retained source → manifest | Non-public Markdown/metadata become an allowlisted archive. |
| Manifest → test-only reconstruction | Portable JSON is strict-parsed and relation-checked without a production write path. |
| Runner → DB/browser/download | Generated DB, credentials, origin, archive, and cleanup remain local/exact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04-01 | Elevation of privilege | POST `/admin/export` | high | mitigate | Task 1 tests session first, exact Origin second, repository access last, with 401/403 ordering. |
| T-03-04-02 | Information disclosure | export manifest | high | mitigate | Tasks 1/3 use explicit strict fields and scans excluding binary/storage/auth/config/internal data. |
| T-03-04-03 | Tampering | attachment headers | high | mitigate | Task 1 uses constant ASCII filename/content type and no request value in headers. |
| T-03-04-04 | Tampering | reconstruction relations | high | mitigate | Task 3 rejects dangling IDs and deep-compares independent source normalization. |
| T-03-04-05 | Elevation of privilege | import/write surface | high | mitigate | Task 3 route enumeration proves reconstruction remains test-only. |
| T-03-04-06 | Denial of service | full export | medium | mitigate | Admin-only manual snapshot uses narrow columns and excludes media bytes/rendering/compression/external I/O. |
| T-03-04-07 | Information disclosure | browser/verifier | high | mitigate | Tasks 2/3 use relative `/api`, generated origin/credentials, secret scans, and no external/cloud access. |
</threat_model>

<verification>
Run `corepack pnpm local:verify -- --phase3-export-api`, the managed export-browser seam, then final `corepack pnpm local:verify -- --phase3-full`. Passing requires non-skipped disposable-DB auth/reconstruction semantics, same-origin browser download, all SEO/feed suites, boundaries, secret/outbound checks, and exact cleanup.
</verification>

<success_criteria>
- PORT-01 is satisfied by a visible authenticated version-1 JSON download of retained Markdown and necessary metadata.
- Independent source normalization equals test-only reconstruction across all lifecycle states.
- Binary media remains explicitly excluded until Phase 4, with references only and automated prohibited-data evidence.
- No import/public/GET variant, external integration, cloud action, or skip-based pass exists.
</success_criteria>

<output>
Create `.planning/phases/03-distribution-and-portability/03-04-SUMMARY.md` when complete.
</output>
