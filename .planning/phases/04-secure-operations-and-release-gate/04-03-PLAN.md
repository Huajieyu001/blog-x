---
phase: 04-secure-operations-and-release-gate
plan: "03"
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - scripts/release-gate/schema.mjs
  - scripts/release-gate/bundle.mjs
  - scripts/release-gate/validate.mjs
  - scripts/release-gate.test.mjs
  - scripts/release-gate.mjs
  - ops/release-evidence.blocked.json
  - docs/RELEASE-GATE.md
  - docs/ROLLBACK.md
  - docs/OPERATIONS.md
  - scripts/check-boundaries.mjs
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - scripts/fixtures/prohibitions/automatic-deploy.json
  - scripts/fixtures/prohibitions/tracked-ready.json
  - README.md
autonomous: true
requirements: [OPS-05, OPS-01, OPS-02, OPS-03]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "OPS-05: the repository's actual release evidence remains `BLOCKED`; no local test, synthetic fixture, Git action, elapsed time, or successful prior phase can lift the freeze or authorize production contact/deployment."
    - "A release can become synthetically READY only when strict version-1 evidence includes explicit future user unfreeze scope/window, current separately authorized host baselines, same-origin/private-link exposure proof, complete backup and recent isolated restore, operations/TLS evidence, and an executable rollback decision record."
    - "Missing, expired, unsupported, secret-bearing, public-data-plane, browser/internal-address, incomplete backup/restore, mutable rollback artifact, or ambiguous authorization evidence fails closed with an exact nonzero result and redacted reason."
    - "The rollback runbook preserves database/media/config, names immutable prior Web/API artifacts, migration compatibility, owner, smoke checks, stop criteria, and rollback decision points without containing remote commands, host authority, credentials, or automatic execution capability."
    - "`--phase4-full` is the canonical local Phase 1-4 acceptance path: it runs all prior API/browser/database regressions, Phase 4 security/status/backup/restore/release suites, exact generated namespaces, semantic skip/zero rejection, redacted logs, known-bad fixtures, and bounded cleanup."
    - "Canonical acceptance uses Docker/Colima/loopback only and cannot claim live host firewall/link/resource/certificate/renewal state, active daily off-host backup, retention, alert delivery, or selected/measured RPO/RTO."
  artifacts:
    - path: scripts/release-gate/schema.mjs
      provides: strict versioned release-readiness evidence schema
      exports: [releaseEvidenceSchema, evidenceReferenceSchema]
    - path: scripts/release-gate/validate.mjs
      provides: local-only fail-closed evidence evaluation with injected clock
      exports: [validateReleaseEvidence, evaluateReleaseReadiness, formatReleaseDecision]
    - path: scripts/release-gate/bundle.mjs
      provides: exact-root relative artifact resolution, byte hashing, and typed artifact loading
      exports: [validateEvidenceBundleRoot, readEvidenceArtifact, hashEvidenceArtifact]
    - path: scripts/release-gate.mjs
      provides: non-deploying release decision CLI
    - path: ops/release-evidence.blocked.json
      provides: canonical real repository release state with unresolved production evidence
    - path: docs/RELEASE-GATE.md
      provides: future authorized evidence collection and stop/go workflow
    - path: docs/ROLLBACK.md
      provides: rollback prerequisites, decision points, preservation, validation, and stop criteria
    - path: scripts/local-verify.mjs
      provides: canonical local-only `--phase4-full` Phase 1-4 acceptance orchestration
  key_links:
    - from: scripts/release-gate.mjs
      to: ops/release-evidence.blocked.json
      via: strict parse plus normal/expect-blocked decision modes; no network or subprocess deployment adapter
      pattern: "expect-blocked|RELEASE BLOCKED"
    - from: scripts/release-gate/validate.mjs
      to: scripts/release-gate/bundle.mjs
      via: every READY evidence reference resolves a safe relative regular file and hashes its actual bytes before semantic readiness evaluation
      pattern: "readEvidenceArtifact|hashEvidenceArtifact"
    - from: scripts/release-gate/validate.mjs
      to: docs/ROLLBACK.md
      via: evidence references require immutable prior artifacts, edge checksum, migration/media plan, owner, stop criteria, and validation outcome
      pattern: "rollback|immutable|stopCriteria"
    - from: scripts/local-verify.mjs
      to: scripts/release-gate.test.mjs
      via: synthetic known-good/known-bad semantic tests plus canonical real BLOCKED assertion
      pattern: "phase4-full|release-gate"
    - from: scripts/local-verify.mjs
      to: apps/web/e2e/phase4-restore.spec.ts
      via: generated restored same-origin browser journey after full Phase 1-3 and Phase 4 security/operations suites
      pattern: "phase4-restore|phase4-full"
  prohibitions:
    - statement: "No artifact or command may automatically unfreeze, connect to, probe, upload to, deploy to, or modify either cloud host; the release validator has no SSH/SCP/rsync/SFTP/curl/wget/cloud-SDK/deployment capability."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/release-gate.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/automatic-deploy.json
      check_clean_fixture: ops/release-evidence.blocked.json
    - statement: "Synthetic READY evidence must never be persisted as the real repository state or described as production authorization; the canonical real state stays BLOCKED until a future explicit user unfreeze and fresh live evidence."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/release-gate.test.mjs
      check_violation_fixture: scripts/fixtures/prohibitions/tracked-ready.json
      check_clean_fixture: ops/release-evidence.blocked.json
    - statement: "A local result must not be represented as proof of production firewall/private-link/resource/TLS/renewal/off-host backup/retention/alert state or as a selected/measured RPO or RTO."
      status: resolved
      verification: judgment
---

<objective>
Seal Phase 4 behind a non-deploying, frozen-host-aware release gate and a complete local acceptance path.

Purpose: Satisfy OPS-05 and integrate OPS-01/02/03 evidence without converting local readiness into production authority.
Output: Strict release evidence/validator, canonical BLOCKED state, rollback/release runbooks, extended boundary checks, and final `--phase4-full` acceptance.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-secure-operations-and-release-gate/04-RESEARCH.md
@.planning/phases/04-secure-operations-and-release-gate/04-PATTERNS.md
@.planning/phases/04-secure-operations-and-release-gate/04-01-SUMMARY.md
@.planning/phases/04-secure-operations-and-release-gate/04-02-SUMMARY.md
@.planning/phases/03-distribution-and-portability/03-VERIFICATION.md

<interfaces>
- Consume Plan 04-01's strict security/topology/config evidence and Plan 04-02's status, complete-backup, and isolated-restore evidence; do not duplicate their authorities.
- The validator reads a validated local evidence bundle only, hashes referenced artifact bytes, uses an injected clock, emits a decision, and exits. It has no socket/network client, remote command runner, deployment mutation, or automatic unfreeze transition.
- Current evidence is deliberately incomplete and `BLOCKED`. Synthetic READY fixtures live only in generated test directories and are removed by exact cleanup.
- READY evidence references contain a safe relative artifact path plus SHA-256/timestamps/outcome; the validator resolves only regular non-symlink files inside the exact bundle root, hashes actual bytes, and strict-parses the artifact's own type/version/outcome. Live server facts remain pending until a future explicitly authorized, separately approved read-only baseline.
- Exact off-host destination, retention, alert recipient, host resource limits, firewall/private-link implementation, certificate state, RPO, RTO, owner, and change window are mandatory future decisions/evidence; do not invent values.
</interfaces>
</context>

## Artifacts this phase produces

- New files: `scripts/release-gate/schema.mjs`, `scripts/release-gate/bundle.mjs`, `scripts/release-gate/validate.mjs`, `scripts/release-gate.test.mjs`, `scripts/release-gate.mjs`, the two named prohibition fixtures, `ops/release-evidence.blocked.json`, `docs/RELEASE-GATE.md`, and `docs/ROLLBACK.md`.
- New symbols: `releaseEvidenceSchema`, `evidenceReferenceSchema`, `validateEvidenceBundleRoot`, `readEvidenceArtifact`, `hashEvidenceArtifact`, `validateReleaseEvidence`, `evaluateReleaseReadiness`, and `formatReleaseDecision`.
- New CLI flags and exact outcomes: `node scripts/release-gate.mjs --evidence=<relative-manifest> --bundle-root=<validated-local-root>` returns 0 and `RELEASE READY` only after actual artifact bytes and semantics validate, 1 and `RELEASE BLOCKED` for valid but unmet evidence, and 2 and `RELEASE INVALID` for malformed/unsafe evidence; `--expect-blocked` returns 0 only for the canonical valid BLOCKED state and does not require absent artifact references; `scripts/local-verify.mjs --phase4-full` is the final local gate.
- New schema: `blog-x-release-evidence` version 1 with strict sections `authorization`, `hostBaselines`, `networkBoundary`, `backupRestore`, `operations`, `rollback`, and `postRelease`; every READY evidence reference includes non-secret `id`, safe relative `artifact`, SHA-256, `observedAt`, `validUntil`, and outcome, and each referenced JSON artifact has an expected literal type/version.
- New generated fixture namespace/path: `${tmpdir()}/blog-x-release-evidence-<token>/`; synthetic READY/known-bad evidence and referenced artifacts are never tracked.
- Updated files: `scripts/check-boundaries.mjs`, `scripts/local-verify.mjs`, `scripts/local-verify.test.mjs`, `docs/OPERATIONS.md`, and `README.md`.
- No deployment command, remote connection/probe, server address, credential, public import/readiness route, external integration, automatic unfreeze state, or production claim is produced.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the real frozen state through a strict local release decision</name>
  <files>scripts/release-gate/schema.mjs, scripts/release-gate/bundle.mjs, scripts/release-gate/validate.mjs, scripts/release-gate.test.mjs, scripts/release-gate.mjs, ops/release-evidence.blocked.json, scripts/fixtures/prohibitions/automatic-deploy.json, scripts/fixtures/prohibitions/tracked-ready.json</files>
  <read_first>
    <item>`04-RESEARCH.md` seven release prerequisites and fail-closed strategy; `04-PATTERNS.md` schema/fixture/evidence conventions.</item>
    <item>`packages/contracts/src/distribution.ts` for strict format/version parsing and `scripts/local-verify.mjs` for injected clock/result/redaction conventions.</item>
    <item>Plan 04-01 topology/config outputs and Plan 04-02 backup manifest, restore report, status, and unresolved decision artifacts.</item>
  </read_first>
  <behavior>
    - "RED: canonical `ops/release-evidence.blocked.json` is strict-valid, normal mode exits 1 with `RELEASE BLOCKED`, and `--expect-blocked` exits 0; neither mode performs a network/subprocess mutation."
    - "RED: a generated complete synthetic bundle returns exact READY/0 only when every named reference resolves to a regular non-symlink relative artifact, actual bytes match SHA-256, the referenced artifact strict-parses to its expected type/version/outcome, and timestamps are current."
    - "RED: missing explicit user unfreeze scope/window, host baseline, secure network, backup/restore, operations/TLS, immutable rollback, or post-release decision section returns BLOCKED/1 with sorted redacted reason codes."
    - "RED: malformed/unknown fields/version, traversal/absolute/symlink/extra bundle artifact, expired/future-inconsistent timestamps, secret-like value, browser/internal/node authority, public API/Postgres, incomplete backup, mutable artifact, or actual byte hash mismatch returns INVALID/2 before readiness evaluation."
    - "RED: two parallel validations are pure/independent and interruption leaves no state transition or tracked READY file."
  </behavior>
  <action>
    Define strict nested version-1 evidence schemas and a validated evidence-bundle root. Require explicit future user authorization reference with named scope and bounded change window; separately authorized main/secondary baseline references; browser same-origin proof and API/PostgreSQL non-public plus encrypted/private-link evidence; complete backup manifest and daily-cadence/recent isolated restore report; restart/log/resource/status/TLS-renewal evidence; rollback with immutable prior Web/API digests, edge config checksum, migration compatibility, media preservation, owner and stop criteria; and post-release smoke/rollback decision record. Each READY reference names one relative JSON artifact. Resolve it beneath the exact generated/provided bundle root, reject absolute/traversal/symlink/non-regular paths and unexpected files, stream-hash actual bytes, compare SHA-256, then strict-parse the artifact's literal type/version and semantic outcome before readiness. Use caller evidence `validUntil` rather than inventing generic freshness windows, except the requirement-level daily backup assertion that the last complete backup is no older than 24 hours at evaluation.

    Implement a local pure validator with injected clock and the exact 0/1/2 outputs above. Scan parsed scalar content and referenced artifact bytes for secret/credential/internal-address authority without echoing offending values. `--expect-blocked` succeeds only when the canonical document is structurally valid, contains no READY authorization/artifact locator, and lists unresolved production-only prerequisites. Keep actual repository state BLOCKED; generate complete/invalid bundles under the exact temp prefix only. Make `release-gate.test.mjs` honor `GSD_PROHIB_SUBJECT`: fail the automatic-deploy and tracked-READY descriptors and pass the canonical BLOCKED control. Do not import networking, child-process remote tools, Docker mutation, Git mutation, or deployment code.
  </action>
  <acceptance_criteria>
    <criterion>The canonical repository evidence produces BLOCKED/1 normally and exact expect-blocked success, with every unresolved production prerequisite named and no sensitive value.</criterion>
    <criterion>Only one complete current synthetic bundle with actual matching artifact bytes/types reaches READY/0; removing or expiring each prerequisite independently produces BLOCKED/1.</criterion>
    <criterion>Every malformed/unsafe/secret/public-port/internal-address/path/symlink/extra-artifact/byte-mismatch fixture produces INVALID/2 before readiness evaluation and without echoing its value.</criterion>
    <criterion>Parallel/interrupted validation cannot persist authorization, change the canonical file, invoke remote/deployment capability, or leave generated fixtures outside the exact temp root.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/release-gate.test.mjs &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked</automated>
  </verify>
  <done>The real frozen state is machine-valid and unambiguously BLOCKED, while only fully populated synthetic future evidence demonstrates the READY branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind rollback and release runbooks to evidence, safety, and boundary checks</name>
  <files>docs/RELEASE-GATE.md, docs/ROLLBACK.md, docs/OPERATIONS.md, scripts/check-boundaries.mjs, scripts/local-verify.test.mjs, scripts/release-gate.test.mjs</files>
  <read_first>
    <item>`docs/INFRASTRUCTURE.md` only as historical context plus `04-PATTERNS.md` documentation/evidence caveats; do not convert history into current truth.</item>
    <item>`scripts/check-boundaries.mjs` operational surface and known-bad fixture pattern; extend artifact-specific coverage without weakening existing rules.</item>
    <item>Plan 04-02 operations/backup/restore runbook and evidence formats so release/rollback references have one authority.</item>
  </read_first>
  <behavior>
    - "RED: release checklist labels current state BLOCKED and orders explicit unfreeze, authorized read-only baselines, secure link/no-public-data-plane, backup/restore, operations/TLS, rollback, release, and post-release decision; any failed item is STOP."
    - "RED: rollback requires immutable prior Web/API digests, previous edge checksum, migration forward/back compatibility decision, media/database preservation, known-good backup, owner, validation checks, and stop criteria."
    - "RED: a rollback evidence fixture missing an immutable prior artifact, owner, migration/media plan, stop criteria, or verification outcome cannot reach READY."
    - "RED: newly created Phase 4 operational/release artifacts and runtime/browser output containing remote command verbs with node authority, credentials, internal/browser address, automatic-unfreeze/deploy action, public data-plane port, or false live/RPO/RTO/TLS claim fail artifact-specific boundary fixtures."
    - "RED: runbooks contain decision/evidence slots for all non-inferable values and never substitute a local/synthetic result for live production evidence."
  </behavior>
  <action>
    Write `RELEASE-GATE.md` as a role-based STOP/GO evidence workflow, not a deployment guide. Mark current state BLOCKED and require future explicit user unfreeze for a defined scope/window before even read-only host baseline steps become eligible. Separate local policy evidence, future authorized host evidence, release-time checks, and post-release decision. Include unresolved slots for off-host destination, retention, encryption-key authority, alert recipient, resource limits, firewall/link, certificate/renewal, RPO/RTO, owner, and change window. A missing/stale/failed slot always means STOP/BLOCKED.

    Write `ROLLBACK.md` with preconditions, immutable prior artifacts, edge config, database migration compatibility, media preservation, known-good backup, owner, trigger/stop criteria, validation, and decision record. Describe ordered role-level actions without hostnames, addresses, credentials, ready-to-run remote commands, broad restore targets, or automatic rollback/deploy logic. Rollback never deletes current media/database/backup authority before independent verification.

    Extend `check-boundaries.mjs` across newly created Phase 4 `ops/`, release/operations docs/scripts, and runtime/browser output using artifact-specific checks. Existing historical infrastructure documentation is explicitly grandfathered as non-live history, remains outside the new address truth claim, and cannot satisfy any release evidence slot. Add known-bad temp fixtures for automatic unfreeze/deploy/contact, credential-bearing evidence, public API/Postgres port, browser/internal/node leakage, READY canonical state, and false production/RPO/RTO/TLS wording. Each fixture must fail-first; clean Phase 4 runbooks must pass.
  </action>
  <acceptance_criteria>
    <criterion>Release and rollback docs enumerate every required evidence/decision/owner/stop condition, visibly remain BLOCKED, and contain no executable remote/deployment authority.</criterion>
    <criterion>Missing immutable rollback artifact, migration/media preservation, owner, stop criteria, or validation makes release evidence BLOCKED.</criterion>
    <criterion>All automatic-contact, secret, address, public-port, READY-state, and false-claim known-bad fixtures fail the boundary audit while clean docs/policies pass.</criterion>
    <criterion>Irreducible judgment prohibition is explicit: a reviewer must confirm no prose presents local/synthetic readiness as live production, RPO/RTO, or TLS truth.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/release-gate.test.mjs scripts/local-verify.test.mjs &amp;&amp; corepack pnpm check:boundaries</automated>
  </verify>
  <done>The future release/rollback workflow is complete and evidence-bound but cannot contact, authorize, mutate, or falsely characterize production while frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seal Phase 4 with full local regression, parallel isolation, and BLOCKED release proof</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs, scripts/check-boundaries.mjs, README.md, docs/OPERATIONS.md</files>
  <read_first>
    <item>`scripts/local-verify.mjs` Phase 3 full orchestration/result gates and Plans 04-01/04-02 focused selectors; extend one runner rather than creating another launcher.</item>
    <item>Phase 1-3 verification reports for exact prior semantic/browser/database expectations and bounded cleanup standard.</item>
    <item>All Phase 4 summaries and tests, especially generated backup/restore/release fixture prefixes and secret lists.</item>
  </read_first>
  <behavior>
    - "RED: `phase4Selection('full')` explicitly contains every Phase 4 security/status/backup/restore/release unit, database, and Playwright suite; unknown/empty/missing selections fail."
    - "RED: `--phase4-full` runs recursive typecheck/build, boundary/ops tests, full Phase 1-3 database/browser regressions, Phase 4 security, process recovery/status, complete backup/restore equality, restored browser journey, and release known-bad/canonical BLOCKED checks."
    - "RED: semantic TAP/Playwright output with skip/TODO/zero tests, missing tool/cache/input, secret in logs, outbound/cloud request, unsafe target, failed restore comparison, or canonical READY state exits nonzero."
    - "RED: build/acceptance runs with network disabled/no pull fallback after local prerequisite inspection; missing cached dependency/base/image is a failure rather than registry contact."
    - "RED: interruption and two parallel full/restore runs preserve distinct Compose/database/media/backup/restore/release-temp names and cleanup only exact generated targets."
  </behavior>
  <action>
    Add `phase4Selection('full')` and `--phase4-full` as the sole final gate. Phase 4 changes no package manifest or lockfile. Before build, inspect the exact locally required Node dependency tree, pinned base-image digests, existing API/Web verifier image/cache identifiers, and BuildKit dependency-install cache record; if any is absent or mismatched, exit nonzero with `OFFLINE PREREQUISITE MISSING` before Compose/build. Then run workspace typecheck/build with Compose build network disabled and pull policy never, operations/boundary fixtures, all prior Phase 1-3 suites/journeys, and every Phase 4 suite. This is an acceptance path for a prepared local workspace, not a claim that fresh CI can build offline. Reuse exact TAP/Playwright assertions; a skipped, TODO, zero, absent, malformed, or nonzero child is failure. Run the restored browser only through its generated loopback Web origin and keep database/API direct access runner-owned.

    Generate validated `blogxverify_...`, distinct `blogxrestore_...`, exact databases/media volumes, loopback ports, backup/restore/release temp roots, and credentials. Extend secret lists/redaction and audit combined service/tool logs without printing raw backup/export/database authority. Cleanup validates every exact target immediately before removal, avoids glob/unresolved-variable deletion, and preserves unrelated/sentinel/`blogxlocal` state. Exercise bounded interruption and two parallel runs.

    Require the final release step to execute canonical `--expect-blocked`; a tracked READY state is failure. Update README/OPERATIONS to describe Phase 4 local acceptance and distinguish it from a future separately authorized production release. No certificate probe, ACME, registry fallback, CDN, cloud/server contact, public diagnostic/import route, heavy monitoring component, or automatic release action belongs in the command.
  </action>
  <acceptance_criteria>
    <criterion>`corepack pnpm local:verify -- --phase4-full --interruption-check --parallel-check` exits 0 only after every explicit Phase 1-4 semantic suite executes with zero skip/TODO and the restored browser journey passes.</criterion>
    <criterion>Known-bad missing tool/cache/input, skip/zero, outbound, secret-log, wrong cleanup, tampered backup, failed restore, and canonical READY cases each exit nonzero.</criterion>
    <criterion>Two parallel runs and interruption preserve unique namespace/path authority and leave unrelated/`blogxlocal` state untouched after exact cleanup.</criterion>
    <criterion>Final output states local Phase 4 readiness passed and release remains BLOCKED; it contains no production health/RPO/RTO/TLS claim or remote contact.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm test:ops &amp;&amp; corepack pnpm check:boundaries &amp;&amp; corepack pnpm local:verify -- --phase4-full --interruption-check --parallel-check</automated>
  </verify>
  <done>All seven Phase 4 requirements have full local regression evidence, exact parallel/interruption safety, and a machine-checked BLOCKED production release state.</done>
</task>

</tasks>

<threat_model>
ASVS Level 1; block_on: high. Every high-severity threat below blocks final acceptance until its executable mitigation passes.

## Trust Boundaries

| Boundary | Sensitive flow |
|---|---|
| User authorization/live evidence → release document | Human authority and production facts must be referenced without secrets or fabrication. |
| Local evidence → release validator | Synthetic/local readiness must not become production authorization. |
| Rollback plan → future operator | Recovery instructions must preserve authority and avoid unsafe/automatic targets. |
| Canonical runner → Docker/browser/filesystem | Generated credentials, namespaces, backups, restored content, logs, and cleanup stay local/exact. |
| Repository artifacts → future release | No remote capability, node authority, secret, or false live claim may be committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-03-01 | Spoofing | unfreeze/live evidence | high | mitigate | Task 1 requires strict scoped user authorization and hashed current evidence references; canonical real state stays BLOCKED. |
| T-04-03-02 | Elevation of privilege | release validator | high | mitigate | Tasks 1/2 prohibit network/subprocess deploy capability and known-bad test automatic unfreeze/contact/READY persistence. |
| T-04-03-03 | Tampering | backup/restore/rollback evidence | high | mitigate | Tasks 1/2 require immutable hashes, complete restore outcome, prior artifacts, migration/media preservation, owner and stop criteria. |
| T-04-03-04 | Information disclosure | evidence/docs/logs | high | mitigate | All tasks scan/redact secrets, addresses, internal origins, credential URLs and raw payloads before output or tracking. |
| T-04-03-05 | Denial of service | full verifier/cleanup | high | mitigate | Task 3 bounds children, disables remote fallback, validates exact generated targets, and tests interruption/parallel isolation. |
| T-04-03-06 | Repudiation | false production readiness | high | mitigate | Tasks 1/3 require explicit BLOCKED output and reject missing/expired/synthetic-as-real evidence; judgment review covers prose truthfulness. |
| T-04-03-07 | Tampering | rollback target | high | mitigate | Task 2 forbids broad/active targets and destructive authority loss; rollback requires known-good backup and independent validation. |
</threat_model>

<verification>
Run release schema/fixture tests, boundary tests, then canonical `corepack pnpm local:verify -- --phase4-full --interruption-check --parallel-check`. Passing requires all Phase 1-4 semantic/API/database/browser evidence, complete backup/restore equivalence, secret-redacted output, local-only/no-fallback execution, exact cleanup, known-bad release rejection, and canonical real `RELEASE BLOCKED` proof.
</verification>

<success_criteria>
- OPS-05 cannot pass without explicit future user unfreeze and complete fresh production evidence; the repository remains BLOCKED now.
- OPS-01/02/03 security topology, process/log/status, complete backup, isolated restore, and rollback evidence are integrated into one strict release decision.
- `--phase4-full` preserves every prior phase and rejects skip/zero/missing tool/outbound/secret/tamper/unsafe cleanup/READY-state failures.
- Release/rollback artifacts have no remote/deploy capability or secret/node authority and make no invented live, retention, alert, resource, RPO/RTO, or TLS claim.
- Main-host freeze and zero production touch remain absolute until a future explicit user action changes the premise.
</success_criteria>

<output>
Create `.planning/phases/04-secure-operations-and-release-gate/04-03-SUMMARY.md` when complete.
</output>
