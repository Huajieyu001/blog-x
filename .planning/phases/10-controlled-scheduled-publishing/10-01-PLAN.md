---
phase: 10-controlled-scheduled-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/admin-posts.ts
  - packages/contracts/src/audit.ts
  - packages/contracts/src/distribution.ts
  - packages/contracts/src/tracer.test.ts
  - apps/api/src/db/schema.ts
  - apps/api/drizzle/0008_scheduled-publishing.sql
  - apps/api/src/app.ts
  - apps/api/src/audit/audit-repository.ts
  - apps/api/test/article-lifecycle.test.ts
  - apps/api/src/content/export-repository.ts
  - apps/api/test/distribution-export.test.ts
  - apps/api/test/backup-restore.test.ts
autonomous: true
requirements: [CONT-05, CONT-07, CONT-08]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A pending publication is retained as draft-only schedule metadata with UTC instant and durable administrator attribution."
    - "Strict admin, audit, and portable schemas preserve valid pending schedules while rejecting malformed, partial, or extra input."
    - "Migration, schema verification, portable export, and recovery preserve schedule state without turning old draft publication timestamps into jobs."
  artifacts:
    - path: "apps/api/drizzle/0008_scheduled-publishing.sql"
      provides: "Additive schedule columns, paired-state checks, due index, and expanded audit constraint"
    - path: "packages/contracts/src/admin-posts.ts"
      provides: "Strict schedule mutation and authenticated response contracts"
    - path: "packages/contracts/src/distribution.ts"
      provides: "Backward-readable and lossless portable schedule authority"
  key_links:
    - from: "apps/api/src/db/schema.ts"
      to: "apps/api/drizzle/0008_scheduled-publishing.sql"
      via: "matching named columns, checks, index, and audit vocabulary"
    - from: "apps/api/src/content/export-repository.ts"
      to: "packages/contracts/src/distribution.ts"
      via: "strict manifest parse with explicit nullable schedule fields"
---

<objective>
Establish the durable and strictly typed schedule authority that every later mutation and worker path consumes.

Purpose: Separate a future publishing instruction from `publishedAt`, preserve audit attribution and recovery, and make invalid partial state impossible.
Output: Migration, Drizzle schema, strict contracts, exact schema verification, and portable round-trip tests.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-controlled-scheduled-publishing/10-RESEARCH.md
@.planning/phases/10-controlled-scheduled-publishing/10-PATTERNS.md
@packages/contracts/src/admin-posts.ts
@packages/contracts/src/audit.ts
@packages/contracts/src/distribution.ts
@apps/api/src/db/schema.ts
@apps/api/src/app.ts
@apps/api/src/content/export-repository.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one pending schedule through strict admin, audit, database, and portable contracts</name>
  <files>packages/contracts/src/admin-posts.ts, packages/contracts/src/audit.ts, packages/contracts/src/distribution.ts, packages/contracts/src/tracer.test.ts, apps/api/src/db/schema.ts</files>
  <behavior>
    - A schedule request accepts exactly one offset-bearing ISO instant; missing offset, null, malformed values, and unknown keys fail strict parsing.
    - An authenticated admin post exposes nullable `scheduledAt` but never the scheduling administrator; public contract files remain unchanged.
    - Audit vocabulary has distinct scheduled, rescheduled, cancelled, and scheduled-published events with only bounded timestamp/state metadata.
    - Portable article data represents both schedule fields as a paired nullable value, reads older version-1 manifests where both are absent, and rejects one-sided or malformed pairs.
  </behavior>
  <action>Write the contract tests first. Add `scheduledAt` and internal `scheduledByAdministratorId` to the retained Drizzle article shape, with matching nullable-pair and draft/non-deleted checks plus a partial `(scheduledAt,id)` due index. Keep the public lifecycle enum unchanged, expose only `scheduledAt` in `adminPostSchema`, and add strict schedule input plus typed conflict responses. Extend the audit event/metadata allowlists without title, summary, Markdown, slug, media, credentials, or environment data. Extend portable version 1 read compatibility with optional paired schedule fields while requiring new exports to emit both explicitly. Do not reinterpret or migrate any existing `publishedAt` value into schedule authority.</action>
  <verify>
    <automated>corepack pnpm --filter @blog-x/contracts test &amp;&amp; corepack pnpm --filter @blog-x/contracts typecheck &amp;&amp; corepack pnpm --filter @blog-x/api typecheck</automated>
  </verify>
  <done>Strict schemas distinguish publication history from pending UTC schedule state, reject every partial/malformed shape, and compile across API and Web consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate retry-safe database invariants and exact audit storage</name>
  <files>apps/api/drizzle/0008_scheduled-publishing.sql, apps/api/src/app.ts, apps/api/src/audit/audit-repository.ts, apps/api/test/article-lifecycle.test.ts</files>
  <behavior>
    - Fresh and repeated migration runs add the same columns, named checks, due index, and audit-event constraint without scheduling legacy rows.
    - The database rejects one-sided schedule fields and schedules on non-draft or deleted rows.
    - Every schedule event maps to article target type and accepts only the strict content-free metadata contract.
    - Exact schema verification detects a missing schedule column, index, check, audit event, or ninth migration.
  </behavior>
  <action>Write PostgreSQL assertions first in the existing lifecycle integration owner. Add migration `0008_scheduled-publishing.sql` with nullable `scheduled_at` and `scheduled_by_administrator_id`, paired-state and active-draft checks, deterministic partial due index, and the four schedule audit events. Leave existing rows' `published_at` bytes untouched and initialize no live jobs. Update `schemaVerify()` exact migration/schema inventory and `expectedTarget` in the audit repository as one atomic slice. Assert invalid database states use constraint failures and create no audit row.</action>
  <verify>
    <automated>corepack pnpm -r typecheck &amp;&amp; corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check</automated>
  </verify>
  <done>Migration retries converge without data loss, schema verification recognizes every new invariant, and the database cannot retain an unattributed or publicly-state schedule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve pending schedules through portable export and recovery</name>
  <files>apps/api/src/content/export-repository.ts, apps/api/test/distribution-export.test.ts, apps/api/test/backup-restore.test.ts</files>
  <behavior>
    - A new portable export emits explicit UTC `scheduledAt` and matching administrator UUID for active schedules, and explicit nulls otherwise.
    - The independent source-map comparison fails if either schedule field is omitted, changed, or paired incorrectly.
    - Backup/restore equality retains the pending deadline and actor while public-distribution objects contain neither field.
  </behavior>
  <action>Extend the existing export selection and ISO serialization only with the two retained schedule fields. Update the independent portable fixtures first, including unscheduled, scheduled, legacy-manifest, null, and malformed-pair cases, then update the repository until strict reparsing and source-map equality pass. Extend recovery evidence to prove a pending schedule survives restore with identical UTC milliseconds and attribution. Do not add administrator secrets, sessions, audit details, or schedule data to public distribution DTOs.</action>
  <verify>
    <automated>corepack pnpm -r typecheck &amp;&amp; corepack pnpm local:verify -- --phase3-export-api</automated>
  </verify>
  <done>New exports are lossless for pending jobs, old version-1 exports remain readable, malformed schedule authority fails closed, and public DTOs remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Contract to database | Untrusted time/state input becomes durable schedule authority. |
| Database to portable archive | Private attribution crosses into a recoverable protected artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | Tampering | Schedule columns/contracts | high | mitigate | Strict offset datetime schemas plus paired-state and draft-only PostgreSQL checks in Task 1/2. |
| T-10-02 | Repudiation | Schedule audit vocabulary | medium | mitigate | Durable actor UUID and four exact article events updated across contract, DB check, and target map in Task 1/2. |
| T-10-03 | Information disclosure | Portable/public projections | medium | mitigate | Protected export carries only required schedule authority while public DTOs remain schedule-free, proven in Task 3. |
</threat_model>

<verification>
Run contract tests and workspace typecheck after each task; run the existing generated migration/concurrency integration selection after database changes and export selection after portability changes. No human UAT is required for these machine-verifiable contracts.
</verification>

<success_criteria>
- Schedule metadata is separate from first-publication history and valid only as an attributed, undeleted draft pair.
- Migration and schema verification are retry-safe and exact.
- Audit and portable schemas are strict, content-free, and recovery-complete.
</success_criteria>

<output>
Create `.planning/phases/10-controlled-scheduled-publishing/10-01-SUMMARY.md` when done.
</output>
