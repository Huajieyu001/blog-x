---
phase: 10-controlled-scheduled-publishing
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - apps/api/src/content/admin-repository.ts
  - apps/api/src/content/scheduled-publisher.ts
  - apps/api/src/app.ts
  - apps/api/src/security/config.ts
  - apps/api/package.json
  - apps/api/test/article-lifecycle.test.ts
  - apps/api/src/content/public-repository.ts
  - apps/api/test/phase2-public-visibility.test.ts
  - apps/api/test/public-discovery.test.ts
  - apps/api/test/public-distribution.test.ts
  - apps/web/e2e/phase3-distribution.spec.ts
autonomous: true
requirements: [CONT-06, CONT-07, CONT-08]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One bounded local command publishes only due drafts in stable `(scheduledAt,id)` order and returns observable secret-free results."
    - "The entire selected batch is one PostgreSQL transaction: any candidate or audit failure rolls every selected article back and returns nonzero."
    - "Concurrent/retried workers publish and audit each article once, preserve slug and first-public time, and lose safely to cancel, reschedule, manual publish, or delete."
    - "Scheduled/not-due and future-publication rows are absent from every public surface until a valid due transaction commits."
  artifacts:
    - path: "apps/api/src/content/scheduled-publisher.ts"
      provides: "Bounded all-or-nothing PostgreSQL due publisher and strict result grammar"
    - path: "apps/api/src/content/admin-repository.ts"
      provides: "Ordered FOR UPDATE SKIP LOCKED batch transaction"
    - path: "apps/api/src/content/public-repository.ts"
      provides: "Database-time public visibility predicate"
  key_links:
    - from: "apps/api/src/app.ts"
      to: "apps/api/src/content/scheduled-publisher.ts"
      via: "DB-only publish-due command that always closes resources"
    - from: "apps/api/src/content/scheduled-publisher.ts"
      to: "apps/api/src/content/admin-repository.ts"
      via: "single ordered transaction retaining row locks through validation, update, and audit"
    - from: "apps/api/src/content/public-repository.ts"
      to: "all public API/Web surfaces"
      via: "shared published, undeleted, non-null and database-time predicate"
---

<objective>
Ship the bounded one-shot due publisher and prove scheduled content cannot leak or publish twice.

Purpose: Convert due intent into public state under one PostgreSQL authority while preserving content privacy, recovery semantics, and deterministic operator evidence.
Output: Local CLI, transactional publisher, concurrency/failure suite, strengthened public predicate, and revision-bound local-delivery gate.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-controlled-scheduled-publishing/10-RESEARCH.md
@.planning/phases/10-controlled-scheduled-publishing/10-PATTERNS.md
@.planning/phases/10-controlled-scheduled-publishing/10-02-SUMMARY.md
@apps/api/src/content/admin-repository.ts
@apps/api/src/content/article-service.ts
@apps/api/src/app.ts
@apps/api/src/content/public-repository.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish one exactly-due draft through the DB-only command</name>
  <files>apps/api/src/content/admin-repository.ts, apps/api/src/content/scheduled-publisher.ts, apps/api/src/app.ts, apps/api/src/security/config.ts, apps/api/test/article-lifecycle.test.ts</files>
  <behavior>
    - An empty due set returns exit zero with claimed/published zero and no mutation or audit.
    - A draft with `scheduledAt` exactly equal to the transaction timestamp is due; a later instant is excluded.
    - One due draft is row-locked, revalidated with the shared publication policy, published at the transaction timestamp, cleared of schedule metadata, audited once under its durable actor, and returned by ID only.
    - The DB-only command starts no Fastify listener and requires no public origin, media root, administrator password, cookie, or HTTP secret.
    - Success stdout and typed failure stderr contain only format/version, command, UTC timestamp, bounded counts/limit, and bounded IDs; no title, summary, Markdown, slug, audit metadata, database URL, credentials, cookie, or environment value appears.
  </behavior>
  <action>Write zero, singleton, exact-equality, future, malformed-row, and output-redaction integration cases first. Add `publish-due` to the DB-only config/dispatch union with a fixed grammar and resource cleanup in `finally`. Implement the orchestrator-locked algorithm: capture one PostgreSQL transaction timestamp; in that same transaction select at most the validated limit of eligible undeleted drafts ordered by `scheduled_at ASC, id ASC` using `FOR UPDATE SKIP LOCKED`; validate every candidate through Plan 02's shared publication policy before committing any visible result; update status, actual `publishedAt`, schedule clear, monotonic version, and one attributed audit per row. Preserve slug exactly. If any selected candidate, update, or audit fails, throw so PostgreSQL rolls back the entire batch and the command emits a typed nonzero result.</action>
  <verify>
    <automated>corepack pnpm -r typecheck &amp;&amp; corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check</automated>
  </verify>
  <done>The DB-only command proves empty and singleton paths, exact UTC eligibility, one complete row/audit transition, bounded redacted output, and whole-batch rollback semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal bounds, deterministic ordering, retries, concurrency, and lifecycle races</name>
  <files>apps/api/src/content/admin-repository.ts, apps/api/src/content/scheduled-publisher.ts, apps/api/package.json, apps/api/test/article-lifecycle.test.ts</files>
  <behavior>
    - No option uses default 25; exactly one `--limit=N` accepts integers 1 through 100, while empty, zero, negative, fractional, over-maximum, duplicate, or unknown arguments fail before database access.
    - More than the limit leaves a deterministic remainder; tied deadlines publish by `(scheduledAt,id)` and successive runs continue in that order.
    - Two simultaneous invocations own disjoint locks and converge to one publication and one audit per ID; immediate retry returns an empty success without changing slug or `publishedAt`.
    - Cancel, reschedule, manual publish, and delete sharing the row lock either win first or observe the committed outcome; no stale candidate can later publish or duplicate an audit.
    - Missing actor, invalid content/media, and injected failure on any candidate roll back every candidate in the selected batch and return nonzero without secret/content logging.
  </behavior>
  <action>Add the package script and write real-PostgreSQL barrier tests before expanding the implementation. Exercise exactly 1, 25, 100, and 101-row boundaries; tied deadlines; two concurrently started publishers; immediate retries; and deterministic cancel/reschedule/manual-publish/delete races without sleeps. Inject failures at validation, update, and audit seams and assert the entire selected batch retains its pre-run status, timestamps, schedules, slugs, and audit counts. Keep one transaction for the ordered batch, retain locks until commit/rollback, and never add a queue, Redis, timer, daemon, HTTP worker endpoint, caller-selected SQL, article IDs, or URL.</action>
  <verify>
    <automated>corepack pnpm -r typecheck &amp;&amp; corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check</automated>
  </verify>
  <done>Bounds and ordering are deterministic, retries and concurrent runs converge, every lifecycle race is safe, and all candidate failures are observable whole-batch rollbacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove non-disclosure across every public surface and hand off verified source</name>
  <files>apps/api/src/content/public-repository.ts, apps/api/test/phase2-public-visibility.test.ts, apps/api/test/public-discovery.test.ts, apps/api/test/public-distribution.test.ts, apps/web/e2e/phase3-distribution.spec.ts</files>
  <behavior>
    - A scheduled/not-due draft with unique title, summary, Markdown, slug, category, and tag sentinels is absent from home/list, search, category, tag, archive, detail, related, distribution, RSS, and Sitemap.
    - A malformed `published` row with future `publishedAt` is excluded by the shared predicate using PostgreSQL time.
    - After a valid due transaction commits, the article appears through normal published projections with stable slug/first-public time and no `scheduledAt` or actor key in any public DTO or generated document.
    - Direct access before due is byte-equivalent to unknown content and all browser requests remain same-origin.
    - The source scope passes its complete automated checks and is handed to the phase orchestrator without the plan executor invoking formal local delivery.
  </behavior>
  <action>Seed unique cross-field sentinels in the existing public integration owners before strengthening `publicPredicate` with `publishedAt &lt;= CURRENT_TIMESTAMP`. Prove all shared consumers rather than adding route-local schedule checks; keep every public contract and selection free of both schedule fields. Extend the existing generated RSS/Sitemap browser owner for pre-due absence and post-commit presence. Run source tests, typecheck, build, boundary checks, and the generated integration selection, then commit and push the completed source/test task. Stop at the explicit executor boundary: the plan executor must not run `local:deliver`, create a delivery receipt, or claim independent review. Record any purely subjective UAT separately without pausing machine-verifiable work.</action>
  <verify>
    <automated>corepack pnpm test &amp;&amp; corepack pnpm -r typecheck &amp;&amp; corepack pnpm -r build &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check</automated>
  </verify>
  <done>Every pre-due and future-publication sentinel is absent from every public surface, normal visibility begins only after commit, all source checks pass, and the pushed task scope is ready for independent review without formal delivery having run.</done>
</task>

</tasks>

<orchestrator_gate>
## Post-implementation reviewed local-delivery gate

This gate belongs to the phase orchestrator after all three plan executors return. It is not executed by any plan executor.

1. Confirm every source/test task commit from Plans 10-01 through 10-03 is complete on `dev`, pushed to `origin/dev`, and the worktree is clean.
2. Run an independent standard-depth GSD code review against the exact Phase 10 source/test diff; the implementation agents must not self-review.
3. Fix every review finding, rerun the affected focused tests plus the complete automated source gate, and repeat independent review until no open finding remains. Commit and push the final review artifact and every fix/retest artifact.
4. After the final review artifact is committed and pushed, freeze that resulting clean `dev` HEAD as the delivery SHA. Confirm local `dev` and `origin/dev` equal it, and confirm the committed review artifact names the exact Phase 10 source/test scope it approved; make no further file change before delivery.
5. From that frozen SHA invoke the zero-option formal `corepack pnpm local:deliver` command exactly once. If the attempt fails, preserve its claim/failure record and never retry that SHA; fix the cause in a new commit, repeat independent review, freeze the new clean SHA, and make one attempt for the new SHA.
6. On success verify the fixed `http://127.0.0.1:3100` runtime serves the frozen revision, the immutable receipt is bound to the same revision, and the production decision is exactly `BLOCKED`. Commit and push only the resulting receipt/evidence without modifying source behavior.
</orchestrator_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local CLI to PostgreSQL | Operator arguments trigger bounded multi-row state transitions. |
| Concurrent writers to article row | Worker and administrator lifecycle operations contend on publication authority. |
| PostgreSQL to public projections | Retained private content may become externally visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-09 | Tampering | Due batch transaction | high | mitigate | Stable order, `FOR UPDATE SKIP LOCKED`, all-candidate validation, and one transaction with full rollback in Tasks 1/2. |
| T-10-10 | Denial of service | CLI batch input | medium | mitigate | Fixed grammar, default 25, maximum 100, and reject-before-connect tests in Task 2. |
| T-10-11 | Information disclosure | Public queries | high | mitigate | One database-time predicate plus sentinel matrix across every public consumer in Task 3. |
| T-10-12 | Information disclosure | CLI/audit output | medium | mitigate | IDs/counts only, bounded schemas, and hostile secret/content redaction assertions in Tasks 1/2. |
| T-10-13 | Elevation of privilege | Worker activation | low | accept | Only a local DB-only command is delivered; no HTTP endpoint or production scheduler/timer is introduced. |
</threat_model>

<verification>
Plan executors run the canonical generated integration selection for database concurrency and browser behavior, but never run `local:deliver`. The phase orchestrator owns independent review, finding closure, clean-SHA freeze, the single formal delivery attempt for that SHA, runtime/receipt verification, and receipt commit/push. Do not retry a failed SHA, connect to any server, or activate a scheduler.
</verification>

<success_criteria>
- Due selection is bounded, ordered, database-time-based, concurrency-safe, retry-idempotent, and all-or-nothing for the selected batch.
- Invalid input/state/content/audit failures return nonzero and leave every selected row unchanged.
- Scheduled and future-publication content is absent from every public path until commit, with no private fields in public DTOs.
- Formal reviewed local delivery succeeds without production or server changes.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe administrator scheduling with no early disclosure and controlled due publication | 01-03 | COVERED | Durable authority, admin lifecycle, worker, public proof, and delivery gate. |
| REQ | CONT-05 | Schedule, inspect, reschedule, cancel | 01-02 | COVERED | Strict persistence plus authenticated responsive controls. |
| REQ | CONT-06 | Hidden from every public surface until due | 03 | COVERED | Shared predicate and full sentinel matrix. |
| REQ | CONT-07 | Bounded idempotent concurrent due publishing | 01,03 | COVERED | Durable schedule plus locked ordered batch/retry proof. |
| REQ | CONT-08 | Authentication, audit, invalid/partial failure closure | 01-03 | COVERED | Strict audit, guarded mutations, nonzero whole-batch rollback. |
| RESEARCH | — | Draft metadata, UTC/database time, durable actor, portable recovery | 01-02 | COVERED | No `publishedAt` overload or public field expansion. |
| RESEARCH | — | One bounded transaction using ordered `FOR UPDATE SKIP LOCKED` | 03 | COVERED | Orchestrator-selected all-or-nothing policy, including empty/tied/race cases. |
| RESEARCH | — | No queue, daemon, timer activation, HTTP worker route, package install, or server operation | 01-03 | COVERED | Explicit scope guards in tasks and acceptance. |
| CONTEXT | — | No phase CONTEXT.md decisions exist | — | COVERED | Research, patterns, requirements, and orchestrator batch decision are fully represented. |

<output>
Create `.planning/phases/10-controlled-scheduled-publishing/10-03-SUMMARY.md` when done.
</output>
