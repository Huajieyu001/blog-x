---
phase: 01-local-publishing-slice
plan: "02"
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - tsconfig.base.json
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/web/package.json
  - apps/web/tsconfig.json
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/src/index.ts
  - packages/contracts/src/tracer.ts
autonomous: true
requirements: [OPS-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The proven tracer is expressed as deployment-separable pnpm packages without changing its browser/API/database path."
    - "Shared wire DTOs live in a contracts-only package; it imports neither service implementation nor database types."
    - "Root commands build/typecheck/test both applications deterministically from the pinned lockfile."
  artifacts:
    - path: pnpm-workspace.yaml
      provides: "Explicit apps/web, apps/api, packages/contracts workspace"
    - path: packages/contracts/src/tracer.ts
      provides: "Initial request/response contracts extracted from the proven tracer"
  key_links:
    - from: apps/web/package.json
      to: packages/contracts/package.json
      via: "workspace dependency on DTOs only"
      pattern: "workspace:"
    - from: apps/api/package.json
      to: packages/contracts/package.json
      via: "workspace dependency on validation/wire schemas"
      pattern: "workspace:"
  assumptions:
    - "Package extraction is reversible and must preserve every 01-01 acceptance test unchanged."
  prohibitions: []
---

<objective>
Extract the proven tracer into the final pnpm workspace/package boundary before feature expansion.

Purpose: Make the separate web/API deployables and shared DTO seam explicit while the end-to-end path is still small.
Output: Package manifests, TypeScript configs, shared tracer contracts, workspace scripts, and unchanged tracer proof.
</objective>

## Phase Goal

**As a** developer, **I want to** run the web and API as explicit workspace applications, **so that** local validation matches their future separate deployment responsibilities.

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-publishing-slice/01-CONTEXT.md
@.planning/phases/01-local-publishing-slice/01-RESEARCH.md
@.planning/phases/01-local-publishing-slice/SKELETON.md
@.planning/phases/01-local-publishing-slice/01-01-SUMMARY.md
@AGENTS.md
@docs/INFRASTRUCTURE.md
</context>

## Artifacts Produced

- Twelve-file workspace/package extraction with shared tracer DTOs and deterministic root commands.

<tasks>

<task type="auto">
  <name>Task 1: Establish package manifests and deployment-separable TypeScript builds</name>
  <files>package.json, pnpm-workspace.yaml, pnpm-lock.yaml, tsconfig.base.json, apps/api/package.json, apps/api/tsconfig.json, apps/web/package.json, apps/web/tsconfig.json, packages/contracts/package.json, packages/contracts/tsconfig.json</files>
  <read_first>
    - REQUIREMENTS.md — deployment boundary and low-resource constraints
    - .planning/phases/01-local-publishing-slice/SKELETON.md — final directory/ownership contract
    - .planning/phases/01-local-publishing-slice/01-01-SUMMARY.md — actual tracer scripts and package versions
    - package.json — approved root dependency set and current commands
  </read_first>
  <action>Create the pnpm workspace and per-package manifests without installing anything outside the already approved set. Assign Next/React to `apps/web`, Fastify/Drizzle/renderer/auth to `apps/api`, Zod to contracts, and test/build tooling at the narrowest shared scope. Add strict TypeScript configs and root orchestration scripts using pnpm filters. Preserve two independent process entry points and the existing same-origin rewrite; do not move Fastify behavior into Next or let the web package acquire `pg`/Drizzle. Refresh the lockfile deterministically and keep the 01-01 tracer green after extraction.</action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false && pnpm -r typecheck && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - Workspace installation uses only Task 01-01-approved packages and produces a consistent lockfile.
    - Both apps build independently; contracts has no runtime service/database dependency.
    - The tracer browser path still passes after package extraction.
  </acceptance_criteria>
  <done>The repository has explicit, independently buildable web/API packages and a contracts-only package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the tracer wire schemas without changing behavior</name>
  <files>packages/contracts/src/index.ts, packages/contracts/src/tracer.ts, package.json, pnpm-lock.yaml</files>
  <read_first>
    - REQUIREMENTS.md — CMS-001..CMS-004 and public read acceptance shapes
    - .planning/phases/01-local-publishing-slice/01-RESEARCH.md — Zod shared-contract recommendation and responsibility map
    - apps/api/src/app.ts — actual tracer request/response shapes
    - apps/web/app/TracerAdmin.tsx — actual browser consumer
    - apps/web/app/[[...path]]/page.tsx — actual SSR consumer
  </read_first>
  <behavior>
    - "Login, publish, public-list, and public-detail schemas reject malformed input and preserve the tracer's successful responses."
    - "Contracts expose wire-safe primitives and never Drizzle rows, password/session digests, or internal errors."
  </behavior>
  <action>Write contract tests first using Zod parsing cases, then extract the tracer login/publish/public DTOs into `packages/contracts/src/tracer.ts` and export them from the package index. Update package scripts/exports so both apps consume compiled/source contracts consistently. Keep persistence entities and rendered implementation details inside `apps/api`; only explicit public/admin wire fields cross the boundary.</action>
  <verify>
    <automated>pnpm --filter @blog-x/contracts test && pnpm exec playwright test apps/web/e2e/walking-skeleton.spec.ts --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - Malformed login/publish inputs fail contract tests with stable field errors.
    - Both apps use the shared tracer schemas without exposing database/session internals.
    - The real browser tracer remains green.
  </acceptance_criteria>
  <done>The proven wire contracts are explicit and ready for focused auth/content expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| contracts → web/API | Accidental exposure or trust of internal fields |
| workspace scripts → packages | Dependency/command drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Information disclosure | shared DTOs | high | mitigate | Explicit allowlisted wire schemas and tests excluding password/session/DB fields. |
| T-02-02 | Tampering | package boundary | medium | mitigate | Independent builds and tracer regression test enforce the established topology. |
| T-02-SC | Tampering | dependencies | high | mitigate | No new dependency; lockfile remains within the single approved package set. |
</threat_model>

## Source Audit

Full set-wide audit: `SKELETON.md#canonical-source-audit`.

| Source | IDs owned here | Status |
|---|---|---|
| REQ | OPS-04 package/run isolation | COVERED |
| RESEARCH | R-ARCH and shared-contract stack | COVERED |
| CONTEXT | D-05/D-11 contracts preserved from tracer | COVERED |

<verification>Independent package builds and the unchanged tracer both pass.</verification>
<success_criteria>The workspace makes deployment ownership explicit without architectural drift.</success_criteria>
<output>Create `.planning/phases/01-local-publishing-slice/01-02-SUMMARY.md`.</output>
