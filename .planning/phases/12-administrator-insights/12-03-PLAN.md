---
phase: 12-administrator-insights
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-01"
  - "12-02"
files_modified:
  - scripts/test-inventory.mjs
  - scripts/test-inventory.test.mjs
  - scripts/default-test.mjs
  - scripts/default-test.test.mjs
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
autonomous: true
requirements:
  - STAT-05
  - ADMN-02
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every Phase 12 contract, API/database, Web helper, and browser test has exactly one immutable owner and runs in its intended default or generated integration child."
    - "A sealed zero-option `--phase12-data` gate proves protected no-store analytics, D-01 current-public filtering/reappearance, SSR UI, privacy states, and responsive behavior in generated local authority with zero failures, skips, or TODOs."
    - "Canonical integration incorporates the two new integration suites exactly once and still proves cleanup, interruption, parallel isolation, current runtime digest, and releaseState BLOCKED."
    - "Standard-depth deep review covers the exact Phase 12 implementation diff; any review fix invalidates prior evidence and triggers the same focused, canonical, and fixed-delivery reruns."
    - "The final local-only handoff refreshes current dev source to `http://127.0.0.1:3100`, verifies health/root/admin behavior, and never becomes production authorization."
  artifacts:
    - path: scripts/test-inventory.mjs
      provides: "Exact 45-file package test inventory with Phase 12 ownership"
    - path: scripts/default-test.mjs
      provides: "Exact default children for contract and Web helper tests"
    - path: scripts/local-verify.mjs
      provides: "Sealed Phase 12 data selector and expanded canonical integration authority"
      exports:
        - phase12Selection
        - createPhase12DataResult
    - path: scripts/local-verify.test.mjs
      provides: "Fail-closed inventory, selection, runtime, cleanup, argument, and BLOCKED-result tests"
  key_links:
    - from: scripts/test-inventory.mjs
      to: scripts/default-test.mjs
      via: "DEFAULT_TEST_FILES must equal flattened sealed child arguments in exact order"
      pattern: "DEFAULT_TEST_FILES"
    - from: scripts/test-inventory.mjs
      to: scripts/local-verify.mjs
      via: "canonical and Phase 12 selection derive exact paths/owners from the immutable manifest"
      pattern: "PACKAGE_TEST_INVENTORY"
    - from: scripts/local-verify.mjs
      to: apps/api/test/admin-analytics.test.ts
      via: "generated disposable PostgreSQL provides ADMIN_ANALYTICS_TEST_DATABASE_URL"
      pattern: "ADMIN_ANALYTICS_TEST_DATABASE_URL"
    - from: scripts/local-verify.mjs
      to: apps/web/e2e/admin-analytics.spec.ts
      via: "current generated Web runtime and authenticated main-browser fixture execute the exact new spec"
      pattern: "admin-analytics\.spec\.ts"
---

<objective>
Seal Phase 12 into the repository's exact test authority, run complete local evidence, provide the deep-review rerun contract, and refresh the fixed 3100 display while production remains blocked.

Purpose: Prevent the new analytics behavior from existing only in ad-hoc tests or an unreviewed local build, and make both coverage and local cleanup machine-checkable.
Output: Updated immutable test inventory/default children, a dedicated Phase 12 verifier, expanded canonical integration, clean review handoff, and current fixed local display evidence.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-administrator-insights/12-RESEARCH.md
@.planning/phases/12-administrator-insights/12-UI-SPEC.md
@.planning/phases/12-administrator-insights/12-01-SUMMARY.md
@.planning/phases/12-administrator-insights/12-02-SUMMARY.md
@.planning/phases/11-privacy-safe-view-authority/11-VERIFICATION.md
@scripts/test-inventory.mjs
@scripts/test-inventory.test.mjs
@scripts/default-test.mjs
@scripts/default-test.test.mjs
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/local-delivery-acceptance.mjs
@scripts/refresh-local.mjs

<interfaces>
Current inventory baseline before Phase 12: 41 total = 11 default + 30 integration; integration owners include database 11, main-browser 14, and one each for backup-restore, media, error-browser, restore-browser, and phase7-browser.

Phase 12 adds exactly four package tests: default `packages/contracts/src/analytics.test.ts`; integration/database `apps/api/test/admin-analytics.test.ts`; default `apps/web/app/lib/admin-analytics.test.ts`; integration/main-browser `apps/web/e2e/admin-analytics.spec.ts`.

Therefore the new exact inventory is 45 total = 13 default + 32 integration. Default children own contracts 3, API 3, Web 7. Integration owner counts are database 12, main-browser 15, and the existing single-owner groups unchanged. Canonical integration excludes only phase7-browser, so it owns 31 paths: 14 API tests and 17 Web E2E specs.

Existing sealed command: `corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check` accepts no path, namespace, port, build-skip, or internal-run override.
</interfaces>

<decisions>
- Add the dedicated sealed `--phase12-data` receipt recommended by research A6 because it can select the exact four Phase 12 feature suites plus verifier self-tests without duplicating Phase 11 privacy-ingress machinery.
- D-01 is the primary lifecycle oracle in both focused and canonical runs: hide current non-public rows from every output, preserve storage, and prove the same article's aggregate reappears after republish.
- All verification, review, and display refresh are local-only. No server, credential, production topology, deployment adapter, or `main` branch operation is permitted; every result remains `BLOCKED`.
</decisions>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Give all four Phase 12 suites exact fail-closed ownership</name>
  <files>scripts/test-inventory.mjs, scripts/test-inventory.test.mjs, scripts/default-test.mjs, scripts/default-test.test.mjs</files>
  <behavior>
    - Test 1: manifest is frozen and exact at 45 total, 13 default, and 32 integration with no missing, duplicate, unowned, or swapped path.
    - Test 2: owner counts are database 12, main-browser 15, and each existing specialized owner remains exactly one.
    - Test 3: default child order exactly flattens to DEFAULT_TEST_FILES with contracts 3, API 3, Web 7, and no caller-selected path/environment authority.
    - Test 4: omission, duplication, scope swap, kind drift, or owner drift for any Phase 12 test fails with the exact affected path.
  </behavior>
  <action>Start RED by updating inventory/default coordinator tests with the exact baseline and four new paths described in `<interfaces>`. Then register `packages/contracts/src/analytics.test.ts` as `contracts-unit/default/null`, `apps/api/test/admin-analytics.test.ts` as `api-unit/integration/database`, `apps/web/app/lib/admin-analytics.test.ts` as `web-unit/default/null`, and `apps/web/e2e/admin-analytics.spec.ts` as `web-e2e/integration/main-browser`. Update the sealed default child arrays in manifest order and the exact totals/owner assertions; do not create a second ad-hoc runner, wildcard, fallback selection, or environment override. Extend synthetic fail-closed coverage to all four paths and require pass-only nonzero TAP arithmetic with zero fail/cancel/skip/TODO. Keep release output exactly BLOCKED and keep all existing test ownership unchanged.</action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/test-inventory.test.mjs scripts/default-test.test.mjs &amp;&amp; corepack pnpm test</automated>
  </verify>
  <done>All 45 package tests are owned exactly once, both new default tests run in sealed children, inventory/default drift fails closed, and the default result remains nonzero pass-only with releaseState BLOCKED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the sealed Phase 12 data gate and expand canonical integration</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs</files>
  <behavior>
    - Test 1: `phase12Selection("data")` returns exactly the contract, API/database, Web helper, browser, and verifier suites with their canonical inventory owners.
    - Test 2: Phase 12 result accepts only exact ordered pass-only suite records plus a current Web runtime authority digest, generated cleanup acknowledgement, and literal releaseState BLOCKED.
    - Test 3: missing/extra/duplicate suite, wrong kind/owner, zero/skipped/TODO count, malformed runtime authority, missing cleanup, or caller option makes the gate fail.
    - Test 4: canonical integration owns exactly 31 non-Phase-7 paths, maps the new database environment, schedules the new main-browser spec once, and preserves interruption/parallel cleanup proof.
  </behavior>
  <action>Start RED in `local-verify.test.mjs` with exact selection/result/CLI/canonical assertions. Add immutable `PHASE12_DATA_RESULT_FORMAT`, `phase12Selection`, and `createPhase12DataResult` modeled on the current sealed phase-result patterns but scoped to the four Phase 12 feature tests plus `scripts/local-verify.test.mjs`. The dedicated run must build/typecheck the current Web runtime once, bind its `.next` and server runtime digests, allocate one validated generated Compose/database namespace, reset only that namespace's fixtures, supply `ADMIN_ANALYTICS_TEST_DATABASE_URL` only to the database suite, run TS node suites with the existing tsx semantic runner, execute only `admin-analytics.spec.ts` in the generated authenticated main-browser authority, inspect schema without changing it, require exact cleanup, and finish with `LOCAL PHASE 12 DATA PASS; RELEASE BLOCKED`. Accept only the literal zero-option CLI `--phase12-data`; reject internal-run, caller namespace/port/path, skip-build, mixed phase modes, or any server/production authority. Expand `canonicalIntegrationSelection` to the exact derived counts in `<interfaces>`, map `admin-analytics.test.ts` to its generated database variable, and add `admin-analytics.spec.ts` to the exact main-browser schedule. Do not weaken Phase 11 selection, phase7 isolation, pass-only TAP validation, secret redaction, bounded output/timeouts, cleanup convergence, runtime hashing, or the sealed canonical invocation.</action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/local-verify.test.mjs scripts/test-inventory.test.mjs scripts/default-test.test.mjs &amp;&amp; corepack pnpm check:boundaries</automated>
  </verify>
  <done>The focused gate and canonical integration each select every intended Phase 12 suite exactly once, execute only generated local authority, prove cleanup/runtime binding, reject every override/drift case, and emit explicit BLOCKED results.</done>
</task>

<task type="auto">
  <name>Task 3: Complete local evidence, deep-review handoff, and fixed 3100 refresh</name>
  <precondition>The current branch is `dev`; Docker Engine and Docker Compose answer their read-only version/info checks; no server credential or remote production target is present in the command scope.</precondition>
  <files>No repository file; verification/review/delivery only, with generated temporary evidence removed by its owning command.</files>
  <action>Run the focused/default/type/boundary gates first, then the sealed `--phase12-data` gate, followed by the exact canonical integration invocation with interruption and parallel checks. Require real nonzero pass-only counts and zero failed/cancelled/skipped/TODO rather than matching remembered totals. Hand the exact Phase 12 implementation file union from Plans 12-01/12-02 plus these six verifier files and the current dev revision to the workflow's enabled standard-depth deep code review. Critical/high findings block delivery; any accepted fix reopens its owning task and invalidates earlier focused/canonical evidence, so rerun all affected gates and the final local refresh after the fix. Once review is clean, run the existing zero-option `corepack pnpm local:deliver` against current dev source, verify `http://127.0.0.1:3100/api/health`, root, authenticated dashboard, and analytics route through the fixed local Web entry, and record the displayed revision plus production `BLOCKED`. Use only runner-generated namespaces and existing safe cleanup; do not contact either server, use credentials, deploy, mutate `main`, create release authorization, or preserve secret-bearing/generated evidence in the repository.</action>
  <verify>
    <automated>corepack pnpm test &amp;&amp; corepack pnpm -r typecheck &amp;&amp; corepack pnpm check:boundaries &amp;&amp; corepack pnpm local:verify -- --phase12-data &amp;&amp; corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check &amp;&amp; corepack pnpm local:deliver &amp;&amp; curl -fsS http://127.0.0.1:3100/api/health</automated>
  </verify>
  <done>Current reviewed dev source passes focused and canonical local evidence, generated resources are acknowledged absent, the fixed 3100 display serves healthy Phase 12 pages, and every receipt/review summary says production release is BLOCKED.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Authenticated administrator understands anonymous article trends and reaches primary creation flow in a clear lightweight workspace | 12-01, 12-02, 12-03 | COVERED | Protected aggregate authority, two SSR pages, and executable evidence form the complete path. |
| REQ | STAT-05 | 7/30/90/400 total, daily, top, source; authenticated, no-store, anonymous PV disclosure | 12-01, 12-02, 12-03 | COVERED | Contract/API, UI, and focused/canonical acceptance each own a distinct part. |
| REQ | ADMN-02 | Hierarchical dashboard cards, content status, primary writing entry, secondary analytics summary | 12-02, 12-03 | COVERED | Exact five-section document order plus browser acceptance. |
| RESEARCH | — | Strict Zod contract; PostgreSQL Shanghai bounds/zero fill; read-only 2000ms bounded query; auth/no-store; honest failures | 12-01, 12-02 | COVERED | A1 route naming follows the later UI-SPEC; A4 bounds are explicit. |
| RESEARCH | — | No package/migration/service; semantic CSS visualization; exact inventory/canonical integration | 12-01, 12-02, 12-03 | COVERED | Package audit is not applicable because no install occurs; A6 dedicated gate is selected. |
| RESEARCH | A5 | Dashboard workflow counts | 12-02 | COVERED | Uses existing strict admin-post response per UI-SPEC 10.1, not analytics SQL. |
| CONTEXT | D-01 | All analytics outputs use current shared public predicate; persisted rows hide/reappear with lifecycle | 12-01, 12-02, 12-03 | COVERED | User-locked decision overrides conflicting UI-SPEC historical-hidden-row copy/fixtures. |
| UI-SPEC | — | Exact Chinese hierarchy/copy, privacy notice, range navigation, daily/details/source/top states | 12-02 | COVERED | Page/server component and browser acceptance are explicit. |
| UI-SPEC | — | 390/768/1280, 44px, light/dark/system, focus, reduced motion, internal-only overflow | 12-02, 12-03 | COVERED | Deterministic Playwright plus canonical local evidence. |
| UI-SPEC | — | Phase 13 retains shared navigation shell, drawer, active route, and cross-page normalization | 12-02 | COVERED | `admin/layout.tsx` and navigation state remain outside Phase 12 file ownership. |
| CONSTRAINT | — | Local-only work, fixed 3100 display, no server/credentials/deployment/main mutation, production BLOCKED | 12-03 | COVERED | Sealed local gates and final delivery action preserve the freeze. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package test files -> inventory/default/canonical selectors | New coverage becomes authoritative only through exact immutable ownership. |
| Local verifier -> Docker/PostgreSQL/Web fixtures | Generated commands allocate and clean disposable local namespaces. |
| Reviewed dev revision -> fixed 3100 display | Only the reviewed current source may become the local display; this is not production release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11 | Tampering | test inventory and selectors | high | mitigate | Exact counts/paths/kinds/owners, immutable arrays, manifest digest, no caller selection, and synthetic drift failures. |
| T-12-12 | Repudiation | local result claims | medium | mitigate | Result schema binds ordered real suite counts, runtime digest, cleanup acknowledgement, review revision, and literal BLOCKED state. |
| T-12-13 | Information Disclosure | child output and environment | medium | mitigate | Minimal generated environment, existing secret redaction/bounded diagnostics, no repository evidence with secrets, and boundary scan. |
| T-12-14 | Denial of Service | local child trees | medium | mitigate | Existing bounded output, timeout, child-tree termination, finite suite selection, and namespace-specific convergence. |
| T-12-15 | Elevation of Privilege | production/server boundary | critical | mitigate | Loopback-only fixed origin, generated Docker authority, no remote/deploy adapter, explicit server/main prohibitions, and releaseState BLOCKED. |
| T-12-SC | Tampering | package-manager surface | high | mitigate | No install task; default, boundary, type, and lock/manifests review prove the dependency surface stays unchanged. |
</threat_model>

<review_and_delivery_handoff>
- Workflow code review is enabled at standard depth. Review the exact Phase 12 file union, with special attention to auth-before-query, D-01 predicate reuse, SQL arithmetic/date boundaries, response privacy, SSR failure separation, browser same-origin requests, inventory ownership, and cleanup authority.
- A review finding is not waived by a passing suite. Fix it in the owning file, rerun focused tests, then rerun `--phase12-data`, canonical integration, and `local:deliver` so evidence and fixed display bind the corrected revision.
- The fixed `3100` environment is a local demonstration target only. Its healthy state never authorizes TLS, server access, production scheduling, or deployment.
</review_and_delivery_handoff>

<verification>
- `node --test --test-reporter=tap scripts/test-inventory.test.mjs scripts/default-test.test.mjs scripts/local-verify.test.mjs` passes exact ownership and verifier contracts.
- `corepack pnpm test`, workspace typechecks, and repository boundary checks pass.
- `corepack pnpm local:verify -- --phase12-data` proves the exact Phase 12 suites with no skip/TODO and terminal BLOCKED marker.
- The sealed canonical integration invocation proves all 31 owned paths, interruption, parallel isolation, current runtime, and cleanup.
- Standard-depth review reports zero unresolved findings; any fix is followed by renewed evidence.
- `corepack pnpm local:deliver` refreshes current reviewed dev source at fixed 3100; health, root, dashboard, and analytics probes remain same-origin and local.
</verification>

<success_criteria>
- All GOAL, STAT-05, ADMN-02, research, UI-SPEC, and D-01 rows in the source audit are covered with no deferred item pulled into scope.
- Exact test ownership advances from 41/11/30 to 45/13/32 and canonical integration from 29 to 31 without weakening existing suites.
- Focused, default, canonical, responsive/accessibility, review, cleanup, and fixed-display seams are executable and fail closed.
- No server access, credential use, deployment, destructive broad cleanup, `main` mutation, new dependency, or production authorization occurs.
- Production release remains explicitly `BLOCKED` in every final result and handoff.
</success_criteria>

<output>
Create `.planning/phases/12-administrator-insights/12-03-SUMMARY.md` when done.
</output>
