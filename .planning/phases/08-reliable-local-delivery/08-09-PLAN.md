---
phase: 08-reliable-local-delivery
plan: "09"
type: execute
wave: 9
depends_on: ["08-08"]
files_modified:
  - ops/local-deliveries/<reviewed-sha>.json
  - .planning/phases/08-reliable-local-delivery/08-09-SUMMARY.md
  - .planning/phases/08-reliable-local-delivery/08-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [DEVX-01, DEVX-02, DEVX-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Plan 08-09 is invoked directly as the terminal execute-plan closeout after 08-08's normal summary and one repository-read-only exact-HEAD review; execute-phase automatic post-review is not resumed."
    - "Task 1 validates the final temp report against current clean `dev` HEAD, atomically records marker and REVIEW digest, proves exact derived receipt/claim/failure absent, then performs no repository write before delivery."
    - "Zero-argument `corepack pnpm local:deliver` runs exactly once; any failure or ambiguity consumes that SHA and is never retried or cleaned away."
    - "Independent derived verification proves exact API/Web revision labels, three healthy canonical services, two retained volumes, fixed 3100 routes, complete pass-only acceptance inventory, no generated residue and `BLOCKED`."
    - "Only the generated SHA receipt is committed first; then only finite closeout documents and normal execute-plan `08-09-SUMMARY.md` are committed. REVIEW files and source/config/PLAN/CONTEXT never change post-delivery."
    - "Goal-backward Phase 08 verification updates status only from executable evidence and keeps any unresolved gap explicit."
    - "Historical receipts remain byte-identical and neither server is contacted."
    - "Final verification proves `08-REVIEW.md` digest equals its pre-delivery marker snapshot and every post-delivery Git path is in Plan 08-04's finite exact allowlist."
  artifacts:
    - path: ops/local-deliveries/<reviewed-sha>.json
      provides: immutable receipt for exact pre-reviewed implementation HEAD
    - path: .planning/phases/08-reliable-local-delivery/08-VERIFICATION.md
      provides: post-fix goal-backward Phase 08 verdict
    - path: .planning/phases/08-reliable-local-delivery/08-09-SUMMARY.md
      provides: exact once-only delivery and closeout handoff
  key_links:
    - from: /private/tmp/blog-x-phase08-reviewed-head-v1.json
      to: ops/local-deliveries/<reviewed-sha>.json
      via: reviewed-delivery gate plus production SHA authority derivation
      pattern: "deliveryAuthorityForRevision"
    - from: ops/local-deliveries/<reviewed-sha>.json
      to: .planning/phases/08-reliable-local-delivery/08-VERIFICATION.md
      via: strict independent evidence and goal-backward requirement verification
      pattern: "DEVX-01|DEVX-02|DEVX-03"
---

<objective>
Deliver the exact clean reviewed implementation once, independently verify it, and close Phase 08 from goal-level evidence.

Purpose: execute the previously missing post-fix canonical delivery without review/source drift, then synchronize GSD status through the finite permitted descendant delta.
Output: one immutable SHA receipt, healthy canonical runtime, updated verification/status documents and closeout summary.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/verify-phase.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-reliable-local-delivery/08-VERIFICATION.md
@.planning/phases/08-reliable-local-delivery/08-04-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-05-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-06-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-07-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-08-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-REVIEW.md
@scripts/reviewed-delivery-gate.mjs
@scripts/refresh-local.mjs
@scripts/refresh-local-runtime-core.mjs
@scripts/test-inventory.mjs
@ops/v1.1-local-delivery-evidence.json
@ops/v1.1-local-delivery-evidence.successor-2.json

<interfaces>
- `node scripts/reviewed-delivery-gate.mjs --preflight-delivery` is a runnable fail-closed check for marker/current reviewed HEAD equality, actual worktree emptiness and absent derived receipt/claim/failure.
- Before Task 1, the root orchestration contract has written exactly one clean current-HEAD report to `/private/tmp/blog-x-phase08-final-review.md` without repository mutation; the secure reviewed marker is still absent.
- `node scripts/reviewed-delivery-gate.mjs --verify-evidence` derives the one strict receipt from recorded reviewed SHA and invokes the independent production verifier across finite descendant commits; current-HEAD equality is not required after delivery.
- `node scripts/reviewed-delivery-gate.mjs --assert-review-unchanged` proves committed `08-REVIEW.md` still matches the digest snapshotted immediately before delivery.
- Historical immutable SHA-256 values are `9a9af65bafabbf94c097525fccba62d3c615d0544ebb127a42f9297efb9303cb` and `f10b124b5fba45b7f7dee863db4277259c4b8529cd6c612c31801d8fb5776049`.
- Canonical authority is project `blogxlocal`, origin `http://127.0.0.1:3100`, services `postgres`, `api`, `web`, volumes `blogxlocal_postgres-data`, `blogxlocal_media-data`, and delivery-SHA API/Web provenance labels.
- Plan 08-04 permits only the derived receipt plus exact 08-04..08-09 summaries, 08-VERIFICATION, ROADMAP, STATE and REQUIREMENTS after delivery. REVIEW/REVIEW-FIX are already ancestors and remain excluded from delta.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke one formal reviewed delivery and commit only its receipt</name>
  <reversibility rating="irreversible">The terminal per-SHA claim and receipt make an attempt non-repeatable; failure must remain visible and stops the plan.</reversibility>
  <files>ops/local-deliveries/<reviewed-sha>.json</files>
  <read_first>
    <item>08-08-SUMMARY.md and 08-REVIEW.md — committed review gate outcome and no later source mutation.</item>
    <item>scripts/reviewed-delivery-gate.mjs — runnable fixed-marker preflight and evidence modes.</item>
    <item>scripts/refresh-local-runtime-core.mjs — formal ordering, authority, rollback and verifier contract.</item>
    <item>Both historical receipts — read-only digest baselines.</item>
  </read_first>
  <behavior>
    - "Marker recording proves the one final temp review is clean, zero-finding and bound to current full HEAD, then snapshots committed REVIEW digest."
    - "Preflight proves marker/current HEAD equality, worktree emptiness and exact receipt/claim/failure absence."
    - "Formal zero-argument command is invoked once with no wrapper retry."
    - "Success produces exactly one derived receipt and canonical healthy runtime; failure stops without removing terminal state."
    - "After receipt-only commit, derived independent verification succeeds against the original reviewed SHA."
  </behavior>
  <action>
    Confirm this plan was invoked directly through execute-plan under the terminal one-shot contract, not by resumed execute-phase automatic post-review. Require `/private/tmp/blog-x-phase08-final-review.md` to be the sole final temp report and the fixed marker to be absent. Run exact `node scripts/reviewed-delivery-gate.mjs --record-reviewed-head`, which validates report/current clean HEAD equality and snapshots the committed `08-REVIEW.md` digest, then run `node scripts/reviewed-delivery-gate.mjs --assert-reviewed-head` and `node scripts/reviewed-delivery-gate.mjs --preflight-delivery`. Stop on any ambiguity. From marker creation through formal invocation, perform no repository write, commit, checkout, amendment, review/report update or summary generation. Hash historical receipts and snapshot prior canonical image/volume facts, then invoke exact `corepack pnpm local:deliver` once. Never retry the same SHA, remove its claim/failure, select another receipt or edit source after failure.

    On success, run `node scripts/reviewed-delivery-gate.mjs --verify-evidence`. Independently inspect exactly three healthy canonical services, exact volumes, API/Web IDs and required SHA/lock/application/origin/seed/refresh labels, host binding 127.0.0.1:3100, and successful `/`, `/search`, `/api/health`. Parse receipt inventories/counts for exact full ownership, nonzero pass-only semantics, cleanup and `BLOCKED`; prove no generated project/container/volume/database/backup/media residue and re-hash history.

    Run focused/default/boundary checks without formal delivery again. Require exactly one new repository path: the derived receipt. Stage and commit only it. Assert clean, unchanged REVIEW digest and independently verify evidence again; the receipt-only descendant must pass although current HEAD is now a descendant of marker SHA. Do not modify REVIEW, source, config or planning docs in this task.
  </action>
  <verify><automated>node scripts/reviewed-delivery-gate.mjs --verify-evidence &amp;&amp; node scripts/reviewed-delivery-gate.mjs --assert-review-unchanged &amp;&amp; node scripts/reviewed-delivery-gate.mjs --assert-clean &amp;&amp; corepack pnpm test &amp;&amp; node --test scripts/default-test.test.mjs scripts/test-inventory.test.mjs scripts/local-verify.test.mjs scripts/phase7-browser-verify.test.mjs scripts/local-delivery-acceptance.test.mjs scripts/refresh-local.test.mjs scripts/reviewed-delivery-gate.test.mjs &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated></verify>
  <done>Exactly one reviewed-SHA attempt succeeded, only its receipt was committed, canonical runtime/evidence pass independently and release remains `BLOCKED`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 08, complete tracking and own terminal closeout verification</name>
  <files>.planning/phases/08-reliable-local-delivery/08-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    <item>Current 08-VERIFICATION.md — prior actionable gaps.</item>
    <item>ROADMAP, STATE and REQUIREMENTS — status formats and DEVX-01..03 mapping.</item>
    <item>Derived receipt plus Task 1 verifier/runtime outputs — authoritative goal evidence.</item>
  </read_first>
  <behavior>
    - "Verification starts from Phase goal/DEVX outcomes rather than completed tasks."
    - "Every verdict cites actual reviewed SHA, derived receipt, runtime/routes, exact inventory and observed counts without secrets or a fixed expected total."
    - "Only passing requirements are marked complete; remaining gaps stay explicit."
    - "After tracking/docs commit, original receipt still verifies, REVIEW digest is unchanged and delta contains only exact allowlisted paths."
    - "Normal execute-plan may create/rewrite 08-09-SUMMARY and perform its mechanical STATE/ROADMAP/REQUIREMENTS progress/session updates after tasks; all are finite allowed docs, and Task 2's terminal verifier is rerun once after the complete closeout before any push."
  </behavior>
  <action>
    Run the GSD verify-phase workflow goal-backward. Recheck canonical health/volumes/routes/labels, data preservation/rollback evidence, complete inventory/count semantics, claim/receipt immutability, historical digests, cleanup and `BLOCKED`. Own the semantic phase-complete decision: update 08-VERIFICATION, ROADMAP, STATE and REQUIREMENTS only according to the resulting verdict, including phase completion only if every DEVX outcome passes. Do not create or hand-commit 08-09-SUMMARY inside the task; normal execute-plan owns its required summary and may apply only its standard mechanical progress/session/ready-requirement mutations afterward.

    Commit exactly the four tracking/verification docs. Do not modify REVIEW/REVIEW-FIX, source, config, PLAN or CONTEXT. Run the runnable derived verifier, unchanged-REVIEW assertion and clean assertion; they must accept only the derived receipt plus exact permitted closeout paths. Re-hash all receipts and confirm canonical runtime remains healthy/`BLOCKED`. This exact verification command remains Task 2's terminal check and the root orchestrator must rerun it after normal execute-plan completes its summary commit and all standard metadata mutations.
  </action>
  <verify><automated>node scripts/reviewed-delivery-gate.mjs --verify-evidence &amp;&amp; node scripts/reviewed-delivery-gate.mjs --assert-review-unchanged &amp;&amp; node scripts/reviewed-delivery-gate.mjs --assert-clean &amp;&amp; git diff --check</automated></verify>
  <done>Phase status/tracking reflects executable goal evidence and Task 2's terminal command is ready for the mandatory post-SUMMARY rerun.</done>
</task>

</tasks>

## Required ordering

1. Plans 08-04..08-07 implementation and summaries committed.
2. Plan 08-08 configured review/fix/re-review completes normally, including execute-plan's final summary commit.
3. Root runs one exact-scope read-only review to the fixed temp report, then invokes 08-09 directly; it does not resume execute-phase post-review.
4. Task 1 atomically records reviewed HEAD/REVIEW digest, proves derived authority absent and invokes zero-argument formal delivery once with no intervening repository write.
5. Independent verification; receipt alone committed.
6. Goal-backward closeout docs committed; independent verification repeated against original SHA.
7. Normal execute-plan commits 08-09-SUMMARY and finishes standard metadata updates; root reruns Task 2's terminal verifier, then may only push/report.

## Terminal compatibility rule

The configured code-review capability remains enabled and is not modified. Its substantive review/fix/re-review is consumed in Plan 08-08 and its final exact-HEAD equivalent is the read-only temp review between plans. Plan 08-09 is therefore executed directly as a one-shot terminal closeout. After delivery, the root orchestrator MUST NOT invoke execute-phase's `code_review_gate`, any code-review workflow, or any writer of `08-REVIEW.md`/`08-REVIEW-FIX.md`. After normal 08-09 SUMMARY/metadata closeout it may only run the following Task 2 terminal command, inspect status and push/report:

`node scripts/reviewed-delivery-gate.mjs --verify-evidence &amp;&amp; node scripts/reviewed-delivery-gate.mjs --assert-review-unchanged &amp;&amp; node scripts/reviewed-delivery-gate.mjs --assert-clean &amp;&amp; git diff --check`

## Multi-Source Coverage Audit

| Source | Item | Owner | Status |
|---|---|---|---|
| PROJECT / ROADMAP | Reliable local delivery goal and five success outcomes | 08-04..08-09 | COVERED |
| REQUIREMENTS | DEVX-01 | 08-04, 08-07, 08-09 | COVERED |
| REQUIREMENTS | DEVX-02 | 08-05..08-07, 08-09 | COVERED |
| REQUIREMENTS | DEVX-03 | 08-04..08-09 | COVERED |
| CONTEXT | D-01 | 08-04, 08-09 | COVERED |
| CONTEXT | D-02 | 08-04, 08-09 | COVERED |
| CONTEXT | D-03 | 08-04, 08-08, 08-09 | COVERED |
| CONTEXT | D-04 | 08-04, 08-09 | COVERED |
| CONTEXT | D-05 | 08-04, 08-09 | COVERED |
| CONTEXT | D-06 | 08-04, 08-07 | COVERED |
| CONTEXT | D-07 | 08-04, 08-07 | COVERED |
| CONTEXT | D-08 | 08-04 | COVERED |
| CONTEXT | D-09 | 08-06, 08-07 | COVERED |
| CONTEXT | D-10 | 08-04, 08-09 | COVERED |
| CONTEXT | D-11 | 08-07, 08-09 | COVERED |
| CONTEXT | D-12 | 08-04, 08-09 | COVERED |
| CONTEXT | D-13 | 08-04, 08-09 | COVERED |
| CONTEXT | D-14 | 08-04, 08-09 | COVERED |
| CONTEXT | D-15 | 08-06, 08-07 | COVERED |
| CONTEXT | D-16 | 08-07, 08-09 | COVERED |
| CONTEXT | D-17 | 08-05, 08-07, 08-09 | COVERED |
| CONTEXT | D-18 | 08-04, 08-07, 08-09 | COVERED |
| CONTEXT | D-19 | 08-09 | COVERED |
| RESEARCH | Reuse sealed CLI, generated fixtures, offline provenance and rollback ordering | 08-04, 08-06, 08-07 | COVERED |
| VERIFICATION | Exhausted numbered delivery authority | 08-04, 08-09 | COVERED |
| VERIFICATION | Red default test and incomplete integration ownership | 08-05..08-07 | COVERED |
| CHECKER | Focus plans below three tasks/fifteen files and real module paths | 08-05..08-07 | COVERED |
| CHECKER | Phase 7 inventory attestation and coverage-neutral lifecycle probes | 08-07 | COVERED |
| CHECKER | Configured clean review before exact delivery SHA | 08-08, 08-09 | COVERED |
| CHECKER | Runnable derived verifier and actual clean assertion | 08-08, 08-09 | COVERED |
| SCOPE | No server/production operation; release stays BLOCKED | all plans | COVERED |

Audit result: all Phase 08 goal, DEVX-01..03, D-01..D-19, research constraints, actionable verification gaps and revision findings have executable owners. Production deployment remains out of scope.

<threat_model asvs_level="1" block_on="high">
| Threat | Severity | Control |
|---|---|---|
| Wrong/unreviewed SHA delivered | high | Final temp report, marker and clean current HEAD must agree before attempt. |
| Same SHA retried | high | Terminal claim/failure preserved; no retry or cleanup path. |
| Arbitrary receipt selected | high | Runnable helper derives one exact path and rejects ambiguity. |
| Post-delivery source/review drift | high | Finite exact descendant allowlist excludes both source and REVIEW. |
| Automatic post-review rewrites REVIEW | high | Direct terminal execute-plan bypass after configured review is consumed; no execute-phase post hook resumes. |
| Partial acceptance or false count | high | Exact inventory/cardinality and nonzero pass-only schema verification. |
| Production access | high | Local-only commands and terminal `BLOCKED`. |
</threat_model>

<verification>
- Every automated command is directly runnable and XML-escaped; no SHA placeholder or bare non-asserting status command exists.
- Receipt verification passes after delivery, receipt commit, tracking docs and normal summary commit without requiring current HEAD equality.
- Final check asserts REVIEW digest equals the marker snapshot and every delivery-SHA descendant path belongs to the finite exact allowlist.
- Exact canonical runtime, routes, volumes, labels, inventory, counts, cleanup and history digests pass.
- No server, production release or REVIEW mutation occurs.
</verification>

<success_criteria>
- Exact reviewed HEAD is running canonically at 127.0.0.1:3100 with immutable evidence.
- One formal attempt and one receipt-only commit precede closeout docs.
- Phase 08 status is goal-verified and all later delta is finite/independently accepted.
- Configured review remains enabled but is never rerun or rewritten after terminal delivery.
</success_criteria>

<output>Normal execute-plan creates/rewrites and commits `.planning/phases/08-reliable-local-delivery/08-09-SUMMARY.md` after tasks. Then the root orchestrator reruns Task 2's exact terminal verifier and performs no REVIEW-capable hook.</output>
