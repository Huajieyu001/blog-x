---
phase: 08-reliable-local-delivery
plan: "07"
type: execute
wave: 7
depends_on: ["08-06"]
files_modified:
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - scripts/local-delivery-acceptance.mjs
  - scripts/local-delivery-acceptance-test-core.mjs
  - scripts/local-delivery-acceptance.test.mjs
  - scripts/phase7-browser-verify.mjs
  - scripts/phase7-browser-verify.test.mjs
  - scripts/refresh-local-runtime-core.mjs
  - scripts/refresh-local.test.mjs
autonomous: true
gap_closure: true
requirements: [DEVX-01, DEVX-02, DEVX-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One receipt-free canonical integration invocation executes all 13 runner-owned API files and all 17 Web E2E files with per-path cardinality exactly one."
    - "The non-Phase-7 producer attests its exact 13 API + 16 E2E owned paths; Phase 7 attests exactly `public-discovery.spec.ts`; the coordinator rejects any missing, extra or duplicate path."
    - "Interruption and parallel-isolation controls are lifecycle probes only: they allocate/interrupt/clean minimal generated authorities and never rerun any manifest-owned package test."
    - "`corepack pnpm test:integration` and formal `local:deliver` consume the same sealed zero-argument coordinator and complete signed/digested result schema."
    - "Acceptance uses actual nonzero pass-only counts with no fixed historical total, acknowledges cleanup, leaves canonical data untouched and remains `BLOCKED`."
  artifacts:
    - path: scripts/local-verify.mjs
      provides: receipt-free manifest-driven canonical integration producer
      exports: ["canonicalIntegrationSelection"]
    - path: scripts/phase7-browser-verify.mjs
      provides: Phase 7 result attesting its exact owned file inventory
    - path: scripts/local-delivery-acceptance.mjs
      provides: strict complete coordinator and aggregate signed/digested record
    - path: scripts/refresh-local-runtime-core.mjs
      provides: formal-delivery evidence consumption of complete acceptance schema
  key_links:
    - from: scripts/test-inventory.mjs
      to: scripts/local-verify.mjs
      via: exact non-Phase-7 integration ownership
      pattern: "INTEGRATION_TEST_FILES|canonicalIntegrationSelection"
    - from: scripts/test-inventory.mjs
      to: scripts/phase7-browser-verify.mjs
      via: exact `phase7-browser` ownership
      pattern: "public-discovery\\.spec\\.ts"
    - from: scripts/local-delivery-acceptance.mjs
      to: scripts/local-delivery-acceptance-test-core.mjs
      via: literal two-child argv and injected test process boundary
      pattern: "createLocalDeliveryAcceptanceTestRuntime"
    - from: scripts/refresh-local-runtime-core.mjs
      to: scripts/local-delivery-acceptance.mjs
      via: strict acceptance parser before canonical migration/cutover
      pattern: "parseLocalDeliveryAcceptanceRecord"
---

<objective>
Make the sealed integration coordinator execute and attest the complete runner-owned package-test inventory exactly once.

Purpose: replace partial overlapping Phase 6/7 claims with a truthful receipt-free generated gate shared by developer testing and formal delivery.
Output: manifest-driven producer, inventory-attesting Phase 7 record, strict coordinator schema and formal evidence wiring.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-reliable-local-delivery/08-CONTEXT.md
@.planning/phases/08-reliable-local-delivery/08-VERIFICATION.md
@.planning/phases/08-reliable-local-delivery/08-05-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-06-SUMMARY.md
@scripts/test-inventory.mjs
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/local-delivery-acceptance.mjs
@scripts/local-delivery-acceptance-test-core.mjs
@scripts/local-delivery-acceptance.test.mjs
@scripts/phase7-browser-verify.mjs
@scripts/refresh-local-runtime-core.mjs
@scripts/refresh-local.test.mjs

<interfaces>
- The actual injected test seam is the existing `scripts/local-delivery-acceptance-test-core.mjs`; production composition remains in `scripts/local-delivery-acceptance.mjs`.
- Plan 08-05 owns exact inventory; Plan 08-06 made the six legacy main-browser files compatible with generated ownership.
- Existing Phase 3/4/5/6 selectors overlap and Phase 5 full publishes a fixed receipt. Canonical integration must reuse lower-level fixture/check primitives without invoking any overlapping full-phase or receipt-producing mode.
- Existing coordinator invokes local Phase 6 data and Phase 7; current evidence fields `phase6Data`/`phase7Browser` must evolve atomically with parser/tests and new receipts while historical receipts remain byte-identical read-only artifacts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build one receipt-free complete generated integration producer</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs</files>
  <read_first>
    <item>scripts/test-inventory.mjs — exact integration paths and fixture owners.</item>
    <item>scripts/local-verify.mjs — reusable Phase selectors, generated fixture primitives, result and cleanup contracts.</item>
    <item>scripts/local-verify.test.mjs — selector/cardinality/TAP/lifecycle regressions.</item>
  </read_first>
  <behavior>
    - "`canonicalIntegrationSelection()` owns exactly 13 API and 16 non-Phase-7 E2E paths, grouped by fixture owner and deduplicated before execution."
    - "Every selected package path executes exactly once and the signed/digested result records the sorted exact path list plus actual counts."
    - "No Phase 5 receipt/claim writer or overlapping `full` composer runs."
    - "Interruption and parallel probes execute zero manifest-owned test paths while proving process-tree termination, namespace isolation and cleanup acknowledgement."
  </behavior>
  <action>
    Write inventory/cardinality and probe-isolation tests first. Add exact `--canonical-integration --interruption-check --parallel-check` mode: schedule manifest paths once through their declared generated fixture families, reuse existing build/seed/database/backup/media/restore/error/browser primitives and emit one strict result with inventory, manifest digest, counts, result digest, cleanup and `BLOCKED`. Redefine interruption/parallel checks as minimal lifecycle/isolation probes that start health fixtures only and never invoke API tests or Playwright. Reject subset/path/env authority and leave canonical `blogxlocal` untouched.
  </action>
  <verify><automated>node --test scripts/local-verify.test.mjs scripts/test-inventory.test.mjs &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The non-Phase-7 producer runs each owned package test once, attests exact inventory and keeps lifecycle probes coverage-neutral.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Phase 7 attest its exclusive owned path</name>
  <files>scripts/phase7-browser-verify.mjs, scripts/phase7-browser-verify.test.mjs, scripts/local-verify.test.mjs</files>
  <read_first>
    <item>scripts/phase7-browser-verify.mjs — current result and cleanup record.</item>
    <item>apps/web/e2e/public-discovery.spec.ts — exclusive Phase 7 package test.</item>
    <item>scripts/test-inventory.mjs — exact Phase 7 ownership.</item>
  </read_first>
  <behavior>
    - "Phase 7 result contains exactly one owned path, the matching manifest digest, actual pass-only counts, result digest, cleanup acknowledgement and `BLOCKED`."
    - "Missing/extra/duplicate path, zero/fail/cancel/skip/TODO or cleanup failure is rejected."
    - "Its generated Web origins/children/root are absent at completion and no canonical authority is touched."
  </behavior>
  <action>
    Add the focused test module and update the Phase 7 producer schema atomically. Derive the exact owned path from the manifest classification, execute it once, and bind inventory/counts to the result digest. Retain unfiltered responsive cases within that file and exact generated cleanup. Update the existing parser-focused tests in `local-verify.test.mjs`; do not let Phase 7 accept caller-supplied files.
  </action>
  <verify><automated>node --test scripts/phase7-browser-verify.test.mjs scripts/local-verify.test.mjs &amp;&amp; git diff --check</automated></verify>
  <done>Phase 7 independently attests exactly its one package file and strict responsive/cleanup outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind complete inventory evidence into the shared coordinator and formal gate</name>
  <files>scripts/local-delivery-acceptance.mjs, scripts/local-delivery-acceptance-test-core.mjs, scripts/local-delivery-acceptance.test.mjs, scripts/refresh-local-runtime-core.mjs, scripts/refresh-local.test.mjs</files>
  <read_first>
    <item>The two Task 1/2 strict result formats.</item>
    <item>scripts/local-delivery-acceptance*.mjs — actual production/test seam, command composition, redaction and parsing.</item>
    <item>scripts/refresh-local-runtime-core.mjs and tests — pre-cutover acceptance/evidence schema and terminal output.</item>
  </read_first>
  <behavior>
    - "Coordinator accepts zero args and invokes only exact canonical-integration and Phase 7 children."
    - "Union of both result inventories equals all 30 integration-owned paths with cardinality one; default + integration union equals complete on-disk inventory."
    - "Aggregate schema uses truthful `generatedIntegration` and `phase7Browser` layers, actual counts/digests and `BLOCKED`; partial/duplicate/malformed results fail."
    - "Formal delivery consumes this same schema before migration and records it without rewriting historical receipts."
  </behavior>
  <action>
    Update production coordinator and the existing hyphenated test core together. Invoke exact literal children, validate manifest/path/digest/cardinality and aggregate actual semantic counts. Rename the misleading layer, bump the new acceptance schema, and update refresh evidence parser/output/tests atomically. Add omission, duplication, extra path, manifest drift, probe-added-count, skip/TODO, cleanup and partial-child regressions. Preserve bounded redaction, offline/generated isolation, acceptance-before-migration and `BLOCKED`; never hard-code an observed count.
  </action>
  <verify><automated>node --test scripts/test-inventory.test.mjs scripts/local-verify.test.mjs scripts/phase7-browser-verify.test.mjs scripts/local-delivery-acceptance.test.mjs scripts/refresh-local.test.mjs &amp;&amp; corepack pnpm test &amp;&amp; corepack pnpm test:integration &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The same complete, exact-once, pass-only generated acceptance gates developer integration and formal delivery.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| Threat | Severity | Control |
|---|---|---|
| Partial/duplicate inventory attested | high | Exact manifest digest, path union and cardinality-one validation. |
| Lifecycle probes inflate coverage | high | Probe result carries zero package paths/counts and tests forbid package execution. |
| Wrong test seam silently added | high | Existing hyphenated test core is the only injected seam; stale/nonexistent paths are source-scanned absent. |
| Formal cutover consumes old partial schema | high | Producer/parser/evidence tests change atomically before delivery. |
| Generated fixture leaks or mutates canonical | high | Exact cleanup acknowledgements and canonical non-ownership assertions. |
</threat_model>

<verification>
- Focused producer, Phase 7, coordinator, refresh and inventory tests pass.
- Real `test:integration` executes all 30 runner-owned files once and cleans all generated authority.
- Root default plus integration path union equals the full package inventory.
- Historical receipt digests remain unchanged; release stays `BLOCKED`.
</verification>

<success_criteria>
- Complete canonical integration has exact per-path cardinality one.
- Phase 7 signs/attests its exact inventory, and lifecycle probes execute none.
- Formal delivery and developer integration share one strict coordinator and actual counts.
</success_criteria>

<output>Create `.planning/phases/08-reliable-local-delivery/08-07-SUMMARY.md` when done.</output>
