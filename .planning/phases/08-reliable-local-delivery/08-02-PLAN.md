---
phase: 08-reliable-local-delivery
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - scripts/phase7-browser-verify.mjs
  - scripts/local-delivery-acceptance.mjs
  - scripts/local-delivery-acceptance-test-core.mjs
  - scripts/local-delivery-acceptance.test.mjs
autonomous: true
requirements:
  - DEVX-03
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The complete isolated Phase 6 data selector reports every database, Node and repository-boundary suite with exact nonzero counts and zero failure/cancel/skip/TODO (D-16, D-17)."
    - "The unfiltered Phase 7 generated-port runner reports its complete responsive search/related browser matrix as exact nonzero counts and always proves exact-child/root cleanup (D-15, D-16, D-17)."
    - "One sealed zero-argument production acceptance coordinator runs Phase 6 with interruption plus two generated parallel namespaces, then Phase 7 with no grep, and accepts no partial mode or authority override; injectable process/orchestration seams exist only in the test-core module (D-14, D-15)."
    - "Only sanitized counts and SHA-256 digests cross from disposable acceptance into delivery evidence; generated projects, ports, database/media state and raw output never become canonical facts (D-15, D-18, D-19)."
    - "Resolved edge truth for DEVX-03 concurrency: D-14/D-15 require one coordinator invocation whose three Phase 6 generated runs have distinct namespace/port authorities and whose exact cleanup is proved before results are accepted."
  artifacts:
    - path: scripts/local-verify.mjs
      provides: machine-readable complete Phase 6 result emission
      exports: ["PHASE6_DATA_RESULT_FORMAT", "createPhase6DataResult", "parseSemanticTapResult", "parseBoundaryResult"]
    - path: scripts/phase7-browser-verify.mjs
      provides: import-safe exact Phase 7 result parser and machine result emission
      exports: ["PHASE7_BROWSER_RESULT_FORMAT", "assertPlaywrightResult"]
    - path: scripts/local-delivery-acceptance.mjs
      provides: sealed zero-argument production v1.1 acceptance coordinator and pure record parsers
      exports: ["LOCAL_DELIVERY_ACCEPTANCE_FORMAT", "parseLocalDeliveryAcceptanceRecord", "parseLocalDeliveryAcceptanceOutputs", "runLocalDeliveryAcceptance"]
    - path: scripts/local-delivery-acceptance-test-core.mjs
      provides: test-only injectable process/orchestration assembly for acceptance fault paths
      exports: ["createLocalDeliveryAcceptanceTestRuntime"]
    - path: scripts/local-delivery-acceptance.test.mjs
      provides: strict count/digest/completeness/redaction/failure and sealed-production-boundary contract tests
  key_links:
    - from: scripts/local-delivery-acceptance.mjs
      to: scripts/local-verify.mjs
      via: exact `node scripts/local-verify.mjs --phase6-data --interruption-check --parallel-check`
      pattern: "phase6-data.*interruption-check.*parallel-check"
    - from: scripts/local-delivery-acceptance.mjs
      to: scripts/phase7-browser-verify.mjs
      via: exact unfiltered Node invocation with no grep argument
      pattern: "phase7-browser-verify.mjs"
    - from: scripts/local-delivery-acceptance-test-core.mjs
      to: scripts/local-delivery-acceptance.mjs
      via: test-only raw process assembly reuses the production module's pure parsers while the production module never imports test-core
      pattern: "createLocalDeliveryAcceptanceTestRuntime|parseLocalDeliveryAcceptanceOutputs"
    - from: scripts/local-verify.mjs
      to: scripts/check-boundaries.mjs
      via: existing `phase6Selection('data')` boundary result is included in totals
      pattern: "parseBoundaryResult|boundarySuite"
    - from: scripts/phase7-browser-verify.mjs
      to: apps/web/e2e/public-discovery.spec.ts
      via: full workers=1 generated-origin Playwright invocation
      pattern: "public-discovery.spec.ts|workers=1"
  prohibitions:
    - requirement_id: DEVX-03
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT seed, edit, truncate, delete or otherwise use canonical `blogxlocal` data/port as the exhaustive acceptance fixture."
---

## Phase Goal

**As the** Blog X developer, **I want to** receive strict machine-readable results from the complete isolated discovery suites, **so that** the fixed delivery receipt cannot claim success from health checks, partial tests or stale evidence.

<objective>
Turn the already-complete Phase 6 and Phase 7 gates into strict count/digest producers, then compose them behind one sealed, generated-authority-only acceptance command.

Purpose: make feature acceptance a reusable pre-cutover fact without exposing canonical data or introducing a second refresh pipeline.
Output: Phase 6/7 result records, strict parsers, sealed `scripts/local-delivery-acceptance.mjs` and test-only `scripts/local-delivery-acceptance-test-core.mjs` assembly.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-reliable-local-delivery/08-CONTEXT.md
@.planning/phases/08-reliable-local-delivery/08-RESEARCH.md
@.planning/phases/08-reliable-local-delivery/08-PATTERNS.md
@.planning/phases/08-reliable-local-delivery/08-01-SUMMARY.md
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/phase7-browser-verify.mjs
@apps/web/e2e/public-discovery.spec.ts
@scripts/check-boundaries.mjs
@scripts/refresh-local-live.mjs
@scripts/refresh-local-test-core.mjs

<interfaces>
- `phase6Selection("data")` is the exact five-database-suite + `scripts/local-verify.test.mjs` + repository-boundary selection and must not become configurable.
- `parseSemanticTapResult` and `parseBoundaryResult` already fail on zero, incomplete, skip/TODO or non-pass results; new records carry their returned count objects.
- `phase7-browser-verify.mjs` owns generated fixture/Web ports, isolated Web root, full `public-discovery.spec.ts`, nonzero discovered/passed equality and exact cleanup.
- Generated Phase 6 children use `blogxverify_*` namespaces and generated ports; the coordinator must consume their machine records without gaining their Compose/data cleanup authority.
- The acceptance coordinator is evidence-only input to the existing refresh runtime; it has no `blogxlocal`, migration, cutover, receipt-writer, server or deployment adapter.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit strict complete machine records from Phase 6 and Phase 7</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs, scripts/phase7-browser-verify.mjs, scripts/local-delivery-acceptance.test.mjs</files>
  <read_first>
    <item>scripts/local-verify.mjs — `phase6Selection`, `runDatabaseSuite`, `runPhase6DataChecks`, `parallelCheck`, TAP/boundary parsers and cleanup.</item>
    <item>scripts/local-verify.test.mjs — exact Phase 6 selection and fail-closed count parser matrices.</item>
    <item>scripts/phase7-browser-verify.mjs — `assertPlaywrightResult`, option parsing, unfiltered invocation and `finally` cleanup.</item>
    <item>apps/web/e2e/public-discovery.spec.ts — complete Phase 7 matrix and prohibition on skip/fixme/only.</item>
    <item>scripts/check-boundaries.mjs — single machine boundary result format consumed by Phase 6.</item>
    <item>.planning/phases/06-public-discovery-data/06-VERIFICATION.md — verified Phase 6 suite scope; use only as history, not count authority.</item>
    <item>.planning/phases/07-responsive-discovery-experience/07-VERIFICATION.md — verified unfiltered browser scope; use only as history, not count authority.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-CONTEXT.md — D-14 through D-17.</item>
  </read_first>
  <behavior>
    - "`createPhase6DataResult` accepts exactly all selected suite records, includes suite ID/kind/counts, computes totals, requires every count set nonzero/pass-only and emits format `blog-x-phase6-data-result`, version 1, `BLOCKED`."
    - "The parent `--phase6-data --interruption-check --parallel-check` run exposes exactly three independently parsed Phase 6 result records: parent plus two generated children; missing/duplicate/partial records fail."
    - "Exported `assertPlaywrightResult` returns `{tests,passed,failed,cancelled,skipped,todo}` and the unfiltered main emits one `blog-x-phase7-browser-result` version-1 record before its PASS marker."
    - "Importing the Phase 7 module for focused tests does not start children; zero/mismatch/non-pass/skip/TODO output still throws."
  </behavior>
  <action>
    Add failing tests first. In `local-verify.mjs`, export `PHASE6_DATA_RESULT_FORMAT = "blog-x-phase6-data-result"` and pure `createPhase6DataResult(suiteRecords)`. Make `runDatabaseSuite` return `parseSemanticTapResult`; collect the exact five database records, the Node TAP record and boundary record inside `runPhase6DataChecks`, emit one canonical `BLOG X PHASE6 DATA RESULT <json>` line, and return it. In `parallelCheck`, parse exactly one such line from each of two successful Phase 6 children, reject namespace/count/schema drift, and forward both canonical lines so the parent command exposes three records. Keep generated cleanup and BLOCKED checks mandatory.

    In `phase7-browser-verify.mjs`, export `PHASE7_BROWSER_RESULT_FORMAT = "blog-x-phase7-browser-result"`; change `assertPlaywrightResult` to return the exact six-field count object after its existing fail-closed checks. Emit one canonical `BLOG X PHASE7 BROWSER RESULT <json>` line with format/version/counts/releaseState before PASS, and guard `main()` with the same direct-entry pattern used by other ESM scripts so unit tests can import it without side effects. Keep the normal full path unfiltered and all forced diagnostic flags test-only.
  </action>
  <acceptance_criteria>
    <criterion>Every selected Phase 6 suite and the complete Phase 7 Playwright run contributes actual parser-derived counts; no remembered count or exit-code-only success is accepted.</criterion>
    <criterion>Phase 6 parent + two parallel child results are exact, nonzero, pass-only and cleanup-bound.</criterion>
    <criterion>Phase 7 emits exactly one full-run record and importing its parser starts no process.</criterion>
    <criterion>Malformed, missing, duplicate, zero, failed, cancelled, skipped, TODO, filtered or incomplete records fail tests.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/local-verify.test.mjs scripts/local-delivery-acceptance.test.mjs</automated>
  </verify>
  <done>Both established acceptance layers produce strict structured records that can be bound into delivery without weakening generated isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coordinate only the complete isolated v1.1 acceptance path</name>
  <files>scripts/local-delivery-acceptance.mjs, scripts/local-delivery-acceptance-test-core.mjs, scripts/local-delivery-acceptance.test.mjs</files>
  <read_first>
    <item>scripts/local-delivery-acceptance.test.mjs — Task 1 fixtures for strict Phase 6 and Phase 7 machine records.</item>
    <item>scripts/local-verify.mjs — exact parent/parallel Phase 6 command and result prefix.</item>
    <item>scripts/phase7-browser-verify.mjs — exact unfiltered command, result prefix and cleanup marker.</item>
    <item>scripts/refresh-local.mjs — minimal child environment/redaction conventions; do not add refresh authority here.</item>
    <item>scripts/refresh-local-runtime-core.mjs — exact argv and digest style that will consume this coordinator in 08-03.</item>
    <item>scripts/refresh-local-live.mjs — sealed zero-argument production assembly pattern; production dependencies are closed over and never caller-injected.</item>
    <item>scripts/refresh-local-test-core.mjs — test-only raw process/FS assembly pattern and explicit rule that production modules never import test-core.</item>
    <item>scripts/local-verify.mjs `createPhase5ResultRecorder` — count/digest shape analog only; do not reuse Phase 5 receipt authority.</item>
    <item>.planning/phases/08-reliable-local-delivery/08-CONTEXT.md — D-15 through D-18 and canonical-data separation.</item>
  </read_first>
  <behavior>
    - "The production export `runLocalDeliveryAcceptance()` accepts zero arguments only, closes over its sealed bounded spawn adapter, and runs exactly Node + `scripts/local-verify.mjs --phase6-data --interruption-check --parallel-check`, then Node + `scripts/phase7-browser-verify.mjs` with no grep."
    - "`createLocalDeliveryAcceptanceTestRuntime({ processBoundary })` exists only in `scripts/local-delivery-acceptance-test-core.mjs`; tests inject success/failure results through it, and the production module never imports or re-exports that seam."
    - "`parseLocalDeliveryAcceptanceOutputs` requires three Phase 6 records, one Phase 7 record, mandatory cleanup/BLOCKED markers and exact six-field nonzero pass-only totals."
    - "The result format is `blog-x-v1.1-local-delivery-acceptance`, version 1, with `phase6Data`, `phase7Browser`, aggregate `counts` and `releaseState: BLOCKED`; each layer stores only run count, result SHA-256, redacted-output SHA-256 and counts."
    - "Unknown CLI arguments, child nonzero/signal/timeout/output overflow, raw secret-bearing output, partial/old result formats and generated-origin leakage into the record fail."
  </behavior>
  <action>
    Create `scripts/local-delivery-acceptance.mjs` with exported `LOCAL_DELIVERY_ACCEPTANCE_FORMAT = "blog-x-v1.1-local-delivery-acceptance"`, version 1, pure `parseLocalDeliveryAcceptanceRecord(value)`, pure `parseLocalDeliveryAcceptanceOutputs({ phase6Output, phase7Output })`, and sealed `runLocalDeliveryAcceptance(...args)`. Reject any argument before process creation; the zero-argument implementation must close over a non-exported bounded minimal-environment production `spawn` adapter, capture a bounded combined stream, redact credentials/database URLs/session values, and execute only the two exact Node argv families in the stated order. Do not export a runner factory, dependency bag, process override or environment override from the production module.

    Create `scripts/local-delivery-acceptance-test-core.mjs` as the sole injectable assembly, exporting `createLocalDeliveryAcceptanceTestRuntime({ processBoundary })`; validate that exact boundary shape, record the two argv calls, and drive the shared pure parsers/digest/redaction contracts without being imported or re-exported by production code. Update `scripts/local-delivery-acceptance.test.mjs` to exercise orchestration failures through this test runtime and add source/API policy assertions that the production function is zero-argument/sealed and has no injectable signature. Parse exactly three Phase 6 records and one Phase 7 record, require all cleanup/BLOCKED markers, aggregate actual counts, and compute SHA-256 over canonical result records and normalized redacted outputs. Emit only `BLOG X V1.1 ACCEPTANCE RESULT <canonical-json>` plus a concise pass marker; never expose raw output, namespace, port, credentials or child environment in the result.

    Build table-driven tests for success and every missing/duplicate/zero/non-pass/skip/TODO/old-format/filtered-argv/nonzero/signal/timeout/overflow/redaction case. Assert the production source contains neither test-core imports/injectable runner parameters, `blogxlocal` Compose/migration/cutover authority, nor remote/server/deployment capability, and that no CLI option can select a subset.
  </action>
  <acceptance_criteria>
    <criterion>The production coordinator has one sealed zero-argument path and two exact child invocations only; passing any argument fails before process creation.</criterion>
    <criterion>`scripts/local-delivery-acceptance-test-core.mjs` is the only injectable orchestration boundary, is used by focused tests and is never imported or re-exported by production modules.</criterion>
    <criterion>Its record binds three full Phase 6 runs and one complete Phase 7 run with actual pass-only counts and two evidence digests per layer.</criterion>
    <criterion>No generated authority or raw child output crosses into the sanitized record.</criterion>
    <criterion>Every partial, stale, malformed, filtered, failed or unclean result is rejected in focused unit tests.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/local-delivery-acceptance.mjs &amp;&amp; node --check scripts/local-delivery-acceptance-test-core.mjs &amp;&amp; node --test scripts/local-delivery-acceptance.test.mjs scripts/local-verify.test.mjs &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>One sealed zero-argument production coordinator produces trustworthy machine-readable evidence for all disposable Phase 6/7 acceptance, while every injectable process/orchestration seam remains confined to the test-core module.</done>
</task>

</tasks>

## Atomic commit sequence

1. Commit strict Phase 6/7 machine records as `feat(08-02): expose complete acceptance results`.
2. Commit the sealed acceptance coordinator as `feat(08-02): bind isolated delivery acceptance`.
3. Commit `08-02-SUMMARY.md` separately; do not invoke the coordinator from canonical refresh until 08-03.

## Artifacts this phase produces

- New file `scripts/local-delivery-acceptance.mjs`.
- New file `scripts/local-delivery-acceptance-test-core.mjs` containing the sole `createLocalDeliveryAcceptanceTestRuntime({ processBoundary })` injectable assembly; production modules never import it.
- New file `scripts/local-delivery-acceptance.test.mjs`.
- `scripts/local-verify.mjs` exports `PHASE6_DATA_RESULT_FORMAT` and `createPhase6DataResult`; machine prefix `BLOG X PHASE6 DATA RESULT`.
- `scripts/phase7-browser-verify.mjs` exports `PHASE7_BROWSER_RESULT_FORMAT` and `assertPlaywrightResult`; machine prefix `BLOG X PHASE7 BROWSER RESULT`.
- `scripts/local-delivery-acceptance.mjs` exports `LOCAL_DELIVERY_ACCEPTANCE_FORMAT`, `parseLocalDeliveryAcceptanceRecord`, `parseLocalDeliveryAcceptanceOutputs`, and sealed zero-argument `runLocalDeliveryAcceptance`; machine prefix `BLOG X V1.1 ACCEPTANCE RESULT`.
- Schema `blog-x-phase6-data-result` version 1: `format`, `version`, `suites[]`, `counts`, `releaseState`.
- Schema `blog-x-phase7-browser-result` version 1: `format`, `version`, `counts`, `releaseState`.
- Schema `blog-x-v1.1-local-delivery-acceptance` version 1: `format`, `version`, `phase6Data`, `phase7Browser`, `counts`, `releaseState`; layer fields are `runs`, `resultSha256`, `outputSha256`, `counts`.
- No new operational CLI flag, Compose project/volume, canonical-data mutation, package dependency, schema, server or deployment capability is produced.

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Risk | Control |
|---|---|---|
| generated Phase 6 children -> coordinator | missing/duplicate/partial suite evidence | exact three-record schema and pass-only arithmetic |
| generated Phase 7 runner -> coordinator | filtered/zero/skipped browser result | unfiltered exact argv, discovered/passed equality and cleanup marker |
| captured child output -> receipt input | secret/topology disclosure | bounded capture, normalization, redaction and digest-only projection |
| coordinator -> canonical refresh | gaining Compose/data authority or exposing production dependency injection | acceptance-only result module with no blogxlocal command family; raw process injection exists only in test-core and production never imports it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-02-01 | Spoofing | machine result lines | high | mitigate | Exact format/version/key/count schemas and required parent/two-child/full-browser cardinality. |
| T-08-02-02 | Tampering | partial/filtered acceptance | high | mitigate | Fixed argv, no grep/subset option and source-selected complete suites. |
| T-08-02-03 | Repudiation | suite execution | high | mitigate | Parser-derived counts and canonical result/output digests bind every run. |
| T-08-02-04 | Information Disclosure | child logs/results | high | mitigate | Redact bounded output and publish only counts/digests/BLOCKED. |
| T-08-02-05 | Denial of Service | local child runners | medium | accept | Bounded time/output and exact cleanup may fail the local gate; no unrelated process/data is touched. |
| T-08-02-06 | Elevation of Privilege | coordinator command surface | high | mitigate | Zero-argument sealed production entry, test-core-only process injection and two literal Node argv families without Compose or remote adapters. |
</threat_model>

<verification>
- Run both task commands and confirm all parser/record/negative-path tests execute without skip/TODO.
- Confirm the full execution argv contains interruption + parallel Phase 6 and no-argument Phase 7, with no grep or partial selector.
- Confirm the production module rejects every argument before spawn, exposes no runner/dependency injection and never imports `scripts/local-delivery-acceptance-test-core.mjs`.
- Confirm `blogxlocal`, canonical volumes and port 3100 are absent from coordinator command authority and generated cleanup remains exact.
</verification>

<success_criteria>
- Full isolated Phase 6 and Phase 7 results are machine-readable, exact-counted and digest-bound.
- The production coordinator stays sealed and zero-argument while the injectable orchestration seam is isolated in `scripts/local-delivery-acceptance-test-core.mjs`.
- Partial, zero, skipped, stale, malformed or failed evidence cannot become a delivery acceptance pass.
- Canonical user data and fixed port remain outside exhaustive test-fixture authority.
- Acceptance output remains sanitized and explicitly `BLOCKED`.
</success_criteria>

<output>
Create `.planning/phases/08-reliable-local-delivery/08-02-SUMMARY.md` when done.
</output>
