---
phase: 08-reliable-local-delivery
plan: "08"
type: execute
wave: 8
depends_on: ["08-07"]
files_modified:
  - scripts/reviewed-delivery-gate.mjs
  - scripts/reviewed-delivery-gate.test.mjs
  - .planning/phases/08-reliable-local-delivery/08-REVIEW.md
  - .planning/phases/08-reliable-local-delivery/08-REVIEW-FIX.md
  - .planning/phases/08-reliable-local-delivery/08-08-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [DEVX-01, DEVX-02, DEVX-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Configured `workflow.code_review` is active and reviews the final implementation scope from Plans 08-04..08-07 plus the reviewed-delivery gate before any formal delivery."
    - "Critical/warning findings are fixed and committed, then re-reviewed for at most three GSD auto-fix rounds; delivery is blocked unless the final verdict is `clean`."
    - "The final committed `08-REVIEW.md` uses the existing GSD report format; `08-REVIEW-FIX.md` records any applied fixes, and no REVIEW artifact changes after delivery starts."
    - "This plan finishes normally: its tasks commit review/fix artifacts, then standard execute-plan creates and commits `08-08-SUMMARY.md`; no reviewed marker or formal delivery occurs inside this plan."
    - "After that normal summary commit, a terminal root-orchestrator handoff performs one read-only exact-HEAD review into `/private/tmp`, then invokes Plan 08-09 directly rather than resuming execute-phase's post-review hook."
    - "A repository gate can atomically record that later read-only review, derive receipt/claim/failure from its validated SHA, enforce preflight equality, permit finite descendant evidence verification and prove REVIEW digest immutability."
  artifacts:
    - path: scripts/reviewed-delivery-gate.mjs
      provides: fail-closed reviewed-HEAD marker, clean preflight and derived evidence verification
    - path: .planning/phases/08-reliable-local-delivery/08-REVIEW.md
      provides: committed clean GSD review report for final implementation scope
    - path: .planning/phases/08-reliable-local-delivery/08-08-SUMMARY.md
      provides: normal execute-plan closeout after committed review/fix/re-review
  key_links:
    - from: .planning/phases/08-reliable-local-delivery/08-REVIEW.md
      to: scripts/reviewed-delivery-gate.mjs
      via: clean review verdict followed by exact-HEAD read-only attestation
      pattern: "status: clean|Reviewed HEAD"
    - from: scripts/reviewed-delivery-gate.mjs
      to: scripts/refresh-local-runtime-core.mjs
      via: shared `deliveryAuthorityForRevision` derivation
      pattern: "deliveryAuthorityForRevision"
---

<objective>
Place a clean configured GSD code-review gate immediately before formal delivery and bind its exact reviewed HEAD to runnable delivery checks.

Purpose: ensure fixes and normal plan closeout finish before the irreversible per-SHA attempt, with no review or source mutation hidden in the post-delivery delta.
Output: reviewed-delivery helper, committed clean review/fix artifacts and a normal 08-08 summary ready for the terminal read-only-review handoff.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/code-review.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/code-review-fix.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/config.json
@.planning/phases/08-reliable-local-delivery/08-04-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-05-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-06-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-07-SUMMARY.md
@.planning/phases/08-reliable-local-delivery/08-REVIEW.md
@.planning/phases/08-reliable-local-delivery/08-REVIEW-FIX.md
@scripts/refresh-local-runtime-core.mjs
@scripts/refresh-local.mjs

<interfaces>
- `.planning/config.json` has `workflow.code_review: true`, standard depth. The existing GSD code-review and code-review-fix workflows own report format, reviewer/fixer roles, exact summary/diff scope and an auto loop capped at three iterations.
- Review-discovered fixes may modify any implementation file in the exact 08-04..08-07 summary scope; those dynamic files are owned exclusively by this sequential review plan until a clean re-review and are committed before the final report.
- Fixed local handoff paths are `/private/tmp/blog-x-phase08-final-review.md` and `/private/tmp/blog-x-phase08-reviewed-head-v1.json`. They contain no secret and are not repository evidence; neither is written during this plan.
- Plan 08-04 excludes REVIEW files from post-delivery tolerance. Therefore review reports, fixes and this plan's summary must all be committed before the reviewed delivery SHA is captured.
- Normal `execute-plan` creates/commits SUMMARY only after plan tasks. Therefore exact-HEAD review and marker creation deliberately occur outside this plan, after that mandatory closeout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a runnable reviewed-HEAD delivery gate</name>
  <files>scripts/reviewed-delivery-gate.mjs, scripts/reviewed-delivery-gate.test.mjs</files>
  <read_first>
    <item>scripts/refresh-local-runtime-core.mjs — strict SHA authority derivation and diagnostic verifier.</item>
    <item>scripts/refresh-local.mjs — exact diagnostic CLI boundary.</item>
    <item>Existing 08-REVIEW.md — GSD status and Reviewed HEAD format.</item>
  </read_first>
  <behavior>
    - "`--assert-committed-review-clean` validates repository `08-REVIEW.md` has GSD format, status clean and zero findings without creating a marker."
    - "`--assert-handoff-absent` requires both fixed final-review and marker paths absent, proving this plan did not start the terminal handoff."
    - "`--record-reviewed-head` accepts no path/SHA override, requires clean `dev`, parses one clean final temp GSD report whose Reviewed HEAD equals current lowercase 40-hex HEAD, snapshots committed `08-REVIEW.md` digest, and atomically creates the fixed marker only if absent."
    - "`--assert-reviewed-head` requires marker schema/permissions/digest, current clean HEAD equality and no ambiguous marker."
    - "`--preflight-delivery` additionally derives exact receipt/claim/failure through `deliveryAuthorityForRevision` and requires all absent."
    - "`--verify-evidence` derives exactly one receipt from recorded SHA and invokes the strict finite-descendant diagnostic verifier without requiring current HEAD equality; `--assert-review-unchanged` compares current `08-REVIEW.md` to the marker snapshot, and `--assert-clean` fails on any porcelain output including untracked files."
  </behavior>
  <action>
    Write fault-injected tests first. Implement one zero-authority helper with the exact fixed modes above, strict JSON/path/SHA/owner/mode/realpath checks and literal child argv. Reuse exported production derivation; do not reconstruct paths loosely. Equality to current HEAD is mandatory for marker recording, `--assert-reviewed-head` and `--preflight-delivery` only. Evidence verification instead uses the recorded SHA and production verifier's finite descendant contract after receipt/docs commits. Reject extra args, env overrides, symlinks, stale/multiple markers, dirty state, wrong branch, malformed review, consumed authority and path aliases while leaving other SHA-addressed history untouched. Do not create either fixed `/private/tmp` artifact or call formal delivery in this plan.
  </action>
  <verify><automated>node --test scripts/reviewed-delivery-gate.test.mjs &amp;&amp; node --check scripts/reviewed-delivery-gate.mjs &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --check</automated></verify>
  <done>A directly runnable helper fail-closes exact review identity, clean state, unconsumed authority and independent derived receipt verification.</done>
</task>

<task type="auto">
  <name>Task 2: Run configured review/fix/re-review and finish before normal summary closeout</name>
  <files>.planning/phases/08-reliable-local-delivery/08-REVIEW.md, .planning/phases/08-reliable-local-delivery/08-REVIEW-FIX.md, review-discovered implementation files from committed 08-04..08-07 summaries</files>
  <read_first>
    <item>GSD code-review.md and code-review-fix.md workflows — exact configured process and report format.</item>
    <item>08-04..08-07 summaries — final implementation file scope and verification evidence.</item>
    <item>.planning/config.json — active review gate and depth.</item>
  </read_first>
  <behavior>
    - "All dependent implementation and summaries are committed and worktree clean before review."
    - "GSD review/fix auto loop is bounded at three; every finding fix is committed and the same complete file scope is re-reviewed."
    - "Final repository report status is clean with zero critical/warning/info findings; otherwise this plan stops and delivery remains blocked."
    - "Tasks end with committed clean REVIEW/FIX artifacts and a clean worktree; normal execute-plan then creates/commits 08-08-SUMMARY."
  </behavior>
  <action>
    Assert Plans 08-04..08-07 and their summaries are committed and clean, then execute the configured Phase 08 GSD code-review workflow at standard depth with `--fix --all --auto`. Supply an exact `--files` override equal to the union of implementation files recorded by 08-04..08-07 summaries plus `scripts/reviewed-delivery-gate.mjs` and its test, so the helper added in Task 1 cannot escape review. Allow at most the workflow's built-in three fix/re-review rounds; commit every fix before re-review and preserve that identical file list for every iteration. If it cannot reach `status: clean` with zero findings, stop without marker or delivery eligibility.

    Commit the final `08-REVIEW.md` and `08-REVIEW-FIX.md` (when changed), and delete only untracked workflow-generated `08-REVIEW.iter2.md`, `08-REVIEW.iter3.md`, `08-REVIEW-FIX.iter2.md` and `08-REVIEW-FIX.iter3.md` backups after their outcome is captured. Validate the committed report with `--assert-committed-review-clean`, finish tasks with a clean worktree, and return normally so execute-plan creates and commits `08-08-SUMMARY.md` through its standard closeout. Do not write either fixed handoff artifact and do not run `--record-reviewed-head`, formal delivery or an exact-HEAD final review inside this plan.
  </action>
  <verify><automated>node scripts/reviewed-delivery-gate.mjs --assert-committed-review-clean &amp;&amp; node scripts/reviewed-delivery-gate.mjs --assert-handoff-absent &amp;&amp; node scripts/reviewed-delivery-gate.mjs --assert-clean &amp;&amp; node --test scripts/reviewed-delivery-gate.test.mjs &amp;&amp; git diff --check</automated></verify>
  <done>Configured review/fix/re-review is clean and committed; tasks return to normal execute-plan summary closeout with no marker or delivery side effect.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| Threat | Severity | Control |
|---|---|---|
| Delivery precedes review fixes | high | Formal plan depends on normal completion of this review plan and its summary. |
| Summary changes HEAD after review tasks | high | Marker is intentionally deferred until after standard summary closeout. |
| Dirty/untracked drift after review | high | Porcelain emptiness assertion including untracked files. |
| Marker/path substitution | high | Fixed secure path, schema, permissions, digest and no overrides. |
| Review artifact hidden post-delivery | high | REVIEW committed before marker; production verifier excludes REVIEW delta. |
</threat_model>

<verification>
- Configured GSD review/fix/re-review reaches clean within three rounds.
- Final committed review artifacts are clean; normal execute-plan summary closeout remains enabled.
- Helper tests and committed-review/clean assertions pass; no marker exists yet.
- Formal delivery, Docker mutation and servers are untouched.
</verification>

<success_criteria>
- Final implementation has a clean configured GSD review with fixes committed.
- Standard 08-08 summary commits before the terminal exact-HEAD handoff.
- No review/source change is possible within permitted post-delivery delta.
</success_criteria>

## Terminal one-shot orchestration contract

After normal execute-plan creates/commits `08-08-SUMMARY.md` and confirms a clean worktree, the root orchestrator MUST:

1. Confirm `workflow.code_review` remains enabled at its configured depth, then run one execute:post-equivalent `gsd-code-reviewer` pass over the exact final implementation scope used in Task 2, writing only `/private/tmp/blog-x-phase08-final-review.md`. It is read-only with respect to the repository and must report `status: clean`, zero findings and Reviewed HEAD equal current HEAD.
2. Execute Plan 08-09 directly through its `execute-plan` tasks. Do not resume execute-phase automatic sequencing or its post-review hook.
3. Treat the configured code-review capability as consumed for this terminal delivery chain. This is a one-shot compatibility rule for execute-phase ordering, not a config change or disabled review gate.

No marker is created between steps 1 and 2; Plan 08-09 Task 1 owns atomic recording immediately before preflight/delivery.

<output>Create `.planning/phases/08-reliable-local-delivery/08-08-SUMMARY.md` through normal execute-plan closeout. Do not create a reviewed marker in this plan.</output>
