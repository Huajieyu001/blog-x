---
phase: 13-responsive-admin-workspace-and-local-delivery
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
  - apps/web/e2e/admin-analytics.spec.ts
  - ops/local-deliveries/${implementationRevision}.json
autonomous: true
requirements: [ADMN-04, QUAL-01, QUAL-02]
user_setup: []
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "About, taxonomy, audit, content and analytics failures are independently visible and recoverable without rendering partial editors or leaking protected content."
    - "The canonical local integration authority can execute the expanded administrator analytics/workspace browser file with all generated failure facts present."
    - "The final suite proves authenticated analytics, content lifecycle, scheduling, editing recovery, soft delete, audit redaction, public reading, backup recovery and same-origin boundaries without skips."
    - "The final source revision is refreshed once to healthy fixed port 3100 and every delivery artifact still says production is BLOCKED."
  assumptions:
    - "Planner assumption for ADMN-04 (spec-less edge probe: unclassified/unresolved): route-specific recovery copy is acceptable when headings, controls, cards and truthful no-mutation outcomes share the workspace language and preserve redaction/recovery."
    - "Planner assumption for QUAL-01 (spec-less edge probe: unclassified/unresolved): the existing canonical generated integration inventory is the regression authority; extending its missing failure-fixture wiring is preferable to a second full harness."
    - "Planner assumption for QUAL-02 (spec-less edge probe: unclassified/unresolved): one immutable clean-SHA attempt plus revision-bound receipt/health proof satisfies each plan-level delivery step; a terminal failed SHA is recorded and never retried."
  artifacts:
    - path: scripts/local-verify.mjs
      provides: "Canonical generated failure-fixture ownership for administrator workspace acceptance"
    - path: scripts/local-verify.test.mjs
      provides: "Fail-closed contract tests for Phase 12 and canonical failure-fixture lifecycle"
    - path: apps/web/e2e/admin-analytics.spec.ts
      provides: "Browser evidence for independent secondary-route failures"
    - path: ops/local-deliveries/${implementationRevision}.json
      provides: "Final revision-bound local delivery evidence with production BLOCKED"
  key_links:
    - from: scripts/local-verify.mjs
      to: apps/web/e2e/admin-analytics.spec.ts
      via: "Both Phase 12 and canonical main-browser runs provision the exact generated failure Web/API origins before the spec executes"
      pattern: "startPhase12FailureFixtures"
    - from: scripts/local-verify.mjs
      to: scripts/test-inventory.mjs
      via: "Canonical selection continues to derive every suite and fixture owner from the sealed inventory"
      pattern: "canonicalIntegrationSelection"
    - from: scripts/refresh-local.mjs
      to: http://127.0.0.1:3100
      via: "Clean revision cutover followed by fixed loopback health and evidence verification"
      pattern: "FIXED_REFRESH"
  prohibitions:
    - statement: "统计 UI 不得将匿名 PV 表述为独立访客或 UV。"
      status: unverified
      flagged: true
      verification: judgment
    - statement: "视觉统一不得弱化审计脱敏、编辑恢复、危险操作确认、预约和无脚本提交语义。"
      status: unverified
      flagged: true
      verification: judgment
    - statement: "本地交付收据不得暗示生产已经部署或解除冻结；releaseState 必须保持 BLOCKED。"
      status: unverified
      flagged: true
      verification: judgment
---

<objective>
Close the real canonical-runner gap exposed by the existing administrator failure spec, prove the complete local regression inventory, and deliver the final Phase 13 revision to the fixed local preview.

Purpose: Finish Phase 13 using the established generated integration and refresh authorities, without another testing framework, schema change, dependency or production operation.
Output: Secondary-route failure evidence, canonical fixture lifecycle correction, complete regression pass, and final fixed-preview receipt.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-administrator-insights/12-03-SUMMARY.md
@.planning/phases/13-responsive-admin-workspace-and-local-delivery/13-01-SUMMARY.md
@apps/web/app/lib/api.ts
@apps/web/app/admin/about/page.tsx
@apps/web/app/admin/taxonomy/page.tsx
@apps/web/app/admin/audit/page.tsx
@apps/web/app/admin/_components/AdminAnalytics.tsx
@apps/web/e2e/admin-analytics.spec.ts
@scripts/test-inventory.mjs
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/refresh-local.mjs

<interfaces>
- `phase12FailureFixtureProcess()` currently controls `analytics-failure` and `content-failure`, while `startPhase12FailureFixtures(context)` supplies `failureFixtureOrigin` and `failureWebOrigin` only during `phase12Data`.
- `seedMainBrowserScenario(context, "apps/web/e2e/admin-analytics.spec.ts")` always exports those failure origins; therefore canonical integration must own the same fixture lifecycle before it runs that inventory item.
- `runCanonicalIntegrationChecks(context)` executes database owners, the canonical main-browser selection, error-browser recovery, restore, and returns suite records in sealed inventory order.
- `runSingle(options)` already guarantees generated namespace cleanup, redacted diagnostics, current-runtime sealing and release BLOCKED behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize administrator failure facts and close canonical fixture ownership</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs, apps/web/e2e/admin-analytics.spec.ts</files>
  <read_first>
    - scripts/local-verify.mjs — `phase12FailureFixtureProcess`, `startPhase12FailureFixtures`, `seedMainBrowserScenario`, `runPhase12DataChecks`, `runCanonicalIntegrationChecks`, `stopManaged` and generated diagnostics.
    - scripts/local-verify.test.mjs — Phase 12 fixture assertions, canonical main-browser selection and pass-only result tests.
    - apps/web/app/lib/api.ts — exact SSR paths for About, taxonomy and audit reads.
    - apps/web/app/admin/about/page.tsx — failure path must omit the editor and preserve existing content.
    - apps/web/app/admin/taxonomy/page.tsx — category/tag failures stay independent.
    - apps/web/app/admin/audit/page.tsx — redacted failure/empty/list contract.
    - apps/web/e2e/admin-analytics.spec.ts — existing isolated analytics/content failure controls and new workspace coverage from Plan 01.
  </read_first>
  <behavior>
    - Generated controls can select analytics-only, content-only or secondary-management failures without exposing an unbounded proxy.
    - About failure renders recovery text and no editor; taxonomy can report category/tag failures; audit failure renders no event identifiers or protected values.
    - Phase 12 data and canonical integration both start the generated failure API/Web before the administrator workspace spec and stop them in `finally`.
    - Missing, non-loopback or fixed-port failure facts fail before Playwright runs; output remains redacted and cleanup is confirmed.
  </behavior>
  <action>
    Add failing runner tests first. Extend the existing generated failure process with exact allowlisted controls and strict JSON responses for `/admin/about`, `/admin/categories`, `/admin/tags`, and `/admin/audit-events`; do not forward arbitrary paths or accept external origins. Permit the fixture starter under Phase 12 data or canonical integration, retaining distinct generated loopback ports and the fixed-port rejection. Wrap the canonical main-browser execution in start/stop ownership so `admin-analytics.spec.ts` receives valid failure facts there as it already does under Phase 12. Extend that browser file to select the secondary-failure scenario and assert the About, taxonomy and audit recovery states, absence of partial editors/data, retained analytics PV wording, and responsive shell. Preserve all existing authentication, no-store fetches, audit redaction, editor recovery and failure independence.
  </action>
  <acceptance_criteria>
    - Both sealed execution paths provision and clean the same bounded generated failure authority exactly once per run.
    - Canonical integration no longer reaches `createMainBrowserEnvironment` with undefined analytics failure facts.
    - About, taxonomy and audit failure pages are truthful, recoverable and do not render protected or partial data.
    - The fixture binds only generated `127.0.0.1` origins, emits no credential/session values and cannot proxy arbitrary requests.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/local-verify.test.mjs
corepack pnpm local:verify -- --phase12-data</automated>
  </verify>
  <done>The expanded administrator failure matrix passes under the sealed Phase 12 path and canonical integration owns every fixture fact the browser inventory consumes.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Phase 13 release-preparation regression inventory</name>
  <files>scripts/local-verify.mjs, scripts/test-inventory.mjs, apps/web/e2e/admin-analytics.spec.ts, apps/web/e2e/article-lifecycle.spec.ts, apps/web/e2e/auth-session.spec.ts</files>
  <read_first>
    - scripts/test-inventory.mjs — exact default/integration ownership and canonical grouping.
    - scripts/local-verify.mjs — `canonicalIntegrationSelection`, `runCanonicalIntegrationChecks` and sealed canonical CLI rules.
    - apps/web/e2e/article-lifecycle.spec.ts — publish/schedule/no-script/soft-delete/audit coverage.
    - apps/web/e2e/draft-preview.spec.ts — edit recovery, stale version and responsive preview coverage.
    - apps/web/e2e/public-reading.spec.ts — same-origin public view and privacy boundary coverage.
    - apps/api/test/backup-restore.test.ts — aggregate/content recovery parity consumed by canonical restore.
  </read_first>
  <action>
    Run the ordinary default suite, Web typecheck/build, the focused Phase 12 administrator selector, and then the canonical generated integration with its required interruption and parallel flags. Treat any skipped, TODO, flaky, failed, missing-owner, missing-cleanup, unredacted, cross-origin or non-BLOCKED result as failure. The canonical run is release-preparation work and must cover every sealed inventory owner, including analytics contracts/database behavior, authenticated administrator journeys, edit/schedule/publish/delete/audit paths, public reading, backup restore, security boundaries and generated cleanup. Do not add another receipt format, duplicate the inventory, change schema, install packages, connect to a server or relax pass-only parsers to obtain a green result.
  </action>
  <acceptance_criteria>
    - Default tests, Web typecheck and Web production build pass.
    - The focused administrator selector passes every expanded workspace/failure test.
    - Canonical integration executes each current inventory item exactly once with pass-only counts and both lifecycle probes.
    - Generated databases, media roots, ports, child processes, containers and volumes are absent after completion.
    - The terminal result explicitly reports local success with release/production BLOCKED.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm test
corepack pnpm --filter @blog-x/web typecheck
PUBLIC_ORIGIN=http://127.0.0.1:3100 corepack pnpm --filter @blog-x/web build
corepack pnpm local:verify -- --phase12-data
corepack pnpm local:verify -- --canonical-integration --interruption-check --parallel-check</automated>
  </verify>
  <done>The complete generated local regression authority passes without skips, leaks, residue or any implication of production readiness.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh and health-check the final Phase 13 source revision once</name>
  <precondition>`dev` is clean, HEAD contains Task 1's committed runner/browser correction, HEAD differs from every prior terminal refresh SHA, and no attempt claim exists for that HEAD.</precondition>
  <files>ops/local-deliveries/${implementationRevision}.json</files>
  <read_first>
    - scripts/refresh-local.mjs — fixed no-argument refresh and clean-revision authority.
    - scripts/refresh-local-live.mjs — production-shaped local adapter and evidence verification.
    - ops/release-evidence.blocked.json — canonical production decision that must remain BLOCKED.
    - AGENTS.md — server freeze, `dev`-only and fixed-preview rules.
  </read_first>
  <action>
    After Task 1 is committed/pushed and Task 2 has passed, record the full clean HEAD and invoke the sealed refresh command exactly once for that new SHA. Never reuse or retry 8cdb839 or any other claimed SHA. If the attempt fails, preserve its immutable failure evidence and stop only this revision's refresh; do not mutate fixed containers by hand. If it succeeds, verify the revision-addressed receipt through the read-only evidence mode, confirm the fixed page and `/api/health`, confirm the active image labels bind the implementation SHA, then commit and push the receipt on `dev`. Keep production, TLS, server addresses and credentials outside the command environment.
  </action>
  <acceptance_criteria>
    - The final implementation SHA has exactly one terminal refresh attempt.
    - Success binds active API/Web labels and receipt to that SHA, keeps fixed 3100 healthy and records `releaseState: BLOCKED`.
    - Failure remains immutable and is not retried or concealed by manual container mutation.
    - The final branch is `dev`, pushed to `origin/dev`, with `main` and all servers untouched.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/refresh-local.mjs
curl -fsS http://127.0.0.1:3100/
curl -fsS http://127.0.0.1:3100/api/health
git status --short --branch</automated>
  </verify>
  <done>The final Phase 13 implementation has one terminal refresh result and, on success, the fixed local preview serves that exact revision with production still BLOCKED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated failure control → Next SSR administrator fetches | Only allowlisted loopback responses may simulate independently unavailable management domains. |
| Canonical inventory → generated Docker/database/browser authorities | Test ownership drives local mutations and must retain exact namespace, cleanup and pass-only accounting. |
| Clean Git SHA → fixed local refresh receipt | One immutable claim authorizes a local cutover but never authorizes production. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Spoofing / Tampering | generated failure origins | high | mitigate | Validate distinct loopback origins, reject port 3100/external facts, expose only exact scenario controls and start/stop under verifier ownership. |
| T-13-06 | Information Disclosure | failure pages and verifier diagnostics | high | mitigate | Preserve audit/analytics allowlists, no-store states and `redactText`; assert partial editors, identifiers, visitor claims and secret values are absent. |
| T-13-07 | Tampering | canonical suite/result authority | high | mitigate | Derive suite paths/owners from `PACKAGE_TEST_INVENTORY`, require pass-only counts, current runtime digests, interruption/parallel probes and confirmed cleanup. |
| T-13-08 | Denial of Service | generated children/containers | medium | mitigate | Reuse bounded free ports, managed child termination, namespace validation and convergent generated cleanup; fail on residue. |
| T-13-09 | Elevation | local refresh vs production | high | mitigate | Accept only clean committed `dev` SHA, fixed loopback topology and immutable BLOCKED evidence; no network/server/deploy adapter is invoked. |
| T-13-SC | Tampering | package supply chain | high | mitigate | No dependency installation or manifest change is planned; halt on package/lockfile drift. |
</threat_model>

<verification>
- Focused: local verifier contract tests and the sealed Phase 12 analytics/workspace selector.
- Release preparation: default tests, Web typecheck/build, then canonical integration with interruption/parallel probes.
- Delivery: one formal refresh for the final new source SHA, read-only receipt verification and fixed 3100 page/API health.
- Subjective visual taste remains a final non-blocking human UAT note; no extra artifact or execution pause is created.
</verification>

<success_criteria>
- Secondary management failures remain independent, honest and non-destructive while audit/analytics privacy wording is preserved.
- Canonical integration supplies every generated fact required by the expanded administrator browser suite.
- The full local inventory passes with exact ownership, pass-only counts, recovery/security coverage and complete cleanup.
- The latest eligible `dev` source revision has one terminal fixed-preview refresh outcome and production remains BLOCKED.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Consistent accessible admin work at phone/tablet/desktop plus current verified local preview | 13-01, 13-02 | COVERED | Shell/presentation first, canonical delivery second. |
| REQ | ADMN-01 | Unified active navigation, logout, mobile close/Escape/focus | 13-01 | COVERED | Task 1 source correction plus generated browser journey. |
| REQ | ADMN-03 | Compact article facts and progressive lifecycle/destructive controls | 13-01 | COVERED | Task 2 reuses existing ArticleActions and proves disclosure/confirmation. |
| REQ | ADMN-04 | Consistent taxonomy/About/audit/analytics states without redaction/recovery regression | 13-01, 13-02 | COVERED | Loaded hierarchy plus independent generated failures. |
| REQ | ADMN-05 | 390/768/1280, 44px, focus and light/dark/system | 13-01 | COVERED | Exact browser geometry and theme outcomes. |
| REQ | QUAL-01 | Analytics/admin/key-flow canonical local regression | 13-02 | COVERED | Existing sealed inventory, repaired fixture ownership, full final run. |
| REQ | QUAL-02 | Per-step fixed local refresh and production BLOCKED | 13-01, 13-02 | COVERED | One clean-SHA attempt after each plan's source increment. |
| RESEARCH | — | No RESEARCH.md exists | — | EXCLUDED | Project config disables research; established codebase patterns and no new dependency make this Level 0 discovery. |
| CONTEXT | — | No CONTEXT.md or D-XX decisions exist | — | EXCLUDED | Roadmap success criteria and explicit orchestrator constraints are the authority. |

## Artifacts this phase produces

- Bounded secondary-route failure behavior inside the existing generated local verifier.
- Contract tests proving Phase 12 and canonical fixture lifecycle ownership.
- Expanded browser acceptance in the already owned administrator analytics/workspace spec.
- Final canonical regression output and one revision-addressed local delivery receipt when refresh succeeds.

<output>
Create `.planning/phases/13-responsive-admin-workspace-and-local-delivery/13-02-SUMMARY.md` when done; include exact focused/canonical/refresh outcomes and keep production explicitly BLOCKED.
</output>
