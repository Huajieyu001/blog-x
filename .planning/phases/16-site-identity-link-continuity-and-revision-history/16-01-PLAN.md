---
phase: 16
plan: "01"
name: site-identity-delivery-audit
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/site-settings.ts
  - apps/api/src/content/site-settings-repository.ts
  - apps/api/src/routes/site-settings.ts
  - apps/api/test/site-settings.test.ts
  - apps/web/app/layout.tsx
  - apps/web/app/admin/_components/SettingsEditor.tsx
  - apps/web/e2e/admin-analytics.spec.ts
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "An administrator can change name, description and public information with stale-write protection, and the next public request uses the saved identity."
    - "The ICP number and MIIT link remain the fixed safe values in contracts, database response, editor guidance and public footer regardless of ordinary settings input."
    - "Identity failures are honest and same-origin; neither an unauthenticated caller nor a malformed response can change or leak settings."
  artifacts:
    - path: apps/api/test/site-settings.test.ts
      provides: "Disposable API proof of default, mutation, stale, ICP-invariant and public projection behavior."
    - path: apps/web/e2e/admin-analytics.spec.ts
      provides: "Responsive browser proof that a saved identity reaches the public shell safely."
---

# Phase 16 Plan 01: Site Identity Delivery Audit

<objective>
Close the remaining SITE-01 end-to-end evidence gap by proving existing identity authority reaches the public shell while the fixed ICP safety invariant survives normal edits and hostile input.
</objective>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Bind identity mutation to the strict public projection</name>
  <files>packages/contracts/src/site-settings.ts, apps/api/src/content/site-settings-repository.ts, apps/api/src/routes/site-settings.ts, apps/api/test/site-settings.test.ts</files>
  <read_first>packages/contracts/src/site-settings.ts; apps/api/src/content/site-settings-repository.ts; apps/api/src/routes/site-settings.ts; apps/api/test/site-settings.test.ts; apps/api/src/db/schema.ts</read_first>
  <action>Add focused disposable-database regressions before changing behavior. Prove an authenticated versioned save returns the strict admin shape and the next public projection reflects only name/description/publicInfo while registrationNumber/URL stay literal defaults. Cover stale writers, malformed/extra registration input, wrong Origin/content-type, anonymous access, audit redaction and failed persistence. If a test exposes a real divergence, apply the smallest contract/repository/route correction; preserve the existing singleton/advisory lock and do not add schema or migration work.</action>
  <acceptance_criteria>
    - Ordinary content fields cannot alter, omit or replace the ICP number/link.
    - No response/audit includes credentials or unrelated settings/storage data.
    - Missing or malformed upstream data fails closed rather than rendering a partial identity.
  </acceptance_criteria>
  <verify>corepack pnpm --filter @blog-x/api test -- site-settings.test.ts; corepack pnpm --filter @blog-x/api typecheck</verify>
  <done>SITE-01 has a verified API-to-public-projection slice, with code changed only if the focused test found a real gap.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove responsive settings-to-footer delivery</name>
  <files>apps/web/app/layout.tsx, apps/web/app/admin/_components/SettingsEditor.tsx, apps/web/e2e/admin-analytics.spec.ts</files>
  <read_first>apps/web/app/layout.tsx; apps/web/app/admin/_components/SettingsEditor.tsx; apps/web/e2e/admin-analytics.spec.ts; apps/web/app/lib/api.ts; apps/web/app/layout.test.mjs</read_first>
  <action>Extend the existing generated administrator browser journey to save an identity, navigate to a public route in a fresh request, and assert the public header/metadata/footer use the changed permitted fields while the MIIT anchor remains exact. Retain 390px/desktop containment, client validation focus, pending/stale recovery, same-origin requests and beforeunload protection. If this exposes a user-visible propagation defect, make the narrow rendering/cache correction and refresh local preview only after the committed correction; otherwise retain current accepted preview and do not create a refresh receipt.</action>
  <acceptance_criteria>
    - Browser proof covers save success, invalid input, public propagation and immutable ICP footer at mobile and desktop widths.
    - The page never treats registration details as editable input or silently falls back after a successful valid save.
  </acceptance_criteria>
  <verify>/bin/zsh /tmp/blog-x-auth-e2e-run.sh; corepack pnpm --filter @blog-x/web typecheck; PUBLIC_ORIGIN=http://127.0.0.1:3100 corepack pnpm --filter @blog-x/web build</verify>
  <done>SITE-01 is either confirmed by targeted real-browser evidence or corrected in the smallest runnable increment.</done>
</task>
</tasks>

<threat_model asvs="L1" block_on="high">
| ID | Threat | Severity | Blocking mitigation |
|---|---|---|---|
| T16-01-1 | Ordinary edit removes or changes legally required ICP disclosure | high | Literal contract/database/footer assertions in both tasks. |
| T16-01-2 | Cross-origin or anonymous identity mutation | high | Task 1 preserves session-first mutation guard and tests 401/403/415 paths. |
| T16-01-3 | Stale write overwrites another administrator state | high | Version/advisory-lock stale regression is required. |
</threat_model>

## Verification and success criteria

- Targeted API/browser tests and typechecks pass; no schema change, dependency, server connection, or deployment is allowed.
- This plan does not own a formal refresh unless its focused tests require a user-visible code correction; Phase 16’s only formal clean-SHA refresh belongs to Plan 03 if any product code changes.

## Artifacts produced

- SITE-01 API and responsive public-shell delivery evidence, plus only minimal corrective code if the audit finds a defect.
