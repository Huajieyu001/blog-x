---
phase: 16
plan: "03"
name: bounded-revision-recovery-audit
type: tdd
wave: 2
depends_on: [16-02]
files_modified:
  - apps/api/src/content/admin-repository.ts
  - apps/api/src/content/article-service.ts
  - apps/api/src/routes/admin-posts.ts
  - apps/api/test/article-revisions.test.ts
  - apps/web/app/admin/_components/ArticleRevisionHistory.tsx
  - apps/web/e2e/article-revisions.spec.ts
autonomous: true
requirements: [CONT-10]
must_haves:
  truths:
    - "Each article retains a bounded newest-first server-side revision history; content is absent from summary list and no more than the declared capacity remains."
    - "An administrator can inspect a strict selected snapshot and explicitly restore it only with the current version; restore saves the displaced content as history and always returns the article to an unpublished draft."
    - "Revision restore preserves slug continuity through the LINK-01 authority, retains media/taxonomy validation, and exposes content-free audit evidence."
  artifacts:
    - path: apps/api/test/article-revisions.test.ts
      provides: "Disposable capacity, stale, restore-to-draft, audit and redirect-continuity regression coverage."
    - path: apps/web/e2e/article-revisions.spec.ts
      provides: "Generated comparison/confirmation/focus/recovery browser proof."
---

# Phase 16 Plan 03: Bounded Revision Recovery Audit

<objective>
Prove and close any real CONT-10 gap in the existing revision authority: bounded server history can be safely compared and restored only as a version-guarded private draft, while preserving slug continuity and audit minimization.
</objective>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Verify bounded revision persistence and atomic restore-to-draft</name>
  <files>apps/api/src/content/admin-repository.ts, apps/api/src/content/article-service.ts, apps/api/src/routes/admin-posts.ts, apps/api/test/article-revisions.test.ts</files>
  <read_first>apps/api/src/content/admin-repository.ts; apps/api/src/content/article-service.ts; apps/api/src/routes/admin-posts.ts; apps/api/test/article-revisions.test.ts; apps/api/test/article-slug-redirects.test.ts; apps/api/src/db/schema.ts</read_first>
  <action>Add a capacity-overflow disposable test that performs more than the declared 20 material edits and proves newest-first bounded rows, no orphan/foreign snapshot, and no loss of the current row. Add restore tests for stale version, concurrent restore, nonexistent/cross-article revision, malformed snapshot, deleted media/taxonomy reference, published/scheduled article, and forced audit failure. Require the successful transaction to snapshot displaced current fields, validate restored references, move status to draft with published/scheduled authorities cleared, maintain/update direct slug aliases, and emit only allowed revision/status metadata. If this exposes a true retention or atomicity gap, fix it in repository/service/route code; no migration is expected or permitted absent a newly planned repository migration/apply/schema-verify task.</action>
  <acceptance_criteria>
    - Revision list stays at the declared cap and exposes no Markdown/summary/SEO/media snapshot fields until a selected authenticated detail request.
    - Restore cannot overwrite a newer live version, publish automatically, or leave partial row/audit/revision/alias changes.
    - Any historic slug restored as current keeps other aliases direct and cannot create a loop or release another article’s reservation.
  </acceptance_criteria>
  <verify>corepack pnpm --filter @blog-x/api test -- article-revisions.test.ts article-slug-redirects.test.ts; corepack pnpm --filter @blog-x/api typecheck</verify>
  <done>CONT-10’s bounded, atomic and draft-only server authority is proven or minimally corrected against disposable PostgreSQL.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Verify accessible comparison and explicit restoration in a generated browser</name>
  <files>apps/web/app/admin/_components/ArticleRevisionHistory.tsx, apps/web/e2e/article-revisions.spec.ts</files>
  <read_first>apps/web/app/admin/_components/ArticleRevisionHistory.tsx; apps/web/e2e/article-revisions.spec.ts; apps/web/app/admin/_components/ArticleEditor.tsx; apps/web/app/admin/admin.module.css</read_first>
  <action>Expand the existing generated browser scenario to create enough material changes to display the bounded count, inspect one strict comparison, confirm/cancel keyboard focus behavior, recover from stale/network errors, and complete a real restore that visibly becomes draft rather than public. Assert no horizontal overflow at 390/768/1280 and no snapshot payload is rendered before selection. If this exposes an actual UI state/focus/error defect, make the minimal component correction. After all Phase 16 product changes are committed, run exactly one clean-SHA fixed local preview refresh and verify `/api/health`, `/posts/<old-slug>`, `/admin/settings`, and an authenticated revision page; if every Phase16 task is evidence-only, cite current accepted behavior and skip the refresh.
  </action>
  <acceptance_criteria>
    - Restore remains a two-step explicit action, pending guards block repeats, and focus returns predictably on cancel/failure.
    - Successful browser restore shows draft/private state and a current history, never a live publication.
    - At most one Phase16 formal refresh is run, only after a clean committed SHA and only if product code changed.
  </acceptance_criteria>
  <verify>/bin/zsh /tmp/blog-x-lifecycle-e2e-run.sh; corepack pnpm --filter @blog-x/web typecheck; PUBLIC_ORIGIN=http://127.0.0.1:3100 corepack pnpm --filter @blog-x/web build; node scripts/check-boundaries.mjs</verify>
  <done>CONT-10 is browser-proven as a bounded, accessible, non-public recovery workflow; the phase has at most one warranted preview refresh.</done>
</task>
</tasks>

<threat_model asvs="L1" block_on="high">
| ID | Threat | Severity | Blocking mitigation |
|---|---|---|---|
| T16-03-1 | Unbounded history exhausts low-resource storage | high | Capacity-overflow retention test and repository cap are mandatory. |
| T16-03-2 | Stale or cross-article revision overwrites live content | high | Version/id ownership checks and concurrency rollback tests block completion. |
| T16-03-3 | Restore republish/leaks revision content or audit payload | high | Draft-only transaction, strict detail endpoint, content-free audit and browser assertions. |
| T16-03-4 | Revision restore breaks durable old links | high | LINK-01 direct-alias regression is part of Task 1. |
</threat_model>

## Verification and success criteria

- Targeted API/browser tests, builds/typechecks and boundary check pass with no skipped capacity or recovery cases.
- No schema migration is planned; any real schema discovery triggers a new plan with repository migration/apply/schema verification, never `drizzle push`.
- This is the sole Phase 16 plan allowed to refresh fixed preview, exactly once and only after a clean committed product-change SHA.

## Artifacts produced

- CONT-10 bounded-capacity and transactional restore evidence, plus only minimal corrections that tests prove necessary.
