---
phase: 06-public-discovery-data
plan: "04"
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - apps/api/Dockerfile.refresh
  - apps/web/Dockerfile.refresh
  - scripts/refresh-local.mjs
  - scripts/refresh-local.test.mjs
autonomous: true
gap_closure: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
user_setup: []
must_haves:
  truths:
    - "The current API/Web images are dependency seeds only: a build copies only the content-addressed pnpm store to a neutral path, removes the inherited `/workspace`, inherited `node_modules`, old source and build output, and creates the target exclusively under `/refresh-workspace`."
    - "If a seed's pnpm store is inside its old application tree, the build copies only that resolved store to `/pnpm-store` before deleting the old tree; no inherited source, package links, node_modules, `.next`, `dist`, or other application output survives."
    - "Both refresh Dockerfiles copy current manifests and lockfile into the clean workspace, run `corepack pnpm install --store-dir=/pnpm-store --offline --frozen-lockfile`, then copy current sources; missing seed/store/package authority fails before any database, volume, or fixed-service mutation."
    - "Tests inspect a built-image filesystem contract: `/workspace` is absent, `/refresh-workspace` is the only application root, runtime/build working directories and commands use it, and only current dependency links/build output exist there."
    - "The orchestrator is fixed to project `blogxlocal` and origin `http://127.0.0.1:3100`, validates clean HEAD/lock/seed/volume/topology authority, builds and inspects both revision-tagged images before migration, and cannot use stale retagging or network fallback."
    - "The orchestrator snapshots fixed volume identity, business data, sequences, ledger, media, archived v1 and retained receipt; it can cut over only API/Web, validate discovery through 3100, and restore captured old images after a post-start failure without touching volumes or PostgreSQL."
    - "Production stays BLOCKED, no server/SSH/deploy capability is introduced, and actual fixed-runtime mutation/evidence is deferred only to ordered Plan 06-05 after this implementation is committed and summarized."
  artifacts:
    - path: apps/api/Dockerfile.refresh
      provides: sanitized-seed API image built only in `/refresh-workspace` from current manifests/source and a neutral offline pnpm store
    - path: apps/web/Dockerfile.refresh
      provides: sanitized-seed Web production image with only current `/refresh-workspace` source/build output and fixed origin
    - path: scripts/refresh-local.mjs
      provides: fail-closed fixed-project refresh state machine, snapshot comparison, image filesystem/provenance inspection, cutover, rollback, and evidence verification
      exports:
        - createRefreshPlan
        - runLocalRefresh
    - path: scripts/refresh-local.test.mjs
      provides: RED/GREEN orchestration, seed sanitization, image filesystem, prohibition, ordering, evidence, and rollback coverage
  key_links:
    - from: apps/api/Dockerfile.refresh
      to: scripts/refresh-local.mjs
      via: orchestrator supplies exact seed/revision/lock/origin arguments, disables build network/pull, and inspects labels plus filesystem before mutation
      pattern: "SEED_IMAGE|/pnpm-store|/refresh-workspace|--offline|--frozen-lockfile|Dockerfile\\.refresh"
    - from: apps/web/Dockerfile.refresh
      to: scripts/refresh-local.mjs
      via: the same sanitized seed contract plus Web production build and fixed PUBLIC_ORIGIN
      pattern: "/workspace|/refresh-workspace|PUBLIC_ORIGIN|127\\.0\\.0\\.1:3100"
    - from: scripts/refresh-local.mjs
      to: compose.yaml
      via: exact blogxlocal migration/schema and API/Web-only no-build cutover on pre-existing fixed volumes
      pattern: "blogxlocal|--no-build|--no-deps|api|web|3100"
    - from: scripts/refresh-local.mjs
      to: scripts/release-gate.mjs
      via: successful local evidence still requires the canonical pure BLOCKED decision
      pattern: "release-gate\\.mjs|--expect-blocked|BLOCKED"
  prohibitions:
    - statement: "A target image must not retain or execute inherited `/workspace`, inherited source, inherited node_modules/package links, or old build output; the seed contributes only its base runtime and copied content-addressed pnpm store."
      status: pending
      verification: test
    - statement: "The implementation must not pull, install online, use host networking, stale-retag a seed as a target, run Compose down, remove/recreate volumes, recreate PostgreSQL, or target a project other than blogxlocal."
      status: pending
      verification: test
    - statement: "Plan 06-04 must not execute the live refresh, write runtime evidence, edit 06-VERIFICATION/completion authorities, contact either server, deploy, unfreeze, or push."
      status: pending
      verification: command
---

<objective>
Implement and test the durable offline fixed-runtime refresh primitive without changing the running `blogxlocal` environment.

Purpose: Remove stale-source ambiguity from dependency-seed inheritance and make the later fixed 3100 refresh safe, deterministic, and independently auditable.
Output: Two sanitized-seed refresh Dockerfiles, one fail-closed orchestrator, its complete RED/GREEN safety suite, and 06-04-SUMMARY.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-CONTEXT.md
@.planning/phases/06-public-discovery-data/06-03-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@compose.yaml
@apps/api/Dockerfile
@apps/web/Dockerfile
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/release-gate.mjs
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json

<interfaces>
- `node scripts/refresh-local.mjs` is the future execution entry. Project/origin/volume/service authority is constant: `blogxlocal`, `http://127.0.0.1:3100`, `blogxlocal_postgres-data`, `blogxlocal_media-data`, and only `api web` for cutover.
- Seed preflight accepts only the images of healthy fixed `blogxlocal-api-1` and `blogxlocal-web-1`, resolves configured refs to exact IDs, requires seed `Config.WorkingDir` to be exactly `/workspace`, and requires `corepack pnpm store path` to resolve to an existing directory. Unknown application roots fail closed.
- Each refresh Dockerfile uses a seed stage to resolve the store. Copy only the resolved store contents into neutral `/pnpm-store`; if it is below `/workspace`, copy it before deletion. After the copy, remove the original resolved store when it is a distinct path, remove `/workspace` and any pre-existing `/refresh-workspace`, then create a new `/refresh-workspace`, set it as `WORKDIR`, and never copy files out of the old application tree. The final filesystem exposes exactly the neutral store, not a second inherited store location.
- Copy root `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `tsconfig.base.json`, and all three workspace package manifests into the new root. Run exactly `corepack pnpm install --store-dir=/pnpm-store --offline --frozen-lockfile`; only afterward copy current API/Web/contracts sources and build Web under `/refresh-workspace/apps/web`.
- Target images must expose no inherited `/workspace`. API command and Web build/start command execute from `/refresh-workspace`; Web's only accepted `.next` is `/refresh-workspace/apps/web/.next`, and API's current compiled/source runtime exists only below the new root. Image filesystem inspection rejects legacy `/workspace`, seed-marker source, old `.next`/`dist`, or a working directory/command resolving outside the new root.
- Required labels are `org.opencontainers.image.revision`, `io.blog-x.lockfile-sha256`, `io.blog-x.seed-image-id`, `io.blog-x.application`, `io.blog-x.public-origin`, and `io.blog-x.refresh-kind=phase6-offline`. Targets are `blog-x-api-local:<short-head>` and `blog-x-web-local:<short-head>`.
- Both builds use `docker build --network=none --pull=false`. Both image IDs, exact labels, seed IDs, workdir/commands, and sanitized filesystems must pass before the state machine may run a migration or Compose service action.
- Pure functions model canonical volume-inspect digests; normalized data dump excluding ledger; sequences; ledger stable tuple plus `applied_at`; media manifest; archived-v1/receipt hashes; allowed timestamp-only change; fixed port topology; strict route bodies; and atomic sanitized evidence.
- Post-start failure uses captured old configured image refs/IDs with API/Web-only `up --no-build --no-deps`, checks restored IDs/health, never acts on PostgreSQL or volumes, emits no passing evidence, and exits nonzero.
- Tests use a fake command adapter for every mutation branch. A narrowly scoped optional Docker integration fixture may build disposable probe images only if available; it must not target `blogxlocal`. The real fixed refresh belongs exclusively to 06-05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify sanitized seed and refresh safety behavior in RED</name>
  <files>scripts/refresh-local.test.mjs</files>
  <read_first>
    <item>06-VERIFICATION.md — sole stale-3100 gap and independent-checker stale-source concern.</item>
    <item>Current Dockerfiles and compose.yaml — inherited `/workspace`, frozen install, health, volume, and port facts.</item>
    <item>scripts/local-verify.test.mjs — fake/source guard and strict evidence conventions.</item>
  </read_first>
  <behavior>
    - "RED: both Dockerfile contracts resolve/copy only a content-addressed pnpm store to `/pnpm-store`, including the source-store-under-`/workspace` case, remove any distinct original store location, then delete old `/workspace` before creating `/refresh-workspace`."
    - "RED: image inspection rejects any visible `/workspace`, inherited source marker, old node_modules/package link, old `.next`/`dist`, non-refresh working directory, or runtime/build command outside `/refresh-workspace`."
    - "RED: current manifests/lock precede offline frozen install, current source follows it, and missing seed/store/package authority fails before migration/runtime/volume action."
    - "RED: exact clean HEAD, lock digest, seed IDs, revision tags, labels, fixed origin/project/volumes/ports, two-image build barrier, snapshots, cutover, evidence, BLOCKED state, and rollback are required."
    - "RED: prohibited network, stale-retag, teardown, volume, PostgreSQL, alternate-project, server/deploy, and self-verification paths are rejected."
  </behavior>
  <action>
    Create nonzero tests first. Use pure validators and a fake argv runner to cover seeds with stores outside and inside the legacy application tree, filesystem inspection success/failure fixtures, command phase ordering, all pre-mutation failures, post-start rollback, evidence suppression, and exact success. Source-contract tests must require explicit legacy-tree deletion and neutral store use in both missing Dockerfiles. Capture the expected missing-implementation failure and commit only the test file as `test(06-04): specify sanitized offline refresh controls`.
  </action>
  <acceptance_criteria>
    <criterion>RED coverage explicitly proves the old workspace/source/node_modules/build tree cannot survive seed inheritance or be used by runtime/build commands.</criterion>
    <criterion>Both possible seed store locations are covered, and only store content may cross into `/pnpm-store` before old-tree deletion.</criterion>
    <criterion>All build-barrier, fixed-authority, data, rollback, evidence, and prohibition branches are nonzero tests driven without fixed Docker mutation.</criterion>
    <criterion>The RED commit contains exactly `scripts/refresh-local.test.mjs`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>/bin/zsh -lc 'set +e; node --test scripts/refresh-local.test.mjs &gt; /private/tmp/blog-x-06-04-red.tap 2&gt;&amp;1; status=$?; test "$status" -ne 0; rg "refresh-local\\.mjs|Dockerfile\\.refresh" /private/tmp/blog-x-06-04-red.tap'</automated>
  </verify>
  <done>The checker blockers are encoded as failing tests before implementation, with no runtime mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sanitized offline images and orchestrator</name>
  <files>apps/api/Dockerfile.refresh, apps/web/Dockerfile.refresh, scripts/refresh-local.mjs, scripts/refresh-local.test.mjs</files>
  <read_first>
    <item>The committed Task 1 RED suite and its exact failures.</item>
    <item>All workspace manifests, lockfile, API/Web/contracts sources, and current Dockerfiles.</item>
    <item>apps/api/src/app.ts — migration/schema ledger behavior.</item>
    <item>scripts/local-verify.mjs and scripts/release-gate.mjs — safe command, digest, redaction, and BLOCKED patterns.</item>
  </read_first>
  <action>
    Implement the Dockerfile seed sanitization and neutral-store flow exactly from interfaces. Do not merely change WORKDIR: explicitly copy only the resolved pnpm store, delete the complete inherited application tree and prior refresh tree, reconstruct current manifests/dependencies/source in `/refresh-workspace`, and overwrite workdir/CMD. Implement import-safe `createRefreshPlan` and `runLocalRefresh` with spawn argv arrays, exact constants, phase barriers, strict provenance/filesystem inspection, snapshots/comparisons, migration/schema, API/Web-only cutover, rollback, redaction, and atomic evidence verifier. Make tests GREEN. If Docker is available, build disposable probe targets with networking disabled and inspect their root filesystems/Config to prove `/workspace` and legacy markers are absent while `/refresh-workspace` commands/build output are current; do not run the fixed refresh. Run local-verifier regressions, typecheck, boundaries, and release BLOCKED. Commit only the two Dockerfiles and two scripts as `feat(06-04): add sanitized offline refresh primitive`.
  </action>
  <acceptance_criteria>
    <criterion>Both target definitions retain exactly the neutral copied `/pnpm-store` from their seeds, remove any distinct original store path, and expose no inherited application path, source, node_modules/package link, or build output.</criterion>
    <criterion>Built-image inspection proves `/workspace` absent; Config.WorkingDir and runtime/build commands use `/refresh-workspace`; only the current Web `.next` and current API source/runtime paths exist below it.</criterion>
    <criterion>Offline frozen install uses current complete manifests/lock and fails before state mutation when any store package is unavailable.</criterion>
    <criterion>All fake-runner safety, ordering, rollback, evidence, topology, historical immutability, and BLOCKED tests pass.</criterion>
    <criterion>The GREEN commit contains exactly the four implementation/test files; runtime evidence, summaries, verification/status files, v1 archive, and receipt are unchanged.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/refresh-local.test.mjs &amp;&amp; node --test scripts/local-verify.test.mjs &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>The committed implementation can build current-source offline images from sanitized dependency seeds and safely orchestrate a later fixed refresh, but has not changed live blogxlocal.</done>
</task>

</tasks>

## Atomic commit sequence

1. RED tests only — `test(06-04): specify sanitized offline refresh controls`.
2. GREEN Dockerfiles/orchestrator/tests only — `feat(06-04): add sanitized offline refresh primitive`.
3. Execute-plan creates and commits `06-04-SUMMARY.md` after both tasks. Plan 06-05 must start from that clean committed HEAD; no runtime evidence belongs to 06-04.

<verification>
- RED then GREEN history is preserved as two atomic product commits.
- Refresh tests prove seed-store isolation, old-tree absence, current `/refresh-workspace` authority, build barrier, fixed safety, rollback, and prohibitions.
- Typecheck, boundaries, local-verifier regressions, release BLOCKED, v1 archive, receipt, and 06-VERIFICATION checks pass.
- No fixed runtime, volume, server, deployment, production, or remote state changes in this plan.
</verification>

<success_criteria>
- Dependency-seed inheritance cannot execute or expose stale application files.
- Both offline targets are reproducibly tied to current committed manifests/lock/source and exact provenance.
- Plan 06-05 can run one committed command to refresh fixed 3100 with rollback and evidence.
</success_criteria>

<output>
Create `.planning/phases/06-public-discovery-data/06-04-SUMMARY.md` after implementation. Do not run the fixed refresh, write runtime evidence, edit 06-VERIFICATION/completion fields, contact servers, deploy, or push.
</output>
