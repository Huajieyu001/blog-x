---
phase: 06-public-discovery-data
plan: "04"
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - apps/api/Dockerfile.refresh
  - apps/web/Dockerfile.refresh
  - scripts/refresh-local.mjs
  - scripts/refresh-local.test.mjs
  - ops/phase6-local-refresh-evidence.json
  - .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
  - .planning/phases/06-public-discovery-data/06-04-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
user_setup: []
must_haves:
  truths:
    - "A clean committed revision can build both API and Web refresh images with Docker build networking disabled even when the original Dockerfile install layer is absent, by inheriting only a locally present dependency-complete seed and revalidating the current manifests with `corepack pnpm install --offline --frozen-lockfile`; any missing package or seed fails before database, volume, or fixed-service mutation."
    - "The refresh never substitutes stale application code: each image starts in a fresh workspace, copies the current complete workspace manifests and lockfile before the offline frozen install, copies current application sources afterward, and is revision-tagged rather than retagging an old image as the target."
    - "Both target images are built and inspected before migration, and their labels bind the exact clean Git HEAD, pnpm-lock SHA-256, actual seed image ID, application kind, and `PUBLIC_ORIGIN=http://127.0.0.1:3100`."
    - "The orchestrator accepts only the fixed Compose project `blogxlocal`, preflights the exact existing PostgreSQL/media volume identity digests, records the old API/Web image authority, and refuses alternate projects, absent/replaced volumes, non-loopback Web publication, or host-published API/PostgreSQL ports."
    - "Before any fixed service is recreated, the orchestrator snapshots business data excluding `blog_x_schema_ledger`, exact sequence values, complete ledger stable tuples plus timestamps, media paths/sizes/digests, archived v1 evidence, and the retained Phase 5 receipt; it migrates and schema-checks with the new API image only after both image builds pass."
    - "Only fixed `blogxlocal` API/Web services are recreated, without Compose down or volume operations; PostgreSQL remains running, the original volume identity digests and all business/sequence/media facts stay equal, and only an existing ledger row's `applied_at` may advance while its stable tuple remains identical."
    - "A post-start failure restores API/Web to the captured old image references, rechecks their health, and leaves PostgreSQL/media volumes untouched; failed or rolled-back runs cannot emit passing evidence."
    - "A successful run serves the Phase 6 revision through the Web origin at `127.0.0.1:3100`: home/category/tag/archive and health succeed, empty search returns `state: empty_query`, unknown related returns strict 404 `not_found`, containers use the revision tags, and only Web publishes loopback port 3100."
    - "The executor commits runtime evidence separately, then updates the historical 06-03 summary and creates 06-04-SUMMARY in a later docs commit; it leaves 06-VERIFICATION to a new independent verifier, keeps the archived v1/receipt byte-identical, contacts no server, and keeps production BLOCKED."
  artifacts:
    - path: apps/api/Dockerfile.refresh
      provides: fresh-workspace API image derived from a local dependency seed with an offline frozen install and source/provenance labels
    - path: apps/web/Dockerfile.refresh
      provides: fresh-workspace Web production image derived from a local dependency seed with an offline frozen install, current build output, fixed origin, and provenance labels
    - path: scripts/refresh-local.mjs
      provides: fail-closed fixed-project refresh state machine, snapshots, two-image build barrier, migration, service cutover, post-checks, rollback, and atomic evidence writer
      exports:
        - createRefreshPlan
        - runLocalRefresh
    - path: scripts/refresh-local.test.mjs
      provides: TDD coverage for offline provenance, exact command ordering, fixed authority, no-volume rules, evidence gating, and rollback
    - path: ops/phase6-local-refresh-evidence.json
      provides: sanitized machine-readable proof that a clean revision is running at fixed 3100 with preserved local data authority
  key_links:
    - from: scripts/refresh-local.mjs
      to: apps/api/Dockerfile.refresh
      via: network-none, pull-disabled build using the captured API seed reference and exact revision/lock/seed/origin build arguments
      pattern: "Dockerfile\\.refresh|--network=none|--pull=false|BLOG_X_SOURCE_REVISION|BLOG_X_LOCKFILE_SHA256|BLOG_X_SEED_IMAGE_ID"
    - from: scripts/refresh-local.mjs
      to: apps/web/Dockerfile.refresh
      via: the same two-image pre-mutation build barrier plus fixed PUBLIC_ORIGIN and a revision-specific Web target tag
      pattern: "blog-x-web-local:|PUBLIC_ORIGIN|127\\.0\\.0\\.1:3100|inspect"
    - from: scripts/refresh-local.mjs
      to: compose.yaml
      via: project-exact migration/schema and API/Web-only no-build cutover using existing named volumes and no host exposure except Web loopback 3100
      pattern: "blogxlocal|--no-build|--no-deps|api|web|3100"
    - from: scripts/refresh-local.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: evidence is atomically published only after provenance, preservation, topology, route, BLOCKED-state, and historical-evidence checks all pass
      pattern: "phase6-local-refresh-evidence\\.json|implementationRevision|lockfileSha256|volumeIdentity|routes|BLOCKED"
    - from: compose.yaml
      to: http://127.0.0.1:3100/api/public/search?q=
      via: refreshed Web same-origin proxy reaches the revision-bound API discovery route
      pattern: "INTERNAL_API_ORIGIN|PUBLIC_ORIGIN|3100"
  prohibitions:
    - statement: "The refresh must not use registry access, pull, online installation, host-network building, stale target retagging, or any fallback that omits the offline frozen lockfile proof."
      status: pending
      verification: test
    - statement: "The refresh must not run Compose down, pass a volume-removal flag, delete/recreate either fixed volume, create an alternate project, recreate PostgreSQL, or touch a non-blogxlocal container."
      status: pending
      verification: test
    - statement: "No executor command may edit archived v1 evidence, the retained Phase 5 receipt, or 06-VERIFICATION; use SSH, contact either server, deploy, unfreeze production, or transition release state from BLOCKED."
      status: pending
      verification: command
---

<objective>
Close Phase 6's sole verification gap by adding a source-proven, fully offline refresh path and using it to put the verified discovery API revision on the fixed `blogxlocal` runtime at `127.0.0.1:3100` without replacing local data authority.

Purpose: Phase 7 needs the fixed same-origin entry to expose search and related APIs, while local refresh must remain reproducible under cache loss, safe for persistent content, and incapable of granting production authority.
Output: Dedicated refresh Dockerfiles, a TDD-guarded refresh orchestrator, a sanitized Phase 6 runtime evidence record, corrected current-state documentation, and an independent-verifier handoff.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-CONTEXT.md
@.planning/phases/06-public-discovery-data/06-03-PLAN.md
@.planning/phases/06-public-discovery-data/06-03-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@compose.yaml
@apps/api/Dockerfile
@apps/web/Dockerfile
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/release-gate.mjs
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json
@.planning/milestones/v1.0-MILESTONE-AUDIT.md

<interfaces>
- The one execution entry is `node scripts/refresh-local.mjs`. Project and public origin are code-level constants: `blogxlocal` and `http://127.0.0.1:3100`. The command rejects caller overrides rather than making fixed authority configurable.
- Existing running `blogxlocal-api-1` and `blogxlocal-web-1` image references are dependency seeds only. Before build, resolve each reference to its local image ID and retain that ID as immutable evidence; use no image pull and verify the reference still resolves to the same ID after both builds.
- Both refresh Dockerfiles use `ARG SEED_IMAGE` before `FROM`, then switch to a new `/refresh-workspace` so no old application source or build output survives. Copy root manifests plus all workspace package manifests and `pnpm-lock.yaml`, run exactly `corepack pnpm install --offline --frozen-lockfile`, then copy current API/Web/contracts sources and build as appropriate.
- API and Web target tags are exactly `blog-x-api-local:<short-head>` and `blog-x-web-local:<short-head>`. Required OCI/custom labels are `org.opencontainers.image.revision`, `io.blog-x.lockfile-sha256`, `io.blog-x.seed-image-id`, `io.blog-x.application`, `io.blog-x.public-origin`, and `io.blog-x.refresh-kind=phase6-offline`.
- Build both targets with `docker build --network=none --pull=false`, their dedicated refresh Dockerfile, the captured seed reference, full HEAD, lock digest, seed ID, fixed origin, and revision tag. A seed/store/package miss is an expected fail-closed outcome: do not migrate, recreate a service, change a volume, or write success evidence.
- Fixed volumes must already exist as `blogxlocal_postgres-data` and `blogxlocal_media-data`. Canonical volume identity is the SHA-256 of normalized `docker volume inspect` fields `Name,Driver,Mountpoint,CreatedAt,Scope,Labels,Options`; capture both exact digests before build and compare at every mutation boundary.
- Pre-refresh data authority is: a normalized PostgreSQL data-only dump excluding `blog_x_schema_ledger`; ordered `pg_sequences` identity/value rows; ordered complete ledger rows split into stable `{scope,migration_count,migration_fingerprint}` and timestamp `applied_at`; and sorted media `{relativePath,bytes,sha256}` rows. PostgreSQL 18 random `\\restrict`/`\\unrestrict` guards are removed only from the dump normalization.
- Also hash every tracked `.planning/milestones` path plus `ops/phase5-full-gate-receipt.json`, record the retained receipt digest, and require exact equality before success. Do not rewrite either authority.
- After both built images pass label/ID inspection, run migration and schema verification with the new API image in a one-off `blogxlocal` Compose process without starting dependencies. Require the same volume/data/sequence/media facts, identical ledger row count/stable tuples, and the existing ledger row's `applied_at` to advance strictly; no other difference is permitted.
- Cut over only API/Web with the revision tags and fixed origin using `docker-compose -p blogxlocal -f compose.yaml up -d --wait --no-build --no-deps api web`. PostgreSQL is inspected but never recreated. API and PostgreSQL must publish no host port; Web must publish exactly `127.0.0.1:3100->3100/tcp`.
- After cutover, verify labels and actual container image IDs, all three health states, volume/data/sequence/ledger/media preservation, canonical `RELEASE BLOCKED`, historical hashes, and Web-origin routes `/`, `/categories`, `/tags`, `/archive`, `/api/health`, `/api/public/search?q=`, and `/api/public/articles/phase6-unknown/related`.
- Capture old API/Web configured image references and IDs before cutover. On any failure after service recreation begins, reapply those exact references with API/Web-only no-build/no-deps Compose, require old image IDs and health, keep volumes attached and unchanged, suppress success evidence, and exit nonzero. A rollback failure is reported distinctly and never triggers broader cleanup.
- `ops/phase6-local-refresh-evidence.json` is sanitized, deterministic in field order, and written atomically only on success. It contains schema version/status; implementation revision; lock digest; project/origin; seed and target refs/IDs/labels; old/new runtime image facts; canonical volume identity digests; business/sequence/media digests; ledger stable/timestamp comparison; route status/contract results; v1/receipt hashes; and `releaseState: BLOCKED`. It contains no credentials, raw database rows, absolute media contents, environment dumps, or server data.
- Phase 8 may generalize this bounded primitive into recurring delivery automation. This plan must itself close the present stale-3100 gap and may not defer the actual refresh.
- The executor may update 06-03-SUMMARY only after successful committed evidence, preserving its historical fail-closed account while correcting current readiness with a dated 06-04 closure addendum. A new independent verifier alone edits 06-VERIFICATION and decides Phase 6/requirements completion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the offline refresh safety state machine in RED tests</name>
  <files>scripts/refresh-local.test.mjs</files>
  <read_first>
    <item>.planning/phases/06-public-discovery-data/06-VERIFICATION.md — the sole stale-runtime gap and required closure evidence.</item>
    <item>compose.yaml — fixed services, build-network boundary, loopback Web publication, and named volume topology.</item>
    <item>apps/api/Dockerfile and apps/web/Dockerfile — current frozen install/build behavior whose missing cached layer caused the block.</item>
    <item>scripts/local-verify.test.mjs — repository conventions for source guards, fake runners, strict markers, historical evidence, and no-receipt assertions.</item>
    <item>scripts/release-gate.mjs and ops/release-evidence.blocked.json — canonical pure BLOCKED authority.</item>
  </read_first>
  <behavior>
    - "RED: plan creation rejects a dirty/uncommitted HEAD, alternate project/origin, absent fixed container, absent or changed fixed volume identity, unresolved/changed seed image, and any pre-existing target tag whose labels do not exactly bind the requested revision inputs."
    - "RED: the planned command sequence builds API and Web with disabled networking, disabled pull, current manifests/lockfile, offline frozen installation, and exact provenance; both image inspections occur before any migration or service action."
    - "RED: an offline dependency miss, either build failure, label mismatch, or seed-ID race stops before migration, Compose service recreation, volume action, or success-evidence publication."
    - "RED: snapshots include normalized business data excluding the ledger, all sequences, ledger stable tuples/timestamps, media manifest, exact volume identities, archived v1 manifest, retained receipt, old image refs/IDs, health, and port topology."
    - "RED: migration/schema use only the new API image after the build barrier; cutover targets only fixed API/Web; PostgreSQL remains the same container and API/PostgreSQL expose no host port while Web is loopback 3100 only."
    - "RED: any post-start image, health, topology, data, ledger, media, route, historical-evidence, or release-state failure restores the captured old API/Web image refs, verifies old image IDs/health, performs no volume action, emits no pass evidence, and exits nonzero."
    - "RED: success requires exact discovery contracts through the Web origin, atomic sanitized evidence, revision-tagged running images, immutable v1/receipt hashes, and production BLOCKED."
    - "RED: source/prohibition assertions reject registry/network fallback, stale target retagging, Compose teardown, volume deletion/recreation, PostgreSQL recreation, alternate projects, public API/database ports, SSH/server/deploy commands, and executor writes to 06-VERIFICATION."
  </behavior>
  <action>
    Create tests first around exported pure planning/validation functions plus a fake command adapter so all ordering, fail-closed, and rollback branches run without touching Docker. Require exact command argv and mutation-phase boundaries rather than trusting log text. Add source-structure assertions for the two dedicated refresh Dockerfiles and orchestrator prohibitions, strict evidence schema/key-set tests, deterministic canonical hashing fixtures, PostgreSQL dump guard normalization, and ledger comparison cases for unchanged timestamps versus the sole allowed migration timestamp advance. Include a simulated clean success path and failures before build, between builds, after migration, during cutover, during route checks, and during rollback. The test file must fail because the implementation/Dockerfiles do not yet exist. Commit only this RED test file as `test(06-04): specify offline fixed refresh controls`; record the failing test output in the later summary, not in tracked runtime evidence.
  </action>
  <acceptance_criteria>
    <criterion>The RED suite has nonzero tests covering preflight, two-image build barrier, offline provenance, snapshot comparison, API/Web-only cutover, rollback, evidence gating, topology, immutable historical evidence, and release BLOCKED.</criterion>
    <criterion>Tests drive command execution through a fake adapter and assert no mutating command occurs on every pre-mutation failure path.</criterion>
    <criterion>Tests fail for missing `scripts/refresh-local.mjs` and refresh Dockerfiles, not for a test syntax or fixture error.</criterion>
    <criterion>The RED commit contains exactly `scripts/refresh-local.test.mjs`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>/bin/zsh -lc 'set +e; node --test scripts/refresh-local.test.mjs &gt; /private/tmp/blog-x-06-04-red.tap 2&gt;&amp;1; status=$?; test "$status" -ne 0; rg "refresh-local\\.mjs|Dockerfile\\.refresh" /private/tmp/blog-x-06-04-red.tap'</automated>
  </verify>
  <done>The missing offline refresh primitive is fully specified as executable failing behavior tests without mutating the local runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement source-proven offline images and fail-closed orchestrator</name>
  <files>apps/api/Dockerfile.refresh, apps/web/Dockerfile.refresh, scripts/refresh-local.mjs, scripts/refresh-local.test.mjs</files>
  <read_first>
    <item>The committed Task 1 RED test revision and its expected failures.</item>
    <item>scripts/refresh-local.test.mjs — complete required state-machine behavior and exact evidence contract.</item>
    <item>compose.yaml — fixed service names, environment variables, named volumes, health checks, and port contract.</item>
    <item>apps/api/package.json, apps/web/package.json, packages/contracts/package.json, package.json, pnpm-workspace.yaml, pnpm-lock.yaml, tsconfig.base.json — complete current frozen workspace authority copied before install.</item>
    <item>apps/api/src/app.ts — `migrate` / `schema:verify` entry points, idempotent ledger update, and schema verification semantics.</item>
    <item>scripts/local-verify.mjs — command redaction, canonical digest, Docker/Compose invocation, dump normalization, and release-gate patterns worth reusing without entering receipt authority.</item>
  </read_first>
  <action>
    Implement both refresh Dockerfiles from explicit local `SEED_IMAGE` args but use a new `/refresh-workspace`; copy all current workspace manifests and the lockfile, run the exact offline frozen install, then copy only the current app/contracts sources. Build Web production output with fixed origin and give API its existing runtime command. Accept exact build args for full revision, lock SHA, seed ID, application kind, and public origin, and persist all required labels. Implement `scripts/refresh-local.mjs` as an import-safe module exporting `createRefreshPlan` and `runLocalRefresh`, with real execution only under direct CLI invocation. Use spawn-style argv arrays, exact constant project/origin/volume/service names, strict identifier/ref validation, output redaction, atomic JSON writes, and phase enums so tests can prove no database/runtime mutation before both images are built and inspected. Treat the current running API/Web images only as dependency/store seeds; verify their refs resolve to the captured IDs before and after builds, never use their source tree, and never move an old tag to a revision target. Implement canonical snapshots and comparisons exactly as defined in interfaces, including normalized volume-inspect digests, ledger stable/timestamp rules, immutable PostgreSQL identity, sanitized route bodies, and complete v1/receipt hash manifest. Implement migration/schema through the new API image, API/Web-only cutover, post-checks, and old-image rollback. Success evidence is written only after every check; rollback and preflight failures leave it absent and return nonzero. Make the RED suite GREEN, then run typecheck, boundaries, local verifier regressions, and release BLOCKED checks. Commit only the two Dockerfiles and two script files as `feat(06-04): add durable offline local refresh`.
  </action>
  <acceptance_criteria>
    <criterion>Both Dockerfiles build in a fresh workspace, perform an offline frozen install against current manifests, copy current source only after install, and emit all exact provenance labels.</criterion>
    <criterion>The orchestrator validates a clean HEAD and fixed authorities, builds and inspects both images before migration, preserves exact snapshot facts, recreates only API/Web, and atomically writes evidence only after route and BLOCKED checks.</criterion>
    <criterion>Every simulated failure path is fail-closed; post-start paths reapply captured old images and verify rollback without any volume or PostgreSQL recreation action.</criterion>
    <criterion>Focused refresh tests, local verifier regressions, workspace typecheck, repository boundaries, and the pure release gate pass.</criterion>
    <criterion>The GREEN commit contains only `apps/api/Dockerfile.refresh`, `apps/web/Dockerfile.refresh`, `scripts/refresh-local.mjs`, and `scripts/refresh-local.test.mjs`; archived evidence, receipt, runtime evidence, summaries, and verification report are unchanged.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/refresh-local.test.mjs &amp;&amp; node --test scripts/local-verify.test.mjs &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>A tested offline build and fixed refresh authority exists in committed source and cannot silently run stale code, mutate fixed data on cache loss, or acquire production authority.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the refresh from the clean implementation commit and retain evidence</name>
  <files>ops/phase6-local-refresh-evidence.json</files>
  <read_first>
    <item>The committed Task 2 HEAD and empty `git status --porcelain`; this exact revision becomes the image/evidence implementation revision.</item>
    <item>scripts/refresh-local.mjs and scripts/refresh-local.test.mjs — exact tested command, failure, rollback, and evidence semantics.</item>
    <item>apps/api/Dockerfile.refresh and apps/web/Dockerfile.refresh — offline frozen install and label contract.</item>
    <item>The live `blogxlocal` container/image/port/health facts and exact `blogxlocal_postgres-data` / `blogxlocal_media-data` normalized inspect identities.</item>
    <item>All tracked `.planning/milestones` files, `ops/phase5-full-gate-receipt.json`, and 06-VERIFICATION — immutable executor inputs.</item>
  </read_first>
  <precondition>`git status --porcelain` is empty at the committed Task 2 revision; the three exact fixed containers are healthy; both exact fixed volumes already exist; API/Web seed refs resolve locally; no `blogxverify_*` resource or receipt lock is active. Otherwise stop without refresh mutation.</precondition>
  <action>
    Record the clean full/short HEAD, lockfile SHA, complete archived-v1/receipt/verification hashes, current container and seed image authority, exact fixed volume identity digests, database/ledger/sequence/media snapshots, health, and port topology. Run exactly `node scripts/refresh-local.mjs`; do not substitute manual Docker commands. The script must build both revision-tagged images fully offline, inspect their image IDs and exact labels, migrate/schema-check with the new API image, cut over only fixed API/Web, then prove data/topology/routes/release state and atomically create `ops/phase6-local-refresh-evidence.json`. If a package is absent from either inherited seed store, stop at the documented pre-mutation failure and report the concrete missing authority; do not use registry access or stale images. If any post-start check fails, require tested rollback to the old image IDs and healthy fixed runtime before stopping. On success, independently inspect that the running containers use the target image IDs/labels, PostgreSQL container and volume identities remain exact, only Web publishes `127.0.0.1:3100`, all preservation comparisons pass, and Web-origin discovery contracts are live. Validate the evidence against the script's strict schema, confirm archived v1/receipt/06-VERIFICATION hashes are unchanged, and commit only `ops/phase6-local-refresh-evidence.json` as `chore(06-04): record fixed runtime refresh evidence`.
  </action>
  <acceptance_criteria>
    <criterion>The exact refresh exits 0 from a clean committed implementation revision without registry, server, or production access.</criterion>
    <criterion>Both revision target images exist and their inspected IDs/labels exactly match evidence for HEAD, lock digest, seed IDs, application kinds, fixed origin, and refresh kind.</criterion>
    <criterion>`blogxlocal-postgres-1` remains the same healthy container; fixed volume identity, business-data, sequence, ledger-stable, and media comparisons pass, with no database difference beyond the allowed existing ledger timestamp advance.</criterion>
    <criterion>API/Web run their revision target IDs and are healthy; only Web publishes loopback 3100; home/categories/tags/archive/health pass, empty search is 200 `empty_query`, and unknown related is 404 `not_found` through the Web origin.</criterion>
    <criterion>Evidence is strict, sanitized, internally consistent, names `releaseState: BLOCKED`, and matches independent inspections; archived v1 evidence, retained receipt, and 06-VERIFICATION are byte-identical.</criterion>
    <criterion>The evidence commit is later than the implementation commit and contains exactly `ops/phase6-local-refresh-evidence.json`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/refresh-local.mjs &amp;&amp; node scripts/refresh-local.mjs --verify-evidence=ops/phase6-local-refresh-evidence.json &amp;&amp; docker-compose -p blogxlocal -f compose.yaml ps &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>The clean Phase 6 revision is actually served at fixed 3100 with machine-readable provenance, preservation, topology, discovery-route, rollback-capability, and BLOCKED-state evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Correct current-state summaries and hand off to an independent verifier</name>
  <files>.planning/phases/06-public-discovery-data/06-03-SUMMARY.md, .planning/phases/06-public-discovery-data/06-04-SUMMARY.md</files>
  <read_first>
    <item>The committed Task 3 runtime evidence and its implementation revision, image/seed IDs, volume/data/ledger/media comparisons, routes, and release state.</item>
    <item>.planning/phases/06-public-discovery-data/06-03-SUMMARY.md — historical cache-block account whose current readiness statements are now superseded.</item>
    <item>.planning/phases/06-public-discovery-data/06-VERIFICATION.md — independent gaps-found report that the executor must leave untouched.</item>
    <item>.codex/gsd-core/templates/summary.md — summary frontmatter and self-check requirements.</item>
  </read_first>
  <action>
    Add a dated closure addendum to 06-03-SUMMARY that preserves the truth that its original attempt stopped safely but explicitly supersedes the now-stale current-runtime and Phase-8-deferral statements with the committed 06-04 evidence reference. Create 06-04-SUMMARY with the RED/GREEN/evidence/docs commit sequence, exact implementation revision, lock/seed/target image provenance, fixed volume/data/sequence/ledger/media comparisons, old-image rollback coverage, fixed topology, checked Web-origin routes, visible change (discovery APIs now live but Phase 7 UI is still absent), deviations, and no-server/production-BLOCKED statement. Run summary structure/artifact/key-link checks and focused refresh tests. Commit only both summaries as `docs(06-04): complete offline fixed refresh plan`. Do not edit 06-VERIFICATION, REQUIREMENTS, ROADMAP, STATE completion fields, archived v1 files, or the Phase 5 receipt. Hand off to a new independent verifier, which must reconstruct evidence from image/container/volume/data/route facts and alone decide whether to replace the Phase 6 verification report and mark requirements complete.
  </action>
  <acceptance_criteria>
    <criterion>06-03-SUMMARY distinguishes its historical safe block from the later successful closure and no longer claims the fixed runtime is currently stale or that Phase 8 must close this Phase 6 gap.</criterion>
    <criterion>06-04-SUMMARY passes GSD summary verification and cites exact committed evidence rather than untracked logs or recollection.</criterion>
    <criterion>The docs commit is later than the evidence commit and contains exactly 06-03-SUMMARY and 06-04-SUMMARY.</criterion>
    <criterion>06-VERIFICATION and all completion/status authorities remain executor-unchanged for the independent verifier; production remains BLOCKED and neither server was contacted.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node .codex/gsd-core/bin/gsd-tools.cjs verify-summary .planning/phases/06-public-discovery-data/06-04-SUMMARY.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify artifacts .planning/phases/06-public-discovery-data/06-04-PLAN.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify key-links .planning/phases/06-public-discovery-data/06-04-PLAN.md &amp;&amp; node --test scripts/refresh-local.test.mjs &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>The execution history is truthful and complete, and an independent verifier has all committed evidence needed to resolve the sole Phase 6 gap without executor self-certification.</done>
</task>

</tasks>

## Atomic commit sequence

1. RED behavior commit: only `scripts/refresh-local.test.mjs` — `test(06-04): specify offline fixed refresh controls`.
2. GREEN implementation commit: only the two refresh Dockerfiles plus `scripts/refresh-local.mjs` and its test — `feat(06-04): add durable offline local refresh`.
3. Clean-HEAD runtime evidence commit: only `ops/phase6-local-refresh-evidence.json` — `chore(06-04): record fixed runtime refresh evidence`.
4. Later documentation commit: only 06-03-SUMMARY and 06-04-SUMMARY — `docs(06-04): complete offline fixed refresh plan`.
5. A separate new verifier owns any later 06-VERIFICATION and requirement/status commit. The executor must not combine, reorder, amend, or push these commits.

## Artifacts this plan produces

- `apps/api/Dockerfile.refresh` and `apps/web/Dockerfile.refresh`: local-seed, fresh-workspace, offline-frozen, revision-labeled image definitions.
- `scripts/refresh-local.mjs`: fixed-authority refresh and rollback state machine plus strict evidence verifier.
- `scripts/refresh-local.test.mjs`: simulated safety/prohibition/rollback suite with no Docker mutation.
- `ops/phase6-local-refresh-evidence.json`: committed, sanitized proof for the exact implementation revision running at fixed 3100.
- Updated 06-03 historical/current-state distinction and new 06-04 execution summary.

<threat_model>
## Trust Boundaries

| Boundary | Risk | Required control |
|----------|------|------------------|
| Old local image -> dependency seed | Stale application source masquerades as current output | Fresh workspace, current manifests/lock/source, offline frozen reinstall, revision labels, no target retag |
| Clean Git tree -> target images | Wrong revision or origin is deployed | Clean HEAD, lock SHA, seed ID, app/origin labels, post-build and post-start inspect |
| Refresh process -> fixed volumes | Content loss or authority replacement | Exact pre-existing volume identity digest, no volume commands, snapshots and repeated comparisons |
| Migration -> persisted database | Unexpected business/sequence/ledger mutation | Both images first, new API one-off migrate/schema, exact fact comparison, only constrained timestamp change |
| New service cutover -> last known good runtime | Route/health failure leaves site unavailable | Captured immutable old image refs/IDs and automatic API/Web-only rollback |
| Local evidence -> release authority | Local success is mistaken for production approval | Pure BLOCKED gate, sanitized local-only evidence, no server adapter or completion self-certification |

## STRIDE Register

| Threat ID | Category | Severity | Mitigation |
|-----------|----------|----------|------------|
| T-06-17 | Spoofing | high | Image labels and running IDs bind clean HEAD, lock SHA, exact seed IDs, application kind, and fixed origin. |
| T-06-18 | Tampering | high | Offline frozen reinstall in a fresh workspace prevents inherited stale source/dependency drift; strict evidence is atomic. |
| T-06-19 | Denial of Service | high | Two-image build barrier occurs before migration; post-start failures restore old images and health. |
| T-06-20 | Tampering | high | Exact volumes and business/sequence/ledger/media facts are compared across each mutation boundary. |
| T-06-21 | Information Disclosure | medium | Evidence stores digests/identifiers/status only, never credentials, raw rows, media bytes, or environment dumps. |
| T-06-22 | Elevation of Privilege | high | Fixed local constants, no server/network/deploy path, pure RELEASE BLOCKED, and independent verification ownership. |
</threat_model>

<verification>
- `node --test scripts/refresh-local.test.mjs` proves state ordering, strict provenance, pre-mutation failure, data comparison, fixed topology, atomic evidence, and old-image rollback without using Docker.
- `node --test scripts/local-verify.test.mjs`, workspace typecheck, repository boundaries, and pure release gate remain green after implementation.
- The integration run starts from the clean committed GREEN revision and uses only `node scripts/refresh-local.mjs`; no manual substitution can manufacture evidence.
- Evidence verification reconstructs labels, image/container IDs, fixed volume identities, data/sequence/ledger/media comparisons, route contracts, historical hashes, and release BLOCKED.
- Archived v1 evidence, retained Phase 5 receipt, and executor-visible 06-VERIFICATION remain byte-identical; no SSH, server, cloud, deploy, or production transition occurs.
- A new independent verifier must inspect the live fixed runtime and committed evidence before Phase 6 or its four requirements can be marked complete.
</verification>

<success_criteria>
- Cache loss in the original Dockerfile layer no longer blocks a strictly offline current-source build when a dependency-complete local seed exists; missing seed/store packages fail before fixed runtime mutation.
- The exact clean Phase 6 implementation revision, lockfile, seeds, target images, fixed volumes, preserved content, topology, and Web-origin discovery behavior are auditable from committed evidence and independent inspection.
- `http://127.0.0.1:3100` actually serves the new search and related API contracts through the fixed `blogxlocal` Web entry, ready for Phase 7 integration.
- Any post-start failure returns API/Web to the captured last-known-good images without touching persistent volumes.
- Production remains `BLOCKED`; neither cloud server is contacted or modified.
</success_criteria>

<output>
Create `.planning/phases/06-public-discovery-data/06-04-SUMMARY.md`, update 06-03-SUMMARY only after successful runtime evidence, then hand off to a new independent Phase 6 verifier. The executor must not edit 06-VERIFICATION or completion/status authorities and must not push.
</output>
