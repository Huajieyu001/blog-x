---
phase: 06-public-discovery-data
plan: "05"
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - ops/phase6-local-refresh-evidence.json
  - .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
user_setup: []
must_haves:
  truths:
    - "The fixed refresh starts from a clean committed 06-04 implementation, preflights exactly project `blogxlocal`, its three healthy containers, the two existing volume identity digests, clean revision/lock, local seed IDs, historical evidence, data and topology before mutation."
    - "Both revision-tagged API/Web images build with networking disabled from sanitized dependency seeds and pass exact label plus filesystem inspection before migration: pnpm resolves the copied complete version directory below `/pnpm-store`, `/workspace` and old source/node_modules/build paths are absent, and current runtime/build authority exists only below `/refresh-workspace`."
    - "If either seed/store/package/build/label/filesystem check fails, the run stops before migration or fixed-service mutation; it cannot use registry access, stale retagging, a different project, or a manual command substitute."
    - "Only after both images pass does the new API image run idempotent migration/schema verification; fixed PostgreSQL/media volume identity, business data, sequences, media, ledger row count and stable tuples stay exact, and only the existing ledger `applied_at` advances strictly."
    - "Only fixed API/Web are recreated with revision tags; PostgreSQL remains the same healthy container, API/PostgreSQL publish no host ports, and Web publishes only `127.0.0.1:3100`."
    - "A post-start failure restores API/Web to captured old configured image references and exact old image IDs, verifies health, leaves volumes/PostgreSQL untouched, writes no passing evidence, and exits nonzero."
    - "Task 1 invokes the mutating `node scripts/refresh-local.mjs` entry exactly once; all subsequent verification begins with its read-only `--verify-evidence` mode and committed evidence binds the clean implementation-containing HEAD plus all store, provenance, preservation, topology, route and BLOCKED facts."
    - "Archived v1 evidence, the retained Phase 5 receipt, and executor-owned 06-VERIFICATION stay byte-identical; 06-03 receives a dated 06-05 closure addendum, 06-05-SUMMARY reports current truth, and a new independent verifier alone decides Phase 6 completion."
  artifacts:
    - path: ops/phase6-local-refresh-evidence.json
      provides: sanitized machine-readable proof that exact clean revision images with no inherited app tree run at fixed 3100 on preserved data authority
    - path: .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
      provides: historical safe-block account plus dated current-state supersession by successful 06-05 evidence
    - path: .planning/phases/06-public-discovery-data/06-05-SUMMARY.md
      provides: execution, evidence, visible runtime change, rollback coverage, safety, and independent-verifier handoff
  key_links:
    - from: scripts/refresh-local.mjs
      to: apps/api/Dockerfile.refresh
      via: exact fixed run builds and inspects sanitized current-revision API target before database action
      pattern: "Dockerfile\\.refresh|--network=none|--pull=false|/refresh-workspace|inspect"
    - from: scripts/refresh-local.mjs
      to: apps/web/Dockerfile.refresh
      via: exact fixed run builds and inspects sanitized current-revision Web target with fixed origin before cutover
      pattern: "blog-x-web-local:|PUBLIC_ORIGIN|127\\.0\\.0\\.1:3100|/workspace"
    - from: scripts/refresh-local.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: atomic evidence publication occurs only after image-filesystem, provenance, migration, preservation, topology, route, history and BLOCKED checks
      pattern: "phase6-local-refresh-evidence\\.json|implementationRevision|filesystem|volumeIdentity|routes|BLOCKED"
    - from: compose.yaml
      to: http://127.0.0.1:3100/api/public/search?q=
      via: fixed revision-tagged Web same-origin proxy reaches the refreshed Phase 6 API
      pattern: "INTERNAL_API_ORIGIN|PUBLIC_ORIGIN|3100"
  prohibitions:
    - statement: "Execution must not use manual Docker substitutions, pull/network fallback, stale target retagging, Compose down, a volume-removal action, PostgreSQL recreation, alternate projects, or non-loopback publication."
      status: pending
      verification: command
    - statement: "Failure handling must not touch either fixed volume or non-blogxlocal resources and must not publish passing evidence before rollback and all terminal checks succeed."
      status: pending
      verification: test
    - statement: "The executor must not edit archived v1 evidence, the retained receipt, 06-VERIFICATION or completion fields; use SSH, contact servers, deploy, unfreeze production, transition from BLOCKED, or push."
      status: pending
      verification: command
---

<objective>
Use the committed sanitized offline primitive to refresh the actual fixed `blogxlocal` API/Web at 3100, retain auditable evidence, correct current-state summaries, and hand off independent re-verification.

Purpose: Close Phase 6's sole stale-runtime gap without risking persistent content or confusing local success with production authority.
Output: A successful fixed runtime, one evidence-only commit, a truthful 06-03 closure addendum, 06-05-SUMMARY, and independent-verifier handoff.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-03-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-04-PLAN.md
@.planning/phases/06-public-discovery-data/06-04-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@apps/api/Dockerfile.refresh
@apps/web/Dockerfile.refresh
@scripts/refresh-local.mjs
@scripts/refresh-local.test.mjs
@compose.yaml
@scripts/release-gate.mjs
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json
@.planning/milestones/v1.0-MILESTONE-AUDIT.md

<interfaces>
- Define `REFRESH_REVISION` as `git rev-parse HEAD` from an empty `git status --porcelain` at the committed 06-04 completion HEAD that contains the GREEN implementation. Run the mutating entry exactly once in Task 1 action. That exact clean implementation-containing HEAD, short HEAD and `pnpm-lock.yaml` SHA-256 become target labels and evidence authority; the later evidence commit must not alter them.
- Fixed runtime authority is exactly three healthy containers under `blogxlocal`; fixed volumes must already exist. Canonical volume identity is the digest of normalized inspect fields `Name,Driver,Mountpoint,CreatedAt,Scope,Labels,Options` and must match at every boundary.
- Before builds, snapshot container refs/IDs/health/ports; seed refs/IDs/workdirs/store paths; normalized business data excluding `blog_x_schema_ledger`; exact sequences; complete ledger stable tuples and timestamps; media paths/sizes/digests; all tracked `.planning/milestones` hashes; retained receipt hash; and 06-VERIFICATION hash.
- Build both targets through the single 06-04 script invocation with `--network=none --pull=false`; no other command may perform the refresh. Require labels for revision/lock/seed/application/origin/kind and require filesystem probes to prove `corepack pnpm --store-dir=/pnpm-store store path` resolves the populated neutral version directory, no store payload is flattened at the unversioned root, `/workspace` plus legacy markers/node_modules/build output are absent, Config.WorkingDir is `/refresh-workspace`, commands are rooted there, and only current API/Web artifacts exist below it.
- Both image builds and inspections precede any migration. Then use the new API image in one-off no-dependency Compose runs for `db:migrate` and `db:schema:verify`; require business/sequence/media equality, identical ledger row count/stable tuples and strictly later `applied_at` for the same existing row.
- Cut over exactly `api web` using revision tags and `docker-compose -p blogxlocal -f compose.yaml up -d --wait --no-build --no-deps api web`. Do not recreate PostgreSQL. Require actual target image IDs and labels, three healthy services, no API/PostgreSQL host port and exactly loopback 3100 for Web.
- Web-origin checks are `/`, `/categories`, `/tags`, `/archive`, `/api/health`, `/api/public/search?q=`, and `/api/public/articles/phase6-unknown/related`, with exact search `200/state=empty_query` and related `404/error=not_found` contracts.
- On any failure after cutover begins, use only the script's captured old refs to recreate API/Web on exact old image IDs, wait healthy, compare volume/PostgreSQL identity again, suppress evidence and exit nonzero. A rollback failure is terminal and never broadens cleanup.
- Success evidence is deterministic, sanitized and atomic. It records schema/status; REFRESH_REVISION/lock; SOURCE_STORE and computed neutral version-path results without host-sensitive contents; seed/target refs/IDs/labels/filesystem probes; old/new runtime facts; volumes/data/sequence/ledger/media comparisons; routes; historical hashes; and `releaseState: BLOCKED`, but no credentials, raw rows, media contents, environment dumps, or server facts.
- `node scripts/refresh-local.mjs --verify-evidence=ops/phase6-local-refresh-evidence.json` is strictly read-only: it reconstructs and compares evidence/runtime facts but cannot build, migrate, recreate, roll back, or rewrite evidence. Task 1 verification must begin with this mode and must contain no second bare refresh invocation.
- The executor commits evidence before editing summaries. 06-03 keeps its original safe-block history but receives a dated “closed by 06-05” addendum and corrected current readiness. 06-05-SUMMARY, never 06-04-SUMMARY, owns actual refresh results.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh fixed blogxlocal from clean HEAD and commit evidence</name>
  <files>ops/phase6-local-refresh-evidence.json</files>
  <read_first>
    <item>The committed 06-04 implementation and 06-04-SUMMARY; stop if worktree is not clean.</item>
    <item>scripts/refresh-local.mjs/tests and both refresh Dockerfiles — exact tested authority.</item>
    <item>Live fixed container/volume facts and immutable v1/receipt/verification inputs.</item>
  </read_first>
  <precondition>`git status --porcelain` is empty; exact fixed containers are healthy; exact fixed volumes and local seeds exist; no `blogxverify_*` resource or receipt/refresh lock is active. Otherwise stop before mutation.</precondition>
  <action>
    Capture every preflight fact from interfaces and assert the clean REFRESH_REVISION, then invoke exactly once `node scripts/refresh-local.mjs`. Do not substitute manual build/migration/Compose commands and do not invoke the bare entry again in acceptance verification. Require two offline sanitized builds, computed neutral version-store checks, and label/filesystem inspections before migration, then migration/schema, API/Web-only cutover, preservation/topology/route/BLOCKED checks, and atomic evidence. After that single mutating run, use only the read-only `--verify-evidence` mode plus read-only Compose/Docker/Git/release checks to re-inspect target store/filesystems and running IDs/labels, fixed PostgreSQL/volumes, data comparisons, ports, routes, v1/receipt/verification hashes, and evidence schema. If pre-mutation authority is missing, stop safely. If post-start verification fails, require old-image rollback and healthy fixed runtime before stopping. On success commit only evidence as `chore(06-05): record fixed runtime refresh evidence`; evidence continues to name REFRESH_REVISION, not the later evidence commit.
  </action>
  <acceptance_criteria>
    <criterion>The bare mutating command executes exactly once from clean REFRESH_REVISION without registry/server/production access; source tests and captured command events reject a second invocation.</criterion>
    <criterion>Both target filesystems resolve the populated pnpm version directory through `--store-dir=/pnpm-store`, contain no flat-root store payload, inherited `/workspace`, seed source/node_modules/build output or legacy marker; current workdir/commands/artifacts live only under `/refresh-workspace`.</criterion>
    <criterion>Both image IDs/labels and running API/Web IDs bind exact HEAD, lock, seeds, applications and fixed origin before/after cutover.</criterion>
    <criterion>PostgreSQL container and fixed volume identities are unchanged; business/sequence/media and ledger stable tuples match, with only strict ledger timestamp advance.</criterion>
    <criterion>Only Web publishes loopback 3100; all routes/contracts pass and release remains BLOCKED.</criterion>
    <criterion>Evidence is strict/sanitized/internally consistent and remains bound to REFRESH_REVISION after its evidence-only commit; v1, receipt and 06-VERIFICATION are byte-identical.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/refresh-local.mjs --verify-evidence=ops/phase6-local-refresh-evidence.json &amp;&amp; docker-compose -p blogxlocal -f compose.yaml ps &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>The exact clean revision is genuinely running on fixed 3100 with sanitized-image, persistence, topology, route, rollback and BLOCKED evidence committed separately.</done>
</task>

<task type="auto">
  <name>Task 2: Correct current summaries and hand off independent verification</name>
  <files>.planning/phases/06-public-discovery-data/06-03-SUMMARY.md, .planning/phases/06-public-discovery-data/06-05-SUMMARY.md</files>
  <read_first>
    <item>The committed Task 1 evidence and its exact revision/image/filesystem/data/route facts.</item>
    <item>06-03-SUMMARY historical safe block, 06-04-SUMMARY implementation result, and untouched 06-VERIFICATION gap report.</item>
    <item>GSD summary template and verification commands.</item>
  </read_first>
  <action>
    Add a dated 06-05 closure addendum to 06-03-SUMMARY without erasing its original fail-closed event; replace only current-state claims that fixed 3100 is stale or Phase 8 must close this specific gap. Create 06-05-SUMMARY with exact ordered commits, clean revision/lock/seed/target provenance, filesystem sanitation proof, volume/data/sequence/ledger/media preservation, rollback coverage, topology, routes, visible change (Phase 6 APIs live; Phase 7 UI still absent), production BLOCKED, and no-server facts. Run summary/artifact/key-link and focused tests. Commit only both summaries as `docs(06-05): complete fixed runtime refresh plan`. Leave 06-VERIFICATION and completion/status authorities to a new independent verifier that reconstructs live/evidence facts before deciding Phase 6 completion.
  </action>
  <acceptance_criteria>
    <criterion>06-03 accurately distinguishes historical block from current 06-05 closure and cites committed evidence.</criterion>
    <criterion>06-05-SUMMARY, not 06-04-SUMMARY, owns actual fixed refresh evidence and passes GSD summary verification.</criterion>
    <criterion>Docs commit follows evidence and contains exactly 06-03-SUMMARY plus 06-05-SUMMARY.</criterion>
    <criterion>06-VERIFICATION, requirements/roadmap/state completion, v1 archive and receipt remain executor-unchanged; production BLOCKED and no servers contacted.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node .codex/gsd-core/bin/gsd-tools.cjs verify-summary .planning/phases/06-public-discovery-data/06-05-SUMMARY.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify artifacts .planning/phases/06-public-discovery-data/06-05-PLAN.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify key-links .planning/phases/06-public-discovery-data/06-05-PLAN.md &amp;&amp; node --test scripts/refresh-local.test.mjs &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>Current documentation is truthful and a separate verifier has committed evidence and live facts needed to resolve the sole Phase 6 gap without executor self-certification.</done>
</task>

</tasks>

## Atomic commit sequence

1. Plan 06-04 RED commit.
2. Plan 06-04 GREEN implementation commit.
3. Plan 06-04 summary docs commit; 06-05 begins only from this clean committed HEAD.
4. Plan 06-05 evidence-only commit — `chore(06-05): record fixed runtime refresh evidence`.
5. Plan 06-05 closure docs commit — `docs(06-05): complete fixed runtime refresh plan`.
6. A new independent verifier alone owns later 06-VERIFICATION and requirement/status changes. Executors do not amend, reorder, combine, push, or self-certify these commits.

<verification>
- One exact fixed refresh command runs from the clean committed 06-04 completion HEAD.
- Built and running targets pass labels plus actual filesystem sanitation checks before migration/cutover.
- Fixed PostgreSQL/volumes/data, old-image rollback, topology, discovery routes, historical evidence and BLOCKED state are independently reconstructable.
- Evidence and docs are separate ordered commits; executor leaves verification/completion authority untouched.
- No server, SSH, registry, deployment, unfreeze, production transition, or push occurs.
</verification>

<success_criteria>
- Current Phase 6 discovery endpoints are served through fixed `http://127.0.0.1:3100` by clean-revision sanitized images.
- Local content and data authority are preserved exactly and a failed cutover restores old images without volume action.
- Committed evidence and summaries are sufficient for an independent verifier to close the sole Phase 6 gap.
- Production remains `BLOCKED` and both servers remain untouched.
</success_criteria>

<output>
Create `.planning/phases/06-public-discovery-data/06-05-SUMMARY.md`, update 06-03-SUMMARY only after committed successful evidence, then hand off to a new independent Phase 6 verifier. Do not edit 06-VERIFICATION/completion fields or push.
</output>
