---
phase: 06-public-discovery-data
plan: "09"
type: execute
wave: 8
depends_on:
  - 06-08
files_modified:
  - ops/phase6-local-refresh-evidence.json
  - .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
autonomous: true
gap_closure: true
independent_plan_check: required
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
estimate:
  tokens: 6000
  confidence: medium
  calibration_factor: 1
user_setup: []
must_haves:
  truths:
    - "Execution is eligible only after an independent GSD plan checker reports no blocker and a clean committed revision contains the complete 06-08 GREEN implementation plus 06-08-SUMMARY; it differs from unattempted `3221f99` and has no claim/evidence."
    - "The executor mechanically proves the exact revision claim absent, invokes `node scripts/refresh-local.mjs` with no options exactly once, then mechanically proves the claim present on either exit path; no wrapper, import, alias, manual phase or second invocation can substitute or retry it."
    - "A failed sole invocation is terminal for that revision: the claim persists, evidence remains absent, exact generated one-off is absent, and read-only reconstruction proves pre-mutation stop or immutable-old-image rollback with all persistence/protected facts preserved. A later attempt requires a separately committed remediation revision and new plan."
    - "A successful invocation produces strict sanitized evidence v3 only after seed/target probes, target-API migration/schema identity, exact fact transitions, API/Web cutover, same-origin route bodies and canonical BLOCKED all pass."
    - "After success, the first and every later check is read-only `--verify-evidence`/claim inspection or read-only status; evidence reconstruction rejects current drift across images/labels/filesystems/store, PostgreSQL/volumes/business/sequences/ledger/media, history, topology/routes and release."
    - "Evidence-only and docs-only commits are separate and ordered. The executor leaves archived v1, retained Phase 5 receipt, 06-VERIFICATION and REQUIREMENTS/ROADMAP/STATE completion byte-identical; a fresh independent Phase 6 verifier alone decides completion afterward."
    - "Only fixed local `blogxlocal` resources are in scope; no registry/network/pull, SSH/server/cloud, deployment, unfreeze, production transition or push occurs, and release remains `BLOCKED`."
  artifacts:
    - path: ops/phase6-local-refresh-evidence.json
      provides: strict sanitized v3 proof bound to the exact clean 06-08 implementation revision and current reconstructed fixed runtime
    - path: .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
      provides: dated current-state closure while preserving every historical safe stop
    - path: .planning/phases/06-public-discovery-data/06-09-SUMMARY.md
      provides: sole-attempt, evidence, visible runtime, rollback safety and independent-verifier handoff
  key_links:
    - from: scripts/refresh-local.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: the sole no-option invocation owns all mutation and terminal atomic evidence publication
      pattern: "runRefreshCli|createLiveRefreshAdapter|phase6-local-refresh-evidence"
    - from: scripts/refresh-local-facts.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: read-only verification reconstructs current sanitized facts and rejects every stage/runtime drift
      pattern: "collectRefreshFacts|projectSanitizedFacts|BLOCKED"
    - from: ops/phase6-local-refresh-evidence.json
      to: .planning/phases/06-public-discovery-data/06-09-SUMMARY.md
      via: summary cites exact evidence commit/digest/revision without self-certifying phase completion
      pattern: "implementationRevision|sha256|independent|BLOCKED"
  prohibitions:
    - statement: "Never invoke the no-option refresh more than once for the same implementation revision, including after pre-mutation, build, migration, cutover, route, evidence or rollback failure."
      status: pending
      verification: command
    - statement: "Do not manually create/edit/remove the claim/evidence, substitute Docker/Compose/migration/cutover/rollback commands, use mutable old refs, or broaden cleanup beyond the exact generated one-off."
      status: pending
      verification: command
    - statement: "Do not contact registry/servers, use SSH, deploy, unfreeze, push, edit protected evidence/completion authority or transition release from BLOCKED."
      status: pending
      verification: command
---

<objective>
Consume exactly one attempt for the independently checked, fully remediated clean revision; commit reconstructed evidence and truthful summaries; then hand completion to an independent verifier.

Purpose: Close Phase 6's stale fixed-runtime gap with one auditable local mutation boundary and no production authority.
Output: Current Phase 6 API/Web at fixed 3100, evidence-only commit, docs-only closure commit, and final-verifier handoff.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-03-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-06-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-07-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-08-PLAN.md
@.planning/phases/06-public-discovery-data/06-08-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@scripts/refresh-local-facts.mjs
@scripts/refresh-local-live.mjs
@scripts/refresh-local.mjs
@scripts/refresh-local.test.mjs
@apps/api/Dockerfile.refresh
@apps/web/Dockerfile.refresh
@compose.yaml
@scripts/release-gate.mjs
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json
@.planning/milestones/v1.0-MILESTONE-AUDIT.md

<interfaces>
- Require a recorded no-blocker result from a fresh independent GSD plan checker for 06-08 and 06-09. Establish `REFRESH_REVISION` from exact clean `git rev-parse HEAD`; it contains 06-08 implementation and summary, is not `3221f99b6617180536f558583e2d84585113813c`, and remains the evidence implementation authority after later commits.
- Before consuming the attempt, use only read-only checks: clean worktree; exact three healthy fixed services/project/labels/null-port topology; exact existing volumes/local seeds; no generated verifier/one-off/refresh lock; canonical BLOCKED; protected hashes; absent evidence; and `--check-attempt-claim=absent --revision=<literal full REFRESH_REVISION>`. Any failure stops without a claim/invocation.
- Invoke exactly one process with exact argv `node scripts/refresh-local.mjs`. No shell wrapper, pnpm alias, dynamic import or direct adapter call is another permitted path. Once process creation begins the revision attempt is consumed regardless of its result.
- Immediately after process exit, before any branch, run only `--check-attempt-claim=present --revision=<literal full REFRESH_REVISION>` and require canonical bytes/digest. On nonzero refresh exit, perform only read-only claim/evidence/one-off/fixed-runtime/fact inspection. Require no final evidence, no generated one-off, and either unchanged pre-mutation facts or verified immutable-old-image rollback facts. Stop; never rerun the same revision.
- On zero exit, begin with `--verify-evidence=ops/phase6-local-refresh-evidence.json`; hash evidence and claim before/after and require exact bytes. The verifier must reconstruct current target IDs/selected labels, seed stability, target filesystem/store probes, fixed Compose authority/ports, PostgreSQL/volumes/business/sequences/ledger/media/protected hashes, exact Web-origin routes and release BLOCKED through read-only commands only.
- Evidence is v3 and names REFRESH_REVISION, lock and claim digest. Its stage projections prove preflight→postMigration's sole `phase1.applied_at` advance, postMigration→postCutover persistence equality and exact target image transition. No raw content/path/env/command exists. Commit only evidence as `chore(06-09): record remediated fixed refresh evidence`; do not alter it after commit.
- After evidence commit, append a dated 06-09 closure addendum to 06-03-SUMMARY while retaining the original cache stop, `b7fa05c` stub stop and `3221f99` audit stop. Create 06-09-SUMMARY with exact one-attempt count, implementation/evidence commits and digests, fact/provenance/rollback/topology/route results, Phase 6 API visibility, Phase 7 UI absence, BLOCKED and zero-server facts. Commit only both summaries as `docs(06-09): complete remediated fixed refresh`.
- Do not edit 06-VERIFICATION or requirement/roadmap/state completion. After docs commit, dispatch a fresh independent Phase 6 verifier that reads plans/summaries/evidence/current runtime and alone decides whether the 19/20 report can close.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume the sole clean-revision attempt and commit reconstructed evidence</name>
  <files>ops/phase6-local-refresh-evidence.json</files>
  <precondition>Independent plan check has no blocker; worktree is clean at a committed 06-08 implementation-plus-summary revision distinct from `3221f99`; exact fixed authority and BLOCKED pass read-only preflight; evidence and exact revision claim are absent. Otherwise stop before process creation.</precondition>
  <action>
    Capture REFRESH_REVISION and read-only preflight receipts outside the repository. Run the literal-revision absent claim check, then start `node scripts/refresh-local.mjs` exactly once. Immediately run the literal-revision present check after exit. On failure, perform only read-only reconstruction sufficient to prove claim persistence, evidence suppression, one-off cleanup and pre-mutation/rollback safety, then stop and require a new committed remediation revision/plan. On success, run `--verify-evidence` first, require claim/evidence bytes unchanged, and allow only further read-only reconstruction/status commands. Confirm every v3 fact/delta, exact target runtime, strict routes and BLOCKED, then commit only the evidence file while retaining REFRESH_REVISION as its authority.
  </action>
  <verify>
    <automated>node scripts/refresh-local.mjs --check-attempt-claim=present --revision=&lt;literal-full-REFRESH_REVISION&gt; &amp;&amp; node scripts/refresh-local.mjs --verify-evidence=ops/phase6-local-refresh-evidence.json &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>One and only one clean-revision invocation succeeded, current fixed runtime matches strict v3 evidence, and evidence is committed separately.</done>
</task>

<task type="auto">
  <name>Task 2: Record truthful closure and hand off independent final verification</name>
  <files>.planning/phases/06-public-discovery-data/06-03-SUMMARY.md, .planning/phases/06-public-discovery-data/06-09-SUMMARY.md</files>
  <action>
    Use only committed evidence and read-only verification results. Preserve all historical safe stops in 06-03 and append the dated 06-09 closure. Create 06-09-SUMMARY with exact ordered commits, attempt count, evidence digest, all fact transitions, target migration/provenance/filesystem/rollback proof, route visibility, BLOCKED and no-server results. Validate summary artifacts/key links and rerun focused unit checks only; commit the two summaries separately. Leave completion authority untouched and request a fresh independent Phase 6 verifier after the docs commit.
  </action>
  <verify>
    <automated>node .codex/gsd-core/bin/gsd-tools.cjs verify-summary .planning/phases/06-public-discovery-data/06-09-SUMMARY.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify artifacts .planning/phases/06-public-discovery-data/06-09-PLAN.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify key-links .planning/phases/06-public-discovery-data/06-09-PLAN.md &amp;&amp; node --test scripts/refresh-local.test.mjs &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>Evidence and truthful summaries are committed in order; a separate verifier has sole authority to update Phase 6 verification and completion.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| ID | Threat | Severity | Control / verification |
|---|---|---|---|
| T-06-09-01 | Same revision is attempted twice | high | Mechanical absent/present claim gates, exact one-process rule and terminal failure requiring a new revision/plan. |
| T-06-09-02 | Stale/wrong image reaches migration/runtime | high | Seed reinspection, target probes, inspected target-API one-off and current target ID reconstruction. |
| T-06-09-03 | Persistent/protected data changes | high | Shared fact transitions and read-only verifier across database, sequences, ledger, media, volumes, PostgreSQL and history. |
| T-06-09-04 | Rollback/evidence falsely passes | high | Immutable old IDs, full rollback facts, one-off cleanup, evidence suppression and terminal non-retry. |
| T-06-09-05 | Evidence is forged or leaks content | high | Strict digest/count-only v3 schema, atomic publication and complete current-fact reconstruction. |
| T-06-09-06 | Remote/production authority expands | high | Exact local argv only, no registry/server/deploy/push, loopback-only Web and canonical BLOCKED. |
</threat_model>

<verification>
- Independent plan checker passes before execution; independent Phase 6 verifier runs only after evidence/docs commits.
- Exactly one bare process consumes the clean 06-08 revision; every later operation is read-only.
- Failure is terminal with persistent claim and no evidence; success yields evidence-only then docs-only commits.
- Protected evidence/completion authority and production BLOCKED remain unchanged by the executor.
</verification>

<success_criteria>
- Exact current revision API/Web is visible through fixed 3100 with complete reconstructed persistence, provenance, topology, routes and BLOCKED evidence.
- No same-revision duplicate refresh path or manual mutation substitute exists.
- Historical safe stops remain truthful and final completion is independently verified.
- No server, registry, deployment, unfreeze, production transition or push occurs.
</success_criteria>

<output>
Create `ops/phase6-local-refresh-evidence.json`, update 06-03-SUMMARY, create 06-09-SUMMARY, then hand off to a fresh independent Phase 6 verifier. Do not edit completion authority or push.
</output>
