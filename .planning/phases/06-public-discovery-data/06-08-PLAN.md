---
phase: 06-public-discovery-data
plan: "08"
type: execute
wave: 7
depends_on:
  - 06-06
files_modified:
  - scripts/refresh-local-facts.mjs
  - scripts/refresh-local-live.mjs
  - scripts/refresh-local.mjs
  - scripts/refresh-local.test.mjs
autonomous: true
gap_closure: true
independent_plan_check: required
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
estimate:
  tokens: 18000
  confidence: medium
  calibration_factor: 1
user_setup: []
must_haves:
  truths:
    - "Real Docker inspect port fixtures are authoritative: API `3001/tcp:null` and PostgreSQL `5432/tcp:null` mean exposed in-image but not host-published and pass; any non-null API/PostgreSQL binding fails; Web has exactly one `3100/tcp` binding to `127.0.0.1:3100` and no other host binding."
    - "Fixed authority requires exactly Compose project `blogxlocal`, services `postgres/api/web`, canonical container names, exact Compose project/service/oneoff labels, exact existing volume names and no unexpected fixed-project container/service; every stage fails closed on drift."
    - "A shared strict read-only collector captures canonical container/image/label/topology facts, exact volume identities, PostgreSQL identity, normalized business dump excluding the ledger, all sequence state, ledger stable tuples/timestamps, media path/byte/hash inventory, archived-v1/Phase-5-receipt/06-VERIFICATION hashes, routes and canonical BLOCKED at preflight, post-migration, post-cutover, rollback and evidence verification."
    - "Migration and schema verification run inside one exact generated fixed-project one-off whose Compose environment selects the inspected target-revision API tag; before either command, container inspect proves its immutable image ID equals the target API ID and its project/service/oneoff labels are exact."
    - "Business dump, sequences, media, volumes, PostgreSQL identity and protected historical hashes remain exact; ledger row count and `{scope,migration_count,migration_fingerprint}` tuples remain exact, only existing `phase1.applied_at` advances strictly after migration, and every other timestamp remains exact."
    - "Both seed references are resolved tag-to-ID before builds and re-inspected after each/both build barriers; any tag drift fails before migration. Target images pass exact required labels plus real no-network filesystem/pnpm-store probes proving `/workspace` and flattened store absence and current `/refresh-workspace` authority."
    - "Every subprocess and adapter-supplied environment is accepted only by a token-for-token command constructor/allowlist; missing, reordered, duplicated or appended flags/tails and every alternate project/service/ref/path/network/pull/server command fail."
    - "The production attempt root is literally `/private/tmp/blog-x-refresh-attempts`, has complete parent/final realpath, lstat, owner, type and mode protections, accepts no runtime root override, and publishes one canonical revision claim atomically with concurrent single-winner and failure persistence."
    - "A post-cutover failure restores API/Web from captured immutable old image IDs, removes only the exact generated migration one-off, suppresses/removes generated evidence temporaries, and uses the shared collector to prove old images/topology/routes plus persistence and protected facts; PostgreSQL, volumes and unrelated resources are never acted upon."
    - "Evidence has a strict exact-key schema and contains only sanitized IDs, selected labels, statuses, counts and SHA-256 digests—no raw rows/media, mount paths, environment, commands, secrets or claim path. It is complete-before-publish, no-overwrite atomic, cleans only its generated temporary, fsyncs the directory and appears only after exact route bodies and BLOCKED."
    - "`--verify-evidence` performs no mutation and reconstructs all current facts through the same collector, image/store/filesystem probes and claim reader; it rejects any evidence, runtime, persistence, route, protected-history, topology, provenance or release drift and leaves evidence/claim bytes exact."
    - "Plan 06-08 never invokes the no-option refresh, Docker, Compose, an offline probe, a server or network. The unclaimed clean `3221f99` audit stop remains truthful; only 06-09 may attempt a later clean implementation-plus-summary revision after an independent plan check passes."
  artifacts:
    - path: scripts/refresh-local-facts.mjs
      provides: shared canonical read-only fact collection, strict topology/route parsing, digest projections and stage/delta comparisons
      exports:
        - collectRefreshFacts
        - assertFixedRuntimeAuthority
        - assertPersistenceTransition
        - projectSanitizedFacts
    - path: scripts/refresh-local-live.mjs
      provides: corrected exact-argv state machine, safe claims, seed/target probes, target-image migration, rollback, atomic evidence and reconstructing verifier
      exports:
        - createLiveRefreshAdapter
        - createRefreshAttemptStore
        - verifyLiveRefreshEvidence
    - path: scripts/refresh-local.mjs
      provides: fail-closed CLI dispatch and shared read-only evidence/claim entry points without duplicate live authority
    - path: scripts/refresh-local.test.mjs
      provides: exhaustive RED/GREEN P0/P1 fixtures and complete fake state-machine evidence
  key_links:
    - from: scripts/refresh-local-live.mjs
      to: scripts/refresh-local-facts.mjs
      via: every preflight/migration/cutover/rollback/verifier boundary calls the same collector and stage comparator
      pattern: "collectRefreshFacts|assertFixedRuntimeAuthority|assertPersistenceTransition"
    - from: scripts/refresh-local-live.mjs
      to: apps/api/Dockerfile.refresh
      via: seed tag/ID stability, target build labels, filesystem/store probes and target-image one-off migration
      pattern: "Dockerfile\\.refresh|SEED_IMAGE_ID|db:migrate|db:schema:verify"
    - from: scripts/refresh-local-live.mjs
      to: compose.yaml
      via: exact config/service/label authority, generated target-API one-off and API/Web-only immutable-image cutover/rollback
      pattern: "blogxlocal|com\\.docker\\.compose|api|web|postgres"
    - from: scripts/refresh-local-live.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: sanitized stage projections publish atomically after routes/BLOCKED and are reconstructed read-only
      pattern: "phase6-local-refresh-evidence|projectSanitizedFacts|fsync|BLOCKED"
  prohibitions:
    - statement: "Do not defer any audited P0/P1 item to Phase 8 or the executor; all nine audit groups must be RED-tested and implemented in 06-08 before a live attempt."
      status: pending
      verification: test
    - statement: "No permissive `includes`/prefix-only argv acceptance, extra token/tail, dynamic authority, shell interpolation, registry pull/build network, Compose down, fixed-volume removal, PostgreSQL recreation, non-loopback route, SSH/server/deploy or READY transition is allowed."
      status: pending
      verification: test
    - statement: "06-08 must not run `node scripts/refresh-local.mjs` without options, `--probe-offline-builds`, Docker/Compose, write a real claim/evidence, edit protected history/verification/completion, contact servers, push or mutate runtime."
      status: pending
      verification: command
---

<objective>
Remediate every audited P0/P1 deficiency in the Phase 6 live refresh adapter under comprehensive TDD, without consuming a live attempt.

Purpose: Make the later single fixed-runtime refresh mechanically safe, data-preserving and independently reconstructable rather than assertion-shaped.
Output: Shared strict fact collector, corrected live state machine/CLI, exhaustive tests, and 06-08 summary on a clean committed revision.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/references/tdd.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-CONTEXT.md
@.planning/phases/06-public-discovery-data/06-RESEARCH.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@.planning/phases/06-public-discovery-data/06-04-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-06-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-07-PLAN.md
@.planning/phases/06-public-discovery-data/06-07-SUMMARY.md
@scripts/refresh-local.mjs
@scripts/refresh-local-live.mjs
@scripts/refresh-local.test.mjs
@scripts/refresh-seed-store.mjs
@apps/api/Dockerfile.refresh
@apps/web/Dockerfile.refresh
@compose.yaml
@scripts/release-gate.mjs
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json

<interfaces>
- Keep constants exact: project `blogxlocal`, origin `http://127.0.0.1:3100`, fixed containers `blogxlocal-postgres-1/blogxlocal-api-1/blogxlocal-web-1`, volumes `blogxlocal_postgres-data/blogxlocal_media-data`, refresh services `api web`, claim root `/private/tmp/blog-x-refresh-attempts`, evidence `ops/phase6-local-refresh-evidence.json`.
- Parse raw Docker inspect `NetworkSettings.Ports`, where an exposed-but-unpublished entry has value `null`. Require API keys to be exactly `3001/tcp:null`, PostgreSQL exactly `5432/tcp:null`, and Web exactly `3100/tcp:[{HostIp:"127.0.0.1",HostPort:"3100"}]`; reject a non-null binding, wildcard/IPv6 bind, extra binding/port, duplicate or malformed object. Unit tests use verbatim Docker inspect-shaped objects, including the currently rejected null form, so current implementation is demonstrably RED.
- Read exact Compose config/ps and container inspect facts. Fixed containers require canonical names, health, `com.docker.compose.project=blogxlocal`, respective service labels, `com.docker.compose.oneoff=False`, and membership in the exact three-service set. Volumes require exact normalized inspect fields in memory. Reject unexpected/missing fixed project resources before claim/build and at every later boundary.
- `collectRefreshFacts` is the single read-only authority at `preflight`, `postMigration`, `postCutover`, `rollback`, and `verifyEvidence`. It executes only exact argv and returns canonical sorted values plus digests: normalized data-only pg_dump excluding `public.blog_x_schema_ledger`; all ordered `pg_sequences` fields; ordered ledger `{scope,migration_count,migration_fingerprint,applied_at}`; media `{relativePath,bytes,sha256}` gathered by a fixed Node argv inside the exact API container; volume inspect; PostgreSQL/container/image/selected-label/topology; `git ls-files .planning/milestones` byte hashes plus fixed receipt and 06-VERIFICATION; exact route observations; and release-gate BLOCKED result.
- Stage comparison is explicit. `preflight -> postMigration` permits only strict advance of existing `scope=phase1.applied_at`; stable ledger tuples/count, all other timestamps, business dump, sequences, media, volumes, PostgreSQL identity, protected hashes, fixed runtime and pre-cutover routes are exact. `postMigration -> postCutover` keeps persistence/protected facts exact while API/Web images become inspected targets and routes become exact Phase 6 success. `postMigration -> rollback` keeps persistence/protected facts exact while API/Web immutable old image IDs and preflight route/topology facts return; passing evidence is absent.
- Before builds, resolve each running container's configured seed reference with exact `docker image inspect <ref>` and require its ID equals container `Image`. Re-inspect both original references after each build and once after both builds; any ID change is terminal before migration. Targets use exact `docker build --network=none --pull=false` token order/build args. Inspect target IDs/config/selected labels, run exact `docker run --rm --network=none --entrypoint corepack <target> pnpm --store-dir=/pnpm-store store path`, then an exact fixed Node `-e` filesystem probe (not shell) proving computed `/pnpm-store/v<digits>`, no unversioned payload, no `/workspace`, correct `/refresh-workspace` workdir/command and current API/Web artifacts.
- Migration owns one generated name `blogxlocal-api-refresh-<12-hex-revision>`. With adapter environment exactly `BLOG_X_API_IMAGE=<target API tag>`, run a detached no-deps Compose one-off with fixed Node keepalive argv, inspect it before database commands, require its immutable `Image` equals target ID and its project/service/oneoff labels are exact, then execute exact `db:migrate` and `db:schema:verify` argv inside it. Remove only this exact one-off in `finally`; any identity mismatch stops before migration.
- Replace the permissive allowlist with canonical command builders and exact token-by-token validators, including exact environment key/value schemas. Each accepted command has one complete argv shape with validated generated revision/tag/container tokens and no additional tail. Tests deep-equal every success command and append/prepend/reorder/duplicate/substitute one token to prove rejection. The only container removal is the exact generated migration one-off; fixed containers, images and volumes have no removal command.
- The claim store production API accepts filesystem/identity seams but no root override. Validate `/private`, `/private/tmp`, and final root by `lstat`/`realpath`: no symlink component, exact resolved paths, trusted root-owned system parents with expected sticky/world-write policy only at `/private/tmp`, and a current-process-owned final root with exact `0700` mode. Revision filenames are lowercase full SHA only. Claim files are regular, current-process-owned, owner-only `0600`, canonical, outside Git, and exclusive-link published with temp/directory fsync and exact temp cleanup. Concurrent callers have one winner; success/failure/rollback/verifier never remove a final claim.
- Rollback sets Compose API/Web image environment to captured immutable old `sha256:` IDs, never mutable refs. Recreate only `api web`, collect rollback facts, require restored old image IDs, exact labels/topology/preflight route observations, exact PostgreSQL/volumes/business/sequences/media/protected hashes and the allowed post-migration ledger state. Ensure the migration one-off is absent and evidence final/temp files are absent. Failure remains terminal without broader cleanup.
- Route facts come only from Web origin. Require HTML routes `/`, `/categories`, `/tags`, `/archive` status 200 with bounded body digests; `/api/health` exact status/body `{ok:true}`; empty search exact full body `{state:"empty_query",query:"",page:1,pageSize:10,totalItems:0,totalPages:0,items:[]}`; unknown related exact 404/body `{error:"not_found"}`. Reject unknown keys, alternate statuses, partial matching, malformed JSON or direct API address.
- Evidence v3 uses strict exact keys and typed nested schemas. Persist only format/version/revision/lock/claim digest, target/old image IDs, selected required labels, stage counts/digests/booleans, exact route statuses/contracts/body digests, and `releaseState:"BLOCKED"`; never persist raw dumps, sequence/ledger rows, media paths, volume mountpoints, host paths, env or command output. Create one `0600` same-directory temp, write/fsync/close, exclusive-link to absent final, fsync directory, unlink temp in all paths, and never overwrite an existing final.
- `verifyLiveRefreshEvidence` strict-parses all exact keys, validates internal before/after delta claims, reads the claim without mutation, invokes the shared collector and seed/target filesystem probes read-only, and requires current facts equal evidence `postCutover`. It rejects changed images/labels/store/filesystem, volumes/PostgreSQL, business/sequence/ledger/media/protected digests, topology/routes/release, extra keys or tampering. It never claims, builds, migrates, cuts over, rolls back, writes or cleans evidence/claim.
- Test the complete fake state machine, not isolated helpers only: successful command/event order and evidence; each pre-claim and post-claim failure barrier; migration target ID; post-migration deltas; cutover; route/release failure rollback; immutable rollback identities/full facts; evidence suppression/temp cleanup; verifier reconstruction/drift. All current P0/P1 deficiencies must fail the RED commit and become GREEN without a real infrastructure command.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode every audited P0/P1 gap as comprehensive RED tests</name>
  <files>scripts/refresh-local.test.mjs</files>
  <action>
    Replace shallow/source-presence assertions with behavior-first fixtures. Add verbatim Docker inspect Ports/labels/volumes/images, pg_dump/sequence/ledger/media/protected hashes, routes, seed/tag/target probes and evidence objects. Drive a fake exact-argv runner through full success, every stage, rollback and verifier. Prove current code fails on null unbound Ports, wrong Compose authority, target-image migration identity, missing persistence collector/comparisons, seed drift, missing filesystem/store probes, permissive extra argv tails, claim root/symlink/realpath/concurrency cases, mutable-ref/incomplete rollback, partial route bodies, evidence extra/raw fields, temp cleanup/fsync and verifier drift. Include explicit no-bare/no-Docker test seams. Capture nonzero RED and commit only the test as `test(06-08): expose audited live refresh gaps`.
  </action>
  <verify>
    <automated>/bin/zsh -lc 'set +e; node --test scripts/refresh-local.test.mjs &gt; /private/tmp/blog-x-06-08-red.tap 2&gt;&amp;1; exit_status=$?; test "$exit_status" -ne 0; rg "Ports|postMigration|target API|seed|rollback|evidence|argv|claim" /private/tmp/blog-x-06-08-red.tap'</automated>
  </verify>
  <done>Every audited deficiency is represented by a deterministic failing behavior test before implementation, with no live action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared collector and complete safe state machine</name>
  <files>scripts/refresh-local-facts.mjs, scripts/refresh-local-live.mjs, scripts/refresh-local.mjs, scripts/refresh-local.test.mjs</files>
  <action>
    Implement all interfaces without deferral. Centralize read-only fact collection/delta comparison and exact command builders; correct Docker null-port/Compose authority parsing; harden literal claim safety; add stable seed and target filesystem/store probes; run migration/schema only after proving the target API one-off ID; use immutable old IDs plus full-fact rollback; publish strict sanitized atomic v3 evidence; and make verification reconstruct every current fact read-only. Make the entire RED suite GREEN, including full success/failure/rollback/verifier command traces. Run focused tests, local-verifier regressions, typecheck, boundaries and canonical BLOCKED only. Do not run Docker, Compose, probe mode or the no-option CLI. Commit exactly the four files as `fix(06-08): complete audited live refresh safety`, then create and commit 06-08-SUMMARY separately. Require a fresh independent GSD plan checker to report no blockers before 06-09 is eligible.
  </action>
  <verify>
    <automated>node --test scripts/refresh-local.test.mjs &amp;&amp; node --test scripts/local-verify.test.mjs &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- ops/phase6-local-refresh-evidence.json .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>A clean committed revision closes all audited gaps with reconstructable behavior and no consumed live attempt; independent plan checking can authorize only the plan shape, not runtime success.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| ID | Threat | Severity | Control / verification |
|---|---|---|---|
| T-06-08-01 | Host port/public topology exposure | high | Raw null-binding fixtures, exact Web loopback binding and Compose label/service authority at every fact stage. |
| T-06-08-02 | Migration runs stale/wrong API image | high | Target tag environment, generated one-off, immutable image/label inspection before exact migration/schema argv. |
| T-06-08-03 | Persistent content drift is hidden | high | Shared business/sequence/ledger/media/volume/PostgreSQL/protected collector and explicit stage deltas. |
| T-06-08-04 | Stale seed/target application is accepted | high | Tag-to-ID reinspection and real target pnpm/filesystem/config/label probes before mutation and during verification. |
| T-06-08-05 | Command allowlist broadens authority | high | Exact builders/token/environment equality; mutation of any token or extra tail is rejected in tests. |
| T-06-08-06 | Claim path/symlink/concurrency bypass | high | Literal root, complete lstat/realpath/uid/mode chain, canonical SHA filename and exclusive atomic publication. |
| T-06-08-07 | Rollback restores wrong image or loses evidence boundary | high | Immutable old IDs, API/Web-only recreation, full fact comparison, one-off cleanup and evidence suppression. |
| T-06-08-08 | Forged/leaking evidence | high | Strict exact-key digest/count schema, terminal atomic writer, directory fsync and shared read-only reconstruction. |
| T-06-08-09 | Remote/production mutation | high | No network/pull/SSH/server/deploy command exists; fixed loopback and canonical BLOCKED remain mandatory. |
</threat_model>

<verification>
- RED commit demonstrates current implementation failures for every audited group; GREEN commit and summary follow in order.
- No implementation verification invokes Docker, Compose, probe mode, bare refresh, real claim/evidence, server or network.
- Both plans pass structural validation; an independent GSD plan checker must return no blocker before execution proceeds.
- Production remains BLOCKED and protected history/completion authority is untouched.
</verification>

<success_criteria>
- All P0/P1 audit findings are fixed in code and behavior tests, not deferred.
- The next clean revision can be attempted once with exact image/data/topology authority and complete rollback/evidence reconstruction.
- 06-09 is the sole remaining live execution plan and remains gated on clean revision, absent claim and independent plan check.
- No server, registry, deployment, production transition or push occurs.
</success_criteria>

<output>
Create `.planning/phases/06-public-discovery-data/06-08-SUMMARY.md`, keep claim/evidence absent, and hand the clean implementation-plus-summary revision to 06-09 only after independent plan checking.
</output>
