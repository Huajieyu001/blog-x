---
phase: 06-public-discovery-data
plan: "06"
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - scripts/refresh-local-live.mjs
  - scripts/refresh-local.mjs
  - scripts/refresh-local.test.mjs
autonomous: true
gap_closure: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
estimate:
  tokens: 10800
  confidence: medium
  calibration_factor: 1
user_setup: []
must_haves:
  truths:
    - "The no-option CLI resolves a strict real local adapter and can no longer fail because of the hardcoded `live fixed refresh ... only` stub; tests traverse that same CLI branch with injected process/filesystem/fetch seams, while this plan never performs the no-option live invocation."
    - "Each clean implementation revision has one outside-repository attempt claim at `/private/tmp/blog-x-refresh-attempts/<full-40-hex-revision>.json`; after all read-only preflight passes and before the first build/mutation, complete sanitized bytes are published by an exclusive atomic same-directory operation and persist on success or failure."
    - "A later empty-argv call for an already-claimed revision fails at the early claim guard before live-adapter construction or any adapter `runArgv`/Docker action; a different valid revision has a distinct claim, so the historical unclaimed `b7fa05c` failure cannot collide with the new implementation revision."
    - "The live adapter fixes authority to `blogxlocal`, `http://127.0.0.1:3100`, existing containers `blogxlocal-postgres-1`, `blogxlocal-api-1`, `blogxlocal-web-1`, and volumes `blogxlocal_postgres-data` plus `blogxlocal_media-data`; any missing, extra, unhealthy, renamed, recreated, or non-loopback authority fails before mutation."
    - "Preflight captures exact container/image/volume identity, normalized business dump excluding `blog_x_schema_ledger`, every sequence, complete ledger stable tuples and timestamps, media hashes, tracked archived-v1 hashes, retained Phase 5 receipt hash, and current 06-VERIFICATION hash without storing credentials or raw content."
    - "Both revision-tagged targets build with exact `--network=none --pull=false` argv and pass immutable label, seed, workdir, command, pnpm version-store and filesystem-provenance inspection before the first migration or Compose mutation."
    - "Only the new API image runs `db:migrate` and `db:schema:verify`; only API/Web cut over; PostgreSQL and both volumes retain exact identity; business dump, sequences, media and ledger stable tuples remain equal, and only the existing `phase1` ledger row's `applied_at` advances strictly while any other row timestamp remains exact."
    - "Any failure after cutover starts restores only API/Web from captured old refs and verifies exact old image IDs plus health; PostgreSQL, volumes, unrelated containers and passing evidence remain untouched."
    - "Evidence is strict, sanitized, deterministic and atomically published only after Web-origin routes and canonical `BLOCKED` pass; it records only the attempt claim's revision and content digest, never its absolute path. `--verify-evidence` reconstructs current provenance, claim digest, filesystem, persistence, topology, routes, history hashes and BLOCKED facts instead of validating JSON shape alone, while leaving claim and evidence bytes unchanged."
    - "The command-policy regression replaces the broad ban on `docker-compose` with phase-specific exact argv allowlists and explicit prohibitions for alternate projects/services, `down`, volume/image removal, pull/network fallback, host-port expansion, SSH/server/deploy and evidence writes during verification."
  artifacts:
    - path: scripts/refresh-local-live.mjs
      provides: strict stateful live adapter, revision-bound atomic attempt claim, exact command policy, snapshots/comparisons, rollback, route checks, atomic evidence writer and reconstructing evidence verifier
      exports:
        - createLiveRefreshAdapter
        - claimRefreshAttempt
        - inspectRefreshAttemptClaim
        - verifyLiveRefreshEvidence
    - path: scripts/refresh-local.mjs
      provides: import-safe orchestrator and CLI dispatch whose no-option path creates the real adapter
      exports:
        - createRefreshPlan
        - runLocalRefresh
        - runRefreshCli
    - path: scripts/refresh-local.test.mjs
      provides: RED/GREEN CLI, exact argv, snapshot, mutation barrier, rollback, evidence sanitation and read-only reconstruction coverage
  key_links:
    - from: scripts/refresh-local.mjs
      to: scripts/refresh-local-live.mjs
      via: the no-option CLI creates the production adapter; tests inject only its OS/process/fetch seams
      pattern: "createLiveRefreshAdapter|runRefreshCli|runLocalRefresh"
    - from: scripts/refresh-local-live.mjs
      to: apps/api/Dockerfile.refresh
      via: exact network-none/pull-false build argv, revision labels and filesystem inspection precede migration
      pattern: "Dockerfile\\.refresh|--network=none|--pull=false|org\\.opencontainers\\.image\\.revision"
    - from: scripts/refresh-local-live.mjs
      to: compose.yaml
      via: fixed-project one-off API migration/schema commands and API/Web-only no-build cutover
      pattern: "blogxlocal|--no-build|--no-deps|api|web"
    - from: scripts/refresh-local-live.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: atomic sanitized writer after preservation, same-origin route and BLOCKED checks; verifier reconstructs each recorded fact
      pattern: "phase6-local-refresh-evidence\\.json|rename|BLOCKED|verify"
    - from: scripts/refresh-local.mjs
      to: /private/tmp/blog-x-refresh-attempts
      via: an early read-only guard rejects an existing revision claim before adapter creation, while the live preflight publishes the exclusive claim immediately before build
      pattern: "blog-x-refresh-attempts|implementationRevision|claimRefreshAttempt"
  prohibitions:
    - statement: "Plan 06-06 must not run the no-option refresh CLI, mutate blogxlocal, write runtime evidence, run Docker/Compose integration, contact a server, use SSH, deploy, unfreeze, push, or change release authority."
      status: pending
      verification: command
    - statement: "The adapter must not accept dynamic project/origin/container/volume/service authority, use a shell command string, pull, build with network, use Compose down, remove a volume, recreate PostgreSQL, publish API/PostgreSQL ports, or address a non-loopback route."
      status: pending
      verification: test
    - statement: "Evidence verification must not write evidence or accept recorded assertions without reconstructing their live/read-only source facts."
      status: pending
      verification: test
    - statement: "Attempt claims must not live in the repository, accept a configurable/broad/root/traversal/symlink path, contain an absolute path or secret, use shell interpolation, be removed after failure, or be overwritten for the same revision."
      status: pending
      verification: test
---

<objective>
Implement the missing real adapter behind the already-tested refresh state machine, entirely under RED/GREEN tests and without touching the fixed runtime.

Purpose: Turn the safe 06-04 primitive into a genuinely executable, evidence-producing local refresh command before any further one-shot attempt is authorized.
Output: A strict live-adapter module, corrected CLI dispatch/evidence verification, and focused TDD coverage committed on a clean new revision.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/references/tdd.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-CONTEXT.md
@.planning/phases/06-public-discovery-data/06-RESEARCH.md
@.planning/phases/06-public-discovery-data/06-04-PLAN.md
@.planning/phases/06-public-discovery-data/06-04-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-05-PLAN.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@scripts/refresh-local.mjs
@scripts/refresh-local.test.mjs
@scripts/refresh-seed-store.mjs
@apps/api/Dockerfile.refresh
@apps/web/Dockerfile.refresh
@compose.yaml
@scripts/release-gate.mjs
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json

<interfaces>
- Preserve `FIXED_REFRESH` byte-for-byte: project `blogxlocal`, origin `http://127.0.0.1:3100`, cutover services `[api, web]`, volumes `[blogxlocal_postgres-data, blogxlocal_media-data]`. Resolve Compose service container IDs and require their exact canonical names; never synthesize a second project.
- Add an import-safe `runRefreshCli({ argv, revisionResolver, claimStore, liveAdapterFactory, io })`. Probe and evidence options retain their behavior. Empty options resolve and validate the current clean full revision through a dedicated read-only seam, call `claimStore.assertAbsent(revision)`, and only then construct the live adapter and enter `runLocalRefresh({adapter})`. For an existing claim this guard returns nonzero before adapter construction and before any adapter `runArgv`/Docker call. Only the actual 06-07 process may call this branch without injected seams. The regression must prove the prior hardcoded stub is unreachable without invoking Docker.
- Keep `runLocalRefresh({adapter})` fake-adapter compatibility. The production factory may receive narrow injectable seams for tests (`runArgv`, claim filesystem operations, evidence filesystem operations, loopback fetch, clock); it must not receive project/origin/service/volume/claim-root overrides. Add read-only CLI claim checks for 06-07: `--check-attempt-claim=absent|present` plus one exact full `--revision=<40-hex>`; these never construct/run the live adapter.
- Attempt storage authority is the literal directory `/private/tmp/blog-x-refresh-attempts`, outside the Git root and not configurable by environment or CLI. The early absence check is read-only even when that directory is absent. At claim time only, create it with mode `0700` if absent; otherwise require the resolved directory to remain exact and be a real non-symlink directory with exact mode `0700`. Derive the filename only from a validated lowercase 40-hex revision. Reject root/broad paths, traversal, symlink components, wrong modes and all noncanonical revisions before file access; never construct or pass a claim path through a shell string.
- The canonical claim bytes are deterministic strict JSON with only `{format:"blog-x-local-refresh-attempt",version:1,implementationRevision:<full SHA>}` and a trailing newline. Publish complete bytes safely in the same directory: create a generated temporary regular file with exclusive `wx` mode `0600`, write/fsync/close it, then use a single filesystem `link(temp, final)` as the exclusive atomic final-name creation, fsync the directory and unlink only the generated temporary name. Final-target `EEXIST` means the attempt is already consumed. Once the canonical claim exists, neither success, failure, rollback, verification nor cleanup may edit/remove it.
- Exact command policy is positive and phase-specific. Allow only the argv families needed for clean Git/lock inspection; fixed Compose config/ps/exec/run/up; Docker container/image/volume inspect and two target builds; target filesystem probes; API migration/schema verification; and the local release gate. Require token-for-token fixed project/file/service flags and reject every unlisted executable/subcommand/flag. Do not retain the current test that bans the `docker-compose` token itself.
- Preflight must finish before claims/builds. The adapter's final preflight action calls `claimRefreshAttempt(revision)` and receives its canonical digest; no build/migration/Compose action may begin without that claim, and any later failure leaves it present. Both target builds plus inspections then finish before migration. Build commands are exactly `docker build --network=none --pull=false ...` for API and Web current refresh Dockerfiles with immutable seed IDs, full revision, lock digest and fixed origin labels. No buildx, pull, host network, remote context or stale retag path is allowed.
- Snapshot schemas use canonical sorting and SHA-256: normalized `pg_dump --data-only` output excluding table `blog_x_schema_ledger`; every `pg_sequences` schema/name/last_value/start/increment/min/max/cache/cycle row; ledger `{scope,migration_count,migration_fingerprint}` plus `applied_at`; media relative path/byte/digest; normalized volume inspect fields `Name,Driver,Mountpoint,CreatedAt,Scope,Labels,Options`; tracked `.planning/milestones` bytes; retained receipt bytes; 06-VERIFICATION bytes. Evidence stores digests/counts/identities, never raw rows, media bytes, environment or credentials.
- Migration/schema uses the just-inspected API target via fixed-project no-dependency one-off Compose argv. After migration, ledger row count and `{scope,migration_count,migration_fingerprint}` tuples are identical; the existing `scope=phase1` row's `applied_at` is strictly later, every other row timestamp (if present) is unchanged, and no new/missing row or equal/regressed `phase1` timestamp is accepted. All other persistence snapshots and the exact PostgreSQL container/volume identities remain equal.
- Cutover command targets exactly `api web` with `--no-build --no-deps`; require API/Web running image IDs equal inspected targets, three services healthy, PostgreSQL container unchanged, API/PostgreSQL with zero host bindings, and Web with exactly `127.0.0.1:3100->3100`.
- Route verification uses Node fetch only against the Web origin: `/`, `/categories`, `/tags`, `/archive`, `/api/health`, `/api/public/search?q=`, `/api/public/articles/phase6-unknown/related`. Parse exact empty-query and not-found JSON contracts; no direct API/container/cloud address.
- Capture old API/Web configured refs and immutable IDs before mutation. Once cutover begins, any failure invokes only exact old refs with API/Web-only no-build/no-deps up, then verifies old IDs, three-service health, PostgreSQL/volume identity and evidence absence. Rollback failure remains terminal and never broadens target scope.
- Write schema-versioned evidence to a same-directory temporary file with restrictive mode, fsync/close and atomic rename only after all checks. Redact command errors before persistence. Evidence includes `attemptClaim: {implementationRevision, sha256}` only. `verifyLiveRefreshEvidence` parses strict keys and reconstructs the claim digest by read-only open plus Git revision ancestry/lock, current image labels/filesystems, container/volume/data/sequence/ledger/media facts, Web-origin routes, historical hashes and canonical release BLOCKED using read-only allowlisted operations; mismatch fails and both evidence and claim bytes remain byte-identical. Verify mode never calls claim creation, `runLocalRefresh`, the live adapter, build, migration, cutover or rollback.
- The bare invocation already attempted at `b7fa05c` predates this claim protocol, failed before mutation and is not retried. Its revision differs from the future 06-06 completion revision, and no legacy/global marker is imported. 06-06 verification uses tests/functions only. The next actual empty-option process invocation belongs exclusively to 06-07 and may occur once only after the GREEN implementation and summary are committed and the worktree is clean.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the live adapter and no-stub CLI contract in RED</name>
  <files>scripts/refresh-local.test.mjs</files>
  <action>
    Extend the focused suite before product code. Add nonzero fake-process and injected-filesystem tests for `runRefreshCli` selecting a live adapter on empty argv, strict phase-specific argv allowlists, canonical fixed authority, complete preflight snapshots, dual build/inspection barrier, API-target migration/schema, persistence comparison, API/Web-only cutover, loopback routes, post-start exact-image rollback, atomic sanitized evidence, and read-only evidence reconstruction. Require: the first clean revision passes read-only preflight then atomically creates its canonical claim before build; a sequential second empty-argv call fails at the early guard with zero adapter construction/`runArgv`/Docker events; a post-claim adapter failure/rollback leaves identical claim bytes; a different revision gets a different filename/digest; unsafe/broad/root/traversal/symlink/configurable claim paths fail; concurrent exclusive publication has one winner; and `--verify-evidence` only reads the claim, leaves its bytes exact, never calls its creator and never invokes refresh. Replace the broad `doesNotMatch(... /docker-compose/)` assertion with positive exact argv fixtures plus explicit rejected argv for alternate projects/services, down/removal, pull/network, PostgreSQL recreation, non-loopback/SSH/server/deploy and verify-mode writes. The simulated empty-argv test injects all seams and proves it cannot hit the former hardcoded failure; never spawn `node scripts/refresh-local.mjs` with no options. Capture RED failure and commit only the test as `test(06-06): specify strict live refresh adapter`.
  </action>
  <verify>
    <automated>/bin/zsh -lc 'set +e; node --test scripts/refresh-local.test.mjs &gt; /private/tmp/blog-x-06-06-red.tap 2&gt;&amp;1; exit_status=$?; test "$exit_status" -ne 0; rg "createLiveRefreshAdapter|runRefreshCli|verifyLiveRefreshEvidence" /private/tmp/blog-x-06-06-red.tap'</automated>
  </verify>
  <done>The missing production adapter, reconstructing verifier and no-stub CLI path are represented by deterministic failing tests without live infrastructure action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the strict live adapter and make focused tests GREEN</name>
  <files>scripts/refresh-local-live.mjs, scripts/refresh-local.mjs, scripts/refresh-local.test.mjs</files>
  <action>
    Implement the new module and CLI wiring exactly from interfaces. Use spawn argv arrays and narrow filesystem/fetch seams; centralize the exact per-phase allowlist before any subprocess call. Implement the fixed safe claim store with injected filesystem primitives, complete-before-exclusive-publish semantics, strict path/mode/type checks and persistent canonical bytes. Keep state private to one adapter instance so preflight facts, claim digest, target IDs, mutation boundary and old-image rollback cannot be supplied by callers. Make `verifyEvidence` delegate to strict live reconstruction rather than shape-only parsing and prove its claim access is read-only. Make all RED tests GREEN, including simulated first/second/different-revision/failure/verify CLI paths. Run only unit/type/boundary/release checks: do not run `--probe-offline-builds`, Docker, Compose, the no-option CLI, create a real attempt claim, or write runtime evidence. Commit exactly the three implementation/test files as `feat(06-06): implement strict live refresh adapter`, then create 06-06-SUMMARY in its separate docs commit.
  </action>
  <verify>
    <automated>node --test scripts/refresh-local.test.mjs &amp;&amp; node --test scripts/local-verify.test.mjs &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- ops/phase6-local-refresh-evidence.json .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>A clean committed revision contains a real, strictly bounded live adapter and reconstructing verifier; no fixed runtime or evidence has changed.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| ID | Threat | Severity | Control / verification |
|---|---|---|---|
| T-06-06-01 | Command injection or authority expansion | high | Spawn argv only; immutable constants; exact per-phase allowlist tests reject shell strings, alternate projects/services and unlisted commands. |
| T-06-06-02 | Persistent data loss | high | Pre/post volume, business dump, sequence, ledger and media comparisons; no down/remove/PostgreSQL cutover argv; post-start rollback is API/Web-only. |
| T-06-06-03 | Stale or network-derived target image | high | Both builds require network-none/pull-false and immutable seeds/revision/lock labels plus filesystem inspection before migration. |
| T-06-06-04 | Forged or leaking evidence | high | Strict schema, reconstructed facts, sanitized digest-only fields, restrictive atomic writer and read-only verifier tests. |
| T-06-06-05 | Remote or production mutation | high | No SSH/server/deploy executable is allowlisted; origin is exact loopback; release gate must remain BLOCKED. |
| T-06-06-06 | Same revision is refreshed twice or a claim is bypassed | high | Fixed outside-repo root, revision-derived path, early absence guard, exclusive atomic publish after preflight and persistence across all terminal paths. |
| T-06-06-07 | Claim path traversal/symlink overwrite | high | Exact root/type/mode validation, lowercase SHA filename, generated same-directory temp, no shell and injected-filesystem rejection tests. |
</threat_model>

<verification>
- RED and GREEN commits are ordered, followed by a summary-only commit.
- No verification command invokes the no-option CLI, Docker, Compose, a server, or runtime evidence writing.
- Focused tests prove exact allowlisted live behavior and explicit prohibited behavior; regressions and canonical BLOCKED pass.
- `b7fa05c` remains a truthful safe failure; the first eligible actual refresh is deferred to 06-07's new clean revision.
</verification>

<success_criteria>
- The current fake-only state machine has a strict production adapter without weakening any 06-04 barrier.
- No-option CLI dispatch no longer contains a hardcoded live-adapter failure.
- A later clean executor can invoke the command once; the persisted revision claim mechanically prevents a second same-revision adapter/Docker run whether the first succeeds or fails.
- Production and both servers remain untouched and `BLOCKED`.
</success_criteria>

## Source Coverage

| Source | Constraint carried forward |
|---|---|
| ROADMAP / REQUIREMENTS | Phase 6 public discovery must be observable at fixed 3100 before Phase 7; product requirements remain pending until independent verification. |
| CONTEXT / RESEARCH | Same-origin only, generated/local verification, no servers, low-resource PostgreSQL design and canonical BLOCKED. |
| 06-VERIFICATION | Close the sole stale-runtime/provenance link without weakening the 19 verified truths. |
| 06-04 PLAN/SUMMARY | Preserve sanitized seeds, exact pnpm store, dual offline build barrier, fixed authority and rollback primitives. |
| Debugger findings | Replace fake-only/hardcoded CLI, shape-only verifier and broad Compose ban with real adapter, reconstruction and exact argv policy. |

<output>
Create `.planning/phases/06-public-discovery-data/06-06-SUMMARY.md`. Do not create runtime evidence or invoke the no-option refresh; hand the clean committed implementation revision to 06-07.
</output>
