---
phase: 06-public-discovery-data
plan: "10"
type: execute
wave: 7
depends_on:
  - 06-08
files_modified:
  - scripts/refresh-local-facts.mjs
  - scripts/refresh-local-live.mjs
  - scripts/refresh-local.mjs
  - scripts/refresh-local-test-core.mjs
  - scripts/refresh-local.test.mjs
autonomous: true
gap_closure: true
independent_plan_check: required
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
estimate:
  tokens: 18000
  confidence: medium
  calibration_factor: 1
user_setup: []
must_haves:
  truths:
    - "Strict evidence v4 records the exact clean implementation revision, raw `pnpm-lock.yaml` SHA-256, original seed references plus inspected IDs, target immutable IDs/selected labels/filesystem/store results, exact fixed topology/routes/release, PostgreSQL identity/schema and every persistence fact. Verification reconstructs each value from real current sources rather than accepting precomputed facts."
    - "Ledger proof is row-addressed and deterministic: every scope has a stable-tuple digest and normalized `applied_at`; only `phase1` strictly advances from preflight to postMigration, every other row timestamp is byte-equivalent after normalization, and all rows remain unchanged after cutover. No raw migration fingerprint or business/media content enters evidence."
    - "Every terminal failure after claim acquisition recollects facts in a finally-style path and atomically persists a sanitized companion failure report before nonzero exit. This covers migration-command failure, migration success followed by schema failure, cutover/route/release/evidence failure, and rollback success/failure; preservation is either proved or an explicit unrecoverable invariant is reported."
    - "Production subprocesses receive only a fixed minimal local environment. Ambient Docker/Compose host or context overrides are rejected; the selected `colima` or `default` context is inspected to an approved local Unix socket, never TCP/SSH/HTTP, and every Docker/Compose command uses that proven authority without inheriting a remote daemon setting."
    - "Same-origin route collection uses `redirect: error`, requires the exact requested final URL and origin, and rejects redirects or response URL drift before evidence publication."
    - "Production factories are sealed and accept no collector, probe, argv, environment, filesystem, fetch, or evidence overrides. Test-only cores may replace the process/filesystem/fetch/clock boundaries, but cannot inject precomputed facts or target probes; tests trace the real production collector and verifier argv through the fake process boundary."
    - "Claim and evidence publication fail closed for parent/final symlinks, wrong UID/mode, file/directory fsync and unlink failures. Evidence final is absent after every recoverable failure; otherwise the process reports `UNRECOVERABLE_EVIDENCE_INVARIANT` and never silently ignores cleanup."
    - "Plan 06-10 is implementation-only: it runs tests and read-only repository checks but never runs Docker/Compose, the bare no-option refresh, a real claim/evidence publication, network/server commands, deployment, push, unfreeze, or release transition."
  artifacts:
    - path: scripts/refresh-local-facts.mjs
      provides: canonical v4 Git/lock/seed/target/schema/persistence/ledger/local-daemon/route fact collection and comparisons
    - path: scripts/refresh-local-live.mjs
      provides: sealed production live adapter/verifier, finally-style failure recollection, strict atomic publication, and sanitized v4 records
    - path: scripts/refresh-local.mjs
      provides: sealed production CLI with fixed local authority and no injectable runtime seams
    - path: scripts/refresh-local-test-core.mjs
      provides: explicitly test-only boundary factories that reuse production collectors and prohibit precomputed facts/probes
    - path: scripts/refresh-local.test.mjs
      provides: RED/GREEN coverage for all five independent-audit blocker groups and production argv traces
  key_links:
    - from: scripts/refresh-local.mjs
      to: scripts/refresh-local-live.mjs
      via: no-option and verify modes instantiate sealed production factories with fixed paths and native boundaries
      pattern: "createProduction|verifyProduction|runRefreshCli"
    - from: scripts/refresh-local-live.mjs
      to: scripts/refresh-local-facts.mjs
      via: success, failure, rollback, and verification all invoke the same canonical source collectors and comparators
      pattern: "collectRefreshFacts|assertPersistenceTransition|evidenceVersion"
    - from: scripts/refresh-local-test-core.mjs
      to: scripts/refresh-local-facts.mjs
      via: tests replace only raw process/filesystem/fetch/clock boundaries while production fact sources still build every argv and parse every response
      pattern: "ForTest|processBoundary|createRefreshFactSources"
    - from: scripts/refresh-local-live.mjs
      to: /private/tmp/blog-x-refresh-attempts
      via: a revision claim and sanitized companion failure report are atomically published below one literal outside-repository root
      pattern: "refresh-attempts|failure|UNRECOVERABLE"
  prohibitions:
    - statement: "Do not run Docker/Compose, the no-option refresh, any live adapter import, a real claim/evidence write, migration, cutover, rollback, registry/network/server/SSH/deploy/push/unfreeze command in this implementation plan."
      status: pending
      verification: command
    - statement: "Do not defer any of the five blocker groups, weaken v4 reconstruction to stored booleans/digests alone, or permit production injection of facts, probes, argv, environment, filesystem, fetch, clock, roots, or evidence paths."
      status: pending
      verification: command
    - statement: "Do not persist raw business rows, Markdown/media content, migration fingerprints, secrets, command environments, host mount paths, or database credentials in evidence/failure reports."
      status: pending
      verification: command
    - statement: "Do not edit protected milestone evidence, the Phase 5 receipt, existing runtime evidence, 06-VERIFICATION, REQUIREMENTS/ROADMAP/STATE completion authority, or release BLOCKED."
      status: pending
      verification: command
---

<objective>
Close all five blockers found by the independent audit of `df4aa3b` under TDD, without consuming another live-refresh attempt.

Purpose: Make the next clean revision's one permitted local mutation independently reconstructable, locally authoritative, sealed against test bypass, and fail-closed on every terminal path.
Output: RED then GREEN commits for evidence v4, durable failure reports, local daemon/environment/fetch authority, sealed production factories, and atomic claim/evidence invariants; followed by 06-10-SUMMARY.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-08-PLAN.md
@.planning/phases/06-public-discovery-data/06-08-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-09-PLAN.md
@.planning/phases/06-public-discovery-data/06-09-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@scripts/refresh-local-facts.mjs
@scripts/refresh-local-live.mjs
@scripts/refresh-local.mjs
@scripts/refresh-local.test.mjs
@pnpm-lock.yaml
@compose.yaml
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json

<interfaces>
- Evidence becomes strict version 4. At live collection time it records exact `git rev-parse HEAD`, clean-worktree state, SHA-256 over the raw committed `pnpm-lock.yaml` bytes, seed `{reference, inspectedId}` pairs, target immutable IDs/selected Compose+revision labels/filesystem/store probes, exact raw Docker/Compose topology, route contracts, canonical BLOCKED, PostgreSQL identity/schema, volumes, business/sequence/media facts and row-addressed ledger transitions.
- The verifier obtains Git authority itself. Before evidence commit, current HEAD must exactly equal `implementationRevision`; after evidence/docs-only commits it must prove that revision is an ancestor, reconstruct raw lock bytes with `git show <revision>:pnpm-lock.yaml`, and require every intervening changed path to belong to the fixed evidence/summary allowlist. Image revision labels always equal the implementation revision.
- Ledger evidence contains no migration fingerprint or business content. For every fixed scope it stores the stable tuple SHA-256 plus normalized UTC `appliedAt` needed to prove the exact transition: only `phase1` is strictly greater at postMigration; all other timestamps and all stable digests are equal; postCutover and terminal success are equal to postMigration row-for-row.
- Schema evidence names and reconstructs exact database identity, expected tables/columns/types/nullability/defaults/indexes/constraints and migration ledger scopes. Comparators operate on sorted canonical rows, reject missing/extra/duplicate records and compare PostgreSQL ID, schema labels, topology, routes and all persistence facts at every applicable phase.
- A terminal handler owns finally-style recollection. It runs after migration command failure, schema failure after successful migration, cutover, route, release or evidence failure, and after rollback attempt. It writes `<literal revision>.failure.json` beside the claim under fixed `/private/tmp/blog-x-refresh-attempts`; the strict sanitized schema identifies stage/error class, claim/revision, recollection status, pre/current/rollback fact digests and preservation comparisons. If facts cannot be recollected, it records that preservation is unproved and exits nonzero; it never fabricates preservation.
- Build a child environment from a fixed allowlist only (required executable/home/temp/locale values plus exact command-specific `BLOG_X_API_IMAGE`/`BLOG_X_WEB_IMAGE`). Reject ambient `DOCKER_HOST`, `DOCKER_CONTEXT`, `COMPOSE_FILE`, `COMPOSE_PROJECT_NAME`, builder/context flags or other Docker/Compose routing overrides before claim/mutation. Do not copy `process.env` wholesale.
- Production preflight runs source-built argv for `docker context show` and `docker context inspect <exact name>`. Accept only `colima` or `default` with a local `unix://` endpoint resolving to the expected local Colima socket or `/var/run/docker.sock`; reject TCP, SSH, HTTP(S), malformed, symlink-escaped or alternate endpoints. No production Docker/Compose source accepts `--host`, `--context`, `-H`, `DOCKER_HOST` or `DOCKER_CONTEXT`.
- Route probes call fetch with `{redirect: "error"}` and require `response.url` to be the exact requested URL with origin `http://127.0.0.1:3100`; a 3xx/redirect exception, empty URL, changed scheme/host/port/path/query or cross-origin final URL fails before publication.
- Expose production entry factories with zero override arguments. Move all injection to `refresh-local-test-core.mjs`; its only seams are fake raw process, filesystem, fetch and deterministic clock/random boundaries. It reuses the same production argv constructors/parsers/collectors/verifier and offers no `collectFacts`, `probeTargets`, precomputed facts, root, evidence path or environment injection.
- The fake process boundary returns raw source output and records argv. Tests must observe exact production commands for Docker context/Compose/inspect, Git status/revision/show, raw lock reading, pg_dump, PostgreSQL identity/schema/business/sequences/ledger, media, seed/target probes and routes. Verifier drift cases alter those raw outputs, never inject expected facts.
- Claim/evidence writers validate literal parent ownership/mode/type/no symlink and final nonexistence/type before temp creation and again before link/rename. They validate temp/final UID and mode, fsync file and directory, and make every close/unlink/fsync error terminal. On evidence publication failure, remove any linked final and fsync the directory; if cleanup succeeds final is absent, otherwise throw `UNRECOVERABLE_EVIDENCE_INVARIANT` naming only the safe invariant/code and stop. Never swallow unlink failures.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode all five audited blocker groups as production-path RED tests</name>
  <files>scripts/refresh-local.test.mjs</files>
  <precondition>Worktree is clean at the committed 06-09 supersession/planning revision; no live refresh, Docker/Compose, claim/evidence mutation, network or server command is permitted.</precondition>
  <action>
    Add failing tests before implementation and commit tests only. Cover: strict v4 schema and reconstruction of exact Git HEAD/ancestor path allowlist/raw lock bytes; seed refs+IDs and target IDs/labels/filesystem/store; exact database/schema/topology/routes/all persistence facts; sorted row-addressed ledger transitions with only strict phase1 timestamp advance. Cover finally-style recollection and atomic companion failure reports for migration-command failure, schema failure after migration success, cutover, route, release, evidence and rollback failures, including recollection failure as explicitly unproved. Cover minimal environment and rejection of every Docker/Compose authority override; accepted local `colima`/`default` Unix sockets and rejected remote/symlink-escaped endpoints; redirect-error/exact-final-URL fetch behavior. Cover production/test factory separation by tracing every real production collector/verifier command through only a fake process boundary and prove there is no precomputed fact/probe seam. Cover parent/final symlink, UID, mode, file fsync, directory fsync, close, unlink, concurrent writer and post-link cleanup failures for claims/evidence; require final evidence absent on recoverable failure or explicit unrecoverable invariant. Assert the test process never invokes real Docker/network or writes the literal host claim/evidence.
  </action>
  <verify>
    <automated>node --test scripts/refresh-local.test.mjs</automated>
    <manual>Require failures to correspond to each named v4/failure-report/local-authority/sealing/atomic invariant, then commit only the RED test file.</manual>
  </verify>
  <done>All five blocker groups have non-vacuous production-path RED coverage, including exact argv traces sourced from raw fake-process responses, and the tests-only commit contains no implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement sealed v4 collection, terminal recollection and fail-closed publication</name>
  <files>scripts/refresh-local-facts.mjs, scripts/refresh-local-live.mjs, scripts/refresh-local.mjs, scripts/refresh-local-test-core.mjs, scripts/refresh-local.test.mjs</files>
  <action>
    Implement exactly the interfaces above until every RED test passes. Keep one canonical collector/comparator family for live execution, terminal failure recollection and evidence verification. Seal the production CLI/factories with literal local roots and no overrides; provide only the narrow test boundary module. Upgrade all schemas and parsers to strict v4 with unknown-key rejection, canonical ordering and sanitized row-addressed ledger proof. Make terminal recollection/reporting and rollback ordering explicit and fail closed if preservation cannot be proved. Build rather than merge the child environment, prove the selected Docker context's local Unix socket before any claim/mutation, and enforce exact redirect-free same-origin responses. Harden claim/evidence publication so no cleanup/fsync error is ignored and ambiguous final evidence is reported as an unrecoverable invariant. Commit the complete GREEN implementation, then create 06-10-SUMMARY with RED/GREEN commit IDs and a truthful statement that no live attempt or Docker operation ran.
  </action>
  <verify>
    <automated>node --test scripts/refresh-local.test.mjs &amp;&amp; node --test scripts/local-verify.test.mjs &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --check</automated>
    <manual>Inspect command traces and production exports: no injectable collector/probe/facts/root/evidence/env seam exists; no test calls a real process beyond the fake boundary; no protected/runtime artifact changed.</manual>
  </verify>
  <done>All five blocker groups are GREEN in one implementation revision, tests exercise the real production sources, evidence v4 is reconstructable, every failure is durably truthful, and no live attempt was consumed.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| ID | Threat | Severity | Control / verification |
|---|---|---|---|
| T-06-10-01 | Stored evidence passes without proving the actual revision, lock, images, schema or persistence | high | Strict v4 sources reconstruct Git/raw lock/seed/target/database/schema/topology/routes/persistence and row-level ledger transitions. |
| T-06-10-02 | A failed mutation exits before proving preservation | high | Finally-style recollection plus atomic sanitized companion failure report on every terminal path; unproved preservation is explicit. |
| T-06-10-03 | Ambient environment routes Docker to a remote daemon or leaks secrets | high | Minimal allowlist, override rejection, inspected local Unix context, and exact argv admission. |
| T-06-10-04 | HTTP redirects silently change the authority being verified | high | `redirect: error` plus exact requested final URL and origin equality. |
| T-06-10-05 | Injectable tests bypass production collection/verifier behavior | high | Zero-argument sealed production factories and a test-only raw boundary; production argv traces are asserted. |
| T-06-10-06 | Atomic failure leaves a trusted-looking claim/evidence final | high | UID/mode/symlink/fsync/unlink validation, cleanup+dir fsync, final absence or explicit unrecoverable invariant. |
| T-06-10-07 | Implementation testing consumes the only live attempt | high | Unit/fake-boundary tests only; explicit ban on Docker, bare refresh, live claims/evidence and network/server actions. |
</threat_model>

<verification>
- Fresh independent plan checker must report no blocker before implementation.
- RED tests are committed alone; GREEN implementation is a later commit; 06-10-SUMMARY records both.
- No Docker/Compose or no-option refresh occurs; exact revision claim and final evidence remain absent.
- Protected evidence/completion authority remains byte-identical and canonical release remains `BLOCKED`.
</verification>

<success_criteria>
- All five audit groups are closed without deferral and without a production/test bypass seam.
- Strict v4 evidence can be reconstructed from exact production sources before and after evidence/docs-only commits.
- Every mutation failure reports proved preservation or an explicit unrecoverable invariant before exit.
- The next live attempt belongs exclusively to 06-11 on a new clean committed implementation-plus-summary revision.
</success_criteria>

<output>
Create one RED test commit, one GREEN implementation commit, and `06-10-SUMMARY.md`. Do not run Docker/Compose or `node scripts/refresh-local.mjs` without options; do not create runtime evidence; do not contact servers or push.
</output>
