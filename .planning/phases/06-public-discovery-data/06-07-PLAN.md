---
phase: 06-public-discovery-data
plan: "07"
type: execute
wave: 6
depends_on:
  - 06-06
files_modified:
  - ops/phase6-local-refresh-evidence.json
  - .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
estimate:
  tokens: 5200
  confidence: medium
  calibration_factor: 1
user_setup: []
must_haves:
  truths:
    - "Execution starts from one clean committed revision containing the completed 06-06 live adapter and summary; the previously consumed `b7fa05c` attempt is historical and is never counted or retried."
    - "Before the sole invocation, a read-only CLI check proves the exact new revision has no claim below fixed `/private/tmp/blog-x-refresh-attempts`; after the process returns, another read-only check proves its canonical claim exists and remains present on both success and failure."
    - "The executor invokes `node scripts/refresh-local.mjs` with no options exactly once for this clean implementation revision and never substitutes manual build, migration, Compose, rollback or evidence commands."
    - "If that one invocation fails for any reason, the plan stops: it does not retry on the same revision, does not hand-edit evidence, and requires a separately planned/debugged new implementation commit before any later attempt."
    - "Success evidence records the sanitized revision-bound attempt-claim digest without an absolute claim path and proves both network-none/pull-false current-revision targets, exact labels/filesystems/provenance, new-API migration/schema, unchanged PostgreSQL/container/volume/business/sequence/media authority, stable ledger tuples with only strict `phase1.applied_at` advance and other timestamps exact, API/Web-only cutover, rollback readiness, same-origin routes and release `BLOCKED`."
    - "After the one mutating process returns success, every check starts with read-only `--verify-evidence` reconstruction; only Web exposes `127.0.0.1:3100`, API/PostgreSQL expose no host ports, and Phase 6 routes are visible through Web origin."
    - "Evidence and summaries are separate ordered commits; archived v1 files, retained Phase 5 receipt and executor-owned 06-VERIFICATION stay byte-identical, and an independent verifier alone may complete requirements/roadmap/state."
    - "No registry, SSH, server, cloud, deploy, production unfreeze, release transition or push occurs."
  artifacts:
    - path: ops/phase6-local-refresh-evidence.json
      provides: atomic sanitized machine evidence for the exact clean 06-06 implementation revision now running at fixed 3100
    - path: .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
      provides: dated closure addendum that preserves the original safe block while identifying the later evidence-backed runtime state
    - path: .planning/phases/06-public-discovery-data/06-07-SUMMARY.md
      provides: single-attempt result, evidence commit, visible change, safety and independent-verifier handoff
  key_links:
    - from: scripts/refresh-local.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: the sole no-option invocation performs the ordered live refresh and atomically publishes evidence only after terminal success
      pattern: "runRefreshCli|createLiveRefreshAdapter|phase6-local-refresh-evidence"
    - from: ops/phase6-local-refresh-evidence.json
      to: http://127.0.0.1:3100/api/public/search?q=
      via: read-only reconstruction confirms the refreshed Web-origin route and exact empty-query body
      pattern: "127\\.0\\.0\\.1:3100|empty_query|BLOCKED"
    - from: ops/phase6-local-refresh-evidence.json
      to: .planning/phases/06-public-discovery-data/06-07-SUMMARY.md
      via: summary cites committed evidence revision/digest and does not self-authorize Phase completion
      pattern: "implementationRevision|evidence|independent|BLOCKED"
  prohibitions:
    - statement: "Do not invoke the no-option refresh more than once on the same clean implementation revision, even after a pre-mutation failure; no manual substitute or same-revision retry is permitted."
      status: pending
      verification: command
    - statement: "Do not pre-create, edit, delete, rename, relocate or clean the attempt claim manually; only the adapter may atomically claim it, and every later check is read-only."
      status: pending
      verification: command
    - statement: "Do not mutate PostgreSQL or volumes outside the adapter, use Compose down/removal, contact registry/servers, use SSH, deploy, unfreeze, push, or transition the canonical release from BLOCKED."
      status: pending
      verification: command
    - statement: "The executor must not edit 06-VERIFICATION, REQUIREMENTS/ROADMAP/STATE completion, archived v1 evidence or the retained Phase 5 receipt."
      status: pending
      verification: command
---

<objective>
Run the newly implemented fixed local refresh exactly once from a clean revision, commit reconstructable evidence, update truthful summaries, and hand Phase 6 back to an independent verifier.

Purpose: Close the stale 3100 runtime gap without risking persistent content or converting local success into production authority.
Output: Refreshed fixed API/Web, evidence-only commit, historical closure addendum, 06-07 summary, and verifier handoff.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-03-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-04-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-05-PLAN.md
@.planning/phases/06-public-discovery-data/06-06-PLAN.md
@.planning/phases/06-public-discovery-data/06-06-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@scripts/refresh-local-live.mjs
@scripts/refresh-local.mjs
@scripts/refresh-local.test.mjs
@apps/api/Dockerfile.refresh
@apps/web/Dockerfile.refresh
@compose.yaml
@scripts/release-gate.mjs
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json
@.planning/milestones/v1.0-MILESTONE-AUDIT.md

<interfaces>
- Establish `REFRESH_REVISION` once from full `git rev-parse HEAD` with empty `git status --porcelain`. It must contain the GREEN 06-06 implementation and 06-06-SUMMARY and must not equal historical `b7fa05c5b41859e037ce6c5d361fc9ee7fc8d44b`. Attempt identity is revision-bound at literal `/private/tmp/blog-x-refresh-attempts/<REFRESH_REVISION>.json`; no legacy/global claim exists and no path is written to the repository or evidence.
- Read-only preconditions require exact healthy `blogxlocal` PostgreSQL/API/Web, exact existing PostgreSQL/media volumes, local seed images, no generated verifier/refresh lock, no evidence file, fixed loopback topology and canonical release BLOCKED. As the final mechanical precondition, run the claim-check CLI with literal argv `--check-attempt-claim=absent` and `--revision=<full REFRESH_REVISION>`; require zero exit and no filesystem mutation. A failed precondition consumes no claim/invocation and stops the task.
- The adapter repeats the early absence guard before constructing the live adapter, performs the complete read-only preflight, then exclusively publishes the canonical claim immediately before its first build. Once published, the claim persists on success, ordinary failure, rollback failure or process interruption. Its bytes contain only format/version/full revision; its identity in evidence is only revision plus SHA-256, never the absolute path.
- Run exactly `node scripts/refresh-local.mjs` once. The adapter owns all builds, inspection, migration/schema, cutover, rollback, routes and evidence. Do not run the same command through pnpm, shell wrappers, Node imports or a second process; do not manually reproduce a failed phase.
- Immediately after the sole process returns, on either exit path, run only the read-only claim-check CLI with literal argv `--check-attempt-claim=present` and `--revision=<full REFRESH_REVISION>` and require success. A nonzero refresh result is then terminal: confirm the adapter either stopped after claiming but before mutation or completed its exact API/Web rollback, confirm no passing evidence, report the failed revision/phase safely, and stop. The persisted claim makes a second same-revision empty-argv process fail before adapter construction/`runArgv`/Docker; a later attempt requires a newly committed implementation revision produced by a new debug/fix plan.
- On zero refresh exit and successful presence check, begin verification with exactly `node scripts/refresh-local.mjs --verify-evidence=ops/phase6-local-refresh-evidence.json`. That mode must read the claim without mutation, reconstruct its sanitized digest, and match immutable revision/lock/seed/target labels, sanitized filesystems, fixed containers/volumes, business dump excluding ledger, all sequences, ledger stable tuples/timestamps, media hashes, archived-v1/receipt/verification hashes, same-origin routes/topology and canonical BLOCKED without rewriting evidence or invoking refresh.
- Evidence must name REFRESH_REVISION even after its later evidence-only commit. Commit only `ops/phase6-local-refresh-evidence.json` as `chore(06-07): record fixed runtime refresh evidence`; no code or summaries share that commit.
- Then add a dated 06-07 closure addendum to 06-03-SUMMARY. Preserve its original cache failure and the `b7fa05c` hardcoded-adapter safe failure as historical events; update only current readiness. Create 06-07-SUMMARY with the attempt count, implementation/evidence commits, preservation/provenance/route facts, visible Phase 6 API change, Phase 7 UI absence, BLOCKED/no-server facts and independent-verifier handoff.
- Commit only both summaries as `docs(06-07): complete fixed runtime refresh execution`. Leave the existing 06-05 supersession record unchanged; it is an indexing/history artifact, not an execution summary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the clean-revision refresh once and commit evidence</name>
  <files>ops/phase6-local-refresh-evidence.json</files>
  <precondition>`git status --porcelain` is empty at a committed 06-06 completion revision distinct from `b7fa05c`; exact fixed containers/volumes/seeds are healthy and present; canonical release is BLOCKED; no generated verifier/refresh lock or prior evidence exists; and `node scripts/refresh-local.mjs --check-attempt-claim=absent --revision=&lt;literal-full-REFRESH_REVISION&gt;` exits zero without changing claim-directory state. Otherwise stop before consuming the invocation.</precondition>
  <action>
    Capture the exact clean REFRESH_REVISION and protected-file hashes with read-only checks; run the literal-revision absent check and retain its machine output outside the repository. Then invoke `node scripts/refresh-local.mjs` exactly once. Do not pre-create the claim, substitute or retry any phase. Immediately after the process exits, run the literal-revision present check before branching on its status. If refresh or presence check fails, inspect only enough read-only state to confirm claim persistence plus pre-mutation stop or exact API/Web rollback and return a blocking report; a new revision is mandatory before another attempt. If both succeed, run `--verify-evidence` first and prove evidence/claim bytes are unchanged across it, then use only read-only fixed Compose/Docker/Git/Web-origin/release checks to reconstruct evidence facts. Require the claim revision/digest with no path, current target image IDs, fixed PostgreSQL/volume/data/sequence/ledger/media authority, loopback-only topology, exact routes, historical hashes and BLOCKED. Commit the generated evidence alone; retain its REFRESH_REVISION binding after the evidence commit.
  </action>
  <verify>
    <automated>node scripts/refresh-local.mjs --check-attempt-claim=present --revision=&lt;literal-full-REFRESH_REVISION&gt; &amp;&amp; node scripts/refresh-local.mjs --verify-evidence=ops/phase6-local-refresh-evidence.json &amp;&amp; node scripts/refresh-local.mjs --check-attempt-claim=present --revision=&lt;literal-full-REFRESH_REVISION&gt; &amp;&amp; docker-compose -p blogxlocal -f compose.yaml ps &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>The exact clean implementation revision is served at fixed 3100 with one successful live invocation and separately committed, reconstructed, sanitized evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve history, summarize closure and hand off verification</name>
  <files>.planning/phases/06-public-discovery-data/06-03-SUMMARY.md, .planning/phases/06-public-discovery-data/06-07-SUMMARY.md</files>
  <action>
    From committed Task 1 evidence, append a dated current-state closure to 06-03-SUMMARY without rewriting its original safe block or the later `b7fa05c` safe failure. Create 06-07-SUMMARY with exact commit order, the single consumed invocation, adapter/provenance/filesystem checks, persistence and ledger result, rollback contract, topology/routes, visible Phase 6 API change, absent Phase 7 UI, production BLOCKED and zero server contact. Run summary/artifact/key-link and focused regression checks, then commit only the two summaries. Leave 06-VERIFICATION and all completion authorities to a fresh independent verifier.
  </action>
  <verify>
    <automated>node .codex/gsd-core/bin/gsd-tools.cjs verify-summary .planning/phases/06-public-discovery-data/06-07-SUMMARY.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify artifacts .planning/phases/06-public-discovery-data/06-07-PLAN.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify key-links .planning/phases/06-public-discovery-data/06-07-PLAN.md &amp;&amp; node --test scripts/refresh-local.test.mjs &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>Truthful summaries and committed evidence are ready for independent Phase 6 re-verification; no executor-owned completion or release authority changed.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| ID | Threat | Severity | Control / verification |
|---|---|---|---|
| T-06-07-01 | Repeated destructive attempt | high | External attempt marker, exact one-process rule, terminal failure and mandatory new revision before retry. |
| T-06-07-02 | Data or volume replacement | high | Adapter-owned canonical snapshots, PostgreSQL identity preservation and read-only evidence reconstruction; no manual mutation. |
| T-06-07-03 | False passing evidence | high | Atomic writer after terminal checks, evidence-only commit and reconstructing verifier before all other acceptance commands. |
| T-06-07-04 | Wrong service/topology cutover | high | Fixed project; target API/Web IDs; unchanged PostgreSQL; only Web loopback 3100. |
| T-06-07-05 | Production/remote authority escalation | high | No registry, SSH, server or deploy actions; canonical release must remain BLOCKED and protected hashes exact. |
| T-06-07-06 | Missing/removed claim permits same-revision retry | high | Mechanical absent-before/present-after checks, persistent exclusive claim and terminal failure policy; verifier proves claim bytes unchanged. |
</threat_model>

<verification>
- Exactly one no-option process is allowed for the clean 06-06 implementation revision; absent-before and present-after are mechanically checked with literal revision argv, and all later commands are read-only.
- A failure is terminal and requires a new committed revision, never a same-revision retry.
- Success yields an evidence-only commit followed by a summary-only commit; protected history and verification authority stay unchanged.
- Independent re-verification, not this executor, decides whether Phase 6 requirements and status can complete.
</verification>

<success_criteria>
- Current Phase 6 routes are genuinely served through fixed Web origin by exact current-revision images.
- PostgreSQL, volumes, business data, sequences, media and ledger stable facts are preserved under the precise timestamp rule.
- Evidence is sanitized, atomic and reconstructable rather than self-asserted.
- Production remains `BLOCKED`; no server, deployment, unfreeze or push occurs.
</success_criteria>

## Source Coverage

| Source | Constraint carried forward |
|---|---|
| ROADMAP / REQUIREMENTS | Fixed 3100 observability closes Phase 6 delivery gap while SRCH/READ completion remains verifier-owned. |
| CONTEXT / RESEARCH | Same-origin visitor path, no server access, strict public-only discovery and low-resource topology. |
| 06-VERIFICATION | Repair only the one stale runtime/provenance link and preserve all already-verified product facts. |
| 06-04 / 06-06 | Use sanitized offline images and the newly committed strict adapter; do not bypass tested barriers. |
| Debugger findings | Treat the consumed `b7fa05c` failure truthfully and allow the next actual refresh only once on a new clean revision. |

<output>
Create `ops/phase6-local-refresh-evidence.json`, update 06-03-SUMMARY, create 06-07-SUMMARY, and hand off to an independent verifier. Do not edit completion authorities or push.
</output>
