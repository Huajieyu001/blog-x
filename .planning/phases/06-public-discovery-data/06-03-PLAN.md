---
phase: 06-public-discovery-data
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
user_setup: []
must_haves:
  truths:
    - "A dedicated `--phase6-data` gate owns a generated `blogxverify_*` PostgreSQL/Compose namespace, migrates it, runs every Phase 6 discovery and reused public-boundary suite exactly once, rejects skips/zero TAP tests, and cleans only its namespace."
    - "The exact full gate includes `--interruption-check`: it kills only its named generated migration process after advisory-lock acquisition, proves two retrying migrations recover on the same volume with retained data/schema, and leaves no interrupt container or lock owner."
    - "The exact full gate includes `--parallel-check`: it launches two distinct internal `--phase6-data --skip-build` children on distinct generated namespaces/ports, requires each child to run the real Phase 6 suites and terminal marker, and proves exact per-child cleanup with no receipt authority."
    - "The Phase 6 gate performs workspace typecheck/build, schema/ledger verification, public discovery/list/visibility/taxonomy regressions, repository boundaries, and an explicit canonical RELEASE BLOCKED decision without writing a receipt."
    - "Phase 6 selection is separate from `phase5Selection('full')`; a complete hash manifest of all tracked `.planning/milestones` evidence plus the retained v1.0 receipt remains unchanged, while Phase 8 retains authority for a v1.1 full receipt."
    - "Before and after the generated full gate, fixed `blogxlocal` container/image/volume identities, business-table data excluding `blog_x_schema_ledger`, sequence state, complete ledger rows, and media-file digests match exactly; generated interruption/parallel work never restarts or mutates the fixed environment."
    - "After the committed Phase 6 gate passes, the fixed `blogxlocal` project rebuilds Web/API from that clean revision, runs idempotent migration/schema verification, and preserves exact business-table, sequence, media, and ledger stable-field values; only `blog_x_schema_ledger.applied_at` may advance. If cached dependencies/images are insufficient, the refresh stops without runtime mutation; Phase 8 owns cache-loss/offline recovery automation."
    - "The refreshed `http://127.0.0.1:3100` is checked through the Web origin for home/category/tag/archive routes, `/api/health`, empty search, and hidden/unknown related 404; the summary reports the revision and visible/API changes."
    - "The executor creates summaries and hands off to an independent verifier, but does not mark Phase 6 or SRCH-01/02/03/READ-08 complete; production remains BLOCKED and neither cloud server is contacted."
  artifacts:
    - path: scripts/local-verify.mjs
      provides: isolated Phase 6 data selection, real interruption recovery, two-child parallel execution, generated database execution, strict TAP enforcement, boundary/release checks, and machine-readable pass marker
      exports:
        - phase6Selection
    - path: scripts/local-verify.test.mjs
      provides: exact once-only Phase 6 selection, interruption lifecycle, two-parallel-child, no-receipt, cleanup, and BLOCKED-state regression coverage
  key_links:
    - from: scripts/local-verify.mjs
      to: apps/api/test/public-discovery.test.ts
      via: parent and both parallel Phase 6 children set `PUBLIC_DISCOVERY_TEST_DATABASE_URL` to their own generated migrated databases and parse each TAP result fail-closed
      pattern: "PUBLIC_DISCOVERY_TEST_DATABASE_URL|public-discovery\.test\.ts|assertSemanticTap|--phase6-data"
    - from: scripts/local-verify.mjs
      to: compose.yaml
      via: interruption kills only the exact generated migration container and parallel checks run two exact generated Phase 6 namespaces/ports before namespace-owned cleanup
      pattern: "interruptionCheck|migration_interrupt|parallelCheck|blogxverify_"
    - from: scripts/local-verify.mjs
      to: scripts/check-boundaries.mjs
      via: Phase 6 terminal checks require zero repository findings before emitting the local data pass marker
      pattern: "auditRepository|evaluateRepositoryBoundaries|LOCAL PHASE 6 DATA PASS"
    - from: scripts/local-verify.mjs
      to: ops/release-evidence.blocked.json
      via: local completion calls the existing pure release gate with `--expect-blocked` and never creates deployment authority
      pattern: "release-gate\.mjs|--expect-blocked|RELEASE BLOCKED"
    - from: compose.yaml
      to: http://127.0.0.1:3100
      via: final manual local refresh pins project `blogxlocal`, explicit revision-tagged Web/API images, fixed origin, existing named volumes, migration, and health waits
      pattern: "blogxlocal|BLOG_X_PUBLIC_ORIGIN|3100"
  prohibitions:
    - statement: "Phase 6 verification must not mutate or regenerate any tracked `.planning/milestones` evidence or the retained v1.0 Phase 5 receipt."
      status: resolved
      verification: test
    - statement: "Local refresh must not run Compose down with volumes, remove a volume, create a second project, use a temporary verifier URL at port 3100, or touch a non-blogxlocal container."
      status: resolved
      verification: command
    - statement: "No Phase 6 command, test, summary, or gate may use SSH, deploy, unfreeze production, contact either server, or transition canonical release state from BLOCKED."
      status: resolved
      verification: test
---

<objective>
Create the independent Phase 6 generated-data gate, run the complete local evidence set, then refresh the fixed `blogxlocal` environment from the verified committed revision without losing data.

Purpose: Make Phase 6 reproducibly verifiable and immediately visible at `127.0.0.1:3100` while preserving historical evidence, local content, and the production freeze.
Output: A strict Phase 6 verifier selection, full local gate result, healthy revision-bound local Web/API/PostgreSQL runtime, and independent-verifier handoff.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-CONTEXT.md
@.planning/phases/06-public-discovery-data/06-RESEARCH.md
@.planning/phases/06-public-discovery-data/06-01-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-02-SUMMARY.md
@scripts/local-verify.mjs
@scripts/local-verify.test.mjs
@scripts/check-boundaries.mjs
@scripts/release-gate.mjs
@compose.yaml
@apps/api/Dockerfile
@apps/web/Dockerfile
@ops/phase5-full-gate-receipt.json
@.planning/milestones/v1.0-MILESTONE-AUDIT.md

<interfaces>
- `phase6Selection("data")` names exactly once: `PUBLIC_DISCOVERY_TEST_DATABASE_URL:apps/api/test/public-discovery.test.ts`, existing public-list, public-visibility, taxonomy, Phase-2 public-boundary suites, and `scripts/local-verify.test.mjs`/boundary checks needed to guard reused authorities.
- CLI flag is exactly `--phase6-data`; it is mutually exclusive with Phase 3/4/5 modes and selects `runPhase6DataChecks(context)` without entering any Phase 5 receipt writer/finalizer path. An `--internal-run --phase6-data --skip-build` child is allowed only for the parallel mechanism and has no receipt authority.
- The Phase 6 runner uses existing generated namespace/database/media validation, migration/schema verification, offline Compose build behavior, `runDatabaseSuite`, strict `assertSemanticTap`, cleanup, and log redaction. It ends with `[local-verify] LOCAL PHASE 6 DATA PASS; RELEASE BLOCKED` only after all checks succeed.
- The gate command is exactly `corepack pnpm local:verify -- --phase6-data --interruption-check --parallel-check`. It may use cached local dependencies/images, loopback, Docker/Compose, and generated temporary namespaces only.
- `--interruption-check` uses the existing exact `${namespace}_migration_interrupt` lifecycle: wait for the advisory-lock marker, kill/remove only that generated container, run two concurrent migration retries, verify schema/ledger and retained sentinel data, and compare the same generated PostgreSQL volume identity before/after.
- When `phase6Data` and `--parallel-check` are combined, `parallelCheck` launches two distinct children with `--internal-run --phase6-data --skip-build --namespace=<unique> --web-port=<unique>`; each child must execute the actual Phase 6 suite set and print its namespace pass plus `LOCAL PHASE 6 DATA PASS; RELEASE BLOCKED`. The parent rejects a duplicate namespace/port, missing marker, child skip/failure, leftover child container/volume, or any Phase 5 receipt call.
- No Phase 6 receipt is introduced. Build a sorted SHA-256 manifest from every tracked path returned by `git ls-files .planning/milestones ops/phase5-full-gate-receipt.json`; the complete manifest must match before/after the full gate.
- Before the full gate, capture fixed `blogxlocal` container IDs/image IDs/started times, volume IDs/creation times, a business-data dump digest that excludes `blog_x_schema_ledger`, exact PostgreSQL sequence state, the complete ledger rows including `applied_at`, and a sorted media relative-path/byte/SHA-256 digest. Because the generated gate never migrates `blogxlocal`, every fact including ledger timestamps must match after interruption, both parallel children, and cleanup.
- Final local refresh requires a clean committed Phase 6 HEAD. Compose project is exactly `blogxlocal`; origin is exactly `http://127.0.0.1:3100`; Web/API image tags contain the short HEAD; exact existing volumes are `blogxlocal_postgres-data` and `blogxlocal_media-data` and their IDs/creation times must match before/after.
- Refresh order is inspect/record volumes and current data authority -> build revision-tagged Web/API with network-none Compose builds -> start/wait PostgreSQL -> run idempotent migrate and schema verify against that PostgreSQL -> recreate/wait API/Web without `down` -> compare volume identity -> route/API checks.
- Phase 8 owns the durable one-command updater plus cache-loss and offline recovery behavior. Phase 6 performs no pull/install/network fallback: if frozen dependency installation layers, pinned base images, or required local build cache are absent, stop before recreating any `blogxlocal` service and leave the last known-good runtime/data untouched.
- On any cache preflight, build, migration, schema, health, volume/data-digest, or route failure, stop and report; never delete/recreate volumes, switch project name, or broaden cleanup.
- Executor leaves REQUIREMENTS/ROADMAP phase statuses pending and does not edit a Phase 6 verification report; a new independent verifier owns completion decisions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an isolated fail-closed Phase 6 data selection</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs</files>
  <read_first>
    <item>scripts/local-verify.mjs — generated namespace lifecycle, phase3/4/5 selections, runDatabaseSuite, strict TAP parser, runSingle branching, CLI exclusivity, cleanup, and release check.</item>
    <item>scripts/local-verify.test.mjs — exact once-only selection, semantic skip/zero, cleanup, topology, and receipt-boundary tests.</item>
    <item>apps/api/test/public-discovery.test.ts — required environment variable and complete Phase 6 semantic scope.</item>
    <item>ops/phase5-full-gate-receipt.json and archived v1.0 audit/verification — immutable evidence outside the Phase 6 writer path.</item>
  </read_first>
  <behavior>
    - "RED: phase6Selection('data') returns the exact unique database/node/boundary suite set and rejects unknown modes."
    - "RED: --phase6-data chooses only runPhase6DataChecks, sets every required generated database environment variable, and cannot acquire/write/finalize a Phase 5 receipt."
    - "RED: --phase6-data --interruption-check waits for the generated migration lock marker, kills/removes only `${namespace}_migration_interrupt`, recovers with two concurrent retries on the same volume, and proves retained sentinel/schema/ledger state."
    - "RED: --phase6-data --parallel-check creates two distinct namespaces and ports, invokes two `--internal-run --phase6-data --skip-build` children, requires each real Phase 6 terminal marker, and rejects duplicate/missing/failed/leftover child authority."
    - "RED: discovery TAP skip, zero, failure, cancellation, TODO, missing footer, or absent database authority fails the gate."
    - "RED: schema/migration drift, boundary finding, non-BLOCKED release decision, dirty log secret, or generated cleanup failure suppresses the terminal Phase 6 pass marker."
    - "RED: parent, interruption path, and both parallel children never acquire a Phase 5 receipt lock or write/finalize a receipt; phase5Selection('full') membership is unchanged."
  </behavior>
  <action>
    Add RED selector/runner source and behavior assertions first. Implement exported `phase6Selection(mode)` and internal `runPhase6DataChecks(context)` using the existing generated Compose lifecycle and strict semantic parser. Select the new discovery suite plus existing public list, public visibility, taxonomy, Phase-2 public-boundary, and local verifier regressions exactly once. Run workspace typecheck/build, each database suite with its required variable bound to `context.databaseUrl`, schema verification, repository boundaries, and the existing pure release check. Add `--phase6-data` to CLI exclusivity and `runSingle` labeling/branching without changing `phase5Selection`, manifest creation, receipt lock, or receipt writer. Preserve the existing process-level `interruptionCheck` but add Phase 6 assertions that it reaches the advisory-lock marker, kills/removes only the exact generated interrupt container, runs two concurrent retry migrations, retains sentinel/schema/ledger state, and compares volume identity. Extend `parallelCheck` so Phase 6 selects child mode `--internal-run --phase6-data --skip-build` rather than infrastructure-only; generate distinct validated namespaces and loopback ports, require each child to execute `runPhase6DataChecks`, verify each namespace-specific data-pass/BLOCKED marker, inspect that both child projects are absent after cleanup, and reject any receipt authority or target mutation. Emit the exact terminal marker only after all outcomes pass. Make tests GREEN, run them repeatedly, and verify historical artifacts through the complete tracked v1 evidence manifest rather than three hand-picked files.
  </action>
  <acceptance_criteria>
    <criterion>`phase6Selection("data")` has unique suite paths and includes discovery/list/visibility/taxonomy/public-boundary plus local-verifier/boundary evidence exactly once.</criterion>
    <criterion>The Phase 6 runner always provides generated database authority; its semantic parser rejects skipped or zero-test output and no selected suite can silently skip.</criterion>
    <criterion>Source/behavior tests prove Phase 6 never calls receipt acquisition, manifest/finalization, or atomic writer paths and does not alter Phase 5 selection.</criterion>
    <criterion>CLI rejects combining `--phase6-data` with any Phase 3/4/5 mode; an internal Phase 6 child is accepted only through the validated generated parallel path and never gets receipt authority.</criterion>
    <criterion>Interruption assertions prove real process kill/retry/sentinel/schema/volume recovery; parallel assertions prove two real Phase 6 child suite runs, distinct namespace/port authority, both terminal markers, and exact per-child cleanup.</criterion>
    <criterion>Focused local-verifier tests, typecheck, and boundaries pass while every tracked `.planning/milestones` file and the retained receipt remain byte-identical.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/local-verify.test.mjs &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json</automated>
  </verify>
  <done>A dedicated Phase 6 command owns complete generated data evidence and cannot write or reinterpret the archived v1.0 receipt chain.</done>
</task>

<task type="auto">
  <name>Task 2: Run the exact Phase 6 full data gate from a clean committed HEAD</name>
  <files>scripts/local-verify.mjs, scripts/local-verify.test.mjs</files>
  <read_first>
    <item>The committed Task 1 HEAD and empty `git status --porcelain`; stop if implementation changes are uncommitted.</item>
    <item>scripts/local-verify.mjs — exact Phase 6 selection, interruption lifecycle, two-child parallel mode, terminal markers, no-receipt path, and cleanup.</item>
    <item>scripts/local-verify.test.mjs — once-only suite, real interruption/parallel selection, and no-receipt assertions.</item>
    <item>All tracked `.planning/milestones` files plus `ops/phase5-full-gate-receipt.json` — complete v1 evidence hash-manifest scope.</item>
    <item>The running fixed `blogxlocal` container/image/volume identities, business-data dump excluding `blog_x_schema_ledger`, sequence state, complete ledger rows, and media manifest — unrelated local authority the generated gate must preserve.</item>
  </read_first>
  <action>
    Require and record a clean committed implementation revision. Before running anything, write a sorted SHA-256 manifest for every tracked path returned by `git ls-files .planning/milestones ops/phase5-full-gate-receipt.json`. Separately snapshot the fixed `blogxlocal` container IDs, image IDs, started times and health; PostgreSQL/media volume IDs and creation times; SHA-256 of a data-only dump excluding `blog_x_schema_ledger`; exact schema/name/value rows from `pg_sequences`; complete ordered `blog_x_schema_ledger` rows including `scope,migration_count,migration_fingerprint,applied_at`; and a sorted media relative-path/byte/SHA-256 manifest. Run exactly `corepack pnpm local:verify -- --phase6-data --interruption-check --parallel-check`; do not substitute focused commands, omit either flag, add Phase 5 flags, reuse earlier logs, or accept a skip. The parent must exercise actual migration interruption/recovery and then the real Phase 6 suite. The parallel stage must start two distinct internal Phase 6 children, each execute all Phase 6 data suites under its own generated database/media/port authority, emit both namespace and Phase 6 data-pass/BLOCKED markers, and clean only its project. After success, confirm the parent interrupt container and all three generated project containers/volumes are absent, no receipt lock/sibling was created, canonical release is BLOCKED, the complete v1 evidence manifest matches, and every pre-gate `blogxlocal` identity, business data, sequence, complete ledger, and media fact matches exactly. On any discrepancy or failure, preserve the last known-good `blogxlocal` runtime and stop before Task 3; do not manually edit evidence or mark requirements complete.
  </action>
  <acceptance_criteria>
    <criterion>The exact three-flag gate exits 0 from the clean committed Task 1 revision and prints the parent plus both child namespace passes and Phase 6 data-pass/release-BLOCKED facts.</criterion>
    <criterion>Parent interruption kills only its exact migration process and proves concurrent retry, retained sentinel, schema/ledger, and unchanged generated volume identity.</criterion>
    <criterion>Each of two distinct parallel namespaces runs all selected suites with nonzero passes and zero failures/skips/TODO/cancellations; every selected suite executes once per namespace.</criterion>
    <criterion>Parent/child generated containers, volumes, interrupt process, and receipt locks are absent after terminal cleanup while all pre-gate fixed `blogxlocal` identity, business-data, sequence-state, complete-ledger, and media digests match.</criterion>
    <criterion>The complete tracked v1 evidence hash manifest matches its pre-gate bytes and no Phase 6 receipt exists.</criterion>
    <criterion>The worktree remains clean and REQUIREMENTS/ROADMAP/STATE completion fields remain unchanged pending independent verification.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm local:verify -- --phase6-data --interruption-check --parallel-check &amp;&amp; node scripts/check-boundaries.mjs &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The clean Phase 6 implementation has one reproducible generated-data pass, zero boundary findings, immutable historical evidence, and an explicit BLOCKED production decision.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh fixed blogxlocal from the verified revision without replacing data volumes</name>
  <files>compose.yaml, apps/api/Dockerfile, apps/web/Dockerfile</files>
  <read_first>
    <item>The clean committed revision that passed Task 2; no local refresh may use a different or dirty source tree.</item>
    <item>compose.yaml — fixed 3100 binding, network-none builds, health checks, restart policy, and named PostgreSQL/media volumes.</item>
    <item>apps/api/Dockerfile and apps/web/Dockerfile — cached frozen dependency layers and runtime commands.</item>
    <item>.planning/REQUIREMENTS.md — DEVX-01/02/03 and Phase 8 own the future one-command updater, cache-loss/offline recovery, provenance automation, and recurring major-step refresh workflow; this task is a bounded manual Phase 6 refresh only.</item>
  </read_first>
  <precondition>`docker volume inspect blogxlocal_postgres-data blogxlocal_media-data` succeeds for the existing fixed local environment; otherwise stop rather than creating replacement data authority.</precondition>
  <action>
    Capture clean full/short HEAD, exact volume IDs/creation times, currently running container/image IDs and started times, SHA-256 of a PostgreSQL data-only dump excluding `blog_x_schema_ledger`, exact ordered `pg_sequences` state, exact ordered ledger fields `{scope,migration_count,migration_fingerprint,applied_at}`, and a sorted media relative-path/byte/SHA-256 manifest into `/private/tmp/blog-x-phase6-*` diagnostics. Before service mutation, preflight the exact pinned base images, frozen installed dependency tree, and Docker build cache needed by both `network: none` Dockerfile builds. Phase 8 owns cache-loss/offline recovery; if this preflight or either build cannot complete without registry/network access, stop safely before migration or Compose recreation, retain the current containers/volumes, and report `LOCAL REFRESH BLOCKED: OFFLINE CACHE MISSING` rather than pulling, installing, retagging stale verifier images, or weakening network isolation. On successful preflight, export explicit revision-tagged `BLOG_X_API_IMAGE=blog-x-api-local:<short-head>`, `BLOG_X_WEB_IMAGE=blog-x-web-local:<short-head>`, and `BLOG_X_PUBLIC_ORIGIN=http://127.0.0.1:3100`. With Compose project exactly `blogxlocal`, build API and Web from the clean tree using the existing `network: none` build contract, start/wait only PostgreSQL, run API `db:migrate` then `db:schema:verify`, and run `up -d --wait --no-build api web`; never use `down`, `--volumes`, another project name, or any volume removal. After migration, require the business-data digest excluding the ledger, every sequence row/value, media manifest, volume identity, ledger row count, and each ledger `scope,migration_count,migration_fingerprint` tuple to equal the before snapshot. The only accepted database difference is that the same ledger row's `applied_at` is strictly later than its prior value; a changed/missing/extra ledger row, a regressed/equal timestamp after the migration update, or any other table/sequence/media mutation fails the refresh. Confirm all three exact containers are healthy and Web/API use the revision-tagged images, then request only loopback Web-origin URLs. Require home, categories, tags, and archive pages plus `/api/health` to succeed; require `/api/public/search?q=` to return the exact empty-query state and an unknown `/api/public/articles/:slug/related` to return exact 404 not_found. Record/report the full revision, image IDs, volume/business-data/sequence/ledger/media preservation, health, checked routes, and the Phase 6 visible change (new data APIs; no Phase 7 UI yet). Do not modify/commit Compose or Dockerfiles unless a demonstrated bug requires returning to a new planned fix; runtime refresh alone leaves them unchanged.
  </action>
  <acceptance_criteria>
    <criterion>Project label remains exactly `blogxlocal`; Web binds only `127.0.0.1:3100`, API/PostgreSQL have no host-published ports, and no verifier origin is persisted at 3100.</criterion>
    <criterion>`blogxlocal_postgres-data` and `blogxlocal_media-data` IDs/creation times, business-data dump digest excluding `blog_x_schema_ledger`, every sequence state row/value, and sorted media path/byte/hash manifest are identical before and after; no new replacement volume or other table/sequence/media mutation exists.</criterion>
    <criterion>Ledger row count and every `scope,migration_count,migration_fingerprint` tuple are identical; for the same row, `applied_at` is the sole allowed change and is strictly later after `db:migrate`. Any other ledger difference fails.</criterion>
    <criterion>Missing pinned image/dependency/build cache fails before migration or service recreation with `LOCAL REFRESH BLOCKED: OFFLINE CACHE MISSING`; Phase 6 never attempts network recovery, and Phase 8 remains the owner of cache-loss/offline recovery automation.</criterion>
    <criterion>Migration and schema verification exit 0; Web/API/PostgreSQL health is healthy; Web/API containers use images tagged with the verified short HEAD.</criterion>
    <criterion>Loopback home/categories/tags/archive/API health checks pass, empty search returns `state: empty_query`, and unknown related returns HTTP 404 `{error:"not_found"}` through `/api` on the Web origin.</criterion>
    <criterion>The summary reports revision, image/volume/health/routes and accurately says Phase 6 adds API data behavior without a new search/related visual interface.</criterion>
    <criterion>No server, SSH, deployment, unfreeze, production release, archived receipt, or requirement-completion action occurs.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker-compose -p blogxlocal -f compose.yaml ps &amp;&amp; curl -fsS http://127.0.0.1:3100/ &amp;&amp; curl -fsS http://127.0.0.1:3100/api/health &amp;&amp; node -e "Promise.all(['/categories','/tags','/archive'].map(async p=&gt;{const r=await fetch('http://127.0.0.1:3100'+p);if(!r.ok)throw new Error(p+' '+r.status)})).then(async()=&gt;{const s=await fetch('http://127.0.0.1:3100/api/public/search?q=');const j=await s.json();if(!s.ok||j.state!=='empty_query')throw new Error('empty search contract failed');const r=await fetch('http://127.0.0.1:3100/api/public/articles/phase6-unknown/related');const b=await r.json();if(r.status!==404||b.error!=='not_found')throw new Error('related 404 contract failed')})"</automated>
  </verify>
  <done>The fixed local site is running the verified Phase 6 revision at 3100 with preserved data volumes, healthy services, working discovery API contracts, and no production action.</done>
</task>

</tasks>

## Atomic commit sequence

1. After Task 1 is GREEN, commit only `scripts/local-verify.mjs` and `scripts/local-verify.test.mjs` as `test(06-03): add isolated Phase 6 data gate`.
2. Task 2 is the exact interruption-plus-parallel evidence execution against that clean commit and creates no product/evidence commit; Phase 8 owns a v1.1 full receipt.
3. Task 3 changes runtime state only and creates no source commit. Execute-plan creates `06-03-SUMMARY.md` later in its normal documentation commit; an independent verifier owns any subsequent verification/status commit.

## Artifacts this phase produces

- `scripts/local-verify.mjs`: exported `phase6Selection`, internal `runPhase6DataChecks`, CLI flag `--phase6-data`, real interruption recovery, two-child Phase 6 parallel mode, exact terminal marker, and separate no-receipt generated-data path.
- `scripts/local-verify.test.mjs`: Phase 6 once-only selection, strict TAP, interruption lifecycle, real parallel child mode, cleanup, complete v1-evidence immutability, and BLOCKED-decision tests.
- Runtime-only `blogxlocal` revision-tagged Web/API images and `/private/tmp/blog-x-phase6-*` diagnostics; these are untracked evidence and do not become production authority.
- `.planning/phases/06-public-discovery-data/06-03-SUMMARY.md`: executor handoff with gate/local-refresh facts, created by execute-plan after tasks finish.

<threat_model>
## Trust Boundaries (ASVS L1; block on high)

| Boundary | Description |
|----------|-------------|
| Verifier -> Docker/Compose | Generated names and cleanup must never target fixed local or production-like resources. |
| Clean Git revision -> local images | Built images must correspond to the committed gate revision, not a dirty or temporary tree. |
| Compose recreation -> persistent volumes | Web/API refresh and migration must retain PostgreSQL/media volume identity and content. |
| Local pass -> release decision | Local evidence must never grant deployment/unfreeze authority or mutate canonical BLOCKED state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Tampering | historical receipt chain | high | mitigate | Separate selector/no writer path, before/after hashes, source tests, and diff checks. |
| T-06-12 | Denial of Service | interruption/parallel cleanup | high | mitigate | Exact generated interrupt container, distinct child namespaces/ports, child Phase 6 markers, and namespace-owned terminal cleanup. |
| T-06-13 | Tampering | blogxlocal persisted state | high | mitigate | Exact volume identity, business-data-with-ledger-excluded digest, sequence/media equality, and stable ledger tuple comparison with only monotonic `applied_at` allowed. |
| T-06-14 | Spoofing | revision/image provenance | medium | mitigate | Clean HEAD gate, revision-tagged images, and container-image identity inspection. |
| T-06-15 | Elevation of Privilege | production release | high | mitigate | Pure expect-blocked gate, no remote adapter, no SSH/deploy commands, and verifier-owned completion decision. |
| T-06-16 | Tampering | offline cache fallback | high | mitigate | Preflight pinned local cache and stop before runtime mutation; Phase 8 owns recovery and no network fallback is permitted here. |
</threat_model>

<verification>
- `node --test scripts/local-verify.test.mjs` proves selection and no-receipt boundaries.
- Exact `corepack pnpm local:verify -- --phase6-data --interruption-check --parallel-check` passes from a clean committed revision with real process recovery and two real Phase 6 child namespaces.
- Boundary and pure release checks report zero findings and `BLOCKED`.
- Complete tracked v1 evidence and pre-gate fixed `blogxlocal` identity/database/media facts remain byte-identical through generated interruption/parallel cleanup.
- Fixed local refresh either safely stops before mutation on missing cache or preserves volume/business-data/sequence/media plus ledger stable fields (allowing only `applied_at` to advance), reaches three healthy services, and passes loopback main-route/discovery checks.
- A separate verifier reads all three summaries and independently decides Phase 6/requirements status; executor does not decide completion.
</verification>

<success_criteria>
- Phase 6 has a named, isolated, repeatable local data gate with nonzero semantic evidence and no v1.0 receipt mutation.
- The latest verified revision is observable at `http://127.0.0.1:3100` after migration and health checks, with existing data retained.
- Production stays `BLOCKED`; neither cloud server is contacted or modified.
</success_criteria>

<output>
Create `.planning/phases/06-public-discovery-data/06-03-SUMMARY.md` when done, then hand off to a new independent Phase 6 verifier. Do not edit REQUIREMENTS.md, ROADMAP.md, STATE.md completion fields, or a Phase 6 verification report from the executor.
</output>
