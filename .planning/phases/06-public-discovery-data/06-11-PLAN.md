---
phase: 06-public-discovery-data
plan: "11"
type: execute
wave: 8
depends_on:
  - 06-10
files_modified:
  - ops/phase6-local-refresh-evidence.json
  - .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
  - .planning/phases/06-public-discovery-data/06-11-SUMMARY.md
autonomous: true
gap_closure: true
independent_plan_check: required
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - READ-08
estimate:
  tokens: 7500
  confidence: medium
  calibration_factor: 1
user_setup: []
must_haves:
  truths:
    - "Execution is eligible only after an independent checker reports no blocker and a new clean committed revision contains the complete 06-10 GREEN implementation plus 06-10-SUMMARY. It differs from `df4aa3b`, and its exact claim, companion failure report and final evidence are absent."
    - "Before execution the executor runs the exact read-only claim-absent and failure-report-absent CLIs for the literal revision. It then starts exact argv `node scripts/refresh-local.mjs` once and only once; inside that process clean Git validation is followed immediately by atomic claim publication before adapter/daemon/collector construction or any Docker check. No wrapper, import, alias, manual Docker/Compose/migration/cutover/rollback/evidence command or retry may substitute for that single process."
    - "A failed sole invocation after claim publication is terminal for its revision. The exact read-only CLIs prove claim present and failure report present; final evidence is absent, exact generated one-off is absent, and the strict sanitized report plus read-only v4 reconstruction proves preservation, records pre-runtime not-applicable, or explicitly reports an unrecoverable invariant. A later attempt requires a new remediation revision and plan."
    - "A successful invocation atomically publishes strict sanitized evidence v4 only after exact Git/raw lock, local daemon, seed/target provenance, target schema, all persistence/ledger transitions, immutable cutover, same-origin routes and canonical BLOCKED pass."
    - "After the sole process returns, every action is read-only except the evidence-only Git commit and later docs-only Git commit. On success the exact failure-report CLI proves absent before every verification/commit; the verifier reconstructs facts from sealed production sources and rejects drift, and no later command mutates Docker, PostgreSQL, volumes, media, claim, failure report, evidence content or runtime."
    - "The evidence commit contains only `ops/phase6-local-refresh-evidence.json`; the later docs commit contains only the historical 06-03 addendum and 06-11-SUMMARY. Existing milestones, Phase 5 receipt, 06-VERIFICATION and REQUIREMENTS/ROADMAP/STATE completion authority stay byte-identical during execution."
    - "A fresh independent Phase 6 verifier runs only after evidence and docs commits. It alone may decide whether Phase 6 closes; executor-produced evidence and summaries do not self-certify completion."
    - "Only fixed local `blogxlocal` resources and loopback Web origin are in scope. No registry/pull/network server contact, SSH/cloud/deploy/push/unfreeze/production transition occurs, and release remains `BLOCKED`."
  artifacts:
    - path: ops/phase6-local-refresh-evidence.json
      provides: atomic strict v4 evidence bound to the sole clean 06-10 implementation revision and reconstructed fixed local runtime
    - path: .planning/phases/06-public-discovery-data/06-03-SUMMARY.md
      provides: dated closure addendum preserving all earlier safe-stop history
    - path: .planning/phases/06-public-discovery-data/06-11-SUMMARY.md
      provides: exact attempt/evidence/commit results and independent-verifier handoff
  key_links:
    - from: scripts/refresh-local.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: the sole exact no-option production process owns every mutation and atomic v4 publication
      pattern: "runRefreshCli|createProduction|phase6-local-refresh-evidence"
    - from: scripts/refresh-local-facts.mjs
      to: ops/phase6-local-refresh-evidence.json
      via: sealed read-only verification reconstructs Git/lock/seeds/targets/schema/persistence/ledger/topology/routes/release facts
      pattern: "collectRefreshFacts|evidenceVersion|BLOCKED"
    - from: ops/phase6-local-refresh-evidence.json
      to: .planning/phases/06-public-discovery-data/06-11-SUMMARY.md
      via: the summary cites exact implementation/evidence revisions and digests without claiming independent completion
      pattern: "implementationRevision|sha256|independent|BLOCKED"
  prohibitions:
    - statement: "Never invoke the no-option refresh more than once for the exact implementation revision, including after Git/claim-publication, post-claim daemon/preflight, migration, schema, cutover, route, release, evidence or rollback failure."
      status: pending
      verification: command
    - statement: "Do not manually create/edit/delete the claim/failure report/evidence, or run substitute Docker/Compose/database/cutover/rollback commands before or after the one production process."
      status: pending
      verification: command
    - statement: "Do not contact registry/servers, use SSH, deploy, push, unfreeze production, edit protected evidence/completion authority, or transition release from BLOCKED."
      status: pending
      verification: command
---

<objective>
Consume exactly one attempt for the independently checked clean 06-10 implementation revision, commit reconstructable v4 evidence and truthful documentation separately, then hand Phase 6 to a fresh independent verifier.

Purpose: Replace the stale fixed local runtime without risking persistent content, retrying an ambiguous mutation, or expanding into production authority.
Output: Current Phase 6 API/Web at fixed 3100, evidence-only commit, docs-only closure commit, and independent verification handoff.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-public-discovery-data/06-03-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-08-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-09-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-10-PLAN.md
@.planning/phases/06-public-discovery-data/06-10-SUMMARY.md
@.planning/phases/06-public-discovery-data/06-VERIFICATION.md
@scripts/refresh-local-facts.mjs
@scripts/refresh-local-live.mjs
@scripts/refresh-local.mjs
@scripts/refresh-local-test-core.mjs
@scripts/refresh-local.test.mjs
@compose.yaml
@ops/release-evidence.blocked.json
@ops/phase5-full-gate-receipt.json
@.planning/milestones/v1.0-MILESTONE-AUDIT.md

<interfaces>
- Record a fresh independent no-blocker check for Plans 06-10 and 06-11. Set `REFRESH_REVISION` from exact clean `git rev-parse HEAD`; require it contains the 06-10 GREEN implementation and summary, differs from `df4aa3b702409754cc52e6f761d2218114c9b2bc`, and retain it as immutable evidence authority after later commits.
- Before process creation, use read-only repository/CLI checks only: clean tree, canonical BLOCKED, protected hashes and absent final evidence. Run exact argv `node scripts/refresh-local.mjs --check-attempt-claim=absent --revision=<literal-full-REFRESH_REVISION>` and exact argv `node scripts/refresh-local.mjs --check-failure-report=absent --revision=<literal-full-REFRESH_REVISION>`; require their canonical single-line outputs. No extra/duplicate option, standalone Docker/Compose/database command or adapter construction is allowed; the sealed refresh owns claim publication, local-authority and runtime preflight.
- Invoke one process with literal argv `node scripts/refresh-local.mjs`. No shell wrapper, pnpm alias, dynamic import, adapter call, redirected clone or second process is permitted. Inside the process, sealed Git sources re-resolve the same clean full revision and confirm claim absent, then atomically publish its claim immediately before constructing the live adapter or reading Docker environment/context/daemon authority and before every collector/build/mutation check. Claim creation is local filesystem authority, not Docker mutation. If claim publication itself fails, the adapter remains unconstructed and this plan stops without rerunning; after publication the revision attempt is consumed and every later exit retains the claim.
- Immediately after exit, run the exact literal-revision read-only claim-present check before branching. On nonzero post-claim exit, run exact argv `node scripts/refresh-local.mjs --check-failure-report=present --revision=<literal-full-REFRESH_REVISION>` and require canonical `REFRESH FAILURE REPORT PRESENT <revision> <sha256>` after strict fixed-path/schema/digest/revision validation. Then inspect only evidence/one-off absence and sealed read-only v4 surfaces; require proved preservation, pre-runtime not-applicable or the exact unrecoverable invariant, record a terminal stop outside the repo, and never retry that revision.
- On zero exit, run exact argv `node scripts/refresh-local.mjs --check-failure-report=absent --revision=<literal-full-REFRESH_REVISION>` and require canonical `REFRESH FAILURE REPORT ABSENT <revision>` before the first evidence verification. This CLI and all subsequent verifier/check modes are read-only: they do not construct the adapter, alter the persistent claim, create a report/evidence, or execute Docker/network commands beyond the verifier's declared read-only sources.
- On zero exit, first run sealed `--verify-evidence=ops/phase6-local-refresh-evidence.json`. Require evidence and claim bytes/digests unchanged by verification. It reconstructs exact current HEAD/raw lock, local context/socket, seed refs+IDs, target IDs/labels/filesystem/store, PostgreSQL identity/schema, volumes/business/sequences/ledger/media, fixed topology, exact same-origin routes, protected history and BLOCKED.
- v4 stage proof requires only `phase1.applied_at` strictly advances preflight→postMigration; every stable tuple and all other timestamps remain exact; postMigration→postCutover→terminal remains row-identical. Seed IDs remain stable, migration runs in target API, and API/Web cutover uses target immutable IDs.
- On success commit only evidence with message `chore(06-11): record sealed fixed refresh evidence`. Re-run the exact failure-report-absent CLI before commit and do not alter evidence afterward. The verifier's post-commit Git mode must prove REFRESH_REVISION ancestry and an intervening-path allowlist containing only that evidence path.
- Then append a dated closure to 06-03-SUMMARY preserving the original cache stop and all `b7fa05c`, `3221f99`, and `df4aa3b` audit stops. Create 06-11-SUMMARY with exact one-attempt count, implementation/evidence/docs commits and digests, v4 transitions, failure-report absence, current route visibility, Phase 7 UI absence, BLOCKED and zero-server facts. Commit only both summaries as `docs(06-11): complete sealed fixed refresh`.
- After the docs commit, prove the failure report still absent with the exact read-only CLI, then run sealed evidence verification again; ancestry/path allowlist now admits only the evidence and two summary paths. Dispatch a fresh independent Phase 6 verifier. The executor does not edit 06-VERIFICATION, REQUIREMENTS, ROADMAP or STATE completion; the independent verifier may update its own authority in a separate result only if all runtime/evidence requirements pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume the sole clean-revision process and commit v4 evidence</name>
  <files>ops/phase6-local-refresh-evidence.json</files>
  <precondition>Independent plan check has no blocker; worktree is clean at a committed 06-10 implementation-plus-summary revision distinct from `df4aa3b`; canonical BLOCKED and protected hashes pass; exact evidence is absent; exact read-only claim-absent and failure-report-absent CLIs accept only the literal full revision and emit canonical outputs. Otherwise stop before process creation.</precondition>
  <action>
    Capture exact REFRESH_REVISION and protected hashes outside the repository. Run the exact literal-revision claim-absent CLI and exact failure-report-absent CLI with no extra argv, then start `node scripts/refresh-local.mjs` exactly once. The process must publish the claim before adapter/daemon construction; immediately after exit prove the literal claim present. If exit is nonzero, first require the exact failure-report-present CLI and canonical digest line, then perform only read-only evidence/one-off/fact inspection, require final evidence absent and preservation/not-applicable/unrecoverable status truthful, and stop permanently for this revision. If exit is zero, first require the exact failure-report-absent CLI, then run sealed `--verify-evidence`; prove strict v4 facts/deltas/current runtime/routes/BLOCKED and evidence/claim byte stability. Check failure-report absent again, commit only the evidence file and retain REFRESH_REVISION as its implementation authority.
  </action>
  <verify>
    <automated>node scripts/refresh-local.mjs --check-attempt-claim=present --revision=&lt;literal-full-REFRESH_REVISION&gt; &amp;&amp; node scripts/refresh-local.mjs --check-failure-report=absent --revision=&lt;literal-full-REFRESH_REVISION&gt; &amp;&amp; node scripts/refresh-local.mjs --verify-evidence=ops/phase6-local-refresh-evidence.json &amp;&amp; node scripts/release-gate.mjs --evidence=ops/release-evidence.blocked.json --expect-blocked &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>The exact revision's one and only process succeeded; sealed verification reconstructed strict v4 evidence and current fixed runtime; evidence alone is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Commit truthful closure and dispatch independent final verification</name>
  <files>.planning/phases/06-public-discovery-data/06-03-SUMMARY.md, .planning/phases/06-public-discovery-data/06-11-SUMMARY.md</files>
  <precondition>Task 1 succeeded and its evidence-only commit exists; no second refresh or mutable runtime command is allowed.</precondition>
  <action>
    Use only committed evidence and sealed read-only results. Require the exact failure-report-absent CLI before documentation. Preserve every earlier safe stop in 06-03 and append the dated 06-11 closure. Create 06-11-SUMMARY with exact commits/digests/attempt count, canonical failure-report absence, Git/lock/local-daemon/seed/target/schema/persistence/ledger/topology/route/BLOCKED results, visible Phase 6 APIs and absent Phase 7 UI. Commit only both summaries. Prove the report remains absent, run sealed evidence verification again after the docs commit, require the exact descendant-path allowlist, validate plan/summary links and protected hashes, then dispatch a fresh independent Phase 6 verifier. Do not self-edit completion authority.
  </action>
  <verify>
    <automated>node scripts/refresh-local.mjs --check-failure-report=absent --revision=&lt;literal-full-REFRESH_REVISION&gt; &amp;&amp; node scripts/refresh-local.mjs --verify-evidence=ops/phase6-local-refresh-evidence.json &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify-summary .planning/phases/06-public-discovery-data/06-11-SUMMARY.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify artifacts .planning/phases/06-public-discovery-data/06-11-PLAN.md &amp;&amp; node .codex/gsd-core/bin/gsd-tools.cjs verify key-links .planning/phases/06-public-discovery-data/06-11-PLAN.md &amp;&amp; node --test scripts/refresh-local.test.mjs &amp;&amp; git diff --exit-code -- .planning/milestones ops/phase5-full-gate-receipt.json .planning/phases/06-public-discovery-data/06-VERIFICATION.md</automated>
  </verify>
  <done>Evidence and truthful summaries are separately committed, post-commit v4 verification remains GREEN, and an independent verifier has sole completion authority.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
| ID | Threat | Severity | Control / verification |
|---|---|---|---|
| T-06-11-01 | Same revision is attempted twice after an authority failure | high | Clean Git then immediate atomic claim before adapter/daemon construction, exact absent/present gates, one literal process and terminal new-revision requirement. |
| T-06-11-02 | Wrong code/image/schema becomes current | high | Exact Git/raw lock, stable seeds, immutable target IDs/labels/filesystem/store and target-API schema reconstruction. |
| T-06-11-03 | Persistent/protected content changes | high | Row-addressed ledger rule plus exact database/sequence/media/volume/history comparisons at every phase. |
| T-06-11-04 | Failure or rollback exits without truthful proof | high | Exact present/absent report CLI, atomic strict companion report, finally recollection, evidence suppression and read-only terminal inspection. |
| T-06-11-05 | Remote or redirected authority is used | high | Sealed minimal environment, proven local Unix Docker context, no registry/pull, redirect-error exact loopback routes. |
| T-06-11-06 | Executor self-certifies completion | high | Separate evidence/docs commits, protected completion files, fresh independent verifier after both commits. |
</threat_model>

<verification>
- Independent plan checker passes before execution and independent Phase 6 verifier runs after evidence/docs commits.
- Exactly one bare process claims the clean 06-10 revision before adapter/daemon construction; every later runtime operation is read-only.
- Post-claim failure is terminal with exact claim-present/report-present proof and no evidence; success has exact report-absent proof and yields evidence-only then docs-only commits.
- No server, registry, deploy, unfreeze, push or release transition occurs; production remains frozen and `BLOCKED`.
</verification>

<success_criteria>
- Exact current revision API/Web is visible through fixed 3100 with strict v4 reconstruction of provenance, persistence, schema, topology, routes and BLOCKED.
- No same-revision retry or manual mutation substitute exists.
- Historical safe stops remain truthful, and independent verification alone decides Phase 6 completion.
- Existing production and protected artifacts remain unchanged.
</success_criteria>

<output>
Create `ops/phase6-local-refresh-evidence.json`, update 06-03-SUMMARY, create 06-11-SUMMARY, and hand off to a fresh independent Phase 6 verifier. Do not edit completion authority, contact servers or push.
</output>
