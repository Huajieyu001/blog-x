---
phase: 11-privacy-safe-view-authority
plan: "03"
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - packages/contracts/src/analytics.ts
  - packages/contracts/src/tracer.test.ts
  - apps/api/src/content/view-aggregation-repository.ts
  - apps/api/src/content/view-retention.ts
  - apps/api/src/security/config.ts
  - apps/api/src/app.ts
  - apps/api/package.json
  - package.json
  - apps/api/test/public-visibility.test.ts
  - apps/api/test/backup-restore.test.ts
  - apps/api/test/distribution-export.test.ts
  - apps/web/app/posts/[slug]/ViewBeacon.tsx
  - apps/web/app/posts/[slug]/page.tsx
  - apps/web/e2e/public-reading.spec.ts
  - scripts/local-verify.mjs
  - scripts/local-verify.test.mjs
autonomous: true
requirements: [STAT-04, STAT-06]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An exact bounded local command deletes only aggregate days outside the latest 400 Shanghai calendar days, and repeated runs converge safely to zero deletions."
    - "A complete PostgreSQL backup and isolated restore preserve every seeded article/day/source counter row exactly."
    - "The existing blog-x-portable-export version 1 remains backward compatible and contains no analytics table, field, counter or visitor data."
    - "A rendered public article sends one invisible credential-omitted same-origin beacon per actual page open, while unavailable pages send none."
    - "Focused Phase 11 verification and the canonical local delivery gate use exact schema, migration, backup and test-inventory authority and end in production BLOCKED."
  prohibitions:
    - "Cleanup cannot delete a retained day, accept an unbounded batch, use host-local calendar time, or require a queue/resident scheduler."
    - "Portable export cannot gain an analytics section or version bump, and backup evidence cannot contain request headers, socket addresses or visitor identifiers."
    - "The browser beacon cannot include cookies, credentials, referrer data, visitor IDs, visible UI, retries or a cross-origin URL."
    - "No server connection, credential use, destructive database command, main-branch mutation or production deployment occurs."
  artifacts:
    - path: "apps/api/src/content/view-retention.ts"
      provides: "strict cleanup argument parsing, bounded database cleanup and redacted result formatting"
      exports: ["parseCleanupViewsArguments", "runViewRetention", "formatCleanupViewsResult"]
    - path: "apps/api/test/backup-restore.test.ts"
      provides: "independent row-by-row aggregate restore equality"
    - path: "apps/web/app/posts/[slug]/ViewBeacon.tsx"
      provides: "nonvisual one-open same-origin anonymous beacon"
      exports: ["default"]
    - path: "scripts/local-verify.mjs"
      provides: "sealed Phase 11 data/recovery verification mode and exact migration constants"
  key_links:
    - from: "apps/api/src/content/view-retention.ts"
      to: "apps/api/src/content/view-aggregation-repository.ts"
      via: "validated limit invokes one ordered PostgreSQL bounded deletion"
      pattern: "cleanupExpiredDailyViews"
    - from: "scripts/local-verify.mjs"
      to: "apps/api/test/backup-restore.test.ts"
      via: "runner seeds source aggregates, creates a complete DB dump, restores it in a generated namespace and supplies the independent expected row map"
      pattern: "BACKUP_RESTORE_EXPECTED_ANALYTICS"
    - from: "apps/web/app/posts/[slug]/ViewBeacon.tsx"
      to: "POST /api/public/articles/:slug/view"
      via: "relative fetch with credentials omit after public detail hydration"
      pattern: "credentials.*omit"
    - from: "scripts/local-verify.mjs"
      to: "scripts/test-inventory.mjs"
      via: "Phase 11 selection and canonical integration retain exact discovered ownership"
      pattern: "phase11"
---

<objective>
Complete the Phase 11 lifecycle around anonymous aggregates: bounded 400-day retention, exact database recovery, portable-export exclusion, real article-page delivery and canonical local proof.

Purpose: Make anonymous PV data maintainable and recoverable without expanding the content export or creating a second operational service.
Output: Cleanup CLI, retention/convergence tests, backup/export equality, browser beacon, focused verifier mode and refreshed fixed local delivery.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-privacy-safe-view-authority/11-RESEARCH.md
@.planning/phases/11-privacy-safe-view-authority/11-01-SUMMARY.md
@.planning/phases/11-privacy-safe-view-authority/11-02-SUMMARY.md
@apps/api/src/app.ts
@apps/api/src/security/config.ts
@apps/api/src/content/export-repository.ts
@apps/api/test/backup-restore.test.ts
@apps/api/test/distribution-export.test.ts
@apps/web/app/posts/[slug]/page.tsx
@scripts/local-verify.mjs
@scripts/test-inventory.mjs

<interfaces>
Use the Phase 11 `articleDailyViews` table and `createViewAggregationRepository(db)` rather than adding another storage seam. Keep portable export authority at strict format `blog-x-portable-export`, version `1`. The existing local verifier owns generated Compose/database namespaces, concurrent migration, schema verification, backup creation, isolated restore, main-browser execution and cleanup; extend those authorities instead of creating an independent runner.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a bounded idempotent 400-day cleanup command</name>
  <reversibility rating="costly">The 400-day cutoff and command result contract govern durable deletion; changing them requires coordinated operational and test updates, though each batch remains bounded.</reversibility>
  <read_first>
    - .planning/REQUIREMENTS.md
    - packages/contracts/src/analytics.ts
    - packages/contracts/src/tracer.test.ts
    - apps/api/src/content/view-aggregation-repository.ts
    - apps/api/src/content/scheduled-publisher.ts
    - apps/api/src/security/config.ts
    - apps/api/src/app.ts
    - apps/api/package.json
    - package.json
    - apps/api/test/public-visibility.test.ts
  </read_first>
  <files>packages/contracts/src/analytics.ts, packages/contracts/src/tracer.test.ts, apps/api/src/content/view-aggregation-repository.ts, apps/api/src/content/view-retention.ts, apps/api/src/security/config.ts, apps/api/src/app.ts, apps/api/package.json, package.json, apps/api/test/public-visibility.test.ts</files>
  <behavior>
    - `cleanup-views` accepts exactly one `--limit=N` with integer N from 1 through 10000 and rejects every other invocation before opening a pool.
    - The retained window is the current Shanghai database date plus the preceding 399 dates; deletion predicate is strictly earlier than `((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Shanghai')::date - 399)`.
    - Each call locks and deletes at most N candidates ordered by `(day, article_id)` and returns only retained-from day, limit and deleted count.
    - Boundary/current rows survive, old rows delete in batches, repeated execution reaches zero, and concurrent cleaners cannot double-report or exceed their limits.
  </behavior>
  <action>
Extend the analytics contract with a strict `blog-x-view-retention` version-1 success result containing only `command: 'cleanup-views'`, `retainedFromDay` as an ISO date, validated `limit`, and nonnegative `deleted`; add contract tests that reject article IDs, slugs, headers and extra fields.

Add `cleanupExpiredDailyViews(limit)` to the aggregate repository. Use one PostgreSQL statement with a database-derived `retainedFromDay`, an ordered candidate CTE limited by the already-validated value and row locking with `SKIP LOCKED`, then `DELETE ... USING` on the composite key. Return one authoritative boundary/count row even when nothing is deleted. Never load candidates into JavaScript, delete without a limit, calculate the cutoff from the API host, or expose deleted row identities.

Create `view-retention.ts` for exact CLI parsing, success/failure formatting and repository invocation. Add `cleanup-views` to `ApiCommand`, `main()` and package scripts while retaining command-aware configuration: invalid arguments are handled before `parseApiRuntimeConfig`, valid execution needs only `DATABASE_URL`, and resources close on every path. Failure output uses only `invalid_arguments`, `configuration_failed` or `cleanup_failed`. Extend the disposable database test with rows immediately before/on the retained boundary, current-day rows and more expired rows than a small batch; prove bounds, order-independent row preservation, convergence, idempotence and two-cleaner safety.
  </action>
  <verify>
    <automated>corepack pnpm --filter @blog-x/contracts typecheck &amp;&amp; corepack pnpm --filter @blog-x/api typecheck &amp;&amp; corepack pnpm local:verify -- --phase11-data</automated>
  </verify>
  <done>The local command has a strict finite input/output contract and repeatably removes only rows outside the latest 400 Shanghai dates in bounded atomic batches.</done>
  <acceptance_criteria>
    - Missing, duplicate, zero, negative, nonnumeric, decimal and greater-than-10000 limits fail before resource creation.
    - The oldest retained date and all newer dates survive; only strictly older rows are eligible.
    - Every invocation reports `deleted <= limit`; repeat-to-zero is idempotent and concurrent invocations preserve exact remaining rows.
    - Command output and errors contain no article, slug, request, visitor, database URL or credential value.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove aggregate recovery and portable-export exclusion</name>
  <reversibility rating="reversible">This extends generated local verification around stable database and portable contracts without changing the backup format or export version.</reversibility>
  <read_first>
    - apps/api/drizzle/0009_article_daily_views.sql
    - apps/api/drizzle/meta/_journal.json
    - apps/api/src/app.ts
    - apps/api/src/content/export-repository.ts
    - apps/api/test/backup-restore.test.ts
    - apps/api/test/distribution-export.test.ts
    - scripts/local-verify.mjs
    - scripts/local-verify.test.mjs
    - scripts/test-inventory.mjs
    - scripts/test-inventory.test.mjs
    - scripts/backup/create.mjs
    - scripts/backup/restore.mjs
  </read_first>
  <files>apps/api/test/backup-restore.test.ts, apps/api/test/distribution-export.test.ts, scripts/local-verify.mjs, scripts/local-verify.test.mjs</files>
  <behavior>
    - Generated source fixtures include multiple aggregate rows and source buckets; after a complete PostgreSQL dump/restore the independently normalized rows are deeply equal in key order and value.
    - Portable export remains format/version 1, reparses through the existing contract, and its serialized value contains no analytics collection, day or counter fields.
    - The Phase 11 verifier accepts only sealed `--phase11-data`, applies all ten migrations, verifies the exact active schema and executes retention, export, backup/restore and privacy suites in runner-owned namespaces.
    - All inventory/migration/schema/backup constants are derived from or updated to the measured Phase 11 authority, and all successful outputs retain `releaseState: 'BLOCKED'`.
  </behavior>
  <action>
Extend the existing restore fixture in `local-verify.mjs` with deterministic `article_daily_views` rows spanning articles, days and multiple source buckets while satisfying the total equality constraint. Capture the source aggregate map before backup in a generated in-memory authority, pass it to the restored comparison test as `BACKUP_RESTORE_EXPECTED_ANALYTICS`, and have `backup-restore.test.ts` independently query/order/normalize restored rows and deep-compare every article ID, ISO day and counter. Keep the expected aggregate authority outside the portable archive and ensure no raw request metadata is introduced into any backup member or diagnostic.

Seed aggregate rows in `distribution-export.test.ts`, retain the exact version-1 manifest reconstruction, and assert its strict top-level/article keys and serialized value exclude analytics table names, view/count/source fields and seeded aggregate values. Also parse the established legacy-compatible v1 shape to prove backward compatibility; do not add an optional analytics field or increment the version.

Add a sealed `--phase11-data` selection/result path to `local-verify.mjs` and focused parser/selection tests. It must use the existing generated namespace, migration, `db:schema:verify`, database-suite, backup-set and isolated-restore authorities; reject caller-selected paths and conflicting phase flags; run nonzero pass-only results; and emit one redacted `blog-x-phase11-data-result` record ending in release BLOCKED. Update exact source-authority constants that still encode nine migrations/nine application tables or pre-Phase-11 schema hashes/counts to the measured ten-migration state. Re-run `assertCompleteTestInventory` and its measured owner-count tests after Plan 11-02; do not hardcode recalled test totals or create a second inventory.
  </action>
  <verify>
    <automated>node --test scripts/local-verify.test.mjs scripts/test-inventory.test.mjs &amp;&amp; corepack pnpm db:generate:check &amp;&amp; corepack pnpm local:verify -- --phase11-data</automated>
  </verify>
  <done>The focused Phase 11 gate proves exact aggregate backup/restore equality and a byte-compatible content-only portable export under the exact ten-migration/test inventory, with a terminal BLOCKED result.</done>
  <acceptance_criteria>
    - Restored aggregate rows deeply equal the independently captured source rows, including each fixed source bucket.
    - Portable export remains version 1 and contains none of the aggregate schema or seeded analytics values.
    - Fresh/repeated migration, active schema verification, retention, export and recovery all execute inside generated local namespaces.
    - The verifier rejects alternate paths, conflicting modes, zero tests, skips, malformed results and stale migration/schema constants.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver the anonymous beacon through the canonical local browser path</name>
  <precondition>Plans 11-01 and 11-02 and Tasks 1-2 are committed on `dev`, pushed to `origin/dev`, and focused Phase 11 data verification passes.</precondition>
  <reversibility rating="reversible">The nonvisual client adapter can be replaced locally while the stable opaque route and aggregate contracts remain unchanged.</reversibility>
  <read_first>
    - apps/web/AGENTS.md
    - apps/web/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - apps/web/app/posts/[slug]/page.tsx
    - apps/web/e2e/public-reading.spec.ts
    - apps/web/next.config.ts
    - scripts/local-delivery-acceptance.mjs
    - scripts/test-inventory.mjs
    - package.json
  </read_first>
  <files>apps/web/app/posts/[slug]/ViewBeacon.tsx, apps/web/app/posts/[slug]/page.tsx, apps/web/e2e/public-reading.spec.ts, scripts/local-verify.mjs, scripts/local-verify.test.mjs</files>
  <behavior>
    - A successfully rendered public detail mounts a nonvisual client component and sends one POST to the relative `/api/public/articles/{encodedSlug}/view` path for that page open.
    - Fetch uses method POST, exact empty JSON, `credentials: 'omit'`, `cache: 'no-store'`, no custom identity/referrer value and no retry or visible error state.
    - React development effect replay and rerender do not send a second request for the same mount; a genuine later page open sends one new request.
    - Draft, unpublished, deleted, future and unknown detail pages never mount the component or emit the beacon.
    - Canonical integration, fixed local refresh and health checks include the changed tests and end with production BLOCKED.
  </behavior>
  <action>
Create the smallest client boundary `ViewBeacon.tsx` and mount it only after `getPublicPost` has returned the strict public detail in the Server Component. Guard the effect with a per-mount ref, encode only the slug in the same-origin relative path, use the exact credential-omitted/no-store empty-JSON fetch contract, swallow network failure without retry or UI, and abort only work that has not completed during unmount. Do not mark the article page itself as a Client Component.

Extend the existing generated main-browser public-reading suite instead of adding a parallel browser owner. Start with an authenticated context so a cookie exists, capture requests/responses for the exact beacon route, and prove one request per successful navigation, no Cookie/Authorization header, exact Origin/method/body, 204/no-store/empty response, no visible element, and zero beacon requests for every unavailable article state. Retain all existing reading, structured-data, responsive and opacity assertions.

Update Phase 11/canonical selection tests only where required so the modified `public-reading.spec.ts`, backup/restore suite, distribution export suite, public visibility suite and Plan 11-02 default suite remain exactly owned by `PACKAGE_TEST_INVENTORY` and are executed once by their established owners. Run the full default gate and canonical integration, then from the clean pushed `dev` commit run the sealed fixed local refresh and health/home probes. If a local cache prerequisite prevents refresh, persist only the redacted local blocker; never contact a server. The terminal delivery decision remains production BLOCKED.
  </action>
  <verify>
    <automated>corepack pnpm test &amp;&amp; corepack pnpm test:integration &amp;&amp; corepack pnpm local:deliver &amp;&amp; curl -fsS http://127.0.0.1:3100/api/health &amp;&amp; curl -fsS -o /dev/null http://127.0.0.1:3100/</automated>
  </verify>
  <done>A public article open delivers exactly one credential-free anonymous beacon through the fixed same-origin runtime, complete local gates pass, and production remains BLOCKED.</done>
  <acceptance_criteria>
    - Browser evidence shows exactly one request on each public article open and none for unavailable pages.
    - The request has no cookie, authorization, persistent identifier or cross-origin destination and the response is invisible and opaque.
    - Default and integration inventories are exact, nonzero and pass-only; backup/restore and portable exclusion are included.
    - The committed `dev` revision is healthy at `http://127.0.0.1:3100`; no server or main branch was touched.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local operator → cleanup CLI | Untrusted arguments can authorize durable aggregate deletion. |
| PostgreSQL source backup → generated restore database | Durable aggregate rows cross backup integrity and isolated restore boundaries. |
| Database → portable export | Analytics must remain excluded from the content portability wire format. |
| Public page → same-origin beacon route | Browser state and cookies must not cross with the anonymous request. |
| Repository tests → canonical local delivery | Exact inventory and result parsing decide only local readiness, never production authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-11 | Tampering | `cleanup-views` | high | mitigate | Parse one finite limit before pool creation; database computes cutoff and deletes an ordered locked batch strictly before the retained boundary. |
| T-11-12 | Denial of Service | retention deletion | high | mitigate | Hard maximum 10000, one bounded statement, deterministic batch order and repeat-to-convergence semantics; no resident worker. |
| T-11-13 | Information Disclosure | portable export and backup evidence | high | mitigate | Keep export v1 content-only; compare aggregates through generated local authority and assert no request/visitor fields enter files or diagnostics. |
| T-11-14 | Tampering | backup/restore counters | high | mitigate | Capture independent source rows and deep-compare every restored key/counter after validated complete-set restore. |
| T-11-15 | Information Disclosure | browser beacon credentials | high | mitigate | Relative same-origin endpoint, `credentials: omit`, empty body contract and browser assertion with an existing session cookie. |
| T-11-16 | Repudiation | local delivery authority | medium | mitigate | Sealed exact inventories and revision-bound results retain production BLOCKED; no local success authorizes deployment. |
</threat_model>

<verification>
Run contract/API/Web typechecks, cleanup boundary/concurrency tests, exact migration generation and active-schema checks, sealed `--phase11-data`, portable export exclusion, complete database backup/restore equality, public-reading browser proof, full default/integration gates, then the committed fixed local refresh and health probes.
</verification>

<success_criteria>
1. Cleanup keeps exactly the latest 400 Shanghai dates and converges through finite idempotent batches.
2. A full database backup/restore preserves every aggregate row exactly.
3. Portable export stays strict backward-compatible version 1 with explicit analytics exclusion.
4. A real public detail open emits one credential-omitted beacon and hidden details emit none.
5. Exact schema, migration, backup and test inventories pass focused and canonical local gates.
6. The pushed `dev` revision is healthy at fixed port 3100 and production remains BLOCKED.
</success_criteria>

## Artifacts this phase produces

- New file `apps/api/src/content/view-retention.ts`; new symbols `parseCleanupViewsArguments`, `runViewRetention`, `formatCleanupViewsResult`, and cleanup limit constant `10000`.
- New aggregate repository method `cleanupExpiredDailyViews(limit)`.
- New CLI command and flag contract `cleanup-views --limit=N`; new package scripts `views:cleanup` at the API and repository roots.
- New analytics contract `viewRetentionResultSchema` and inferred result type.
- New verifier result `blog-x-phase11-data-result` and sealed flag `--phase11-data`; exact migration/schema authority advances to migration count 10 and application table count 10.
- New environment seam `BACKUP_RESTORE_EXPECTED_ANALYTICS` used only inside generated local restore comparison.
- New file `apps/web/app/posts/[slug]/ViewBeacon.tsx`; existing `public-reading.spec.ts` gains anonymous-beacon delivery proof without creating another inventory entry.
- Portable export remains `blog-x-portable-export` version 1 and gains no new field or file.

## Source Coverage Audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Anonymous recoverable PV trend on real published article opens | 11-01, 11-02, 11-03 | COVERED | Route/database tracer, privacy guard, browser delivery and recovery form one complete path. |
| REQ | STAT-01 | Same-origin public-only opaque count | 11-01, 11-02 | COVERED | Plan 11-03 supplies final browser delivery proof without changing ownership. |
| REQ | STAT-02 | Aggregate-only Shanghai-day persistence | 11-01, 11-02 | COVERED | Schema and privacy scans exclude identity/event persistence. |
| REQ | STAT-03 | Atomic concurrent count and bounded transient filtering | 11-01, 11-02 | COVERED | PostgreSQL upsert plus ephemeral policy and focused tests. |
| REQ | STAT-04 | Bounded idempotent 400-day cleanup | 11-03 | COVERED | Exact database cutoff and finite CLI batch. |
| REQ | STAT-06 | Full DB recovery and portable v1 exclusion | 11-03 | COVERED | Independent restored-row equality and strict content-only archive. |
| RESEARCH | — | One aggregate table, shared predicate and PostgreSQL day/upsert | 11-01 | COVERED | No alternate visibility or host clock. |
| RESEARCH | — | Fixed coarse sources, prefetch/crawler filtering and ephemeral limiter | 11-02 | COVERED | Exact host/token sets and capacity behavior are locked. |
| RESEARCH | — | Retention, backup/export, browser and local delivery validation | 11-03 | COVERED | Focused and canonical generated-local evidence. |
| CONTEXT | — | No CONTEXT.md decisions exist for Phase 11 | — | COVERED | Milestone decisions from PROJECT/STATE are enforced as prohibitions and verification gates. |

<output>
Create `.planning/phases/11-privacy-safe-view-authority/11-03-SUMMARY.md` when done.
</output>
