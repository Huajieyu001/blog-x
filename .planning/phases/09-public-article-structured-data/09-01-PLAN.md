---
phase: 09-public-article-structured-data
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/lib/site-metadata.ts
  - apps/web/app/lib/site-metadata.test.ts
  - apps/web/app/posts/[slug]/page.tsx
  - apps/web/e2e/public-discovery-fixture.ts
  - apps/web/e2e/public-discovery.spec.ts
  - apps/web/e2e/public-reading.spec.ts
autonomous: true
requirements:
  - SEO-03
  - SEO-04
  - SEO-05
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SEO-03 boundary: a successfully resolved published article page contains exactly one parseable native `script[type='application/ld+json']` whose exact seven keys are `@context`, `@type`, `headline`, `description`, `datePublished`, `mainEntityOfPage`, and `url`."
    - "SEO-03 parity: `headline`, `description`, and `datePublished` equal the same resolved article's visible `h1`, summary, and `time[datetime]`; `mainEntityOfPage` and `url` both equal the page's same-origin canonical link, including encoded slug semantics."
    - "SEO-04 boundary: the builder accepts only `title`, `summary`, `slug`, and `publishedAt` from an already validated public detail result; JSON-LD never contains Markdown, rendered HTML, SEO-only description, taxonomy, cover/storage data, status, internal origin, filesystem path, or administrative state."
    - "SEO-05 serialization boundary: author-controlled closing-script text, `<`, U+2028, and U+2029 cannot terminate or create markup; the emitted raw text contains the required escapes and `JSON.parse` recovers the original four public values exactly."
    - "SEO-05 reachability boundary: draft, unpublished, soft-deleted, unknown, malformed/non-OK detail, home, search, and every other non-article route contain zero `BlogPosting` JSON-LD scripts."
    - "The fixed local delivery at `http://127.0.0.1:3100` is refreshed once after implementation commits, all automated checks pass, and the terminal production decision remains `BLOCKED`."
  artifacts:
    - path: apps/web/app/lib/site-metadata.ts
      provides: narrow public BlogPosting builder and controlled JSON raw-text serializer
      exports: [buildBlogPosting, serializeJsonLd]
    - path: apps/web/app/posts/[slug]/page.tsx
      provides: server-rendered article-only JSON-LD emission after the valid public-result guard
    - path: apps/web/app/lib/site-metadata.test.ts
      provides: exact-key, canonical, privacy, escaping, and parse-round-trip unit proof
    - path: apps/web/e2e/public-discovery-fixture.ts
      provides: strict hostile-but-public structured-data fixture without widening the public contract
    - path: apps/web/e2e/public-discovery.spec.ts
      provides: isolated SSR/browser parity, injection-containment, privacy, and non-article absence proof
    - path: apps/web/e2e/public-reading.spec.ts
      provides: real publication-lifecycle positive and zero-script coverage
  key_links:
    - from: apps/web/app/posts/[slug]/page.tsx
      to: apps/web/app/lib/site-metadata.ts
      via: four explicit fields passed to buildBlogPosting only after getPublicPost returns `ok`
      pattern: "buildBlogPosting|serializeJsonLd"
    - from: apps/web/app/lib/site-metadata.ts
      to: apps/web/app/lib/site-metadata.ts
      via: canonical post path is resolved through publicUrl rather than headers, string concatenation, or INTERNAL_API_ORIGIN
      pattern: "publicUrl.*encodeURIComponent"
    - from: apps/web/e2e/public-discovery.spec.ts
      to: apps/web/e2e/public-discovery-fixture.ts
      via: generated loopback hostile public detail is parsed through the existing strict contract before SSR
      pattern: "structured-data-hostile|application/ld\\+json"
    - from: apps/web/e2e/public-reading.spec.ts
      to: apps/web/app/posts/[slug]/page.tsx
      via: real published and unavailable lifecycle routes prove the valid-result reachability guard
      pattern: "application/ld\\+json|unavailableBodies"
---

<objective>
Expose one safe, deterministic Schema.org `BlogPosting` record from each genuinely public article page while making every private-data, raw-script, and non-public-route boundary executable.

Purpose: help search engines understand published Blog X articles without creating a second content authority or widening the public projection.
Output: a narrow builder/serializer, article-only SSR emission, focused unit/browser regression coverage, and one final fixed local-delivery proof.
</objective>

<execution_context>
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/workflows/execute-plan.md
@/Users/xanadu/Desktop/ai-coding/blog-x/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@apps/web/AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-public-article-structured-data/09-RESEARCH.md
@.planning/phases/09-public-article-structured-data/09-PATTERNS.md
@apps/web/node_modules/next/dist/docs/01-app/02-guides/json-ld.md
@apps/web/app/lib/site-metadata.ts
@apps/web/app/lib/site-metadata.test.ts
@apps/web/app/lib/api.ts
@apps/web/app/posts/[slug]/page.tsx
@packages/contracts/src/public-posts.ts
@apps/web/e2e/public-discovery-fixture.ts
@apps/web/e2e/public-discovery.spec.ts
@apps/web/e2e/public-reading.spec.ts

<interfaces>
- `getPublicPost(slug): Promise<PublicResult<PublicPostDetail>>` returns `ok`, `not_found`, or `upstream_error`; only `ok` crosses the existing page guard.
- `PublicPostDetail` is a strict public contract but still contains `status`, taxonomy, `seoDescription`, `renderedHtml`, ToC, and cover data, so the JSON-LD builder must accept a narrower four-field input rather than this whole type.
- `publicUrl(path, origin)` is the sole same-origin URL authority and rejects non-root-relative, protocol-relative, backslash, and cross-origin paths.
- `pageMetadata` already emits the article canonical from `/posts/${encodeURIComponent(article.slug)}`; structured URL fields must follow the identical path rule.
- Next.js 16.3 documents native page `<script type="application/ld+json">` markup and escaping `<` before `dangerouslySetInnerHTML`; `next/script`, a client effect, and global layout emission are not this phase's interface.
- Existing test ownership is exact: extend the three listed unit/E2E files and their existing runners; add no test file or inventory owner.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Emit one strict public BlogPosting through the real article SSR path</name>
  <reversibility rating="costly">The exact crawler-visible seven-field shape and its regression contract would require coordinated helper, SSR, and browser-test changes to revise later.</reversibility>
  <files>apps/web/app/lib/site-metadata.ts, apps/web/app/lib/site-metadata.test.ts, apps/web/app/posts/[slug]/page.tsx, apps/web/e2e/public-discovery.spec.ts</files>
  <read_first>
    <item>apps/web/AGENTS.md — mandatory local Next.js documentation rule.</item>
    <item>apps/web/node_modules/next/dist/docs/01-app/02-guides/json-ld.md — native script and `<` escaping guidance for Next.js 16.3.</item>
    <item>apps/web/app/lib/site-metadata.ts — `publicOrigin`, `publicUrl`, `pageMetadata`, and existing URL/metadata ownership.</item>
    <item>apps/web/app/lib/site-metadata.test.ts — current Node test style and same-origin/privacy assertions.</item>
    <item>apps/web/app/lib/api.ts — `PublicResult` and strict `getPublicPost` adapter semantics.</item>
    <item>packages/contracts/src/public-posts.ts — exact strict `PublicPostDetail` fields that must be narrowed.</item>
    <item>apps/web/app/posts/[slug]/page.tsx — valid-detail guard, visible title/summary/time, and canonical route construction.</item>
    <item>apps/web/e2e/public-discovery.spec.ts — generated-loopback article route and named focused-grep conventions.</item>
  </read_first>
  <behavior>
    - "A generated-loopback published article returns HTTP 200 and exactly one parseable native BlogPosting script."
    - "The parsed object has exactly seven approved keys, visible title/summary/time parity, and two URL fields equal to the canonical link."
    - "The helper receives only title, summary, slug, and publishedAt and resolves encoded same-origin URLs through publicUrl."
    - "Unit serialization contains no literal `<`, safely represents U+2028/U+2029, and parses back to the unchanged fixed-shape object."
  </behavior>
  <action>
    Write the focused Node and Playwright assertions first. In `site-metadata.test.ts`, prove exact key order/set, `https://schema.org`, `BlogPosting`, encoded-slug canonical parity, four-field content parity, absence of all excluded field names/markers, hostile closing-script containment, U+2028/U+2029 escaping, and an exact `JSON.parse` round trip. In `public-discovery.spec.ts`, add the exact focused test name `published article emits strict BlogPosting`; use the existing `related-populated` strict article to require one native JSON-LD script, parse its `textContent`, compare its four content facts to `h1`, `.articleSummary`, `article time[datetime]`, and compare both URL facts to the sole canonical link and generated Web origin.

    Then add non-exported `PublicBlogPostingInput` with exactly `title`, `summary`, `slug`, and `publishedAt`, plus exported `buildBlogPosting` and `serializeJsonLd` in `site-metadata.ts`. The builder must return only `@context`, `@type`, `headline`, `description`, `datePublished`, `mainEntityOfPage`, and `url`; both URL values come from one `publicUrl` call over the encoded slug. The serializer must stringify once and escape every `<`, U+2028, and U+2029 without HTML/XML entity encoding. In `PublicArticlePage`, build from four explicit properties after the existing `not_found`/`upstream_error` guards and render one native `application/ld+json` script with the serializer. Do not spread the article object, refetch, inspect request headers, use `seoDescription`, add optional schema properties without approved public sources, or emit from layout/client code.
  </action>
  <acceptance_criteria>
    <criterion>SEO-03 is exact: a valid published article has one parseable `BlogPosting` object with only the seven approved keys; headline, description, and publication time equal visible reader content, and both URLs equal the canonical link.</criterion>
    <criterion>SEO-04 is exact: the builder's input surface is four fields and its output contains no Markdown/body HTML, internal address, storage/cover data, taxonomy, status/admin state, `seoDescription`, guessed author/publisher/image, or any full-object spread.</criterion>
    <criterion>The article script is unreachable until `getPublicPost` has returned `kind === "ok"` and remains a server-rendered native script.</criterion>
    <criterion>Hostile closing-script text plus U+2028/U+2029 survives semantic round trip while the serialized raw text contains no literal `<` and cannot create a sibling element.</criterion>
    <criterion>The focused generated-loopback browser test executes the real Next article page and compares parsed structured data to visible and canonical DOM facts.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm test &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/phase7-browser-verify.mjs --grep "published article emits strict BlogPosting"</automated>
  </verify>
  <done>SEO-03 and SEO-04 have production code plus unit and real SSR/browser proof, with the public seven-field contract and private-field boundary explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove hostile serialization and zero output on every non-public route class</name>
  <files>apps/web/e2e/public-discovery-fixture.ts, apps/web/e2e/public-discovery.spec.ts, apps/web/e2e/public-reading.spec.ts</files>
  <read_first>
    <item>apps/web/e2e/public-discovery-fixture.ts — strict-parsed public detail fixture, finite route controls, and existing related empty response.</item>
    <item>apps/web/e2e/public-discovery.spec.ts — `requireGeneratedOrigin`, `hiddenSentinels`, `expectNoDiscoveryDisclosure`, and article/non-article test seams.</item>
    <item>apps/web/e2e/public-reading.spec.ts — real admin-created published, draft, unpublished, soft-deleted, and unknown article journey.</item>
    <item>apps/web/app/lib/site-metadata.test.ts — Task 1 raw-text and exact-key unit authority to corroborate in the browser.</item>
    <item>apps/web/app/posts/[slug]/page.tsx — Task 1 script position relative to the valid-detail guard.</item>
    <item>scripts/phase7-browser-verify.mjs — exact generated Web/fixture origins, Playwright selection, and cleanup authority.</item>
    <item>scripts/local-delivery-acceptance.mjs — final generated integration plus Phase 7 browser acceptance ownership.</item>
  </read_first>
  <behavior>
    - "A strict public article containing closing-script text and U+2028/U+2029 still yields one inert, parseable script and no injected DOM node or executable sibling script."
    - "Accepted-but-excluded detail fields remain available to the normal page where appropriate but never enter the exact JSON-LD object or its raw script text."
    - "Home and search pages expose zero article JSON-LD scripts."
    - "Real draft, unpublished, soft-deleted, and unknown article URLs return the established 404 body and expose zero article JSON-LD scripts; a finite malformed detail response becomes the existing safe upstream-error outcome and also cannot reach emission."
  </behavior>
  <action>
    Add one finite `structuredDataDetail` for slug `structured-data-hostile` to `public-discovery-fixture.ts`, parsed by `publicPostDetailSchema` before its 200 response, with a title containing a closing-script/injected-element sentinel, a summary containing literal U+2028/U+2029, stable ISO publication time, and distinct accepted-but-excluded markers in `seoDescription`, rendered HTML, taxonomy, and any other already-valid detail fields. Return the existing strict empty-related response for this slug. Also add a separate finite raw response at `/public/articles/structured-data-malformed` that otherwise resembles a detail but deliberately carries extra `markdown`, internal-path, and administrative-state sentinel keys; do not pass this negative fixture through the success parser because its purpose is to prove the Web's strict `publicPostDetailSchema` rejects it. Do not add arbitrary private keys to a successful strict DTO and do not change contracts, API routes, schemas, packages, test inventory, or runner ownership.

    In `public-discovery.spec.ts`, add the exact focused test name `hostile and non-article JSON-LD boundaries`. Parse the sole script raw text and object on the hostile article, assert semantic parity with visible title/summary/time/canonical values, exact seven-key allowlist, no literal raw-text closing boundary, no injected sentinel element or second script, no excluded-field marker inside JSON-LD, and only generated same-origin browser requests. Visit `/`, `/search`, and `/posts/structured-data-malformed` in the same generated Web runtime; require zero article JSON-LD scripts on all three non-emission paths, require the malformed response to use the existing safe upstream-error presentation, and require none of its private sentinels to appear in rendered HTML.

    In `public-reading.spec.ts`, extend the existing real lifecycle journey: on the published desktop page require exactly one parsed object and the same visible/canonical parity; inside the current unavailable-slug loop require zero article JSON-LD before recording each unchanged 404 body. Do not add a second lifecycle fixture or weaken the one-body equality assertion. These real lifecycle assertions are owned by the final generated integration/local-delivery gate, while the hostile and non-article assertions run immediately in the isolated Phase 7 harness.
  </action>
  <acceptance_criteria>
    <criterion>SEO-05 is exact for injection: the hostile strict public article produces one parseable script, zero injected DOM elements/sibling scripts, escaped raw-text boundaries, and values that round-trip without semantic loss.</criterion>
    <criterion>SEO-05 is exact for reachability: `/`, `/search`, the finite malformed detail, and real draft/unpublished/soft-deleted/unknown article routes each contain zero article JSON-LD scripts; the malformed private sentinels do not appear in rendered HTML.</criterion>
    <criterion>The hostile successful fixture remains valid under `publicPostDetailSchema`; private arbitrary keys are not smuggled into a success response, and accepted page fields outside the four-field input remain absent from JSON-LD.</criterion>
    <criterion>The published real lifecycle route repeats the exact seven-key and visible/canonical parity proof, while all existing unavailable routes retain HTTP 404 and one indistinguishable body.</criterion>
    <criterion>No package, lockfile, API, shared contract, database/schema, test inventory, runner owner, server, deployment, `main`, or production-release state changes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase7-browser-verify.mjs --grep "hostile and non-article JSON-LD boundaries" &amp;&amp; corepack pnpm -r typecheck &amp;&amp; node scripts/check-boundaries.mjs</automated>
  </verify>
  <done>SEO-05 has isolated hostile/non-article browser proof and real lifecycle assertions ready for the one final fixed local-delivery run, with every previously unclassified route and serialization edge assigned an executable outcome.</done>
</task>

</tasks>

## Atomic commit sequence

1. Commit the strict builder, serializer, article SSR emission, unit tests, and positive generated-browser tracer as `feat(09-01): emit strict public BlogPosting data`.
2. Commit hostile/non-article and real lifecycle boundary coverage as `test(09-01): seal structured data boundaries`.
3. After both task commits leave a clean worktree, run the phase-level fixed delivery once; commit its revision-bound receipt and `09-01-SUMMARY.md` through the normal GSD handoff without changing production state.

## Artifacts this phase produces

- `apps/web/app/lib/site-metadata.ts`: new non-exported type `PublicBlogPostingInput` and new exact exports `buildBlogPosting` and `serializeJsonLd`.
- `apps/web/app/posts/[slug]/page.tsx`: one native article-only `script[type="application/ld+json"]` built after the existing valid public-result guard.
- `apps/web/app/lib/site-metadata.test.ts`: new exact-shape/canonical/privacy test and raw-text escaping/parse-round-trip test; no new test file.
- `apps/web/e2e/public-discovery-fixture.ts`: new strict `structuredDataDetail` constant/route for slug `structured-data-hostile`, an empty-related response for that slug, and one finite deliberately malformed detail route for fail-closed proof.
- `apps/web/e2e/public-discovery.spec.ts`: new focused tests `published article emits strict BlogPosting` and `hostile and non-article JSON-LD boundaries`.
- `apps/web/e2e/public-reading.spec.ts`: new JSON-LD parity assertions on the existing published route and zero-script assertions inside the existing unavailable-state loop.
- No API, contract, database/schema, package, lockfile, service, deployment, or new file is produced.

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Author-controlled public text -> HTML raw-text script | Title and summary are safe as visible React text but require a distinct serializer before raw script embedding. |
| Strict public detail DTO -> narrower crawler projection | A public-safe page DTO still has more fields than the approved JSON-LD allowlist. |
| Public-result control flow -> emitted article markup | Only an `ok` article result may reach the native script; all other article and non-article paths must emit nothing. |
| Public slug/origin -> canonical crawler URL | URL facts must remain same-origin and identical to the canonical link without trusting request headers or internal API configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Tampering / Elevation of Privilege | `serializeJsonLd` and native script | high | mitigate | Task 1 escapes every raw `<`, U+2028, and U+2029 after one stringify; Task 2 proves one parsed script and no injected sentinel node/sibling script in Chromium. |
| T-09-02 | Information Disclosure | `buildBlogPosting` input/output | high | mitigate | Task 1 accepts four explicit public fields, returns an exact seven-key object, forbids whole-DTO spread, and unit-tests all excluded field classes. |
| T-09-03 | Information Disclosure | article route reachability | high | mitigate | Script construction remains after the `ok` guard; Task 2 asserts zero scripts for malformed/non-OK, draft, unpublished, deleted, unknown, home, and search routes. |
| T-09-04 | Spoofing | canonical `url` and `mainEntityOfPage` | medium | mitigate | Both fields share one `publicUrl` result over the encoded slug and browser tests compare them to the sole canonical link and generated public origin. |
| T-09-05 | Repudiation | crawler-output acceptance evidence | low | accept | Deterministic Node/Playwright assertions and the revision-bound local-delivery receipt provide sufficient local evidence; no external crawler submission or production claim is in scope. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan / task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Search engines obtain safe structured data only from genuine public article pages and it matches reader-visible content. | 09-01 Tasks 1-2 | COVERED | Positive parity plus fail-closed route coverage. |
| REQ | SEO-03 | Published article emits exact parseable `BlogPosting` matching visible facts and canonical URL. | 09-01 Task 1 | COVERED | Exact seven-key shape and generated SSR proof. |
| REQ | SEO-04 | Only four strict public input facts contribute; no Markdown, internals, paths, or admin state leaks. | 09-01 Task 1 | COVERED | Narrow type, no spread, exact output and exclusion tests. |
| REQ | SEO-05 | Non-public/unknown/non-article pages emit nothing and hostile output stays parseable/inert. | 09-01 Task 2 | COVERED | Isolated malicious fixture plus real lifecycle zero-script checks. |
| RESEARCH | — | Web-only server-rendered native script; reuse `publicUrl`; no new dependency, API, schema, service, or test owner. | 09-01 Tasks 1-2 | COVERED | Implementation and prohibitions are explicit. |
| RESEARCH | — | Extend unit, isolated Phase 7, and real main-browser seams; perform fixed local delivery once at closeout. | 09-01 Tasks 1-2 / phase verification | COVERED | Immediate focused gates plus one clean-worktree delivery. |
| CONTEXT | — | No `CONTEXT.md` exists; REQUIREMENTS.md and 09-RESEARCH.md are authoritative. | 09-01 | N/A | No locked or deferred D-XX decision exists to omit or reinterpret. |

<verification>
1. Task 1 must pass `corepack pnpm test`, workspace typecheck, and the named positive generated-browser tracer.
2. Task 2 must pass the named hostile/non-article generated-browser test, workspace typecheck, and repository boundary audit.
3. After both task commits and a clean `dev` worktree, run `corepack pnpm local:deliver` exactly once. It must execute generated integration including `public-reading.spec.ts`, execute the unfiltered isolated Phase 7 browser suite, refresh the fixed `http://127.0.0.1:3100` runtime, write revision-bound local evidence, and end with `RELEASE BLOCKED`.
4. Confirm no cloud host was contacted, no deployment occurred, `main` was untouched, and package/lockfile/API/contracts/database/schema/test inventory are unchanged.
</verification>

<success_criteria>
- SEO-03: the published article's single exact seven-key BlogPosting is parseable and equal to visible/canonical facts in isolated and real browser paths.
- SEO-04: an exact four-field input and seven-field output make every private/internal/full-content field structurally unreachable and regression-tested.
- SEO-05: hostile text cannot escape the script, and every named non-public/non-article/malformed route class has executable zero-script proof.
- Default tests, workspace typecheck, boundary audit, both isolated browser focuses, the unfiltered isolated suite, and final generated integration all pass locally.
- The fixed local page is updated once at phase closeout and production remains `BLOCKED`.
</success_criteria>

<output>
Create `.planning/phases/09-public-article-structured-data/09-01-SUMMARY.md` when done.
</output>
