# 个人博客系统需求分析

> 文档状态：需求分析中  
> 版本：v0.1  
> 更新日期：2026-08-01  
> 本阶段目标：确认产品范围、部署边界与待决策事项，不在本阶段锁定具体技术栈。

## 1. 项目背景

计划建设一套个人博客系统，用于长期发布、管理和归档个人内容。

当前可用基础设施：

| 节点 | 配置 | 域名条件 | 初步职责 |
| --- | --- | --- | --- |
| 对外节点 | 2C2G | 已绑定域名 | 前台站点、HTTPS 入口、反向代理 |
| 内部节点 | 2C4G | 未绑定域名，新增绑定较麻烦 | 后端服务、数据库、搜索/任务等资源型服务 |

已确认约束：

- 博客前台必须部署在 2C2G 节点。
- 所有访客流量通过已绑定域名进入 2C2G 节点。
- 2C4G 节点不依赖公网域名即可参与系统运行。
- 架构需适配低配置服务器，避免为个人博客引入不必要的微服务和高内存组件。

## 2. 产品目标

### 2.1 核心目标

- 为访客提供快速、清晰、适配移动端的阅读体验。
- 为博主提供方便的文章创建、编辑、发布和归档能力。
- 保证文章正文、元数据及图片可备份、可迁移、可恢复。
- 具备基础 SEO、订阅、监控和安全能力。
- 在现有两台服务器上稳定运行，并保留后续扩展空间。

### 2.2 非目标

首个版本暂不以以下能力为目标：

- 多作者协作与复杂角色权限。
- 用户社区、论坛、私信或即时聊天。
- 会员、付费阅读和订单系统。
- 个性化推荐算法。
- Kubernetes、微服务集群或多地域高可用。

## 3. 目标用户与角色

### 3.1 访客

- 浏览首页、文章、分类、标签和归档。
- 在电脑或移动设备上阅读文章。
- 通过搜索、RSS 或搜索引擎发现内容。
- 根据最终决策参与评论或内容分享。

### 3.2 博主/管理员

- 登录后台并管理文章。
- 使用 Markdown 编写和预览内容。
- 管理分类、标签、封面和文章图片。
- 发布、修改、下线或删除文章。
- 查看站点运行状态和基础访问数据。

首期默认仅设置一个管理员账号。

## 4. 建议部署边界

```text
访客 / 管理员
      │
      │ HTTPS：博客域名
      ▼
2C2G 对外节点
├── 前台站点
├── HTTPS 终止与反向代理
├── /api、/uploads 等同域转发
└── 基础访问日志与限流
      │
      │ 私网或加密连接，仅允许指定节点访问
      ▼
2C4G 内部节点
├── 博客后端 API
├── 数据库
├── 定时任务/搜索服务（按需）
└── 内容数据与备份任务
```

### 4.1 网络要求

- 浏览器只访问 2C2G 节点对应的 HTTPS 域名，不直接请求 2C4G 的 IP 和端口。
- 2C2G 节点将同域名下的后端请求转发到 2C4G，因此不要求 2C4G 绑定域名。
- 两台服务器之间优先使用云厂商私网；若没有可用私网，则建立加密隧道或使用受防火墙限制的加密连接。
- 2C4G 的 API 端口只允许来自 2C2G 或内部网络的请求。
- 数据库端口不得直接暴露到公网。
- 对公网原则上只开放 2C2G 的 80/443 端口；SSH 管理端口需限制来源或采取等效保护措施。

### 4.2 节点职责原则

- 2C2G 主要承担静态资源、轻量前台进程和流量入口，避免部署高内存数据库及重型搜索服务。
- 2C4G 主要承担数据持久化和资源消耗较高的后台任务。
- 图片实际存储位置在技术设计阶段确定，但对浏览器应始终通过博客域名提供，避免额外域名和跨域依赖。
- 备份文件跨节点保存时必须加密；后续建议增加服务器之外的第三份备份。

## 5. MVP 功能需求

优先级定义：P0 为首版必须具备，P1 为重要增强，P2 为远期候选。

### 5.1 前台阅读

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| FE-001 | P0 | 首页文章列表 | 展示标题、摘要、发布日期、分类/标签及分页信息 |
| FE-002 | P0 | 文章详情 | 正确渲染 Markdown、图片、链接、引用、表格和代码块 |
| FE-003 | P0 | 文章目录 | 根据标题层级生成目录，并支持定位到对应章节 |
| FE-004 | P0 | 分类与标签 | 访客可查看某分类或标签下的文章列表 |
| FE-005 | P0 | 归档页 | 可按时间浏览已发布文章 |
| FE-006 | P0 | 独立页面 | 至少支持“关于”页面，内容可由管理员维护 |
| FE-007 | P0 | 响应式布局 | 手机、平板和桌面端均可正常阅读与导航 |
| FE-008 | P0 | 主题模式 | 支持浅色/深色显示，记住用户选择或跟随系统 |
| FE-009 | P0 | 错误页面 | 提供友好的 404 和服务异常提示 |
| FE-010 | P1 | 全站搜索 | 可按文章标题、摘要或正文检索已发布内容 |
| FE-011 | P1 | 相关文章 | 在文章末尾按分类或标签展示相关内容 |

### 5.2 内容管理

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| CMS-001 | P0 | 管理员登录 | 未登录用户不能访问受保护的后台功能 |
| CMS-002 | P0 | 文章管理 | 支持新建、编辑、预览、发布、下线和删除文章 |
| CMS-003 | P0 | Markdown 编辑 | 支持常用 Markdown 语法、代码高亮和预览 |
| CMS-004 | P0 | 草稿状态 | 草稿不会出现在公开页面、搜索、RSS 或 Sitemap 中 |
| CMS-005 | P0 | 分类与标签管理 | 支持创建、修改、删除并关联文章 |
| CMS-006 | P0 | 图片上传 | 校验文件类型与大小，上传后可插入文章并公开访问 |
| CMS-007 | P0 | 文章元数据 | 支持标题、摘要、封面、固定链接、发布时间等字段 |
| CMS-008 | P0 | 内容导出 | 可导出 Markdown 原文及必要元数据，降低迁移成本 |
| CMS-009 | P1 | 自动保存 | 编辑过程中定期保存，降低内容丢失风险 |
| CMS-010 | P1 | 定时发布 | 可设置未来时间自动发布文章 |
| CMS-011 | P1 | 操作日志 | 记录重要的登录、发布、删除和配置变更操作 |

### 5.3 SEO 与内容分发

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| SEO-001 | P0 | 页面元数据 | 每篇文章具有独立标题、描述和规范链接 |
| SEO-002 | P0 | Sitemap | 只收录允许公开访问的页面和已发布文章 |
| SEO-003 | P0 | robots.txt | 可配置搜索引擎抓取规则 |
| SEO-004 | P0 | RSS/Atom | 订阅源包含最新已发布文章和永久链接 |
| SEO-005 | P0 | 分享卡片 | 提供 Open Graph 等基础分享元数据 |
| SEO-006 | P1 | 结构化数据 | 为文章补充适当的结构化信息 |

### 5.4 互动与统计

以下内容尚未确认，不作为首版 P0：

| 编号 | 优先级 | 候选需求 | 备注 |
| --- | --- | --- | --- |
| INT-001 | 待定 | 评论 | 可选无评论、第三方评论或自建评论；自建将增加审核和反垃圾需求 |
| INT-002 | P1 | 阅读量 | 需定义去重规则，并避免写请求影响文章读取性能 |
| INT-003 | P1 | 匿名访问统计 | 优先采用隐私友好的聚合统计，不采集非必要个人信息 |
| INT-004 | P2 | 邮件订阅 | 涉及邮件服务、退订、隐私与反垃圾规则，后续单独分析 |

## 6. 非功能需求

### 6.1 性能与资源

- 公开页面应优先使用缓存、预渲染或等效的轻量方案，降低 2C2G 节点压力。
- 图片需限制上传大小并支持压缩、缩略图或现代图片格式。
- 正常流量下，两台节点均应保留足够内存用于系统和突发请求，不以长期耗尽交换分区作为正常运行方式。
- 静态资源应设置合理缓存策略；文章更新后能够正确刷新缓存。
- 性能指标和预期访问量在获得实际内容规模后进一步量化。

### 6.2 可用性与故障处理

- 服务进程异常退出后能够自动恢复。
- 2C4G 暂时不可用时，2C2G 应展示明确的降级或错误页面；是否要求“已有文章继续可读”将在渲染方式确定时决策。
- 监控至少覆盖服务存活、CPU、内存、磁盘空间和 HTTPS 证书有效期。
- 严格控制日志大小并设置轮转，防止磁盘被写满。

### 6.3 安全

- 全站强制 HTTPS，HTTP 自动跳转 HTTPS。
- 密码采用安全哈希存储，禁止明文保存。
- 后台登录具备失败次数限制、限流或等效防护。
- 所有输入需进行服务端校验，并防范 XSS、SQL 注入、CSRF 和恶意文件上传。
- 上传文件不可作为服务端可执行代码运行。
- 密钥、数据库密码等敏感配置不得提交到代码仓库。
- 系统和依赖应有可执行的安全更新流程。

### 6.4 备份与恢复

- 数据库至少每日自动备份。
- 文章图片、附件、站点配置需纳入备份范围。
- 备份应跨节点保存，跨节点副本必须加密。
- 首期建议至少保留 7 个每日备份和 4 个每周备份，最终周期待确认。
- 必须定期进行恢复演练；“存在备份文件”不等同于“可以成功恢复”。
- 后续建议增加不位于两台云服务器上的第三份备份副本。

### 6.5 可维护性

- 部署步骤可重复执行，并具备明确的升级和回滚流程。
- 开发、测试和生产配置相互隔离。
- 关键配置通过环境变量或安全配置文件管理。
- 数据结构变更必须提供迁移方案。
- 系统应有最小化的运维说明，包括部署、备份、恢复和常见故障处理。

## 7. 数据对象草案

首期核心数据对象：

- 文章：标题、固定链接、摘要、正文、封面、状态、发布时间、更新时间、SEO 信息。
- 分类：名称、固定链接、描述、排序。
- 标签：名称、固定链接。
- 页面：标题、固定链接、正文、状态。
- 媒体：文件信息、访问地址、类型、大小、上传时间、引用关系。
- 管理员：账号、密码哈希、状态、最后登录信息。
- 站点配置：站点名称、描述、Logo、导航、社交链接等。

评论、订阅用户和访问明细只有在对应功能确认后才进入正式数据模型。

## 8. 关键验收场景

MVP 至少满足以下端到端场景：

1. 管理员通过博客域名进入后台，登录并创建一篇包含图片和代码块的 Markdown 草稿。
2. 管理员预览草稿，发布后文章出现在首页、分类、标签、归档、RSS 和 Sitemap 中。
3. 访客通过手机和桌面浏览器使用 HTTPS 访问文章，页面内容、目录、图片和代码高亮显示正常。
4. 浏览器的 API 和图片请求均通过博客域名完成，不要求访问 2C4G 的公网地址。
5. 未授权用户无法调用写入接口，2C4G 的数据库无法从公网直接访问。
6. 管理员下线文章后，公开列表、搜索、RSS 和 Sitemap 不再展示该文章。
7. 系统能够从备份恢复文章正文、元数据和图片，并验证恢复后的内容可访问。

## 9. 待确认事项

以下问题会影响下一阶段的技术方案和工作量：

1. **内容方向**：以技术文章为主，还是包含摄影、视频或大文件？
2. **写作方式**：只在网页后台写作，还是同时支持本地 Markdown + Git 发布？
3. **前台渲染**：是否要求 2C4G 故障时，已发布文章仍能在 2C2G 上继续阅读？该要求将影响静态生成、缓存和发布流程。
4. **评论策略**：不开放、使用第三方评论，还是自建评论？
5. **图片规模**：预计图片数量、单图大小和月度新增量是多少？
6. **隐私内容**：是否需要私密文章、密码访问或登录后可见？
7. **统计要求**：只需要基础阅读量，还是需要来源、设备和地区等访问分析？
8. **服务器网络**：两台云服务器是否属于同一云厂商/地域，是否具有可用私网？
9. **系统环境**：两台服务器的操作系统、已占用端口和现有服务是什么？
10. **恢复目标**：最多可接受丢失多少时间的数据，以及最长可接受多久恢复服务？

## 10. 下一阶段输出

待第 9 节关键问题确认后，进入技术方案设计阶段，预期输出：

- 总体架构与请求链路。
- 前后端技术栈选型及取舍。
- 数据模型和 API 边界。
- 图片存储、发布与缓存方案。
- 两台服务器的安全网络连接方案。
- 部署、监控、备份与恢复方案。
- MVP 开发任务拆分和验收清单。
